/**
 * سياسة فترة الراحة البيولوجية — **مصدر واحد يقرأه الخادم والموبايل معًا**
 * (القرار #153، والقرار 197).
 *
 * **والمدة سياسة بثلاثة مستويات لا رقمٌ إلزامي واحد:**
 * 1. **سياسة المستأجر** — `tenants.min_rest_days`، يحددها المالك لكل مزارعه.
 * 2. **مستوى المزرعة** — `farms.rest_days`، يعدّلها المشرف **صعودًا فقط**.
 * 3. **الدورة الواحدة** — `house_prep_cycles.rest_target_days`، تمديد لهذه
 *    المرة وحدها ولا يغيّر السياسة.
 *
 * **والتمديد سهل والتقصير صعب:** التقصير عن الحد الأدنى **للطبيب أو المالك
 * وبسبب مكتوب** — ولو تساوى الاتجاهان **قصّرت الراحة عند أول ضغط تشغيلي،
 * والخسارة تظهر بعد دفعتين فلا تُربط بسببها**.
 */

/**
 * **الحدّ الأدنى المطلق — ثلاثة أيام. لا ينزل أحد تحته بأي حال**، لا المالك
 * ولا الطبيب.
 *
 * **ثابت في الكود لا صفٌّ في جدول** (القرار #153 حرفيًّا: «رقم مثبَّت في النظام
 * لا إعداد قابل للتحرير»): **صفٌّ يُحرَّر، والحدّ الأدنى المطلق لا يُحرَّر** —
 * وإعداد بلا أرضية يمكن ضبطه على يوم واحد.
 *
 * **ومكرَّر عمدًا في قيود `CHECK` بالقاعدة** (`tenants.min_rest_days` ·
 * `farms.rest_days` · `house_prep_cycles.rest_target_days`): القاعدة لا
 * تستورد TypeScript، **والحارس الإجرائي وحده يُعيد الثقبَ أي مسار كتابة جديد
 * لا يمرّ به**. فالرقم واحد في موضعين لأن الطبقتين لا تتحدثان لغة واحدة —
 * **وتغييره يلزمه تغييرهما معًا، وهو المقصود: لا يُغيَّر بسهولة.**
 */
export const ABSOLUTE_MIN_REST_DAYS = 3;

/** رسالة الرفض العربية — نفس النص في تحقّق الخادم وفي الشاشة. */
export const REST_DAYS_BELOW_ABSOLUTE_MIN_MESSAGE = `لا تقل فترة الراحة عن ${String(ABSOLUTE_MIN_REST_DAYS)} أيام بأي حال`;
