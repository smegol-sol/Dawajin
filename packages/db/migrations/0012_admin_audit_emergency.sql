-- مفتاح الطوارئ — الطبقة الثانية من الاسترداد (القرار 187، والقرار 196).
--
-- `actor_id` يبقى كما هو: `NOT NULL` ومفتاحًا إلى `platform_admins`. وصفّ
-- الطوارئ يحمل فيه **معرّف المدير الذي أُعيد تعيينه — هدف الإجراء**، والاسم
-- النصّي أدناه يقول **من نفّذه**. فالصفّ يجيب «ماذا جرى لمن، وبيد من» بلا
-- إضعاف القيد لأجل حالة واحدة.
ALTER TABLE "admin_audit_log" ADD COLUMN "is_emergency" boolean DEFAULT false NOT NULL;--> statement-breakpoint
-- فارغ في كل صفّ عدا مسار الطوارئ — والوسم أعلاه هو ما يصنّف الصفّ، لا امتلاء
-- هذا العمود: حقلٌ فارغ قد يفرغ لخلل، والوسم يقول ما جرى ولا يُحسب من غياب غيره.
ALTER TABLE "admin_audit_log" ADD COLUMN "emergency_operator" varchar(128);
