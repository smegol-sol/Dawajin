# CLAUDE.md

توجيه لأي عمل داخل هذا المستودع — بشري أو Claude Code.

## المراجع الملزمة

اثنتان تحكمان كل قرار في هذا المشروع، وأي تعارض بينهما وبين هذا الملف يُحسم لصالحهما:

- **`docs/app-complete-spec.md`** — الوثيقة الشاملة: ماذا يرى المستخدم (الشاشات، نظام التصميم، القواعد السلوكية).
- **`docs/backend-technical-spec.md`** (والنسخة الأصلية `docs/backend-technical-spec.pdf` عند أي شك) — المواصفة التقنية: ماذا يحدث خلف الشاشة (البنية، قاعدة البيانات، المنطق، الـ API).
- **`docs/STATE.md`** — أين وصلنا الآن وما ينتظر ماذا. يُقرأ أولًا، وهو فهرس لا مرجع ملزم.

اقرأهما قبل أي تعديل جوهري. الخطة التنفيذية المرحلية في `docs/work-plan.md`.

## المبادئ المعمارية السبعة

(التفصيل الكامل والمبرر في `docs/backend-technical-spec.md` §1 و`docs/decisions.md`)

1. **الفرض المركزي** — أي قيد أمني يُفرض في طبقة واحدة، لا باستدعاء يدوي متكرر.
2. **المعاملة والقفل** — كل عملية متعددة الجداول في معاملة، مع إعادة قراءة الحرّاس تحتها.
3. **دفتر حركة** — لا عمود رصيد يُحدَّث؛ الرصيد = مجموع الحركات دائمًا.
4. **السجل غير قابل للتعديل** — لا `UPDATE` على سجل ميداني؛ التصحيح بسجل جديد مرتبط.
5. **لا يُمنع الميدان بسبب الإدارة** — إعداد ناقص أو رصيد غير كافٍ يُنبّه ولا يمنع.
6. **الوجود ثم التعيين** — غير موجود ← 404، موجود غير مُسند ← 403 (بهذا الترتيب دائمًا).
7. **عزل مطلق** — `tenant_id` من JWT حصريًا، فلترة مركزية.

## بيئة التطوير المحلية

الأوامر التي تلمس قاعدة بيانات (`test:integration`، `test:coverage`، `check:all`، `migrate`، `dev`)
تقرأ `DATABASE_URL`/`TEST_DATABASE_URL` من `process.env` مباشرة — لا تحميل `dotenv` داخل كود
الخادم نفسه عمدًا (`apps/api/src/lib/env.ts`)، لأن الإنتاج يحقن المتغيّرات من المنصة لا من ملف
(نفس منطق حارس `localhost` في الإنتاج). هذا يعني أن أي طرفية محلية (أو جلسة Claude Code) يجب أن
تُصدِّر هذه المتغيّرات بنفسها قبل تشغيل تلك الأوامر — هذا ليس نقصًا في الإعداد، بل نتيجة مباشرة
لعدم وجود تحميل تلقائي في الكود.

```bash
cp .env.example .env   # مرة واحدة — القيم الافتراضية تطابق دور/قاعدتَي dawajin_dev وdawajin_test
                        # المحليتين إن كانتا مُنشأتين مسبقًا بنفس بيانات .env.example
set -a && source .env && set +a   # في كل جلسة/طرفية جديدة قبل أي أمر يلمس القاعدة
```

**وقاعدة `dawajin_test` تحتاج تجهيزًا صريحًا قبل أول `check:all` على جهاز نظيف** — الترحيلات ثم
جدول العلامة، بـ`DATABASE_URL` موجَّهًا إليها لا إلى قاعدة التطوير (نفس ما يفعله CI في خطوتين
منفصلتين). بلا ذلك يرفض حارس `testGuard` التشغيل بـ«جدول العلامة `__test_marker__` غير موجود»،
فتسقط اختبارات التكامل، **فتفشل بوابة التغطية لسبب بيئي بحت لا كودي**:

```bash
DATABASE_URL="$TEST_DATABASE_URL" NODE_ENV=test pnpm --filter @dawajin/db run migrate
DATABASE_URL="$TEST_DATABASE_URL" NODE_ENV=test pnpm --filter @dawajin/db run setup:test-db
```

تأكيدات تخطيط RTL (`check:all` الفحص الأخير) تحتاج متصفحًا مطابقًا لنسخة Playwright. في بيئة
تحمل نسخة مثبَّتة مسبقًا لا تطابقها (فتفشل بـ`Executable doesn't exist`)، صدّر المسار الموجود بدل
`playwright install` (القرار #103):

```bash
# حاوية لينكس
export PLAYWRIGHT_CHROMIUM_EXECUTABLE=/opt/pw-browsers/chromium-1194/chrome-linux/chrome

# macOS — المسار مختلف تمامًا
export PLAYWRIGHT_CHROMIUM_EXECUTABLE="$HOME/Library/Caches/ms-playwright/chromium_headless_shell-1208/chrome-headless-shell-mac-arm64/chrome-headless-shell"
```

**والرقم في اسم المجلد يتبع نسخة Playwright فيتغيّر مع كل ترقية — فالقاعدة «صدّر المسار الموجود
عندك» لا نسخ رقم من هنا.** اقرأه من الجهاز نفسه:

```bash
ls ~/Library/Caches/ms-playwright/     # macOS
ls /opt/pw-browsers/                   # حاوية لينكس
```

**ورسالة الفشل تسمّي النسخة المتوقَّعة لا الموجودة** (`chromium_headless_shell-1234` مثلًا بينما
المثبَّت `1208`) — فالفارق بين الرقمين هو العرَض، والعلاج تصدير الموجود لا مطاردة المتوقَّع.

`.env` مُستبعَد بـ`.gitignore` (`.env` و`.env.*`) ولا يُلتزم أبدًا — القرار #67 في `docs/decisions.md`
دقّق كل تاريخ المستودع للتأكد من عدم تسرّب سرّ حقيقي واحد.

## الأوامر الأساسية

```bash
pnpm migrate          # تطبيق الترحيلات (خطوة نشر صريحة — لا تُستدعى تلقائيًا أبدًا)
pnpm seed:demo        # بيانات تجريبية عبر الـ API حصريًا، بحارس بيئة (production تُرفض)
pnpm test             # اختبارات الوحدة لكل الحزم (لا اتصال قاعدة بيانات)
pnpm test:integration # اختبارات التكامل — قاعدة اختبار منفصلة، متسلسلة
pnpm check:all        # كل الفحوص الآلية (typecheck·ESLint·Prettier·تغطية·...) — تفشل البناء، لا تحذيرات
pnpm typecheck        # tsc --noEmit لكل الحزم
pnpm dev              # تشغيل خادم الـ API محليًا

# داخل apps/api أو packages/shared:
npx eslint apps/api/src packages/db/src packages/shared/src --fix   # strict-type-checked + قواعد المشروع (القرار #61)
npx prettier --write apps/api/src packages/db/src packages/shared/src
pnpm --filter @dawajin/api run test:coverage       # تغطية apps/api (≥80% عام، 100% لطبقة الصلاحيات/الدفتر — القرار #63)
pnpm --filter @dawajin/shared run test:coverage    # تغطية packages/shared (100% لتطبيع الجوال)
```

## قاعدة المفتاح الأجنبي — ملزمة بلا استثناء

**كل مفتاح أجنبي بين جدولين يحملان `tenant_id` يجب أن يكون مركَّبًا:**
`(fk_column, tenant_id) → target(id, tenant_id)`.

المفتاح المفرد يتحقق من **وجود** الصف المُشار إليه لا من **مالكه**، فيسمح بصف
مستأجر يشير إلى صف مستأجر آخر. أُثبت على القاعدة مرتين قبل الإصلاح (مزرعة داخل
موقع مستأجر آخر، وعنبر داخل مزرعة مستأجر آخر) — كلاهما قُبل صامتًا.

- كل جدول يُشار إليه يحتاج `UNIQUE (id, tenant_id)` — Postgres يشترط مرجعًا
  فريدًا صريحًا ولو كان `id` مفتاحًا أساسيًا.
- **ولا استثناء قائمًا اليوم — والتاريخ يبقى مكتوبًا:** كان `admin_audit_log.actor_id`
  **الاستثناء الوحيد** لأن الفاعل مدير منصة و`users.tenant_id` له `NULL`،
  فالمفتاح المركَّب يرفض كل صف مشروع. **وزال بالقرار 194:** مدير المنصة صار في
  `platform_admins` — **جدول بلا `tenant_id` إطلاقًا** — فالمرجع اليوم
  `actor_id → platform_admins(id)`، **ولا مركَّب يُطلب فلا استثناء يُستثنى**.
  أي استثناء جديد يحتاج مبرّرًا مكتوبًا بنفس الوضوح.
- **لا تعتمد على حارس طبقة الخدمة وحده**: هو إجرائي، يُعيد الثقبَ أي مسار كتابة
  جديد لا يمرّ به. يبقى لأنه يعطي `404` مفهومة بدل خطأ مفتاح خام — لا كبديل.

**ويفرضها فحصٌ آليّ منذ القرار 206 — «المفتاح المركَّب» في `pnpm check:all`:** يقرأ
`pg_constraint` **من قاعدة الاختبار المهجَّرة** لا من المخطط (فيقيس ما هو مطبَّق لا ما هو
مكتوب)، **ويُسقط البناء** عند أي مفتاح مفرد بين جدولين يحملان `tenant_id`، **وعند مفتاح
مركَّب من جدولٍ عمودُه قابل للعدم** (`MATCH SIMPLE` يُرضيه العدم). **والاستثناءات قائمة موجبة
بعلّتها في `scripts/checks/composite-fk.ts` — جدولٌ جديد لا يُستثنى بالسكوت.**

**فهذه القاعدة مفروضة لا موصى بها.** وقبل الفحص **مرّت تحتها أربع مخالفات**، **وسُمّيت ثلاثة
منها في تقرير PR #69 ثم مرّت دفعتان فوقها** — والعلّة أن مخالفتها لم تكلّف شيئًا (القرار 206).

التفصيل في `docs/decisions.md` القرارات #120 و#122 و205 و206.

## قاعدة الإسناد — وعطبها المؤجَّل المعلن

**الإسناد يقيّد القراءة كما يقيّد الكتابة** (القرار #126). مربٍّ يفتح بيانات عنبر غير مُسند له
اطّلاع على ما ليس له، سواء كتب فيه أم لا. الفرض في `enforceEntityAccess` وحده، ويُركَّب في
`app.ts` **مرتين**: عامًّا (لـ`query`+`body`) وبنمط مسار لكل مسار يحمل معرّف كيان في الرابط —
Express لا يملأ `req.params` في middleware مركَّب بلا نمط. أي مسار جديد بمعرّف في الرابط يُضاف
إلى `ENTITY_ID_PATH_PATTERNS`، ولا يُنثَر الفرض داخل الموجّهات.

**والإسناد بمستويين — لا مستوى واحد (القرار #128):**

- **المربّي يُسند بالعنبر** (`user_assignments.house_id`)
- **المشرف والطبيب يُسندان بالمزرعة** (`user_assignments.farm_id`) — مسؤولان عن **بعض**
  مزارع المستأجر يُسندها إليهما المالك تصديقًا ميدانيًا. صفٌّ واحد بـ`farm_id` يفتح كل عنابر
  تلك المزرعة ولا يفتح شيئًا خارجها.

**وصفٌّ واحد يحمل مستوى واحدًا حتمًا** — `CHECK ((house_id IS NULL) <> (farm_id IS NULL))`.
**والتفرّد بفهرسين جزئيين لا بفهرس واحد:** فهرس فريد عادي على عمود يقبل `NULL` يعامل كل `NULL`
كقيمة مميّزة فيقبل تكرار الإسناد صامتًا — أُثبت على القاعدة (ثلاثة صفوف متطابقة قُبلت).

`ASSIGNMENT_SCOPED_ROLES` في `apps/api/src/lib/entityScope.ts` **قائمة موجبة لا شرط
سالب**: دور جديد لا يحصل على تجاوز صامت — يبقى خارج القيد حتى يُدرَج فيها بقرار مكتوب.

**وقاعدة السرد — تسري على كل مسار سرد قادم بلا استثناء (القرار #129):**

> **أي مسار يُرجع قائمة كيانات يُفلتر بالإسناد افتراضيًا.** المربّي يرى عنابره المُسندة له
> وحدها، والمشرف والطبيب عنابر مزارعهم المُسندة إليهم، والمالك كل شيء داخل مستأجره.
> **وما لا يخصّ المستخدم غائب تمامًا من الرد — لا اسمًا ولا معرّفًا**، لا معروضًا مُعطَّلًا.

**والسرد يحتاج طبقتين لا واحدة، وحذف أيٍّ منهما يترك ثقبًا مختلفًا:**

1. **نمط مسار في `ENTITY_ID_PATH_PATTERNS`** — يقرّر «هل يصل هذه المزرعة أصلًا»، ويرفض بـ403.
2. **فلترة في طبقة الخدمة** (`assignedHousesFilter`) — تقرّر «ماذا يرى داخلها».

**و403 لا قائمة فارغة** لمزرعة لا يبلغها إسناده: الفارغة تقول «لا عنابر هنا» وهي كذبة عن
مزرعة مليئة بعنابر ليست له — تُقرأ نفيَ وجود لا نفيَ صلاحية.

**وقاعدة الرؤية تختلف بالدور ولا تُوحَّد (القرار #131):**

| الدور           | المواقع             | المزارع        | العنابر              |
| --------------- | ------------------- | -------------- | -------------------- |
| المالك          | كلها                | كلها           | كلها                 |
| المشرف · الطبيب | ما فيه مزرعة مُسندة | المُسندة إليه  | كل عنابر تلك المزارع |
| المربّي         | ما فيه عنبر مُسند   | الحاوية لعنبره | المُسندة إليه        |

توحيدها في شرط واحد بـ`OR` يبدو أنظف **ويغيّر المعنى**: يصير إسناد عنبر واحد كافيًا ليرى المشرف
مزرعة لم تُسند إليه. القاعدة قرار مالك لا اختصار هندسي.

**والقرار 210 يقيّد هذا الجدول ولا ينقضه — مقرَّرٌ ولم يُنفَّذ بعد** (§7-ب البند 60، بعد المرحلة 3):
**الصفة تصير على صفّ الإسناد لا على الحساب**، **فتُقرأ قواعد الرؤية لكل مزرعة على حدة لا للشخص
كله**، ومزارعُه المرئية **اتحادُ ما تفتحه صفوفه وكلُّ مزرعة بصفتها**. **والشرط يبقى لكل صفّ لا
لكل شخص** — **فما حذّر منه #131 أعلاه (توحيدٌ بـ`OR` يوسّع صامتًا) لا يقع**: هذا **يجمع مخرجات
شروطٍ لم تتغيّر**، لا يخلط الشروط نفسها. **و`users.role` يبقى الدور الأساسي** لشاشة الدخول
وقائمة التنقّل. **ولا يُقرأ الجدول أعلاه منقوضًا: هو الحاكم حتى يُبنى 210.**

**وكل عدّاد يُحسب تحت الفلتر نفسه وفي نفس الاستعلام** — عدّاد يعدّ ما لا يراه المستخدم **تسريب
يُعامَل كخطأ لا كتحسين**: «موقعك فيه خمس مزارع» لمن يرى واحدة يكشف ما حُجب عنه.

نطاق الإسناد كلّه في ملف واحد: `apps/api/src/lib/entityScope.ts`.

**العزل بين المستأجرين غير متأثر بهذا إطلاقًا** — يفرضه فلتر `tenant_id` في الخدمة والمفتاح
المركَّب في القاعدة.

## قاعدة فصل القرارات عن الفروع

**فرع الميزة يحمل قرارات تلك الميزة فقط.** أي قرار **سياق أو نطاق** — قرار مالك يحدّ التصميم
ولا ينبع من الكود المكتوب — يذهب إلى **فرع وثائق مستقل**.

السبب: خلطهما يجعل مراجعة الميزة مراجعةً لقرارات لا علاقة لها بها، ويجعل التراجع عن الميزة
تراجعًا عن قرارات مالك. وقع فعلًا في PR #15 (القرارات #135–#139 دُفعت على فرع الشاشة)،
وتُرك كما هو لأن فكّه بعد الدفع أكلف مما يفيد. القرار #140.

## قاعدة البوابة قبل الالتزام — في نفس الأمر لا في أمر سابق

**لا التزام ولا دفع في هذا المستودع إلا وبوابة `check:all` خضراء، والربط بينهما `&&` في نفس
الأمر:**

```bash
pnpm check:all && git add -A && git commit -F - <<'EOF'
...
EOF
```

**فصلهما في سطرين يجعل الالتزام يمرّ رغم الحمرة** — وقع فعلًا (القرار #145). السبب والحادثة هناك.

## قاعدة `git stash` — ممنوعة بلا استثناء

**لا `git stash` إطلاقًا في هذا المستودع.** أي مقارنة بشجرة نظيفة تكون في **worktree منفصل**
(`git worktree add`). السبب والحادثة في القرار #141.

## قاعدة ملف الحالة

**`docs/STATE.md` يُقرأ أولًا في كل جلسة جديدة**، ويُحدَّث في **نفس الـPR** الذي يغيّر حالته:
دفعة تُدمج · دفعة تبدأ · سؤال يُطرح على المالك أو يُحسم.

**وهو فهرس حالة لا سجل**: كل بند يشير إلى موضعه في `docs/work-plan.md` أو `docs/decisions.md`
ولا يعيد شرحه. **وخمسة أقسام لا سادس لها** — ما زاد عليها يذهب إلى الملفين، فملفٌ يطول
يصير سجلًّا ثانيًا يتباعد عن الأول.

**ولا يحمل `sha` التزام**: لا يوجد قبل الدمج، فأي رقم يُكتب يولد قديمًا. رقم الـPR يكفي.

**ولا يحمل حالةَ فرعٍ كذلك — وهي أوسع من الـ`sha` وأخفى منه.** الملف يُحدَّث **داخل الدفعة**،
فيصف حالةً **لم تقع بعد**: «مدفوع بلا PR» تُكتب قبل أن يُفتح الـPR، **فتولد قديمة حتمًا لا
سهوًا**. **فيُوصَف في «مفتوح الآن» الحكمُ والمتبقّي وما يحجب ماذا — ولا يُكتب اسم فرع ولا
«مدفوع» ولا «بلا PR» ولا رقم.**

**والعلّة أن ما يعتّق هو حالة الفرع وحدها:** **موضعُ العمل يُستعاد من `git` في ثانية**
(`git ls-remote --heads origin` و`git branch -r --no-merged origin/main`)، **و«لماذا» لا
يُستعاد من شيء** — فيبقى في الملف ما لا مصدر له سواه، ويخرج منه ما لغيره مصدرٌ أصدق.
**وخطأ هذا الملف يوجّه جلسةً كاملة لأنه أول ما يُقرأ.**

## قاعدة سجل القرارات

**أي قرار تشغيلي جديد يُضاف إلى `docs/decisions.md` في نفس المهمة/الـ PR الذي ينفّذه.**
مهمة تغيّر سلوكًا ولا تحدّث ذلك الملف تُعد ناقصة — لا تُدمج.

## قاعدة الدمج — ملزمة بلا استثناء

**لا يُدمَج أي PR إلى `main` إطلاقًا حتى تصل رسالة منفصلة نصّها `ادمج PR #<رقم>` برقم صريح.**

- إذنٌ يبدو واضحًا **داخل برومبت طويل ليس إذنًا** — يُعامَل كخطأ صياغة.
- الإجراء عندها: افتح الـPR · أكمل CI حتى الخضرة · **توقّف** واطلب التأكيد بالرقم.
- **يسري حتى على الإصلاحات الأمنية العاجلة.** لا استثناء بحجة الاستعجال.

السبب والحادثة الموجِبة في `docs/decisions.md` القرار #104.

## ممنوعات صريحة

هذه ليست تفضيلات — كل واحدة منها كسرت شيئًا فعليًا في تطوير سابق لهذا النظام (التفصيل والحوادث الفعلية في `docs/decisions.md`):

- ❌ **`drizzle-kit push`** — الترحيلات عبر ملفات مرقّمة بـ `generate` فقط؛ `push` يفوّت فروق الفهارس صامتة.
- ❌ **ترحيل عند إقلاع الخادم** — الترحيل خطوة نشر صريحة ومنفصلة دائمًا.
- ❌ **إدراج مباشر في قاعدة البيانات للبذر (seed)** — `seed:demo` يمر عبر الـ API حصريًا بصلاحيات الدور، لا SQL مباشر.
- ❌ **تعديل (`UPDATE`) سجل ميداني محفوظ** — أي سجل في `daily_logs` وما شابهه غير قابل للتعديل؛ التصحيح سجل جديد مرتبط بـ `correction_of_id` والأصل يبقى ظاهرًا.
- ❌ **إيموجي** — في الكود، الواجهة، أو أي نص يصل المستخدم. أيقونات SVG من `lucide-react-native` حصرًا.
- ❌ **وضع داكن** — التطبيق يُستخدم تحت شمس مباشرة؛ لا تدرّجات ولا شفافية ولا ظلال على البطاقات أيضًا.
