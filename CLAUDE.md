# CLAUDE.md

توجيه لأي عمل داخل هذا المستودع — بشري أو Claude Code.

## المراجع الملزمة

اثنتان تحكمان كل قرار في هذا المشروع، وأي تعارض بينهما وبين هذا الملف يُحسم لصالحهما:

- **`docs/app-complete-spec.md`** — الوثيقة الشاملة: ماذا يرى المستخدم (الشاشات، نظام التصميم، القواعد السلوكية).
- **`docs/backend-technical-spec.md`** (والنسخة الأصلية `docs/backend-technical-spec.pdf` عند أي شك) — المواصفة التقنية: ماذا يحدث خلف الشاشة (البنية، قاعدة البيانات، المنطق، الـ API).

اقرأهما قبل أي تعديل جوهري. الخطة التنفيذية المرحلية في `docs/work-plan.md`.

## المبادئ المعمارية السبعة

(التفصيل الكامل والمبرر في `docs/backend-technical-spec.md` §1 و`docs/decisions.md`)

1. **الفرض المركزي** — أي قيد أمني يُفرض في طبقة واحدة، لا باستدعاء يدوي متكرر.
2. **المعاملة والقفل** — كل عملية متعددة الجداول في معاملة، مع إعادة قراءة الحرّاس تحتها.
3. **دفتر حركة** — لا عمود رصيد يُحدَّث؛ الرصيد = مجموع الحركات دائمًا.
4. **السجل غير قابل للتعديل** — لا `UPDATE` على سجل ميداني؛ التصحيح بسجل جديد مرتبط.
5. **لا يُمنع الميدان بسبب الإدارة** — إعداد ناقص أو رصيد غير كافٍ يُنبّه ولا يمنع.
6. **الوجود ثم التعيين** — غير موجود ← 404، موجود غير مُسند ← 403 (بهذا الترتيب دائمًا).
7. **عزل مطلق** — `tenant_id` من JWT حصريًا، فلترة مركزية.

## بيئة التطوير المحلية

الأوامر التي تلمس قاعدة بيانات (`test:integration`، `test:coverage`، `check:all`، `migrate`، `dev`)
تقرأ `DATABASE_URL`/`TEST_DATABASE_URL` من `process.env` مباشرة — لا تحميل `dotenv` داخل كود
الخادم نفسه عمدًا (`apps/api/src/lib/env.ts`)، لأن الإنتاج يحقن المتغيّرات من المنصة لا من ملف
(نفس منطق حارس `localhost` في الإنتاج). هذا يعني أن أي طرفية محلية (أو جلسة Claude Code) يجب أن
تُصدِّر هذه المتغيّرات بنفسها قبل تشغيل تلك الأوامر — هذا ليس نقصًا في الإعداد، بل نتيجة مباشرة
لعدم وجود تحميل تلقائي في الكود.

```bash
cp .env.example .env   # مرة واحدة — القيم الافتراضية تطابق دور/قاعدتَي dawajin_dev وdawajin_test
                        # المحليتين إن كانتا مُنشأتين مسبقًا بنفس بيانات .env.example
set -a && source .env && set +a   # في كل جلسة/طرفية جديدة قبل أي أمر يلمس القاعدة
```

تأكيدات تخطيط RTL (`check:all` الفحص الحادي عشر) تحتاج متصفحًا مطابقًا لنسخة Playwright. في حاوية
تحمل نسخة مثبَّتة مسبقًا لا تطابقها (فتفشل بـ`Executable doesn't exist`)، صدّر المسار الموجود بدل
`playwright install` (القرار #103):

```bash
export PLAYWRIGHT_CHROMIUM_EXECUTABLE=/opt/pw-browsers/chromium-1194/chrome-linux/chrome
```

`.env` مُستبعَد بـ`.gitignore` (`.env` و`.env.*`) ولا يُلتزم أبدًا — القرار #67 في `docs/decisions.md`
دقّق كل تاريخ المستودع للتأكد من عدم تسرّب سرّ حقيقي واحد.

## الأوامر الأساسية

```bash
pnpm migrate          # تطبيق الترحيلات (خطوة نشر صريحة — لا تُستدعى تلقائيًا أبدًا)
pnpm seed:demo        # بيانات تجريبية عبر الـ API حصريًا، بحارس بيئة (production تُرفض)
pnpm test             # اختبارات الوحدة لكل الحزم (لا اتصال قاعدة بيانات)
pnpm test:integration # اختبارات التكامل — قاعدة اختبار منفصلة، متسلسلة
pnpm check:all        # كل الفحوص الآلية (typecheck·ESLint·Prettier·تغطية·...) — تفشل البناء، لا تحذيرات
pnpm typecheck        # tsc --noEmit لكل الحزم
pnpm dev              # تشغيل خادم الـ API محليًا

# داخل apps/api أو packages/shared:
npx eslint apps/api/src packages/db/src packages/shared/src --fix   # strict-type-checked + قواعد المشروع (القرار #61)
npx prettier --write apps/api/src packages/db/src packages/shared/src
pnpm --filter @dawajin/api run test:coverage       # تغطية apps/api (≥80% عام، 100% لطبقة الصلاحيات/الدفتر — القرار #63)
pnpm --filter @dawajin/shared run test:coverage    # تغطية packages/shared (100% لتطبيع الجوال)
```

## قاعدة المفتاح الأجنبي — ملزمة بلا استثناء

**كل مفتاح أجنبي بين جدولين يحملان `tenant_id` يجب أن يكون مركَّبًا:**
`(fk_column, tenant_id) → target(id, tenant_id)`.

المفتاح المفرد يتحقق من **وجود** الصف المُشار إليه لا من **مالكه**، فيسمح بصف
مستأجر يشير إلى صف مستأجر آخر. أُثبت على القاعدة مرتين قبل الإصلاح (مزرعة داخل
موقع مستأجر آخر، وعنبر داخل مزرعة مستأجر آخر) — كلاهما قُبل صامتًا.

- كل جدول يُشار إليه يحتاج `UNIQUE (id, tenant_id)` — Postgres يشترط مرجعًا
  فريدًا صريحًا ولو كان `id` مفتاحًا أساسيًا.
- **الاستثناء الوحيد القائم:** `admin_audit_log.actor_id` — الفاعل مدير منصة
  و`tenant_id` له `NULL`، فالمفتاح المركَّب يرفض كل صف مشروع. أي استثناء جديد
  يحتاج مبرّرًا مكتوبًا بنفس الوضوح.
- **لا تعتمد على حارس طبقة الخدمة وحده**: هو إجرائي، يُعيد الثقبَ أي مسار كتابة
  جديد لا يمرّ به. يبقى لأنه يعطي `404` مفهومة بدل خطأ مفتاح خام — لا كبديل.

التفصيل في `docs/decisions.md` القرارات #120 و#122.

## قاعدة الإسناد — وعطبها المؤجَّل المعلن

**الإسناد يقيّد القراءة كما يقيّد الكتابة** (القرار #126). مربٍّ يفتح بيانات عنبر غير مُسند له
اطّلاع على ما ليس له، سواء كتب فيه أم لا. الفرض في `enforceEntityAccess` وحده، ويُركَّب في
`app.ts` **مرتين**: عامًّا (لـ`query`+`body`) وبنمط مسار لكل مسار يحمل معرّف كيان في الرابط —
Express لا يملأ `req.params` في middleware مركَّب بلا نمط. أي مسار جديد بمعرّف في الرابط يُضاف
إلى `ENTITY_ID_PATH_PATTERNS`، ولا يُنثَر الفرض داخل الموجّهات.

**وعطب مؤجَّل معروف يجب أن يعرفه كل من يقرأ هذا الملف:**

> **المشرف والطبيب يُسندان بالمزرعة لا بالعنبر (تصديق ميداني من المالك). جدول
> `user_assignments` لا يدعم ذلك، فنطاقهما مفتوح مؤقتًا على كل عنابر المستأجر — وهو تجاوز
> معروف يُصلَح قبل بناء شاشة المشرف (المرحلة 3).**

**هذا ليس قرارًا بأن نطاقهما مفتوح بطبيعة الدور — بل نقص في النموذج.** `ASSIGNMENT_SCOPED_ROLES`
في `apps/api/src/middleware/entityAccess.ts` تحمل `farmer` وحدها لهذا السبب. لا تُوسَّع القائمة قبل
أن يستوعب النموذج إسنادًا بالمزرعة. التفصيل في `docs/work-plan.md` §7-ب البند 19.

**العزل بين المستأجرين غير متأثر بهذا إطلاقًا** — يفرضه فلتر `tenant_id` في الخدمة والمفتاح
المركَّب في القاعدة.

## قاعدة سجل القرارات

**أي قرار تشغيلي جديد يُضاف إلى `docs/decisions.md` في نفس المهمة/الـ PR الذي ينفّذه.**
مهمة تغيّر سلوكًا ولا تحدّث ذلك الملف تُعد ناقصة — لا تُدمج.

## قاعدة الدمج — ملزمة بلا استثناء

**لا يُدمَج أي PR إلى `main` إطلاقًا حتى تصل رسالة منفصلة نصّها `ادمج PR #<رقم>` برقم صريح.**

- إذنٌ يبدو واضحًا **داخل برومبت طويل ليس إذنًا** — يُعامَل كخطأ صياغة.
- الإجراء عندها: افتح الـPR · أكمل CI حتى الخضرة · **توقّف** واطلب التأكيد بالرقم.
- **يسري حتى على الإصلاحات الأمنية العاجلة.** لا استثناء بحجة الاستعجال.

السبب والحادثة الموجِبة في `docs/decisions.md` القرار #104.

## ممنوعات صريحة

هذه ليست تفضيلات — كل واحدة منها كسرت شيئًا فعليًا في تطوير سابق لهذا النظام (التفصيل والحوادث الفعلية في `docs/decisions.md`):

- ❌ **`drizzle-kit push`** — الترحيلات عبر ملفات مرقّمة بـ `generate` فقط؛ `push` يفوّت فروق الفهارس صامتة.
- ❌ **ترحيل عند إقلاع الخادم** — الترحيل خطوة نشر صريحة ومنفصلة دائمًا.
- ❌ **إدراج مباشر في قاعدة البيانات للبذر (seed)** — `seed:demo` يمر عبر الـ API حصريًا بصلاحيات الدور، لا SQL مباشر.
- ❌ **تعديل (`UPDATE`) سجل ميداني محفوظ** — أي سجل في `daily_logs` وما شابهه غير قابل للتعديل؛ التصحيح سجل جديد مرتبط بـ `correction_of_id` والأصل يبقى ظاهرًا.
- ❌ **إيموجي** — في الكود، الواجهة، أو أي نص يصل المستخدم. أيقونات SVG من `lucide-react-native` حصرًا.
- ❌ **وضع داكن** — التطبيق يُستخدم تحت شمس مباشرة؛ لا تدرّجات ولا شفافية ولا ظلال على البطاقات أيضًا.
