# CLAUDE.md

توجيه لأي عمل داخل هذا المستودع — بشري أو Claude Code.

## المراجع الملزمة

اثنتان تحكمان كل قرار في هذا المشروع، وأي تعارض بينهما وبين هذا الملف يُحسم لصالحهما:

- **`docs/app-complete-spec.md`** — الوثيقة الشاملة: ماذا يرى المستخدم (الشاشات، نظام التصميم، القواعد السلوكية).
- **`docs/backend-technical-spec.md`** (والنسخة الأصلية `docs/backend-technical-spec.pdf` عند أي شك) — المواصفة التقنية: ماذا يحدث خلف الشاشة (البنية، قاعدة البيانات، المنطق، الـ API).

اقرأهما قبل أي تعديل جوهري. الخطة التنفيذية المرحلية في `docs/work-plan.md`.

## المبادئ المعمارية السبعة

(التفصيل الكامل والمبرر في `docs/backend-technical-spec.md` §1 و`docs/decisions.md`)

1. **الفرض المركزي** — أي قيد أمني يُفرض في طبقة واحدة، لا باستدعاء يدوي متكرر.
2. **المعاملة والقفل** — كل عملية متعددة الجداول في معاملة، مع إعادة قراءة الحرّاس تحتها.
3. **دفتر حركة** — لا عمود رصيد يُحدَّث؛ الرصيد = مجموع الحركات دائمًا.
4. **السجل غير قابل للتعديل** — لا `UPDATE` على سجل ميداني؛ التصحيح بسجل جديد مرتبط.
5. **لا يُمنع الميدان بسبب الإدارة** — إعداد ناقص أو رصيد غير كافٍ يُنبّه ولا يمنع.
6. **الوجود ثم التعيين** — غير موجود ← 404، موجود غير مُسند ← 403 (بهذا الترتيب دائمًا).
7. **عزل مطلق** — `tenant_id` من JWT حصريًا، فلترة مركزية.

## الأوامر الأساسية

```bash
pnpm migrate          # تطبيق الترحيلات (خطوة نشر صريحة — لا تُستدعى تلقائيًا أبدًا)
pnpm seed:demo        # بيانات تجريبية عبر الـ API حصريًا، بحارس بيئة (production تُرفض)
pnpm test             # اختبارات الوحدة لكل الحزم (لا اتصال قاعدة بيانات)
pnpm test:integration # اختبارات التكامل — قاعدة اختبار منفصلة، متسلسلة
pnpm check:all        # كل الفحوص الآلية (typecheck·ESLint·Prettier·تغطية·...) — تفشل البناء، لا تحذيرات
pnpm typecheck        # tsc --noEmit لكل الحزم
pnpm dev              # تشغيل خادم الـ API محليًا

# داخل apps/api أو packages/shared:
npx eslint apps/api/src packages/db/src packages/shared/src --fix   # strict-type-checked + قواعد المشروع (القرار #61)
npx prettier --write apps/api/src packages/db/src packages/shared/src
pnpm --filter @dawajin/api run test:coverage       # تغطية apps/api (≥80% عام، 100% لطبقة الصلاحيات/الدفتر — القرار #63)
pnpm --filter @dawajin/shared run test:coverage    # تغطية packages/shared (100% لتطبيع الجوال)
```

## قاعدة سجل القرارات

**أي قرار تشغيلي جديد يُضاف إلى `docs/decisions.md` في نفس المهمة/الـ PR الذي ينفّذه.**
مهمة تغيّر سلوكًا ولا تحدّث ذلك الملف تُعد ناقصة — لا تُدمج.

## ممنوعات صريحة

هذه ليست تفضيلات — كل واحدة منها كسرت شيئًا فعليًا في تطوير سابق لهذا النظام (التفصيل والحوادث الفعلية في `docs/decisions.md`):

- ❌ **`drizzle-kit push`** — الترحيلات عبر ملفات مرقّمة بـ `generate` فقط؛ `push` يفوّت فروق الفهارس صامتة.
- ❌ **ترحيل عند إقلاع الخادم** — الترحيل خطوة نشر صريحة ومنفصلة دائمًا.
- ❌ **إدراج مباشر في قاعدة البيانات للبذر (seed)** — `seed:demo` يمر عبر الـ API حصريًا بصلاحيات الدور، لا SQL مباشر.
- ❌ **تعديل (`UPDATE`) سجل ميداني محفوظ** — أي سجل في `daily_logs` وما شابهه غير قابل للتعديل؛ التصحيح سجل جديد مرتبط بـ `correction_of_id` والأصل يبقى ظاهرًا.
- ❌ **إيموجي** — في الكود، الواجهة، أو أي نص يصل المستخدم. أيقونات SVG من `lucide-react-native` حصرًا.
- ❌ **وضع داكن** — التطبيق يُستخدم تحت شمس مباشرة؛ لا تدرّجات ولا شفافية ولا ظلال على البطاقات أيضًا.
