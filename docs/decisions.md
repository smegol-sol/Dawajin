# سجل القرارات ومبرراتها

> **نسخة حيّة من الملحق أ في `backend-technical-spec.pdf` (القسم 26 وما بعده).**
> **القاعدة الدائمة:** أي قرار تشغيلي جديد يُضاف إلى هذا الملحق في نفس المهمة/الـ PR الذي ينفّذه.
> **المهمة التي تغيّر سلوكًا ولا تحدّث هذه الوثيقة تُعد ناقصة — لا تُدمج.**
>
> لا تُلغَ قيدًا هنا قبل قراءة سببه. أغلب القيود نتجت عن أخطاء وقعت فعلًا في تطوير سابق لنفس النظام (مؤشّرة أدناه بـ «حادثة فعلية»).

---

## المبادئ المعمارية السبعة (القسم 1 من المواصفة التقنية)

سبعة قيود بنيوية حاكمة، كل واحد نتج عن خطأ وقع فعلًا. أي كود جديد يُراجَع مقابلها:

| # | المبدأ | القاعدة |
|---|---|---|
| 1 | الفرض المركزي | أي قيد أمني يُفرض في طبقة واحدة (middleware مركزي)، لا باستدعاء يدوي في كل نقطة |
| 2 | المعاملة والقفل | كل عملية متعددة الجداول في معاملة (transaction)، مع إعادة قراءة الحرّاس (guards) داخلها تحت القفل |
| 3 | دفتر حركة | لا عمود رصيد يُحدَّث — الرصيد = مجموع الحركات (ledger, لا `balance` column) |
| 4 | السجل غير قابل للتعديل | لا `UPDATE` على سجل ميداني — التصحيح بسجل جديد مرتبط |
| 5 | لا يُمنع الميدان بسبب الإدارة | إعداد ناقص أو رصيد غير كافٍ يُنبّه ولا يمنع |
| 6 | الوجود ثم التعيين | غير موجود ← 404 · موجود غير مُسند ← 403 (الترتيب إلزامي) |
| 7 | عزل مطلق | `tenant_id` من JWT حصريًا · فلترة مركزية |

---

## أ.1 — سلامة البيانات والتوثيق

**1. الاستلام الأعمى**
الكمية المرسلة مخفية حتى يُدخل المربي عدّه.
لماذا: عرضها يجعله يضغط «تأكيد» بلا عدّ.
التنازل يكسر: تبويب الاستلام كله يصبح توقيعًا شكليًا.

**2. رمز التسليم**
أربعة أرقام يمليها الناقل لحظة التسليم.
لماذا: التوقيع يثبت الموافقة لا الحضور.
التنازل يكسر: يمكن تأكيد استلام لم يحدث.

**3. حد التسامح بالوحدة لا بالنسبة**
لماذا: 0.5% من 3 أكياس = 0.015 كيس، أي تسامح صفري فعليًا.
التنازل يكسر: كل شحنة صغيرة تفتح نزاعًا كاذبًا.

**4. السجل غير قابل للتعديل**
ملاحظة → مراجعة → سجل تصحيح مرتبط، والأصل يبقى مشطوبًا وظاهرًا.
لماذا: سند قابل للتعديل بأثر رجعي ليس سندًا.
التنازل يكسر: قيمة السجلات كدليل، وأرصدة المخزون المرتبطة بها.

**5. لا مهلة تصحيح تلقائي**
كل ادعاء يمر بالمشرف، مع تصعيد للمالك بعد 48 ساعة.
لماذا: المهلة تخلق مسارين بسلوك مختلف؛ والتصعيد لازم لأن المشرف نقطة اختناق وحيدة.

**6. المشرف لا ينشئ تصحيحًا من تلقاء نفسه**
لماذا: حماية المربي من تغيير أرقامه دون علمه.
التنازل يكسر: بيانات الميدان تصبح قابلة للتعديل الصامت.

**7. النزاع يحسمه المالك لا المشرف**
لماذا: المشرف هو المُرسِل، فقد يكون طرفًا في النزاع.

**8. لا تنازل عن الرصيد أثناء النزاع**
المادة تبقى محسوبة بالكمية المعدودة في كل الحالات.
لماذا: التغذية لا تتوقف بانتظار قرار إداري.

---

## أ.2 — إدخال البيانات والميدان

**9. النموذج تدفق رأسي واحد**
ممنوع تقسيمه لخطوات.
لماذا: يُملأ مرة يوميًا بتسلسل واحد، والتقسيم يزيد النماذج الناقصة.

**10. لا تعبئة تلقائية ولا نسخ**
لا من يوم سابق، ولا من عنبر مجاور، ولا قيمة أمس كافتراضية.
لماذا: يحوّل التسجيل لضغط أزرار بلا عدّ.
التنازل يكسر: كل البيانات، وهي تبدو سليمة.

**11. لا يُمنع الميدان بسبب الإدارة**
لماذا: المربي في العنبر لا يستطيع الاتصال بالإدارة.
حادثة فعلية: إعداد ناقص جمّد شاشة السجل اليومي كليًا.

**12. لا يُمنع الهالك ولا العلاج بنقص الرصيد**
لماذا: العلاج والتغذية أولى من دقة السجل، والسجل يُصحَّح بالجرد لاحقًا.

**13. لا زر يفشل عند الضغط**
المعطّل يحمل سببه في نصه.
لماذا: الزر الذي يبدو فعّالًا ويرفض بعد الضغط يجعل المستخدم يظن النظام معطّلًا.

---

## أ.3 — المخزون والدفتر

**14. دفتر حركة لا حقل كمية**
لماذا: الحقل المحدَّث ينحرف عند أول خطأ أو طلب متزامن، ولا يمكن تتبّع مصدر الانحراف.

**15. مستويا مخزون**
الشحنة سحب من رصيد معلوم لا إنشاء من العدم.
لماذا: بلا مصدر، يستطيع المشرف إرسال أي كمية بلا سقف يحاسبه.
التنازل يكسر: تتبّع الفاقد من طرفه الأهم.

**16. فصل الهالك عن فرق الجرد**
نوعان مستقلان وعمودان منفصلان في التقرير.
لماذا: الهالك خسارة معروفة السبب، وفرق الجرد نقص مجهول قد يكون سرقة — والثاني وحده يستحق التحقيق.
حادثة فعلية: غياب مسار الهالك جعله يُسجَّل كتسوية جرد فأفسد التقرير.

**17. أنواع حركة مخصصة لا نوع عام**
لماذا: `movement_type` يقرأه المستخدم في كشف الحركة، والعمليات لها ضوابط مختلفة (الشحنة لها استلام أعمى وحد تسامح ونزاع، التحويل لا).

**18. التمييز بين الفرق العشوائي والمنحاز**
لماذا: الحادثة الواحدة ليست دليلًا، والنمط هو الدليل.
التنازل يكسر: اتهامات على أساس ضجيج قياس.

**19. التقريب للعرض فقط**
لماذا: جمع أرقام مقرَّبة ينتج فرقًا بين المجموع وحاصل الجمع.
التنازل يكسر: ثابت الدفتر بفروق يصعب تفسيرها.

---

## أ.4 — الصلاحيات والبنية

**20. الفرض المركزي لا اليدوي**
لماذا: القيد اليدوي يُنسى في المسار الرابع دائمًا.
حادثة فعلية: اكتُشف نفس نوع الثغرة ثلاث مرات (الشحنات · التحويل · المنتجات)، وفي كل مرة أغلقه الحل المركزي وحده.

**21. المعاملة مع إعادة قراءة الحرّاس تحت القفل**
لماذا: اللّف وحده يعطي أمانًا زائفًا — الكتابات ذرّية والقرار على حالة قديمة (TOCTOU).
حادثة فعلية: خطوتا تجهيز متزامنتان منعتا انتقال العنبر لفترة الراحة إلى الأبد بلا خطأ ظاهر.

**22. الوجود أولًا ثم التعيين**
لماذا: الرد بـ403 قبل التحقق من الوجود يسرّب أن المعرّف موجود في مستأجر آخر.

**23. رقم الجوال معرِّفًا، فريدًا داخل المستأجر، شاملًا المعطَّلين**
لماذا: المربي يحفظ رقمه بلا خطأ إملائي · شمول المعطَّلين يمنع «أوقف الحساب وأنشئ آخر بنفس الرقم» · التفرّد داخل المستأجر يسمح للطبيب المستقل بخدمة عدة ملّاك.

**24. إسنادات تراكمية داخل المزرعة**
لماذا: المربي المسؤول عن عنبرين متجاورين حالة شائعة، وهي سبب وجود مبدّل العنبر أصلًا.

**25. التصفية قرار نهائي**
لا إعادة فتح إلا بمسار صريح للمالك بسبب مسجَّل.
لماذا: إعادة الفتح الصامتة تُدخل سجلات بعد التصفية فتفسد التقرير النهائي وأرصدة الدورة الجديدة.

**26. الترحيلات عبر Drizzle Kit كخطوة صريحة**
لماذا: `push` يفوّت فروق الفهارس صامتة.
حادثة فعلية: أنتج فهرسًا غير جزئي كان سيكسر تصحيحات السجل اليومي.

**27. البيانات التجريبية عبر الـ API**
لماذا: الإدراج المباشر يملأ الجداول ويترك الأخطاء مختبئة.
حادثة فعلية: البناء عبر الـ API كشف تسعة أخطاء في جولة واحدة، منها ستة أعمدة بلا أي مسار يكتبها.

**28. الاختبار يفشل قبل الإصلاح وينجح بعده**
لماذا: اختبار يمر بالصدفة لا يحمي شيئًا.
حادثة فعلية: 32 اختبارًا خضراء بينما ست ثغرات جسيمة قائمة.

**29. الفصل بين سجلات التدقيق الثلاثة**
لماذا: خلط عمليات المالك بسجل مدير المنصة يصعّب التتبع ويعرض بيانات في شاشة خاطئة.

---

## أ.5 — العرض والتنبيه

**30. المقارنة بالمعيار بجانب كل مؤشر**
لماذا: «نفوق 3%» لا تعني شيئًا حتى تعرف أن معيار اليوم 21 هو 1.5%.
التنازل يكسر: التطبيق يجمع بيانات ولا يعيدها كقرار.

**31. أيام التغطية هي الرقم البطل**
لماذا: الكمية معلومة، وأيام التغطية قرار.

**32. لا شرطة صامتة لمؤشر غير محسوب**
لماذا: تجعل المستخدم يظن أن القيمة صفر لا أن الإدخال مفقود.

**33. الفصل بين «يحتاج تدخلك» والجرس**
الأول حالة محسوبة تختفي بالفعل · الثاني حدث مؤرَّخ يختفي بالقراءة.
لماذا: تكرار العنصر في الاثنين يفقد المستخدم الثقة في كليهما.

**34. لا تنبيه على عنبر بلا دفعة نشطة**
لماذا: يعلّم المستخدم تجاهل التنبيهات — أسوأ ما يصيب نظام إنذار.

**35. العاجل قائمة مغلقة**
لماذا: عشرون إشعارًا يوميًا تُنتج تجاهلًا كاملًا خلال أسبوع.

**36. التصعيد يكشف الإهمال**
لماذا: الإشعار الذي لا يستجيب له أحد أخطر من غيابه — يوهم بأن الأمر معالَج.

**37. اللون يعكس الحالة لا نوع المؤشر**
لماذا: المؤشر الجيد يجب أن يطمئن لا أن ينذر (نفوق 0.11% أخضر).

**38. الشارة للحالات المتغيرة لا للخصائص الثابتة**
لماذا: الشارة تعني «انتبه».
حادثة فعلية: «مغلق» (نوع التهوية) قُرئت على أنها عنبر متوقف.

**39. اسم الصنف يحمل هويته الثابتة فقط**
لا نسب متغيرة (حُذفت نسبة بروتين العلف من الاسم).
لماذا: الخاصية المتغيرة في الاسم تعني صنفًا جديدًا مع كل تغيير فتتشظى الأرصدة.
استثناء: تركيز الدواء (20% مقابل 10%) جزء من هويته الصيدلانية ويؤثر في حساب الجرعة.

**40. لا وضع داكن ولا تدرّجات ولا شفافية**
لماذا: التطبيق يُستخدم تحت شمس مباشرة، والشاشة الداكنة تصبح مرآة.

**41. الشارة = لون + أيقونة + نص**
لماذا: عمى الألوان شائع ولن يخبرك أحد أنه لا يميّز الأخضر عن الأحمر.

**42. استثناء أحجام النص**
13px للشارات و12px لتسميات التبويبات فقط · الحد الأدنى 15px لكل نص محتوى.

---

## سجل القرارات الإضافية (تُضاف أثناء التنفيذ)

> يُضاف هنا أي قرار تشغيلي جديد يُتّخذ أثناء البناء ولم يرد في المواصفة الأصلية، بنفس الصيغة: **القرار · لماذا · ما الذي يكسره التنازل عنه**. لا تُدمج مهمة تغيّر سلوكًا دون تحديث هذا القسم.

**43. شاشة الصحة الفرعية لدى المشرف — عرض فقط**
مصفوفة الصلاحيات (§12.2) تمنع المشرف من إنشاء مهمة صحية أو تنفيذها (❌ في الصفّين)، بينما الوثيقة الشاملة تذكر شاشة فرعية باسم "الصحة" ضمن شاشات المشرف بلا تحديد نطاقها.
القرار: تُبنى كشاشة **مراقبة/عرض فقط** (حالة الدفعات الصحية وبلاغاتها) بلا أي زر كتابة.
لماذا: الالتزام الحرفي بمصفوفة الصلاحيات — أي زر كتابة هنا سيفشل عند الضغط (403)، وهذا يخالف قاعدة "لا زر يفشل عند الضغط" (القرار #13).

**44. مصدر الرقم الكلي في "اليوم X من Y" (عمر الدفعة)**
الوثيقة الشاملة تطلب إعدادًا متغيّرًا لا رقمًا ثابتًا، دون تحديد مصدره.
القرار: `Y = MAX(day) FROM breed_standards WHERE breed = batch.breed` — آخر يوم معرّف لسلالة الدفعة (غالبًا 45)، لا حقل منفصل بإعدادات المستأجر.
لماذا: يتّسق تلقائيًا مع أي سلالة جديدة تُضاف لاحقًا بلا حاجة لحقل إعداد إضافي يُنسى تحديثه.

**45. الاسم المؤقت للتطبيق**
معلّق صراحة في الوثيقة الشاملة (§13-7).
القرار: استخدام "دواجن" (مشتق من اسم المستودع Dawajin) في `app.json` والمراجع المؤقتة، إلى حين حسم اسم تجاري نهائي.
لماذا: يحتاج `app.json` وبنية EAS اسمًا من اليوم الأول؛ التغيير لاحقًا يتطلب إعادة بناء EAS على أي حال فلا خسارة إضافية من البدء باسم مؤقت واضح المصدر.

**46. تحويل المواصفة التقنية إلى Markdown**
تم إنجاز `docs/backend-technical-spec.md` كنسخة مراجَعة يدويًا من `docs/backend-technical-spec.pdf`، لإصلاح التباس ترتيب الأرقام الناتج عن استخراج النص الآلي من PDF باتجاه RTL. الملف الجديد هو مصدر العمل اليومي للفريق؛ الـ PDF الأصلي يبقى مرجعًا حرفيًا عند أي شك.

**47. فهرس جزئي إضافي لمعايير السلالة العالمية**
النص الحرفي في §9 يذكر `UNIQUE (tenant_id, breed, day)` فقط لجدول `breed_standards`. لكن PostgreSQL يعامل قيم `NULL` كمتمايزة في قيد UNIQUE عادي، فهذا القيد وحده **لا يمنع** تكرار صفوف عالمية (`tenant_id IS NULL`) لنفس السلالة واليوم — وهي بالضبط الصفوف الأهم لأنها الافتراضية لكل المستأجرين.
القرار: أُضيف فهرس فريد جزئي ثانٍ `breed_standards_global_breed_day_uq ON (breed, day) WHERE tenant_id IS NULL`، بنفس نمط `users_platform_phone_unique` الموثّق أصلًا في §9 لنفس السبب بالضبط.
لماذا: بلا هذا الفهرس، خطأ برمجي بسيط في `seed:demo` أو مهمة تعبئة مستقبلية يمكن أن يُدخل صفّي معايير متضاربين لنفس (السلالة، اليوم) العالميين بصمت، فتصبح "المقارنة بالمعيار" (القرار #30) غامضة أو خاطئة لكل المستأجرين دفعة واحدة.
التنازل يكسر: سلامة `breed_standards` كمرجع وحيد — نفس فئة الخطأ التي بررت وجود `users_platform_phone_unique` أصلًا.

**48. zod الإصدار 4 بدل 3**
المواصفة التقنية §2.2/§2.3 تذكر `zod 3+` دون تثبيت إصدار رئيسي صريح.
القرار: اعتماد `zod@^4` موحّدًا في `packages/shared` و`apps/api` و`apps/mobile` معًا.
لماذا: `@hookform/resolvers` (مطلوبة أصلًا في الملحق ب لربط `react-hook-form` بمخططات zod) يدعم `^3.25.0 || ^4.0.0` في أحدث إصداراته — واعتماد الأحدث منذ البداية في مشروع جديد بلا كود قديم مرتبط بـ zod 3 يتجنّب ترقية لاحقة مكلفة.
التنازل يكسر: لا شيء إن استُخدم zod 3 بدلًا منه — القرار تفضيل هندسي لا اضطرار، مسجَّل هنا فقط التزامًا بقاعدة "كل قرار تشغيلي جديد يُوثَّق".

**49. توحيد بنية سجلات التدقيق الثلاثة**
لم تحدد المواصفة التقنية أي أعمدة لـ`entity_audit_log`/`settings_audit_log`/`admin_audit_log` (§7.7 يذكر الجدول والنطاق فقط). التصميم الأول (Phase 0) استخدم ثلاثة أشكال مختلفة (زوج `entity_type`+`entity_id` في الأول، `setting_key` وحيد في الثاني، `target_tenant_id` مخصص في الثالث)، وافتقر الثلاثة لعمود `reason` في اثنين منها ولعمود `request_id` في كليهما.
القرار: توحيد الثلاثة على نفس الأعمدة الاثني عشر بالضبط: `id · uuid · tenant_id (nullable في admin_audit_log فقط) · actor_id · entity_type · entity_id (نصي) · action · before · after · reason · request_id · created_at`.
لماذا: فصل الجداول الثلاثة سببه عزل الجمهور (لا يختلط تدقيق المالك بسجل مدير المنصة — القرار #29) لا اختلاف البيانات المطلوب تسجيلها؛ لا سبب هندسي وجيه للاختلاف. `entity_id` نصي عمدًا (لا صحيح) ليحمل مفتاح إعداد نصيًا في `settings_audit_log` أو معرّفًا رقميًا في الآخرين بلا تمييز — وأي منهما لم يكن قابلًا لـ FK حقيقي أصلًا (تعدّد أنواع الكيانات المستهدفة في `entity_audit_log` وحده يمنع ذلك).
التنازل يكسر: أي استعلام أو تقرير مستقبلي "متى وماذا ولماذا حدث X" يحتاج شكلًا موحدًا ليُكتب مرة واحدة ويعمل على الثلاثة؛ الأشكال المتفرقة تضاعف كود القراءة بلا فائدة.
مُنفَّذ فعليًا في الترحيل `packages/db/migrations/0002_unify_audit_log_columns.sql` (الجداول كانت فارغة في هذه المرحلة، فلا هجرة بيانات مطلوبة).

**50. `health_tasks.priority` — القيم**
`عادي · عاجل`. `NOT NULL DEFAULT 'عادي'` (الوثيقة الشاملة §3.8).
لماذا: قيمتان كافيتان لفرز شاشة الصحة لدى المربي ("المتأخرة أولًا" مبني على `scheduled_date`، والعاجل يميّز بلون فقط ضمنها).

**51. `health_observations.status` — القيم**
`جديد · قيد المراجعة · مغلق`. `NOT NULL DEFAULT 'جديد'` (الوثيقة الشاملة §5-ج).
لماذا: دورة حياة البلاغ الصحي الثلاثية تكفي لتغطية تدفق §14 (بلاغ → رد الطبيب → إغلاق) بلا حالات وسيطة غير مستخدمة.

**52. `shipments.dispute_status` — القيم**
`مفتوح · مغلق`. منفصل عمدًا عن `dispute_outcome` (سبب الحسم: خطأ قياس · فاقد نقل · فاقد بعد التسليم) — عمودان مختلفا الغرض: الأول "هل النزاع منتهٍ؟" والثاني "لماذا انتهى هكذا؟".
لماذا: خلطهما في عمود واحد يمنع الاستعلام البسيط "كل النزاعات المفتوحة الآن" بلا فلترة قيم لا علاقة لها بالحالة.

**53. `products.storage_conditions` — القيم**
`عادي · مبرّد 2-8°م · مجمّد` (الوثيقة الشاملة §5-ج، نموذج إضافة المنتج).
لماذا: تصنيف صيدلاني عملي يحدد شرط التخزين الفعلي للمنتج، تحدده الشركة/الطبيب لا قيمة هندسية.

**54. `shipments.type` — تأكيد**
مؤكَّد: نفس قيم `product_category` (علف · دواء · لقاح · فيتامين · مستلزمات) — لا نوع enum منفصل، كما كان منفَّذًا فعليًا منذ المرحلة 0 دون تصريح صريح بالسبب.
لماذا: تقييد فئة الشحنة بالدور (§12.2: المشرف "علف+مستلزمات"، الطبيب "أدوية+لقاحات") يقارَن مباشرة بفئة المنتج المرسَل؛ نوعان منفصلان يستلزمان جدول تحويل بينهما بلا أي فائدة.

**55. ترتيب خطوات التجهيز صريح**
أُضيف `house_prep_steps.step_order integer NOT NULL` بفهرس فريد `(cycle_id, step_order)`، وأُضيف مخطط zod مشترك `prepProtocolSchema` في `packages/shared` يثبّت شكل `tenants.prep_protocol` (jsonb) كمصفوفة `{key, label, required, order}`.
لماذا: الخطوات متسلسلة فعليًا (لا تُطهَّر قبل الغسيل) وشاشة المشرف تعرض "الخطوة التالية" — بلا ترتيب صريح لا يمكن حساب أيها التالية، والاعتماد على ترتيب الإدراج في القاعدة (implicit ordering) غير موثوق وغير مسموح في SQL أصلًا.
التنازل يكسر: أول محاولة لعرض "الخطوة التالية" في المرحلة 3 كانت ستكتشف غياب أي عمود ترتيب وتضطر لحل مرتجل حينها.

**56. منحنيات معايير السلالات تقريبية — بوابة قطع صريحة قبل الإنتاج**
`breed_standards` مبذورة حاليًا (`packages/db/src/seed/breed-standards-data.ts`) بمنحنى Gompertz مولَّد رياضيًا، **وليست** جداول الأداء الرسمية المنشورة من الشركات المربية.
لماذا هذا خطير تحديدًا (لا مجرد "بيانات تجريبية عادية"): كل مؤشر "مقارنة بالمعيار" في التطبيق (القرار #30 — "لا معنى لرقم مجرد دون معياره") يعتمد على هذه الجداول. بيانات تقريبية معقولة الشكل تُنتج **مقارنات خاطئة بأرقام تبدو صحيحة** — لا تفشل بوضوح كبيانات فارغة أو null، بل تضلّل المستخدم بثقة زائفة في دقة رقم غير حقيقي.
القرار:
1. تحذير صريح ثلاثي المواضع (مطبَّق): أعلى `packages/db/src/seed/breed-standards-data.ts` · هنا في `decisions.md` · بند بوابة قطع منفصل في `docs/work-plan.md` (قائمة "الاكتمال" بالمرحلة 7).
2. المصادر الرسمية المطلوبة عند الاستبدال: **Aviagen** لسلالتي Ross 308 وArbor Acres، **Cobb-Vantress** لسلالة Cobb 500 (جداول الأداء "Performance Objectives" المنشورة رسميًا من كل شركة).
3. **بند بوابة قطع منفصل عن بقية معايير القبول** — لا يُسلَّم النظام لأي عميل قبل استبدال هذه المنحنيات، حتى لو اكتملت كل بنود §26 الأخرى في المواصفة التقنية. هذا البند لا يُنجَز تلقائيًا مع بقية "الاكتمال" لأن البيانات التقريبية *تبدو* مكتملة (45 يومًا × 3 سلالات، بلا فراغات) — الاكتمال الشكلي هنا يخفي الخطأ الجوهري بدل كشفه.
التنازل يكسر: ثقة كل تقرير أداء وكل قرار تشغيلي (تعديل علف، تنبيه نفوق) يعتمد على "أعلى من المعيار" أو "أقل من المعيار" في النظام كله.

**57. تسجيل الدخول عند تكرار نفس الجوال في أكثر من مستأجر**
غموض لم تُعالجه أي من الوثيقتين صراحة: القرار #23 يجعل رقم الجوال فريدًا *داخل* المستأجر لا عالميًا، وهذا يسمح فعليًا بأن يملك نفس الشخص (نفس الجوال) حسابًا في أكثر من مستأجر — الحالة الواقعية الأوضح: طبيب مستقل يخدم عدة مُلّاك دواجن، كل واحد بمستأجره الخاص. `POST /api/auth/login` كما صُمِّم أصلًا (جوال+كلمة مرور فقط) لا يملك طريقة لحسم أي حساب يُقصَد عند تطابق كلمة المرور نفسها في أكثر من صف.
القرار: `loginSchema` يقبل `tenantId` اختياريًا. الخادم يقارن كلمة المرور مقابل *كل* الصفوف المطابقة للجوال المطبَّع (عبر كل المستأجرين، مع تقييد `tenantId` إن أُرسل)، ثم:
- تطابق صفر → 401 `invalid_credentials` (رسالة عامة، لا كشف عن حالة الحساب — يشمل حسابًا معطَّلًا `is_active=false`، الذي يُستبعد من الاستعلام أصلًا فيُعامَل كمطابقة صفر).
- تطابق واحد → توكن عادي.
- تطابق أكثر من واحد (بلا `tenantId` في الطلب) → `200 {needsTenantSelection: true, accounts: [{tenantId, fullName, role}, ...]}` بلا توكن؛ الواجهة تعرض قائمة اختيار ثم تُعيد الطلب مع `tenantId` المختار.
لماذا: يحفظ مبدأ "لا كشف أي الحقلين خاطئ" (§11) مع حل تعارض حقيقي بلا افتراض أن الجوال مفتاح فريد عالميًا — وهو افتراض كان سيكسر بصمت أول مرة يُسجّل فيها طبيب مستقل حسابين.
مُنفَّذ في `apps/api/src/routes/authPublic.ts`، مُثبَت باختبار تكامل (`authPublic.integration.test.ts`، "طلب 4/5").

**58. لا هدف اختباري فعلي لـ"cross-tenant-404" داخل مسارات المرحلة 1 كما نُطاق حاليًا**
اكتشاف صادق أثناء بناء مصفوفة الصلاحيات الكاملة التي طلبها المالك (allow+deny-403+cross-tenant-404 لكل مسار): طبقة `enforceEntityAccess` (الحارس المركزي الثالث) تفحص حصريًا `houseId`/`batchId` القادمَين من params/query/body (`apps/api/src/middleware/entityAccess.ts`). لا مسار من مسارات المرحلة 1 الأربعة (`/api/auth/login`, `/api/auth/me`, `/api/auth/change-password`, `/api/auth/register-push-token`) ولا `/api/settings` يأخذ معرّف كيان في الرابط أصلًا — الأول عام بلا مصادقة، والثلاثة التالية تعمل على `req.user.id` الذاتي، والأخير يعمل ضمنيًا على `req.user.tenantId`. لا واحد منها يستقبل `houseId`/`batchId` من عميل بحيث يمكن اختبار محاولة وصول عابرة للمستأجر بمعرّف كيان حقيقي.
القرار: لا يُختلَق اختبار مصطنع لسد هذا البند شكليًا. الحقل الفعلي لاختبار cross-tenant-404 يبدأ مع أول مسار يأخذ معرّف كيان في الرابط (`/houses/:houseId`, `/batches/:batchId`, ...) — المرحلة 2/3 حسب `docs/work-plan.md`. هذا مُسجَّل هنا صراحة بدل أن يُفترض خطأً لاحقًا أنه غُطِّي.
التنازل يكسر: تقرير إغلاق مرحلة يدّعي تغطية بند لم يُختبَر فعليًا يُفقِد الثقة بكل تقارير الإغلاق اللاحقة.

**59. بادئة `/api` مفقودة في مسارات المصادقة الأولى — خطأ حقيقي اكتُشف وأُصلح أثناء البناء**
`backend-technical-spec.md §17` صريح: "كلها تبدأ بـ /api ... ما لم يُذكر خلاف ذلك" — والاستثناء المذكور صراحة لـ`/auth/login` هو الإعفاء من `requireAuth`/`requireTenant`/`enforceEntityAccess` ("بلا مصادقة")، لا من بادئة `/api`. المسودة الأولى لـ`authPublic.ts`/`authProtected.ts` سجّلت المسارات بلا `/api` (`/auth/login` لا `/api/auth/login`) خلطًا بين الإعفاءين.
القرار: تصحيح الأربعة كلها إلى `/api/auth/login` · `/api/auth/me` · `/api/auth/change-password` · `/api/auth/register-push-token`، مع إبقاء `/auth/login` وحده مُركَّبًا قبل سلسلة `requireAuth` في `app.ts` (الإعفاء الوحيد المقصود هو الفرض المركزي، لا مسار الرابط).
اكتُشف بالمراجعة النصية المباشرة لـ§17 قبل كتابة أي اختبار — لا حاجة لإثبات إضافي بعد التصحيح، فاحص `openapi-coverage` (فحص برمجي حقيقي لا نصي) يفشل البناء فورًا لو تكرر هذا الخلط مستقبلًا.

**60. CI أحمر منذ أول دفعة على PR #1 — خطوة "اختبارات الوحدة" تفشل ببنية لا اختبار فعلي فيها**
اكتُشف أثناء تحقق دوري روتيني من حالة CI بعد دفع نواة خادم المرحلة 1: **كل الدفعات الست** على هذا الـPR (من سقالة المرحلة 0 وحتى هذه الدفعة) فشلت CI عند نفس الخطوة تحديدًا — `pnpm run test` (اختبارات الوحدة، منفصلة عن `test:integration`). السبب: هذا المشروع لم يكتب أي اختبار وحدة (`*.test.ts` غير `*.integration.test.ts`) في أي مرحلة من البداية — كل الاختبار تكامل حقيقي ضد قاعدة اختبار فعلية عمدًا. `apps/api/package.json`'s `test` سكربت يستهدف `src/**/*.test.ts` عبر `vitest run`، وvitest يخرج برمز غير صفري افتراضيًا عند "لا ملفات اختبار مطابقة" — لا علاقة للفشل بأي كود أو منطق مضاف، بل ببنية سكربت لم تُختبر قط ضد الحالة الفعلية (صفر ملفات).
القرار: إضافة `--passWithNoTests` لسكربت `test` في `apps/api/package.json`. هذا **ليس تجاوزًا لاختبار فاشل** — لا يوجد اختبار وحدة أصلًا ليُتجاوَز أو يُعطَّل؛ الفحص الحقيقي (`test:integration`، 42 اختبارًا حاليًا) يبقى إلزاميًا وغير متأثر.
لماذا لم يُكتشف سابقًا: أول من راجع نتيجة CI الفعلية بعد كل دفعة كان يفترض "الفحوص الستة نجحت" (`check:all`) كافيًا، والتحقق الدوري من CI نفسه لم يُفعَّل إلا متأخرًا في الجلسة. **هذا PR كان أحمر فعليًا طوال الوقت** رغم أن كل تقارير الإغلاق السابقة وصفت الاختبارات كناجحة — لأنها كانت تُشغَّل محليًا (`pnpm run test:integration` وحدها، لا `pnpm run test` معها) لا عبر مسار CI الكامل. التنازل يكسر: أي ادعاء "الفحوص خضراء" مستقبلًا يجب أن يستند لنتيجة CI الفعلية على الـcommit المدفوع، لا فقط لتشغيل محلي جزئي.

**61. بوابات جودة كود آلية — ESLint صارم + حدود تعقيد + قواعد مشروع مخصصة (تفرض لا توصي)**
طلب صريح: الكود ما زال صغيرًا، فتُطبَّق هذه القواعد الآن قبل تراكم مخالفات يصعب إصلاحها لاحقًا — كقواعد ESLint تفشل البناء، لا توصيات.
القرار:
1. **ESLint 9 (flat config)** جذري في `eslint.config.mjs`، محصور بـ`apps/api`·`packages/db`·`packages/shared` (الخادم فقط — `apps/mobile` له إعداده الخاص `eslint-config-expo`، نطاق React Native مختلف تمامًا): `typescript-eslint` `strictTypeChecked`+`stylisticTypeChecked` (يمنع `any` صريحًا/ضمنيًا، وصول/استدعاء/تعيين غير آمن على `any`)، `ban-ts-comment` (`ts-ignore` يحتاج وصفًا ≥10 أحرف)، `no-unused-vars`، `no-console` (استخدم pino)، `import-x/order` (مجموعات: خارجي·داخلي·نسبي).
2. **حدود تعقيد** (ESLint core، بلا إضافات): `max-lines` 400 · `max-lines-per-function` 60 · `complexity` 10 · `max-depth` 4 · `max-params` 4 (استثناء ملفات الاختبار من `max-lines-per-function` — سيناريوهات الإعداد `beforeAll` تتجاوزه بلا دلالة على تعقيد فعلي).
3. **قواعد مشروع مخصصة** (`eslint-rules/*.mjs`، لا اعتماد على `@typescript-eslint/utils` — AST عادي كافٍ): `no-db-in-routes` (استعلام Drizzle مباشر ممنوع في `routes/*.ts`، يجب أن يعيش في `services/*.ts` جديدة — أُنشئت `authService.ts`·`settingsService.ts`·`healthService.ts` لهذا)، `require-tx-for-multi-table-write` (كتابة db.insert/update/delete لأكثر من جدول بنفس الدالة بلا tx)، `no-unvetted-house-id-reuse` (فحص تقريبي عمدًا — اسمي لا تدفق بيانات كامل، موثَّق في الملف نفسه)، `no-english-user-error` (نص إنجليزي في `new HttpError(...)`/رسائل zod)، `no-float-quantity-column` (`real()`/`doublePrecision()` ممنوعان في مخطط عمود كمية)، `no-magic-config-number` (رقم مُدمَج لخاصية تطابق عمود إعداد tenants فعلي أو سياسة أمنية معروفة — نطاق ضيق عمدًا بعد أن أنتجت نسخة أولى بنمط عام (`weight`/`day` كنص) نتائج كاذبة على ثوابت منحنى النمو الموسومة أصلًا في `breed-standards-data.ts`؛ صُحِّح ليطابق أسماء أعمدة `tenants` الفعلية فقط).
4. **Prettier** مفروض عبر `.prettierrc.json`+فحص `--check` في `check:all` — لا نقاش تنسيق.
5. **التعطيل الموثَّق لا العالمي**: `import-x/no-named-as-default`/`no-named-as-default-member` مُعطَّلتان *عالميًا* بتعليق مسبَّب في `eslint.config.mjs` (لا استثناء بند بند) لأنهما تُبلِّغان زورًا بشكل *منهجي* على كل استيراد افتراضي لحزم CJS (`express`/`pino`/`rateLimit`) تحت `esModuleInterop` — هذا الاستثناء الوحيد المعطَّل عالميًا، ومبرَّر بأنه ليس استثناءً لحالة محددة بل خطأ بنيوي في القاعدة نفسها مع إعداد هذا المشروع. كل تعطيل آخر (نوافذ تحديد المعدل الثابتة بالمواصفة §11/§3.4) سطر بسطر بـ`eslint-disable-next-line` معلَّل في موضعه.
6. **`@typescript-eslint/no-misused-promises`** مُهيَّأة بـ`checksVoidReturn: { arguments: false }` — تهيئة قياسية موثَّقة لمشاريع Express+TS (معالج async يُمرَّر حيث النوع المتوقَّع void، وExpress لا ينتظر عودته أصلًا؛ الخطأ يُمسَك فعليًا عبر try/catch+next(error))، لا تخفيف قدرة القاعدة على كشف إساءة استخدام وعد حقيقية أخرى.
7. **`@typescript-eslint/restrict-template-expressions`** بخيار `allowNumber: true` — رقم في قالب نصي يُنتج دائمًا التمثيل المتوقَّع، لا خطر حقيقي (خلافًا لكائن/`undefined`).
التنازل يكسر بالضبط ما وصفه الطلب: تراكم مخالفات على كود أكبر تصبح إعادة هيكلة مؤلمة بدل تصحيح فوري بسيط الآن.

**62. ترجمة ZodError إلى 400 — كل فشل تحقق كان يصل 500 عام بدل 400 محدَّد**
اكتُشف أثناء بناء فاحص `no-english-user-error` (تدقيق كل مسار وصول رسالة خطأ للمستخدم): `errorHandler.ts` لم يكن يترجم `ZodError` (رمي `.parse()` الطبيعي عند فشل تحقق) إلى `HttpError` أبدًا — يسقط في المعالج العام فيصير 500 "حدث خطأ غير متوقع" بدل 400 حقيقي بتفاصيل الحقول. مُثبَت حيًّا: طلب `POST /api/auth/login` بـ`{phone:"",password:""}` قبل الإصلاح أعاد `500 {code:"internal_error"}`؛ بعده `400 {code:"invalid_input", message:"بيانات غير صالحة", details:{phone:[...],password:[...]}}`.
القرار: `apps/api/src/lib/zodErrors.ts` يحوّل أي `ZodError` إلى `HttpError(400, "invalid_input", "بيانات غير صالحة", z.flattenError(error).fieldErrors)`. الرسالة العليا عربية عامة دائمًا (يفي بـ§18)؛ رسائل zod التفصيلية (إنجليزية افتراضيًا) تبقى في `details` كحقل تقني ثانوي لا كرسالة عرض أساسية — لا حاجة لترجمة كل رسالة zod فرديًا.
مُثبَت باختبار وحدة حقيقي (`zodErrors.test.ts`) لا تكامل فقط — لا اتصال قاعدة بيانات لازم لهذا المنطق.
التنازل يكسر: كل فشل تحقق مدخلات في كل مسار مستقبلي كان سيظهر للمستخدم كـ"خطأ خادم غير متوقع" مربك بدل رسالة تحقق واضحة، ويُسجَّل في السجلات كـ`logger.error` (خطأ خادم حقيقي) بدل `logger.warn` (تحقق مدخلات عادي) — ضجيج تنبيه كاذب متكرر في الإنتاج.

**63. تغطية اختبار حقيقية مقاسة — c8/v8 عبر Vitest، حدود مفروضة لا مُقدَّرة**
طلب صريح: "ركّب أداة تغطية فعلية... بلا قياس يبقى شرط الـ80% رغبة لا بوابة". اكتشافان حقيقيان أثناء التركيب، كلاهما وُثِّق ليمنع تكراره:
1. **مسار وحدة منفصل عن مسار تكامل يُخفي التغطية الحقيقية**: قياس التغطية عبر `vitest.integration.config.ts` وحده أظهر 0% تغطية لملفات الاختبارات الجديدة (`tenant.test.ts` وغيرها) رغم وجودها فعليًا — لأن ذلك الإعداد يستثني `*.test.ts` عمدًا (يخدم `test:integration` فقط). أُنشئ `vitest.coverage.config.ts` منفصل يشمل النمطين معًا (`*.test.ts` و`*.integration.test.ts`) خصيصًا لقياس التغطية — لا يستبدل السكربتين الأصليين (يبقيان منفصلين لأسباب سرعة/عزل مختلفة في CI).
2. **قياس تغطية عبر حدود حزم monorepo لا يعمل من جذر حزمة أخرى**: محاولة أولى لقياس `packages/shared/src/phone.ts` (تطبيع الجوال) من داخل `apps/api/vitest.coverage.config.ts` عبر أنماط `include`/مفاتيح `thresholds` بمسارات نسبية (`../../packages/shared/src/phone.ts`) مرّت **بصمت** — لا الملف ظهر في `coverage-final.json` ولا فشل أي حد بشأنه، لأن v8 provider في Vitest لا يُدرِج ملفات مُحلَّلة من حزمة workspace أخرى (مرتبطة برابط رمزي عبر `node_modules`) في تقرير التغطية بصرف النظر عن أنماط include، حتى بمسارات مطلَقة صريحة. لولا فحص `coverage-final.json` يدويًا لبقي هذا الحد "متحقق" زورًا. الحل: `packages/shared` لها الآن `vitest.config.ts`+اختبار وحدة خاصان بها (`phone.test.ts`)، تُقاس تغطيتها داخل حدود حزمتها هي — تصميم أنظف أصلًا، لا مجرد التفاف حول قيد أداة.
القرار النهائي:
- **الحدود**: ≥80% عام (أسطر/عبارات/دوال/فروع) لـ`apps/api` · **100%** لطبقة الصلاحيات (`middleware/auth.ts`·`tenant.ts`·`entityAccess.ts`·`requireRole.ts`·`lib/authContext.ts`) وحساب الدفتر (`lib/inventoryBalance.ts`) وتطبيع الجوال (`packages/shared/src/phone.ts`، مقاسة في حزمتها). **لا "معادلات" (FCR/EPEF)** بعد — تلك المرحلة 4، لا هدف لقياسه الآن.
- **الأرقام الفعلية عند إغلاق هذه الدفعة**: `apps/api` أسطر/عبارات 91.01%، دوال 97.5%، فروع 81.81%؛ الملفات الستة المطلوبة 100/100/100/100 بالضبط؛ `packages/shared/src/phone.ts` 100/100/100/100.
- فروع دفاعية غير قابلة للبلوغ عبر PostgreSQL/مدخلات حقيقية (مثل `row?.balance ?? 0` حين يضمن `COALESCE(SUM(...),0)` بلا `GROUP BY` صفًا واحدًا دائمًا) اختُبرت بمحاكاة معزولة (`db.execute` مُصطنَع) بدل حذفها أو تجاهلها — العقد الدفاعي يستحق تغطية رغم عدم بلوغه بالمدخلات الحالية.
- `scripts/checks/coverage.ts` يشغّل كلا الفحصين (apps/api وpackages/shared) ويُظهر الأرقام الحقيقية من `coverage-summary.json` — لا تقدير أبدًا، مطابقةً للطلب حرفيًا.
التنازل يكسر: أي رقم تغطية غير مقاس فعليًا (كما كان الحال قبل هذا القرار) يمنح ثقة زائفة مطابقة تمامًا لخطر بيانات معايير السلالات التقريبية (القرار #56) — "يبدو مكتملًا" يخفي فجوة حقيقية بدل كشفها.

**64. الحوكمة — فرع + PR، conventional commits، ولا تقرير إغلاق مرحلة بلا رابط CI أخضر**
طلب صريح بعد اكتشاف القرار #60 (ست دفعات وCI حمراء ونحن نظن العكس).
القرار:
1. **كل عمل على فرع + PR** — لا دفع مباشر على `main`. (فرض هذا آليًا يحتاج قاعدة حماية فرع في إعدادات GitHub — إجراء إداري خارج المستودع، لا يمكن للكود فرضه؛ مُسجَّل هنا كالتزام صريح.)
2. **CI يشغّل `check:all` (شامل ESLint وPrettier والتغطية) + اختبارات الوحدة + التكامل على كل PR** — مطبَّق في `.github/workflows/ci.yml`.
3. **conventional commits** — مفروض آليًا عبر `scripts/check-commit-messages.ts` كخطوة CI على أحداث PR فقط، ويفحص نطاق `origin/<base>..HEAD` **لا كل التاريخ**: رسائل ما قبل تبنّي القاعدة لا تُعاد كتابتها (إعادة كتابة تاريخ مدفوع فعلًا تغيّر كل SHA وتكسر أي نسخة عمل قائمة). مُثبَت عمليًا: يفشل بخروج 1 على تاريخ غير متوافق، ويمر بخروج 0 على نطاق متوافق.
4. **لا PR يُدمج وفيه `TODO` بلا issue مرتبط** — التزام مراجعة، لا فحص آلي (فحص نصي لـTODO ينتج ضجيجًا على تعليقات مشروعة؛ التمييز يحتاج حكمًا بشريًا). `TODO` الوحيد الحالي في المستودع: `apps/mobile/app/_layout.tsx:23` (ملفات خط Tajawal — عمل مرحلة 1 موبايل).
5. **لا تقرير إغلاق مرحلة بلا رابط CI أخضر على الـcommit المدفوع فعليًا** — "أخضر محليًا" غير مقبول (السبب المباشر: القرار #60).

**65. تطبيق بوابات الجودة على كل الكود بلا استثناء ملف — وما كشفه ذلك**
طلب صريح: القواعد تُطبَّق على كل الكود الموجود لا الجديد فقط، ولا استثناء لملف بحجة أنه سابق للقواعد.
كان إعداد ESLint الأول يستثني: `apps/mobile/**` كاملًا · `scripts/**` · `eslint-rules/**` · كل `*.config.ts`، ويُعطّل خمس قواعد على مستوى glob لملفات الاختبار. أُزيلت الاستثناءات كلها.
**القياس قبل أي إصلاح: 137 مخالفة في 29 ملفًا من 127 مفحوصًا** (apps/api 68 · scripts 30 · apps/mobile 25 · eslint-rules+config 9 · packages 5)، زائد **8 أخطاء typecheck**.
ثلاثة قرارات اعتمدها المالك:
- **(أ) تصحيح مُطابِق الملفات** في `no-db-in-routes` و`require-tx-for-multi-table-write` ليستثني `*.test.ts` حصريًا. **إصلاح خلل لا تخفيف**: نيّة القاعدتين «ملف معالج مسار» و«مسار كتابة إنتاجي»، لا «أي ملف داخل مجلد routes/». 57 من 137 مخالفة كانت كلها في ملفات اختبار تُدرج fixtures مباشرة عمدًا (القرار #27 يفرّق بين fixtures وseed:demo صراحةً).
- **(ب) `no-console` مُعطَّلة بالمسار** لـ`scripts/**` و`migrate.ts` و`setup-test-db.ts` و`apps/api/src/scripts/**` — أدوات سطر أوامر مخرجاتها للإنسان في الطرفية، وpino (سجل JSON لخادم يخدم طلبات) ليس بديلًا صحيحًا لها.
- **(ج) `globals.node` + `sourceType: commonjs`** لملفات إعداد الموبايل (`metro`/`babel`/`eslint`) — نقص إعداد لا مخالفة كود.
الـ64 الباقية أُصلحت فعليًا بلا تخفيف أي قاعدة وبلا `eslint-disable` على مستوى ملف: ترتيب الاستيراد · أنواع غير آمنة في `introspectRoutes` (سببها الحقيقي: `pino` و`@dawajin/db` غير معرَّفتين كتبعية جذرية رغم استيراد `scripts/` لهما) · فروع `undefined` في `coverage.ts` و`enum-usage.ts` · وعد طافٍ في `check-all.ts` · دوال فارغة في الموبايل (استُبدلت بمساعد `bestEffort` موثَّق بدل `.catch(() => {})` المجهول) · تعقيد 16 في `no-english-user-error` (قُسِّم لدالتين مسمّاتين) · كتل fixture وdescribe طويلة (استُخرجت لدوال مسمّاة وقُسِّمت حسب المعنى لا اعتباطًا).
**الاكتشاف الأهم:** `scripts/**` (17 ملفًا — كل أدوات الفحص) وملفات `vitest.config` الأربعة **لم تكن داخل أي tsconfig**، فلم يفحصها `pnpm typecheck` ولا مرة منذ المرحلة صفر، ولم يستطع ESLint تحليلها (16 خطأ تحليل قاتل). أُنشئ `tsconfig.json` جذري يغطّيها، ووُسِّعت بوابة typecheck لتشملها صراحةً.

**66. تشديد إعدادات TypeScript وتوحيدها عبر الحزم**
مراجعة كل ملفات tsconfig بحثًا عن أي إعداد يقيّد التحقق:
- **`apps/mobile` كانت أقل صرامة من بقية الحزم** — ترث `expo/tsconfig.base` لا `tsconfig.base.json` الخاص بالمشروع، فتحصل على `strict: true` صراحةً لكن **بلا `noUncheckedIndexedAccess`**. قِيس الأثر: صفر أخطاء عند تفعيله — فجوة مجانية بقيت مفتوحة بلا سبب.
- أُضيفت لكل الحزم (بعد قياس أثر كل علم على حدة): `noUncheckedIndexedAccess` · `noImplicitOverride` · `noFallthroughCasesInSwitch` · `verbatimModuleSyntax` (أربعتها بصفر أخطاء) و`exactOptionalPropertyTypes` (خطآن أُصلحا فعليًا: zod يستنتج الحقل الاختياري `T | undefined`، فأُعلن ذلك صراحةً في `LoginInput` و`TenantSettingsUpdate` — يفرّق بين "الحقل غائب" و"موجود بقيمة undefined").
- **`noPropertyAccessFromIndexSignature` مقترح ولم يُفعَّل** — 19 مخالفة، أغلبها `process.env.X` و`req.params.houseId`. له قيمة حقيقية (يوثّق أن المفتاح قد لا يوجد) لكنه أسلوبي بالأساس؛ القرار للمالك.
- **`skipLibCheck: true` يبقى مفعَّلًا** بوعي: فحص كل `.d.ts` في `node_modules` بطيء وكثيرًا ما تكسره أنواع طرف ثالث؛ ممارسة شبه عالمية. مُسجَّل هنا كي لا يُكتشف لاحقًا كأنه إغفال.

**67. لا أسرار في تاريخ git — نتيجة تدقيق كامل**
فُحص كل تاريخ المستودع (`git log --all -p`) بحثًا عن: ملفات `.env`/`.pem`/`.key`/`.p12`/`.keystore` · أنماط أسرار معروفة (AWS `AKIA…` · GitHub `ghp_`/`github_pat_` · OpenAI `sk-` · Slack `xox…` · مفاتيح PEM خاصة · JWT محفوظة) · إسنادات نصية لكلمات مرور/توكنات.
**النتيجة: لا سر حقيقي واحد.** الملف الوحيد من عائلة `.env` المُضاف يومًا هو `.env.example` (قيم نائبة)، و`.env` نفسه مُستبعَد بـ`.gitignore` (`.env` و`.env.*`) ولم يُتعقَّب قط.
المطابقات الأربع الوحيدة كلها قيم اختبار: `ExponentPushToken[x]` · `"wrong-password"` (كلمة مرور خاطئة عمدًا في اختبار) · `probe-test-secret` (سر JWT داخل `_probe.integration.test.ts` حصريًا).
بيانات الاعتماد الظاهرة في `.env.example` و`ci.yml` (`dawajin_dev_password` · `dawajin_ci_password` · `dev-only-secret-change-me` · `ci-only-secret`) مقصودة وغير حساسة: قاعدة بيانات محلية/حاوية CI عابرة، ولا وجود لها في أي بيئة حقيقية — بل إن `loadEnv()` يرفض إقلاع الإنتاج بسر التطوير الافتراضي صراحةً.

**68. مقارنة الجداول الـ29 بالقسم 7 — عمودًا عمودًا**
قُورن كل جدول في `packages/db/src/schema/` بتعريفه في §7 آليًا (جداول §7 ذات الأعمدة الصريحة، والتعريفات النثرية في §7.3–7.5، وسجلات التدقيق في §7.7).
**النتيجة: الـ29 جدولًا كلها مطابقة.** الفروق الثلاثة الوحيدة كلها مبرَّرة وموثَّقة:
- `house_prep_steps.step_order` — إضافة متعمَّدة (القرار #55: الخطوات متسلسلة فعليًا وبلا عمود ترتيب صريح لا يمكن حساب "الخطوة التالية").
- سجلات التدقيق الثلاثة بـ12 عمودًا موحَّدًا — §7.7 يحدد نطاق كل جدول لا أعمدته (القرار #49).
- `shipments.created_at` — عمود اصطلاحي موجود في كل جدول آخر بالمواصفة، غير مُعدَّد لـ`shipments` تحديدًا؛ فائض حميد.
تنبيه منهجي: النسخة الأولى من سكربت المقارنة أعطت أربعة "فروق" كلها زائفة — `[a-z_]+` في التعبير النمطي يستبعد الأرقام فلم يطابق `phone_e164`، وخلايا §7 المركّبة (`| id · tenant_id |`، `| type · product_id FK NOT NULL |`) وأعمدة مذكورة في خانة الملاحظات (`mortality_cause_note`) فاتت المُحلِّل. **كل "فرق" وجب التحقق منه يدويًا في المصدر قبل الإبلاغ عنه** — الأداة التي تقيس ليست معصومة، وهي نفسها تحتاج إثباتًا.

**69. جدول البوابات — إثبات أن كل بوابة تقيس شيئًا فعليًا**
بعد اكتشاف أربع بوابات لا تحرس ما نظنّ (الفاحص النصي للمسارات · CI حمراء ونحن نظن العكس · قياس التغطية عبر حدود الحزم · `scripts/` خارج typecheck)، اختُبرت **كل** بوابة في `check:all` بمخالفة متعمَّدة: أُدخلت المخالفة، ووُثِّق فشل البوابة، ثم أُزيلت ووُثِّق نجاحها.
ثمانٍ من التسع أثبتت أنها تفشل فعليًا على مخالفة حقيقية.
**الاستثناء الوحيد — `enum-usage`: لا يمكنه الفشل إطلاقًا.** يُرجع `ok: true` ثابتًا في الكود (سطر 64)، فهو **بوابة إبلاغ لا بوابة إنفاذ**. هذا مقصود ومُوثَّق (يبقى ليّنًا حتى المرحلة 7 لأن فرض 100% قبل اكتمال المسارات غير واقعي)، لكن الحقيقة المجرّدة أنه **بنيويًا عاجز عن إفشال البناء اليوم** — وهو بالضبط شكل فاحص OpenAPI القديم الذي استُبدل لهذا السبب نفسه. يُسجَّل هنا صراحةً كي لا يُحسَب حارسًا وهو ليس كذلك، ويجب تحويله لبوابة إنفاذ حقيقية عند المرحلة 7.

**70. `noPropertyAccessFromIndexSignature` — مرفوض (لا يُعاد فتحه)**
عُرض ضمن تدقيق tsconfig (القرار #66) بكلفة 19 مخالفة، أغلبها `process.env.X` و`req.params.houseId`.
القرار: **مرفوض.** مكسب أسلوبي بحت (يفرض `obj["key"]` بدل `obj.key`) بلا أي قيمة أمنية أو منع فساد بيانات لمشروع بهذا الحجم. الحماية الحقيقية من المفتاح الغائب يوفّرها `noUncheckedIndexedAccess` **المفعَّل بالفعل** (القرار #66) — وهو يضيف `| undefined` للنوع فيفرض التحقق، بينما هذا العلم يغيّر صيغة الكتابة فقط.
مُسجَّل هنا صراحةً كقرار مرفوض بمبرره كي لا يُعاد طرحه في كل مراجعة إعدادات لاحقة.

**71. `enum-usage` — تحويله لبوابة إنفاذ حقيقية فورًا، لا بعد سبع مراحل**
القرار #69 كشف أنه كان يُرجع `ok: true` ثابتًا في الكود: عاجز بنيويًا عن إفشال البناء، وهو بالضبط شكل فاحص OpenAPI القديم الذي استُبدل لهذا السبب نفسه. التأجيل للمرحلة 7 يعني أن الفاحص لا يحرس شيئًا خلال المراحل الست التي يُبنى فيها كل شيء.
**القرار: يفشل الآن.** حُذف `ok: true` الثابت، وأُعيد بناء المنطق:
- **الوحدة المفحوصة هي القيمة لا النوع.** النوع المغطّى جزئيًا (بعض قيمه مكتوبة وبعضها لا) هو الحالة الخطرة تحديدًا: يبدو مستخدَمًا وفيه قيمة ميتة.
- **"يكتبها" لا "تُذكَر".** الفحص يقتصر على **طبقة الكتابة** — الملفات التي تُنفِّذ `.insert(`/`.update(` فعليًا. حارس قراءة مثل `requireRole("owner")` أو `user.role === "owner"` يذكر القيمة ولا يكتبها أبدًا. النسخة الأولى من هذا التحويل مسحت كل `apps/api` نصيًا فأبلغت زورًا عن `USER_ROLE."farmer"` و`"supervisor"` و`"vet"` كقيم ميتة، بينما السبب الحقيقي أن مسار `POST /users` لم يُبنَ بعد (المرحلة 4). اكتشاف الملف يتم بموقع الكتابة نفسه لا باسم المجلد، فيبقى دقيقًا لو تغيّرت البنية.
- **نوع لم تكتب طبقة الكتابة أي قيمة منه بعد يُتجاوَز** — ميزة لم تُبنَ لا قيمة ميتة. أول مسار يكتب قيمة واحدة منه يُفعِّل الحراسة على **كل** قيمه فورًا.
السبب الأصلي (مذكور في المواصفة §26): قيمتان ميتتان في نسخة سابقة من هذا النظام أفسدتا تقرير الفاقد — جمّع حسب قيم لا يمكن أن تظهر في البيانات إطلاقًا.
مُثبَت بمخالفة متعمَّدة كبقية البوابات: كتابة `role: "owner"` وحدها في طبقة الكتابة ← فشل يسمّي القيم الأربع الميتة (`farmer`/`supervisor`/`vet`/`platform_admin`)؛ وبإزالتها ← نجاح. يمر اليوم لأن لا طبقة كتابة تكتب أي قيمة enum بعد.

**72. المرحلة صفر مغلقة — لا توسيع نطاق بعدها**
بعد إغلاق بنود §8 الثلاثة (القرارات #66-68) وجدول البوابات (#69): **المرحلة صفر مغلقة نهائيًا.**
أي اكتشاف جديد بعد هذه النقطة يُسجَّل في `docs/work-plan.md` كدَين تقني مؤجَّل ولا يُعالَج فورًا — **باستثناءين وحيدين: ثغرة أمنية، أو فساد بيانات.** هذان يُعالَجان فور اكتشافهما بصرف النظر عن المرحلة.
لماذا: هذه الجلسة وحدها كشفت خمس بوابات لا تحرس ما نظنّ، وكل اكتشاف جرّ توسيعًا جديدًا. التوسيع بلا حدّ يمنع أي مرحلة من الإغلاق فعليًا — والمشروع يحتاج تسليمًا متدرّجًا لا نظافة لا نهائية.

**73. المرحلة 1 (موبايل) — تحميل خط Tajawal فعليًا وتحويل قيد "500/700 فقط" لبوابة إنفاذ**
عند استئناف عمل نظام التصميم (المرحلة 1) وُجد `app/_layout.tsx` بـ`Font.loadAsync({})` فارغًا
منذ المرحلة صفر (دَين موثَّق في `work-plan.md` §7-ب البند 4) — الخط لم يكن يُحمَّل إطلاقًا، وقيد
"لا وزن أخف من 500" (app-complete-spec.md §7.2) كان بلا أي حارس آلي.
القرار:
- **التحميل**: `app/_layout.tsx` يستورد `Tajawal_500Medium` و`Tajawal_700Bold` حصرًا من
  `@expo-google-fonts/tajawal` (الحزمة توفّر سبعة أوزان؛ الوزنان المسموحان فقط يُحمَّلان).
- **`theme.ts` لا يصدّر `font.family` عامًا بعد الآن** — الخط ثابت الوزن (static)، فلكل وزن اسم
  عائلة مستقل (`Tajawal_500Medium`/`Tajawal_700Bold`) يُختار عبر `fontFamily` لا `fontWeight`؛
  هذا هو النمط الصحيح لخطوط RN المحمَّلة عبر `expo-font` (خلافًا لخط متغيّر الوزن على الويب).
  التصدير الجديد: `font.familyRegular`/`font.familyBold` (+ `font.familyNumber` لخط الأرقام
  أحادي المسافة حسب المنصة، و`font.lineHeightHeroNumber` للرقم البطل §8.5).
- **بوابة إنفاذ حقيقية لا توثيق فقط**: وُسِّع `scripts/checks/design-tokens.ts` (الفاحص القائم
  أصلًا لرموز التصميم) بفحصين جديدين على كل ملف `.ts`/`.tsx` في `apps/mobile`: أي `fontWeight`
  حرفي خارج `{500, 700}`، وأي `fontFamily` حرفي خارج `{Tajawal_500Medium, Tajawal_700Bold}` —
  تعبير مشتق من `font.weight*`/`font.family*` (أي مرجع للرمز المركزي في `theme.ts`) يُستثنى من
  الفحص الحرفي لأنه موثوق بنيويًا أصلًا. مُثبَت بمخالفة متعمَّدة على كلا الفحصين (`fontWeight:
  "400"` ثم `fontFamily: "Tajawal_400Regular"`) — كلاهما فشل بالرسالة المتوقعة، ونجح بعد التراجع.
التنازل يكسر: بلا هذا، أي مطوّر مستقبلي يستخدم وزنًا أخف (شائع جدًا مع Tajawal — الوزن 400
الافتراضي في أغلب أمثلة الحزمة نفسها) لا يكتشفه أحد إلا بمراجعة بصرية، بالضبط نمط الحوادث
الموثَّقة في القرار #69 (بوابة تبدو موجودة ولا تحرس شيئًا).

**74. بيئة التطوير المحلية — توثيق `TEST_DATABASE_URL`/`DATABASE_URL` بدل تركهما ضمنيَّين**
عند تشغيل `pnpm check:all` في بيئة عمل جديدة (حاوية Claude Code) فشلت خطوة التغطية بـ"لا تقرير —
تحقق من تشغيل الاختبارات"، رغم أن PostgreSQL نفسه متاح ومهيَّأ فعليًا (قاعدتا `dawajin_dev` و
`dawajin_test` ودور `dawajin` موجودون مسبقًا بنفس بيانات `.env.example`). السبب الفعلي: لا شيء
يحقن `DATABASE_URL`/`TEST_DATABASE_URL` في `process.env` محليًا — `apps/api/src/lib/env.ts` يقرأ
`process.env` مباشرة بلا `dotenv` عمدًا (الإنتاج يُحقَن من المنصة)، وCI يحقنها عبر `env:` في
`ci.yml`، لكن لا طرفية محلية تفعل ذلك تلقائيًا. بوابة تغطية "لا تعمل بصمت" لسبب بيئي هي بالضبط
شكل البوابات الصورية الخمس التي عولجت في القرار #69 — تستحق توثيقًا صريحًا لا تكرار اكتشاف.
القرار: **توثيق لا تعديل كود** — أُضيف قسم "بيئة التطوير المحلية" في `CLAUDE.md` يشرح أن هذه
الأوامر تحتاج تصدير المتغيّرات يدويًا (`cp .env.example .env` مرة واحدة، ثم `source .env` في كل
جلسة) قبل أي أمر يلمس قاعدة البيانات. لا `dotenv` يُضاف لكود الخادم — يبقى ذلك مقصودًا (حارس
`localhost` في الإنتاج بالمبدأ نفسه). `.env` المحلي مُستبعَد بـ`.gitignore` أصلًا (القرار #67).
مُثبَت: `pnpm --filter @dawajin/api run test:coverage` بعد تصدير المتغيّرات أعطى 91.01%/97.5%
(فوق الحد العام 80%، و100% على الملفات الستة المحروسة) — البوابة تعمل فعليًا، لم تكن معطوبة.

**75. المرحلة 1 (موبايل) — 15 مكوّنًا مشتركًا حسب قائمة work-plan.md §1، وقرارات تصميم غير محسومة نصًا**
بناء المكوّنات المشتركة الأربعة عشر المذكورة حرفيًا في `work-plan.md` (Badge · Button · Card ·
StatTile · SectionHeader · AppHeader · BottomTabBar · NumberStepper · حقول النموذج · الحالات
الأربع للقوائم · Chips · Segmented Control · Bottom Sheet · شريط التقدم · نمط الرسم البياني —
حقول النموذج والحالات الأربع كل منها يغطي عدة حالات فمجموع الملفات 15) كشف نقاطًا لم يحسمها
النص حرفيًا، حُسمت هنا بدل تأجيلها لأنها بنيوية تتكرر عبر كل استخدام مستقبلي:

- **ألوان شارات §8.1 (14 حالة)**: النص لا يربط كل حالة بلون. البطاقة الحمراء (`status.critical`)
  حُصرت في الحالات الثلاث المصرَّح بها حرفيًا في §7.1 ("الخطر · النزاع · الرفض فقط") — طُبِّقت على
  "رُفض" و"نزاع" و"معطّل" (عنبر خارج الخدمة = خطر تشغيلي). الباقي success/warning/info حسب طبيعة
  الحالة (نشطة/جاهزة=success، انتقالية/تحتاج متابعة=warning، معلوماتية بحتة=info). قيم مجموعة
  العنابر السبع مأخوذة حرفيًا من `HOUSE_STATUS` في `packages/shared/src/enums.ts` (مصدر الحقيقة
  الوحيد) لا من صياغة §8.1 المختصرة (مثل "تحت التجهيز" مقابل الاسم الفعلي "تحت التنظيف والتطهير").
- **Badge مكوّن عام لا مربوط بـenum محدَّد**: يأخذ `tone`+`icon`+`label` فقط. الحالات السبع لدفعات/
  سجلات (`دفعة نشطة`·`بانتظار مراجعة`·`صُحِّح`·`رُفض`·`مطابق`·`فرق مسجّل`·`نزاع`) تخلط قيمًا من عدة
  enum مختلفة (`BATCH_STATUS`, `REVIEW_STATUS`, `SHIPMENT_VARIANCE_STATUS`) لا يوجد بينها enum
  موحَّد بعد — منطق مراجعة السجل اليومي نفسه لم يُبنَ (المرحلة 2). ربط Badge بـenum محدَّد الآن
  سابق لأوانه؛ يبقى عامًا حتى تُبنى شاشات المرحلة 2/3 فتُستهلَك القيم الفعلية من الـAPI.
- **الأرقام الكبيرة (NumberStepper 34px، الرقم البطل 44px) بخط Tajawal وزن 700 لا أحادي المسافة**:
  §7.2 يفرض "الأرقام: خط أحادي المسافة" كقاعدة عامة، لكن §8.10 و§8.5 يحددان "وزن 700" صراحة لهذين
  الرقمين تحديدًا — تعليمة المكوّن الأكثر تحديدًا رجّحت هنا. الأرقام الصغيرة (StatTile، عدّادات
  الشارات/التبويبات/Segmented Control) تستخدم `font.familyNumber` (أحادي المسافة) كما هو منصوص.
- **`Card` يوفّر منطقة عائمة (`children`) لصف المؤشرات بدل فرض StatTile داخليًا** — يبقيه مكوّن
  تخطيط لا يفرض تركيبة واحدة، ويستهلكه أي مكوّن مؤشر لاحق بلا تعديل Card نفسه.
- **`BottomSheet` هو المكوّن الوحيد بظل** — §7.6 يحصر الظل بالطبقات العائمة و"الورقة الصاعدة" هي
  هذا المكوّن بالتحديد؛ كل مكوّن آخر بلا ظل بحد.
كل الخمسة عشر ملفًا يمرّون على `check:all` كاملًا (9/9) بلا استثناء قاعدة واحدة — بما فيها فاحص
وزن الخط والفاحص العام لرموز التصميم من القرار #73، دون تعديل أي منهما.

**76. المرحلة 1 (موبايل) — بنية اختبار jest-expo لقواعد RTL الملزمة فقط، وعقبتان بيئيتان حقيقيتان**
طلب صريح: اختبارات RTL فعلية على قواعد §10 الملزمة حصرًا — **لا snapshot، ولا اختبار ألوان أو
مسافات** (تلك مسؤولية `scripts/checks/design-tokens.ts`، القرار #73). النطاق المختار: القواعد
القابلة للفحص آليًا بلا ذاتية — #1 (سهم الرجوع يشير يمينًا)، #2 (الأرقام بـ`writingDirection: ltr`)،
#3 (علامة الصح غير معكوسة)، #4 (عنوان الشاشة يمين حصرًا)، #6 (نص عربي حقيقي)، #7 (أسماء طويلة/
قصيرة في نفس المكوّن). **القاعدة #5** (Auto Layout: flex+gap لا مسافات ثابتة) **استُبعدت عمدًا** —
هي بالتعريف قاعدة تخطيط/مسافات، مستبعدة صراحة بطلب المستخدم رغم ذكرها في §10.

بنية الاختبار: `apps/mobile/jest.config.js` (`preset: jest-expo`، `testMatch: **/*.rtl.test.tsx`
حصرًا — لا يلتقط أي نمط ملف اختبار آخر لاحقًا بالخطأ) + ملفات مُسمّاة `Component.rtl.test.tsx`
متجاورة مع كل مكوّن (نفس اصطلاح `apps/api` — `*.test.ts` بجانب المصدر). 5 ملفات، 11 اختبارًا:
NumberStepper وStatTile (#2) · Chip (#3 و#6 و#7) · AppHeader (#1 و#4 و#7) · Card (#6 و#7).

**عقبتان بيئيتان حقيقيتان اكتُشفتا وأُصلحتا لا التفافًا حولهما**:
1. **`jest-expo` لا يحوّل ملفات `.mjs` افتراضيًا** — نمط التحويل المدمج `\.[jt]sx?$` يستثني
   الامتداد صراحة، بينما `lucide-react-native` (تبعية موبايل أساسية، decisions.md سابقًا) يشحن
   ESM بامتداد `.mjs` حصرًا لحالة تصدير `"react-native"`. النتيجة قبل الإصلاح: `SyntaxError:
   Unexpected token 'export'` عند أول استيراد أيقونة في أي مكوّن مُختبَر — يمنع أي اختبار RTL
   لأي مكوّن يستخدم أيقونة (كلها تقريبًا) لا حالة استثنائية. الإصلاح: `jest.config.js` يستورد
   `jest-expo/jest-preset` مباشرة ويضيف مدخل `babel-jest` نفسه (نفس الخيارات) لنمط `.mjs` أيضًا،
   مع تمديد `transformIgnorePatterns` المدمج بإضافة `lucide-react-native` (لم تكن مذكورة في
   قائمة jest-expo الافتراضية، فتُستثنى من التحويل بصرف النظر عن إصلاح الامتداد).
2. **`react-test-renderer` (محرّك `@testing-library/react-native` v13 الوحيد اليوم) مُعلَن
   deprecated من فريق React نفسه** — لا بديل مستقر لاختبار مكوّنات RN تحديدًا بعد. `@typescript-
   eslint/no-deprecated` يفشل على أي استيراد لنوع `ReactTestInstance`. لا يمكن "إصلاح" قيد أداة
   بيئي خارجي؛ نطاق التعطيل حُصر بملف واحد (`apps/mobile/test-utils/rtl.ts`) هو نقطة العبور
   الوحيدة من `.props` غير المكتوب (`any`) لنوع آمن — لا تعطيل عام للقاعدة، ولا `eslint-disable`
   متناثر في كل ملف اختبار (`eslint.config.mjs`، نطاق `apps/mobile/test-utils/**/*.ts` فقط).

مُثبَت: `pnpm test` (يشمل الحزم الأربع بلا اتصال قاعدة بيانات) و`pnpm check:all` (9/9) كلاهما
أخضر بعد الإصلاحين. اختبار عملي إضافي: حُذف مدخل `.mjs` من `transform` مؤقتًا فأعاد نفس
`SyntaxError` الأصلي حرفيًا — يثبت أن الإصلاح هو السبب الفعلي لا مصادفة.

**77. CI لم يكن يعمل على الدفع لفرع العمل — كان يعمل على PR فقط (ومرة عند الدفع لـmain مباشرة)**
اكتُشف عند سؤال صاحب المنتج "لا أرى بطاقة CI" رغم عدة commits مدفوعة على
`claude/integrated-work-plan-b5i6x7`. السبب: `.github/workflows/ci.yml` كان `on.push.branches: [main]`
— يعمل عند `pull_request` (أي فرع) وعند الدفع المباشر لـ`main` فقط، لا عند الدفع لأي فرع عمل آخر.
بلا PR مفتوح، الدفعات على فرع العمل لا تُشغِّل CI إطلاقًا — بالضبط الحادثة نفسها الموصوفة في
القرار #64 ("ست دفعات وCI حمراء ونحن نظن العكس")، لكن هنا لم يكن CI أحمر بل **لم يعمل أصلًا**.
القرار: أُزيل قيد `branches: [main]` من `on.push` — يعمل الآن على الدفع لأي فرع، مع بقاء
`pull_request` كما هو. خطوة فحص رسائل commit (`check-commit-messages.ts`) تبقى مقيَّدة بـ
`if: github.event_name == 'pull_request'` لأنها تحتاج `github.base_ref` (غير موجود في حدث push)
— لا تغيير هناك، الحارس الموجود صحيح أصلًا.

**78. إثبات فشل كل اختبار RTL الأحد عشر بمخالفة متعمَّدة — جدول كامل**
تطبيقًا لمنهجية القرار #28 ("اختبار يمر بالصدفة لا يحمي شيئًا") والقرار #69 (جدول البوابات):
كل اختبار من الأحد عشر في القرار #76 أُدخلت عليه مخالفة متعمَّدة على المكوّن الحقيقي (لا الاختبار
نفسه)، وُثِّق فشله بالرسالة المتوقعة، ثم أُزيلت المخالفة ووُثِّق نجاحه من جديد. لا اختبار مرّ
بالصدفة.

| المكوّن | المخالفة المتعمَّدة | النتيجة |
|---|---|---|
| NumberStepper (رقمان) | حذف `writingDirection: "ltr"` من نمط القيمة | فشل الاختباران بـ`Received: undefined` |
| StatTile (رقم) | نفس الحذف على `styles.value` | فشل بـ`Received: undefined` |
| StatTile (سبب الغياب) | استبدال `{unavailableReason}` بشرطة حرفية `"-"` | فشل — النص المتوقّع غير موجود في الشجرة |
| Chip (الصح غير معكوس) | إضافة `transform: [{scaleX: -1}]` على أيقونة Check | فشل بـ`Received: {"transform": [{"scaleX": -1}]}` |
| Chip (لا صح دون تحديد) | عرض Check دائمًا بلا شرط `selected` | فشل — `UNSAFE_queryByType` وجد العنصر |
| Chip (أسماء متغيّرة الطول) | تجاهل `label` وعرض نص ثابت `"نص"` | فشل — النصان الأصليان غير موجودين |
| AppHeader (السهم يمينًا) | استبدال `ArrowRight` بـ`ArrowLeft` | فشل — `No instances found with node type: "ArrowRight"` |
| AppHeader (العنوان يمين، قصير وطويل) | حذف `textAlign: "right"` من نمط العنوان | فشل الاختباران بـ`Received: undefined` |
| Card (أسماء متغيّرة الطول) | تجاهل `title` وعرض نص ثابت `"نص"` | فشل — النصان الأصليان غير موجودان |

الإثبات الإضافي في القرار #76 (حذف مدخل `.mjs` من `jest.config.js` فأعاد `SyntaxError` الأصلي)
يكمل هذا الجدول لبنية الاختبار نفسها لا لمكوّن بعينه.
مُثبَت: `pnpm check:all` (9/9) و`pnpm test` أخضران بعد استعادة كل الملفات — `git status` نظيف،
لا أثر لأي مخالفة تجريبية في الشجرة النهائية.

**79. توحيد رموز التصميم مع ملف المصمم الرسمي (tokens.json v5.0) — الدفعة الأولى: الرموز فقط**
المصمم أرسل `tokens.json v5.0` رسميًا. المقارنة قيمة-بقيمة (كل مسار في الملفين) أظهرت **تطابقًا شبه تام** — الاختلافات كلها تسمية/تمثيل (مصفوفة مسافات مقابل كائن مسمّى، `regular` مقابل `medium`، إلخ) عدا أربع حالات فعلية:

- **`field.height`**: كودنا الفعلي (`FormField.tsx`) كان يستخدم **`52` فقط، حرفيًا، بلا توكن أصلًا** — لا نطاقًا كما أوحى النص. حُسم على **56** (مسحوبة من `touchTarget.primary` الموجود أصلًا، لا رقم مستقل جديد) — تغيير سلوك حقيقي (52→56)، وتوحيد مع بقية عناصر اللمس القياسية.
- **`statusEdge.side`**: محسوم عمليًا في `Card.tsx` أصلًا — `borderRightWidth`/`borderRightColor`، خاصية **فيزيائية** لا **منطقية** في React Native. RN لا يعكس الخصائص الفيزيائية تحت `I18nManager.isRTL` (خلافًا لـ`borderStartWidth`/`borderEndWidth` اللتين تنعكسان) — فـ"يمين" في الكود يبقى يمينًا بصريًا دائمًا، مطابقًا لقرار المالك ("الحد على اليمين — جهة بدء القراءة"). وُثِّق هذا صراحة في `tokens.json` (`component.statusEdge.side: "physical-right"`) وفي تعليق `Card.tsx` بدل نص المصمم المرتبك (`"inline-end-of-list-card-right"` — inline-end في RTL هو اليسار فعليًا، تناقض ذاتي).
- **`elevation.floating`**: لم يكن لدينا توكن مركزي أصلًا — قيم الظل كانت مضمَّنة حرفيًا في `BottomSheet.tsx` وحده (`shadowOpacity:0.15, shadowRadius:12`) بلا سبب موثَّق. اعتُمدت قيمة المصمم حرفيًا (`0.18`/`24px`) كتوكن `elevation.floating` مركزي. اتجاه `shadowOffset` بقي خاصًا بـ`BottomSheet` (ظل صاعد، height سالب) لأنه هندسي يعتمد على موضع المكوّن على الشاشة، لا نية تصميم عامة.
- **`$derived` (شفافية الحالة)**: كنا نخزّن 8 قيم hex محسوبة يدويًا مسبقًا (`statusDerived` في `tokens.json`). حُوِّلت لصيغة (`statusBackgroundAlpha:0.1`, `statusBorderAlpha:0.28`) تُشتق حسابيًا وقت التشغيل في `theme.ts` (دالة `withAlpha`) — يمنع انحراف حسابي مستقبلي عند تغيير لون حالة، ويطابق نمط ملف المصمم.

**إضافات جديدة بلا مقابل سابق**: `$minContentSize:15` (بوابة إنفاذ جديدة — انظر أدناه) و`component.*` كتوكن موحَّد لستة عناصر (`bottomTabBar`/`badge`/`progressBar`/`field`/`chart`/`statusEdge`) بدل أرقام مدمَجة متفرقة في 4 ملفات مكوّنات — أُعيد ربط `FormField`/`BottomTabBar`/`ProgressBar`/`Card` بها فورًا (لا توكن ميت لا يستهلكه أي كود).

**حذف حقيقي**: `typography.numberFamily` (`"ui-monospace"`) كان **ميتًا تمامًا** — `theme.ts` يحسب `font.familyNumber` مستقلًا عبر `Platform.select`، ولا مرجع واحد للحقل في أي كود (تحقُّق: `grep -rn numberFamily` صفر نتائج خارج الملف نفسه). حُذف بدل إبقائه كضلال توثيقي.

**فاحص رموز التصميم — بوابة سابعة حقيقية**: `scripts/checks/design-tokens.ts` وُسِّع بفحص `fontSize` حرفي دون `$minContentSize` (15px) خارج الاستثناءات الثلاثة (`badge`/`tabLabel`/`technicalRef`، مقروءة من `tokens.json` لا مكرَّرة). مُثبَت بمخالفة متعمَّدة: أول محاولة استخدمت `fontSize:13` فمرّت خطأً — 13 يصادف حجم الشارة المستثنى أصلًا بصرف النظر عن موضع الاستخدام، وهو حد معروف للفحص (استثناء بالقيمة لا بالسياق). أُعيدت المحاولة بـ`fontSize:10` (خارج كل الاستثناءات) ← فشل بالرسالة المتوقعة؛ وبالتراجع ← نجح.

**قيد بنيوي حقيقي اكتُشف أثناء التنفيذ — لا `react-native` في سكربتات Node خالصة**: المحاولة الأولى لجعل الفاحص "يقرأ من الملف المركزي نفسه" استوردت `font` من `constants/theme.ts` مباشرة — فشلت فورًا (`esbuild: Unexpected "typeof"` عند تحميل `node_modules/react-native/index.js`، الحزمة مكتوبة لـMetro لا Node المجرَّد). الحل: نقل أسماء عائلات الخط المحمَّلة فعليًا (`Tajawal_500Medium`/`Tajawal_700Bold`) من ثابتين حرفيين داخل `theme.ts` إلى حقل جديد في `tokens.json` (`typography.loadedFamilies`) — الفاحص يستورد `tokens.json` فقط (JSON خالص، آمن دائمًا في Node)، و`theme.ts` نفسه صار يقرأ منه بدل التكرار. مصدر واحد فعليًا، بلا استيراد `react-native` خارج بيئة RN/Metro/Jest.

**المُتبقّى مؤجَّل صراحة لدفعة ثانية منفصلة (بطلب المالك)**: ستة مخالفات مرئية في صفحة العرض (تباين بطاقة الهوية الداكنة، موضع سهم `AppHeader`، لون زر إعادة المحاولة، شبكة Chips، تعبئة SegmentedControl النشط، موضع تسمية NumberStepper) — لم تُلمَس في هذه الدفعة.

**80. المرحلة 1 (موبايل) — الدفعة الثانية: ست مخالفات مرصودة بصريًا من المالك، وسببها الجذري**
مراجعة بصرية من المالك لصفحة `/design-system` رصدت ست مخالفات. كلها أُصلحت وأُثبتت بلقطات حقيقية (Playwright على Expo web) بعد الإصلاح لا وصفًا:

- **بطاقة الهوية الداكنة — التباين منهار.** السبب الجذري **ليس في `Card.tsx`** (الذي يلوّن `title`/`subtitle` أبيض صحيحًا عبر `titleOnDark`)، بل في `StatTile` المُستهلَك كـ`children`: كل نصوصه مثبَّتة على `color.textBody` بلا وعي بسياق الأب، **والقيمة نفسها كانت `brandPrimary` — لون خلفية البطاقة بالضبط، أي غير مرئية إطلاقًا لا متدنّية التباين فقط**. الحل في `StatTile` (خاصية `onDark`) لا `Card` — القيمة تصير `textOnDark` والنصوص الثانوية أبيض 72% (`withAlpha` صُدِّرت من `theme.ts`). **رُفض حقن `onDark` تلقائيًا من `Card` عبر `cloneElement`**: القرار #75 يجعل `Card` مكوّن تخطيط عامًا لا يعرف `StatTile`؛ الحقن يعيد ربطهما. المستهلك يمرّرها صراحة كما يمرّر `tone` أصلًا.
- **زر «إعادة المحاولة» أحمر ممتلئ** (`ListState.tsx`, `statusCritical`) — إعادة المحاولة إجراء آمن، والأحمر محصور بالخطر/النزاع/الرفض (§7.1). استُبدل الزر المرسوم يدويًا بمكوّن `Button variant="secondary"` المشترك بدل تكرار تنسيق متغيّر ثالث محليًا؛ حُذفت أنماط `retryButton`/`retryLabel` الميتة.
- **`SegmentedControl` النشط بتعبئة داكنة** (`brandPrimary`) → `accentSuccess`، توحيدًا مع اصطلاح "نشط/محدَّد" في كل مكوّن آخر (`BottomTabBar`, `Chip`, الزر الأساسي). `brandPrimary` يبقى محجوزًا للهوية.
- **الشرائح ليست شبكة عمودين منتظمة** — كانت في `Row` عام (`flexWrap` بعرض محتوى حر) فاختلف عدد الشرائح لكل صف حسب طول النص. أُضيفت `chipGrid`/`chipGridItem` بعرض ثابت 48%.
- **تسمية `NumberStepper` تحت الرقم لا فوقه** — الأسوأ: لم تكن هناك تسمية مرئية أصلًا، بل كان `computedLine` (سطر النتيجة المحسوبة) يُستغَل كتسمية في صفحة العرض. أُضيفت خاصية `label` حقيقية تُعرض **فوق** العنصر بنفس نمط تسمية `FormField` (§8.11)، و`computedLine` عاد لغرضه الأصلي («= 312.5 كجم»).
- **سهم الرجوع في `AppHeader` يظهر يسارًا** — **لم يُصلَح عمدًا بقرار المالك**، سُجِّل دَينًا تقنيًا في `work-plan.md` §7-ب (البند 6) لأن سببه يحتاج تحقيقًا في سلوك `react-native-web`/RTL لا إصلاحًا أعمى. لكن **أُعيدت تسمية الاختبار** ليصف ما يفحصه فعلًا: `UNSAFE_getByType(ArrowRight)` يتحقق من **نوع الأيقونة** (اتجاه رأسها الرسومي) لا **موضعها على الشاشة**. العنوان القديم ("سهم الرجوع يشير يمينًا") كان أوسع من نطاق الاختبار، وهو ما ضلّلنا: الاختبار ينجح دائمًا والعرض خاطئ. القاعدة المستخلصة: **اختبار اسمه أوسع من نطاقه أسوأ من غياب الاختبار** — يمنح ثقة كاذبة.
- **قيد بنيوي مُسجَّل (§7-ب البند 7)**: لا قدرة تحقّق بصري آلي لأي قاعدة موضع/تخطيط. `react-test-renderer` لا يُنفّذ تخطيط Yoga فلا توجد إحداثيات x/y ليُستعلَم عنها — نفس سبب استبعاد قاعدة #5 في القرار #76، ونفس سبب عجز اختبار `AppHeader` عن اكتشاف البند 6.

**ملاحظة تشغيلية**: تشغيل `expo start --web` يعدّل `apps/mobile/tsconfig.json` ويحذف `expo-env.d.ts` كأثر جانبي (يعيد تنسيق `include` ويسقط منه `expo-env.d.ts`). أُعيدا لحالتهما قبل الالتزام — ليسا من تغييرات هذه المهمة.

`check:all` 9/9 و11 اختبار RTL خضراء بعد الدفعتين.

**81. التحقّق البصري الآلي — اللقطة المرجعية مرفوضة، وتأكيدات التخطيط معتمَدة ومجدولة**
البند 7 في `work-plan.md` §7-ب (لا قدرة تحقّق آلي لأي قاعدة موضع/تخطيط RTL) كُشف عمليًا حين ثبت أن اختبار `AppHeader` ينجح دائمًا بينما السهم يظهر في الجهة الخطأ فعليًا. طُلب تقدير الجهد لحسمه، فسُجِّل الحسم هنا كي لا يُعاد تقديره في كل مرحلة.

**❌ اللقطة المرجعية (pixel baseline) — مرفوضة:**
- **تحرس السؤال الخطأ.** تجيب عن «هل تغيّر شكل الصفحة؟» — وهو سؤال يجيب عنه المصمم بعينه مرة كل مرحلة عند بوابة الاعتماد (§15-6). ما انكسر فعليًا كان **قاعدة موضع ملزمة** (§10 قاعدة 1)، وهو سؤال مختلف تمامًا.
- **تفشل لاختلاف تصيير العربية بين البيئات.** التطبيق يستخدم Tajawal بنص عربي مُشكَّل (shaping)؛ تصيير الحروف يختلف بين حاوية التطوير ونسخة `fontconfig`/`freetype` على `ubuntu-latest`. مرجع يُلتقط في بيئة سيفشل في الأخرى — فشلٌ لاختلاف بيئة لا لخطأ كود.
- **ورفع العتبة لتمريرها يحوّلها بوابة صورية** — بالضبط نمط القرار #69 (بوابة تبدو موجودة ولا تحرس شيئًا). البديل الوحيد لتفادي ذلك (تثبيت حاوية Playwright الرسمية للوظيفة كلها) يضاعف كلفة التركيب.
- **وضريبة مستمرة**: 60-90 ثانية على كل تشغيل CI، وإعادة توليد المرجع ومراجعته بصريًا عند كل تغيير تصميم مقصود — صورة ثنائية في `git` لا يُقرأ فرقها نصيًا في المراجعة.
التقدير المُسجَّل لو أُعيد طرحها يومًا: **يوم عمل + الضريبة المستمرة أعلاه.**

**✅ تأكيدات التخطيط (`boundingBox()`) — معتمَدة ومجدولة:**
`expect(arrowBox.x).toBeGreaterThan(bellBox.x)` وما شابهها: **حتمية تمامًا، مستقلة عن الخط والتصيير**، يُقرأ فرقها نصيًا في المراجعة، بلا مرجع ثنائي ولا إعادة توليد عند كل تغيير لون. تغطي قواعد §10 الموضعية كلها — #1 و#4 و#5 (Auto Layout) التي استُبعدت صراحة في القرار #76 لانعدام هذه القدرة تحديدًا.
**التقدير: نصف يوم (~3-4 ساعات)** — نفس كلفة تشغيل Expo في CI، بلا مشكلة الخط ولا ضريبة المرجع.
**الجدولة: بند مجدول لا مؤجَّل** — يُنفَّذ في **أول مهمة تبني شاشة حقيقية**، لا على صفحة العرض وحدها. أول هدف اختباري له: سهم `AppHeader` (البند 6 في §7-ب).

**82. صفحة نظام التصميم معتمَدة رسميًا — بوابة §15-6 البشرية مغلقة**
المالك فحص `/design-system` على الجهاز واعتمدها صراحةً. هذه بوابة بشرية بطبيعتها (§15-6: "صفحة نظام التصميم أولًا، تُراجَع وتُعتمد قبل تصميم أي شاشة") — لا يمكن لأي فحص آلي إغلاقها، ولا لأي جلسة أن تعتمدها بالنيابة.
الأثر: **الشاشات الفعلية صارت مسموحة** — الحاجز الذي منع بناءها منذ المرحلة صفر ارتفع. دُمج PR #2 بعد الاعتماد (CI أخضر على `38c412e`).

**83. حذف `elevation.card: "none"` — توكن بلا مستهلك يوثّق قاعدة موثَّقة مرتين أصلًا**
أُضيف في القرار #79 ضمن توحيد الرموز مع ملف المصمم v5.0، ثم تبيّن أنه **لا يقرأه أي كود** (تحقُّق: صفر نتائج لـ`elevation.card` في `apps/mobile` و`scripts`). القاعدة نفسها ("لا ظلال على البطاقات والأزرار — إطارات فقط") مُوثَّقة بالفعل في موضعين: `$forbidden` في ملف الرموز نفسه، و§7.6 في الوثيقة الشاملة — ومفروضة عمليًا بغياب أي `shadow*` في مكوّنات البطاقات.
القرار: **حُذف.** ثلاثة مواضع لنفس القاعدة تعني احتمال تباعدها، وقيمة `"none"` لا تُفرض شيئًا لأن لا شيء يقرأها. نفس مبرر حذف `typography.numberFamily` في #79.
`elevation.floating` يبقى — له مستهلك حقيقي واحد (`BottomSheet.tsx`).

**84. تمييز "الحساب المعطَّل" — بعد مطابقة كلمة المرور حصرًا (تعديل صريح على §11)**
§11 من المواصفة التقنية تنصّ على "رسالة رفض عامة"، والتنفيذ السابق طبّقها بفلترة `isActive` **داخل الاستعلام** فصار الحساب المعطَّل يُرجع 401 العامة نفسها، واختبار تكامل متعمَّد كان يثبّت ذلك حرفيًا.
هذا كان يخلق حالة ميدانية حقيقية سيئة: المشرف يعطّل حساب مربي (انتهاء عقد موسمي) والمربي يرى "بيانات خاطئة" فيظن أنه نسي كلمة مروره، ويعيد المحاولة حتى يصطدم بحدّ الـ429، ثم يتصل بالمشرف بلا داعٍ.

**القرار:** الفلترة على `isActive` تنتقل من الاستعلام إلى **ما بعد `bcrypt.compare`**. من لا يعرف كلمة المرور يحصل على `401 invalid_credentials` تمامًا كما قبل؛ ومن يعرفها ويكون حسابه معطَّلًا يحصل على **`403 account_disabled`** برسالة «حسابك معطّل — راجع المشرف».

**لماذا هذا ليس نقضًا لـ§11 بل تحقيقًا لنيّتها:** الغرض من الرفض العام هو منع **التعداد (enumeration)** — ألا يعرف من لا يملك كلمة المرور شيئًا عن وجود الرقم أو حالته. الترتيب هنا هو الضمانة نفسها: لا معلومة إضافية تُكشف إلا **بعد** إثبات معرفة كلمة المرور، ومن أثبتها ليس مهاجمًا. و403 هو الرمز الصحيح في جدول §19 («مصادَق وغير مخوَّل»).
**ما يكسره التنازل عنه:** إعادة الفلترة إلى الاستعلام تُرجع الحالة الميدانية أعلاه؛ ونقل التمييز إلى **قبل** المقارنة يفتح تعدادًا حقيقيًا لأرقام الجوال المسجَّلة، وهو ما تمنعه §11 فعلًا.
**مُثبَت باختبارين متلازمين لا واحد:** «حساب معطَّل بكلمة مرور صحيحة ← 403» **و**«كلمة مرور خاطئة لحساب معطَّل ← 401 العامة نفسها» + «رقم غير موجود ← نفس 401 ونفس الرمز». الأول وحده يثبت التمييز، والثاني والثالث معًا يثبتان عدم التسريب — واجتماعها هو ما يثبت أن الترتيب صحيح. وأُثبت الاختبار الأول بمخالفة متعمَّدة (تعطيل الفلتر ← فشل بـ«expected 200 to be 403»، وبالتراجع ← نجاح).

**85. سبب سهم `AppHeader` — `I18nManager.forceRTL` لا يضبط اتجاه المستند على الويب (إغلاق §7-ب البند 6)**
البند 6 كان مُعلَّقًا منذ القرار #80 بنصّ صريح: «يحتاج تحقيقًا في سلوك `react-native-web`/RTL لا إصلاحًا أعمى بلا قدرة على التحقق البصري». صارت القدرة متاحة (القرار #81، منفَّذ في هذه المهمة) فأُجري التحقيق **بالقياس لا بالتخمين**:

`document.documentElement.dir` كان **غير مضبوط إطلاقًا** و`getComputedStyle(header).direction === "ltr"` رغم `I18nManager.forceRTL(true)`. المتصفح يقرأ اتجاه المستند لا حالة `I18nManager` الداخلية، فلم ينعكس `flexDirection: "row"` وظهر السهم أقصى اليسار.
القياس قبل/بعد على نفس الصفحة: قبل ضبط `dir` كان السهم عند `x=16` والجرس عند `x=350`؛ بعده انعكسا تمامًا (`350` و`16`).

**القرار:** `lib/rtl.ts` يضبط `dir="rtl"` و`lang="ar"` على `documentElement` عند `Platform.OS === "web"`. **لم يُغيَّر أي مكوّن** — المكوّنات كانت صحيحة من البداية، والبيئة وحدها لم تُخبَر أن المستند عربي. لا أثر للفرع على iOS/أندرويد فلا خطر انعكاس مزدوج.
**الدرس المُسجَّل:** الإصلاح الصحيح كان **سطرين في التمهيد** لا إعادة كتابة `AppHeader`. لولا القدرة على القياس لكان التخمين الأرجح هو قلب `flexDirection` في المكوّن — وهو ما كان **سيكسر العرض على الأجهزة الحقيقية** (حيث الانعكاس التلقائي يعمل أصلًا) بينما يُصلح الويب. هذا بالضبط ما حذّر منه القرار #80.

**86. الحد الأدنى لكلمة مرور مستخدم المستأجر — 8 محارف لا 12**
§11 تذكر «كلمة مرور 12 محرفًا» في **سطر مدير المنصة تحديدًا** ضمن قواعده الخاصة (مسار دخول منفصل · 3 محاولات · لا واجهة لإنشائه)، ولا تذكر حدًّا لمستخدمي المستأجر.
القرار: **8 محارف** لمستخدمي المستأجر (مربي/مشرف/طبيب/مالك)، ويبقى حدّ الـ12 لمدير المنصة عند بناء مساره.
لماذا: المستخدمون يدخلون كلمة المرور على هاتف تحت شمس مباشرة بيد واحدة (§1-بيئة الاستخدام)؛ فرض 12 محرفًا على مربٍّ يدخل يوميًا يدفع لكلمات قابلة للتخمين أو مكتوبة على ورقة. مدير المنصة حساب واحد نادر الاستخدام وعالي الخطورة، فحدّه الأعلى مبرَّر.

**87. توسيع نطاق مهمة شاشة الدخول — شاشة تغيير كلمة المرور بُنيت معها**
النطاق الأصلي كان «التوجيه لتغيير كلمة المرور» لا بناء الشاشة (شاشة المربي رقم 7، المرحلة 2).
القرار: بُنيت الشاشة فعليًا في نفس المهمة.
لماذا: كل حساب يُنشأ بكلمة مؤقتة و`must_change_password` (§11)، فهذا **المسار الافتراضي لكل مستخدم جديد** لا حالة حافة. التوجيه لهيكل مؤقت كان سيترك كل مستخدم جديد في طريق مسدود، ويجعل ادعاء «تسجيل الدخول يعمل لكل الأدوار» غير صحيح.
النطاق المبني: حقلان + تأكيد، **بلا سهم رجوع وبلا تجاوز بزر رجوع أندرويد** (`useBlockHardwareBack` + `gestureEnabled: false`) — إخفاء السهم وحده لا يكفي، زر النظام منفذ مستقل. وبعد الحفظ **يُعاد قراءة الملف من الخادم** لا يُفترض النجاح.

**88. `tenantName` في استجابة `needsTenantSelection` — المعرّف يُرسَل ولا يُعرَض**
الاستجابة كانت تُرجع `{tenantId, fullName, role}` فقط. عرض `tenantId` على الشاشة يخالف §12 صراحةً (❌ المعرّفات الداخلية)، والاسم+الدور وحدهما **متطابقان بصريًا** في الحالة الوحيدة التي وُجدت الشاشة لأجلها (القرار #57: طبيب مستقل يخدم مالكَين) — فيختار المستخدم عشوائيًا.
القرار: `leftJoin` على `tenants` يُرجع `tenantName`. البطاقة تعرض **اسم المستأجر عنوانًا** والاسم والدور سطرًا ثانويًا. `tenantId` يبقى في الحمولة كقيمة تُرسَل في طلب الحسم و**لا تُعرَض إطلاقًا**.
لا تسريب: كلمة المرور تحققت لهذه الحسابات قبل الاستجابة، فالمستخدم يملكها فعلًا.
مُثبَت: اختبار تكامل يؤكد الاسمين، واختبار شاشة يفحص **كل عقد النص المعروضة** أن لا رقم `tenantId` يظهر في أيٍّ منها (أُثبت بمخالفة متعمَّدة: إضافة المعرّف للسطر الثانوي ← فشل، وبالتراجع ← نجاح).

**89. عدّاد حدّ محاولات الدخول صار لكل تطبيق لا على مستوى الوحدة**
`loginRateLimit` كان ثابتًا على مستوى الوحدة (module)، و`express-rate-limit` يحفظ عدّاده داخل الكائن — فكل `createApp` في نفس العملية يشارك العدّاد نفسه.
لم يظهر الأثر حتى احتاج اختبار «الحساب المعطَّل» طلبين إضافيين، فابتلعهما حدّ الـ5 وصار الاختبار يفحص 429 بدل ما كُتب له.
القرار: `createLoginRateLimit()` تُستدعى داخل `authPublicRouter` — عدّاد مستقل لكل تطبيق.
لماذا: حالة قابلة للتغيير مشتركة على مستوى الوحدة بين نسخ التطبيق خطأ بنيوي بصرف النظر عن الاختبارات؛ والاختبار الذي يفشل بسبب طلبات اختبار آخر هو نمط القرار #28 نفسه (اختبار لا يفحص ما يدّعي فحصه).

**90. نطاق بوابة ESLint وُسِّع ليشمل كود الموبايل وتأكيدات التخطيط**
`scripts/checks/eslint.ts` كان يفحص `apps/api/src` و`packages/db/src` و`packages/shared/src` فقط — أي أن **كل كود الموبايل كان خارج البوابة** منذ المرحلة صفر، رغم أنه يمرّ عليها نظيفًا فعليًا.
القرار: أُضيفت `apps/mobile` و`layout-tests` و`playwright.config.ts` للبوابة. لم يكلّف ذلك إصلاحًا واحدًا — الكود كان نظيفًا أصلًا، وهذا بالضبط ما يجعل الإضافة مجانية وواجبة: بوابة لا تشمل الكود لا تحرسه، وأول مخالفة قادمة كانت ستمرّ صامتة.
`scripts/` نفسها ما زالت خارج البوابة (مخالفات قائمة فيها: `complexity: 24` في `design-tokens.ts` وقاعدتا `prefer-regexp-exec`) — دَين مُسجَّل في `work-plan.md` §7-ب، لا يُعالَج في مهمة شاشة.

**91. اختبارات الشاشات تعيش خارج `app/` — Expo Router يشحن كل ملف فيها كمسار**
وُضعت اختبارات الشاشات ابتداءً بجوارها (`app/auth/login.rtl.test.tsx`) على قياس النمط المعتاد في مشاريع React. تفتيش مخرَج `expo export --platform web` بعدها أظهر أن **الاختبارات صارت مسارات فعلية في شجرة التوجيه**، وأن `@testing-library/react-native` صار جزءًا من **حزمة الإنتاج** (مطابقتان في الحزمة، صفر بعد النقل).
Expo Router لا يستثني اللاحقة `.test.tsx` — كل ملف تحت `app/` مسار، بلا استثناء.

القرار: اختبارات الشاشات في `apps/mobile/screen-tests/` وتستورد الشاشة بـ`@/app/auth/login`. `jest.config.js` يطابق `**/*.rtl.test.tsx` في كل الحزمة فلم يحتج تعديلًا.
**والأهم — بوابة تمنع العودة:** `scripts/checks/nav-coverage.ts` (مالك شجرة التوجيه) يمشيها كاملة ويفشل البناء عند أي ملف بلاحقة `.test.*`/`.spec.*` داخل `app/`.
مُثبَت بمخالفة متعمَّدة: نسخ ملف اختبار إلى `app/auth/` ← فشل بالرسالة المتوقَّعة، وبحذفه ← نجاح.
**كيف اكتُشف:** ليس بفحص آلي ولا بمراجعة كود، بل بتفتيش مخرَج البناء الذي أنشأته تأكيدات التخطيط في هذه المهمة نفسها. لولا وجود خطوة بناء ويب حقيقية في `check:all` لبقي هذا مخفيًا حتى أول قياس لحجم حزمة الإنتاج.

**92. نص إرشادي داخل الحقل — إضافة إلى التسمية لا بديلًا عنها**
§8.11 تنصّ: «التسمية فوق الحقل دائمًا (لا placeholder يختفي)». قُرئ هذا ابتداءً كمنع مطلق للنص الإرشادي، فبُني الحقل بلا `placeholder` إطلاقًا.
المراجعة البصرية للمالك كشفت الفجوة: المربي أمام حقل «رقم الجوال» لا يعرف أيدخل صفرًا بادئًا أو رمز دولة، والتسمية وحدها لا تجيب عن ذلك.
القرار: `placeholder` مسموح **بشرط بقاء التسمية فوق الحقل**. ما تمنعه §8.11 هو استعمال النص الإرشادي **تسميةً** فيختفي عند الكتابة فيضيع اسم الحقل — لا التلميح إلى الصيغة. حقل الجوال يعرض `77xxxxxxx`، وتحته سطر بحجم المحتوى (15px لا 11px — نص يُقرأ تحت شمس مباشرة، §7.2): «بصفر بادئ أو بدونه، وبرمز الدولة أو بدونه — النظام يوحّد الصيغ».
لون النص الإرشادي `#4A4A4A` بالضبط — الحد المسموح تمامًا لنص فعّال (§12)، لا أفتح منه.

**93. شاشة اختيار الحساب بلا AppHeader — متغيّرا §8.8 كلاهما يفترضان مستخدمًا داخل التطبيق**
بُنيت الشاشة ابتداءً بـ`AppHeader variant="sub"` (سهم رجوع + جرس). رصد المالك بصريًا أن كليهما بلا معنى هنا:
- **سهم الرجوع بلا وجهة**: كلمة المرور تحققت بالفعل، فالرجوع لشاشة الدخول يعني إعادة إدخالها. إما «تسجيل خروج» وإما لا شيء.
- **الجرس بلا محتوى**: لم يُختَر حساب بعد، فلا جلسة ولا إشعارات لأحد.
القرار: **لا AppHeader على هذه الشاشة**، بل كتلة عنوان بسيطة (عنوان + سطر سياق) كشاشة الدخول قبلها. §8.8 تصف الهيدر الموحّد لشاشات **داخل** التطبيق بعد الدخول؛ شاشات ما قبل اختيار الحساب ليست منها — وهو نفس المبرر الذي جعل شاشة الدخول بلا هيدر من البداية.
**أثر جانبي عولج صراحة:** حذف الهيدر أفقد تأكيد §10 قاعدة 1 (سهم الرجوع) هدفه الاختباري الوحيد. نُقل التأكيد إلى `/design-system` (المستهلك الحقيقي المتبقّي للمتغيّر الفرعي) بدل أن تسقط حراسة §7-ب البند 6 مع حذف مستهلكها.

**94. تأكيد التمديد الرأسي يجب أن يُقاس على شاشة طويلة لا قصيرة**
شكوى المالك: «فراغ كبير أعلى النموذج — الحقول تبدأ عند منتصف الشاشة والزر ملتصق بالأسفل» (سببها `justifyContent: "space-between"`).
كُتب التأكيد أولًا على مقاس صغير (360×640) ظنًّا أن الشاشة الأضيق هي الحالة الأصعب. **أُثبت بمخالفة متعمَّدة أنه لا يحرس شيئًا**: بإعادة `space-between` مرّ التأكيدان كما هما — لأن المحتوى يملأ الشاشة القصيرة فلا تبقى مساحة فائضة تُوزَّع، فالتخطيطان متطابقان هناك تمامًا.
القرار: تأكيدا التمديد على **390×844** (حيث توجد المساحة الفائضة فعلًا)، ويبقى على المقاس الصغير سؤاله المختلف وحده: هل يبقى الزر مرئيًا بلا تمرير؟
بعد النقل، أُعيدت المخالفة المتعمَّدة فسقط التأكيدان بالأرقام: الفجوة `218.5` مقابل حد `120`، وبداية النموذج `339.5` مقابل حد `281`.
**الدرس:** «أثبِت البوابة بمخالفة متعمَّدة» ليس طقسًا شكليًا — هنا كشف أن بوابةً جديدة كاملة كانت ستُدمج وهي عمياء عن العيب الذي وُضعت لأجله بالضبط. نفس نمط القرار #69 و#80.

**95. سياسة كلمة المرور مصدر واحد في `@dawajin/shared` — لا رقم مكرَّر في طرفين**
كان الحد `8` مكتوبًا حرفيًا في مخطط zod بالخادم (`authProtected.ts`)، ومكرَّرًا كثابت مستقل `MIN_PASSWORD_LENGTH` في شاشة الموبايل. التكرار كان يعمل بالمصادفة: تغيير الحد في الخادم وحده يجعل الشاشة تعرض شرطًا كاذبًا، ويكتشفه المستخدم برسالة خطأ **بعد** الضغط.
ورصد المالك الوجه الأسوأ للمشكلة: الشاشة لم تكن تعرض الشرط إطلاقًا — لا `placeholder` ولا سطر شروط — فكان المستخدم يعرف الحد الأدنى بالفشل لا بالقراءة.
القرار: `packages/shared/src/password.ts` يصدّر `MIN_PASSWORD_LENGTH` و`PASSWORD_TOO_SHORT_MESSAGE` و`PASSWORD_REQUIREMENTS_HINT`. **الخادم يبني منها مخطط zod، والشاشة تعرض منها سطر الشروط** — انحراف أحدهما عن الآخر صار مستحيلًا بنيويًا لا بالانضباط.
**مصدر الرقم للتوثيق:** `apps/api/src/routes/authProtected.ts` سطر 20 قبل التعديل — `newPassword: z.string().min(8, "...")`. لا شرط تعقيد آخر إطلاقًا (لا أحرف كبيرة ولا رموز)، فسطر الشروط يقول ذلك صراحة: «8 محارف على الأقل — لا شرط آخر»، لا نصًا مخترعًا يوحي بشروط غير مطبَّقة.

**96. تخطيط شاشات المصادقة في مكوّن واحد `AuthScreen` — إصلاح الشاشة لا يساوي إصلاح العيب**
أُصلح `justifyContent: "space-between"` في `login.tsx` (القرار #94) وبقي حرفيًا في `change-password.tsx`، فرصده المالك بصريًا بعد الإصلاح مباشرة. السبب البنيوي: التخطيط كان مكرَّرًا في `StyleSheet` كل شاشة، والتأكيد الذي كُتب لحراسته كان يعرف `LoginScreen` وحدها.
القرار: `components/ui/AuthScreen.tsx` يملك التخطيط (تدفّق من الأعلى · مسافات المقياس · `KeyboardAvoidingView` · `ScrollView`)، وتستهلكه الشاشات الثلاث. صفر `justifyContent` في أي شاشة مصادقة الآن.
**والأهم — التأكيدات صارت عامة**: `AuthScreen` يصدّر مِرساتَي `auth-header` و`auth-content` (و`auth-footer` عند وجود إجراء)، فتقيس التأكيدات أي شاشة مصادقة بلا معرفة ببنيتها الداخلية، وتُطبَّق بحلقة على الشاشات الثلاث.
**قيد اكتُشف أثناء التنفيذ:** `router.push` يُبقي الشاشة السابقة مركَّبة، فـ`auth-header` يطابق عنصرين على شاشة اختيار الحساب (واحد مخفي من شاشة الدخول). كل قياس محصور بحاوية شاشته (`login-screen`/`select-account-screen`/`change-password-screen`) — بلا الحصر يقرأ القياس الشاشة الخطأ.
**إثبات بمخالفة متعمَّدة (القرار #94):** بإعادة `space-between` إلى `AuthScreen` وحده سقطت **أربعة** تأكيدات على الشاشات الثلاث — وهو في ذاته إثبات أن المكوّن مشترك فعلًا. الأرقام: الدخول (بداية `296.5` مقابل حد `281`، فجوة `205.5` مقابل `120`) · اختيار الحساب (بداية `530`) · تغيير كلمة المرور (فجوة `164.5`).
**حدّ مُسجَّل بصراحة:** تأكيد «البداية في الثلث الأعلى» **لا يميّز على شاشة تغيير كلمة المرور** — حقولها الثلاثة تجعل المحتوى طويلًا فلا يُدفَع تحت الثلث حتى مع التمديد. الحارس الفعلي لتلك الشاشة هو تأكيد الفجوة وحده. ذُكر هنا كي لا يُظنّ لاحقًا أن التأكيدين يحرسان الشاشات الثلاث بالتساوي.

**97. الشكل الرابع لتسجيل الدخول — مؤجَّل لا ملغى، وهو تقليص لسطح الاكتشاف لا إغلاق للثقب**
الشكل الرابع (الرقم ← الخادم يعيد قائمة المستأجرين ← يختار الحساب ← يُدخل كلمة مروره ← التحقق مقابل صف واحد بـ`tenant_id + phone_e164`) طُرح لإغلاق ثقب #98.
**وُصف ابتداءً بأنه «يغلق الثقب إغلاقًا كاملًا وبنيويًا» — وهذا الوصف خاطئ، صُحِّح بالتجربة لا بالتحليل.** التجربة تحت الشكل الرابع بالضبط (التحقق مقابل صف سالم وحده): `كلمة خالد (Temp1234) تطابق صف سالم؟ true`. المهاجم المتعمِّد يدخل، لأن التحقق **ينجح فعلًا** — الكلمتان متساويتان.
**التوصيف الصحيح:** يمنع **العثور العرَضي** (لا تعود شاشة الاختيار تدلّ المهاجم على وجود حساب آخر، ولا يعود ظهورها قناةً جانبية تقول «كلمتك تطابق أكثر من حساب»)، و**لا يمنع الاستيلاء المتعمِّد**.
**الحالة: مؤجَّل لا ملغى**، بقيوده الخمسة: (أ) `tenantId` إلزامي في المخطط لا اختياري — وإلا عاد السلوك القديم كما هو؛ (ب) لا يُرجَع `fullName` قبل التحقق، اسم المستأجر وحده يكفي للتمييز؛ (ج) حدّ معدّل أشدّ على مسار القائمة وثابت الزمن (بلا bcrypt فهو أرخص أداة تعداد من الدخول نفسه)؛ (د) الحسابات المعطَّلة تُخفى من القائمة؛ (هـ) ترتيب نشر مقصود، وأي نافذة توافق بلا `tenantId` تُسجَّل تدقيقيًا كي لا تدوم صامتة.
**تحذير صريح كي لا يُظن لاحقًا أن الثقب أُغلق:** حارس #99 يمنع **الاستخدام الدائم** للكلمة المؤقتة، و#100 يمنع **تصادمها**. لا هذا ولا ذاك يجعل الشكل الرابع بلا لزوم، ولا الشكل الرابع يُغني عنهما.

**98. ثقب أمني مُثبَت — استيلاء كامل عبر تصادم كلمتين مؤقتتين يدويتين**
شخصان **مختلفان** بنفس رقم الجوال في مستأجرَين مختلفين (مسموح صراحة بالقرار #23)، وتصادفت كلمتاهما المؤقتتان. الأثر المُثبَت حيًّا على قاعدة الاختبار:
- خالد (`farmer`) يُدخل **رقمه هو وكلمته هو** ← الخادم يُرجع `needsTenantSelection` بحسابَين، كاشفًا مزرعة سالم واسمه ودوره.
- يختار حساب سالم ← `200` ورمز صالح لحساب سالم (`owner`, tenant آخر).
- `GET /api/auth/me` بالرمز ← `200` وهوية سالم كاملة. **وصول فعلي بصلاحيات مالك في مزرعة لا علاقة له بها.**
**السبب الجذري ليس آلية المقارنة بل وجود كلمتين متطابقتين أصلًا** — لأن المشرف يكتب الكلمة المؤقتة يدويًا، والكلمات اليدوية قصيرة ومتكررة بطبيعتها (`Temp1234`, `123456`). `loginWithPhonePassword` يقارن الكلمة بكل صفوف الرقم عبر كل المستأجرين ويعامل كل تطابق كأنه «حساب لنفس الشخص» — افتراض ضمني غير مكتوب: *تطابق الكلمة يثبت وحدة الشخص*. وهو خطأ: يثبت فقط تساوي الكلمتين.
الخطورة عملية لا نظرية، ولا تحتاج أي مهارة: يكفي أن تظهر شاشة الاختيار.

**99. `must_change_password` كان إشارة للواجهة لا قيدًا — الحارس الخادمي `requireLiveSession`**
**مُثبَت حيًّا قبل الإصلاح:** مستخدم بـ`must_change_password = true` يسجّل الدخول ← رمز كامل ← `GET /api/settings` ← **`200`**. `requireAuth` يتحقق من توقيع JWT فقط ولا يلمس القاعدة، والحقل لا يُقرأ في أي حارس. فالكلمة المؤقتة تبقى صالحة **أبدًا** لمن يخاطب الـAPI مباشرة — وهذا يضاعف خطورة #98: المهاجم لا يحتاج فتح التطبيق أصلًا.
**القرار: حارس واحد `requireLiveSession(db)` بعد `requireAuth` مباشرة، يفرض قيدين بقراءة واحدة:**
1. `is_active = false` ← `401 account_disabled` — يغلق أيضًا دَين «رمز صالح لحساب عُطِّل يبقى مقبولًا حتى انتهائه» (§7-ب).
2. `must_change_password = true` ← `403 password_change_required` على كل مسار عدا المسموحَين.
**لماذا حارس واحد لا اثنان:** كلا القيدين يحتاج نفس الصف بنفس المفتاح (`users.id` من الرمز). فصلهما = استعلامان متطابقان على كل طلب، ونافذة تعارض بينهما لو تغيّر الصف بين القراءتين.
**الكلفة المقبولة:** استعلام واحد بمفتاح أساسي لكل طلب محمي — ثمن جعل التعطيل وإجبار التغيير **فوريَّين**. تخلٍّ متعمَّد عن نقاء `requireAuth`؛ البديل الوحيد (إبقاء الحالة في الرمز) يعني بقاء الثقبين.
**`/api/auth/me` مسموح** تحت الكلمة المؤقتة خلافًا للصياغة الحرفية «كل مسار عدا تسجيل الخروج ومسار التغيير»: التطبيق يستدعيه عند الإقلاع ليعرف **لماذا** الجلسة مقيَّدة فيوجّه لشاشة التغيير؛ ومنعه يجعل العميل يمحو الرمز ويعود لشاشة الدخول في **حلقة لا تنتهي**. ولا يكشف شيئًا جديدًا: استجابة تسجيل الدخول نفسها تُرجع هذه الحقول بعينها قبل أي حارس. (لا مسار «تسجيل خروج» في الخادم — الخروج يمحو الرمز عميلًا.)
**مُثبَت بـcurl مباشر على خادم حيّ** (لا عبر التطبيق): `/api/settings` ← 403 · `/api/auth/register-push-token` ← 403 · `/api/auth/me` ← 200 · `change-password` ← 204 · ثم `/api/settings` ← 200. وللتعطيل: 200 قبل، ثم `401 account_disabled` بنفس الرمز بلا إعادة دخول.
**ومُثبَت أن الحارس حامل للوزن بمخالفة متعمَّدة:** نزعه من سلسلة `app.ts` ← `/api/settings` يعود **200**؛ إعادته ← **403**.

**100. الكلمة المؤقتة تُولَّد آليًا — ولا تُكتب يدويًا إطلاقًا**
معالجة السبب الجذري لـ#98 لا عرَضه. ثلاث طبقات:
- **التوليد** (`apps/api/src/lib/tempPassword.ts`): 12 محرفًا من أبجدية 32 بلا محارف متشابهة بصريًا (`0/O`, `1/l/I` — الكلمة تُملى شفهيًا وتُنسخ من شاشة المشرف) ≈ 60 بتّة. `randomInt` من `node:crypto` لا `Math.random`.
- **بوابة القبول** `assertGeneratedTemporaryPassword()`: ترفض أي كلمة لا تطابق الشكل المولَّد — حارس شكل لا حارس عشوائية (لا فحص يثبت أن قيمة خرجت من مولّد آمن)، لكنه يرفض الكلمات اليدوية القصيرة/المتكررة وهي مصدر الخطر الفعلي.
- **بوابة ثابتة** `dawajin/no-manual-temp-password`: تفشل البناء عند تمرير نص حرفي إلى `bcrypt.hash`/`hashSync` في كود الخادم الإنتاجي (ملفات الاختبار مستثناة — تركيب بيانات الاختبار يجزّئ كلمات ثابتة عمدًا، وهو ما يُثبت الثقب أصلًا).
**السياسة في `@dawajin/shared` والتوليد في `apps/api` — قيد تقني لا تفضيل:** الحزمة المشتركة تُستهلَك **كمصدر TypeScript خام** (`main: "./src/index.ts"`) ويحزمها Metro داخل تطبيق الموبايل؛ أي `import "node:crypto"` فيها يدخل حزمة React Native ويكسر البناء. فالثوابت والتحقق من الشكل (نقيّان) في `packages/shared/src/tempPassword.ts`، والتوليد خادمي. الموبايل لا يولّد كلمات مؤقتة إطلاقًا.
**مسار الإنشاء `POST /api/users` غير مبني بعد (المرحلة 4)** — لا يوجد اليوم أي موضع في الخادم يُنشئ مستخدمًا بكلمة مؤقتة. الطبقات الثلاث جاهزة له، والبوابة الثابتة تمنع نسيان استدعائها. **توصية عند بنائه: ألا تقبل دالة الإنشاء معامل كلمة مرور إطلاقًا** — تولّدها داخليًا وتُرجع النص مرة واحدة، فيصير «المشرف لا يستطيع كتابتها» مفروضًا بنظام الأنواع لا باتفاق.
**صلاحية زمنية — «أول دخول فقط» لا «24 ساعة»:** `must_change_password` نفسه هو العلامة، فلا عمود جديد ولا ترحيل. خيار الـ24 ساعة (~2.5–3 س) يحتاج عمودًا وترحيلًا ومسار إعادة توليد للمشرف، وإلا قُفل المستخدم خارجًا بلا مخرج — مؤجَّل لقيمة حدّية.

**101. إبطال كل الكلمات المؤقتة القائمة — الترحيل 0004**
حارس #99 يمنع الاستخدام الدائم للكلمة المؤقتة، و#100 يمنع تصادمها مستقبلًا — لكن **أيًّا منهما لا يُبطل ما أُنشئ قبلهما**. أي حساب قائم قد تكون كلمته يدوية، ولا سبيل للتمييز بعد التجزئة (bcrypt لا يكشف الأصل).
`migrations/0004_invalidate_manual_temp_passwords.sql`: `UPDATE users SET must_change_password = true;` — الحسابات التي غيّرت كلمتها فعلًا ستُجبر مرة إضافية، وهي كلفة مقبولة مقابل ضمان ألا تبقى كلمة يدوية واحدة صالحة.
**ملاحظة تنفيذية:** الترحيل بيانات لا مخطط، فلا تولّده `drizzle-kit generate`. كُتب يدويًا وأُضيف لـ`_journal.json`، ولقطته نسخة من 0003 بـ`id` جديد و`prevId` يشير إليها — نسخها بلا تعديل يُنتج `collision` يرفضه drizzle. تحقُّق عدم الانحراف: `generate` يُرجع "No schema changes, nothing to migrate".

**102. `scripts/` و`eslint-rules/` خارج بوابة ESLint — دَين مكشوف لا مُصلَح**
`scripts/checks/eslint.ts` يمرّر لائحة مسارات صريحة لا تشمل `scripts/` ولا `eslint-rules/`.
الأثر المُقاس: `npx eslint .` على الشجرة النظيفة يُخرج **3 أخطاء حقيقية** في `scripts/checks/design-tokens.ts` وحده — `complexity 24` (الحد 10) ومرّتا `prefer-regexp-exec` — موجودة على `main` وغير مرئية للبوابة إطلاقًا. أدوات الفحص نفسها تُكتب بلا الحراسة التي تفرضها على كود التطبيق.
**لم تُصلَح في هذه الدفعة عمدًا:** نطاقها أمني (#98–#101)، وإصلاح `checkDesignTokens` يعني إعادة هيكلة فاحص عامل لا علاقة له بالثقب. الملفان المكتوبان هنا (`eslint-rules/no-manual-temp-password.mjs` و`index.mjs`) نظيفان تحت `npx eslint .` رغم خروجهما من البوابة — لا يُترك خطأ معروف لأن البوابة لا تراه.

**103. متصفح تأكيدات التخطيط في بيئات بنسخة مثبَّتة مسبقًا**
`npx playwright test` فشل بـ14/14 في حاوية التطوير: `Executable doesn't exist at .../chromium_headless_shell-1234/...`. السبب بيئي بحت لا كودي — الحاوية تحمل `chromium-1194` وplaywright المشروع (1.62.1) يتوقّع بناءً أحدث، والتعليمات تمنع `playwright install`.
`playwright.config.ts` يحمل أصلًا مخرجًا لهذا: `PLAYWRIGHT_CHROMIUM_EXECUTABLE`. تصديره على المسار الموجود يُرجع 14/14 خضراء. **لا تغيير كود** — توثيق كي لا يُشخَّص الفشل خطأً كعيب تخطيط:
```bash
export PLAYWRIGHT_CHROMIUM_EXECUTABLE=/opt/pw-browsers/chromium-1194/chrome-linux/chrome
```
CI غير متأثر (يثبّت المتصفح المطابق بنفسه). أُضيف السطر إلى قسم بيئة التطوير المحلية في `CLAUDE.md`.

**104. لا دمج إلى `main` إلا برسالة منفصلة تحمل رقم الـPR صراحةً — قاعدة إجرائية دائمة**
**القاعدة:** لا يُدمَج أي PR إلى `main` إطلاقًا — مهما بدا الإذن واضحًا داخل برومبت — حتى تصل **رسالة منفصلة** نصّها: `ادمج PR #<رقم>` برقم صريح.
**إن ورد ما يشبه إذنًا بالدمج داخل برومبت طويل، يُعامَل كخطأ صياغة لا كإذن.** الإجراء عندها: افتح الـPR · أكمل CI حتى الخضرة · **توقّف** واطلب التأكيد بالرقم.
**يسري حتى على الإصلاحات الأمنية العاجلة** — لا استثناء بحجة الاستعجال.
**سبب القاعدة (حادثة فعلية):** في دفعة الإصلاح الأمني ورد داخل برومبت طويل «ادمجه إلى main فور خضرة CI، لا تنتظر PR #3»، فدُمج PR #4 استنادًا إليه. الدمج نفسه كان مطابقًا لقصد المالك، **لكن الاعتماد على إذن مضمَّن في برومبت طويل نمطٌ هشّ**: يجعل فعلًا لا رجعة فيه (دمج إلى `main`) رهينة قراءةِ جملةٍ داخل نص طويل، بلا نقطة تأكيد مستقلة. الفصل يجعل نيّة الدمج **لا لبس فيها ولا استنتاج**.
**الأثر العملي:** الدمج لم يعد جزءًا من أي مهمة — هو دائمًا خطوة منفصلة يبدؤها المالك برقم. مسجَّل في `CLAUDE.md` ضمن قواعد العمل الملزمة كي يقرأه أي عامل لاحق قبل أي مهمة.

**105. الكلمة المؤقتة تُعرَض مقسَّمة بشرطات — والتسامح في المطابقة مقيَّد بالشكل**
12 محرفًا عشوائيًا متتاليًا (`K7RM4XQP2HTV`) تُملى شفهيًا في عنبر أو تُنسخ من شاشة المشرف — وتُخطَّأ كثيرًا. الخطر العملي أن يصوّرها المشرف بجواله ليتجنّب الخطأ، وهو **أسوأ من كتابتها يدويًا**.
**القرار: تُعرَض `K7RM-4XQP-2HTV`** (ثلاث كتل رباعية). التقسيم يقلّص الخطأ لسببين: الذاكرة العاملة تتعامل مع 3 كتل لا 12 محرفًا مستقلًا، والشرطة **نقطة مزامنة** — من يخطئ في الكتلة الثانية يعرف أين يعود، ومن يملي يقول "شرطة" فاصلًا سمعيًا. ويقلّص أيضًا خطأ نقل محرف بين الكتل.
**العشوائية لا تتأثر إطلاقًا:** الشرطات **زخرفة عرض لا جزء من التوليد**. الشكل القانوني المخزَّن والمُجزَّأ يبقى 12 محرفًا من أبجدية 32 = 60 بتّة بالضبط. لو أُدخلت الشرطة في الأبجدية لصارت جزءًا من فضاء الاحتمالات وأفسدت الحساب — لذلك `formatTemporaryPassword` دالة عرض منفصلة، والمولّد لا يعرفها.
**بوابة القبول تقبل الشكلين** (`isGeneratedTemporaryPassword` تُطبّع قبل المطابقة): المستخدم قد ينسخها كما عُرضت أو يكتبها بلا شرطات، ورفض أحدهما بلاغُ «لا تعمل» لا أمان.
**والتسامح في التحقق مقيَّد بالشكل لا عام** (`verifyPasswordAllowingTempFormat`): المحاولة الثانية (بعد حذف الشرطات) لا تقع إلا إذا كان الناتج مطابقًا **لشكل الكلمة المولَّدة بالضبط**. فكلمة مرور يختارها المستخدم وتحوي شرطة (`my-secret-pass`) لا تُمسّ إطلاقًا — مُثبَت باختبار صريح: `mysecretpass` تُرفض مقابل تجزئة `my-secret-pass`.
**لكن التقسيم يعالج العرَض لا السبب.** الجذر أن الكلمة تُنقل **بوسيط بشري** أصلًا. العلاج الجذري إيصالها للمستخدم مباشرة (رسالة نصية/رابط تفعيل) بلا مرور بالمشرف — مسجَّل في `work-plan.md` §7-ب كبند مستقل.

**106. الشكل الرابع لتسجيل الدخول — مُنفَّذ بقيوده الخمسة، وحدّه موثَّق صراحةً**
تنفيذ ما أُجّل في القرار #97. التدفّق صار: **الرقم ← قائمة المستأجرين ← اختيار الحساب ← كلمة المرور مقابل صف واحد** (`tenant_id + phone_e164`، المفتاح الفريد الفعلي).
**ما يغلقه فعلًا:** ينهار الافتراض الخاطئ في #98 («تطابق الكلمة يثبت وحدة الشخص») من أصله — كلمة شخص لا تُقارَن بصف شخص آخر أبدًا. ويُلغي **قناة جانبية** كانت في ظهور شاشة الاختيار نفسه: كانت تظهر بحسب تطابق الكلمة فتقول ضمنًا «كلمتك تطابق أكثر من حساب»؛ صارت تظهر بحسب الرقم وحده.
**وما لا يغلقه — مُثبَت باختبار مُلتزَم لا وصفًا:** مهاجم يعرف الرقم ويختار مستأجرًا آخر بكلمة متصادمة **يدخل** (`200` ورمز صالح). الاختبار مكتوب ومحفوظ في `authPublic.integration.test.ts` تحت عنوان «حدّه المُوثَّق» كي لا يُظن أن الثقب أُغلق به. الحماية الحقيقية من التصادم هي #100 (توليد آلي) و#99 (حارس الكلمة المؤقتة).
**القيود الخمسة كلها منفَّذة:**
- **(أ) `tenantId` إلزامي** في مخطط zod لا اختياري — مُثبَت: الطلب بلا `tenantId` ← `400 invalid_input`. جعله اختياريًا يُعيد السلوك القديم كاملًا، فالإلزام **هو الإصلاح نفسه** لا تفصيلًا فيه.
- **(ب) لا `fullName` ولا `role` قبل التحقق** — اسم المستأجر وحده يميّز الحسابات. إرجاع الاسم الكامل يحوّل التسريب من «هذا الرقم مسجَّل لدى مزرعة» إلى «هذا الرقم يخصّ فلانًا تحديدًا». مُثبَت باختبارين: خادميّ (`fullName === undefined`) وشاشيّ (لا اسم ولا دور معروض).
- **(ج) حدّ أشدّ على `/api/auth/accounts`**: 3/دقيقة مقابل 5 للدخول — الطلب **أرخص** (بلا `bcrypt`) فهو أنفع أداة تعداد. مُثبَت: الطلب الرابع ← `429`.
- **(د) الحسابات المعطَّلة تُخفى** من القائمة (إظهارها يسرّب حالتها قبل أي تحقق)، ورسالة «معطَّل» (#84) تبقى عبر الدخول المباشر بـ`tenantId` بعد نجاح الكلمة. مُثبَت: قائمة فارغة للمعطَّل، و`403 account_disabled` عند الدخول المباشر.
- **(هـ) النشر:** التغيير **كاسر للتوافق** — أي عميل لا يرسل `tenantId` يُرفض `400`. لذلك بُني على فرع الشاشات التي تستهلكه لا على `main` (انظر أدناه)، فيصل الطرفان معًا ولا تُنشر نافذة توافق صامتة أصلًا.
**التسريب المُدخَل، مقبول صراحةً:** من يُدخل رقمًا يعرف هل هو مسجَّل ولدى أي مزرعة. تحديد المعدّل يبطّئ المسح الشامل ولا يمنع استعلامًا مستهدفًا (طلب واحد يكفي). التبادل رابح بوضوح: تسريب «رقم مسجَّل هنا» أخفّ بمراتب من استيلاء كامل على حساب مالك.
**لماذا بُني على فرع المرحلة 2 لا على `main`:** الشاشات الثلاث التي تستهلك التدفّق (`login` · `select-account` · `password`) تعيش على ذلك الفرع وغير مدموجة. بناؤه على `main` كان **يعطّل الطرفين معًا**: خادم يطلب `tenantId` وشاشات لا ترسله. القرار #104 يحكم دمجه: يُفتح PR وتُكمَل CI ثم يُنتظَر التأكيد بالرقم.
**أثر جانبي أمني مكسب:** كلمة المرور لم تعد تُحفظ في حالة وسيطة بين الشاشات. التدفّق القديم كان يحتفظ بها في الذاكرة لإعادة الطلب بـ`tenantId` بعد الاختيار؛ الآن تُطلب **بعد** الاختيار فتُرسَل مرة واحدة ولا تُخزَّن إطلاقًا (§11).
**شاشة الاختيار تُتخطّى عند حساب واحد** — لا معنى لاختيار من عنصر واحد، والـ`tenantId` معروف فيُنتقل لكلمة المرور مباشرة.

**107. فاحص الألوان يقرأ الكود لا التعليقات — ومرجع القرار مستثنى صراحةً**
مع بلوغ ترقيم القرارات ثلاث خانات، صار `#106` **لونًا سداسيًا مختصرًا صالحًا شكلًا** (`#110066`). فأبلغ فاحص رموز التصميم عن 18 مخالفة، كلها إشارات إلى القرار في تعليقات وعناوين اختبارات — ولا واحدة منها لون. هذا عطب في الفاحص لا في الكود: بوابة تبلّغ عن كل ذكر لقرار تُدرَّب على تجاهلها، وبوابة تُتجاهَل لا تحرس شيئًا.
**القرار: فحص اللون وحده يجري على الكود بعد حذف التعليقات، ومعه تُستثنى صيغة «القرار #NNN» أينما وردت.** الحدّ المفاهيمي: «اللون الحرفي» **قيمة في الكود** لا إشارة في نثر. الاستثناء الثاني ضروري لأن عناوين الاختبارات نصوص لا تعليقات، وهي موضع الاستشهاد بالقرارات الطبيعي.
**بقية الفحوص تبقى على النص كاملًا** — والإيموجي تحديدًا: منعه يشمل التعليقات (`CLAUDE.md`)، فحصره في الكود كان ليفتح ثغرة حقيقية.
**التوسعة مُثبَتة أنها ليست ثقبًا** — ثلاث مخالفات متعمَّدة بعدها: `color: "#106"` **كقيمة في الكود** ← بُلِّغ عنها · `color: "#AB12CD"` ← بُلِّغ عنها · إيموجي **داخل تعليق** ← بُلِّغ عنه. ثم عاد الفاحص أخضر بعد الاسترجاع. الاستثناء مقيَّد بالموضع والصيغة، لا بالقيمة.
**قاعدة مكتوبة تتبع منه:** الاستشهاد بقرار يُكتب دائمًا «القرار #NNN» لا «#NNN» وحدها — صيغة موحَّدة تُبقي الاستثناء دقيقًا بلا توسيعه.

**108. commit الدمج يُعفى من صيغة conventional ببنيته لا بنصّه**
بوابة رسائل الـcommit (القرار #64) كانت تعفي الدمج بشرط نصّي: `subject.startsWith("Merge ")` — أي رسالة git التلقائية الإنجليزية وحدها. فحين كُتبت رسالة دمج بالعربية تشرح **لماذا** جرى الدمج (`merge: دمج main…` في القرار #104) رفضتها البوابة، لأن `merge` ليست نوعًا في conventional commits.
**القرار: المعيار هو عدد الآباء (`%P`) لا نصّ الرسالة.** أي commit له أكثر من أب هو commit دمج مهما كُتب فيه. هذا **تعريف بنيوي** لا استدلال نصّي: لا يُخدَع برسالة تبدأ بـ`Merge ` وليست دمجًا، ولا يفشل على دمج حقيقي بلغة أخرى.
**البديل المرفوض: إضافة `merge` إلى أنواع conventional commits.** ليست نوعًا في المواصفة، وإضافتها توسّع القاعدة كي تستوعب خطأ في الفاحص — وهو عكس الاتجاه الصحيح. **والبديل الآخر المرفوض: إعادة كتابة رسالة الدمج** — تغيّر SHA لتاريخ مدفوع فعلًا، وهو ما رفضه القرار #64 نفسه.
**مُثبَت بمخالفة متعمَّدة:** بالفاحص الجديد — 13 رسالة تمرّ وcommit الدمج معفى (`exit 0`)؛ ثم commit عادي برسالة «رسالة لا تتبع الصيغة إطلاقًا» ← فشل يسمّيه وحده (`exit 1`)؛ ثم عاد أخضر بعد الاسترجاع. الإعفاء مقيَّد بالبنية، لا بالنص.
**إصلاح عرضي معه:** المرجع كان يُقصّ إلى 8 محارف دائمًا فيُطبع `origin/main` كـ`origin/m` — القصّ مناسب لـSHA لا لاسم مرجع، فصار مشروطًا بكون المرجع SHA فعليًا.

**109. الشعار مصدر واحد — مكوّن `<Logo />` وبوابة تفرضه**
**الحالة قبل القرار — تُذكر بدقة:** لم يكن في المستودع شعار أصلًا. لا ملف أصل، ولا SVG مضمَّن في أي شاشة. الموجود كان **اسم التطبيق مكتوبًا نصًا** في شاشة الدخول (`<Text>دواجن</Text>`). فالعمل ليس استخراج تضمين قائم بل **تثبيت القاعدة قبل وصول العمل الفني**، حين تكون كلفتها صفرًا.
**القرار: كل استعمال للشعار يمرّ بـ`components/ui/Logo.tsx` وحده**، بواجهة `<Logo variant="full|icon|mono" />`. تغيير الشعار مستقبلًا تعديل في هذا الملف لا أكثر — ولا يظهر في diff أي شاشة.
**الأشكال الثلاثة مشتقّة من تعريف واحد (`BRAND`) لا ثلاثة تعريفات متوازية**: الحرف المختصر `initial` مشتقّ حسابيًا من `name`، فلا ينحرف عنه عند تغييره. `full` (رمز + اسم) لشاشات ما قبل الدخول · `icon` (مربّع مختصر) للأماكن الضيقة · `mono` (أحادي اللون) على خلفية داكنة.
**الشكل الحالي نصّي مؤقتًا** — لا عمل فني من المصمم بعد. حين يصل يُستبدَل داخل الملف وحده. **دليل نجاح التصميم أن استبدال العمل الفني لا يلمس أي شاشة.**
**القاعدة مفروضة آليًا لا بالاتفاق** (المبدأ المعماري الأول): فاحص حادي عشر في `check:all` يفشل البناء على ثلاثة أشكال للتضمين خارج الملف — اسم العلامة نصًا كعلامة · بيانات مسار SVG (`<Path d=…>`) · استيراد أصل شعار من `assets/`.
**دقّة الفاحص كلّفت تصحيحين، وكلاهما وجده الفاحص لا المراجعة:**
- **اسم العلامة كلمة عربية شائعة.** أول نسخة طابقت أي ورود للنص، فأبلغت عن سطر «نظام إدارة مزارع دواجن التسمين» — استعمال لغوي عادي لا تضمين. المطابقة صارت على الاسم **وحده** عقدةَ نصّ (`>دواجن<`) أو قيمةً حرفية تساويه بالضبط.
- **نمط استيراد الأصل طابق استيراد المكوّن نفسه** (`from "@/components/ui/Logo"`)، أي أبلغ عن الاستعمال الصحيح. صار مقيَّدًا بملفات الوسائط (امتداد صريح أو تحت `assets/`).
**ولا قائمة استثناءات:** `Chart.tsx` يستعمل `react-native-svg` بشرعية (تمثيل بيانات لا عمل فني) ويمرّ بلا استثناء، لأن الممنوع هو **بيانات المسار** لا SVG كلّه. قائمة استثناءات تُوسَّع لاحقًا حتى تُفرَّغ البوابة.
**والاختبار يستورد الاسم ولا يكتبه:** أول نسخة من `Logo.rtl.test.tsx` كتبت «دواجن» حرفيًا — **فأوقفها الفاحص**. لو مرّت لصار تغيير العلامة تعديلًا في ملفين، أي نقضًا للقاعدة من داخل اختبارها. `BRAND` صار مُصدَّرًا ليستورده الاختبار.
**مُثبَت بأربع تجارب:** اسم العلامة نصًا في شاشة ← بُلِّغ عنه · `<Path d="…">` في شاشة ← بُلِّغ عنه · `import from "@/assets/brand-logo.svg"` ← بُلِّغ عنه · الاستعمال الصحيح (استيراد المكوّن + الاسم داخل جملة) ← أخضر.

**110. تضييق استثناء فاحص الألوان — إحالة القرار وحدها لا التعليقات كلها**
تصحيح للقرار #107. علاجُه حذف **التعليقات كلها** قبل فحص اللون، وذلك أوسع من المشكلة التي عالجها.
**المشكلة كانت:** مرجع قرار ثلاثي الخانات (`#106`) لون سداسي مختصر صالح شكلًا. **الحل كان يجب أن يستثني نمط الإحالة، لا كل ما في التعليقات.**
**الأثر الفعلي للتوسّع — مُثبَت بمخالفة متعمَّدة لا مُستنتَجًا:** `// كان #AB12CD سابقًا` ← مرّ · `// كان #999999 سابقًا` (رمادي أفتح من الحد) ← مرّ · لون في تعليق كتلي ← مرّ. ثلاثة ألوان حقيقية تفلت، وهو ما لم يذكره القرار #107 لأنني لم أختبره: **أثبتُّ يومها أن الإيموجي داخل تعليق ما زال يُلتقط، ولم أثبت اللون داخل تعليق.** إثبات جزئي عن بوابة مخفَّفة يقرأ كإثبات كامل، وهو أسوأ من لا إثبات.
**القرار: الاستثناء مقصور على `القرار #NNN`** — الكلمة قبل الرقم (وتثنيتها/جمعها)، ثم رقم من ثلاث خانات فأقل، مع سلسلة معطوفة. **التعليقات تُفحَص كبقية الكود**: لون حرفي فيها مخالفة أيضًا.
**ولا يمكن تمويه لون بإحالة مجاورة:** النمط يُحذف بذاته فقط، فتعليق `// القرار #106` قبل `color: "#106"` لا يُنقذ الثاني — مُثبَت.
**وفرض توحيد صيغة الاستشهاد جاء تبعًا:** اشتراط كلمة «القرار» أمسك ثلاثة مواضع كانت تكتب `(القيد ب في #106)` و`(القيد أ، #106)`. وُحِّدت إلى `القرار #106` كما تقتضي قاعدة #107 نفسها. النمط الأضيق **يفرض** القاعدة بدل أن يكتفي بذكرها.
**الدرس المنهجي:** عند تخفيف بوابة، الإثبات المطلوب ليس «ما زالت تلتقط شيئًا» بل «ما زالت تلتقط **كل صنف كانت تلتقطه** عدا الصنف المستثنى صراحة». تعداد الأصناف قبل التخفيف، لا بعده.

**111. جرد البوابات: أربع منها معياريها نصّي لا بنيوي — مُسجَّل لا مُعالَج**
خُفِّفت بوابتان في جلسة واحدة (#108 رسائل الـcommit، #107/#110 الألوان)، وكل تخفيف كان مبرَّرًا منفردًا. النمط المتكرر ليس الصدفة: **معيار نصّي يُخطئ في الاتجاهين** — يمرّر ما يجب منعه، ويمنع ما يجب تمريره.
**البوابات البنيوية (سبع):** المسارات المكررة وتغطية OpenAPI (كلتاهما تقرأ شجرة توجيه Express الحقيقية — وقد استُبدل بهما ماسح نصّي كان يفوّت مسارًا مسجَّلًا بمتغيّر) · typecheck · ESLint · Prettier (الثلاث تُحلّل شجرة بناء) · التغطية (تشغيل فعلي بقياس) · تأكيدات تخطيط RTL (تخطيط Yoga حقيقي بإحداثيات).
**البوابات النصّية (أربع) — كلها مُثبَتة بمخالفة متعمَّدة:**
- **صحة enum** — الأشد. «القيمة مكتوبة» = `apiSource.includes("\"value\"")`، مطابقة نصّية في أي موضع من الملف. مُثبَت: إضافة تعليق `// "owner"` وحده إلى ملف في طبقة الكتابة قلبت `USER_ROLE` إلى «ملموس» وأفشلت البوابة. والعكس أخطر: قيمة تُكتب عبر متغيّر (`status: newStatus`) لا تُرى إطلاقًا. وتحديد «طبقة الكتابة» نفسه نصّي (`includes(".insert(")`).
- **رموز التصميم** — مُثبَت: `fontSize: 10` يفشل، بينما `fontSize: font.size.content - 5` (القيمة نفسها 10، دون الحد 15) **يمرّ** لأن `extractFontLiteral` تتخطّى كل تعبير يذكر `font.size`. و`borderRadius: ODD_RADIUS` (‏7، خارج المقياس الخمسي) يمرّ لأن النمط يطابق الأرقام الحرفية وحدها.
- **تغطية التنقل** — شقّها الأول نصّي: `Tabs.Screen name="…"` بـregex. مُثبَت: تحويل اسم شاشة مسجَّلة بصحة إلى متغيّر أنتج **فشلًا زائفًا** («غير مسجَّلة») رغم سلامة التسجيل. شقّها الثاني (منع ملفات غير المسارات تحت `app/`) أمين للقاعدة الحقيقية لأن Expo Router نفسه يقرّر بالاسم — لكنه محصور بلواحق معدودة.
- **الشعار مصدر واحد** (#109) — regex كسابقاتها، ويُذكر هنا لأنه من عملي في الجلسة نفسها.
**لا إصلاح الآن — بطلب المالك صراحةً** («أريد القائمة لا الإصلاح»). مُسجَّل في `work-plan.md` §7-ب البند 11. العلاج المبدئي قراءة AST عبر TypeScript compiler API (متاح في المستودع أصلًا) بدل regex — يُجدوَل في المرحلة 7.
**ملاحظة على الحدّ:** ليست كل مطابقة نصّية عيبًا. فحص صيغة conventional commits نصّي بطبيعته لأن موضوعه **نصّ**، وكذلك منع الإيموجي. العيب حين يكون **الموضوع بنيةً** (مسار، تسجيل شاشة، كتابة قيمة، حجم خط محسوب) ويُفحص بالنص.

**112. المستوى الثالث: الموقع ← المزرعة ← العنبر، ومصادر الطاقة على المزرعة**
الهرم في الوثيقة الشاملة §3.1 كان `المستأجر ← المزرعة ← العنبر`، وهو **ناقص مستوى**: الموقع الواحد قد يقوم فيه أكثر من مزرعة. سبعة مواقع في ميدان المالك (الجبل · الكرنة · الصعيد · الطويلة · الجاح · الخماسية · الحمراء).
**الشكل: جدول `sites` + `farms.site_id NOT NULL`.** ورُفض بديلان: إعادة تسمية `farms` → `sites` ثم `farms` جديد — `farms.id` مرجع لثلاثة مفاتيح أجنبية (`houses` · `inventory_movements` · `shipments`) فتصير إعادة هيكلة لا إضافة؛ و`farms.parent_id` ذاتي المرجع — شجرة بلا نوع لا تمنع مزرعة تحت مزرعة.
**واسم المزرعة فريد داخل موقعها** (`UNIQUE (site_id, name)`) لا عبر المستأجر: «مزرعة 1» في الجبل وفي الحمراء اسمان مشروعان — مُثبَت.
**والطاقة انتقلت من العنبر إلى المزرعة، وحُذف عمود العنبر.** المولّد يخدم مزرعة فيها أكثر من عنبر، فحملها على العنبر إدخال مزدوج لواقع واحد. **ورُفض حملها في المستويين** رغم أن العنبر المغلق يموت طيره بانقطاع التهوية: لا شيء في النظام يستهلك الحقل اليوم (لا إنذار انقطاع ولا تقرير طاقة)، فالإدخال المزدوج كلفة بلا عائد. مسجَّل في `work-plan.md` §7-ب البند 14 ليُبحث عند بناء الإنذار المبكر.
**والقائمة قيمتان لا أربع:** حُذفت «حكومية» و«تجارية» — لا وجود لهما في الميدان ولا تصلان أماكن العنابر. إدراجهما كان وصفًا لواقع غير موجود.
**وتبقى قائمة لا حقل نعم/لا** رغم أن القيمتين تبدوان قابلتين للاختزال إلى «هل معها شمسية؟»: القيد «لا مزرعة بلا مصدر طاقة» **غير قابل للتعبير عنه** بحقل منطقي — `has_solar = false` لا يفرّق بين مزرعة بمولّد ومزرعة بلا طاقة، فيسقط القيد نفسه. والقائمة تعبّر عنه بـ`cardinality >= 1`. والمخاطرة غير متماثلة: التراجع لاحقًا إلى نعم/لا سهل، والعكس يحتاج ترحيلًا لا يمكنه استرجاع أي مزرعة كان فيها مولّد.
**مُثبَت على القاعدة بسبع مخالفات متعمَّدة:** موقع مكرَّر ← `sites_tenant_name_uq` · مزرعة بلا موقع ← `not-null` · طاقة `NULL` ← `not-null` · مصفوفة فارغة ← `farms_power_sources_not_empty` · «حكومية» ← `invalid input value for enum` · «مزرعة 1» في موقعين ← **نجح** · «مزرعة 1» مرتين في موقع واحد ← `farms_site_name_uq`.
**إضافة خالصة لا تمسّ مدموجًا:** `houses` و`inventory_movements` و`shipments` تبقى على `farm_id` بلا تغيير، ولا مسار من المسارات التسعة يلمس `farms`، والجدولان فارغان — فلا ترحيل بيانات ولا نافذة توافق. **وهذه أرخص لحظة ممكنة لإدخال المستوى**؛ بعد بناء المزارع أو العنابر كان يصير إعادة هيكلة.
**المواصفتان عُدِّلتا بإذن المالك:** الشاملة §3.1 و§5-د/2، والتقنية §7.1 و§9 و§17 و§12.2. **والمستوى الثالث وتصحيح قائمة الطاقة جاءا من واقع ميداني لم تعرفه الوثيقة** — لا من إعادة تصميم.

**113. `LOCATION_TYPE` لا يُعاد تسميته — يُوثَّق حصره بالمخزون**
مع دخول «الموقع» مصطلحًا حقيقيًا في الواجهة، عاد `LOCATION_TYPE = ["warehouse","house"]` مظنّة التباس.
**القياس:** الاسم حيّ وحامل للحمل — أربعة أعمدة (`inventory_movements` · `stocktakes` · `wastage` · `inventory_transfers`)، ونوع `location_type` في Postgres، و**قيد `CHECK` يبني عليه منطق موقع الحركة**، و`inventoryBalance.ts` مع اختباراته.
**القرار: لا إعادة تسمية.** المكسب لغوي والمخاطرة حقيقية (ترحيل يغيّر نوعًا وأربعة أعمدة ويلمس منطق مخزون مُختبَرًا). الفصل يقع بالتسمية الجديدة: `sites` للمواقع الجغرافية، و`location_type` مقصور على **نوع موقع المخزون** — موثَّق صراحةً في §7.1 من المواصفة التقنية وفي `schema/farms.ts`.
**القاعدة المستخلَصة:** لا يُعاد تسمية اسم حيّ لرفع لبس مفهومي يمكن رفعه بالتوثيق وتسمية الجديد.

**114. نقل مزرعة بين المواقع — القيد على الحالة لا على الوقت**
مع منع النقل (الموقع جغرافيا) ومنع الحذف (القرار في §7-ب البند 13)، يترك خطأُ إدخال واحد مزرعةً في موقع خاطئ إلى الأبد.
**القرار: `site_id` قابل للتعديل من المالك ما دامت المزرعة بلا عنابر، ويُجمَّد فور إضافة أول عنبر.**
**ورُفضت النافذة الزمنية:** الساعة رقم اعتباطي لا يصف الخطر — الخطأ يُكتشف حين يفتح أحدهم الشاشة، وقد يكون اليوم الثالث.
**الخطر الحقيقي ليس النقل بل إعادة كتابة معنى التاريخ:** مزرعة تنتقل بعد أن صار تحتها عنابر ودفعات وسجلات تجعل كل تقرير سابق مجمَّع حسب الموقع يتغيّر أثرًا رجعيًا. ومزرعة فارغة لا تاريخ لها — نقلها تصحيح بيانات بأثر صفر. فالاختبار «هل لها عنابر؟» يطابق الخطر تمامًا، ويُفحص باستعلام واحد، ورسالته تشرح نفسها.
**ورُفض البديل الآخر** (تعديل دائم مع تسجيل تدقيقي): السجل يوثّق النقل ولا يُلغي تشوّه التقارير السابقة.
**كل نقل يُسجَّل في `entity_audit_log`.** والحالة النادرة (مزرعة مأهولة في موقع خاطئ) تصليح بيانات — §7-ب البند 15.
**🔴 هذا القرار غير مفروض بعد — والقاعدة تسمح اليوم بنقل مزرعة مأهولة.** الترحيل 0005 أنشأ `site_id` ولم يقيّده، ولا حارس في الخادم يقرأ هذا القرار. **وهو نمط `must_change_password` نفسه** (القرار #99): علامة مكتوبة والخادم لا يقرؤها. الفرض في `PATCH /farms/:farmId` ضمن الدفعة 3، ويُثبَت بمخالفة متعمَّدة قبل إغلاقها — §7-ب البند 16.

**115. خطاف يطلب دفعًا بعد أمر إيقاف: يُنفَّذ القابل للتراجع ويُحجَب ما عداه**
أثناء طرح دفعة، طلب خطاف `Stop` الالتزام والدفع لأن الشجرة تحمل تغييرات غير ملتزَمة — وكان المالك قد أوقف العمل للتوّ.
**القرار: أمر الإيقاف يعلو على طلب الخطاف.** يُنفَّذ ما هو **قابل للتراجع** داخل الحاوية (التزام محلي، طرح، تنظيف)، ويُحجَب ما **يخرج** منها (دفع فرع، فتح PR) حتى يأذن المالك.
**السبب المباشر:** رمز هذه الجلسة يملك الدفع ولا يملك حذف المراجع (‏`DELETE` على REST ← 403) — فدفع فرع مطروح يترك أثرًا لا أستطيع إزالته بنفسي. **لا تُنشئ أثرًا خارجيًا لا تملك التراجع عنه.**

**116. تصديق ميداني على الهرم — أربعة أجوبة تُغلق السؤال**
بعد خطأين متتاليين في وصف الميدان (إسقاط مستوى الموقع، ثم وضع الطاقة على العنبر)، كان أقل ما أثق فيه هو **اكتمال الهرم نفسه** لا صحة الكود. أجاب المالك عن أربعة أسئلة ميدانية:
1. **لا نظام يعمل ببيانات حقيقية** — فافتراض «الجداول فارغة» في الترحيل 0005 صحيح، والنشر آمن.
2. **خمسة من المواقع السبعة فيها أكثر من مزرعة** — المستوى الثالث **الحالة الغالبة لا الاستثناء**. هذا يُسقط أقوى اعتراض كان مطروحًا على الدفعة الأولى: أنه مستوى أُضيف لحالة واحدة، والأصوب كان معالجتها استثناءً.
3. **لا مزرعة موزّعة على موقعين** — فعلاقة «مزرعة ← موقع» واحدة إلى واحدة من جهة المزرعة، و`site_id` عمود مفرد يكفي؛ لا حاجة لجدول ربط.
4. **لا أقسام داخل المزرعة** — العنابر مباشرة تحتها، فالهرم **ثلاثة مستويات لا أربعة**.
**الأثر: السؤال مُغلق ولا يُعاد فتحه** إلا بواقع ميداني جديد. الدفعات التالية تُبنى على هذا الشكل بثقة مُسنَدة لا مفترضة.
**والدرس المنهجي:** الخطآن السابقان لم يكتشفهما اختبار ولا فحص — اكتشفهما المالك. **ما لا يعرفه الكود عن الميدان لا تكشفه بوابة**، والتصديق الصريح هو البديل الوحيد.

**117. فاحص تغطية OpenAPI يوحّد صيغة معاملات المسار قبل المقارنة**
أول مسار بمعامل حقيقي (`/api/sites/:siteId`) أسقط الفاحص العاشر: Express يكتب المعامل `:siteId` وOpenAPI يكتبه `{siteId}`، والمقارنة كانت **حرفية**. فأبلغ عن المسار الواحد **مرتين ومتناقضًا**: «مسجَّل في Express بلا توثيق» و«موثَّق بلا خادم» معًا.
**لم يظهر العطب قبل الآن** لأن المسارات التسعة السابقة كلها ثابتة بلا معاملات.
**القرار: التوحيد قبل المقارنة** (`{x}` ← `:x`)، وهو تعديل بوابة بموجب الاستثناء الوحيد المأذون: **أنها كسرت بناءً صحيحًا فعليًا**.
**والتوحيد لم يُرخِ البوابة — مُثبَت بثلاث مخالفات متعمَّدة:** مسار موثَّق حُذف خادمه ← التُقط · مسار قائم حُذف توثيقه ← التُقط · **واسم معامل مختلف بين الطرفين** (`{id}` مقابل `:siteId`) ← التُقط بأربعة أسطر. الأخيرة تُظهر أن الفاحص ما زال يميّز المعاملات بأسمائها لا يتجاهلها.

**118. المواقع خادميًا — القراءة لكل الأدوار والكتابة للمالك حصرًا**
أول مسار أعمال في المرحلة 2-أ، وأول مسار في المشروع كله يأخذ **معرّف كيان في الرابط**.
**الصلاحية:** الكتابة (`POST` و`PATCH`) للمالك حصرًا تنفيذًا لسدّ ثغرة §12.2 (القرار #112). **والقراءة مفتوحة لكل الأدوار** لأن كل دور يحتاج معرفة موقع عنبره — وحصرها بالمالك كان سيعطّل الميدان بلا مكسب أمني.
**والعزل بالوجود ثم التعيين (المبدأ السادس) مُنفَّذ ومُثبَت:** موقع مستأجر آخر يعطي **404 لا 403** — تمييزهما يجعل الرد أداة تعداد لمواقع الآخرين. ومعرّف غير موجود إطلاقًا يعطي **نفس 404 بالضبط**، فلا فرق يُبنى عليه استدلال.
**والحارسان مُثبَت أنهما حاملان للحمل لا زينة:** حذف `requireRole("owner")` من `POST` ← سقط اختباران · حذف شرط `tenantId` من قراءة الموقع ← سقط اختبار العزل · ثم عاد الكل أخضر بالاسترجاع. **بوابة لا يسقط شيء بحذفها لا تحرس شيئًا.**
**لا `DELETE`** (§7-ب البند 13)، و**الاسم وحده قابل للتعديل** — لا شيء آخر في الموقع.
**وتحقّق `siteId` في طبقة المسار لا الخدمة:** `Number("abc")` يعطي `NaN` فيصل الاستعلام ويعطي 500 بدل 400 — والخطأ في **صيغة الطلب** لا في الخادم. مُثبَت: `/api/sites/abc` ← 400.
**وفحص تكرار الاسم يجري تحت المعاملة**، والفهرس الفريد يبقى الحارس الأخير: الفحص لأجل **رسالة مفهومة** (409 `duplicate_name`) لا لأجل الأمان.

**119. مساران لنفس الخطأ يجب أن يعطيا نفس الرمز والرسالة — والعيب انكشف بتعطيل الفحص عمدًا**
منع تكرار اسم الموقع له **حارسان**: فحص مسبق في طبقة الخدمة (لرسالة مفهومة)، والفهرس الفريد خلفه (للأمان تحت التزامن). وكانا يعطيان جوابين مختلفين:
- الفحص المسبق ← `409 duplicate_name` · «يوجد موقع بهذا الاسم»
- الفهرس ← `409 duplicate` · «قيمة مكررة — يوجد سجل بنفس البيانات» (الرسالة العامة، لأن `sites_tenant_name_uq` لم يكن في جدول الرسائل)
**الحالة الوحيدة التي تبلغ الفهرس هي التزامن** — ضغطتا «حفظ» في اللحظة نفسها. فالمستخدم يرى رسالتين مختلفتين لنفس الخطأ بحسب التوقيت، والشاشة تحتاج معالجة شكلين.
**القرار: الرمز والرسالة يتطابقان في المسارين.** أُضيف `CONSTRAINT_OVERRIDES` لقيدَي المواقع والمزارع برمز `duplicate_name` ونصّ الفحص المسبق حرفيًا. **والقيود القائمة لم تتغيّر**: الافتراضي يبقى `duplicate`، فاختبار `users_tenant_phone_uq` القائم يمرّ كما هو — تغيير الافتراضي كان سيكسر سلوكًا مدموجًا بلا داعٍ.
**والقاعدة العامة:** كل قيد يقابله فحص مسبق يجب أن يحمل رمزه ورسالته — وإلا صار الحارس الأخير يتكلم لغة أخرى.
**طريقة الاكتشاف تستحق التوثيق بقدر العيب:** لم يسقط اختبار، ولم تُبلّغ بوابة. **عُطِّل الفحص المسبق عمدًا** ليصطدم كل طلب بالفهرس، فظهر الفرق فورًا. الاختبارات كلها كانت تمرّ بالمسار الأول فلا تبلغ الثاني قط — **مسار لا يسلكه اختبار هو مسار غير مُختبَر مهما بلغت التغطية.**
**والإصلاح مُثبَت بنفس الطريقة:** أُعيد التعطيل بعد الإصلاح فمرّت الاختبارات العشرون كلها — بينما كانت تسقط قبله برسالة `expected 'duplicate' to be 'duplicate_name'`.
**واختبار الفهرس مكتوب بالوحدة لا بالتكامل**: بلوغه فعليًا يحتاج تزامنًا حقيقيًا لا يُستدعى بثبات، فيُستدعى المترجِم بخطأ pg مُصطنع ويُفحص بيقين. ومعه اختبار تكامل يؤكّد **الثبات** (طلبان متزامنان ← 201 و409 `duplicate_name`) لا أيّ المسارين وقع — فيصحّ في الحالتين ولا يهتزّ.
**وأُضيف اختبار الطبيب صراحةً:** كان مرفوضًا بالاستدلال (الحارس قائمة بيضاء تسمح للمالك وحده)، والاستدلال سليم — **لكن قاعدة المشروع الإثبات بالتجربة**. الأدوار الثلاثة غير المالكة كلها مُثبَتة الآن.

**120. اتساق المستأجر بين المزرعة وموقعها — قيد بنيوي لا حارس إجرائي**
**ثقب مُثبَت على القاعدة، اكتُشف بالطريقة التي أوصى بها المالك:** تعطيل الحارس الأول والنظر فيما خلفه.
حُذف `assertSiteInTenant` من إنشاء المزرعة، فكانت النتيجة **`201` لا خطأ**: مزرعة تُنشأ للمستأجر أ داخل موقع المستأجر ب. والسبب أن المفتاح الأجنبي المفرد على `site_id` **راضٍ** — الموقع موجود، وإن كان لغير صاحب المزرعة. وأُكِّد بإدراج مباشر على القاعدة: صف «مزرعة المستأجر 1 داخل موقع المستأجر 2» قُبل صامتًا.
**فلم يكن خلف الحارس شيء.** والحارس إجرائي: أي مسار كتابة جديد لا يمرّ به يُعيد الثقب — وهو ما يخالف المبدأ الأول (الفرض المركزي) والسابع (العزل المطلق).
**القرار: مفتاح أجنبي مركَّب** `farms(site_id, tenant_id) → sites(id, tenant_id)`، ومعه قيد تفرّد `sites(id, tenant_id)` لأن Postgres يشترط مرجعًا فريدًا صريحًا ولو كان `id` مفتاحًا أساسيًا.
**مُثبَت بعد الإصلاح:** الإدراج عبر المستأجرين ← مرفوض · **والتحديث كذلك** (`UPDATE farms SET site_id=...`) ← مرفوض · والإدراج السليم داخل نفس المستأجر ← نجح.
**والحارس الإجرائي يبقى** رغم القيد: هو ما يعطي `404 «الموقع غير موجود»` بدل خطأ مفتاح أجنبي خام، ويحفظ قاعدة «الوجود ثم التعيين». القيد حارس أخير لا بديل.
**وأثر جانبي كاشف:** الترحيل فشل أول مرة على قاعدة الاختبار لوجود **صف مخالف حقيقي** خلّفته تجربة تعطيل الحارس — فرفضه القيد الجديد. دليل عملي أن القيد يعمل، وأن الترحيل يفشل بصوت عالٍ على بيانات مخالفة بدل أن يمرّ فوقها.
**والقاعدة العامة:** كل مفتاح أجنبي بين جدولين يحملان `tenant_id` يجب أن يكون **مركَّبًا** — المفرد يقبل الخلط عبر المستأجرين. تُراجَع بقية العلاقات عند بنائها (`houses → farms` أولها، في الدفعة 4).

**121. المزارع خادميًا — وفرض القرار #114 مُنفَّذًا لا موسومًا**
`GET/POST /sites/:siteId/farms` · `GET/PATCH /farms/:farmId`. القراءة لكل الأدوار والكتابة للمالك حصرًا — نفس قسمة المواقع (#118)، والأدوار الثلاثة غير المالكة **مُثبَتة صراحةً** لا استدلالًا.
**والقرار #114 صار مفروضًا** بعد أن كان موسومًا في §7-ب البند 16 بأنه بلا حارس: نقل `site_id` مرفوض بـ`409 farm_has_houses` إن كان للمزرعة عنبر فأكثر.
**الفرض في طبقة الخدمة لا المسار**: القاعدة تسمح بالنقل (لا `CHECK` يمنعه)، فلو بقي في المسار لأمكن تجاوزه بأي مستدعٍ آخر. والفحص **تحت المعاملة** التي تُجري التحديث، فلا نافذة بين الفحص والكتابة.
**ومُثبَت بما يتجاوز رمز الرد:** بعد الرفض تُقرأ المزرعة من القاعدة ويُتحقَّق أن `site_id` **لم يتغيّر فعلًا** — رفضٌ بلا منع لا قيمة له. وحذف عدّاد العنابر أسقط الاختبار، فالحارس حامل للحمل.
**وحدّ التجميد دقيق:** الاسم ومصادر الطاقة يبقيان قابلين للتعديل في مزرعة مأهولة — التجميد للموقع وحده. وإرسال **نفس** الموقع لمزرعة مأهولة يعطي 200 لا 409، لأنه ليس نقلًا.
**والنقل يُسجَّل تدقيقيًا بفعل `move` لا `update`** — تمييز يجعل تتبّع التاريخ ممكنًا.
**أربعة حرّاس مُثبَت أنها حاملة للحمل** بحذف كلٍّ منها: عدّاد العنابر (#114) · `requireRole("owner")` · شرط المستأجر في القراءة · حارس الموقع في الإنشاء — وهذا الأخير كشف الثقب في القرار #120.

**122. كل مفتاح أجنبي بين جدولين يحملان `tenant_id` صار مركَّبًا — أربعون مفتاحًا في ترحيل واحد**
تعميم القرار #120 على المخطط كله بعد جرد كامل: **41 مفتاحًا** بين جدولين يحملان `tenant_id`، **واحد مركَّب** (‏`farms → sites`) و**أربعون مفردة**.
**وفُحصت البدائل الثلاثة فلم يمنعها شيء:** لا قيد `CHECK` يذكر `tenant_id` في المخطط كله · لا قيد تفرّد `(id, tenant_id)` لأي جدول غير `sites` · والمفتاح المفرد يتحقق من الوجود لا من المالك. **الأربعون ثقوب بنيوية فعلية** — مُثبَت على `houses → farms` بإدراج مباشر: عنبر المستأجر 5 داخل مزرعة المستأجر 6، قُبل صامتًا.
**قرار المالك: تُصلَح كلها الآن في دفعة مستقلة قبل الدفعة 4**، وحجّته رقمي أنا: الجداول فارغة فالكلفة ترحيل واحد، وبعد امتلائها تصير أثقل بمراتب — وإصلاح `houses` وحدها يعني تكرار النقاش ست مرات على سطح أكثر امتلاءً في كل مرة.
**النتيجة: 7 قيود تفرّد + 39 مفتاحًا مركَّبًا في الترحيل 0007.**
**والاستثناء الوحيد — `admin_audit_log.actor_id` يبقى مفردًا، ورفضتُ تحويله:** الفاعل مدير منصة و`users.tenant_id` له `NULL`، بينما `admin_audit_log.tenant_id` قد يحمل مستأجرًا حقيقيًا يستهدفه الفعل. المفتاح المركَّب لم يكن ليضعف الحراسة بل **يرفض كل صف مشروع**. تنفيذ «الأربعين» حرفيًا كان سيكسر سجل تدقيق المنصة.
**والثمانية عشر إلى `users` أُدرجت رغم أن مدخلها منعدم اليوم** (قيمها من الـJWT لا من جسم الطلب): الكلفة الحدّية صفر ما دام الترحيل يُكتب أصلًا، والمكسب أن «الفاعل من الرمز حصرًا» تصير **مفروضة بنيويًا لا بالانضباط**.
**مُثبَت على كل جدول مُشار إليه، وعلى المسارين معًا:**
- **إدراج عابر:** `houses→farms` · `batches→houses` · `user_assignments→houses` · `user_assignments→users` · `daily_logs→batches` · `daily_logs→houses` · `daily_logs→users` · `products→users` · `entity_audit_log→users` · **والتصحيح الذاتي** `daily_logs→daily_logs` — كلها مرفوضة.
- **تحديث عابر (المسار الثاني):** `UPDATE houses.farm_id` · `batches.house_id` · `daily_logs.created_by` · `products.created_by` — كلها مرفوضة.
- **والسليم ينجح:** عنبر داخل مزرعة مستأجره · دفعة داخل عنبره · سجل تدقيق بفاعل مستأجره · `UPDATE` إلى مزرعة مستأجره.
**تصحيح ذاتي أثناء الإثبات:** حالتا `daily_logs` فشلتا أول مرة بعمود `NOT NULL` ناقص — فلم يُبلَغ المفتاح أصلًا. الاختبار كان خاطئًا لا القيد؛ أُعيد بالأعمدة الإلزامية فمرّ. **فحصٌ لا يبلغ ما يزعم فحصه يُقرأ كنجاح وهو لا شيء.**
**والقاعدة كُتبت في `CLAUDE.md`** لا في سجل القرارات وحده — قسم مستقل ملزم، كي يلتزم بها كل عمل قادم بلا تذكير.
**تفصيلان تقنيان:** الترتيب في الترحيل مقصود (قيود التفرّد قبل المفاتيح التي تشير إليها — drizzle-kit ولّدها متداخلة فأُعيد ترتيبها)؛ والمرجعان الذاتيان (`daily_logs.correction_of_id` و`notifications.escalated_from_id`) يشيران عبر `table.id` لا عبر ثابت الجدول، وإلا وقع استنتاج نوع دائري.

**123. العنابر خادميًا — وتجميد المزرعة بعد أول دفعة (نظير #114)**
`GET/POST /farms/:farmId/houses` · `GET/PATCH /houses/:houseId`. القراءة لكل الأدوار والكتابة للمالك حصرًا، والأدوار الثلاثة غير المالكة مُثبَتة صراحةً في الإنشاء والتعديل معًا.
**والحالة تُقرأ ولا تُكتب هنا:** `PATCH /houses/:id/status` بآلة الحالات السبع نطاق قائم بذاته في المرحلة 3. **مُثبَت:** إرسال `status` في جسم التعديل لا يغيّرها — تبقى `جاهز للإسكان`.
**ونقل العنبر بين المزارع مقيَّد بنفس منطق #114:** قابل ما دام العنبر **بلا دفعات**، ويُجمَّد فور أول دفعة، برمز `409 house_has_batches`. السبب واحد: الخطر ليس النقل بل **إعادة كتابة معنى التاريخ**. ومُثبَت بقراءة القاعدة بعد الرفض لا برمز الرد.
**وسعة الخزان تميّز `null` الصريحة عن الغياب:** `null` تعني «الحقل مخفي في الواجهة» (§7.1) فتُمحى القيمة فعلًا، والغياب يعني «لا تغيير». مُثبَت بمسارين.
**والمفتاح المركَّب يحرس فعلًا — مُثبَت بالمقارنة لا بالوصف:** حذف `assertFarmInTenant` من الإنشاء أعطى في الدفعة 3 (قبل المفتاح المركَّب) **`201` وصفًا عابرًا للمستأجرين**؛ وأعطى الآن **`500`** — القاعدة ترفضه. فالحارس الإجرائي **طبقة ثانية حقيقية** لا وحيدة: قيمته أنه يحوّل خطأ المفتاح الخام إلى `404` مفهومة تحفظ «الوجود ثم التعيين».
**أربعة حرّاس مُثبَت أنها حاملة للحمل** بحذف كلٍّ منها: عدّاد الدفعات (#123) · `requireRole("owner")` · شرط المستأجر في القراءة · حارس المزرعة في الإنشاء.
**ويغلق §7-ب البند 2:** `cross-tenant-404` كان بلا هدف اختباري لأن لا مسار يأخذ معرّف كيان في الرابط. `/api/houses/:houseId` أول هدف حقيقي — ومُثبَت أن عنبر مستأجر آخر يعطي **نفس 404** التي يعطيها معرّف غير موجود إطلاقًا.
**وأُعيد اختبار #114 عبر المسار الحقيقي:** تجهيزة اختبار المزارع كانت تُدخل عنبرًا **بإدراج مباشر** لأن مسار العنابر لم يكن موجودًا. صارت تمرّ بـ`POST /farms/:farmId/houses` — فالقيد يُفحص الآن على عنبر أنشأه المستخدم كما ينشئه في الميدان.

**124. `enforceEntityAccess` لا يرى معرّفات المسار — حدّ مُوثَّق لا مُصلَح**
**عيب بنيوي في «الطبقة الثالثة والأخيرة من الفرض المركزي»، انكشف بأول مسار يأخذ معرّفًا في الرابط.**
الحارس مركَّب بـ`api.use(...)` على مستوى الموجّه، ويقرأ `houseId` من `req.params`/`query`/`body`. و**Express لا يملأ `req.params` في middleware على مستوى الموجّه** — يملؤها للمسار المطابق وحده. **مُثبَت بتجربة مستقلة:** `{}` في الـmiddleware مقابل `{"houseId":"42"}` داخل المسار نفسه.
**الأثر:** فحص الإسناد لا يعمل لأي معرّف يأتي في **مسار الرابط**. مُثبَت من طرف إلى طرف: المربّي يقرأ عنبرًا **غير مُسند** له فيحصل على `200` لا `403`.
**وما ليس متأثرًا — وهذا مهم:** **العزل بين المستأجرين سليم تمامًا**، يفرضه فلتر `tenantId` في طبقة الخدمة **والمفتاح المركَّب في القاعدة** (#122). المتأثر **الإسناد داخل المستأجر الواحد** فقط: مربٍّ يرى عنابر مستأجره كلها لا عنابره المُسندة وحدها.
**ولم يظهر قبل الآن** لأن المسارات السابقة كلها بلا معاملات مسار — نفس سبب انكشاف عيب فاحص OpenAPI (#117) في الدفعة نفسها التي أدخلت أول معامل.
**لماذا لم يُصلَح في هذه الدفعة:** الإصلاح يغيّر **بنية سلسلة الفرض المركزي** لكل مسار قادم، ومعه سؤال منتج لا يحسمه المنفّذ: **هل قراءة بيانات عنبر يجب أن تكون مقيَّدة بالإسناد أصلًا؟** المصفوفة §12.2 تقيّد الأفعال (إنشاء سجل، تنفيذ مهمة) ولا تنصّ على قراءة بيانات العنبر. القرار للمالك.
**والحدّ مُوثَّق باختبار مُلتزَم** بعنوان «حدّه المُوثَّق» — على نمط اختبار حدّ الشكل الرابع (#106) — كي لا يُظن الحارس عاملًا. مسجَّل في §7-ب البند 18.

**125. فاحص صحة enum صار يشترط ذكر العمود لا القيمة وحدها — وحدّه الباقي موثَّق**
كسر الفاحص **بناءً صحيحًا** في الدفعة 4 بسببين، كلاهما حقيقي:
- **تصادم نصّي:** `entityType: "house"` في سجل تدقيق العنابر جعل `LOCATION_TYPE` (‏`warehouse|house`) «ملموسًا»، فبُلِّغ عن `"warehouse"` كقيمة ميتة. وسجل التدقيق لا علاقة له بموقع المخزون إطلاقًا — هذا مثال حيّ على العيب المسجَّل في القرار #111 (بوابة معيارها نصّي لا بنيوي).
- **تجهيزة اختبار تُحتسب طبقةَ كتابة:** ملف `test-support/hierarchy.ts` يحوي `.insert(` و`powerSources: ["مولدات"]`، فصار `POWER_SOURCE` «ملموسًا» و`"شمسية"` ميتة. **بيانات الاختبار كانت تقرّر نتيجة بوابة تفحص كود الإنتاج.**
**القرار — تعديلان بموجب الاستثناء الوحيد المأذون (كسر بناءً صحيحًا):**
1. **`test-support/**` مستثنى** من مسح طبقة الكتابة: تجهيزات لا مسارات API.
2. **«ملموس» صار يشترط شيئين معًا**: أن تذكر طبقة الكتابة **عمودًا من نوع هذا الـenum** *و*تكتب إحدى قيمه. الربط عمود↔enum مبنيّ من `schema/enums.ts` (‏`pgEnum` ← ثابت القيم) ومن ملفات المخطط (`xEnum("column")`).
**والتضييق لم يُرخِ البوابة — مُثبَت:** حصر كتابة `HOUSE_TYPE` بقيمة واحدة في مسار حقيقي ← بُلِّغ عن القيمتين الأخريين كميتتين. والتصادم القديم ← لم يعد يُبلَّغ.
**وحدّه الباقي — أذكره لأن إغفاله يجعل الإصلاح يُقرأ أوسع مما هو:** أسماء الأعمدة **ليست فريدة عبر الـenums**. `status` عمود في `houses` و`batches` و`health_observations`… فذكر `status` مع قيمة تخصّ enum آخر يعيد الالتباس. **ظهر فعلًا أثناء الإثبات:** حقنة اختبار حوت `"مغلق"` مع ذِكر `status` فأُبلغ عن `HEALTH_OBSERVATION_STATUS` خطأً. الكود الحقيقي لا يقع فيه اليوم، لكن الاحتمال قائم.
**فالإصلاح يضيّق صنف الخطأ ولا يزيله.** الإزالة الكاملة تحتاج قراءة AST بنوعية الأعمدة — **§7-ب البند 11 يبقى مفتوحًا** ولا يُشطب بهذا.

> **تصحيح لاحق ملزم (القرار #127) — هذا القرار يصف إصلاحًا لآلية لم تكن تحرس شيئًا أصلًا.**
> إثبات «التضييق لم يُرخِ البوابة» أعلاه صحيح كوصف للآلية وخاطئ كاستنتاج عن التغطية: نُفِّذ بـ**حقن** قيمة حرفية في مسار حقيقي، فأثبت أن الآلية تتفاعل مع حرفية محقونة — **لا** أن أي enum كان تحت الحراسة فعلًا. الواقع المُقاس: البوابة كانت تحرس **صفر قيمة** حين كُتب هذا القرار، وقبله، ومنذ أول commit. **وهذا نفس الخطأ المنهجي المسجَّل في #107** (إثبات الإيموجي داخل تعليق دون اللون): إثبات الآلية ثم كتابة خلاصة تُقرأ كإثبات للتغطية. راجع #127.


**126. الإسناد يقيّد القراءة — وتركيب الحارس بنمط مسار (يغلق #124)**
**القرار #124 وثّق العطب وأجّل السؤال المنتج: هل قراءة بيانات عنبر مقيَّدة بالإسناد أصلًا؟ الجواب من المالك: نعم.** مربٍّ يفتح بيانات عنبر غير مُسند له اطّلاع على ما ليس له، سواء كتب فيه أم لا.
**الإصلاح سطران، وموضعهما هو القرار:** `enforceEntityAccess` يبقى في `app.ts` لا يُنثَر داخل الموجّهات (المبدأ الأول — الفرض في طبقة واحدة)، لكنه يُركَّب **مرتين**: عامًّا كما كان ليمسح `query`+`body`، وبنمط مسار لكل مسار يحمل معرّف كيان في الرابط (`ENTITY_ID_PATH_PATTERNS`). إضافة مسار جديد صارت **سطرًا واحدًا بجوار أخواته** لا بحثًا في كل ملف مسار.
**السبب التقني مُثبَت بتجربة مستقلة قبل الإصلاح لا بعده:** `api.use(mw)` ← `req.params = {}` · `api.use("/api/houses/:houseId", mw)` ← `{"houseId":"42"}` · داخل المسار ← `{"houseId":"42"}`. Express يملأ `params` من النمط المطابق وحده.
**و«المالك ومدير المنصة يمرّان» صارت قائمة أدوار مقيَّدة صريحة** (`ASSIGNMENT_SCOPED_ROLES`) بدل شرط سلبي — الفرق أن إضافة دور جديد لا تمنحه تجاوزًا صامتًا.
**مُثبَت بثلاث مخالفات متعمَّدة فُرضت ثم أُزيلت، لا بالوصف:**
- **حذف تركيب النمط** من `app.ts` ← المربّي عاد إلى **200** لعنبر غير مُسند. فالتركيب هو الحامل للحمل.
- **توسيع القائمة** إلى `farmer, supervisor, owner` ← **11 اختبارًا سقط**. فالقائمة تُقرأ فعلًا.
- **تفريغ القائمة** ← سقط اختبار الرفض وحده (31/30). فالفرق بين الأدوار حقيقي لا صدفة.
**واختبار الحدّ قُلب:** «حدّه المُوثَّق: … ← 200 لا 403» صار «المربّي يقرأ عنبرًا **غير مُسند** له ← 403»، ويتحقق من رمز الخطأ `forbidden` لا من الحالة وحدها.
**واختبار كتبتُه أثناء العمل حُذف لأنه يثبت غير ما يزعم:** «المربّي يعدّل عنبرًا غير مُسند له ← 403» كان يمرّ **حتى بعد حذف الحارس** — لأن `PATCH /houses/:houseId` محروس بـ`requireRole("owner")` أصلًا، فالـ403 من حارس الأدوار لا من الإسناد. انكشف بالمخالفة الأولى لا بالمراجعة. **اختبار اسمه أوسع من نطاقه يوثّق حراسة لا وجود لها.** **والدرس المعمَّم: اختبار يمرّ لسبب غير الذي يزعمه لا يُكتشف بالمراجعة بل بالمخالفة المتعمَّدة** — وهو نفس درس #127 في البوابات.
**والمصفوفة §12.2 عُدِّلت بصف «قراءة بيانات عنبر»:** كانت تقيّد الأفعال وتسكت عن القراءة — **والسكوت لم يكن قرارًا** بل ثغرة قُرئت كإباحة.
**وما لم يُنفَّذ — يُسجَّل كما هو بالضبط، عطبًا لا تصميمًا:** **المشرف والطبيب يُسندان بالمزرعة لا بالعنبر (تصديق ميداني من المالك). جدول `user_assignments` لا يدعم ذلك، فنطاقهما مفتوح مؤقتًا على كل عنابر المستأجر — وهو تجاوز معروف يُصلَح قبل بناء شاشة المشرف (المرحلة 3).** مسجَّل في §7-ب البند 19 وفي `CLAUDE.md` معًا، وموثَّق باختبار مُلتزَم يقرأ الواقع (200) كي لا يُظن الحارس عاملًا عليهما. **من يقرأ لاحقًا يجب أن يعرف أن هذا عطب مؤجَّل لا تصميم.**
**والعزل بين المستأجرين لم يكن ولا يزال متأثرًا** — يفرضه فلتر `tenantId` في الخدمة والمفتاح المركَّب في القاعدة (#122). المتأثر الإسناد داخل المستأجر الواحد وحده.

**127. بوابة صحة enum كانت خاملة منذ أول commit — أُعيدت كتابتها على AST**
**بوابة تعلن نجاحًا وهي لا تفحص شيئًا أخطر من بوابة غائبة:** الغائبة معلومة، والكاذبة تُحتسب ضمن «11/11 نجحت» فتشتري ثقة بلا مقابل.
**المُقاس لا المُستنتَج:** شُغِّل فاحص **كل commit على شجرته هو** عبر worktrees منفصلة. النتيجة `0/26` عند `036c8f7` (أول commit) و`0/27` من `5db3b82` فصاعدًا — **لم تحرس قيمة واحدة قط**. لم تكن انحدارًا من #125 بل **وُلدت خاملة**، و#125 عدّلها دون أن يكتشف ذلك.
**السبب الجذري — معيار غير قابل للتحقق في هذه المعمارية أصلًا:** المعيار كان «طبقة الكتابة (`.insert(`/`.update(`) تذكر العمود **وتكتب القيمة نصًّا حرفيًا**». ولا خدمة في هذا النظام تكتب قيمة enum حرفية: القيم تصل من جسم الطلب، يتحقق منها zod مقابل الثابت المشترك، وتُدرَج **كمتغيّر**. فالحرفيات لا توجد إلا في `packages/shared/src/enums.ts` (خارج المجلد الممسوح)، وفي `openapi/spec.json` (ليس `.ts`)، وفي الاختبارات (مستثناة عمدًا بالقرار #125). **الطريق الوحيد لتفعيلها كان مخالفة المعمارية** — أي أن نجاحها كان يعني التزام المعمارية لا صحة الـenums.
**المعيار الجديد — الارتباط لا الذِّكر:** enum **مربوط** إن وُجد `z.enum(...)` في `apps/api/src` يربطه — بالاسم (`z.enum(HOUSE_TYPE)`) فيقبل كل قيمه، أو بقائمة حرفية فيقبلها وحدها. **القيمة الميتة = قيمة في الثابت المشترك لا يقبلها أي ربط.** وهذا يلتقط صنف العطب الحقيقي: **تضييق مخطط التحقّق** فتصير قيمة باقية غير قابلة للكتابة من أي مسار.
**والقراءة بالـAST لا بمطابقة نصية** (TypeScript compiler API): يُلغي صنفَي الخطأ اللذين وقعا فعلًا مع الفاحص السابق — الذِّكر داخل تعليق، وتصادم اسم عمود بين نوعين.
**مُثبَت بخمس مخالفات متعمَّدة، لا بالوصف:**
- **الشجرة النظيفة** ← `2/27` مربوط (`5/111` قيمة محروسة): `HOUSE_TYPE` و`POWER_SOURCE`. **رقم غير صفري لأول مرة في تاريخ المستودع.**
- **تضييق ربط الإنشاء وحده** إلى `["مفتوح","هجين"]` ← **نجحت** بحق: `PATCH` ما زال يقبل `"مغلق"` بالاسم، فالقيمة حيّة. (اتحاد الارتباطات يعمل.)
- **تضييق الربطين معًا** ← **فشلت**: `HOUSE_TYPE."مغلق"` ميتة، مع موضعَي الربط في الرسالة.
- **نفس الحالة + ذِكر `"مغلق"` داخل تعليق** ← **بقيت فاشلة**. الفاحص السابق كان سيُخدَع بهذا بنيويًا.
- **حذف ربط `POWER_SOURCE`** ← العدّاد نزل `2/27` ← `1/27`. فالرقم حيّ لا زينة.
**والمقارنة الحاسمة:** نفس المخالفة (تضييق الربطين) عُرضت على **الفاحص القديم** ← `✓ نجحت، 0/27`. وعلى الجديد ← `✗ فشلت`. الفرق ليس تحسينًا في الدقة بل فرق بين **فحص ولا فحص**.
**والتباس مكتشَف أثناء الإثبات، فرضه الفاحص بدل تجاهله:** `["مفتوح","مغلق"]` قائمة حرفية تنتمي لـ`HOUSE_TYPE` **و**`DISPUTE_STATUS` معًا. القائمة الملتبسة **تُفشِل البوابة برسالة صريحة** لا تُتجاهَل صامتة — والحل استعمال الثابت المشترك بالاسم.
**وهل تسرّبت قيمة ميتة خلال فترة الخمول؟ لا — صفر.** شُغِّل الفاحص **الجديد** على كل commit في التاريخ: لا قيمة ميتة في أي نقطة. عدد الارتباطات نما `0` ← `1` (‏`POWER_SOURCE` مع دفعة المزارع) ← `2` (‏`HOUSE_TYPE` مع دفعة العنابر). **نجونا بالمعمارية لا بالبوابة:** كل مسار يربط بالثابت المشترك بالاسم، وهذا يقبل كل القيم بنيويًا.
**ودرس منهجي يتكرر للمرة الثالثة** (#107 لونًا داخل تعليق، #126 اختبارًا يمرّ بحارس الأدوار، وهذا): **إثبات أن الآلية تتفاعل مع مدخل محقون ليس إثباتًا أنها تحرس شيئًا قائمًا.** ولا يُكتشف الفرق بالمراجعة — بل بسؤال «كم عنصرًا يفحص هذا فعلًا؟» ثم بالمخالفة المتعمَّدة.

**128. إسناد بمستويين — العنبر للمربّي والمزرعة للمشرف والطبيب (يغلق §7-ب البند 19)**
**الشكل المُعتمَد بعد تقدير ثلاثة أشكال: عمود `farm_id` على نفس الجدول.** رُفض جدول ثانٍ (`user_farm_assignments`) رغم أنه أرخص بساعتين: يخلق **مصدرَي حقيقة** لسؤال واحد — «بماذا هذا المستخدم مُسند؟» — وكل مستهلك قادم (شاشة العنابر، توجيه الإشعارات، نطاق التقارير) يجب أن يتذكّر الجدولين، **ونسيان أحدهما يفتح صامتًا لا يفشل صاخبًا**. ورُفض التشكيل متعدد الأشكال (`scope_type` + `scope_id`) لأنه **لا يقبل مفتاحًا أجنبيًا إطلاقًا** — يشتري نظافة في المخطط بثمن الضمانة البنيوية الوحيدة التي تحمي العزل (قاعدة `CLAUDE.md`).
**والتوقيت لم يكن صدفة:** `user_assignments` **بلا كاتب في كود الإنتاج بعد** (لا `POST /users` ولا مسار إسناد — المرحلة 4). فالتغيير اليوم بلا هجرة بيانات وبلا إعادة عمل واجهة؛ بعد بناء شاشات الإسناد كان سيكلّف الضِّعف.
**التنفيذ:** `house_id` صارت nullable، وأُضيف `farm_id` بمفتاح مركَّب `(farm_id, tenant_id) → farms(id, tenant_id)`، و`CHECK ((house_id IS NULL) <> (farm_id IS NULL))` يفرض **مستوى واحدًا لكل صف** — لا صف بلا مستوى ولا صف بمستويين.
**والفخّ الذي كان سيمرّ صامتًا — أُثبت لا وُصف:** الفهرس الفريد القائم على `(user_id, house_id)`، بعد أن صار العمود يقبل `NULL`، يعامل كل `NULL` كقيمة مميّزة. جُرِّب على جدول مؤقت بفهرس فريد عادي: **ثلاثة صفوف متطابقة لنفس (المستخدم، المزرعة) قُبلت كلها**. الحل فهرسان **جزئيان**: `WHERE house_id IS NOT NULL` و`WHERE farm_id IS NOT NULL`. ومعهما اسم قيد جديد، فأُضيفت رسالته إلى خريطة 409 (`user_assignments_user_farm_uq` ← «المستخدم مُسند لهذه المزرعة بالفعل»).
**والفرض استعلام واحد لا اثنان:** قراءة الوجود تجلب `farm_id` أصلًا، فيُفحص المستويان معًا بـ`or(house_id = ?, farm_id = ?)`.
**و`ASSIGNMENT_SCOPED_ROLES` صارت ثلاثة أدوار** (`farmer` · `supervisor` · `vet`) — وتبقى **قائمة موجبة لا شرطًا سالبًا**: دور جديد لا يحصل على تجاوز صامت.
**مُثبَت على القاعدة بسبع محاولات (بنقاط حفظ لكل عبارة، لا معاملة واحدة):** صف بلا مستوى ← مرفوض · صف بمستويين ← مرفوض · إسناد مزرعة سليم ← مقبول · تكراره ← مرفوض بالفهرس الجزئي · إسناد عنبر لنفس المستخدم ← مقبول (المستويان يتعايشان) · تكراره ← مرفوض · إسناد مزرعة مستأجر آخر ← مرفوض بالمفتاح المركَّب.
**ومُثبَت على طبقة الفرض بثلاث مخالفات:** إخراج `vet` من القائمة ← سقط «الطبيب بلا إسناد ← 403» · حذف شرط مستوى المزرعة ← سقط اختبارا المشرف والطبيب المُسندَين · استبدال مطابقة المزرعة بـ`isNotNull` ← سقط «403 لعنبر في مزرعة أخرى». فالتقييد بمزرعة **العنبر تحديدًا** حامل للحمل لا زينة.
**وحادثة أثناء الإثبات تستحق التسجيل لأنها الدرس نفسه للمرة الرابعة:** المخالفة الأولى **لم تنطبق أصلًا** — كتبتُ نص الاستبدال على ثلاثة أسطر بينما Prettier جمع القائمة في سطر واحد. النتيجة كانت `38 passed` وكادت تُقرأ «قائمة الأدوار غير حاملة للحمل». اكتُشف بفحص الملف بعد التعديل لا بقراءة النتيجة. **فأُضيف تأكيد `assert` داخل أداة المخالفة نفسها: تفشل صراحةً إن لم تجد النص المستهدف.** مخالفة لا تنطبق تُقرأ كنجاح وهي لا شيء — نفس درس #122 و#126 و#127.
**وحدٌّ باقٍ مفتوح صراحةً (§7-ب البند 20):** `GET /farms/:farmId/houses` غير مقيَّد بالإسناد — لا نمط مسار لـ`farmId`. موثَّق باختبار مُلتزَم يذكر الأدوار الأربعة بالاسم كي لا يُظن مقيَّدًا، ويحتاج قاعدة منتج قبل التنفيذ: هل يرى المربّي أسماء عنابر مزرعته غير المُسندة له في قائمة؟

**129. السرد يُفلتر بالإسناد — قاعدة سلفية لا إصلاحًا موضعيًا (يغلق §7-ب البند 20)**
**قرار المالك حرفيًا: «لا يحق للمربّي رؤية العنابر غير المُسندة له، ولا حتى أسماءها. ليست من اختصاصه.»** فالإخفاء كامل: غياب من الرد، لا عرض مُعطَّل — «العنبر الشمالي» وحده يكشف بنية مزرعة ليست من اختصاصه.
**والسرد يحتاج طبقتين لا واحدة، وحذف أيٍّ منهما يترك ثقبًا مختلفًا:** نمط مسار لـ`farmId` في `ENTITY_ID_PATH_PATTERNS` يقرّر **هل يصل هذه المزرعة أصلًا** (‏403)، وفلترة في طبقة الخدمة تقرّر **ماذا يرى داخلها**. الفلترة وحدها تعطي قائمة فارغة لمزرعة كاملة لا يخصّه منها شيء — و**الفارغة تقول «لا عنابر هنا» وهي كذبة**: تُقرأ نفيَ وجود لا نفيَ صلاحية.
**ونطاق الإسناد نُقل إلى ملف واحد** (`lib/entityScope.ts`): كان `ASSIGNMENT_SCOPED_ROLES` داخل `middleware/entityAccess.ts` وحده، فلما احتاجته الخدمة كان الخياران تكراره أو استيراد خدمة من middleware — كلاهما يجعل «من هو المقيَّد بالإسناد؟» سؤالًا بجوابين محتملين.
**والفلترة بـ`EXISTS` لا بـ`JOIN` عمدًا:** الانضمام على جدول تراكمي (مستخدم واحد لعدة عنابر) يضاعف صفوف النتيجة فيحتاج `DISTINCT`، ونسيانه يُظهر العنبر مرتين. `EXISTS` تجيب عن سؤال وجودي بلا أثر على العدد.
**والقاعدة سُجِّلت سلفًا في `CLAUDE.md` لا في هذا الملف وحده** — بطلب المالك: **أي مسار سرد قادم يُفلتر بالإسناد افتراضيًا**، كي لا يُعاد اكتشاف هذا في كل مسار جديد.
**مُثبَت بأربع مخالفات متعمَّدة، كلٌّ منها بأداة تتحقق أن الاستبدال انطبق فعلًا (درس #128):**
- **حذف الفلترة من الخدمة** ← المربّي رأى الخمسة بدل الاثنين.
- **حذف نمط المسار من `app.ts`** ← اختبارا الـ403 عادا إلى **200**.
- **إسقاط شقّ «إسناد المزرعة» من بلوغ المزرعة** ← المشرف المُسند بمزرعته حصل على 403.
- **إسقاطه من فلترة السرد** ← المشرف رأى قائمة فارغة في مزرعته المُسندة.
**والاختبار الحاسم هو الثاني، لأنه يجيب عن السؤال الذي أخطأتُ فيه سابقًا:** «هل يفشل الاختبار للسبب الذي يزعمه؟». مسار السرد **بلا `requireRole`** (‏القراءة لكل الأدوار)، وحذف نمط المسار أعاد الردّ إلى `200` — فالـ403 من `enforceEntityAccess` حصرًا، لا من حارس أدوار سبقه. هذا بالضبط ما كشف زيف اختبار `PATCH` في القرار #126.
**وحدٌّ أذكره كي لا يُقرأ الإثبات أوسع مما هو:** اختبار «مزرعة مستأجر آخر ← 404» **بقي ناجحًا تحت المخالفة الثانية** — لأن `assertFarmInTenant` في طبقة الخدمة يعطي نفس الـ404. فهو يوثّق طبقة الخدمة لا الفرض المركزي؛ الطبقتان تعطيان 404 والفرق بينهما غير مرئي من الخارج.
**والاختبار الذي وثّق الثغرة قُلب:** «سرد عنابر مزرعة — %s ← 200 (السرد غير مقيَّد بالإسناد بعد)» بأدواره الأربعة حُذف، وحلّ محله فحصٌ يثبت التقييد. **والغياب يُفحص على الرد الخام** (`JSON.stringify`) لا على الحقول المفكوكة: اسم مسرَّب داخل أي حقل آخر — رسالة، عدّاد، ترتيب — لا تكشفه مقارنة المعرّفات وحدها.

**130. فحص «الغياب» كان على نصّ JSON فاصطدم برقم عشوائي — صار على القيم**
**العيب في الاختبار لا في الكود، وأمسكه CI بعد خضرة محلية كاملة.** فحص «العنابر غير المُسندة غائبة» في القرار #129 كُتب `JSON.stringify(res.body).not.toContain(String(house.id))`. ومطابقة **سلسلة فرعية** على رقم تصطدم بأي أرقام أخرى في الرد.
**وقع فعلًا:** المعرّف المخفي `83`، واللاحقة العشوائية في أسماء العنابر `839734` — و`"سرد 1 839734"` تحتوي `"83"`. فبُلِّغ عن تسريب لا وجود له، والرد كان يحمل العنبرين المُسندين بالضبط.
**والأخطر أن نفس الـcommit كان أخضر في تشغيل وأحمر في آخر** (‏`pull_request` نجح و`push` فشل): اللاحقة عشوائية لكل تشغيل، فالاصطدام احتمالي. **الخضرة المحلية (144/144) كانت حظًّا لا دليلًا** — وهذا صنف عطب لا يكشفه تكرار التشغيل المحلي بل الاحتمال وحده.
**الإصلاح — الفحص على القيم لا على النصّ:** دالة تجمع كل قيمة أولية في الرد مهما عمق تداخلها، ثم يُقارَن الرقم رقمًا والنصّ نصًّا. **يحفظ الغرض الأصلي** (اسم مسرَّب في أي حقل — رسالة، عدّاد، ترتيب — يُلتقط) **بلا اصطدام ولا تساهل**.
**مُثبَت حتميًّا لا احتماليًّا:** أُعيد تشغيل الفحصين على **الحمولة الحرفية من سجل CI الفاشل**. القديم: يبلّغ تسريبًا للمعرّف `83` ← **إيجابية كاذبة**. الجديد: لا يبلّغ. وعلى حمولة فيها تسريب حقيقي: الجديد **يلتقطه**.
**وحدٌّ في إثبات التكامل أذكره:** حذف الفلترة يُسقط الاختبار عند تأكيد **تطابق مجموعة المعرّفات** الذي يسبق تأكيدات الغياب — فلا يثبت تأكيدات الغياب نفسها. تلك مُثبَتة بالسكربت الحتمي أعلاه، لا بمخالفة التكامل.
**والدرس يكمّل #128:** هناك «مخالفة لا تنطبق تُقرأ نجاحًا»، وهنا **«فحص ينجح لسبب عرضي أو يفشل لسبب عرضي»**. كلاهما يجعل نتيجة الاختبار مستقلة عمّا يزعم فحصه. والفرق أن هذا لا يُكشف بالمخالفة المتعمَّدة — بل بأن **يُبنى الفحص على بنية لا على نصّ** من البداية.

**131. نطاق سرد المواقع والمزارع — قاعدة رؤية لكل دور، وعدّادات تحتها**
**القاعدة التي كُتبت في #129 لم تكن مطبَّقة إلا على العنابر.** `GET /api/sites` و`GET /api/sites/:siteId/farms` كانا بلا فلتر وبلا نمط مسار — فمربٍّ مُسند لعنبر واحد يرى المواقع السبعة ومزارعها كلها. **نفس نمط `must_change_password` (#99): علامة مكتوبة والخادم لا يقرؤها.** انكشف عند التخطيط لشاشة المالك لا باختبار ساقط.
**وقاعدة الرؤية تختلف بالدور — قرار المالك، لا تُوحَّد:** المالك كل مواقع ومزارع مستأجره · المشرف والطبيب يرون الموقع بوجود **مزرعة مُسندة** فيه، ومنه المُسندة إليهم وحدها، وكل عنابرها · المربّي يرى الموقع بوجود **عنبر مُسند** فيه، ومنه المزرعة الحاوية له وحدها، ومنها عنابره المُسندة وحدها.
**ولماذا لم أوحّدها في شرط واحد بـ`OR`:** يبدو أنظف **ويغيّر المعنى** — يصير إسناد عنبر واحد كافيًا ليرى المشرف مزرعة لم تُسند إليه. **مُثبَت بمخالفة:** إسقاط تفريع المربّي أسقط أربعة اختبارات.
**والعزل يسبق الإسناد دائمًا:** `sites.tenant_id` في `WHERE` و`assertSiteInTenant` قبل أي فلتر، والانضمامات كلها مقيَّدة بنفس المستأجر. **مُثبَت:** موقع مستأجر آخر يعطي **404 لا 403** حتى لدور مقيَّد بالإسناد.
**والعدّادات تحت الفلتر نفسه وفي نفس الاستعلام:** بطاقة الموقع تحمل `farmCount` و`houseCount` المرئيين، وبطاقة المزرعة `houseCount` وتوزيعًا **بثلاث مجموعات لا سبع** (مشغول · جاهز وشاغر · غير ذلك — قرار المالك). و«غير ذلك» يُحسب **طرحًا** لا بشرط ثالث، فلا يمكن أن تفترق المجموعات عن الإجمالي.
**والشجرة الاختبارية مبنيّة كي يفترق الأدوار على نفس الصفوف** لا كي يمرّ كل دور على شجرته: موقع فيه مزرعتان إحداهما مُسندة للمشرف وفيها عنبر مُسند للمربّي. فنفس الموقع يعطي **المالك `2/3` والمشرف `1/2` والمربّي `1/1`** — وهذا وحده ما يجعل «العدّاد تحت الفلتر» قابلًا للفحص. وموقع ثالث بلا مزارع يفصل «فارغ» عن «محجوب»: يظهر للمالك بصفرين ويختفي عن الباقين.
**مُثبَت بست مخالفات متعمَّدة، وبفصلٍ مقصود بين صنفين منها:**
- كسر فلتر المزارع في سرد المواقع ← **5 اختبارات**؛ وفي سرد المزارع ← **2**.
- **كسر مطابقة العدّاد للفلتر وحده** (القائمة تبقى مفلترة) ← **اختبار واحد مستقل** في كل نقطة. هذا الفصل مقصود: تسريب العدّاد لا يظهر في «من يرى ماذا» إطلاقًا.
- توحيد قاعدة الرؤية بين الأدوار ← **4 اختبارات**.
- حذف نمط مسار `siteId` ← الـ403 عادت **200** في ثلاثة اختبارات. ومسارات القراءة الثلاثة **بلا `requireRole`** (مُتحقَّق منه بالقراءة)، فالـ403 من `enforceEntityAccess` حصرًا لا من حارس أدوار سبقه.
**ونمط مسار واحد يغطي مسارين:** `api.use("/api/sites/:siteId", ...)` يطابق البادئة، فيشمل `/api/sites/5` و`/api/sites/5/farms` معًا.
**ولا استعلام لكل صف — مقيس لا موصوف:** عُدَّت استعلامات `SELECT` على مجمّع الاتصال في طلب حقيقي على موقع فيه **5 مزارع و10 عنابر**: `GET /api/sites` ← **2** (إعادة قراءة الجلسة + التجميع)، و`GET /api/sites/:id/farms` ← **3** (‏+ حارس وجود الموقع). العدد لا يتغيّر بعدد الصفوف.
**ودالة `assignmentReachesFarm` حُذفت** — صار حارس المزرعة يستعمل `visibleFarmCondition` نفسها، فبقيت بلا مستدعٍ. **كودٌ ميت في طبقة صلاحيات أسوأ من كود ناقص:** يُقرأ لاحقًا كقاعدة سارية وهو لا يُنفَّذ.
**واختباران قائمان قُلبا:** «المربي يقرأ القائمة ← 200» كان يمرّ بمجرد كون الرد مصفوفة، صار يفحص أن القائمة **فارغة** لمربٍّ بلا إسناد؛ و«المربي يسرد مزارع الموقع ← 200» صار **403** — والفارغة هنا كذبة لأنها جواب عن مزرعة لا يحقّ له سؤالها أصلًا.
**وحدٌّ مقصود:** `GET /api/farms/:farmId/houses` لم يُمَس (#129).

**132. شاشة البنية التحتية — التخطّي قاعدة معمَّمة، والرجوع لا يهبط فيما لم يُعرض**
**قاعدة واحدة لا حالتان خاصتان (قرار المالك):** أي مستوى يحتوي **عنصرًا واحدًا مرئيًا لهذا المستخدم** يُتخطّى وينزل لما تحته — تنطبق على المواقع كما على المزارع، و**متتابعةً**: من يرى موقعًا واحدًا فيه مزرعة واحدة يفتح التبويب فيجد العنابر بلا ضغطة. **والصفر ليس واحدًا:** لا عناصر ← حالة فارغة في مكانها.
**والرجوع يعيد إلى ما جاء منه فعلًا لا إلى مستوى تُخطّي:** كل مدخل في الأثر يحمل علامة `skipped` تُضبط على **الأب** لحظة النزول — لأن السؤال ليس «هل رأينا هذا المستوى؟» بل **«هل اختار المستخدم فيه؟»**. وإن كانت كل المستويات فوقه متخطّاة فلا مستوى يُرجَع إليه: تُغادَر الشاشة كما يُغادَر أي تبويب.
**والنموذج خالص بلا React ولا شبكة** (`lib/infrastructureNavigation.ts`): سؤال «إلى أين يعود من هبط بلا اختيار؟» منطقي بحت، وفحصه عبر شاشة كاملة يخلطه بجلب البيانات وحالات القائمة. أحد عشر اختبارًا عليه وحده، وستة على الشاشة من طرف إلى طرف.
**وزر الرجوع العتادي في أندرويد بنفس السلوك حرفيًا:** يبتلع الحدث ما دام في الأثر مستوى يُرجَع إليه، ويتركه للنظام حين لا يبقى شيء. بلا هذا يخرج المستخدم من التبويب كله من المستوى الثالث بدل أن يصعد مستوى.
**والتخطّي مبنيّ على القائمة المجلوبة لا على العدّاد:** العدّاد والقائمة رحلتان منفصلتان للخادم، فبناء قرار التنقّل على العدّاد يجعل الشاشة تنزل إلى مستوى تعرض فيه عنصرين. **مُثبَت:** ربط التخطّي بـ`farmCount` أسقط ثلاثة اختبارات.
**والعدّادات تُعرض كما جاءت — لا حساب في العميل ولا جلب للعدّ (القرار #131):** العميل لا يرى ما حُجب عنه، فأي عدّ محلي سيكون **أقل من الحقيقة المرئية لا أدقّ منها**، وسيصير مصدر حقيقة ثانيًا يتباعد عن الأول.
**والقدرات لا الأدوار:** `infrastructureCapabilitiesFor` في موضع واحد، والشاشة تسأل `canCreate`/`canEdit`. **والإخفاء تهذيب واجهة لا حراسة** — الخادم يفرض `requireRole("owner")` بأي حال. **مُثبَت:** توسيع القدرة لكل الأدوار أسقط اختبارًا.
**و403 حالة عرض لا انهيار:** يقع حين تتغيّر الإسنادات بين تحميل القائمة والدخول. ورسالته **مفصولة عن 404** عمدًا: «لم يعد ضمن ما أُسند إليك» غير «لم يعد موجودًا» — ودمجهما كان سيخفي الفرق عن المستخدم في الميدان.
**والحالة الفارغة تقول «لا شيء هنا لك» لا «لا شيء هنا»:** «لا مواقع **مُسندة إليك** بعد». الصياغة الأولى كذبة على المربّي — الموقع موجود ومليء، لكنه ليس له.
**مُثبَت بخمس مخالفات متعمَّدة:** إلغاء قفز الرجوع فوق المتخطّى ← **5** اختبارات · عرض القائمة الفارغة كمحتوى ← **2** · ربط التخطّي بالعدّاد ← **3** · توسيع القدرات ← **1** · حذف سطر السياق ← **2**.
**ومخالفة سادسة كتبتُها ولم تكن مخالفة أصلًا — أسجّلها لأنها الدرس نفسه:** حاولت إثبات «الصفر ليس واحدًا» بتغيير الشرط إلى `<= 1`، فمرّت الاختبارات كلها. السبب أن `data[0]` على قائمة فارغة `undefined` بأي حال — **فحماية الصفر بنيوية لا شرطية: لا يمكن النزول إلى عنصر غير موجود.** المخالفة الصحيحة كانت على الحالة الفارغة نفسها (عرضها كمحتوى) وأسقطت اختبارين. **مخالفة تمرّ لا تعني أن الحارس غائب — قد تعني أن المخالفة لم تلمسه** (نظير #128 و#130).
**وبوابة الألوان أمسكت `#129` داخل تعليق في هذه الدفعة** — إحالة قرار بلا كلمة «القرار» تُقرأ لونًا سداسيًا. هذا **إثبات حيّ لما شكّ فيه المالك في #107**: اللون داخل التعليق يُلتقط فعلًا بعد تضييق #110، ولم يكن يُلتقط قبله.
**و`toApiFailure` فُصلت عن `toLoginRequestError`:** شاشات البنية التحتية تحتاج نفس التمييز (لا شبكة / 403 / 404) ولا تحتاج خطأ دخول. نسخ الدالة كان سيجعل «كيف يُقرأ فشل الطلب؟» سؤالًا بجوابين يتباعدان.
**ولم تُعدَّل عتبات التغطية:** الملفات الجديدة الحاملة لقرارات منتج (`infrastructureNavigation` · `capabilities` · `infrastructureErrors` · `houseStatusTone`) عند **100%** فعلًا، لكن إضافتها إلى `coverageThreshold` تعديل بوابة — والقاعدة القائمة تمنعه بلا سبب قاهر. **يبقى قرارًا للمالك**، ومعناه اليوم أن الأربعة يمكن أن تنحدر تغطيتها صامتة.

**133. اختبار شاشة يسقط على ذاكرة تحويل باردة — وبوابة التغطية أخفت السبب مرتين**
**الأعراض متناقضة ظاهريًا:** `check:all` أخضر محليًا، وأحمر في CI، **بنفس أرقام التغطية حرفيًا** (‏72.8 · 74.07 · 57.89 · 78.57). فالتغطية لم تكن السبب رغم أن البوابة سمّت نفسها.
**تشخيصي الأول كان خاطئًا وأسجّله كما وقع:** رأيت تحذير `A worker process has failed to exit gracefully` محليًا، فنسبتُ الفشل إلى مؤقّت جمع المهملات في عميل react-query (خمس دقائق افتراضيًا). **أصلحته ودفعته — وبقي CI أحمر.** التسريب كان حقيقيًا ويستحق الإصلاح، **لكنه لم يكن السبب**. الخطأ المنهجي: قفزتُ من «عرَض موجود» إلى «سبب» بلا إثبات يربطهما.

**السبب الحقيقي — مهلة `waitFor` على ذاكرة تحويل باردة:** المهلة الافتراضية في `@testing-library/react-native` **ثانية واحدة**. وأول اختبار في ملف الشاشة يأخذ ~370ms على جهاز ذاكرة babel فيه **دافئة**. وCI ينسخ المستودع من جديد كل مرة، فذاكرته **باردة دائمًا** — فيتجاوز الثانية ويسقط.
**ولهذا كان الفشل حتميًا لا عشوائيًا:** لا علاقة له بحمل العدّاء ولا بذاكرته. تكرّر على ثلاثة رؤوس متتالية، **آخرها commit وثائق بلا سطر كود** — وهذه هي التجربة التي أسقطت فرضية الموارد نهائيًا.
**وما حسم التشخيص أخيرًا: إعادة إنتاج بيئة CI لا قراءة الكود.** worktree نظيف على نفس الـcommit + `pnpm install --frozen-lockfile` + ذاكرة jest ممسوحة ← **الاختبار سقط، وأرقام التغطية طابقت ما عرضه CI حرفيًا** (‏72.8 · 74.07 · 57.89 · 78.57). وقبل ذلك استُبعد بالقياس: عتبات jest (رسائلها على stderr **ويلتقطها** الفاحص ولم تظهر) · قاعدة بيانات متّسخة (أُنشئت قاعدة فارغة ورُحّلت ← 204/204) · اختلاف البيئة (بـ`NODE_ENV=test` صارت فروع api **88.6%** مطابقة لـCI) · بطء العتاد (الاختبارات تمرّ تحت عامل واحد وحِمل CPU) · **واختلاف نسخ الحزم** (متطابقة تمامًا بين الشجرتين).
**الإصلاحان:** `asyncUtilTimeout: 20s` في ملف اختبار الشاشة — **والمهلة الأطول لا تُبطئ اختبارًا ناجحًا** لأن `waitFor` تعود فور تحقق الشرط. ومعه إصلاح التسريب (`gcTime: 0` و`clear()` و`unmount()`) الذي يبقى صحيحًا في ذاته.
**مُثبَت قبل/بعد في نفس البيئة الباردة:** قبل ← اختبار ساقط وخروج 1؛ بعد ← **119/119 وخروج 0**.
**وعيبان في البوابة يُسجَّلان ولا يُصلَحان الآن (§7-ب البند 22):**
**(أ) الشقّ الخاص بـvitest أسوأ — يقرأ المجرى الخطأ أصلًا:** `runCoverage` تفلتر `result.stdout` بحثًا عن أسطر تبدأ بـ`ERROR`، **وvitest يكتب أخطاء العتبة على `stderr`**. مُثبَت بتجربة: خفض عتبة الفروع إلى 99% ← `EXIT=1` والرسالة على stderr وحده (`stdout` صفر مطابقة). فأي فشل عتبة في `apps/api` أو `packages/shared` يُعرض **بلا سبب إطلاقًا**.
**(ب) والشقّ الخاص بـjest يقرأ المجرى الصحيح لكن يفلتر ضيقًا:** `runJestCoverage` تُرجع `ok: result.status === 0` ثم **تعرض أسطر «عتبة التغطية» فقط** من stderr. فأي فشل jest لسبب آخر — اختبار ساقط، عامل لا يخرج، انهيار إعداد — يُعرض **«التغطية دون الحد المطلوب» ومعه أرقام سليمة**. الرسالة تسمّي السبب الخطأ، والمشخِّص يبدأ من المكان الخطأ. **بوابة تكذب في سبب فشلها أخطر من بوابة صامتة** — لأنها توجّه الجهد بعيدًا. لم تُعدَّل بقاعدة المالك (لا تعديل بوابات بلا سبب قاهر).
**والدرس مزدوج:**
**(أ) «أخضر محليًا» لا يعني شيئًا حين يكون الفرق بيئيًا** — وذاكرة التحويل الدافئة فرق بيئي **غير مرئي إطلاقًا** في الكود أو في `git diff`. أي اختبار يعتمد مهلة قريبة من زمنه الفعلي سيمرّ عند كاتبه ويسقط في CI إلى الأبد.
**(ب) وعرَضٌ موجود ليس سببًا حتى يُربط بإثبات.** التسريب كان حقيقيًا، ونسبتُ إليه الفشل، ودفعتُ إصلاحًا لم يُصلح شيئًا — **جولة كاملة ضاعت لأني لم أطالب نفسي بدليل الربط**، وهو نفس ما أطالب به الكود.

**134. تحقق تقني لمخرجات التقارير (سياق للمرحلة 6 — لم يُبنَ شيء)**
**ثلاث مخرجات لكل تقرير بقرار المالك:** عرض في التطبيق · **PDF منسّق** بنظام التصميم صالحًا للطباعة وللمشاركة عبر واتساب · **Excel** بيانات خام قابلة للتعديل للمحاسب لا تنسيقًا مغلقًا.
**والتحقق أُجري فعلًا بملفات مولَّدة ومعروضة، لا بمراجعة توثيق مكتبات.**

**١. العربية في PDF — العطب الشائع أدقّ مما يُقال:**
جُرّب `pdfkit` (المسار المباشر بلا محرّك تخطيط): **شكّل الحروف صحيحًا** — الوصل سليم لأن `fontkit` يطبّق جداول OpenType. **لكنه عكس ترتيب الكلمات**: «تقرير أداء الدفعة — مزرعة الجبل» خرجت «الجبل مزرعة — الدفعة أداء تقرير»، والتصقت كلمتان. فالعطب **ليس في تشكيل الحروف بل في خوارزمية Bidi** (ترتيب الكلمات والمقاطع داخل السطر) — وهي غائبة عن مكتبات PDF المباشرة كلها تقريبًا.
**والفرق مهم عمليًا:** من يفحص بكلمة واحدة يراها سليمة ويستنتج أن المكتبة تدعم العربية. **العطب لا يظهر إلا بجملة فيها كلمتان فأكثر، أو بخلط عربي وأرقام** — وهو بالضبط شكل كل سطر في التقارير.

**٢. المسار الذي نجح: HTML ← PDF بمحرّك متصفح.**
Chromium (عبر Playwright الموجود أصلًا في المشروع) أنتج صفحة **سليمة تمامًا**: تشكيل صحيح، وترتيب كلمات صحيح، وأعمدة الجدول من اليمين، وأرقام لاتينية بـ`direction: ltr` داخل خلاياها، وTajawal مضمَّنًا base64، وألوان الرموز الفعلية من `tokens.json`. **وطبقة النص قابلة للنسخ والبحث** (مُثبَت بـ`pdftotext`) — لا صورة مسطّحة.
**السبب البنيوي:** محرّك المتصفح يحمل HarfBuzz للتشكيل **وICU للـBidi** معًا. أي مسار لا يحمل الاثنين سيكسر أحدهما.

**٣. الخادم أم التطبيق؟ — مقايضة حقيقية لا تفضيل:**
- **الخادم (Chromium بلا واجهة):** مخرَج **واحد متطابق** لكل المستخدمين، **وقابل للفحص في CI** كأي بوابة أخرى. كلفته متصفح في حاوية الإنتاج (~300 ميغابايت) وذاكرة عند التوليد.
- **التطبيق (`expo-print`):** يستعمل WebView النظام، فالتشكيل والـBidi سليمان كذلك، بلا كلفة خادم ويعمل بلا شبكة. لكن **المخرَج يتغيّر بنظام الجهاز ونسخته**، ولا يمكن فحصه في CI إطلاقًا.
- **وللمشاركة عبر واتساب تحديدًا:** المشاركة تحتاج **ملفًا محليًا**. فتوليد التطبيق يعطيه جاهزًا، وتوليد الخادم يحتاج تنزيلًا إلى ذاكرة التطبيق المؤقتة قبل المشاركة (خطوة واحدة إضافية بـ`expo-file-system`).
- **الترجيح المبدئي: الخادم**، لأن «التقرير الذي يراه المالك على هاتفه هو نفسه الذي يصل المحاسب والبنك» تساوي أكثر من خطوة تنزيل — والفحص الآلي للفارق البصري ممكن على الخادم وحده. **القرار للمالك عند المرحلة 6.**

**٤. مشاركة ملف من Expo — تحتاج إضافات ليست مثبَّتة اليوم:**
`expo-print` و`expo-sharing` و`expo-file-system` **غير موجودة في `apps/mobile/package.json`** (مُتحقَّق منه). والمشاركة عبر **لوحة المشاركة العامة** لا عبر نية موجَّهة لواتساب: الأخيرة تحتاج `<queries>` في بيان أندرويد 11+ لرؤية الحزمة، والأولى تتجنّب ذلك كلّه وتُظهر واتساب ضمن الخيارات. **وهذا الشق يحتاج تحققًا على جهاز حقيقي — لا يمكن إثباته في هذه البيئة**، وأسجّله كذلك بدل ادّعاء نجاحه.

**٥. Excel — بلا مشكلة عربية أصلًا:**
جُرّب `exceljs` على الخادم: اسم ورقة عربي، `rightToLeft: true` على العرض، وأعمدة عربية. **وأُعيدت قراءة الملف للتحقق**: القيم رجعت **أرقامًا لا نصوصًا** (`typeof === "number"`) — وهذا هو المطلوب بالضبط: المحاسب يعدّل ويجمع، لا يستقبل تنسيقًا مغلقًا. لا تشكيل ولا Bidi في XLSX لأن العرض يتولّاه Excel نفسه.
**والتوليد على الخادم** — نفس منطق الـPDF، ولأن المكتبة Node وليست RN.

**ولا شيء من هذا نُفِّذ:** لا تبعية أُضيفت ولا كود شُحن. الملفات المولَّدة كلها خارج المستودع في مجلد مؤقت. **الغرض اكتشاف العطب قبل المرحلة 6 لا بناؤها**، وقد ظهر عطب حقيقي كان سيُكتشف متأخرًا: مكتبة تبدو داعمة للعربية عند فحصها بكلمة، وتكسر عند أول جملة.

**135. النطاق: قطاع اللاحم حصرًا — لا بياض ولا أمهات**
قرار مالك يُسجَّل لأنه **يحدّ التصميم لا يصفه**. وقطاع الأمهات ليس «نفس النظام بأرقام أخرى» بل **مختلف معماريًا**: عنابر مغلقة بحساسات حرارة ورطوبة، وعدّادات أوتوماتيكية، وكاميرات حرارية — أي **بيانات تصل من أجهزة لا من إدخال بشري**.
**والفرق ينقض افتراضًا يقوم عليه كل شيء بُني حتى الآن:** الشاشات مصمَّمة لإدخال يدوي تحت شمس مباشرة، والتحقق مبنيّ على **معقولية ما يُدخله إنسان** (§15)، والسجل غير قابل للتعديل لأن الإنسان يخطئ ويصحّح بسجل جديد. تيّار قياسات من جهاز كل دقيقة لا يشبه أيًّا من ذلك — لا في الإدخال ولا في التحقق ولا في حجم التخزين.
**فبناؤه لاحقًا وارد، ولا يُخطَّط له بتوسيع هذا النموذج** — ولا يُضاف عمود أو جدول اليوم «تحسّبًا له». مُضاف إلى `app-complete-spec.md` §1.

**136. لا حسابات مالية في التطبيق إطلاقًا**
لا أسعار ولا تكاليف ولا هوامش. تقارير التطبيق **تشغيلية بحتة**: نفوق · أوزان · معامل تحويل · استهلاك علف وماء · أيام تغطية · أداء الدفعة.
**والمواصفة لم تكن تخالف هذا، لكنها لم تكن تنصّ عليه** — فُحصت: لا ذكر لسعر أو تكلفة أو هامش في أي شاشة أو تقرير (الإشارتان الوحيدتان لـ«الربحية» نصّ تمهيدي لا ميزة). **والسكوت ليس قرارًا** — نفس درس §12.2 في #126: ما لا يُنصّ عليه يُقرأ إباحة، وأول من يبني تقريرًا سيسأل «ولمَ لا نضيف التكلفة؟». صار منصوصًا في §1 و§5-د-4.
**والسبب ليس تقنيًا بل حدود مسؤولية:** رقم مالي في يد مربٍّ أو مشرف يغيّر معنى التطبيق من توثيق ميداني إلى أداة محاسبة، ويجرّ معه تدقيقًا وصلاحيات وحفظًا لا يحتملها التصميم الحالي.

**137. الحسابات في منصة ويب منفصلة تشارك نفس قاعدة البيانات**
منصة للمحاسبين والمراجعين، **تشارك القاعدة** لترث العزل بين المستأجرين والأدوار والمصادقة بدل بنائها ثانية.
**والمشاركة قرار مقصود لا اختصار:** قاعدة ثانية تعني مزامنة، والمزامنة تعني **نسختين من الحقيقة** تختلفان تحت أول عطل شبكة — وهو أسوأ ما يصيب أرقامًا يوقّع عليها محاسب.

**138. المنصة تمرّ بنفس الخادم — لا اتصال مباشر بالقاعدة**
**السبب حاسم وليس تفضيلًا معماريًا:** قواعد الإسناد وتقييد القراءة وفلترة السرد **تعيش في طبقة الخدمة لا في القاعدة** — `visibleFarmCondition` و`assignedHousesFilter` و`enforceEntityAccess` (القرارات #126 و#128 و#129 و#131). **اتصال مباشر يتخطاها كلها**: محاسب يقرأ `SELECT * FROM houses` يرى كل عنابر كل مزارع المستأجر بلا أي إسناد.
**ويجعل الصلاحيات مصدرَي حقيقة** — وهو بالضبط النمط الذي كلّفنا #99 (علامة مكتوبة والخادم لا يقرؤها) و#128 (قاعدة مكتوبة والجدول لا يستوعبها). تكراره بعد حادثتين يكون اختيارًا لا سهوًا.
**والقيد العملي على كل ما يُبنى من الآن:** للخادم **مستهلك ثانٍ قادم**. منطق الصلاحيات يبقى في طبقة خدمة قابلة لإعادة الاستخدام لا داخل معالجات المسارات، ولا شيء في المخطط يفترض أن القاعدة لتطبيق الموبايل وحده. مُضاف إلى `backend-technical-spec.md` §1.

**139. ثلاث مخرجات لكل تقرير**
عرض في التطبيق · **PDF منسّق** يطابق نظام التصميم صالحًا للطباعة وللمشاركة عبر واتساب · **Excel** بيانات خام قابلة للتعديل للمحاسب لا تنسيقًا مغلقًا.
**والفرق بين المخرَجين الأخيرين ليس صيغة ملف بل جمهورًا:** الـPDF **يُقرأ ولا يُعدَّل** (توقيع بصري على حالة موثّقة)، وExcel **يُعدَّل ويُجمَع** — ولهذا يجب أن تصل قيمه **أرقامًا لا نصوصًا** (مُثبَت في #134). خلطهما يُنتج PDF يحاول أن يكون جدولًا، وExcel يحاول أن يكون تقريرًا، ولا ينفع أيًّا من الجمهورين.
**والتحقق التقني لتوليدهما بالعربية أُجري قبل هذا القرار لا بعده** — القرار #134.

**140. فرع الميزة يحمل قرارات تلك الميزة فقط**
**قاعدة دائمة:** أي قرار **سياق أو نطاق** — قرار مالك يحدّ التصميم ولا ينبع من الكود المكتوب — يذهب إلى **فرع وثائق مستقل**، لا إلى فرع الميزة الجاري.
**السبب:** خلطهما يجعل مراجعة الميزة مراجعةً لقرارات لا علاقة لها بها، **ويجعل التراجع عن الميزة تراجعًا عن قرارات مالك** — وهما شيئان لا يجوز أن يُربطا بمصير واحد.
**وقعت المخالفة فعلًا في PR #15:** القرارات #135–#139 (نطاق اللاحم · لا مالية · منصة الويب · التقارير) دُفعت على فرع الشاشة لأنها وصلت أثناء العمل عليه. **وتُركت كما هي بقرار المالك:** فكّها بعد الدفع وبعد خضرة CI يكلّف أكثر مما يفيد — إعادة كتابة تاريخ ودورة فحص كاملة مقابل ترتيب لا يغيّر محتوى.
**والقاعدة تسري من الآن لا بأثر رجعي.** مكتوبة في `CLAUDE.md` لا هنا وحده.

**141. `git stash` ممنوعة في هذا المستودع — والمقارنة في worktree منفصل**
**قاعدة دائمة بلا استثناء.** أي مقارنة بشجرة نظيفة تكون بـ`git worktree add` لا بـ`git stash`.
**الحادثة:** أثناء تشخيص فشل CI في الدفعة 5 شغّلتُ `git stash -u` لمقارنة الفرع بـ`main`. لم يكن هناك ما يُخبَّأ (كان كل شيء ملتزَمًا)، فرجع `git stash pop` بخطأ «لا مخبّأ». **لم يُفقد شيء** — تُحقّق أن `HEAD` مطابق للمدفوع — **لكن النتيجة كانت ستكون مختلفة تمامًا لو كان في الشجرة عمل غير ملتزَم**، ولا شيء في الأمر كان سيحذّر.
**والخطر ليس في الأمر بل في أنه يبدو آمنًا:** `stash` تنقل العمل إلى مكان لا يظهر في `git status` ولا في `git log`، والاستعادة تعتمد على أمر ثانٍ قد لا يُنفَّذ إن انقطع المسار بينهما. أما `worktree` فلا تلمس الشجرة العاملة إطلاقًا.
**والطريقة الصحيحة هي ما استُعمل بعدها** ونجح: worktree نظيف على نفس الـcommit — وهو ما كشف السبب الحقيقي في #133.

**142. معيار الهشاشة الزمنية: زمن يتجاوز نصف المهلة**
**أي اختبار زمنه الفعلي يتجاوز نصف مهلته يُعتبر هشًّا** ويُرفع سقفه — لا يُنتظر حتى يسقط.
**المصدر تجربة لا تقدير:** اختبار الشاشة في الدفعة 5 كان عند **~370ms على مهلة 1000ms — أي 37%** — ومرّ على جهاز التطوير وسقط في CI **حتميًا في كل تشغيل**، لأن ذاكرة تحويل babel هناك باردة دائمًا (القرار #133). فالهامش الذي بدا مريحًا (الضعف والنصف) لم يكن مريحًا إطلاقًا.
**ولماذا النصف لا الثلث ولا الربع:** الفرق بين ذاكرة دافئة وباردة قِيس بأكثر من ضعفين على أول ملف يُحمَّل. **معيار النصف يعني: يمرّ ولو تضاعف الزمن** — وهو أدنى ما يحتمل هذا الفرق.
**والمهلة الأطول لا تُبطئ اختبارًا ناجحًا** — `waitFor` تعود فور تحقق الشرط، فالسقف تأمينٌ لا تكلفة.
**ولم تُفحص بقية الاختبارات على هذا المعيار بعد** — §7-ب البند 24، بقرار المالك لا يُنفَّذ الآن.
**143. بوابة التغطية صارت تقول ما قالته الأداة — لا سببًا لم يُقرأ**
**كانت تسمّي سببًا لم يُثبت.** `runCoverage` تفلتر `stdout` بحثًا عن `ERROR` **وvitest يكتب أخطاء العتبة على `stderr`** — فأي فشل عتبة في `apps/api` أو `packages/shared` يُعرض **بلا سبب إطلاقًا**، تحت عنوان «التغطية دون الحد المطلوب» **ومعه أرقام سليمة**. و`runJestCoverage` تقرأ المجرى الصحيح لكنها تفلتر أسطر «عتبة التغطية» وحدها، فاختبار ساقط يظهر بنفس العنوان الكاذب.
**والكلفة قِيست لا قُدِّرت: جولتا تشخيص كاملتان في الدفعة 5** (القرار #133) بدأتا من التغطية والسبب اختبار يسقط على مهلة زمنية.
**القاعدة الجديدة: لا يُسمّى سبب لم يُقرأ.** يُقرأ المجرَيان معًا؛ فإن حمل المخرَج سطر عتبة صريحًا عُرضت أسطر العتبة، وإلا عُرضت **آخر 25 سطرًا كما قالتها الأداة فعلًا**، بعنوان يقول صراحةً «لا سطر عتبة في المخرَج». **عرضٌ خام أصدق من تسمية مخترَعة.**
**واسم الحزمة يسبق سببها:** ثلاث أدوات تكتب بصيغ مختلفة، وسبب بلا صاحب يُقرأ على أنه سبب الحزمة الخطأ.
**ولم تُخفَّض عتبة ولم يتحوّل فشل إلى تحذير** — `ok` يبقى `status === 0` حصرًا. التغيير في **ما يُقال عن الفشل** لا في متى يقع.
**مُثبَت بمخالفتين، كلٌّ على أداة:**
- **vitest بفشل غير عتبة** (‏`404` صارت `400` في `getSite`) ← الفاحص عرض **التأكيد الساقط وملفه وسطره**: `expect(res.status).toBe(404)` عند `sites.integration.test.ts:218`.
- **jest بفشل غير عتبة** (لون حالة خاطئ) ← عرض `FAIL lib/houseStatusTone.rtl.test.tsx` وملخّص jest.
**وقبلهما مخالفة لم تُثبت شيئًا فاستُبدلت:** عكس ترتيب سرد المواقع (`asc` ← `desc`) **لم يُسقط اختبارًا واحدًا** — التأكيدات تقارن مجموعات لا ترتيبًا. **مخالفة لا تلمس تأكيدًا لا تثبت شيئًا** (نظير #128 و#130 و#132).

**144. عتبات التغطية لأربعة ملفات تحمل قرارات منتج — استثناء مصرَّح به**
`infrastructureNavigation` (قاعدة التخطّي والرجوع) · `capabilities` (من يملك الإنشاء والتعديل) · `infrastructureErrors` (نصّ 403 مقابل 404) · `houseStatusTone` (لون الحالة). **الأربعة ليست تفاصيل عرض بل سلوكًا موصوفًا** — انحدار تغطيتها انحدار في قرار منتج لا في تجميل.
**أُضيفت عند مستواها الفعلي اليوم (100%) لا عند سقف مرغوب** — العتبة تحرس ما تحقّق فلا تكذب، ولا تعِد بما لم يُبنَ.
**واستثناء مصرَّح به من المالك ومقصور على هذه الإضافة** — قاعدة «لا تعديل بوابات» تبقى سارية على ما عداها.
**مُثبَت أنها تُفرض:** تفريغ اختبار `houseStatusTone` ← `Jest: "lib/houseStatusTone.ts" coverage threshold for branches (100%) not met: 50%`. العتبة تعضّ فعلًا لا تُسجَّل فقط.

**145. البوابة قبل الالتزام — مربوطتان بـ`&&` في نفس الأمر**
**قاعدة دائمة:** لا التزام ولا دفع إلا وبوابة `check:all` خضراء، **والربط `&&` في نفس الأمر** لا سطرين متتاليين.
**الحادثة:** شغّلتُ `pnpm check:all` ثم `git commit` **في أمرين منفصلين**. البوابة طبعت `✗ التغطية`، **ومرّ الالتزام والدفع** لأن الأمر الثاني لا يعرف نتيجة الأول. اكتُشف بعد الدفع لا قبله.
**والسبب البيئي كان بريئًا** — قاعدة البيانات في الحاوية توقّفت فتخطّت 163 اختبار تكامل فسقطت التغطية، وأُعيد التحقق على نفس الشجرة المدفوعة فمرّت 11/11. **لكن البراءة لا تُصلح الإجراء:** لو كان الفشل حقيقيًا لدُفع أحمر بنفس الطريقة تمامًا.
**ولماذا قاعدة لا تنبيهًا:** «سأتذكّر أن أنظر إلى المخرَج» **ليس ضمانة** — هو بالضبط صنف الاعتماد على الانضباط الذي يرفضه المبدأ الأول (الفرض في طبقة واحدة لا باستدعاء يدوي متكرر). `&&` تجعل الشرط **بنيويًا**: الالتزام لا يُنفَّذ أصلًا إن رجعت البوابة بغير صفر.
**وهي نظير قاعدة الدمج (#104) في موضع أصغر:** كلتاهما تمنع فعلًا لا رجعة فيه بشرط صريح يسبقه، لا بثقة في اليقظة.

**146. المالك ومدير المنصة طبقتان لا تختلطان — والفصل بنيوي لا قيمة في عمود**
**التمييز الأساسي، ولا يُشتق أحدهما من الآخر ولا يُقاس عليه:**
- **«المالك» في النظام الحالي = صاحب الشركة المشتركة، داخل المستأجر.** هذا هو الموجود اليوم.
- **«مدير المنصة» = فوق المستأجرين جميعًا، وليس دورًا في أي مستأجر.** غير موجود اليوم.
**والفصل بنيوي:** مدير المنصة **لا يُخزَّن في نفس جدول أدوار المستأجر**، ولا يُشتق من دور «المالك»، ولا يُمنح بقيمة إضافية في نفس الحقل. **أي تصميم يجعل الفرق بينهما قيمة واحدة مرفوض.**
**السبب:** **خطأ واحد في شرط استعلام يحوّل مالك مزرعة إلى مدير منصة.** القيمة الواحدة تعني أن كل ما يفصل بين «صاحب مزرعة» و«من يرى كل العملاء» هو مقارنة نصّية واحدة في سطر واحد.
**والتصميم القائم اليوم يخالف هذا القرار — أُسجّله الآن لا حين نصطدم به:**
- `USER_ROLE` يحمل `platform_admin` **قيمةً سادسة** في نفس الثابت (`packages/shared/src/enums.ts:8`)، وتُخزَّن في **نفس العمود** `users.role`.
- `users.tenant_id` **قابل لـ`NULL`** ومعه فهرس جزئي `users_platform_phone_unique WHERE tenant_id IS NULL` — أي أن مدير المنصة صفٌّ بلا مستأجر **في جدول مستخدمي المستأجرين نفسه**.
- و`requireTenant` يقصّر الدائرة على هذه القيمة بالذات (`apps/api/src/middleware/tenant.ts:16`): `if (req.user.role === "platform_admin") next()`. **فسلسلة العزل كلها تُتخطّى بقيمة enum واحدة في عمود واحد** — وهو حرفيًا الخطر الذي يصفه هذا القرار.
**والمخالفة تسبق القرار ولا تُصلَح الآن** — لا شيء من مدير المنصة مبنيّ بعد. مسجَّلة في §7-ب البند 25 لتُحسم **قبل** أول سطر منه.

**147. ما يملكه مدير المنصة وما يراه — أعداد لا محتويات**
**ما يملكه:** إنشاء المستأجرين وتفعيلهم وتعليقهم · خطط اشتراك بسقوف · تعديل سقف مستأجر بعينه **بمعزل عن خطته** · **لوحة تحكم منفصلة عن التطبيق، لا شاشة داخله**.
**وما يراه:** أسماء المستأجرين · خططهم · سقوفهم · حالة اشتراكهم. **وتقارير استخدام لغرض تحسين المنصة، وهي أعداد ومقاييس لا محتويات:** عدد العنابر النشطة لكل مستأجر مقابل سقفه · عدد المستخدمين النشطين وتاريخ آخر نشاط **للمستأجر ككل** · مؤشرات تفاعل مجمَّعة (كم مستأجرًا يستعمل كل ميزة، وأي ميزة مهجورة).
**وما لا يراه إطلاقًا:** أي بيان تشغيلي — دفعات · نفوق · علف · أوزان · تقارير المزارع · **أسماء المزارع والعنابر والمواقع** · **أسماء موظفي المستأجر**.
**وثلاث قواعد تحكم هذا القسم — قواعد لا تفضيلات:**
1. **أعداد لا محتويات.** أي مقياس يكشف **واقعة داخل مزرعة بعينها** ممنوع **مهما كان مختصرًا**.
2. **لا مقاييس على مستوى الموظف الفرد.** النشاط يُجمَّع على مستوى المستأجر فقط — **النظام لا يكون أداة مراقبة على موظفي العميل.**
3. **مصدر هذه التقارير طبقة قراءة منفصلة تُخرج أعدادًا فقط**، لا استعلامات مباشرة على جداول التشغيل. **السبب: الاستعلام المباشر يجعل التوسع من عدّاد إلى محتوى تعديل سطر واحد** — إضافة عمود إلى `SELECT` قائم. الطبقة المنفصلة تجعل التوسع **تغييرًا مرئيًا في واجهة** لا سطرًا في استعلام.
**ومخالفة قائمة اليوم:** خمس شاشات لمدير المنصة **داخل تطبيق الموبايل** (`apps/mobile/app/platform/`)، والمواصفة §5-هـ تصفه بـ«شاشة واحدة بخمسة تبويبات» **داخل التطبيق**. كلها `PlaceholderScreen` فارغة، لكن الوجهة المكتوبة تخالف «لوحة منفصلة لا شاشة داخله». §7-ب البند 25.

**والدخول من مسار منفصل تمامًا — عنوان مختلف وشاشة مختلفة، والتحقق في جدول مدير المنصة وحده:**
- **لا شاشة دخول موحَّدة تبحث في الجدولين ثم تقرر.** ذلك **يعيد بناء الخلط الذي منعه #146 من باب آخر**: الفصل في التخزين بلا فصل في التحقق يترك نفس النقطة الواحدة التي يقرّر فيها سطرٌ من أنت.
- **محاولة دخول مدير منصة من شاشة التطبيق تُرفض، ومحاولة دخول مستخدم مستأجر من مسار المنصة تُرفض.** **والرسالة في الحالتين واحدة ولا تكشف أي جدول فيه الحساب** — وإلا صار فرق الرسالة أداة تعداد لحسابات المنصة (نفس منطق «الوجود ثم التعيين»، المبدأ السادس، ورسالة الرفض العامة في §11).
- **ولا يُنشأ حساب مدير منصة من أي واجهة إطلاقًا** — يُنشأ بإجراء مباشر مقيَّد. **وهو الحساب الوحيد في النظام الذي لا يُنشئه أحد من داخل التطبيق**، ولا يخضع لقاعدة «البذر عبر الـAPI حصرًا» (#27) لأنه ليس بيانات مستأجر.
**وسؤال مفتوح بلا ترجيح، القرار للمالك:** هل يُلزم مدير المنصة بتحقق بخطوتين؟ وهل يُحصر دخوله في المتصفح دون الجوال؟

**148. السقف بعدد العنابر وحده**
- **بعدد العنابر فقط.** لا سقف على المزارع ولا على المواقع — **المستأجر يوزّع عنابره كما شاء**.
- **على عنابر المستأجر كلها مجتمعة**، لا لكل مزرعة.
- **كل العنابر من نوع واحد** — النطاق لاحم حصرًا (#135)، فلا فئات ولا أوزان مختلفة في السقف.
- **عدد المستخدمين خارج السقف حاليًا** — **قرار صريح لا سكوت** (نظير درس #136)، **وقابل للمراجعة**.

**149. إدارة الاشتراكات — تنفيذ لحظي، والاختيار للمالك لا للمنصة**
**مدير المنصة يرفع ويخفض سقف عنابر أي مستأجر لحظيًا، بمعزل عن خطته، والأثر يسري فورًا لا في تجديد قادم.**
**الرفع:** يسري فورًا **بلا أي إجراء من المستأجر**. والعنابر الموقوفة بسبب سقف سابق **تعود عاملة بنفس بياناتها**.
**الخفض تحت العدد الفعلي — القاعدة الحاسمة:**
- **مدير المنصة لا يختار أي العنابر تُوقف** — **هو لا يرى أسماءها أصلًا** بحكم #147. الاختيار **لمالك المزرعة حصرًا**.
- بمجرد الخفض يدخل المستأجر حالة **«فوق السقف»**: يُمنع إنشاء أي عنبر جديد، ويُطلب من المالك اختيار العنابر التي يبقيها.
- **لا يُوقف النظام أي عنبر تلقائيًا ولا يختار نيابة عن المالك.**
**والإيقاف ليس حذفًا — قاعدة لا تُخالف:** العنبر الموقوف **يبقى بكل بياناته التاريخية كاملة**. لا يُحذف صف ولا يُفرَّغ سجل. **الإيقاف حجب للاستخدام لا إزالة**، والعودة تعيده **كما كان بالضبط**.
**والعنبر ذو الدفعة القائمة لا يُوقف حتى تُغلق دفعته فعليًا — بحدث الإغلاق لا بمرور مدة:**
- **لا يوجد أي حد زمني** يُوقف عنبرًا فيه دفعة قائمة. دفعة تتأخر عن عمرها المتوقع لأي سبب (مرض · تأخر مشترٍ · بطء نمو) **تبقى عاملة والتسجيل عليها مفتوح**.
- **وعمر الدورة المتوقع للتنبيه فقط**، ولا يُغلق دفعة ولا يُوقف عنبرًا إطلاقًا.
- وإن كانت كل العنابر الزائدة ذات دفعات قائمة، **يبقى المستأجر فوق السقف ممنوعًا من الإنشاء الجديد حتى تُغلق دفعة**.
**والعنبر قيد التجهيز بعد إغلاق دفعته: لا يُوقف أثناء التجهيز** — إيقافه وسط خطوات التعقيم **يقطع سجلها**. يصير قابلًا للإيقاف **بعد اكتمال تجهيزه ودخوله حالة الجاهزية**.
**ومعنى «موقوف» عمليًا — منصوصًا لا مستنتَجًا:** لا تسجيل جديد عليه إطلاقًا · بياناته التاريخية **تبقى قابلة للقراءة والتقارير** · **يظهر لمالك المزرعة معلَّمًا «موقوف — تجاوز سقف الاشتراك»، لا يختفي بصمت**.
**والفرض في الخادم دائمًا:** السقوف تُفرض عند الإنشاء **وعند أي تسجيل على عنبر موقوف**. **إخفاء الزر تهذيب واجهة لا حراسة** (نفس قاعدة #129 و#131). **والسقف يُقارن بالعدد الفعلي داخل المستأجر لا بأي عدد يرسله العميل** (نفس منطق «`tenant_id` من الرمز حصرًا»، المبدأ السابع).
**وكل تغيير سقف يُسجَّل في أثر لا يُمحى:** من غيّره، ومن أي رقم إلى أي رقم، ومتى.

**150. عمر الدورة المتوقع — إعداد تشغيلي لا اشتراكي**
**القيمة تحدَّد لدى مالك المزرعة لا لدى مدير المنصة.** عمر الدورة **قرار تشغيلي يختلف بالسوق والمشتري** (‏٣٠ · ٤٥ · ٥٠ · ٦٠ يومًا)، **ومدير المنصة لا يملك معطيات تشغيلية أصلًا** بحكم #147 — فوضعها عنده يناقض القرار نفسه.
- قيمة افتراضية **على مستوى المستأجر** يضبطها المالك.
- **قابلة للتجاوز لكل دفعة على حدة** عند بدئها.
- استعمالها: حساب **تاريخ الإغلاق المتوقع**، والتنبيه عند تجاوزه.
- **لا تُغلق دفعة تلقائيًا ولا توقف عنبرًا. الإغلاق فعل بشري دائمًا** (متسق مع #149 والمبدأ الرابع).

**151. سؤالان مفتوحان — بخياراتهما بلا ترجيح، القرار للمالك**
**يُسجَّلان الآن كي لا يُحسما ضمنًا بأول سطر كود** — وهو ما يحدث حين يُترك السؤال بلا تسجيل.
**١. تعريف «العنبر النشط» — والتعريف يغيّر معنى السقف نفسه:**
| الخيار | ماذا يعني |
| --- | --- |
| المنشأ | كل عنبر في القاعدة يُحتسب، ولو لم يُستعمل قط |
| الذي فيه دفعة قائمة | العنبر الفارغ لا يُحتسب — فيمكن الاحتفاظ بعنابر كثيرة معطّلة |
| الذي فيه دفعة قائمة **أو قيد التجهيز** | يشمل ما بين الدورتين |
| الذي سُجّل عليه إدخال خلال مدة | يربط السقف بالاستعمال الفعلي، ويحتاج تعريف المدة |
**و«قيد التجهيز» حالة ثالثة بين المشغول والجاهز** — تفصيلها في قرار منفصل يصل لاحقًا، ولا يُبنى عليها قبله.
**٢. عمر الدورة الافتراضي:** على مستوى **المستأجر كله**، أم **لكل مزرعة على حدة**.

**152. تهيئة بلا بناء — نقطة تحقق واحدة للإنشاء**
**في أي عمل قادم يتضمن إنشاء مزرعة أو عنبر: يمرّ الإنشاء عبر نقطة تحقق واحدة قابلة لإضافة السقف لاحقًا، ولا يُنثَر منطق الإنشاء في مواضع متعددة.**
**ولا يُضاف أي فحص سقف الآن — فقط لا يُصعَّب إضافته.**
**والحال اليوم مستوفٍ للشرط — مُتحقَّق منه لا مفترضًا:** إنشاء العنبر له **موضع كتابة واحد في المشروع كله** (`apps/api/src/services/housesService.ts` — `insert(houses)` واحدة)، يصله مسار واحد (`POST /api/farms/:farmId/houses`). فإضافة فحص السقف مستقبلًا **تعديل في دالة واحدة**. وهذا نتيجة القرار #61 (لا استعلام في route) لا صدفة.

**153. دورة تجهيز العنبر — الخطوات تفتح العنبر لا التقويم، والراحة استثناء بشرطين**
**قرار منتج يخصّ دورة حياة العنبر.** صيغته الأولى كانت **أفقر مما هو مبنيّ**، فصُحّحت بقرار المالك: **يبقى التفصيل القائم ويُصحَّح المبدأ وحده.** ما يلي هو النص النافذ.

**ما يبقى كما هو في المواصفة — ولا يُختزل:**
- **خطوات التجهيز تبقى كما في §3.3 بأسمائها** (8-9 خطوات)، ولا تُختزل إلى أربع.
- **وتبقى قابلة للإعداد لكل مستأجر** (`tenants.prep_protocol` وشاشة إعدادات المالك). **لا تُثبَّت قائمة واحدة — كل شركة لها بروتوكولها.**
- **ترتيب الخطوات ملزم. القرار #55 قائم ولا يُنقض**، ويُشطب سؤال الترتيب لأنه **محسوم**.
- **حالات العنبر الثلاث تبقى كما هي** (`تحت الإخلاء` · `تحت التنظيف والتطهير` · `في فترة الراحة`). **و«قيد التجهيز» مظلّة وصفية لها لا حالة رابعة تُضاف إلى `HOUSE_STATUS`.**
- **والمربّي يعلّم خطوات التجهيز.** **المنفّذ هو من يعلّم، وتعليم غيره عنه يجعل الأثر كذبًا** — والمشرف **يعتمد الاكتمال**. (تصحيح صريح: الصيغة الأولى منعت المربّي، وكانت خطأً.)

**والمبدأ الباقي وهو جوهر القرار: الخطوات تفتح العنبر لا التقويم.**
**لا يفتح العنبر مرور الوقت ولا مؤقّت تلقائي.** السبب: **عنبر يُفتح بانقضاء المدة والتعقيم لم يتم يستقبل دفعة في بيئة ملوّثة.** الوقت **دليل على الجاهزية لا سببٌ لها**.

**وفترة الراحة استثناء بشرطين معًا لا بشرط واحد:**
**الراحة انتظار بطبيعتها ولا يوجد فيها فعل يُنجَز** — فلا تُفتح إلا بـ **(١) مضيّ المدة** و**(٢) تأكيد بشري بالجاهزية**. **لا المدة وحدها ولا التأكيد قبل انقضائها.**
**ولهذا يجب أن يُسجَّل متى اكتملت الراحة لا متى بدأت فقط:** العمود القائم `house_prep_cycles.rest_started_at` **يقيس مدة منقضية** — وهي **بنية «التقويم يفتح العنبر» بعينها**. **تُغيَّر البنية لا التفسير.**

**والمدة متغيرة على ثلاثة مستويات:**
1. **سياسة المستأجر — المالك:** الرقم الافتراضي لكل مزارعه، **وهو من يحدد الحد الأدنى الذي لا يُنزل عنه**.
2. **مستوى المزرعة — المشرف:** يعدّل بما يناسب مزرعته **صعودًا فقط**. مزرعة بعنابر أقدم أو موقع أصعب تحتاج أطول.
3. **الدورة الواحدة — المشرف أو الطبيب:** تمديد **لهذه المرة فقط** ولا يغيّر السياسة.

**والقاعدة الحاكمة: التمديد سهل والتقصير صعب.**
**التقصير عن الحد الأدنى ممنوع إلا من الطبيب أو المالك وبسبب مكتوب.** السبب: **لو تساوى الاتجاهان، قصّرت الراحة عند أول ضغط تشغيلي، والخسارة تظهر بعد دفعتين فلا تُربط بسببها** — وهذا صنف عطب لا يصلحه التنبيه لأن الأثر مؤجَّل عن الفعل.

**والحد الأدنى المطلق ثلاثة أيام. لا ينزل أحد تحته بأي حال — لا المالك ولا الطبيب. رقم مثبَّت في النظام لا إعداد قابل للتحرير.**

**وللطبيب فوق ذلك: إلزام مدة أطول بعد دفعة مرضت، بسبب مكتوب. المرض يُبطل كل الأرقام الافتراضية.**

**والإسناد:** المشرف يسند التنظيف والتعقيم **إلى مربٍّ أو عامل بمدة مستهدفة**. المنفّذ يعلّم الخطوات، والمشرف يعتمد.

**وأثر ورقابة:** كل تعديل مدة يُسجَّل — **من غيّره · من أي رقم إلى أي رقم · ولماذا**. وتقارير المزرعة **تُظهر متوسط المدة الفعلية مقابل المستهدفة**، فمزرعة تنهي راحة عشرة أيام في يومين دائمًا **تظهر رقمًا لا شكوى**.

**وسؤال واحد يبقى مفتوحًا بلا ترجيح، القرار للمالك:** هل يمكن إغلاق دفعة وبدء تجهيز عنبر آخر في نفس الوقت، أم التجهيز يخصّ **عنبرًا واحدًا في كل مرة**؟
**وسؤال الترتيب شُطب** — محسوم بالقرار #55.

**وإعادة فحص التعارضات بعد التصحيح — خمسة زالت واثنان قائمان (§7-ب البند 26):**

| # | التعارض الأصلي | الحال بعد التصحيح |
| --- | --- | --- |
| ١ | عدد الخطوات (4 مقابل 8-9) | **زال** — الخطوات تبقى كما في §3.3 |
| ٢ | فترة الراحة | **تحوّل ولم يزل** — لم يعد «5 مقابل 10» بل **«رقم إلزامي واحد» مقابل «سياسة بثلاثة مستويات بحدٍّ أدنى مطلق»** |
| ٣ | «قيد التجهيز» حالة رابعة | **زال** — مظلّة وصفية لا قيمة في `HOUSE_STATUS` |
| ٤ | المربّي لا يعلّم | **زال، والقائم كان صحيحًا** — §12.2 يعطيه «✅ المسندة» وهذا هو الصواب |
| ٥ | الترتيب مفتوح مقابل #55 | **زال** — #55 قائم والسؤال مشطوب |
| ٦ | قائمة ثابتة مقابل `prep_protocol` | **زال** — تبقى قابلة للإعداد لكل مستأجر |
| ٧ | `rest_started_at` | **قائم ومؤكَّد** — تُغيَّر البنية لا التفسير |

**والتصحيح استحدث ستّ فجوات لم تكن في التعارضات الأصلية — لأن النموذج الجديد أغنى من المخطط القائم:**
1. **المستويان ٢ و٣ للمدة بلا تخزين إطلاقًا.** `tenants.min_rest_days integer NOT NULL DEFAULT 10` **موجود** فالمستوى الأول مغطّى، **و`farms` بلا أي عمود مدة**، و`house_prep_cycles` بلا مدة مستهدفة.
2. **الحدّ الأدنى المطلق (٣ أيام) بلا موضع.** `min_rest_days` **إعداد قابل للتحرير بلا أرضية** — يمكن ضبطه على 1 اليوم. والقرار يطلب **رقمًا مثبَّتًا في النظام** فوق الإعداد.
3. **لا اعتماد للمشرف.** `house_prep_steps` فيه `completed_by` **ولا `approved_by`** — و«المنفّذ يعلّم والمشرف يعتمد» يحتاج حقلين لا حقلًا.
4. **لا إسناد ولا مدة مستهدفة على الخطوة.** لا عمود مُسنَد إليه ولا مدة مستهدفة، والقرار ينصّ على «يسند … بمدة مستهدفة».
5. **لا أثر لتعديلات المدة.** «من · من أي رقم إلى أي رقم · ولماذا» بلا جدول ولا أعمدة.
6. **قاعدة صلاحية غير ممثَّلة في §12.2:** «التقصير عن الحد الأدنى للطبيب أو المالك بسبب مكتوب» — والمصفوفة لا تحمل صفًّا لتعديل مدة الراحة أصلًا.

**ولا يُلمس شيء من هذا الآن** — القرار سياق لا تنفيذ، والتجهيز كله في المرحلة 3.

**154. الأمن الوقائي وسجل الزيارات — التصريح والبلاغ على المزرعة، والدخول على العنبر**
**قرار منتج يخصّ الأمن الحيوي. تسجيل فقط — لا سطر كود، ولا يُفتح قبل إغلاق المرحلة 2-أ.**
**صيغته الأولى صُحّحت بخمسة أحكام من المالك قبل أي بناء** (التفصيل في «التصحيح» أدناه). ما يلي هو **النص النافذ**.

**أولًا — مستويان لا مستوى واحد.**
**التصريح يُصدَر على مستوى المزرعة، والدخول الفعلي يُسجَّل على مستوى العنبر.**
تصريح واحد قد يغطّي عدة عنابر في المزرعة نفسها، وكل دخول فعلي إلى عنبر يُسجَّل مستقلًّا.
السبب: التصريح **إذن مسبق** بنطاق، والدخول **واقعة** في مكان وزمن — خلطهما في سجل واحد يجعل
تتبّع «من دخل هذا العنبر بالضبط ومتى» مستحيلًا، وهو السؤال الوحيد الذي يُطرح عند تفشٍّ مرضي.

**ثانيًا — من يصرّح يُحدَّد بالتخصص لا بالرتبة.**

| نوع الزيارة | من يصرّح |
| --- | --- |
| بيطرية · تطعيم | **الطبيب** |
| فنيون · صيانة · توريد · خدمات | **المشرف** |
| أي نوع | **المالك (`owner`)** — مالك المستأجر، **يصرّح في كل مزارعه** |
| — | **المربّي لا يصرّح إطلاقًا** |

الرتبة لا تعني معرفة بمخاطر الزيارة. من يفهم أثر الزائر على العنبر هو من يأذن به:
الطبيب يعرف ما تحمله زيارة بيطرية، والمشرف يعرف ما تحمله شاحنة علف.

**ولا وجود لدور «مالك مزرعة» في النظام** — الأدوار خمسة، و`owner` فيها مالك المستأجر كله.
أينما ورد اللفظ في الصيغة الأولى فالمقصود `owner`، وقد صُحّح في هذا النص كله.

**ثالثًا — بلاغ الدخول غير المصرّح: يُرفع على المزرعة، ونوع مستقل لا يتحوّل إلى تصريح أبدًا.**

- **البلاغ يُرفع على المزرعة لا على العنبر. الحقل الإلزامي الوحيد: المزرعة.**
  **والموقع يُوصف بنص حر اختياري** («العنبر الشمالي» · «بوابة العلف») — نصًّا يكتبه المبلّغ،
  لا اختيارًا من قائمة عنابر.
- **والقرار #129 قائم ولا يُنقض ولا يُستثنى منه.** المربّي **لا يرى أسماء العنابر غير المُسندة
  له — ولا لغرض البلاغ**.
- **والمشرف يربط البلاغ بالعنبر الصحيح عند المعالجة** — وهو يرى مزارعه كاملة فالربط في مقدوره
  وحده. **ويُسجَّل الربط بمن قام به.**
- **لا يصير تصريحًا بأثر رجعي — إطلاقًا.** السبب حاسم: لو جاز تحويله، لصار كل دخول غير مصرّح
  يُغطّى بتصريح يُكتب بعده، **فيصير عدّاد الدخول غير المصرّح صفرًا دائمًا وبلا معنى**. العدّاد
  الذي لا يمكن أن يرتفع لا يقيس شيئًا.
- **يظهر للمشرف والطبيب والمالك.**
- **وللمالك أن ينشئ تصريحًا منفصلًا لاحقًا، والبلاغ يبقى قائمًا** كما هو — التصريح الجديد واقعة
  مستقلة لا تصحيح للبلاغ ولا إلغاء له (المبدأ الرابع).
- **مجهول في العرض، مُسجَّل في النظام.** الجهتان مقصودتان معًا: **الإخفاء يمنع المحاباة** (مربٍّ
  لا يبلّغ عن زيارة أذن بها مشرفه إن ظهر اسمه)، **والتسجيل يمنع البلاغات الكيدية** (بلاغ بلا أثر
  للمبلّغ سلاح بلا مسؤولية).
- **والإخفاء قرار طبقة خدمة لا طبقة عرض: هوية المبلّغ تُحذف من جسم الرد في الخادم لا من الشاشة.**
  **نفس منطق #129 حرفيًّا** — ما لا يخصّ المستخدم غائب من الرد الخام لا معروضًا مُعطَّلًا. إخفاءٌ
  في الواجهة وحدها يُكشف بأول استدعاء API مباشر، فيكون سرًّا معلنًا.
- **البلاغ واقعة لا اتهام.** لا حقل يسمّي موظفًا مسؤولًا عن الدخول. النظام يسجّل **ما حدث**،
  ومن يُسأل عنه قرار إداري خارج النظام.
- **ولا يُعاد استخدام `health_observations` للبلاغ الأمني** — الأسباب في «التصحيح» أدناه،
  وأوّلها حاسم وحده.

**رابعًا — ما يُسجَّل، وكله أثر غير قابل للتعديل (المبدأ الرابع).**

| السجل | ما يحمله |
| --- | --- |
| **التصريح** | نوع الزيارة · من أصدره · الزائر وجهته · العنابر المسموحة · مدة الصلاحية |
| **الدخول** | العنبر · الوقت · الربط بتصريح أو ببلاغ |
| **البلاغ** | المزرعة (إلزامي) · وصف الموقع نصًّا (اختياري) · المبلّغ (مُسجَّل ومحجوب عن الرد) |
| **ربط البلاغ بعنبر** | العنبر · **من ربط** · متى |

**والربط سجل جديد لا `UPDATE` على البلاغ** — نتيجة مباشرة للمبدأ الرابع: البلاغ واقعة محفوظة
كما رُفعت، ومعالجة المشرف واقعة ثانية مرتبطة بها. تعديل البلاغ ليحمل العنبر يمحو أن المبلّغ
لم يذكره، وهو بالضبط ما يجب أن يبقى ظاهرًا.

**خامسًا — أربعة أسئلة مفتوحة، بخياراتها وبلا ترجيح. القرار للمالك.**

1. **من يسجّل الدخول الفعلي؟** المربّي عند الاستقبال · الزائر نفسه · المصرِّح مسبقًا.
2. **الدخول إلى عنبر بدفعة في أيامها الأولى:** يُمنع، أم يُسمح بتصريح استثنائي؟
3. **زائر قادم من مزرعة أخرى:** يُسجَّل ذلك ويُنبَّه عليه؟ وهل تُفرض مدة حظر؟
4. **إجراءات الدخول الإلزامية** (تبديل ملابس · رش · تطهير): قائمة تحقّق تُعلَّم عند كل زيارة، أم
   تبقى خارج النظام؟

**سادسًا — التصحيح: خمسة أحكام، وأثرها على التعارضات الثلاثة.**

**١. البلاغ على المزرعة لا على العنبر — والسبب أهمّ من الحكم.**
**تصميمي الأول طلب من المربّي اختيار عنبر لا يراه**: جعلتُ العنبر الحقل الإلزامي الوحيد، وقرار
العزل #129 يُغيّب عنابره غير المُسندة من الرد الخام أصلًا — فكان الحقل الوحيد الإلزامي هو بالضبط
ما لا يستطيع رؤيته. وعرضتُ حينها خيارين، أحدهما **استثناء ضيّق في #129** يعرض أسماء عنابر مزرعته
«لغرض البلاغ وحده».
**والحكم: الاستثناء الضيّق في قرار عزل يتوسع دائمًا، فالحل في تغيير التصميم لا في ثقب القاعدة.**
استثناء بغرض واحد اليوم يصير مسارًا تُبنى عليه شاشة غدًا، ثم يُنسى غرضه ويبقى الثقب. **البلاغ
على المزرعة يلغي الحاجة إلى الاستثناء من أصلها** — والمشرف، وهو يرى مزارعه كاملة بحكم إسناده
(#128)، يربطه بالعنبر عند المعالجة. **القاعدة تبقى صمّاء، والوظيفة تتحقق.**

**٢. الإخفاء في الخادم لا في الشاشة** — أُدرج في «ثالثًا» أعلاه حكمًا نافذًا بعد أن كان ملاحظة.

**٣. `health_observations` لا يُعاد استخدامها للبلاغ الأمني.** تشبهه شكلًا (بلاغ يرفعه المربّي ·
حالة · رد · تصعيد للمالك بعد ساعتين، §3.9) — **والتشابه الشكلي فخّ**. أربعة أسباب:

1. **🔴 `batch_id NOT NULL` — وهو حاسم وحده.** يمنع البلاغ **أثناء التجهيز والراحة، وهي أخطر
   أوقات الاختراق الحيوي** لأن العنبر مفتوح ومغسول وبلا طير يُلاحَظ عليه شيء. جدولٌ يعجز عن حمل
   البلاغ في أخطر أوقاته لا يصلح له.
2. `symptoms` و`severity` كلاهما `NOT NULL` — والقرار يجعل **المزرعة الحقل الإلزامي الوحيد**.
3. `created_by NOT NULL` ويُعاد في الرد — والقرار يطلب حذف المبلّغ من جسم الرد.
4. فيها `vet_response` و`responded_by` أي **مسار حسم** — والقرار ينصّ أن البلاغ لا يتحول إلى
   تصريح أبدًا. إعادة استعمالها تُدخل مسار الحسم من الباب الخلفي.

**٤. «مالك المزرعة» = `owner`** — مالك المستأجر، يصرّح في كل مزارعه. صُحّح اللفظ في النص كله.

**٥. صفوف §12.2 أُضيفت** في `backend-technical-spec.md`: **إصدار تصريح زيارة · تسجيل دخول فعلي ·
رفع بلاغ أمني · قراءة البلاغ الأمني** — وصفّ البلاغ القائم أُعيدت تسميته **«رفع بلاغ صحي»**
تمييزًا له. **وما لم يُحسم بعد وُسم `؟` لا `❌`** — والفرق مقصود: `❌` منع، و`؟` **غير محسوم**.
**والسكوت ليس قرارًا** — نفس درس صفّ «قراءة بيانات عنبر» في #126، حين قُرئ سكوت المصفوفة إباحةً.

**وأثر التصحيح على التعارضات الثلاثة: زالت كلها.**

| # | التعارض في الصيغة الأولى | الحال بعد التصحيح |
| --- | --- | --- |
| ١ | 🔴 «المربّي يبلّغ عن أي عنبر في مزرعته» يخالف #126 و#129 المفروضين بالكود | **زال — بتغيير التصميم لا بثقب القاعدة.** البلاغ على المزرعة، و#129 يبقى صمّاء بلا استثناء |
| ٢ | «مالك المزرعة» ليس دورًا قائمًا | **زال** — اللفظ صُحّح إلى `owner` |
| ٣ | §12.2 بلا صفوف للتصريح والدخول والبلاغ الأمني | **زال** — أربعة صفوف أُضيفت، وغير المحسوم وُسم `؟` |

**والتغطيات الجزئية الستّ — واحدة صارت حكمًا، وواحدة مواتية، والباقي قائم:**

1. **`health_observations`** — صارت **حكمًا** (لا يُعاد استخدامها)، لا ملاحظة.
2. **التصريح بالمزرعة يطابق نموذج الإسناد القائم — تغطية مواتية لا معطِّلة.**
   `user_assignments.farm_id` للمشرف والطبيب (#128) يجيب سلفًا عن «من يحق له التصريح في هذه
   المزرعة»، **وهو نفسه ما يجعل ربط البلاغ بالعنبر في مقدور المشرف** — بلا جدول جديد ولا مفهوم جديد.
3. **«الدفعة في أيامها الأولى» مشتق بلا عمود جديد** — `batches.start_date` موجود.
4. **الإخفاء قرار طبقة خدمة** — صار حكمًا في «ثالثًا».
5. **الأثر غير القابل للتعديل مغطّى مبدئيًا** — المبدأ الرابع و`entity_audit_log` قائمان.
6. **قيدان إلزاميان لحظة البناء، لا قبله:** أي مسار جديد يحمل معرّف كيان في الرابط **يُضاف إلى
   `ENTITY_ID_PATH_PATTERNS`** وإلا كان الفرض معطَّلًا صامتًا (§7-ب البند 18)، وأي جدول جديد
   يحمل `tenant_id` **مفاتيحه الأجنبية مركَّبة** (قاعدة CLAUDE.md، القرار #122).

**ولا وجود لأي بنية زيارات أو أمن حيوي في المشروع إطلاقًا** — لا جدول تصريح ولا زائر ولا دخول في
ملفات المخطط الاثني عشر، ولا ذكر لـ«زيارة/تصريح/زائر» في أيٍّ من الوثيقتين. ما ورد فيهما من
«تطهير» يخصّ دورة تجهيز العنبر (§3.3) لا الزائرين.

**ولا يُلمس شيء من هذا الآن** — القرار سياق لا تنفيذ. مسجَّل دَينًا في `work-plan.md` §7-ب البند 27.

**155. من يُدخل رصيدًا لا يصادق عليه — مبدأ عام ملزم**
**قرار مالك. تسجيل فقط — لا سطر كود.**

> **من يُدخل رصيدًا لا يصادق عليه.**
> **والرصيد قبل المصادقة مسوّدة لا يُبنى عليها صرف.**

**السبب: رصيد افتتاحي بلا شاهد يجعل كل حركة بعده مبنية على رقم لم يره أحد.**
الدفتر يضمن أن **مجموع الحركات = الرصيد** (المبدأ الثالث) — **ولا يضمن أن نقطة البداية صحيحة**.
خطأ في أول رقم لا يظهر أبدًا كتناقض في الدفتر، لأن كل شيء بعده متسق معه. يظهر عند أول جرد،
بعد أشهر، فرقًا لا يُعرف مصدره — **ويُقرأ عندها سرقةً أو إهمالًا وهو خطأ إدخال في اليوم الأول.**

**ويسري على كل رصيد يدخل النظام بلا حركة سابقة تولّده** — الافتتاحي عند التشغيل لأول مرة،
ورصيد منتج جديد، ورصيد موقع جديد.

**والمبدأ محقَّق فعلًا في موضعين، ومنقوض في ثالث — بالبنية لا بالنية:**

| الموضع | الحال |
| --- | --- |
| `shipments` | **محقَّق** — `sent_by` و`received_by` شخصان بالضرورة، والاستلام أعمى (§3.6) |
| `inventory_transfers` | **محقَّق** — `confirmed_by` و`confirmed_at` منفصلان عن `created_by` |
| `stocktakes` | **منقوض** — `opened_by` وحده. **ولا `closed_by` ولا `approved_by` إطلاقًا** |

**والنقض في `stocktakes` هو بالضبط موضع الرصيد الافتتاحي** (`is_opening boolean`) — أي أن
الموضع الوحيد الذي يدخل فيه رقم بلا حركة تولّده هو الموضع الوحيد بلا مصادقة. التفصيل في #156.

**156. فحص المخزون — ست إجابات، وثلاث ثغرات، وستة أعمال بلا صاحب**
**فحص وتسجيل فقط — لا سطر كود، ولا حلول مقترحة. القرار للمالك.**

**الحالة العامة أولًا:** **المخزون مصمَّم بالكامل في المواصفة وغير مبنيّ إطلاقًا في الكود.**
المخطط موجود (`packages/db/src/schema/inventory.ts` — سبعة جداول)، **ولا مسار API واحد مبنيّ**:
الموجّهات المبنية هي `auth` · `sites` · `farms` · `houses` · `settings` · `health` (فحص جاهزية
لا صحة قطيع). فكل ما يلي **ثغرات تصميم تُحسم قبل البناء، لا عيوب في كود قائم.**

**١. العلف يدخل العنبر بشحنة، ويدخل المخزن باستلام من مورّد — وكلاهما مُصمَّم، والفاعل غير معيَّن.**

المسار في المواصفة كامل وغير مبتور: `المورّد → المخزن المركزي → شحنة → مخزون العنبر → استهلاك`
(§3.5 في الوثيقة الشاملة، §13.1 في التقنية). وله:
- **نوع حركة:** `استلام` في `inventory_movement_type` — أول قيمة في الـenum.
- **جدول مصدر الحركة:** «استلام من موّرد ← استلام (+) مخزن» (§13.2).
- **مسار API:** `POST /inventory/warehouse-receipt` (§17).
- **حقل مورّد:** `products.supplier`.

**والناقص شيئان اثنان لا أكثر — وهذا حجم الثغرة بدقة:**
1. **لا صفّ في مصفوفة الصلاحيات §12.2** لاستلام المورّد. المصفوفة تحمل «إنشاء شحنة» و«تأكيد
   الاستلام» و«جرد · تحويل · هالك» — **ولا تحمل «استلام من مورّد»**. فمن يضغطه غير محسوم.
2. **لا شاشة في أي دور من الخمسة.** المربّي عنده «الاستلام» وهو **استلام شحنة داخلية من المخزن
   لا من مورّد**؛ والمشرف عنده «إرسال شحنة · جرد · تحويل · هالك»؛ والمالك عنده **«بطاقة المخزن
   المركزي: الرصيد وأيام تغطيته» — عرضٌ بلا زر**؛ والطبيب كتالوج فقط.

**فالثغرة ليست في الدفتر ولا في النموذج — بل في الفاعل والواجهة.** مسار موجود لا شاشة تصله،
وفعل موجود لا دور يملكه. **وحجمها: صفّ صلاحيات واحد وشاشة واحدة** — لكن **بلا حسمهما لا يدخل
المخزنَ كيلو علف واحد**، فكل ما بعده (شحنة · استهلاك · تغطية · تقرير فاقد) يبدأ من رصيد صفر.

**٢. نعم، المخزن كيان — وعلى مستوى الشركة (المستأجر) وحده.**

`warehouses` = `id · tenant_id · name · is_active · created_at`. **لا `site_id` ولا `farm_id`.**
والتعليق في المخطط والمواصفة معًا: «مخزن افتراضي واحد لكل مستأجر يُنشأ تلقائيًا — الجدول يسمح
بأكثر لاحقًا». والوثيقة الشاملة تسمّيه **«مخزن المالك»** و«المخزن المركزي».

**وهنا ثغرة بنيوية أكبر من موضع المخزن نفسه: المخزن خارج نموذج الإسناد كلّيًا.**
- `user_assignments` تحمل `house_id` أو `farm_id` **حصرًا** (`CHECK` يفرض أحدهما لا غير) —
  **ولا `warehouse_id`**. فلا سبيل لإسناد مستخدم إلى مخزن أصلًا.
- و`enforceEntityAccess` يمسح **`houseId` · `batchId` · `farmId` · `siteId` فقط** — **ولا يعرف
  `locationType` ولا `locationId`**، وهما مفتاحا كل حركة مخزون (`inventory_movements` ·
  `stocktakes` · `wastage` · `inventory_transfers`).

**والأثر المحدَّد حين يُبنى المخزون:** عقد التحويل في المواصفة هو
`from_location_type/id → to_location_type/id` — **لا `houseId`**. فطلبٌ يسمّي عنبرًا غير مُسند
للمستخدم في `to_location_id` **يمرّ من الفرض المركزي دون أن يراه**، لأن المفتاح الذي يقرؤه
الحارس غير موجود في الجسم. **وهذا نفس صنف الثقب الذي أُغلق في القرارين #126 و#129، في مسار لم
يُبنَ بعد** — يُسجَّل الآن كي لا يُكتشف بعد البناء.

**٣. الرصيد الافتتاحي: عمود واحد في المخطط، وصفر ذكر في الوثيقتين.**

- **الموجود:** `stocktakes.is_opening boolean NOT NULL DEFAULT false` — وهو الأثر الوحيد لفكرة
  «رصيد افتتاحي» في المشروع كله.
- **وغير الموجود، وهو الأكثر:** **لا ذكر لكلمة «افتتاحي» في أيٍّ من الوثيقتين** — لا في الشاملة
  ولا في التقنية. **ولا شاشة، ولا مسار مستقل** (`POST /inventory/stocktake` عام)، **ولا صفّ
  صلاحيات** يميّزه عن الجرد الدوري، **ولا قيد** يمنع فتح جرد افتتاحي ثانٍ بعد أن جرت حركات.
- **ومن يُدخل:** `opened_by` — شخص واحد.
- **ومن يصادق: لا أحد.** `stocktakes` فيه `closed_at` **بلا `closed_by`**، ولا `approved_by`.
  **فحتى الجرد الدوري يُغلق دون أن يُعرف من أغلقه** — والافتتاحي أشدّ، لأنه لا يُقارن برصيد دفتري
  سابق أصلًا: `stocktake_items.book_qty` يساوي صفرًا، فكل ما يُكتب فرقٌ يُقبل كما هو.

**وهذا هو الموضع المنقوض في المبدأ #155 بعينه.** ومقارنته بجاريه في نفس الملف تُظهر أن الغياب
سهو لا اختيار: `inventory_transfers` — **جدول أقل خطرًا** لأنه ينقل رصيدًا موجودًا — **فيه
`confirmed_by` و`confirmed_at`**؛ و`stocktakes` — الذي **يخلق** الرصيد من العدم — بلا شيء.

**٤. عمل أمين المخازن بلا صاحب — والأدوار الخمسة لا يغطّيه أيٌّ منها.**

الأقرب هو **المشرف**: يرسل الشحنات ويجري الجرد والتحويل والهالك (§12.2، §5-ب). **لكنه لا يصلح
لسببين بنيويين لا لسبب ذوقي:**
1. **نطاقه مزرعة والمخزن مستأجر.** المشرف مُسنَد بـ`farm_id` (#128) — «مسؤول مزرعة أو أكثر».
   والمخزن **لا يخصّ مزرعة**. فمشرف مزرعة واحدة يتصرّف في مخزن الشركة كله، **ولا قيد يمنعه**
   لأن المخزن خارج نموذج الإسناد (البند ٢).
2. **صلاحيته بالفئة لا بالمكان.** «المشرف → علف ومستلزمات · الطبيب → أدوية ولقاحات» (§3.5).
   فالمخزن الواحد يتقاسمه دوران **بحسب صنف البضاعة**، **ولا أحد مسؤول عن المخزن ككل** — عن
   دقّة رصيده، وجرده الدوري، وترتيب استلاماته.

**والنتيجة الملموسة: «من استلم شحنة المورّد ووقّع عليها؟» سؤال بلا جواب في النظام** — لا صفّ
صلاحيات، ولا حساب، ولا حقل. **والعمل يقع في الواقع كل أسبوع.**

**٥. نعم، للأدوية واللقاحات مخزون كامل — والصرف مملوك، والاستلام لا.**

- **مخزونها قائم:** `products.category` فيها `دواء · لقاح · فيتامين`؛ و`inventory_movements`
  لا تفرّق بين صنف وآخر (`product_id` وحده)؛ ولها حركة خصم خاصة: `تنفيذ علاج (−)` (§13.2).
- **ومن يملك الصرف — بثلاث يدين لا يد واحدة، وهذا مقصود:**
  **الطبيب** يأمر (`health_tasks`، «إنشاء مهمة صحية: ✅ vet وحده») **ويرسل** (شحنة أدوية ولقاحات)؛
  **المربّي** ينفّذ فيُخصم من رصيد عنبره (`تنفيذ علاج`)، **ولا يكتب اسم دواء ولا جرعة إطلاقًا**
  (§3.8)؛ **والمالك** يملك الكل. **والمربّي لا يصرف دواءً من تلقاء نفسه** — وهذا قيد قائم في
  التصميم لا ثغرة.
- **والناقص هو الاستلام نفسه:** دخول الأدوية من المورّد إلى المخزن **بلا فاعل معيَّن** — نفس
  ثغرة البند ١، وأثرها هنا أثقل لأن الدواء يحمل **صلاحية وفترة سحب** (`withdrawal_days`)
  و**ظروف تخزين** (`storage_conditions`)، وكلها تُلتقط **لحظة الاستلام** أو لا تُلتقط أبدًا.

**وملاحظة بنيوية مستقلة:** `wastage_reason` فيه **«انقطاع تبريد»** — سببُ تلفٍ يخصّ **مكانًا**،
والمخزن **صفّ واحد بلا ظروف تخزين ولا موقع فرعي**. فالنظام يقبل تسجيل نتيجة عطل **لا يستطيع
نسبته إلى مكان**، ولا يستطيع منعه ولا التنبيه إليه. `storage_conditions` صفة **على المنتج**
لا على المخزن.

**٦. ستة أعمال أخرى بلا صاحب مسجَّل — بنفس المعيار.**

1. **تغطية غياب المربّي — أوسعها أثرًا.** `user_assignments` = `user_id · house_id · farm_id ·
   tenant_id · created_at`: **بلا بداية ولا نهاية ولا بديل ولا جدول تاريخ.** فغياب يوم واحد له
   ثلاثة مخارج كلها معطوبة: **(أ)** لا سجل يومي — فيسقط العنبر في «عنابر لم تُسجَّل» ويُحسب على
   المربّي في تقرير الالتزام؛ **(ب)** يدخل غيره بحسابه — **فيكذب الأثر**، وهو نفس ما رُفض صراحة
   في القرار #153 («المنفّذ هو من يعلّم، وتعليم غيره عنه يجعل الأثر كذبًا»)؛ **(ج)** إسناد دائم
   ثم حذفه — و`DELETE /users/:id/assignments` **يمحو أنه كان مُسندًا أصلًا** بينما
   `daily_logs.created_by` يبقى يشير إليه. **فالنظام يعرف من سجّل، ولا يعرف من كان مسؤولًا يومها.**
2. **تغطية غياب المشرف — وهي مسدودة تمامًا.** صفّ «تأكيد وإنشاء تصحيح» في §12.2: **✅ للمشرف
   وحده، و❌ للمالك نفسه.** فغياب المشرف **يجمّد تصحيح السجلات ولا يملك أحد تجاوزه — ولا المالك**.
   وهذا الصفّ الوحيد في المصفوفة كلها الذي يملكه المشرف حصرًا دون المالك.
3. **«العامل» في دورة التجهيز.** القرار #153: «المشرف يسند التنظيف والتعقيم **إلى مربٍّ أو عامل**
   بمدة مستهدفة» — و**«عامل» ليس دورًا في `user_role`** ولا يملك حسابًا، بينما
   `house_prep_steps.completed_by` يشير إلى `users`.
4. **من يصادق على الجرد وفروقه** — البند ٣ أعلاه: `closed_at` بلا `closed_by`،
   و`stocktake_items.reason` **نصّ حرّ** يُغلق به فرق بأي حجم.
5. **الناقل — وهنا تعارض لا فجوة.** غيابه عن النظام **مقصود** (`carrier_name` و`vehicle_number`
   نصّ حرّ، والاستلام الأعمى يقوم على ألّا يكون في النظام). **لكن تقرير الفاقد يطلب صراحةً
   «الفروقات حسب المربي والمشرف والناقل»** — **فيجمّع بحسب كيان غير موجود**: تجميعٌ على نصّ حرّ
   يُكتب يدويًا في كل شحنة، «أبو محمد» و«ابو محمد» ناقلان مختلفان. **التقرير الثالث من الخمسة
   غير قابل للتنفيذ كما هو موصوف.**
6. **مسؤول الصيانة.** `house_status` فيه **«تحت الصيانة»** و«معطّل» — **ولا دور يملك الصيانة**
   ولا سجل لما أُصلح ومتى ومن أصلحه. المشرف يغيّر الحالة فقط، فيبقى العنبر معطّلًا في النظام
   بلا أثر لما يجري فيه فعلًا.

**ولا يُلمس شيء من هذا الآن** — فحص وتسجيل، والقرار للمالك. مسجَّل دَينًا في `work-plan.md`
§7-ب البند 28.

**157. أحكام المخزون — سبعة على ما كشفه الفحص #156**
**قرارات مالك. تسجيل فقط — لا سطر كود، ولا تنفيذ الآن.**

**١. المخزن خارج نموذج الإسناد — شرط إغلاق بلفظه، لا دَينًا مؤجَّلًا.**

> **لا يُبنى أي مسار مخزون قبل أن يعرف الفرض المركزي `locationType`/`locationId` ويفرض عليهما ما
> يفرضه على العنبر والمزرعة.**

**ومكتوب في مدخل المرحلة 3 نفسها لا في جدول الديون وحده** — على نمط البند 25 (القرار #147).
السبب في وضعه هناك: دَينٌ في جدول لا يمنع أحدًا من فتح المرحلة، **وشرطٌ في مدخلها يمنع**. وهذا
البند من صنف لا يُصلَح بعد البناء: `enforceEntityAccess` يمسح اليوم `houseId · batchId · farmId ·
siteId`، وعقد التحويل `from/to_location_type/id` **لا يحمل أيًّا منها** — فكل مسار مخزون يُبنى
قبل الحسم يمرّ من الحارس دون أن يراه، **ويصير إصلاحه لاحقًا مراجعةَ كل مسار كُتب** بدل تعديل
طبقة واحدة (المبدأ الأول).

**٢. الرصيد الافتتاحي — مصادِق مسجَّل، وقيد يمنع الثاني، ومن أغلق الجرد الدوري.**

- **يُسجَّل: من أدخله · من صادق عليه · ومتى.** (والمصادِق غير المُدخِل — المبدأ #155.)
- **وقيد يمنع افتتاحيًا ثانيًا بعد أول حركة.** **والتصحيح بعدها يكون جردًا لا افتتاحيًا.**
- **والجرد الدوري كذلك يحتاج من أغلقه** — لا `closed_at` وحده.

**السبب: التحويل ينقل رصيدًا موجودًا وفيه تأكيد، والافتتاحي يخلق الرصيد من العدم وبلا شاهد.**
**والافتتاحي الثاني يمحو كل ما قبله بلا أثر** — فرقٌ بين تصحيحين: الجرد يُقيَّد **حركةَ تسوية**
تبقى في الدفتر ويُسأل عنها، والافتتاحي **يعيد تعريف نقطة الصفر** فتصير كل حركة سابقة معلَّقة على
رقم لم يعد موجودًا. **وهو المحو الوحيد الممكن في نظام دفتره غير قابل للحذف.**

**٣. الناقل كيان لا نصّ حرّ — ويُحسم به تعارض تقرير الفاقد.**

**والقرار #154 هو ما يجعله ممكنًا:** الناقل زائرٌ يدخل المزرعة، فمكانه الطبيعي **سجل الزيارات**
لا حقلٌ نصّي في الشحنة. **والتعارض المسجَّل في #156 يُحسم بذلك:** «الفروقات حسب المربي والمشرف
**والناقل**» — **التجميع على نصّ يدوي مستحيل** («أبو محمد» و«ابو محمد» ناقلان)، **وعلى كيان ممكن**.

**والاستلام الأعمى لا يتأثر إطلاقًا** — وهذا محلّ لبسٍ يُقطع صراحةً: **الناقل معلوم في الاستلام
الأعمى، والكمية وحدها هي المخفية.** المربّي يعرف من وقف أمامه بالشاحنة؛ ما لا يعرفه هو **الكمية
المرسلة** حتى يعدّ ويُدخل عدده. فتسجيل الناقل كيانًا **لا يكشف شيئًا مما يُخفيه §3.6**.

**٤. استلام الأدوية من المورّد — يُضاف صراحةً، وتُلتقط عنده ثلاثة.**
**الصلاحية · فترة السحب · ظروف التخزين.**

**السبب: هذه الحقول تُلتقط لحظة الاستلام أو لا تُلتقط أبدًا.** بعدها لا مصدر لها: العبوة تدخل
المخزن، ويُخلط الوارد الجديد بالقديم في رصيد واحد، **فلا يبقى في النظام ما يقول متى تنتهي صلاحية
ما في اليد**. وهي الحقول الثلاثة التي **يُبنى عليها منعٌ لا تنبيهٌ فقط**: تحذير فترة السحب عند
تصفية الدفعة (§14.6) لا يعمل بلا تاريخ استلام، و«انقطاع تبريد» في `wastage_reason` لا يُنسب
إلى شيء بلا ظروف تخزين مسجَّلة (البند ٥).

**٥. تعارض مسجَّل — نتيجة عطل لا يُنسب إلى مكان.**

`wastage_reason` فيه **«انقطاع تبريد»**، و`warehouses` = `id · tenant_id · name · is_active`
**بلا ظروف تخزين ولا موقع فرعي**، و`storage_conditions` صفة **على المنتج** لا على المكان.

> **فالنظام يقبل تسجيل نتيجة عطل لا يستطيع نسبته إلى مكان ولا منعه.**

يُسجَّل تعارضًا لا يُحلّ الآن.

**٦. ستة أعمال بلا صاحب — مرتَّبة بخطورتها كما ظهرت في الفحص، بلا حلول مقترحة.**

1. **تغطية غياب المربّي — الأولى لأنها تمسّ مصدر كل البيانات.** `user_assignments` بلا بداية ولا
   نهاية ولا بديل ولا جدول تاريخ. وغياب يوم واحد مخارجه ثلاثة كلها معطوبة: **لا سجل** (فيسقط
   العنبر في «لم تُسجَّل» ويُحسب على المربّي في تقرير الالتزام)، أو **دخول غيره بحسابه** (**فيكذب
   الأثر** — وهو ما رُفض صراحة في #153)، أو **إسناد يُحذف بعده** (والحذف يمحو أنه كان مُسندًا
   بينما `daily_logs.created_by` يبقى يشير إليه). **فالنظام يعرف من سجّل ولا يعرف من كان مسؤولًا
   يومها.**
2. **تغطية غياب المشرف — الثانية لأنها مسدودة تمامًا لا ناقصة.** صفّ «تأكيد وإنشاء تصحيح» في
   §12.2 **✅ له حصرًا و❌ للمالك نفسه** — وهو الصفّ الوحيد في المصفوفة كلها الذي يملكه المشرف دون
   المالك. **فغيابه يجمّد تصحيح السجلات ولا يملك أحد تجاوزه.** (والسؤال المفتوح في البند ٧.)
3. **أمين المخازن.** لا دور يغطّيه: المشرف نطاقه مزرعة والمخزن مستأجر، وصلاحيته **بالفئة** لا
   بالمكان — **فلا أحد مسؤول عن المخزن ككل**، عن دقّة رصيده وجرده وترتيب استلاماته.
4. **من يصادق على الجرد وفروقه.** يُغلق اليوم بـ`closed_at` بلا `closed_by`، و`reason` نصّ حرّ
   يُغلق به فرق بأي حجم. (يعالجه البند ٢ عند التنفيذ.)
5. **«العامل» في دورة التجهيز.** القرار #153 يسند التنظيف «إلى مربٍّ **أو عامل**»، و«عامل» ليس
   قيمة في `user_role` ولا يملك حسابًا، بينما `house_prep_steps.completed_by` يشير إلى `users`.
6. **مسؤول الصيانة.** `house_status` فيه «تحت الصيانة» و«معطّل» بلا دور يملك الصيانة ولا سجل لما
   أُصلح ومتى ومن أصلحه — فيبقى العنبر معطّلًا في النظام بلا أثر لما يجري فيه فعلًا.

**٧. سؤال مفتوح بلا ترجيح — القرار للمالك.**

> صفّ **«تأكيد وإنشاء تصحيح»** مملوك للمشرف وحده و**❌ للمالك**:
> **هل يبقى كذلك، أم يملك المالك تجاوزه عند غياب المشرف؟**

يُسجَّل بلا ترجيح، **ولا يُبنى الصفّ على حاله الحالي قبل حسمه** — بناؤه كما هو اليوم يثبّت
انسدادًا قد لا يكون مقصودًا أصلًا.

**ولا يُلمس شيء من هذا الآن** — أحكام تُنفَّذ في المرحلة 3، والبند ١ منها **شرط إغلاق لفتحها**.

**158. الإسناد بمدة لا بحالة — لكل إسناد بداية ونهاية**
**قرار مالك على غياب المربّي (الأول في ترتيب الخطورة، #157 البند ٦). تسجيل فقط — لا سطر كود.**

> **الإسناد يصير بمدة لا بحالة: لكل إسناد بداية ونهاية.**

- **البديل مربٍّ آخر يُسند للعنبر من تاريخ إلى تاريخ، والأصلي يبقى مسندًا في تاريخه هو.**
- **لا يُحذف إسناد ولا يُعدَّل بأثر رجعي.**
- **قرار الإسناد البديل بيد المشرف أو المالك.**
- **وكل سجل يُنسب لمن سجّله فعلًا — لا يدخل أحد بحساب غيره إطلاقًا.**
- **وتقرير الالتزام يُحسب على المسؤول في ذلك اليوم لا على المسند اليوم.**
- **والقاعدة نفسها تسري على إسناد المشرف والطبيب بالمزرعة** — إسناد بمدة، **وبديل بقرار المالك**.

**السبب: المخارج الثلاثة القائمة كلها معطوبة.**
**لا تسجيل** يُحسب غيابًا على المربّي في تقرير الالتزام؛ **ودخول بحساب غيره يكذب الأثر** — وهو
ما رُفض صراحة في #153 («المنفّذ هو من يعلّم، وتعليم غيره عنه يجعل الأثر كذبًا»)؛ **وحذف الإسناد
يمحو أنه كان مسؤولًا** بينما `daily_logs.created_by` يبقى يشير إليه.
**والإسناد بمدة يحلّ الثلاثة معًا: النظام يعرف من كان مسؤولًا في أي يوم.**

**وشرط إغلاق بلفظه:**

> **المرحلة 3 لا تُفتح قبل تنفيذ الإسناد بمدة بالكامل.**

**ومكتوب في مدخل المرحلة 3 نفسها مع شرط البند 25**، لا في جدول الديون وحده — لنفس سبب #157
البند ١: دَينٌ في جدول لا يمنع فتح المرحلة، وشرطٌ في مدخلها يمنع.

**ولا يُنفَّذ داخل الدفعة 6** — البذر لا يمسّ الإسناد، **وإدخال تغيير بنيوي في منتصف دفعة جارية
خطأ**.

**وأثره على المصمَّم اليوم — جرد كامل، بلا لمس شيء.**

**أولًا: القيد `CHECK` لا يتأثر إطلاقًا.**
`(house_id IS NULL) <> (farm_id IS NULL)` يخصّ **مستوى** الإسناد (عنبر أو مزرعة) لا **زمنه**.
يبقى حرفيًا كما هو، والحكم عمودي عليه لا معارض له.

**ثانيًا: الفهرسان الجزئيان يسقط معناهما — وهذا أخطر ما في التغيير.**
`UNIQUE (user_id, house_id) WHERE house_id IS NOT NULL` ونظيره للمزرعة يمنعان اليوم **صفًّا
ثانيًا لنفس المستخدم على نفس العنبر**. **وبالإسناد بمدة يصير الصفّ الثاني مشروعًا وضروريًا**:
مربٍّ مُسند في يناير، يغيب فيحلّ غيره، ثم **يعود إليه في مارس** — صفّان مشروعان **يرفضهما الفهرس
اليوم بـ409**.
**فالفهرس لا يُحذف بل يتغيّر سؤاله**: من «هل تكرّر الإسناد؟» إلى «**هل تتداخل مدّتان؟**».
**وهذا لا يُعبَّر عنه بفهرس فريد إطلاقًا** — يحتاج `EXCLUDE USING gist` على `daterange`
(وامتداد `btree_gist`)، أو حارسًا داخل معاملة تحت قفل. **وهو نفس درس القرار #128:** فهرس فريد
على عمود يقبل `NULL` بدا كافيًا وكان يقبل التكرار صامتًا — **والتفرّد هنا سؤال زمني لا سؤال
تساوٍ، فأداته مختلفة**.

**ثالثًا: كل استعلامات النطاق الخمسة تصير خاطئة صامتة — لا معطَّلة.**
كلها تسأل «**هل يوجد صفّ؟**» بلا زمن:

| الموضع | الدالة/الاستعلام |
| --- | --- |
| `apps/api/src/lib/entityScope.ts` | `assignedHousesFilter` · `visibleFarmCondition` (فرعا المربّي وغيره) · `visibleHouseCondition` |
| `apps/api/src/middleware/entityAccess.ts` | `assertHouseAccess` — `EXISTS` على `user_assignments` |

بعد التغيير سيوجد صفّ **منتهٍ** فتُقرأ إجابته «نعم»، **فمربٍّ انتهت مدته يبقى يرى عنبره ويكتب
فيه**. **وهذه ثغرة صلاحيات لا خلل عرض** — الإسناد يقيّد القراءة كما يقيّد الكتابة (#126).
**فالتغيير ليس إضافة عمودين، بل تغيير سؤال كل استعلام إسناد في المشروع من «هل مُسند؟» إلى «هل
مُسند اليوم؟».**

**رابعًا: رسالتا خطأ تصيران خاطئتين نصًّا.**
`apps/api/src/lib/pgErrors.ts` — «المستخدم مُسند لهذا العنبر بالفعل» و«…لهذه المزرعة بالفعل»
مربوطتان باسمَي الفهرسين. بعد التغيير يصير السبب الحقيقي «**تتداخل المدّتان**» لا «مُسند بالفعل»،
**والاسم الذي يُطابَق عليه سيتغيّر أصلًا** فتسقط المطابقة صامتة إن لم تُحدَّث.

**خامسًا: مواضع مصمَّمة لم تُبنَ بعد وتتأثر.**
- **`DELETE /users/:id/assignments` (§17) يتعارض مع الحكم نصًّا** — «لا يُحذف إسناد». المسار
  المصمَّم نفسه يتغيّر إلى **إنهاء مدة لا حذف**. وهو مسار المرحلة 3، مملوك للمشرف — **متسق مع
  «قرار البديل بيد المشرف أو المالك»**.
- **تقرير الالتزام (الخامس من الخمسة)** — «لكل مربي: نسبة الأيام المسجَّلة». يُحسب اليوم حتمًا
  على المسند **الآن** لأن لا زمن في الإسناد. **فالحكم يجعله غير قابل للحساب صحيحًا بلا الإسناد
  بمدة** — أي أن **المرحلة 4 تعتمد على هذا الحكم أيضًا، لا المرحلة 3 وحدها**.
- **§12.2** — صفّ «إدارة المستخدمين» (✅ owner · ✅ supervisor مرّبين فقط) يغطّي بديل **المربّي**
  ولا يغطّي بديل **المشرف والطبيب**، إذ «المشرف لا يدير مستخدمين آخرين». **والحكم يجعل بديلهما
  بيد المالك وحده — فلا تعارض، والتغطية قائمة بالمصادفة لا بالنص.**
- **`daily_logs.created_by` لا يتغيّر** — «كل سجل يُنسب لمن سجّله فعلًا» **محقَّق بالبنية أصلًا**،
  والحكم يثبّته صراحةً لا يستحدثه.
- **البذر:** لا سكربت بذر في المستودع أصلًا اليوم (`scripts/` فيه `check-all` و
  `check-commit-messages` وحدهما) — **فتصريح «لا يُنفَّذ داخل الدفعة 6» بلا كلفة قائمة**.

**سادسًا: الترحيل بلا فقد بيانات.**
عمودان على جدول قائم + استبدال فهرسين. الصفوف القائمة تحتاج تاريخ بداية — و**`created_at`
موجود فيها فيصلح مصدرًا**، ونهايتها مفتوحة. **ولا صفّ يُفقد ولا يُعاد بناؤه.**

**وحجم التغيير بالأرقام — أربعة ملفات إنتاجية لا أكثر:**

| الموضع | العدد |
| --- | --- |
| ملفات إنتاجية تتأثر | **4** (`farms.ts` · `entityScope.ts` · `entityAccess.ts` · `pgErrors.ts`) |
| استعلامات يتغيّر سؤالها | **5** |
| قيد `CHECK` | **0 — لا يتأثر** |
| فهارس تُستبدل | **2** (والبديل ليس فهرسًا فريدًا) |
| رسائل خطأ | **2** |
| ملفات اختبار تُدرج إسنادات | **3** (10 مواضع إدراج) |
| ترحيل | **1** |

**والأهم في تقدير الحجم: لا شيء من هذا مبنيّ خطأً.** كله مبنيّ على افتراض كان صحيحًا وقته،
**فالتغيير توسيع نموذج لا إصلاح عطب**. **وكلفته تقع كلها في طبقة واحدة** لأن القرار #126 جمع نطاق
الإسناد في ملف واحد (`entityScope.ts`) بدل نثره في الموجّهات — **ولو كان منثورًا لكان هذا الحكم
مراجعةَ كل موجّه في المشروع**.

**وخمسة أحكام لاحقة أُضيفت إلى هذا القرار بعد تسجيله — تحسم سؤاله المفتوح وترفع ثلاثة بنود من
أثر جانبي إلى صلب الشرط:**

**١. القراءة المعتمدة هي (ب) القسمة — لا تداخل إطلاقًا.**

> **تُغلق مدّة الأصلي عند بداية غيابه، ويُفتح له صفّ جديد عند عودته.**

**ونصّ الحكم الأول احتمل قراءتين** — «الأصلي يبقى مسندًا في تاريخه هو» و«لا تعديل بأثر رجعي»
تحتملان التداخل والقسمة معًا، وسُجّل السؤال حينها بلا ترجيح. **وهذه هي القراءة المعتمدة، والأولى
تسقط.**

**السبب: الحكم جاء ليجعل «من كان مسؤولًا في هذا اليوم؟» سؤالًا بجواب واحد، والتداخل يُبقيه
بجوابين فيلغي غرضه.** عنبرٌ بمسؤولَين في نفس اليوم يعيد بالضبط الحالة التي جاء الحكم لإنهائها،
ويحتاج فوقها قاعدة أسبقية تُسأل في كل استعلام.
**وإغلاق مدة مستقبلًا ليس تعديلًا بأثر رجعي — الأصلي يبقى مسندًا في تاريخه.** ما مضى لا يُمسّ،
وما يُغلق إنما يُغلق من لحظة الغياب فصاعدًا.

**٢. منع التداخل قيد في قاعدة البيانات لا فحص في الكود.**

> **لا يجوز إسنادان لنفس العنبر بمدتين متداخلتين.**

**والفهرسان الفريدان يُستبدلان لا يُحذفان** — **سؤالهما يتغيّر** من «هل تكرّر الإسناد؟» إلى
«**هل تتداخل مدّتان؟**»، ويبقى مكانهما في القاعدة.

**ودرس القرار #128 يُسجَّل هنا صراحةً لأنه يتكرر بأداة مختلفة:** هناك بدا فهرس فريد عادي كافيًا،
**وكان يقبل التكرار صامتًا** لأن `NULL` في فهرس فريد «مميّزة دائمًا» — وأُثبت على القاعدة بثلاثة
صفوف متطابقة قُبلت. **والخطر نفسه قائم هنا:** فهرس فريد على `(user_id, house_id, start_date)`
يبدو كافيًا **ويقبل مدّتين متداخلتين ببدايتين مختلفتين صامتًا**. **التداخل ليس تساويًا، فلا
تمنعه أداة التساوي.**
**والفرض في القاعدة لا في الخدمة** — لنفس سبب قاعدة المفتاح المركَّب في `CLAUDE.md`: حارس طبقة
الخدمة إجرائي، **يُعيد الثقبَ أي مسار كتابة جديد لا يمرّ به**.

**٣. الاستعلامات الخمسة في صلب شرط الإغلاق — لا أثرًا جانبيًا له.**

> **شرط الإغلاق لا يتحقق بإضافة العمودين، بل بتحويل كل استعلام إسناد من «هل يوجد صفّ؟» إلى
> «هل يوجد صفّ ساري اليوم؟».**

**السبب: مربٍّ انتهت مدته يبقى يرى عنبره ويكتب فيه** — **وهي ثغرة صلاحيات لا خلل عرض**، لأن
الإسناد يقيّد القراءة كما يقيّد الكتابة (#126). **وعمودان مضافان بلا تحويل الاستعلامات يجعلان
النظام أسوأ من حاله اليوم**: تصير في القاعدة مدد منتهية **يقرؤها الفرض سارية**، فيُظنّ القيد
قائمًا وهو مفتوح.

**ولكل استعلام من الخمسة مخالفة متعمَّدة عند التنفيذ تثبت أن الصفّ المنتهي لا يمرّ** — خمس
مخالفات لا واحدة. الاستعلامات هي: `assignedHousesFilter` · `visibleFarmCondition` (فرع المربّي)
· `visibleFarmCondition` (فرع المشرف والطبيب) · `visibleHouseCondition` · `assertHouseAccess`.
**واختبار واحد يغطّي الخمسة لا يثبت شيئًا عن أربعة منها** — نفس درس #128 حين مرّ اختبارٌ لأن
حارس الأدوار سبق الحارس المقصود، ودرس #107 حين بدت مخالفة مطبَّقة ولم تكن.

**٤. `DELETE /users/:id/assignments` — تعارض محسوم لا مفتوح.**
**المسار يصير إنهاء مدة لا حذفًا.** وكان مسجَّلًا في الجرد أعلاه ملاحظةً؛ **وهو الآن حكم**:
الحذف يمحو أن المستخدم كان مسؤولًا، وهو بعينه العطب الثالث الذي جاء القرار ليغلقه.

**٥. تقرير الالتزام (الخامس من الخمسة، المرحلة 4) يعتمد على هذا الحكم — وغير قابل للحساب صحيحًا
بدونه.**
«لكل مربي: نسبة الأيام المسجَّلة» يُحسب اليوم حتمًا على المسند **الآن**، إذ لا زمن في الإسناد —
**فينسب أيامًا كان مسؤولًا عنها غيره، ويحجب أيامًا كان مسؤولًا عنها هو**. **فالشرط يخدم مرحلتين
لا مرحلة واحدة**، ويُسجَّل هنا كي لا يُكتشف عند بناء التقارير فيُبنى التقرير على أساس خاطئ.

**ولا يُلمس شيء من هذا الآن** — أحكام تُنفَّذ قبل فتح المرحلة 3.

**159. تحويل العلف بين العنابر والمزارع — الكمية لا تكون في مكانين ولا في لا مكان**
**قرار منتج. تسجيل فقط — لا سطر كود، ولا يُفتح قبل إغلاق المرحلة 2-أ.**

**أولًا — الحاجة.**
عنبر انتهت دفعته وبقي فيه علف. **المشرف يصدر أمر صرف** إلى المربّي المسؤول عن العنبر المرسِل،
**ويصل إشعار للعنبر المستلم، والمستلم يؤكد الاستلام.**

**ثانيًا — الأطراف والأدوار.**

- **المشرف يصدر أمر التحويل، وهو وحده من يبدأه.**
- **جائز بين عنابر المزرعة الواحدة وبين مزارع مختلفة** — **بشرط أن تكون المزرعتان مسندتين لنفس
  المشرف لحظة إصدار الأمر.**
- **والشرط يُفحص لحظة الإصدار فقط.** تغيّر الإسناد لاحقًا **لا يبطل حركة مسجَّلة ولا يغيّر تاريخها**.
- **مربّي العنبر المرسِل ينفّذ ويسجّل الخروج، ومربّي العنبر المستلم يؤكد الوصول**، والطرفان يريان
  الحركة **في سجل عنبرهما**.

**ثالثًا — القاعدة الحاكمة: الكمية لا تكون في مكانين ولا في لا مكان.**

> **ثلاث حالات لا اثنتان: صادرة · في الطريق · مستلمة.**

**عند الخروج تُخصم من رصيد المرسِل وتدخل «في الطريق»، ولا تدخل رصيد المستلم إلا بتأكيده.**
**السبب: بلا حالة وسيطة، أي تأخير في التأكيد يعني كمية اختفت من النظام أو حُسبت مرتين.**

**رابعًا — التأكيد بالكمية لا بزر.**
**المستلم يؤكد بالكمية التي استلمها فعلًا، لا بزر «تم».**
**وأي فرق بين المصروف والمستلم يُسجَّل عجزًا ظاهرًا ولا يُبتلع بصمت، ويُعرض على المشرف.**
**السبب: تأكيد بزر واحد يصير شكليًا ولن يُكتشف فرق أبدًا — وحينها السجل بلا قيمة.**

**خامسًا — السجل: أثر لا يُمحى.**

لكل حركة، **على العنبرين معًا**: الكمية المصروفة · الكمية المستلمة · نوع العلف · **المشرف الذي
أصدر الأمر** · المزرعة المرسِلة والمستلمة · من صرف · من استلم · تاريخ ووقت الخروج · تاريخ ووقت
الوصول.

**والسجل لا يُعدَّل ولا يُحذف منه شيء — من أحد إطلاقًا، ومالك المزرعة داخل في ذلك.**
**التصحيح بحركة مضادة يُنشئها المالك**، تشير إلى الأصلية وتحمل **سببًا مكتوبًا**. **والأصل يبقى
مرئيًا معلَّمًا «مصحَّح»، والرصيد يعكس التصحيح.**

**السبب: سجل يستطيع أحد تحريره ليس سجلًا — والفرق أن التعديل يصير غير مرئي.** ولو عدّل المالك
مباشرة، **انتهى أول خلاف على كمية مفقودة بأن يقول السجل ما يريده المالك، فيفقد الجميع الثقة به
ويعودون إلى الورق.**

**والملاحظات والنزاع:**
**للمشرف وللمالك إضافة ملاحظات على أي حركة. والملاحظة إضافة لا تعديل** — لا تغيّر الحركة الأصلية
ولا أرقامها، **وتُسجَّل بصاحبها ووقتها**.
**وحين يُختلف على حركة تُعلَّم «محل نزاع»، ولا يُغلق النزاع إلا بموافقة المالك والمشرف معًا. لا
يغلقه أحدهما منفردًا.**

**والرصيد لا ينتظر النزاع:**
**الكمية المستلمة المسجَّلة هي المعتمدة، والرصيد يمشي عليها من اللحظة الأولى.** والنزاع يجري
**بجانب الرصيد لا فوقه**، ويُغلق بحركة تصحيح إن اتفق الطرفان على لزومها، أو **بلا تصحيح إن اتفقا
على صحة الحركة**.
**السبب: لو انتظر الرصيد الاتفاق، بقيت عنابر برصيد معلّق شهورًا وتعطّل التشغيل.**

**والنزاع المفتوح يُرى: يظهر في تقرير المزرعة مع مدته. الظهور هو ما يدفع الطرفين إلى الحسم.**

**سادسًا — الأسئلة الأربعة: اثنان حُسما واثنان يبقيان مفتوحين.**

1. **التحويل بين مزرعتين والأمن الوقائي — مفتوح، ومربوط بسؤال #154 في بند واحد.**
   **لا يُحسم منفردًا**: القرار #154 يحمل سؤالًا مفتوحًا عن «**زائر قادم من مزرعة أخرى: يُسجَّل
   ويُنبَّه عليه؟ وهل تُفرض مدة حظر؟**» — والشاحنة الداخلية بين مزارع المستأجر الواحد هي الحالة
   نفسها بلفظ آخر. **فحسم أحدهما دون الآخر يُنتج قاعدتين متناقضتين لناقلَي عدوى متطابقين.**
   **يُحسمان معًا.**
2. **أمر لم يُنفَّذ أو لم يُؤكَّد لمدة طويلة — مفتوح بلا ترجيح:** يُلغى تلقائيًا · يبقى معلّقًا
   ويُنبَّه عليه · غير ذلك.
3. **رفض المرسِل التنفيذ (علف تالف مثلًا) وكيف يُسجَّل — مفتوح بلا ترجيح.**
4. **مرحلة العلف — محسوم: لا يُمنع.**
   **لا يُمنع تحويل علف مرحلة إلى عنبر في مرحلة مختلفة. يُنبَّه ولا يُمنع.**
   **السبب: §3.4 تنصّ أن العنبر يستهلك مرحلتين معًا في أيام الانتقال** — فالمنع يكسر حالة مشروعة
   موصوفة في المواصفة نفسها. **وهو تطبيق للمبدأ الخامس** (لا يُمنع الميدان بسبب الإدارة).

**سابعًا — فحص المبنيّ والمواصفة: خمسة تعارضات وست تغطيات جزئية.**

**والحالة العامة كما في #156: المخزون مصمَّم وغير مبنيّ إطلاقًا** — لا مسار API واحد. فما يلي
فروق بين الحكم وبين **تصميم قائم**، لا عيوب في كود يعمل.

**التعارضات الخمسة:**

1. **🔴 طرفا التحويل بلا حارس في الطبقتين معًا — وهذا أخطرها، والتحويل بين مزرعتين يوسّعه لا
   يضيّقه.** `inventory_transfers.from_location_id` و`to_location_id` **عددان صمّاء بلا أي مفتاح
   أجنبي إطلاقًا وبلا قيد `CHECK`** — مفاتيح الجدول الثلاثة على `created_by` و`confirmed_by`
   و`product_id` وحدها. **والدفتر حلّ هذه المشكلة نفسها:** `inventory_movements` يحمل `house_id`
   و`farm_id` **بمفاتيح مركَّبة**، وقيد `CHECK` يربط `location_id = house_id`. **فجدول الأمر غير
   محروس والدفتر محروس** — وقاعدة `CLAUDE.md` (المفتاح المركَّب) لا تنطبق على عمود متعدد الأشكال
   أصلًا. ويجتمع هذا مع **شرط الإغلاق (#157 البند ١)**: `enforceEntityAccess` لا يقرأ
   `locationType`/`locationId`. **فالطرفان بلا حارس في القاعدة ولا في الفرض المركزي معًا.**
2. **الصفّ القائم في §12.2 أوسع من الحكم.** «جرد · تحويل · هالك: ❌ farmer · ✅ supervisor بفئته ·
   ✅ vet بفئته · ✅ owner» — **والحكم يجعل المشرف وحده من يبدأ التحويل**. الطبيب يخرج بحكم الفئة
   (فئته أدوية لا علف)، **لكن المالك يبقى مخوَّلًا في الصفّ القائم** خلافًا للحكم. **والصفّ لا
   يحمل المربّي إطلاقًا** بينما الحكم يعطيه فعلين: **تسجيل الخروج وتأكيد الوصول بالكمية**.
3. **قاعدة إغلاق النزاع تختلف عن القاعدة القائمة.** §12.2: «**حسم نزاع: ✅ للمالك وحده**»
   (ونزاع الشحنة كذلك: `dispute_closed_by`). **والحكم: لا يُغلق إلا بموافقة المالك والمشرف معًا.**
   **فصنفا نزاع بقاعدتَي إغلاق مختلفتين في نظام واحد** — يُسجَّل الفرق ولا يُوحَّد من تلقاء نفسه.
4. **مسار التصحيح بحركة مضادة غير ممثَّل في التحويل.** `shipments` فيها `correction_of_uuid`،
   و`inventory_transfers` **بلا شيء منه** — فالوصف الوحيد المتاح اليوم لتصحيح تحويل هو `UPDATE`،
   **وهو الممنوع بالمبدأ الرابع وبالحكم صراحةً**.
5. **الملاحظات بلا موضع، وإضافتها في `reason` تكون تعديلًا.** `inventory_transfers.reason` عمود
   واحد يُكتب عند الإنشاء، و`log_notes` مخصّص للسجل اليومي. **فكتابة ملاحظة المشرف أو المالك في
   `reason` هي `UPDATE` على سجل محفوظ** — أي أن تنفيذ «الملاحظة إضافة لا تعديل» بالبنية القائمة
   **ينقض الحكم نفسه**.

**والتغطيات الجزئية الستّ:**

1. **`inventory_transfers` قائم — بحالتين لا ثلاث.** `created_at` ثم `confirmed_by`/`confirmed_at`.
   **ولا حالة «في الطريق» ولا موضع يحمل الكمية فيها**، ولا عمود حالة أصلًا (بينما `shipments` فيها
   `status`: معلّقة · مستلمة · ملغاة).
2. **كمية واحدة لا كميتان.** `quantity` عمود واحد — **لا مصروفة ولا مستلمة ولا فرق**.
   **والتأكيد القائم (`confirmed_by` + `confirmed_at`) هو بالضبط «زر تم» الذي يرفضه الحكم الرابع.**
3. **والنمط المطلوب مبنيّ كاملًا في مكان آخر من النظام نفسه.** `shipments` تحمل: `sent_quantity` ·
   `counted_quantity` · `variance` · `variance_status` · `dispute_status` · `dispute_outcome` ·
   `dispute_closed_by` · `correction_of_uuid` · `notes_sender`/`notes_receiver` · `status` · ورمز
   تسليم. **فالنظام يعرف كيف يفعل هذا كله — لكنه يعرفه للشحنة وحدها.** **والحكم يسوّي بين
   مسارَي نقلٍ بُني أحدهما غنيًّا والآخر فقيرًا.**
4. **الحاجة معترف بها نصفَ اعتراف.** تصفية الدفعة تفرض «**جرد إلزامي للمتبقي**» (§14.6 وشاشة
   المالك 6) — **فالنظام يعرف أن علفًا يتبقّى عند نهاية الدفعة، ويعدّه ولا ينقله**، فيبقى الرصيد
   مقيَّدًا على عنبر يدخل التنظيف والراحة. **الحكم يصل الطرف المفقود.**
5. **حركتا الدفتر موجودتان وتوقيتهما فراغ لا تعارض.** §13.2: «تحويل ← تحويل صادر (−) + تحويل
   وارد (+)» **في سطر واحد كأنهما لحظة واحدة**، ولا نصّ يحدّد متى تُقيَّد الثانية. **والحكم الثالث
   يحسم أنهما لحظتان وبينهما «في الطريق».**
6. **سابقتان تخصّان سؤالين مفتوحين:** **الرفض** له نمط قائم (`health_task_executions.failed` و
   `failure_reason` و`POST /health-tasks/:id/fail`) — يخصّ السؤال ٣. **والناقل** غائب عن التحويل
   بالكلّية بينما الشحنة فيها `carrier_name`/`vehicle_number` — **ويتقاطع مع #157 البند ٣**
   (الناقل كيان لا نصّ حرّ).

**وثلاث ملاحظات على الأسئلة المفتوحة — وقائع لا ترجيحات:**

- **السؤال ١ امتداد مباشر لسؤال مفتوح قائم:** القرار #154 يسجّل في أسئلته «**زائر قادم من مزرعة
  أخرى: يُسجَّل ويُنبَّه عليه؟ وهل تُفرض مدة حظر؟**» — **ولا يذكر الشاحنات الداخلية بين مزارع
  المستأجر الواحد**. فالسؤالان يُحسمان معًا أو يتناقضان.
- **السؤال ٤ يصطدم بواقع مسجَّل في المواصفة:** §3.4 «كل مرحلة صنف مخزون مستقل — **وفي أيام
  الانتقال يُستهلك نوعان معًا**». **فالعنبر يحمل مرحلتين مشروعًا في أيام الانتقال**، ومنعٌ مطلق
  يصطدم بذلك. **واقعة تُسجَّل، والقرار للمالك.**
- **وشرط «مسندتين لنفس المشرف لحظة الإصدار» يتّكئ على القرار #158.** الإسناد اليوم بلا زمن،
  **فـ«لحظة الإصدار» غير قابلة للتحقق بأثر رجعي أصلًا**؛ وبعد الإسناد بمدة تصير قابلة.
  **فهذا الحكم تابع لشرط الإغلاق الثالث ولا يُبنى قبله** — تفاعل موجب يُسجَّل كي لا يُعاد اكتشافه.

**ثامنًا — ستة أحكام لاحقة على ما كشفه الفحص، أُضيفت إلى هذا القرار بعد تسجيله.**

**١. طرفا التحويل يُحرسان في الطبقتين — وتبعية صريحة.**

> **قيد في القاعدة يربط النوع بالمعرّف كما في `inventory_movements`، وفرض مركزي يقرأ
> `locationType`/`locationId`.**

**وهذا الحكم تابع لشرط الإغلاق (#157 البند ١) ولا يُبنى قبله — تبعية صريحة لا ترتيبًا مستحسنًا.**
السبب: الحارس المركزي هو من يقرأ الطرفين، **فبناء التحويل قبل أن يعرف الحارسُ `locationType`/
`locationId` يُنتج مسارًا يمرّ دون أن يُرى** — ثم يصير إصلاحه مراجعةَ ما كُتب بدل تعديل طبقة
واحدة (المبدأ الأول). **والقيد في القاعدة ليس تكرارًا للفرض:** الأول يمنع صفًّا فاسدًا مهما كان
مسار الكتابة، والثاني يمنع فاعلًا غير مخوَّل — **وحذف أيٍّ منهما يترك ثقبًا مختلفًا** (نفس بنية
القرار #129).

**٢. نمط الشحنات هو المرجع — `inventory_transfers` يُوسَّع ليحمل ما تحمله `shipments`:**
**كمية مصروفة ومستلمة وفرق وحالته · وحالة الحركة الثلاثية · والنزاع ونتيجته ومن أغلقه · ومسار
تصحيح.**

**السبب: النظام يعرف هذا النمط ويطبّقه على الشحنة وحدها، ومسارا نقلٍ بُني أحدهما غنيًّا والآخر
فقيرًا يجب أن يتساويا.** والفرق بينهما اليوم **ليس فرقًا في الخطر بل في تاريخ الكتابة**: الشحنة
تُنقَل من المخزن إلى العنبر بكل ضماناتها، **والتحويل يُنقَل بين عنبرين — وقد يعبر مزرعتين —
بكميّة واحدة وتأكيد بزر**.

**٣. الملاحظات في جدول مستقل لا في `reason`.**
**الكتابة في `reason` تعديل على سجل محفوظ**، **فتنفيذ «الملاحظة إضافة لا تعديل» بالبنية القائمة
ينقض الحكم نفسه** — والجدول المستقل يحمل الملاحظة بصاحبها ووقتها ويترك الحركة الأصلية وأرقامها
كما هي.

**٤. صفّ §12.2 يُصحَّح.**
**المشرف وحده يبدأ التحويل، والمالك لا يبدأه.** **ويُضاف المربّي بفعلين: تسجيل الخروج وتأكيد
الوصول.** فالصفّ القائم كان **أوسع من الحكم في طرف** (يخوّل المالك) **وأضيق منه في طرف** (لا
يحمل المربّي أصلًا).

**٥. فرق قاعدة إغلاق النزاع مقصود لا سهو — ويُسجَّل كذلك.**

> **نزاع الشحنة يغلقه المالك لأنه طرف مع المورّد، ونزاع التحويل يلزمه المالك والمشرف معًا لأن
> المشرف طرف فيه.**

**فالقاعدتان مختلفتان لأن موقع المالك مختلف**: في الشحنة هو الحَكَم من خارج الواقعة، وفي التحويل
**المشرف هو مُصدِر الأمر** فلا يُحسم نزاعٌ هو طرف فيه بغيابه. **ولا تُوحَّد القاعدتان لاحقًا
بحجّة الاتساق** — التوحيد هنا يبدو أنظف ويغيّر المعنى، كما في القرار #131.

**٦. شرط «مسندتين لنفس المشرف لحظة الإصدار» تابع للقرار #158 — تبعية صريحة، ولا يُبنى قبله.**
الإسناد اليوم **بلا زمن**، فـ«لحظة الإصدار» غير قابلة للتحقق أصلًا: `user_assignments` تقول من هو
مُسنَد **الآن** لا من كان مُسندًا وقتها. **وبعد الإسناد بمدة يصير الشرط قابلًا للفحص وقت الإصدار
وللتحقق بعده** — وهو ما يجعل الحكم «تغيّر الإسناد لاحقًا لا يبطل حركة مسجَّلة» **ممكن الإثبات لا
مجرّد نيّة**.

**ولا يُلمس شيء من هذا الآن** — أحكام تُنفَّذ في المرحلة 3 بعد شرطَي الإغلاق. مسجَّل دَينًا في
`work-plan.md` §7-ب البند 30.

**160. استقبال الكتاكيت وحركة الطيور — رصيد واحد وخروج مصنَّف**
**قرار منتج. تسجيل فقط — لا سطر كود، ولا يُفتح قبل إغلاق المرحلة 2-أ.**

**أولًا — سلسلة الاستقبال.**

- **المالك يشتري ويُدخل الشحنة ببياناتها كاملة:** السلالة · المورّد أو الفقاسة · الناقل · الكمية.
- **المشرف يصادق ويوزّعها على العنابر المستهدفة.**
- **مربّي كل عنبر يؤكد استلام حصته.**
- **والدفعة تبدأ بتأكيد المربّي** — لا بالشراء ولا بالمصادقة. **الدفعة تبدأ حين تصل الطيور فعلًا.**

**والشحنة الواحدة تتوزع على عنابر متعددة:** شحنة واحدة لها **توزيعات**، كل توزيعة **يؤكدها مربّي
عنبرها بمعزل عن غيره**.
**السبب: تصميم «شحنة لعنبر واحد» يفرض تفتيتها يدويًا ويضيع أثر أنها شحنة واحدة من مورّد واحد.**

**ثانيًا — الاستلام الأعمى.**

**المربّي يؤكد بما عدّه فعلًا ولا يرى الرقم المتوقع.**
**والعدّ بالصناديق: عدد الصناديق وعدد ما بها** — لا رقمًا كليًا يُكتب من الذاكرة.

**والنافق عند الوصول حقل مستقل: يُخصم من الكمية ويُنسب للمورّد أو الناقل لا للعنبر.**
**🔴 و«يُنسب للمورّد أو الناقل» ضُيِّق بالقرار 208:** يبقى **خارج عهدة المربّي وخارج نسبة نفوقه**
كما هو هنا وفي «عاشرًا» ١، **ولا يُخصم من سجل المورّد** — **فهو شرطُ بداية للدفعة لا حكمٌ على
أحد**. **والحاكم عند التعارض هو 208.**
**السبب: لو دخل ضمن المستلم، صار نفوقًا على المربّي من اليوم الأول وهو مسؤولية غيره، وتشوّهت
تقارير النفوق كلها من أول يوم.**

**والفرق بين المشترى والمستلم يُسجَّل عجزًا ظاهرًا، ويُعلَّم محل نزاع، ولا يُغلق إلا بقاعدة نزاع
الشحنة القائمة.**
**السبب: هذا الفرق يكشف مورّدًا يُنقص، وهو من أثمن ما ينتجه النظام.**

**وهذا الرقم أساس كل نسبة في النظام:** نسب النفوق والتحويل الغذائي **كلها تُقسم عليه**،
**فرقم خاطئ هنا يفسد كل تقرير لاحق.**

**والناقل كيان مربوط بسجل الزيارات** (#154 و#157).

**ثالثًا — القاعدة الحاكمة لحركة الطيور: رصيد واحد وخروج مصنَّف.**

> **لا يوجد «تعديل رصيد» إطلاقًا. الرصيد = المستلم ناقص مجموع الخروجات المصنَّفة.**

**والأنواع خمسة لا غير:**

| # | نوع الخروج |
| --- | --- |
| ١ | **نافق عند الوصول** — على المورّد لا على العنبر |
| ٢ | **نافق** |
| ٣ | **مذبوح لاستهلاك المربين** |
| ٤ | **أمر صرف** |
| ٥ | **مبيعات تسويق** |

**والسبب — وهو جوهر القرار: لو لم يكن أمر الصرف والمذبوح نوعين مستقلين، سُجّلا نفوقًا**، لأن
المربّي أمامه رصيد ناقص **ولا بد أن يفسّره، والنفوق هو الخانة الوحيدة المتاحة**.
**والنتيجة مزدوجة: نسبة النفوق ترتفع كذبًا فلا يُعرف أداء المزارع الحقيقي، والسرقة تختفي داخل
النفوق تمامًا.**

**رابعًا — أمر صرف الطيور: خروج بلا مقابل.**

- **من يصدره: المالك والمشرف.**
- **وحصة دورية:** المالك يحدد عددًا مسموحًا للمشرف **في كل دورة**، وما تجاوزها **يحتاج موافقة
  المالك قبل التنفيذ**.
  **السبب: الحق بلا حصة يتحول إلى بند مفتوح، والحصة تضبطه بلا أن تُلغيه.**
- **والرقابة فورية لا شهرية: كل أمر يصدره المشرف يصل المالك حال إصداره.**
  **السبب: مراجعة بعد شهر تأتي وقد نُسي السياق فتصير الأرقام بلا معنى.**
- **وحقول الأمر كلها إلزامية:** العدد · **المستفيد بالاسم** · العنبر المصروف منه · من أصدر الأمر ·
  التاريخ والوقت.
  **والمستفيد إلزامي بلا استثناء: أمر صرف بلا مستفيد ليس أمر صرف بل رصيد ناقص** — وهو **الحقل
  الذي يمنع تحوّل البند إلى مصرف مفتوح**.
- **ويظهر للمالك مجمَّعًا لكل دورة:** العدد الكلي · لمن · وبأمر من.

**خامسًا — المذبوح لاستهلاك المربين.**
**حصة معتمدة مسبقًا من المالك، والمربّي يسجّل ضمنها بلا استئذان، وتجاوزها يحتاج موافقة.**
**والعمر الأدنى المسموح به إعداد يضبطه المالك لا رقمًا ثابتًا في النظام.**

**سادسًا — خروج التسويق.**

- **التصريح يصدره المالك وحده** — **بضاعته تخرج من ملكه، وليس قرارًا تشغيليًا.**
- **وأمر التسويق يُنفَّذ على حمولات:** تصريح واحد بكمية، وتحته **حمولات متعددة**، كل حمولة لها
  **عددها ووقتها وشاحنتها**. **والتصريح يبقى مفتوحًا حتى يكتمل أو يُغلق بقرار.**
- ~~**والعدّ عند التحميل استلام أعمى:** من يحمّل **يعدّ ويسجّل ولا يرى الرقم المتوقع**.~~
  **🔴 منقوضٌ بالقرار 208 — ولا يُبنى على حاله.** **الاستلام الأعمى يحمي حين يكون العادّ
  مستلِمًا وينقلب حين يكون مُسلِّمًا**، **والمربّي هنا مُسلِّم**. **وفعله مطابقة لا عدّ:**
  مطابقة الكميات في التصريح بما خرج من عنبره · وتأكيد هوية السائق. **ولا دور للسائق في
  التسجيل.**
- **والناقل زيارة تخضع للأمن الوقائي.**

**سابعًا — إغلاق الدفعة.**

> **الدفعة لا تُغلق برصيد غير مفسَّر. المستلم يجب أن يساوي مجموع الخروجات بالضبط.**

**وأي فرق يظهر صراحة كـ«فاقد غير مفسَّر» ويُعلَّم محل نزاع، ولا يُبتلع في الإغلاق.**
**السبب: بلا هذه القاعدة يمكن التحايل على كل التصنيف السابق بإغلاق فيه فرق صامت.**

**ثامنًا — أربعة أسئلة مفتوحة بخياراتها وبلا ترجيح. القرار للمالك.**
**✅ وأُغلقت كلها — الثالث في «عاشرًا»، والثلاثة الباقية بالقرار 208. ولا مفتوح فيها.**

1. ~~**من يسجّل عدّ التحميل عند التسويق — مفتوح:** المربّي · المشرف · طرف ثالث.~~ **✅ المربّي (208 حكم ١)** — «هو المسؤول الأول عن خروج أي كميات خارج عنبره». **وفعله مطابقة لا عدّ** (حكم ٢).
2. ~~**حمولة خرجت ولم تصل — مفتوح:** كيف تُسجَّل، **وهل لها حالة «في الطريق» كالعلف**؟~~ **✅ لا حالة «في الطريق» للطيور (208 حكم ٤)** — الخروج يكتمل عند باب العنبر، **وتأكيد المالك بعده واقعةُ حالةٍ لا عهدةٌ تنتقل**. **وخلافُه لنمط العلف مقصود: العلف يعود إن لم يصل، والطير المُحمَّل لا يعود.**
3. **التسويق الجزئي — محسوم في «عاشرًا» أدناه: التسويق لا يُغلق الدفعة إطلاقًا.**
4. ~~**النافق عند الوصول: هل يُخصم من الكمية المشتراة في سجل المورّد** لمتابعة أدائه عبر الشحنات؟~~ **✅ لا يُخصم (208 حكم ٥)** — **شرطُ بداية للدفعة لا حكمٌ على أحد**، لا على العنبر ولا على المورّد. **ويضيّق نصّ «ثانيًا» أعلاه** («يُنسب للمورّد أو الناقل»).

**تاسعًا — فحص المبنيّ والمواصفة: سبعة تعارضات وست تغطيات جزئية.**

**وهذا القرار يمسّ مبنيًّا فعلًا لا مصمَّمًا فقط** — بخلاف #156 و#159: `batches` جدول قائم في
المخطط، **ومعادلات §15 مكتوبة نصًّا**، فبعض ما يلي **نقضٌ لنصّ قائم لا ملء فراغ**.

**التعارضات السبعة:**

1. **🔴 معادلتا §15 تنقضان القاعدة الحاكمة نصًّا — لا فجوةً بل تناقضًا مكتوبًا.**
   `current_birds = initial_bird_count − Σ mortality`
   `mortality_pct = Σ mortality / initial_bird_count × 100`
   **فالخروج الوحيد المعترف به في النظام كله هو النفوق**، والحكم يجعل الرصيد ناقصَ **خمسة**
   أنواع. **والمعادلتان تُصحَّحان لا تُكمَّلان.**
2. **🔴 والمقسوم عليه نفسه يتغيّر.** `initial_bird_count` هو مقام `mortality_pct` **ومقام نسبة
   البقاء في EPEF** — **والحكم يجعل المقام «المستلم المؤكَّد» لا «المشترى»**. فتغيّر رقم واحد
   **يغيّر كل نسبة في كل تقرير**، وهو بالضبط ما يقوله الحكم الثاني: «رقم خاطئ هنا يفسد كل تقرير
   لاحق». **والنظام اليوم لا يفرّق بين الرقمين أصلًا** — عمود واحد يحمل الاثنين.
3. **🔴 لا مسار استقبال كتاكيت إطلاقًا، ولا الكتاكيت منتج.** `product_category` = علف · دواء ·
   لقاح · فيتامين · مستلزمات — **لا طيور**؛ و`shipments.product_id NOT NULL` فالجدول للمنتجات.
   **فسلسلة الاستقبال الثلاثية غير ممثَّلة بأي جدول**، والقائم بديلًا عنها **حقل واحد**:
   `batches.initial_bird_count integer NOT NULL` — **رقم يكتبه من ينشئ الدفعة**.
4. **🔴 والفاعل واللحظة كلاهما يتعارض.** §12.2: «**إنشاء دفعة: ❌ farmer · ✅ supervisor · ✅ owner**»
   — **والحكم يجعل الدفعة تبدأ بتأكيد المربّي**. فالمربّي **ممنوع اليوم** من الفعل الذي يجعله
   الحكم **مُبتدِئ الدفعة**. **ولا استلام أعمى للكتاكيت**: النمط مبنيّ في `shipments` (رمز تسليم ·
   `counted_quantity`) **وللعلف والمستلزمات وحدها**.
5. **🔴 لا نافق عند الوصول، وقائمة أسباب النفوق كلها أسباب موت داخل العنبر.**
   `mortality_cause` = مرض تنفسي · إجهاد حراري · مشاكل مياه/علف · حادث · غير معروف · أخرى —
   **ولا «عند الوصول» ولا نسبة إلى مورّد أو ناقل**. فالنافق عند الوصول اليوم **إما يُكتب في نفوق
   اليوم الأول على المربّي** — وهو حرفيًا التشويه الذي يصفه الحكم — **أو يُطرح صامتًا من
   `initial_bird_count`** فيختفي أثره **ولا يُحاسَب المورّد أبدًا**.
6. **🔴 أربعة من الأنواع الخمسة بلا موضع — والخانة الوحيدة المتاحة هي النفوق.**

   | النوع | حاله في المبنيّ |
   | --- | --- |
   | نافق | **موجود** — `daily_logs.mortality_count` |
   | نافق عند الوصول | **لا مكان** |
   | مذبوح لاستهلاك المربين | **لا مكان** |
   | أمر صرف | **لا مكان** |
   | مبيعات تسويق | **جزئي** — `batches.sold_bird_count`، **رقم واحد عند الإغلاق لا حركة** |

   **فتحذير الحكم ليس احتمالًا بل وصفًا للحال:** المربّي أمامه رصيد ناقص، **والخانة الوحيدة
   المتاحة له `mortality_count`.**
7. **التسويق رقم لا حركات — وبنيته لا تحتمل الحكم السادس.** `sold_bird_count` و
   `market_avg_weight_g` **عمودان على `batches` يُكتبان عند الإغلاق** — **لا تصريح ولا حمولات ولا
   شاحنة ولا وقت لكل حمولة ولا عدّ أعمى عند التحميل**، **وهو `UPDATE` على صفّ الدفعة لا حركة
   مقيَّدة** (يتقاطع مع المبدأ الرابع).
   **ويجيب ضمنًا عن السؤال الثالث بـ«نعم»:** كتابة رقم المبيع **عند الإغلاق** تفترض أن التسويق
   يُغلق الدفعة. **افتراضٌ بنيوي لا قرار مالك** — يُسجَّل كي يُحسم صراحةً.

**والتغطيات الجزئية الستّ:**

1. **نمط الاستلام الأعمى مبنيّ كاملًا — للشحنة وحدها.** `shipments` تحمل رمز التسليم و
   `counted_quantity` و`variance` و`variance_status` والنزاع بحالاته ونتائجه ومن أغلقه.
   **وهذا ثالث مسار يطلب النمط نفسه** بعد التحويل (#159 البند ٢): **ثلاثة مسارات نقل، والنمط
   مبنيّ لواحد.**
2. **وإحالة الحكم إلى «قاعدة نزاع الشحنة القائمة» متسقة تمامًا** — المالك يغلقها وحده لأنه **طرف
   مع المورّد**، وهو نفس تعليل #159 البند ٥. **استعمال مقصود لقاعدة قائمة لا تعارض معها.**
3. **بنية «شحنة → توزيعات» غير موجودة حتى للعلف.** `shipments` تحمل `house_id` واحدًا — **شحنة
   لعنبر واحد**. **وسبب الحكم ينطبق على العلف أيضًا** (تفتيت يدوي يضيع أثر الشحنة الواحدة من
   مورّد واحد) — **واقعة تُسجَّل، ولا يُوسَّع الحكم من تلقاء نفسه.**
4. **الإغلاق لا يفحص شيئًا من هذا.** §14.6: «قفل → **جرد إلزامي** → تحذير فترة السحب → إغلاق» —
   **والجرد الإلزامي للعلف لا للطيور**، ولا قاعدة تمنع إغلاقًا برصيد غير مفسَّر. **فالحكم السابع
   يضيف حارسًا غير موجود** لا يعدّل حارسًا قائمًا.
5. **نمط «إعداد على المستأجر» قائم ولا عمود لهذا.** `tenants.min_rest_days` و`prep_protocol`
   وشاشة إعدادات المالك — **فالعمر الأدنى للذبح (الحكم الخامس) يجد نمطًا جاهزًا وحقلًا غير
   موجود.** وكذلك حصة الصرف الدورية وحصة المذبوح.
6. **قائمة العاجل للمالك «مغلقة» ولا تحمل أمر الصرف.** §16: «المالك: نزاع · بلاغ شديد بلا رد
   ساعتين · تصفية · رصيد سالب» — **والحكم الرابع يضيف عنصرًا سادسًا إلى قائمة موصوفة نصًّا بأنها
   مغلقة**. تعديل واعٍ لا إضافة عابرة.

**وثلاث ملاحظات مشتقّة — وقائع لا ترجيحات:**

- **لحظة بدء الدفعة تحرّك حارسًا قائمًا.** §14.6: «بدء دفعة في عنبر غير جاهز: مسموح بسبب +
  `housed_before_ready = true` + audit + تنبيه» — **والحارس يُفحص اليوم عند `POST /batches`**.
  **والحكم ينقل لحظة البدء إلى تأكيد المربّي**، فيصير موضع الفحص سؤالًا يحتاج جوابًا صريحًا: عند
  الإدخال، أم المصادقة، أم التأكيد؟ **والعلامة دائمة فلا تحتمل موضعًا خاطئًا.**
- **وقاعدة «لا تنبيه على عنبر بلا دفعة نشطة» (§16) تتحرك معها** — العنبر بين المصادقة والتأكيد
  **بلا دفعة نشطة بحكم الحكم الجديد**، وفيه طيور فعلًا.
- **§12.2 بلا صفوف لستة أفعال:** إدخال شحنة كتاكيت · المصادقة والتوزيع · تأكيد استلام الحصة ·
  أمر صرف الطيور (بحصة) · تسجيل المذبوح (ضمن حصة) · تصريح التسويق وعدّ الحمولة.
  **وصفّ «تصفية دفعة: ✅ للمالك وحده» متسق** مع «التصريح يصدره المالك وحده».

**عاشرًا — اثنا عشر حكمًا لاحقًا على ما كشفه الفحص، أُضيفت إلى هذا القرار بعد تسجيله.**

**١. معادلتا §15 تُصحَّحان لا تُكمَّلان.**

```
current_birds = المستلم المؤكَّد − Σ (الخروجات المصنَّفة الخمسة)   [لا النفوق وحده]
mortality_pct = Σ النفوق / المستلم المؤكَّد × 100                  [لا المشترى]
EPEF          → نسبة البقاء مقامها المستلم المؤكَّد                 [لا المشترى]
```

**والنافق عند الوصول خارج البسط والمقام معًا** — **لا يدخل نفوق العنبر ولا يُحتسب على المربّي**.
وهو تطبيق مباشر للحكم الثاني: الطير الذي وصل ميتًا **لم يكن يومًا في عهدة المربّي**، فحسابه في
أيٍّ من طرفي النسبة يحمّله ما ليس له.

**٢. المشترى والمستلم رقمان مستقلان لا عمود واحد.**
`initial_bird_count` بصيغته الحالية **يخلطهما، وهو مقام كل نسبة في النظام** — فخلطهما ليس اختصارًا
في التسمية بل **خطأ يسري إلى كل تقرير**: نسبة نفوق تُقسم على عدد لم يصل، ونسبة بقاء تُحسب على
طيور لم تُسلَّم.

**٣. لحظة بدء الدفعة — الفعلان يُفصلان، ولا يُخوَّل المربّي الإنشاء.**

| الفعل | من | حال الدفعة |
| --- | --- | --- |
| **إنشاء سجل الدفعة** | المشرف أو المالك **عند التوزيع** | **«قيد الوصول»** |
| **بدء الدفعة فعليًا** | **تأكيد المربّي** | **«نشطة»** |

**فالمربّي لا يُنشئ دفعة، لكنه هو من يبدأها.** **و§12.2 يُصحَّح على هذا الفصل لا بتخويله الإنشاء**
— وهذا يرفع التعارض الرابع في الفحص أعلاه **بلا توسيع صلاحية المربّي إطلاقًا**.
**وأثره على `BATCH_STATUS`:** القيمتان اليوم «نشطة · منتهية»، **و«قيد الوصول» قيمة ثالثة** تُضاف.

**٤. فحص جاهزية العنبر يقع مرتين بمعنيين مختلفين.**

- **عند التوزيع:** **تنبيه وقائي قبل إرسال الطيور، ولا يترك علامة.**
- **عند تأكيد المربّي:** **هنا تُسجَّل `housed_before_ready`**، لأنها **واقعة دخول طيور فعلي**.

**السبب: العلامة دائمة، فلا تُسجَّل على نيّة بل على دخول وقع.** توزيعٌ أُلغي أو لم يصل **لا يترك
وصمة دائمة على عنبر لم تدخله طير**.

**٥. حالة «قيد الوصول» تحلّ مشكلة التنبيهات.**

> **التنبيهات تتبع الوجود الفعلي للطيور لا حالة السجل.**
> **وعنبر فيه طيور وصلت ولم تُؤكَّد بعد يُعامَل معاملة المشغول.**

وهذا يغلق الملاحظة المشتقّة الثانية في الفحص: قاعدة §16 «لا تنبيه على عنبر بلا دفعة نشطة»
**تُقرأ بالوجود الفعلي**، فلا يسقط عنبرٌ فيه طير من كل تنبيهات النظام لأن أحدًا لم يضغط زرًّا.

**٦. التسويق حركة لا `UPDATE` عند الإغلاق.**
**و`sold_bird_count` رقم واحد لا يصلح — يُستبدل بحمولات مسجَّلة.** فالمبيع يصير **مجموع الحمولات**
لا رقمًا يُكتب، وكل حمولة واقعة لها وقتها وعددها وشاحنتها.

**٧. نمط الاستلام الأعمى يُعمَّم مرة واحدة لا يُكرَّر ثلاثًا.**
صار مطلوبًا في **ثلاثة مسارات**: **الشحنة** (مبنيّ) · **التحويل** (#159 البند ٢) · **الكتاكيت**
(هذا القرار). **فيُعمَّم النمط مرة واحدة** — كميّة معلنة وكميّة معدودة وفرق وحالة فرق ونزاع ونتيجة
ومن أغلقه ومسار تصحيح — **بدل ثلاث نسخ تتفارق مع الوقت**. **وثلاث نسخ لسلوك واحد تعني ثلاثة
مواضع لإصلاح كل عيب، وثلاثة احتمالات لأن يُصلَح في اثنين وينسى الثالث.**

**٨. بنية «شحنة → توزيعات» غير موجودة حتى للعلف — بند مؤجَّل بلا حكم.**
يُسجَّل ولا يُوسَّع القرار إليه الآن. **والتوزيعات في هذا القرار تخصّ الكتاكيت وحدها.**

**٩. §12.2 — تُضاف صفوف الأفعال الستة** التي يستحدثها القرار (في
`backend-technical-spec.md`)، **وما لم يُحسم منها موسوم `؟` لا `❌`** على قاعدة #157 البند ٥.

**وخانتان حُسمتا بعد إضافة الصفوف:**
- **الموافقة على تجاوز حصة المذبوح: المالك.** (كانت قراءة قياسية موسومة بحاشية، فثُبِّتت وحُذفت الحاشية.)
- **والمالك في صفّ «المصادقة على الشحنة وتوزيعها»: `❌` صراحة لا `؟`.**
  **السبب: المالك هو من يُدخل الشحنة، فمصادقته عليها مصادقة على نفسه — وهو نقض للمبدأ #155**
  («من يُدخل رصيدًا لا يصادق عليه»). **المشرف وحده يصادق ويوزّع.**
  **وهذا `❌` منعٌ مبرَّر لا سكوتٌ عن حكم** — الفرق الذي أُنشئت له علامة `؟` أصلًا.

**١٠. التسويق لا يُغلق الدفعة إطلاقًا.**

> **الإغلاق فعل مستقل يقرره المالك حين يفرغ العنبر.**
> **والتسويق الجزئي هو الحالة الغالبة لا الاستثناء:** يخرج جزء عند وزن معيّن **ويبقى الباقي
> يُربّى أيامًا إضافية.**
> **وبيع العنبر كاملًا على حمولات متتابعة هو نفس الآلية** — الفرق أن **الحمولة الأخيرة تُفرغه**.

**السبب: النظام يفترض اليوم أن التسويق يُغلق الدفعة بحكم كتابة رقم المبيع عند الإغلاق. وهذا
افتراض بنيوي لا قرار — وهو مخالف للواقع الغالب.**
**فحالة واحدة تغطّي الاثنتين**: لا فرع خاص بـ«التسويق الكامل» ولا آخر بـ«الجزئي» — حمولات
متتابعة، وإغلاق مستقل عنها.

**١١. كل حساب بعد تسويق جزئي يقوم على الرصيد الحالي لا على العدد الابتدائي.**
**استهلاك العلف والماء، ونسبة النفوق.**
**السبب: الطيور التي خرجت لم تعد معرّضة للنفوق، فحسابها في المقام يجعل النسبة مضلّلة** — تظهر
أقل مما هي، **والمزرعة التي سوّقت نصف عنبرها تبدو أفضل من نظيرتها لا لأنها أفضل**.
وكذلك استهلاك الفرد: علفُ يومٍ مقسومًا على عدد ابتدائي نصفه خرج **يعطي رقمًا لا يخصّ أحدًا**.

**١٢. العمر عند البيع ليس رقمًا واحدًا للدفعة — لكل حمولة عمرها ووزنها.**
**وأي تقرير يعرض «عمر البيع» يعرضه لكل حمولة، ومتوسطًا مرجّحًا بالعدد إن جُمع.**
والمتوسط البسيط **يساوي بين حمولة من مئة طير وأخرى من خمسة آلاف** فيكذب على من يقرأه؛
**والترجيح بالعدد وحده يعطي رقمًا يعني شيئًا.** ويلزم عنه أن `market_avg_weight_g` — رقم واحد
على الدفعة — **يتبع نفس المصير: متوسط مرجّح لا قيمة مكتوبة**.

**ولا يُلمس شيء من هذا الآن** — أحكام تُنفَّذ في المرحلتين 3 و4. مسجَّل دَينًا في
`work-plan.md` §7-ب البند 31.

**161. المخازن وأمين المخزن — مخزن واحد بمستوى، لا أنواع متعددة**
**قرار منتج. تسجيل فقط — لا سطر كود، ولا يُفتح قبل إغلاق المرحلة 2-أ.**

**أولًا — المخزن كيان واحد له مستوى.**

> **حقل واحد يحدد مستواه: مركزي (للشركة) · موقع · عنبر.**

**والمالك ينشئ ما يناسب حجمه:** مركزي واحد يصرف للعنابر مباشرة · أو مركزي ثم مخزن لكل موقع ثم
العنابر · أو مخازن مواقع بلا مركزي.
**السبب: بناء أنواع مختلفة يجعل كل تغيّر في حجم العميل تعديلًا في النظام. المستوى حقل، فالتوسع
إعداد لا برمجة.**

**وثلاثة قيود:**

- **لا يُلغى مخزن فيه رصيد** — يُفرَّغ أولًا بحركات مسجَّلة.
- **مخزن العنبر واحد لكل عنبر ويُنشأ معه تلقائيًا** — لا يُنشئه المالك ولا يحذفه.
- **لكل مخزن صاحب مسؤول، وكل حركة بين صاحبين.**

**ثانيًا — أصحاب المخازن.**

| المستوى | الصاحب |
| --- | --- |
| **المركزي** | **أمين المخزن — دور جديد يُضاف** |
| **مخزن الموقع** | **المشرف** المسؤول عن مزارع ذلك الموقع |
| **مخزن العنبر** | **مربّيه** |

**ولا يُنشأ دور ثانٍ لمخازن المواقع.**

**ثالثًا — من يأمر بالصرف: قاعدة واحدة.**
**صاحب المخزن المصدر هو من يأمر بالصرف منه:** المالك من المركزي · المشرف من مخزن مزرعته إلى
عنابرها · **والتحويل بين العنابر بأمر المشرف** (#159).

**رابعًا — الحركة واحدة لا ثلاث.**
**كل حركة من مخزن إلى مخزن تخضع لنفس آلية #159:** أمر · خروج · **حالة «في الطريق»** · **تأكيد
بالكمية المستلمة فعلًا** · **فرق يظهر عجزًا** · **سجل لا يُعدَّل والتصحيح بحركة مضادة**.
**ومخزن العنبر ليس نوعًا خاصًا — هو مخزن مستواه «عنبر».**

**خامسًا — طلب المربّي عند نفاد العلف.**
**المربّي يرفع طلبًا عند نفاد الكمية أو قربها. والطلب ليس ملاحظة:** للمشرف **إصدار أمر صرف مباشرة
منه، ويُربط الطلب بالأمر تلقائيًا**. **وطلب لم يُلبَّ خلال مدة يظهر للمالك.**
**السبب: تأخر العلف يعني توقف نمو، وهي خسارة لا تظهر في أي تقرير آخر. ومربٍّ يطلب ومشرف لا يستجيب
أشيع سبب لانخفاض الأداء ولا يعرفه أحد.**

**سادسًا — الأصناف.**
**المخزون عام لا خاص بالعلف:** علف · لقاحات · أدوية · **معقمات ومطهرات** · مستلزمات العنابر.
**ومخزن العنبر يحمل ما يُصرف إليه فعلًا** (علف وأدوية ومطهرات) — **لا معدات ولا مستلزمات إنشائية**.
**والأصناف الدوائية واللقاحات تحمل تاريخ صلاحية: لا يُصرف صنف منتهي الصلاحية، ويُنبَّه قبل انتهائه
بمدة يضبطها المالك.**
**السبب: لقاح منتهٍ يُستعمل يعني دفعة بلا حماية — والخسارة تظهر بعد أسابيع ولا تُربط بسببها.**

**سابعًا — ما لا يراه أمين المخزن.**
**يرى مخزنه وأوامر الصرف الصادرة منه والواردة إليه فقط. لا يرى أرصدة العنابر ولا بيانات الدفعات.**

**ثامنًا — الأرصدة الافتتاحية.**
**من يُدخل رصيدًا لا يصادق عليه (#155):**
**أمين المخزن يُدخل رصيد المركزي والمالك يصادق · والمربّي يُدخل رصيد عنبره والمشرف يصادق.**

**تاسعًا — توريد العلف: حقول الإدخال.**
**تُدخَل:** مرحلة العلف (بادئ · نامي · ناهي) · اسم المنتج التجاري · المورّد أو المطحنة ·
**الكمية بالطن**.
**والنظام يحوّل الطن إلى أكياس تلقائيًا بحسب وزن الكيس المسجَّل للصنف.**

**ووزن الكيس إعداد على مستوى الصنف بقيمة افتراضية ٥٠ كجم** — وهو الوزن الوحيد المتداول لدى موردي
المالك اليوم.
**السبب: النظام يُباع لعملاء آخرين، ورقم مثبَّت في الكود يعني إصدارًا جديدًا عند أول عميل يستلم
بغير ذلك.**

**والرصيد يُحفظ بالكيس لا بالطن. الطن وحدة إدخال وعرض فقط.**
**السبب: الاستهلاك والتحويل والجرد كلها بالكيس، وحفظ الرصيد بالطن يُنشئ كسورًا تتراكم مع كل حركة.**

**والمرحلة والمنتج والمورّد ثلاثة حقول مستقلة:** المرحلة **تحكم الصرف بحسب عمر الدفعة**، والمنتج
والمورّد **يُتابَع بهما الأداء عبر الشحنات**.

**عاشرًا — الأكياس الفارغة في مخزن العنبر.**

> **مخزن العنبر يحمل رصيدين: أكياس ممتلئة وأكياس فارغة. واستهلاك كيس ينقل واحدًا من الأول إلى
> الثاني.**
> **والفارغ معادلة تحقق لا جردًا فقط: الممتلئ المستلم = الفارغ + الممتلئ المتبقي.**

**وأي فرق يعني كيسًا خرج بلا تفسير، ويظهر فورًا بلا انتظار جرد.**
**السبب: هذه أسرع وسيلة في النظام لكشف خروج علف غير مسجَّل.**

**وللكيس الفارغ حالتان: صالح · تالف (ممزق أو مبلَّل).**
**السبب: بلا حالة التالف يُحسب التلف مفقودًا ويُتَّهم المربّي بلا ذنب.**
**وتسجيل التالف يجب أن يكون سهلًا ومباشرًا، وإلا أبلغ المربون أرقامًا متوازنة بدل الحقيقة وضاعت
قيمة المعادلة.**

**والأكياس الفارغة أصل قابل للبيع وإعادة الاستخدام:** تُسجَّل حركة خروجها **عددًا فقط، بلا سعر ولا
قيمة**، ويوصف الكيس **صالحًا أو تالفًا لا غير**.
**ومن يسجّل: المربّي يسجّل الاستهلاك والتالف، والمشرف يعتمد خروج الأكياس.**

**حادي عشر — خمسة أسئلة مفتوحة بخياراتها وبلا ترجيح. القرار للمالك.**
**✅ وأربعةٌ منها حُسمت بالقرار 207 — والخامس (٤) وحده باقٍ.**

1. ~~**إسناد أمين المخزن:** مخزن واحد بعينه · عدة مخازن مركزية · للشركة كلها.~~ **✅ للشركة كلها (207 حكم ١)** — لا يُسند بمخزن، ونطاقه محورٌ آخر.
2. ~~**الأدوية واللقاحات:** هل صرفها بيد أمين المخزن كبقية الأصناف، أم يحتاج **موافقة الطبيب**؟~~ **✅ بموافقة الطبيب (207 حكم ٧)** — على شكل القرار 203 بطرفين مختلفين، لا آلية جديدة.
3. ~~**الجرد الدوري:** من يقوم به ومن يصادق عليه، وكيف يُسجَّل فرقه؟~~ **✅ (207 أحكام ٣ و٤ و٥ و٦)** — المركزي بأمين المخازن والمالك يصادق · ومخزن الموقع بالمشرف والمالك يصادق **عند نهاية الدفعة** · **ولا جرد دوري لمخزن العنبر** (معادلة العهدة تغني عنه).
4. **صنف انتهت صلاحيته وهو في المخزن:** كيف يخرج من الرصيد ومن يأمر بإتلافه؟ — **🔴 باقٍ**. **والقرار 207 لم يمسّه**: حكمه الثامن يخصّ **ما قارب** الانتهاء لا **ما انتهى**. **وهو سؤال §7-ب البند 54 نفسه بلفظ آخر** — دَينٌ واحد بموضعين.
5. ~~**الأكياس الفارغة:** هل تُجمَّع في مخزن المزرعة أو المركزي قبل بيعها، أم تُباع من العنبر مباشرة؟~~ **✅ تعود إلى المخزن الذي صُرفت منه — أي مخزن الموقع (207 حكم ٩)**، والقاعدة تتبّعية لا ثابتة. **و«مخزن المزرعة» في نصّ السؤال خطأ لفظ ثالث** — لا مستوى «مزرعة» في المخازن («ثالث عشر» ٣، والقرار 207).

**ثاني عشر — فحص المبنيّ والمواصفة: ثمانية تعارضات، وموضعان داخل القرار نفسه، وست تغطيات جزئية.**

**والتعارضات الثمانية:**

1. **🔴 `LOCATION_TYPE` قيمتان (`warehouse` · `house`) — والقرار يجعل العنبر مستوًى لا نوعًا.**
   وهذا ليس فرقًا في التسمية: **قيد `CHECK` في `inventory_movements` يفرض القسمة نصًّا** —
   `location_type='house' AND location_id = house_id` **أي أن معرّف الموقع هو معرّف العنبر نفسه**،
   `OR location_type='warehouse' AND house_id IS NULL AND farm_id IS NULL`. **ومخزن العنبر في
   القرار كيان له معرّفه الخاص** — فالقيد **يرفض النموذج الجديد** لا يستوعبه.
   **والقسمة ترث إلى أربعة جداول:** `inventory_movements` · `stocktakes` · `wastage` ·
   `inventory_transfers`، **وكلها تحمل `location_type`/`location_id`**. فتغيير النموذج يمسّ
   **الدفتر نفسه** لا جدولًا طرفيًا.
2. **🔴 `warehouses` بلا مستوى وبلا صاحب وبلا موقع.**
   `id · tenant_id · name · is_active · created_at` — **لا عمود مستوى، ولا `site_id`، ولا صاحب
   مسؤول**. **والقرار يقوم على الثلاثة معًا** («حقل واحد يحدد مستواه» · «لكل مخزن صاحب» · «مخزن
   الموقع»). والوصف القائم في المواصفة: «**مخزن افتراضي واحد لكل مستأجر**».
3. **🔴 دور جديد في `USER_ROLE` — والخطر أن يُضاف فيرى كل شيء، لا ألّا يرى شيئًا.**
   `ASSIGNMENT_SCOPED_ROLES` **قائمة موجبة**، و`visibleFarmCondition` تبدأ بـ
   `if (!isAssignmentScoped(viewer.role)) return undefined;` — **و`undefined` تعني «بلا قيد»**.
   فأمين المخزن **إن أُضيف إلى الأدوار ولم يُدرَج في القائمة رأى كل مزارع المستأجر وكل عنابره
   وكل دفعاته** — **وهو النقيض الحرفي للحكم السابع**. يُسجَّل الآن لأنه **عطبٌ صامت لا يُخطئ فيه
   اختبار قائم**: لا اختبار اليوم لدور لم يوجد بعد.
   **ولا تعارض مع #146** — ذاك يخصّ `platform_admin` لأنه **فوق المستأجرين**، وأمين المخزن **دور
   داخل المستأجر** فمكانه الطبيعي `USER_ROLE`.
4. **🔴 `user_assignments` لا تحتمل مستوى ثالثًا — والقيد يرفضه صراحةً.**
   `CHECK ((house_id IS NULL) <> (farm_id IS NULL))` **يفرض مستوًى واحدًا من اثنين حتمًا**.
   وإسناد أمين المخزن **إلى مخزن** (السؤال المفتوح الأول) يحتاج **`warehouse_id` ومستوى ثالثًا**،
   فيتحول القيد من «أحدهما» إلى «واحد من ثلاثة». **ويتقاطع هذا مع القرار #158** الذي يغيّر نفس
   الجدول (بداية ونهاية وفهرسان جزئيان) — **تغييران على جدول واحد، وترتيبهما يحتاج قرارًا.**
5. **🔴 لا تاريخ صلاحية في النظام إطلاقًا — والحكم السادس يقوم عليه.**
   `products` فيه `withdrawal_days` و`storage_conditions` **ولا حقل صلاحية**، **ولا على المنتج
   ولا على الحركة**. **والصلاحية خاصية دفعة تشغيلية (lot) لا خاصية صنف** — عبوتان من نفس اللقاح
   تختلفان — **ولا مفهوم lot في المخطط**. فـ«لا يُصرف صنف منتهي الصلاحية» **غير قابل للتنفيذ
   بالبنية القائمة أصلًا**.
   **وهذا النمط تكرّر ثالثةً:** `wastage_reason` فيه **«انتهاء صلاحية»** — **سببُ تلفٍ لا يستطيع
   النظام كشفه**، تمامًا كـ«انقطاع تبريد» (#157 البند ٥). **قائمة الأسباب أغنى من النموذج مرتين.**
6. **🔴 وزن الكيس له مصدران متعارضان اليوم.**
   `products.package_size` + `package_unit` (**على الصنف** — وهو ما يطلبه الحكم التاسع)،
   **و«وزن كيس العلف» إعداد على المستأجر** في شاشة إعدادات المالك، **و`daily_log_feed_rows.
   bag_weight_kg` «السائد وقت الإدخال»**. **فالحقل موجود ثلاث مرات بثلاثة معانٍ** — والحكم يجعل
   **الصنف هو المصدر**. يُسجَّل التعارض ولا يُحلّ هنا.
7. **§12.2 يمنع المربّي من الجرد — والحكم الثامن يجعله يُدخل رصيد عنبره الافتتاحي.**
   «جرد · تحويل · هالك: **❌ farmer**». **والرصيد الافتتاحي جردٌ افتتاحي** (`stocktakes.is_opening`)
   — فالصفّ القائم **يمنع الفعل الذي يسنده الحكم إليه**.
8. **فئات المنتجات لا تطابق قائمة الحكم.**
   القائم: علف · دواء · لقاح · **فيتامين** · مستلزمات. وقائمة الحكم: علف · لقاحات · أدوية ·
   **معقمات ومطهرات** · مستلزمات العنابر. **فـ«معقمات ومطهرات» ليست فئة اليوم** (تقع ضمن
   «مستلزمات»)، **و«فيتامين» فئة قائمة لم يذكرها الحكم**. **وحدّ «ما يحمله مخزن العنبر» (لا معدات
   ولا مستلزمات إنشائية) لا يقبل الفرض على «مستلزمات» ككتلة واحدة.**

**وموضعان داخل القرار نفسه يحتاجان حسمك — لا في المبنيّ بل في نصّه:**

1. **«صاحب المخزن المصدر هو من يأمر بالصرف منه» مقابل «المركزي صاحبه أمين المخزن، والمالك يأمر
   منه».** القاعدة الثالثة والجدول الثاني **لا يجتمعان على المركزي**: إمّا أن أمين المخزن ليس
   «صاحبًا» بالمعنى الآمر، أو أن المركزي **استثناء من القاعدة**. **ولا أرجّح.**
2. **«المشرف من مخزن مزرعته» — ولا مستوى «مزرعة» في القرار.** المستويات ثلاثة: مركزي · **موقع** ·
   عنبر، ومخزن المشرف **مخزن موقع**. والموقع قد يضمّ **أكثر من مزرعة** (القرار #113) — فـ«مخزن
   مزرعته» يقرأ مستوًى رابعًا غير معلن، أو يقصد مخزن الموقع بلفظ آخر. **يُسجَّل ولا يُفسَّر.**

**والتغطيات الجزئية الستّ:**

1. **الحكم الرابع يستعمل آلية قائمة بالقرار لا يستحدثها** — «كل حركة من مخزن إلى مخزن تخضع لآلية
   #159» **يجعل التعميم الذي أمر به #160 البند ٧ أوسع وأوضح**: النمط يُعمَّم مرة واحدة **لكل
   الحركات**، لا لثلاثة مسارات معدودة.
2. **«مخزن العنبر يُنشأ مع العنبر تلقائيًا» يجد موضعًا واحدًا جاهزًا.** إنشاء العنبر له **موضع
   كتابة واحد في المشروع كله** (`housesService.ts` — `insert(houses)` واحدة، القرار #152)، فالخطاف
   **تعديل في دالة واحدة** لا مسح للمسارات.
3. **«لا يُلغى مخزن فيه رصيد» بلا حارس اليوم** — `warehouses.is_active` عمود بلا أي منطق (لا مسار
   مبنيّ أصلًا)، **والرصيد محسوب من الدفتر لا مخزَّن**، فالحارس **قراءة مجموع تحت قفل** لا عمود
   يُفحص.
4. **«طلب لم يُلبَّ يظهر للمالك» له نمط جاهز حرفيًا.** §16 تحمل تصعيدًا ساعيًا (cron) لخمسة بنود
   **كلها للمالك**، منها «**شحنة >48 ساعة**». **فالطلب بند سادس على نفس الآلة** — لا آلية جديدة.
   **ولا جدول طلبات في المخطط إطلاقًا** (`log_notes` للسجل اليومي وحده).
5. **المورّد حقل نصّي على المنتج لا كيان** (`products.supplier varchar(160)`) — **والحكم التاسع
   يطلب متابعة أدائه عبر الشحنات**. **وهذا ثالث كيان يُطلب وهو نصّ حرّ**: الناقل (#157 البند ٣)،
   والمورّد هنا، **والسؤال الرابع في #160** (خصم النافق عند الوصول من سجل المورّد) **يفترض وجود
   سجل مورّد أصلًا**. **ثلاثة قرارات تتقاطع على كيان واحد غير موجود.**
6. **الأكياس الفارغة: لا شيء منها في المخطط، ومعادلتها ثانية لا أولى.**
   §13.3 تحمل ثابت الدفتر: «لكل (مستأجر · منتج): Σ الحركات == رصيد المخزن + Σ أرصدة العنابر».
   **والحكم العاشر يضيف ثابتًا ثانيًا مستقلًّا** («الممتلئ المستلم = الفارغ + الممتلئ المتبقي»)
   — **يُفحص لكل عنبر لا لكل مستأجر**. **وحالتا الكيس (صالح · تالف) ليستا `wastage`**: الهالك
   خروجٌ من الرصيد، **والكيس التالف يبقى في رصيد الفارغ بوصف مختلف**.
   **وملاحظة متسقة مع #136:** «عددًا فقط بلا سعر ولا قيمة» **يطابق قاعدة «لا حسابات مالية في
   التطبيق إطلاقًا»** — فبيع الأكياس **حركة عدد** لا معاملة مالية.

**ثالث عشر — أحد عشر حكمًا لاحقًا، أُضيفت إلى هذا القرار بعد تسجيله.**

**١. الحارس يُقلب: الافتراض «لا يرى شيئًا» لا «يرى كل شيء» — وشرط إغلاق.**

> **أي دور غير مُدرَج صراحة في قائمة المصرَّح لهم يُحجب عنه كل شيء. لا يُمنح دور جديد رؤيةً
> بالسكوت.**
> **ولا يُضاف أي دور جديد قبل قلب هذا الافتراض.**

**السبب: العطب صامت ولا يكشفه اختبار — لا اختبار لدور لم يوجد بعد.**
**ودور يُنسى فيُحجب أهون من دور يُنسى فيرى كل مزارع الشركة.** الأول عطلٌ يُبلَّغ عنه في أول ساعة
عمل ويُصلَح، والثاني **تسريب لا يشتكي منه أحد** لأن من يراه لا يعلم أنه لا يحقّ له.

**والحال اليوم عكس ذلك حرفيًا:** `visibleFarmCondition` تبدأ بـ
`if (!isAssignmentScoped(viewer.role)) return undefined;` — **و`undefined` تعني «بلا شرط» أي
«كل شيء»**. **وهذا يمسّ `entityScope.ts` وحده** (المصدر الواحد للنطاق، #126)، فالقلب تعديل في
ملف واحد **ما دام الفرض مركزيًّا** — وهذا نفسه مبرّر جمعه هناك.
**ولا يُنقض به المبدأ السادس:** «غير موجود ← 404، موجود غير مُسند ← 403» يبقى كما هو؛ **القلب
يخصّ الافتراض عند غياب قاعدة، لا ترتيب الردّين.**

**٢. حسم التعارض الأول: أمين المخزن أمين حفظ لا آمر صرف.**
**يستلم من المورّد · وينفّذ أوامر الصرف · ويجرد · ويرفع طلبات التوريد. ولا يأمر بالصرف من
المركزي — الأمر للمالك وحده.**

**وقاعدة «صاحب المخزن يأمر» تُعدَّل إلى:**

> **الآمر بالصرف هو المالك من المركزي، والمشرف من مخزن الموقع. ولا استثناء.**

**والفصل متسق مع المبدأ #155**: من يحفظ الرصيد ليس من يأمر بإخراجه، كما أن من يُدخل الرصيد ليس
من يصادق عليه.

**٣. حسم التعارض الثاني: «مخزن مزرعته» خطأ في اللفظ.**
**المقصود مخزن الموقع، يديره المشرف المسؤول عن مزارع ذلك الموقع.**
**ولا مستوى «مزرعة» في المخازن — المستويات ثلاثة: مركزي · موقع · عنبر.**

**٤. تاريخ الصلاحية خاصية عبوة لا صنف.**
**يُسجَّل على دفعة التوريد لا على المنتج — عبوتان من نفس اللقاح تختلفان.**
وهذا يعني أن **حركة الاستلام هي حاملة التاريخ**، وهو ما يجعل الحكم السادس («لا يُصرف منتهي
الصلاحية») **قابلًا للتنفيذ أصلًا** — كان مستحيلًا على البنية القائمة لأن لا مفهوم عبوة فيها.

**٥. وزن الكيس مصدر واحد لا ثلاثة: على الصنف.**
**وإعداد المستأجر («وزن كيس العلف») والحقل المحفوظ في صفّ العلف (`daily_log_feed_rows.
bag_weight_kg`) يُسجَّلان تعارضًا يُحسم قبل البناء** — لا يُحلّ هنا.

**٦. الجرد الافتتاحي ليس الجرد الدوري.**
**المربّي يُدخل افتتاحي عنبره ولا يملك الجرد الدوري.** **و§12.2 صُحِّح على هذا التمييز**: صفّ
«جرد · تحويل · هالك» فُصل، وأُضيف صفّان — **إدخال الرصيد الافتتاحي** و**مصادقة الرصيد الافتتاحي**
— على المبدأ #155.

**وخانتا المستوى الأوسط حُسمتا بعدها:**
**افتتاحي مخزن الموقع يُدخله المشرف المسؤول عن ذلك الموقع، ويصادق عليه المالك.**
**والمالك لا يُدخل افتتاحيًا إطلاقًا — هو مصادِق لا مُدخِل، والجمع بينهما نقض للمبدأ #155.**
**فتكتمل المستويات الثلاثة بلا موضع واحد يجمع الإدخال والمصادقة:** أمين المخزن يُدخل المركزي ·
المشرف يُدخل مخزن موقعه · المربّي يُدخل عنبره — **والمالك يصادق على المركزي ومخازن المواقع،
والمشرف يصادق على عنابر مزارعه.**

**٧. فئات المنتجات — تُصحَّح القائمة على القائم.**
**الفيتامينات فئة قائمة تبقى**، **والمعقمات والمطهرات تُفصل عن «مستلزمات» فئةً مستقلة**.
فتصير الفئات: **علف · دواء · لقاح · فيتامين · معقمات ومطهرات · مستلزمات**.
**وحدّ ما يحمله مخزن العنبر يكون بالفئة لا بكتلة «مستلزمات»** — وهو ما يجعل الحدّ قابلًا للفرض
أصلًا: كتلة واحدة تضمّ المطهر والمعدّة الإنشائية معًا لا تُفرَض عليها قاعدة.
**(القيمة الجديدة تدخل `product_category` عند البناء — لا يُلمس §8 الآن.)**

**٨. الكيس التالف ليس هالكًا.**
**يبقى في رصيد الفارغ بوصف مختلف ولا يخرج من الرصيد. والهالك خروج.**
فالفرق فرقُ **أثر على الرصيد** لا فرقُ تسمية: تسجيله هالكًا **يُنقص رصيد الفارغ فتختلّ معادلة
التحقق** («الممتلئ المستلم = الفارغ + الممتلئ المتبقي») **فتفقد قدرتها على كشف ما بُنيت له**.

**٩. المورّد والناقل كيانان يُنشآن مرة واحدة لا نصوصًا حرة.**
**ثلاثة قرارات تتقاطع عليهما:** الناقل (#157 البند ٣) · سجل المورّد (#160 السؤال الرابع) ·
متابعة أداء المورّد عبر الشحنات (#161 «تاسعًا»). **فيُنشآن مرة واحدة**، لا مرة لكل قرار.
**والحال اليوم: `products.supplier varchar(160)` و`shipments.carrier_name` — نصّان حرّان لا
يُجمَّع عليهما شيء.**

**١٠. ترتيب التغييرين على `user_assignments` صريح.**

> **القرار #158 يُنفَّذ أولًا (الإسناد بمدة)، ثم يُضاف مستوى المخزن فوقه.**

والسبب أن الثاني يبني على بنية الأول: القيد يتحول من «أحد مستويين» إلى «أحد ثلاثة»، **وقيد عدم
التداخل الزمني (#158 البند ٢) يجب أن يكون قائمًا قبل أن يُوسَّع ليشمل المستوى الثالث** — عكسُ
الترتيب يعني كتابة القيد مرتين.

**١١. ملاحظة عامة تُسجَّل: قائمة أسباب الهالك أغنى من النموذج مرتين.**
**«انقطاع تبريد»** (#157 البند ٥) و**«انتهاء صلاحية»** (#161 التعارض الخامس) — **النظام يعرض
أسبابًا لا يستطيع كشفها ولا منعها**، فيقبل تسجيل نتيجةٍ لا يملك سببها.
**تُحسم مع بناء المخزون** — والحكم الرابع أعلاه يغلق الثانية منهما.

**ولا يُلمس شيء من هذا الآن** — أحكام تُنفَّذ في المرحلة 3، **والبند ١ منها شرط إغلاق لأي دور
جديد**. مسجَّل دَينًا في `work-plan.md` §7-ب البند 32.

**162. الماء — التسجيل بعدد التعبئات، والتنبيه على تغيّر النمط**
**قرار منتج. تسجيل فقط — لا سطر كود، ولا يُفتح قبل إغلاق المرحلة 2-أ.**

**أولًا — السياق.**
**الخزان يُملأ بمضخات تعمل بالطاقة الشمسية، فالتعبئة نهارية لا مستمرة. والخزان احتياطي الليل
والغيم لا مصدر دائم.**
**والسعة تختلف بين العنابر** (٥٠٠ لتر · ١٠٠٠ لتر) **وهي مسجَّلة اليوم في بطاقة العنبر.**
**ولا يوجد عدّاد ماء — التقدير بعدد مرات التعبئة.**

**ثانيًا — التسجيل، وهو أهم ما في القرار.**

> **المربّي يسجّل يوميًا عدد مرات تعبئة الخزان، والنظام يحسب الكمية بضربه في سعة الخزان.**
> **ولا يُطلب من المربّي تقدير لترات: يسجّل ما فعله، والحساب على النظام.**

**السبب: لو قدّر كل مربٍّ باللترات بطريقته، ضاعت المقارنة بين العنابر وبين الأيام.**
**والتعبئة الجزئية لا تُسجَّل — تعبئة كاملة أو لا شيء. الخطأ في تقدير الكسور أكبر من فائدته.**

**وقيمة هذا التسجيل: انخفاض شرب الطيور أول علامة على المرض، تسبق ظهور أي عرض بيوم أو يومين.**

**ثالثًا — التنبيه على تغيّر النمط لا على رقم ثابت.**
**يُنبَّه حين ينخفض عدد التعبئات عن معدل الأيام السابقة لنفس العنبر، لا عند حدٍّ ثابت.**
**السبب: لكل عنبر معدله بحسب حمولته وعمره وحرارة موقعه، فالحد الثابت إما يزعج أو لا يمسك شيئًا.**

**والتنبيه يذهب للمربّي والمشرف والطبيب.**
**ولا يُفترض سبب واحد:** الانخفاض قد يعني **شربًا أقل (مرض)** أو **ضخًّا أضعف (عطل مضخة أو
ألواح)**. **النظام يعرض الواقعة ولا يشخّص.**

**رابعًا — كفاية الاحتياطي.**
**يُعرض كم يكفي الخزان بالساعات عند الحمولة والعمر الحاليين، ويُنبَّه حين لا يغطي حتى الضخ
التالي.**
**والحساب تقديري لا قاطع** — أرقام الاستهلاك تختلف بالحرارة والسلالة، **فالنظام يُنبّه ولا يمنع
شيئًا** (المبدأ الخامس).
**والحمولة المعتمدة في الحساب هي الرصيد الحالي لا العدد الابتدائي** (#160).

**خامسًا — تنبيه عند توزيع الدفعة.**
**حين يوزّع المشرف الكتاكيت على العنابر، يُنبَّه إن كانت سعة خزان عنبر لا تحتمل الحصة المقترحة
في ذروة العمر. تنبيه لا منع.**

**سادسًا — ربط بالصيانة.**
**عطل المضخة أو انخفاض إنتاج الألواح انقطاع ماء.** **ويُربط تنبيه الماء ببند الصيانة** —
واستبدال الألواح الشمسية وارد في خدمات المشرف بالقرار #154.

**سابعًا — ثلاثة أسئلة مفتوحة بخياراتها وبلا ترجيح. القرار للمالك.**

1. **مصدر أرقام الاستهلاك المتوقع:** جدول قياسي للسلالة · إعداد يضبطه المالك · محسوب من تاريخ
   المزرعة نفسها.
2. **عنبر بأكثر من خزان: كيف يُسجَّل؟**
3. **هل يُسجَّل وقت التعبئة أم عددها اليومي فقط؟**

**ثامنًا — فحص المبنيّ والمواصفة: تعارض واحد صريح ومبنيّ، وسبع تغطيات جزئية.**

**وهذا القرار أقرب القرارات إلى المبنيّ: آليته الأساسية موجودة بالفعل** — بخلاف #156 و#159 و#161.

**التعارض — واحد، لكنه مكتوب في ثلاثة مواضع ومبنيّ في الكود:**

1. **🔴 «التعبئة الجزئية لا تُسجَّل» يناقض ما هو مبنيّ وموصوف: النظام اليوم مصمَّم للكسور صراحةً.**
   - **في الكود:** `daily_logs.water_tanks numeric(8, 3)` — **ثلاث منازل عشرية**، أي أن الحقل
     مبنيّ لقبول `0.25` و`0.5`.
   - **وفي الوثيقة الشاملة مرتين:** «**كمية الماء — الخزانات بخطوة 0.25**» (شاشة السجل اليومي)،
     و«**يُستخدم في: … خزانات الماء (0.25)**» (مكوّن العدّاد).
   **فالكسر ليس احتمالًا سكت عنه التصميم بل قرارُ واجهة معلن وخطوةُ عدّاد مضبوطة عليه.**
   **والحكم يجعل الخطوة ١ ويُلغي المنازل العشرية** — تغيير في الحقل وفي المكوّن وفي نصّ الوثيقة
   معًا.

**والتغطيات الجزئية السبع:**

1. **آلية الحكم الثاني مبنيّة كاملة — بأسمائها.** `daily_logs` تحمل:
   `water_tanks` (**عدد التعبئات**) · `tank_capacity_l` (**السائد وقت الإدخال**) ·
   `water_liters` (**محسوب**). **فـ«يسجّل ما فعله والحساب على النظام» قائم حرفيًّا**، والحكم
   **يثبّته ويصحّح دقّته لا يستحدثه**.
   **ونمط «السائد وقت الإدخال» نفسه هو ما يجعل تغيير سعة الخزان لاحقًا لا يفسد سجلات الماضي** —
   نفس نمط `bag_weight_kg`.
2. **سعة الخزان على العنبر قائمة — ولها سلوك لم يذكره الحكم.**
   `houses.water_tank_capacity_l numeric(10,2)`، **و`NULL` تعني «حقل الماء مخفي»** في المواصفة.
   **فعنبر بلا سعة مسجَّلة لا يُسجَّل له ماء أصلًا**، ولا تنبيه نمط ولا كفاية احتياطي.
   **يُسجَّل ولا يُحسم**: هل يبقى الإخفاء بعد أن صار الماء مؤشرًا صحيًّا مبكرًا؟
3. **نمط «التنبيه على تغيّر النمط» له سابقة واحدة في النظام — ونجحت.**
   §16 تحمل للطبيب: «**نفوق ضعف المعدل**» — **تنبيه نسبي إلى خطّ أساس لا حدٌّ ثابت**، وهو بالضبط
   بنية الحكم الثالث. **فالحكم يوسّع نمطًا قائمًا لا يستحدث فلسفة تنبيه.**
4. **ولا تنبيه ماء في القائمة اليوم إطلاقًا — والقائمة موصوفة نصًّا بأنها «مغلقة».**
   العاجل: المربّي (مهمة · شحنة · ردّ المشرف) · الطبيب (بلاغ شديد · تعذّر علاج · نفوق ضعف
   المعدل) · المشرف (نزاع · عنبر لم يُسجَّل · تغطية أقل من يومين) · المالك (نزاع · بلاغ · تصفية ·
   رصيد سالب). **والحكم الثالث يضيف تنبيهًا واحدًا إلى ثلاث قوائم منها** (المربّي والمشرف
   والطبيب) — **وهي ثاني مرة تُوسَّع فيها قائمة معلَنة الإغلاق** بعد #160.
5. **«كم يكفي بالساعات» له نظير محسوب بالفعل — للعلف.**
   §15: `coverage_days = balance / daily_consumption` و`daily_consumption = متوسط آخر 3 أيام`.
   **فالحكم الرابع هو نفس المعادلة بوحدة أصغر ومصدر استهلاك مختلف** — **والفرق الجوهري أن العلف
   استهلاكه مقيس والماء استهلاكه مقدَّر** (لا عدّاد)، **وهو ما يبرّر نصّ الحكم على أن الحساب
   تقديري لا قاطع.**
6. **🔴 وهذا القرار هو المستهلك الغائب لحقل الطاقة — دَينٌ مسجَّل منذ الدفعة 3.**
   `farms.power_sources` مصفوفة (`شمسية` · `مولدات`) بقيد `cardinality >= 1`، **وسُجِّل في
   §7-ب البند 14 أنه لا يستهلكه شيء اليوم: «لا إنذار انقطاع ولا تقرير طاقة… ليُبحث عند بناء
   الإنذار المبكر»**. **وهذا هو الإنذار المبكر.**
   **ويعيد فتح ما حُسم وقتها:** الطاقة **على المزرعة وحدها، وحُذف عمود العنبر عمدًا** لأن المولّد
   يخدم مزرعة فيها عنابر — **والمضخة الشمسية في هذا القرار تخدم خزان عنبر**. فسؤال «أي مستوى
   يحمل الطاقة؟» **يعود بمعطًى جديد**: عطل مضخة عنبر واحد ليس عطل مزرعة. **يُسجَّل ولا يُحسم.**
7. **الحكمان الخامس والسادس يتّكئان على ما لم يُبنَ بعد.**
   **الخامس** يقع في **لحظة توزيع الكتاكيت** — وهي خطوة يستحدثها #160 ولا وجود لها اليوم؛
   فالتنبيه **بند في تلك الخطوة لا ميزة مستقلة**.
   **والسادس** يربط بـ**بند الصيانة** — **وهو عمل بلا صاحب مسجَّل** (#157 البند ٦/٦):
   `house_status` فيه «تحت الصيانة» **بلا دور يملكها ولا سجل لما أُصلح**. **فالربط يحتاج طرفًا
   ثانيًا غير موجود.**

**وملاحظة على السؤال الأول — واقعة لا ترجيح:** `breed_standards` جدول مرجعي **لكل سلالة ولكل يوم
من 1 إلى 45**، ويحمل اليوم **الوزن المستهدف والنفوق المتوقع ومعامل التحويل** — **ولا يحمل ماءً**.
فالخيار الأول في السؤال («جدول قياسي للسلالة») **يجد جدولًا جاهزًا بعمود ناقص**، لا بنية جديدة.

**تاسعًا — سبعة أحكام لاحقة، أُضيفت إلى هذا القرار بعد تسجيله. وأولها سحبٌ لحكم.**

**١. حكم التعبئة الجزئية مسحوب — والخطوة ٠٫٢٥ تبقى.**

> **خطوة ٠٫٢٥ تبقى كما هي مبنيّة وموصوفة، ولا يُغيَّر الحقل ولا المكوّن ولا نصّ الوثيقة.**

**السبب: المربّي يرى ربع الخزان ونصفه بالعين، فالربع قراءة لا تخمين.**
**والتصميم القائم أصوب من الحكم الأول هنا** — وهو ما يجعل هذا **سحبَ حكمٍ لا تعديلَه**:
`daily_logs.water_tanks numeric(8,3)` والخطوة `0.25` في شاشة السجل ومكوّن العدّاد **تبقى ثلاثتها
كما هي بلا مساس**.
**والتعارض الوحيد في فحص هذا القرار زال بذلك — لا بتعديل المبنيّ بل بسحب ما خالفه.**

**٢. ثلاثة أرباع القرار مبنيّ — والقرار يثبّتها ويضيف التنبيه وحده.**

| المبنيّ | موضعه |
| --- | --- |
| **عدد التعبئات** | `daily_logs.water_tanks` |
| **سعة الخزان السائدة وقت الإدخال** | `daily_logs.tank_capacity_l` |
| **الحساب التلقائي** | `daily_logs.water_liters` (محسوب) |

**والجديد هو التنبيه وحده** (الأحكام ثالثًا إلى سادسًا).

**ونمط «السائد وقت الإدخال» يُصان: تغيير سعة الخزان لاحقًا لا يفسد سجلات الماضي.**
فالسجل يحمل السعة التي كانت وقت كتابته لا التي صارت اليوم — **ولولاه لتغيّر استهلاك الماء
لعام مضى بتبديل خزان**. وهو نفس نمط `bag_weight_kg` في صفوف العلف، **ويُصان في الحكم الثالث
أدناه أيضًا** حين تصير السعة إلزامية.

**٣. سعة الخزان تصير حقلًا إلزاميًا للعنبر لا اختياريًا، و`NULL` لا يُقبل بعد اليوم.**

**السبب: إخفاء حقل الماء كان مقبولًا حين كان رقمًا، ولا يُقبل بعد أن صار مؤشرًا صحيًّا مبكرًا.
عنبر بلا سعة يفقد التسجيل والتنبيه معًا** — أي يفقد **أول علامة على المرض** لا حقلًا في نموذج.

**والحال اليوم: `NULL` مقبولة في ثلاثة مواضع لا واحد**، وكلها تتغيّر معًا:
1. **القاعدة:** `houses.water_tank_capacity_l numeric(10,2)` **بلا `NOT NULL`**، وتعليقه في
   الكود والمواصفة: «**`NULL` = حقل الماء مخفي في الواجهة**».
2. **الإنشاء:** `POST /farms/:farmId/houses` — السعة **اختيارية** (`waterTankCapacityL?`)،
   فالعنبر يُنشأ بلا سعة.
3. **التعديل:** `PATCH /houses/:houseId` — **يقبل `null` صراحةً** (`number | null | undefined`)،
   أي أن سعةً مسجَّلة **يمكن محوها بعد تسجيلها**. وهذا الموضع **أخطرها**: يُبطل الحقل بعد أن
   بُنيت عليه سجلات.

**ومعالجة العنابر القائمة بلا سعة عند التنفيذ:**
**ترحيل واحد على ثلاث خطوات في ملف مرقَّم** (لا `drizzle-kit push` — القاعدة قائمة):
**تعبئة رجعية للصفوف الفارغة → ثم `SET NOT NULL` → ثم منع `null` في مسار التعديل.**
**ولا تُخترع سعة لعنبر قائم**: العنبر بلا سعة **يحتاج قيمة من المالك لا قيمة افتراضية** — رقمٌ
مُخترع يُنتج استهلاك ماء كاذبًا وتنبيهات نمط مبنية عليه، **وهو أسوأ من غياب الحقل**.
**فالترحيل يفصل الخطوتين:** الفرض في القاعدة **لا يُطبَّق قبل أن تكتمل التعبئة الرجعية**،
والعنابر الناقصة **تُعرض للمالك ليملأها** قبل تشغيل القيد.
**وقيمة السعة السابقة في السجلات لا تُمسّ إطلاقًا** — `tank_capacity_l` أثرٌ لما كان (الحكم
الثاني)، **فالتعبئة الرجعية تخصّ `houses` وحدها ولا تلمس `daily_logs`.**
**(ونصّ «`NULL` = حقل الماء مخفي» في `backend-technical-spec.md` §7.1 يسقط عند التنفيذ — لا
يُلمس الآن.)**

**٤. الطاقة: لا يُعاد فتح ما حُسم في §7-ب البند 14.**

> **المولّد يخدم مزرعة، والمضخة الشمسية تخدم خزان عنبر — كيانان مختلفان لا واحد.**
> **والمضخة تُعالَج كمعدّة على العنبر ضمن بند الصيانة، ولا تعود عمودَ طاقة على العنبر.**

**فالملاحظة التي رُفعت في فحص هذا القرار تسقط**: لم يكن في الأمر مستوًى واحد يتنازعه العنبر
والمزرعة، بل **شيئان اثنان خُلطا بلفظ «الطاقة»**. `farms.power_sources` يبقى كما هو بلا مساس،
**ودَين §7-ب البند 14 يُغلق بهذا** — لا بإضافة عمود.

**٥. الحكمان الخامس والسادس تابعان — تبعيتان صريحتان.**

- **الخامس (تنبيه عند التوزيع) يقع في خطوة توزيع الكتاكيت التي يستحدثها #160** — فهو **بند في
  تلك الخطوة لا ميزة مستقلة**، ولا يُبنى قبلها.
- **والسادس (الربط بالصيانة) يحتاج بند الصيانة، وهو عمل بلا صاحب** (#157 البند ٦/٦): «تحت
  الصيانة» حالة بلا دور يملكها ولا سجل لما أُصلح. **ولا يُبنى قبل أن يوجد طرفه الثاني.**

**وباقي القرار غير تابع لهما** — التسجيل والتنبيه على النمط وكفاية الاحتياطي **قائمة بذاتها**.

**٦. السؤال الأول يبقى مفتوحًا — ومعه واقعة تُقاس بها كلفة خياره الأول.**
`breed_standards` جدول **لكل سلالة ولكل يوم من 1 إلى 45**، يحمل **الوزن المستهدف والنفوق المتوقع
ومعامل التحويل — ولا يحمل ماءً**.
**فالخيار الأول («جدول قياسي للسلالة») يعني إضافة عمود لجدول قائم لا بناء بنية جديدة.**
**والسؤال يبقى مفتوحًا بلا ترجيح** — الواقعة تقيس الكلفة ولا ترجّح الخيار.

**٧. ملاحظة عامة تُسجَّل: هذه ثاني قائمة موصوفة نصًّا بأنها «مغلقة» تُوسَّع.**
الأولى **قائمة العاجل للمالك** (#160 الحكم الرابع: أمر الصرف)، **والثانية قوائم العاجل للمربّي
والمشرف والطبيب** (تنبيه الماء هنا).

> **يُراجَع عند البناء: أيّ القوائم مغلقة فعلًا، وأيّها وُصفت كذلك سهوًا.**

**والفرق ليس لفظيًّا:** قائمة **مغلقة فعلًا** تعني أن كل إضافة إليها قرار مالك موثَّق؛ وقائمة
**وُصفت كذلك سهوًا** تعني أن الوصف يمنع إضافات مشروعة **ويُخيف من تعديل هو الصواب**. **وتوسيعها
مرتين في قرارين متتاليين مؤشّرٌ على الثاني لا الأول.**

**ولا يُلمس شيء من هذا الآن** — أحكام تُنفَّذ في المرحلتين 2 و3. مسجَّل دَينًا في
`work-plan.md` §7-ب البند 33.

**163. تضييق القاعدة #27 — الحساب تهيئة لا بيانات**
**قرار مالك، اتُّخذ لفتح الدفعة 6.**

> **قاعدة «البذر عبر الـAPI حصريًا» (#27) تخصّ البيانات الميدانية. والحساب تهيئة لا بيانات.**

**فالمستأجر وحساباته الأربعة يُنشآن بإدراج مباشر معزول، وكل ما بعدهما — المواقع والمزارع
والعنابر — يمرّ بالـAPI بصلاحية المالك.**

**والسبب بنيوي لا تفضيلي:** إنشاء المستأجر يخصّ **طبقة مدير المنصة**، وهي مجمَّدة بشرط الإغلاق
(§7-ب البند 25)؛ و`POST /users` من المرحلة 4. **فلا مسار API قائم أصلًا**، وبناء واحد هنا **يمسّ
طبقة الأدوار** التي مُنع مسّها في هذه الدفعة.

**وهذا نفس استثناء حساب مدير المنصة في القرار #147 حرفيًّا:** «ولا يُنشأ حساب مدير منصة من أي
واجهة إطلاقًا — يُنشأ بإجراء مباشر مقيَّد، **ولا يخضع لقاعدة البذر عبر الـAPI حصرًا (#27) لأنه
ليس بيانات مستأجر**». **فالتضييق ليس جديدًا بل تعميم لاستثناء قائم ومكتوب.**

**وغرض #27 يبقى مصانًا كاملًا:** القاعدة وُضعت لأن الإدراج المباشر **يبذر بيانات لا يستطيع
مستخدم حقيقي إنشاءها** فيخفي أخطاء الصلاحيات والتحقق — وهذا يخصّ **البيانات الميدانية**، وهي
هنا كلها عبر الـAPI بلا استثناء واحد.

**وأربعة قيود على التضييق:**

1. **حارس بيئة صارم بلا باب خلفي.** التهيئة لا تعمل إلا في `development` أو `test`،
   **وتفشل بصوت عالٍ في أي قيمة أخرى — بما فيها الغائبة والمجهولة**. الافتراض «لا تعمل» لا
   «تعمل»، وهو نفس قلب الحارس في القرار #161.
2. **صفوف الإسناد تُكتب لتُرحَّل بلا فقد، ولا يُبنى عليها منطق.** مستوى واحد لكل صفّ (#128)،
   و`created_at` الافتراضي **هو مصدر تاريخ البداية** حين يصير الإسناد بمدة (#158). فلا صفّ يحتاج
   تخمينًا ولا إعادة إنشاء.
3. **لا مساس بـ`USER_ROLE` ولا بـ`platform_admin` ولا بشاشات `platform/`** — البند 25 شرط إغلاق
   قائم، والدفعة لا تقترب منه.
4. **كلمة المرور من البيئة لا من الكود** (`SEED_DEMO_PASSWORD`). كلمة مثبَّتة في المستودع تصير
   كلمة كل من نسخه، **ونصٌّ حرفي في الكود يخالف القرار #100** ويلتقطه الفاحص المخصص.

**164. الدفعة 6 — بذر المواقع السبعة، وبها تُغلق المرحلة 2-أ**

**ما بُني:** `pnpm seed:demo` كامل. **خطوتان بحدٍّ صريح بينهما:** تهيئة الحسابات في
`scripts/seed/accounts.ts` (الموضع الوحيد الذي يكتب في القاعدة، القرار #163)، **ثم كل ما بعدها
عبر الـAPI**.

**و«عبر الـAPI» تعني الطلب الكامل لا استدعاء الخدمة.** السكربت **يفتح خادمًا مؤقتًا على منفذ
يختاره النظام** ويخاطبه بـ`fetch` حقيقي — فيمرّ بـ`requireAuth` → `requireLiveSession` →
`requireTenant` → `enforceEntityAccess` → `requireRole` → التحقق من الجسم → الخدمة.
**واستدعاء الخدمة رأسًا كان سيتخطّى السلسلة كلها** فيبذر بيانات لا يستطيع مستخدم حقيقي إنشاءها —
وهو بالضبط ما وُضعت القاعدة #27 لمنعه.

**والمبذور — مُثبَت على القاعدة الحيّة لا موصوفًا:**

| الكيان | العدد |
| --- | --- |
| مستأجر | 1 |
| حسابات (مالك · مشرف · طبيب · مربّي) | 4 |
| مواقع | **7** — الجبل · الكرنة · الصعيد · الطويلة · الجاح · الخماسية · الحمراء |
| مزارع | 13 |
| عنابر | 35 |
| صفوف إسناد | 9 |

**وخمسة من المواقع السبعة بأكثر من مزرعة** — تصديق المالك في #116: «المستوى الثالث الحالة
الغالبة لا الاستثناء». **وبيانات بمزرعة واحدة لكل موقع كانت ستخفي المستوى الذي بُني له.**

**والإثبات الحيّ — أربعة أدوار على خادم يعمل:**

```
GET /api/sites  (المالك 770000001)  ← 7 مواقع
GET /api/sites  (المشرف 770000002)  ← الجبل · الكرنة        (مزارعه المُسندة)
GET /api/sites  (الطبيب 770000003)  ← الصعيد                (مزارعه المُسندة)
GET /api/sites  (المربّي 770000004)  ← الجبل                 (موقع عنبريه)
```

**فالقرارات #129 و#131 مرئية على بيانات حقيقية لا في اختبار وحده** — وهذا ما يجعل البذر أداة
تحقّق لا مجرّد تعبئة.

**والعطالة على مستوى المستأجر لا على كل طلب:** وجود المستأجر بالاسم يعني أن الهرم مبذور فيُرجَع
بلا عمل. **مُثبَت: تشغيل ثانٍ أعطى «مبذور سابقًا — لا تغيير» والأعداد لم تتغيّر.** وهذا لازم لأن
دليل الجوال يطلب تشغيل الأمر عند كل مشكلة قوائم فارغة.

**وستة تأكيدات آلية** في `scripts/seed.integration.test.ts` — الأعداد · أسماء المواقع السبعة ·
دخول الحسابات الأربعة فعلًا · نطاق الرؤية (7 مقابل 1) · العطالة · **ورفض الحارس في `production`
مع إثبات أن لا صفّ كُتب**.

**وعطبٌ التقطه أول تشغيل للاختبار وسُجِّل في الكود:** حدّ الدخول **٥ محاولات في الدقيقة لكل
تطبيق** (§11)، والاختبار كان يسجّل دخولًا في كل استدعاء فارتدّ **429** — **ففشل لسبب لا يخصّ ما
يزعم فحصه**، وهو صنف العطب الذي حذّر منه القرار #133. **الإصلاح: رمز واحد لكل حساب يُجلب مرة في
`beforeAll`.** **والبذر نفسه يسجّل دخولًا واحدًا فقط**، فإعادة تشغيله لا تقترب من الحدّ.

**وأسماء المزارع والعنابر افتراضية** — المالك سمّى المواقع السبعة ولم يسمِّ ما تحتها. **تُستبدل
بأسماء الميدان متى وردت، ولا يُبنى عليها شيء.**

**والمرحلة 2-أ تُغلق بهذه الدفعة.** والمرحلة 3 **لا تُفتح** قبل تجربة المالك على جواله، وقبل
شرطَي الإغلاق: **فصل مدير المنصة (البند 25) والإسناد بمدة (البند 29)** — وقد انضمّ إليهما شرطان
لاحقان: **حراسة `locationType`/`locationId`** (#157) و**قلب افتراض الحارس قبل أي دور جديد**
(#161).

**165. مخزن الجلسة يعرف منصته — والمتصفح لم يكن يعمل أصلًا**
**عطبٌ حقيقي كشفه أول تشغيل على جهاز المالك، لا مراجعة ولا اختبار.**

**ما وقع:** المالك شغّل التطبيق على متصفح ماكه، فعلق على شاشة تحميل بلا خطأ ظاهر — وفي الزاوية
إشعار: `ExpoSecureStore.default.getValue…`.

**والسبب: `expo-secure-store` لا يعمل في المتصفح إطلاقًا** — لا Keychain ولا Keystore هناك.
و`app/index.tsx` **يقرأ الجلسة عند الإقلاع قبل أن يرسم شيئًا**، فيرمي المخزن استثناءً **فيتوقف
كل شيء قبل أول بكسل**. **ولا رسالة مفهومة للمستخدم** — شاشة تحميل أبدية.

**القرار: `lib/session.ts` يعرف منصته.**

| المنصة | المخزن |
| --- | --- |
| **الجوال** | **`expo-secure-store` — بلا تغيير**، والمواصفة §11 قائمة كما هي |
| **الويب** | `localStorage`، وحين يكون محجوبًا **يعمل التطبيق بلا جلسة محفوظة ولا يرمي** |

**ولا `AsyncStorage` إطلاقًا** — القاعدة الأصلية باقية: نصّ عادي على قرص الجهاز هو ما مُنع.

**والحدّ مكتوب صراحةً ولا يُقرأ تصريحًا مفتوحًا:** `localStorage` **أضعف حمايةً** — بلا تشفير،
ويقرؤه أي سكربت في نفس الأصل. **مقبول للتطوير والمراجعة على المتصفح**، **ومنصة المحاسبين
المخطَّطة (#137 و#138) تحتاج قرارها المستقل** في تخزين الرمز حين تُبنى.

**ولماذا الويب أصلًا:** فحوص تخطيط RTL تعمل في متصفح، ومنصة المحاسبين متصفح بالكامل — **فالويب
مسار في الطريق لا التفاف عليه**. وقد صار اليوم **المسار الوحيد المتاح للمالك** لأن شبكته تعزل
الأجهزة.

**والفرع مُثبَت أنه حامل للحمل لا زينة:** تثبيته على غير الويب **يُسقط ثلاثة من أربعة اختبارات**
في `lib/session.rtl.test.tsx` — منها الاختبار الذي يفحص **أن `expo-secure-store` لا يُلمس على
الويب إطلاقًا**، وهو العطب بعينه. **وتعليق `jest.config.js` صُحّح**: كان يصنّف `session` غلافًا
رفيعًا «بلا تفرّع منطقي» — ولم يعد كذلك.

**وأربعة عيوب في `docs/run-on-phone.md` كشفها التشغيل نفسه — كلها تمنع الدليل من العمل:**

1. **`.env` لا يُقرأ تلقائيًا** (لا `dotenv` في الكود عمدًا)، والدليل **لا يذكر تحميله** —
   فـ`pnpm migrate` يفشل عند أول محاولة. **أُضيف `set -a && source .env && set +a` إلى كل أمر
   يلمس القاعدة.**
2. **🔴 الدليل يوجّه إلى تعديل `.env` في جذر المشروع** — **وتطبيق الجوال لا يقرأ ذاك الملف
   إطلاقًا**، بل `apps/mobile/.env` وحده. **فالخطوة الموصوفة «أهم خطوة في الدليل كله» كانت بلا
   أثر.** استُبدلت بأمر واحد يكتشف الرقم ويكتبه في الملف الصحيح.
3. **سطر اكتشاف عنوان الشبكة معطوب على كل ماك:** `!a.internal` داخل تنصيص مزدوج **يُفسَّر توسيعَ
   تاريخ في zsh** (الصدفة الافتراضية على macOS) فيفشل بـ`event not found`. أُعيدت صياغته بلا `!`.
4. **ولا كلمة عن الشبكات التي تعزل الأجهزة** (Client Isolation) — **وهي ما وقع فعلًا**: فشل
   الوصول على الراوتر **وعلى نقطة اتصال الجوال معًا**، والجدار مطفأ. أُضيف قسم تشخيص كامل
   **ومعه بديل المتصفح**.

**ودرس منهجي يُسجَّل:** الفحص الحاسم كان **فتح عنوان الخادم في متصفح الجوال** — لأنه **لا يمرّ
بـExpo Go إطلاقًا**، فيفصل «هل يصل الجوال؟» عن «هل التطبيق يعمل؟». **وقبله ضاعت جولات في اختبار
السؤالين معًا** — وهو نفس صنف الخطأ في القرار #133: القفز من عرَض إلى سبب بلا فحص يفصل بينهما.

**وما أثبته التشغيل إيجابًا:** الحزمة تُبنى (`expo export` نظيف، 5.61 ميجابايت)، والخادم يعمل،
والبذر ينشئ 7 مواقع و13 مزرعة و35 عنبرًا، والحسابات الأربعة تدخل فعلًا. **العطل كان في المتصفح
والدليل، لا في المشروع.**

**166. زرّ الخروج — نقصٌ قائم كشفه أول تشغيل، لا ميزة للتجربة**
**طلب المالك زرّ خروج «للتجربة ثم نحذفه». والقرار: يُبنى دائمًا لا مؤقتًا.**

**السبب الأول عملي:** العمل واحد في الحالتين، **والمؤقت يزيد عليه حذفًا** — ويخاطر بأن يُنسى.

**والسبب الثاني أثقل، وهو ما جعله نقصًا لا تأجيلًا:** التطبيق قبل هذا **لا يمسح الرمز إلا حين
يرفضه الخادم** (`app/index.tsx`). **فمن دخل على جهاز بقي داخلًا** — وهو **عيب أمني لا نقص راحة**،
كشفه سؤال بسيط من المالك أثناء أول تجربة حقيقية.

**والموضع: أيقونة الحساب في الترويسة.**
كانت موجودة في `AppHeader` **بخاصية `onAccountPress` بلا وظيفة**، فأُعطيت معناها الطبيعي.
**والبديل المطروح — تبويب «المزيد»** — أقرب إلى المواصفة (§4، مذكور عند الطبيب وحده) **لكنه
تبويب جديد كامل يحتاج قرارًا في محتواه**، فيؤجّل ما لا يحتمل التأجيل. **وحين يُبنى «المزيد»
يرث الخروج أو يتركه في الترويسة — قرار مستقل وقتها.**

**والورقة تحمل الاسم والدور لا الزرّ وحده.** وليس تجميلًا: **مراجعة الصلاحيات بأربعة حسابات على
جهاز واحد تُنسب إلى الحساب الخطأ بلا هوية ظاهرة** — فيُظنّ التسريبُ صحيحًا أو الصحيحُ تسريبًا.
وهي أشيع أخطاء هذه المراجعة بالذات، وقد كان المالك على وشك الوقوع فيها.

**والشاشات النائبة صارت تحمل ترويسة.**
`PlaceholderScreen` كانت **بلا ترويسة إطلاقًا** — فالمربّي والمشرف والطبيب **يهبطون على شاشات
نائبة بلا أي سبيل للخروج**. فالزرّ في شاشة المالك وحدها كان سيترك ثلاثة أدوار من أربعة محبوسة.

**والخروج يوجّه إلى شاشة الدخول مباشرة.**

**وكان يوجّه إلى `/` بحجّة أن `app/index.tsx` هو من يقرّر الوجهة — وكان خطأً، أبلغ عنه المالك
وأُعيد إنتاجه بمتصفح آليّ قبل الإصلاح.** الرصد: الورقة تُغلق **والرمز يُمحى فعلًا** والشاشة
**كما هي** — فيبدو الزرّ معطَّلًا وهو يعمل.

**والسبب: المستخدم قد يكون على `/` أصلًا** — المسار الجذر يعرض تبويبات الدور **بلا تغيير
العنوان**. فالتوجيه إليه **لا شيء**: لا إعادة تركيب، ولا إعادة تشغيل لأثر `index` الذي يقرأ
الرمز ويقرّر. **الاعتماد على أثرٍ يعمل مرة عند التركيب في مسارٍ قد تكون عليه بالفعل — رهانٌ لا
تصميم.**

**ولا ازدواج في القرار كما ظننت:** وجهة `index` سؤالها «أين يذهب عائدٌ **يحمل** رمزًا؟» وتحتاج
قراءةً وتحقّقًا؛ ووجهة الخروج سؤالها «أين يذهب من **لا رمز له**؟» — **وجوابه واحد لا يحتاج
قراءة**. فما بدا تكرارًا كان سؤالين مختلفين.

**والدرس المتكرر:** الاختبارات الأربعة كانت خضراء **وهي تفحص الفرع لا الرحلة** — تستدعي
`onLogout` وتتأكد أنه نُودي، **ولا تفحص أين انتهى المستخدم**. **والمالك كشفه في أول ضغطة.**

**وأربعة تأكيدات:** الاسم والدور بالعربية · الزرّ يستدعي الخروج مرة واحدة · **يعمل قبل وصول
الهوية** (فشل `GET /auth/me` لا يحبس المستخدم) · والشاشة النائبة تحمل الترويسة وأيقونتها.

**167. `direction` ليست خاصية نمط في React Native — سطر أحمر في كل إقلاع**
**عطبٌ التقطه المالك من وحدة تحكم متصفحه أثناء تجربته، لا اختبار ولا مراجعة.**

`apps/mobile/components/ui/FormField.tsx` كان يحمل في نمط حقل الجوال:

```
ltrInput: { direction: "ltr", writingDirection: "ltr" }
```

**و`direction` ليست من خصائص نمط React Native** — فيرفضها المدقّق وقت التشغيل:
`Invalid style property of "direction". Did you mean "writingDirection"?` **في كل إقلاع.**

**والمواضع الستة الأخرى في المشروع تستعمل `writingDirection` وحدها** — فهذا الموضع **شاذّ لا
قاعدة**، وقد مرّ لأن أحدًا لم يقرأ وحدة التحكم في تشغيل حقيقي.

**والإصلاح: حذف السطر.** و**rn-web يترجم `writingDirection` إلى `direction` في CSS**، فالسلوك على
الويب لا يتغيّر — **مُثبَت لا مفترَضًا**: تأكيد التخطيط الذي يقرأ
`getComputedStyle(element).direction` على حقل الجوال **بقي أخضر بعد الحذف**، والسبعة عشر تأكيدًا
كلها خضراء.

**ولماذا لم تلتقطه بوابة:** الفحوص الإحدى عشرة تقرأ الكود والتخطيط، **ولا تقرأ وحدة تحكم متصفح
أثناء تشغيل حقيقي**. **وتحذيرات التشغيل صنف لا تراه أي بوابة قائمة اليوم** — يُسجَّل ولا تُضاف
بوابة الآن (البوابات موقوفة بقرار المالك).

**168. ثلاثة عيوب عرض كشفها التحقق الميداني الأول لشاشة المالك**
**المالك جرّب الشاشة على المتصفح وأبلغ. وهذه أول مرة يُفحص فيها المبنيّ بعينٍ خارج الاختبارات.**

**وما ثبت أنه يعمل — يُسجَّل تحقّقًا ميدانيًّا تمّ لا وصفًا:**

- **المواقع السبعة تظهر للمالك، والعدّادات متسقة** — الصعيد: 3 مزارع بعنابر 2+3+2 = 7، مطابق
  لبطاقة الموقع.
- **التخطّي يعمل** (القرار #132): «الطويلة» بمزرعة واحدة **قفزت إلى العنابر مباشرة**، و«الصعيد»
  بثلاث مزارع عرضت المزارع عاديًّا.
- **وسطر السياق ظهر عند التخطّي** (`الطويلة › مزرعة الطويلة`).
- **والرجوع بعد التخطّي صحيح**: من عنابر «الطويلة» **إلى قائمة المواقع مباشرة** لا إلى شاشة
  مزارع متخطّاة.

**وفحص متبقٍّ مؤجَّل بسبب خارج المشروع:** **زر الرجوع العتادي في أندرويد** لم يُفحص — شبكة المالك
تعزل الأجهزة فلا يصل جواله بحاسوبه (القرار #165). **يُفحص متى توفّرت شبكة تصل الطرفين.**

**والعيوب الثلاثة:**

**١. 🔴 نصّ حالة العنبر يُعرض بخطّ الأرقام — فتتفكك الكلمة العربية.**
`SiteCards.tsx` كان يعرض الحالة بـ**`StatTile`**، وهو **مكوّن المؤشرات الرقمية**: قيمته
`familyNumber` (أحادي المسافة) و`writingDirection: "ltr"` — صحيح لرقم، **خطأ لكلمة عربية**.
**والخطّ أحادي المسافة لا يشكّل العربية**، فتظهر حروفها منفصلة: «جـا هـز لـلإسكـان».

**مُثبَت بقراءة الأنماط الفعلية من المتصفح** لا بالنظر: `fontFamily: "monospace, monospace"` ·
`direction: "ltr"` · `fontSize: 18px` على نصّ «جاهز للإسكان» بعينه.

**والإصلاح: `Badge` بدل `StatTile`** — وهو مكوّن الحالة المخصص (§8.1: لون وأيقونة ونصّ معًا
دائمًا، **والاعتماد على اللون وحده ممنوع** لأن عمى الألوان شائع). وأُضيف `houseStatusIcon`
بجدول صريح بجانب `houseStatusTone` — **نفس مفاتيح الجدول الأول**، وحالة غير معروفة تأخذ **علامة
استفهام لا أيقونة سليمة**.

**ونطاقه: هذا الموضع وحده.** بقية استعمالات `StatTile` في الشاشة تحمل **أعدادًا** (المزارع ·
العنابر · مشغول · جاهز وشاغر · غير ذلك) وهي ما بُني له. **فالعطب ليس في `StatTile` بل في إعطائه
نصًّا عربيًّا.**

**ولا علاقة له بخطأ `Invalid style property of "direction"`** — ذاك في حقل الجوال وأُصلح في
القرار #167. **عطبان منفصلان تشابها في العرَض**: أحدهما خاصية نمط غير موجودة، والآخر خاصية
صحيحة بقيمة خاطئة للمحتوى.

**٢. ترتيب المزارع — الاستعلام كان صحيحًا، والأسماء هي المشكلة.**
السرد **مرتَّب بالاسم أصلًا** (`ORDER BY name ASC`) في المستويات الثلاثة. و«الأولى · الثالثة ·
الثانية» **ترتيب أبجدي صحيح**: «الأولى» بالهمزة أولًا، ثم «الثالثة» و«الثانية» يتفقان في «الثا»
ويفترقان في اللام قبل النون. **فالخطأ في تسمية بيانات العرض لا في الاستعلام.**

**وحُسم بأمرين:**
- **تيبريكر صريح**: `ORDER BY name, id` في المستويات الثلاثة. الاسم وحده **غير حتميّ** عند
  تساوي اسمين، فترتيب الصفحة يتغيّر بين نداءين بلا سبب ظاهر.
- **وأسماء البذر صارت بأرقام لاتينية** («مزرعة الصعيد 2» لا «الثانية») — **كلمات الترتيب تُرتَّب
  أبجديًّا لا عدديًّا**، فتُنتج ترتيبًا صحيحًا يبدو خطأً.
  **والأسماء القائمة في قاعدة المالك لا تتغيّر** (البذر عطالته على مستوى المستأجر) — تظهر
  الأسماء الجديدة في بذر جديد.

**٣. اسم تبويب «المزارع والعنابر» يُقتطع.** شريط خماسي على عرض 390 يعطي كل تبويب ~78 بكسل،
و12 بكسل خطًّا (§8.9) لا تكفي لستة عشر حرفًا. **صار «المزارع»** — **والشاشة نفسها تسمّي المستوى
في ترويستها** (المواقع · المزارع · العنابر) فلا يضيع المعنى.
**ويمسّ تبويبات المالك وحدها** — لا تغيير في `tabBarOptions` المشتركة، فلا أثر على الأدوار الأخرى.

**169. الملفان المولَّدان في `apps/mobile` — التفريق بينهما هو الحل، لا معاملتهما ككتلة**
**دَين متكرّر منذ القرار #80: تشغيل Expo يترك الشجرة متّسخة في كل مرة**، فيختلط أثره بأثر العمل الحقيقي في `git status` وفي كل مراجعة.

**والملفان مختلفان اختلافًا يحسم المعاملة:**

| الملف | طبيعته | القرار |
| --- | --- | --- |
| `expo-env.d.ts` | **مولَّد بالكامل** — سطران وتعليق، ونصّه نفسه يقول `This file should not be edited and should be in your git ignore` | **يخرج من التتبّع** |
| `tsconfig.json` | **ليس مولَّدًا** — يحمل خيارات التشديد الخمسة (القرار #66) ومسار `@/*` | **يبقى متتبَّعًا بكل خياراته** |

**واستثناء الأول وحده لا يُنهي التكرار — والسبب مُقاس لا مُفترض.** `expo start --web` على `main` طبع `TypeScript: The tsconfig.json#include property has been updated` وترك ` D apps/mobile/expo-env.d.ts` و` M apps/mobile/tsconfig.json`. فالمُفسِد ليس وجود الملف بل **إشارة `include` إليه**: Expo يزامن `include` مع شكله القانوني في كل إقلاع، فما دامت الإشارة قائمة يُعاد الكتابة.

**وقياسٌ ثانٍ صحّح فهمًا أوسع من الأول: Expo يحذف `".expo/types/**/*.ts"` أيضًا لا `"expo-env.d.ts"` وحدها.** فحذف الإشارة الأولى وحدها **جُرّب وأُثبت أنه لا يكفي** — أُعيدت الكتابة كما هي بالضبط. والإشارة الثانية **كانت تطابق صفر ملفات**: `apps/mobile/.expo/` موجود **بلا مجلد `types`** إطلاقًا، و`pnpm --filter @dawajin/mobile run typecheck` يمرّ بعد حذفها.

**و«صفر ملفات» نتيجة لا سبب — والسبب أن `**/*.ts` لا يغطّي المجلدات المبدوءة بنقطة.** فالإشارة **ليست زائدة بل معطَّلة مؤقتًا**: `.expo/types/` لا يُولَّد ما دام `experiments.typedRoutes` غير مفعَّل في `app.json` (مُتحقَّق منه: غير موجود). **مُثبَت بمخالفة متعمَّدة لا باستنتاج**: زُرع خطأ نوع في `.expo/types/probe.ts` فمرّ typecheck بالصيغة الجديدة صامتًا، وبإعادة الإشارة رجع `error TS2322: Type 'string' is not assignable to type 'number'`.
**فمن يُفعّل `typedRoutes` لاحقًا يعيد الإشارة**، وإلا خرجت أنواع مسارات الراوتر من التغطية **بلا أن يحمرّ فحص واحد** — تدقيق أضعف لا بناء فاشل، وهو حرفيًا صنف العطب الذي وصفه القرار #161: **عطبٌ صامت لا يكشفه اختبار قائم.**

**والحل النهائي: مطابقة `include` وحده — ولا شيء غيره.** بعد `["**/*.ts", "**/*.tsx"]` طبع Expo مخرَجه **بلا سطر تعديل tsconfig**، والفرق في الملف **سطر واحد** هو تعديلنا نحن. **والتنسيق الأصلي محفوظ حرفًا بحرف**: السطران الفارغان و`"@/*": ["./*"]` مضغوطًا — Expo يعيد التنسيق **حين يكتب**، وهو لم يعد يكتب. **فلم يُلمس أي خيار مترجِم ولا مسار.**

**وثلاثة أجزاء لا جزء واحد**، وحذف أيٍّ منها يُبقي الدين: إخراج الملف من التتبّع · إضافته إلى `.gitignore` (وإلا عاد بعد أول إنشاء) · وحذف إشارتَي `include`.

**والإثبات شجرة نظيفة بعد تشغيل حقيقي لا اختفاء ملف.** `expo export` — وهو ما يشغّله `scripts/checks/layout-rtl.ts` — **لا يُحدث هذا الأثر إطلاقًا**، والمُحدِث `expo start` وحده. **فبوابة `check:all` لم تكن لتكشف الدين ولا لتثبت إغلاقه**، والقياس وقع على العملية التي تُفسد فعلًا. **وهذا نظير درس القرار #128 حرفيًّا:** أول محاولة إثبات هنا قتلت Expo قبل أن يقلع (حلقة انتظار بلا مهلة)، فأعطت شجرة نظيفة **تُقرأ نجاحًا وهي لا شيء** — **مخالفة لم تنطبق**.

**170. `CLAUDE.md` كان يوجّه ماكًّا إلى مسار لينكس — فشل بيئي يُقرأ فشلَ بوابة**
**عطبٌ في التوثيق لا في الكود، وقع فعلًا في هذه الجلسة قبل أن يُسجَّل.**

**ما وقع:** `pnpm check:all` على ماك نظيف أعطى فشلين — `✗ التغطية` و`✗ تأكيدات تخطيط RTL` — **وكلاهما بيئي بحت**. والقارئ الذي يفتح `CLAUDE.md` ليعرف السبب يجد سطرًا واحدًا يوجّهه إلى `/opt/pw-browsers/chromium-1194/chrome-linux/chrome`، **وهو مسار حاوية لينكس لا وجود له على أي ماك**. فيبقى الفشل بلا تفسير، **ويُقرأ على أنه فشل بوابة أو انحدار في الكود**.

**وهذا نظير القرار #74 حرفيًّا** — سُجِّل يومها أن بوابة التغطية «لا تعمل بصمت» لسبب بيئي، وعولج بتوثيق تصدير متغيّرات القاعدة. **والعلاج نفسه لم يكن كاملًا**: غطّى المتغيّرات وترك المتصفح وقاعدة الاختبار.

**والدَينان اثنان لا واحد:**

**١. مسار المتصفح يختلف بالمنصة، ورقمه يتغيّر بالنسخة.** المسار على macOS هو `~/Library/Caches/ms-playwright/chromium_headless_shell-<رقم>/chrome-headless-shell-mac-arm64/chrome-headless-shell` — بنية مختلفة تمامًا عن مسار الحاوية، لا مجرد جذر آخر. **والرقم يتبع نسخة Playwright** (كان `1208` مثبَّتًا و`1.62.1` تتوقّع `1234`)، **فالقاعدة «صدّر المسار الموجود عندك» لا رقمًا محفوظًا في وثيقة تبيت**. وأُضيف أمر قراءته من الجهاز نفسه بدل نسخه.
**ورسالة الفشل تسمّي النسخة المتوقَّعة لا الموجودة**، فمن يتبعها حرفيًّا يطارد رقمًا لن يجده — وهو ما يجعل التوثيق هنا **أنفع من الرسالة**.

**٢. قاعدة `dawajin_test` تحتاج تجهيزًا صريحًا بخطوتين، وغيابه يُخفي نفسه.** بلا `migrate` ثم `setup:test-db` بـ`DATABASE_URL` موجَّهًا إليها، يرفض `testGuard` التشغيل بـ«جدول العلامة `__test_marker__` غير موجود»، **فتسقط اختبارات التكامل فتفشل بوابة التغطية** — والعنوان الظاهر «التغطية دون الحد المطلوب» لا يقول شيئًا عن قاعدة بيانات. **ولولا إصلاح القرار #143** (عرض آخر 25 سطرًا كما قالتها الأداة) **لبقي السبب مجهولًا**؛ وهذا استعمال حيّ لذلك الإصلاح أثبت قيمته.
**وCI لا يقع فيه** لأنه ينشئ القاعدة من الصفر في خطوتين مسمّاتين في `ci.yml` — **فالفجوة بين CI والجهاز المحلي هي بالضبط ما لا توثّقه أي بوابة**.

**ولماذا توثيق لا بوابة جديدة:** كلاهما **حال جهاز لا حال مستودع** — لا يُفرضان بفحص يقرأ الشفرة، وأي بوابة تحاول ستفشل على أول بيئة مختلفة. **والبوابات موقوفة بقرار المالك أصلًا.**

**والقاعدة المستخلَصة:** أي أمر في `CLAUDE.md` يحمل **مسارًا مطلقًا خاصًّا ببيئة واحدة** دَينٌ مؤجَّل حتى يفتح المشروعَ أحدٌ على غيرها. **يُذكر المساران معًا، أو يُذكر كيف يُقرأ المسار لا ما هو.**

**171. المناطق الآمنة — عطبٌ يفرضه نظام التشغيل، ولا بوابة تراه**
**كشفه أول تشغيل على جوال المالك، بعد أن مرّ على 11 بوابة و129 اختبارًا و17 تأكيد تخطيط.**

**العرَض:** الساعة وأيقونات النظام تركب على ترويسة كل شاشة، وأزرار أندرويد تلامس شريط التبويبات.

**والسبب مفروض من نظام التشغيل لا من إعدادنا** — وهذا يحسم أنه لا يزول بتعديل في `app.json`:
- **Expo Go `54.0.8` يستهدف `targetSdk=36`، والجهاز `ro.build.version.sdk=36`.** وأندرويد 15 (API 35) **يفرض** edge-to-edge على كل تطبيق يستهدف 35 فأعلى، **وأُلغي الانسحاب منه في 36**.
- **و`app.json` خالٍ من أي مفتاح edge-to-edge** (قُرئ كاملًا)، والمصدر الحاكم في react-native خاصية Gradle (`EDGE_TO_EDGE_ENABLED` في `PropertyUtils.kt`) — **وهي لا تخصّنا أصلًا ما دمنا داخل Expo Go الذي لا نملك بيانه**. **فلن يزول ببناء خاص بنا ما دام يستهدف 35+.**

**والأرقام مقيسة على الجهاز لا مقدَّرة:**

| القياس | القيمة |
| --- | --- |
| شريط الحالة | `[0,0][1264,135]` — 135 بكسل (≈38.6dp عند كثافة 560) |
| شريط التنقّل | `[0,2592][1264,2728]` — 136 بكسل (≈38.9dp) |
| عنوان الترويسة | يبدأ عند `y=42` **داخل** شريط الحالة — **تداخل 93 بكسل** |
| تسميات التبويبات | `[…,2592][…,2592]` — **`h=0`**، مقصوصة بالكامل عند حدّ شريط التنقّل |

**والمدى كل شاشة في التطبيق، لا بعضها:** `headerShown: false` مضبوط **مرة واحدة على المكدّس الجذر** (`app/_layout.tsx`) فلا ترويسة أصلية تزيح شيئًا في أي مسار؛ و`AppHeader` تستهلكه خمس شاشات حقيقية و`design-system` **و`PlaceholderScreen` وحدها تغطّي ستّ عشرة شاشة نائبة** (#166). وشريط التبويبات متأثر معه لأن `tabBarStyle.height: 72` كان **ثابتًا بلا حشو سفلي**.

**ولماذا لم تكشفه بوابة ولا متصفح — وهو الأهم:** **لا شريط حالة في المتصفح ولا شريط تنقّل**، وتأكيدات التخطيط الـ17 تقيس **مخرَج `expo export --platform web`**. **فالبوابة كانت خضراء فوق عطب لا تراه** — صنف القرار #169 نفسه حرفيًّا، حيث `expo export` لا يُحدث الأثر الذي يُحدثه `expo start`.

**الإصلاح — المناطق الآمنة وحدها، والحزمة مثبَّتة أصلًا بلا مستهلك:** `react-native-safe-area-context` و`expo-status-bar` كانتا في `package.json` **بصفر استيراد في التطبيق كله** (مُتحقَّق منه بالبحث). أُضيف `SafeAreaProvider` على المكدّس الجذر · و`paddingTop: spacing.md + insets.top` في `AppHeader` (**يُضاف إلى الحشو الرأسي لا يستبدله**) · و`tabBarScreenOptions` صار **خطّافًا** `useTabBarScreenOptions()` يُرجع `height: 72 + insets.bottom` و`paddingBottom: insets.bottom` — **فيبقى الـ72 المنصوص عليه في §8.9 هو المساحة المرئية فعلًا لا الإجمالي**.

**وأثره على الاختبارات كان أوسع مما تُوقِّع — يُسجَّل لأنه الدرس:** `useSafeAreaInsets` **يرمي** بلا مزوّد (`No safe area value available`). توقَّعتُ سقوط `AppHeader.rtl.test.tsx` وحده، **فسقط ثلاثة**: هو، و`PlaceholderScreen.rtl.test.tsx`، و`screen-tests/owner/farms-houses.rtl.test.tsx` — **لأن الأخيرين يصيّران `AppHeader` ضمنًا**. أُضيف `renderWithSafeArea` في `test-utils/rtl.ts` بمقاييس **صفرية مقصودة**: تُبقي كل تأكيد قائم يقيس ما كان يقيسه بالضبط، فلا يتغيّر شيء بإضافة المزوّد. **ودفعة تكسر اختبارًا قائمًا ليست أصغر بل أنقص.**

**وتأكيدات التخطيط الـ17 لم تتأثر — شُغِّلت فعلًا لا استُنتِجت.** التعليل البنيوي (المخرَج ويب وحشوه صفر) صحيح، **لكنه لا يُقبل بلا تشغيل**: `17 passed (4.3s)`. وهذا نظير درس القرار #128 — إثبات الآلية ليس إثبات التغطية.

**وحصيلة تجربة الجوال نفسها — الشرط الخامس (#164) مستوفًى:**
**ما أُثبت على جهاز حقيقي:** زرّ الرجوع **العتادي** من شاشة داخلية — **بزرّ الهاتف لا بزرّ التطبيق، والاختبارات الأربعة القائمة تفحص زرّ التطبيق فلا يغنيه اختبار قائم** · وزرّ تسجيل الخروج على أندرويد · والخط العربي والاتجاه RTL · والعدّادات والشارات · والورقة الصاعدة · وتسجيل الدخول والتنقّل بين المستويات الثلاثة.
**وما بقي مفتوحًا:** الرجوع العتادي على شاشة المواقع نفسها، وعلى شاشة تغيير كلمة المرور حيث الاعتراض مقصود (`_layout.tsx` — `gestureEnabled: false`) **ويُختبر حين يوجد مستخدم بكلمة مرور مؤقتة** · **وعدم بقاء الجلسة بعد إغلاق التطبيق، وسببه غير محسوم** (يُشخَّص بسجلّ الخادم لحظة إعادة الفتح: وصل `/auth/me` ← الرمز قُرئ؛ لم يصل شيء ← لم يُقرأ أصلًا) · **وعطب رسم نصّ «عرض العنابر»** — مؤجَّل عمدًا لأن التخطيط كلّه كان مبنيًّا فوق قياس خاطئ، **فيُقاس بعد هذا الإصلاح لا قبله**.

**والغرض من الشرط الخامس تحقُّق لا نجاح:** أن يرى المالك بعينه على جهازه قبل أن يُبنى فوقه. **وقد رأى، فأثمرت الجلسة ثلاثة اكتشافات لم تكشفها بوابة ولا متصفح** — فالشرط مستوفًى، والمتبقّي لفتح المرحلة 3 **أربعة شروط برمجية خالصة: 25 · 29 · 32 · 28**.

**وعطب «عرض العنابر» قِيس ولم يُصلَح — والقياس نفى الفرضية:** الفرضية كانت قصًّا محتملًا، وشجرة العرض تقول غير ذلك: `'عرض العنابر' [905,1240][1149,1307] w=244 h=67` في البطاقة الأولى، و`[905,2164][1149,2231] w=244 h=67` في الثانية — **النصّ الكامل في كلتيهما بنفس العرض والارتفاع والمدى الأفقي**. **فلا تخطيط ولا بيانات: رسمُ حروف وحده**، ونحو 100 بكسل من أصل 244 هي المطلية في الثانية.

**والقياس بعد الإصلاح — على `main` بعد الدمج يُعاد، وهذه أرقام الفرع:**

| القياس | قبل | بعد | المطلوب |
| --- | --- | --- | --- |
| عنوان الترويسة | `[182,42][1068,149]` | `[182,177][1068,284]` | يبدأ عند `y ≥ 135` ✅ |
| تسميات التبويبات | `h=0` عند `y=2592` | `h=51` عند `y=2456..2507` | `h > 0` ✅ |
| حاوية شريط التبويبات | — | `[0,2340][1264,2728]` — **`h=388`** | `72dp×3.5 + 136 = 388` ✅ |

**ولم يتضاعف الحشو — وهو احتمال حذّرتُ منه قبل القياس ولم أستبعده بالترجيح:** React Navigation يضيف حشوًا سفليًّا من تلقائه متى وُجد المزوّد، فكان يمكن أن يجتمع مع حشونا. **المقيس 388 بكسل بدءًا من `y=2340`، والمتوقَّع 388 بدءًا من `2728−388=2340` — مطابقة بالبكسل، فلا ازدواج.**

**والحدّ الذي سُجِّل أولًا — اللقطة غير المتطابقة — أُغلق:** كان القياس الأول من شاشة المواقع واللقطة المصاحبة من شاشة الدخول (الجلسة لا تبقى، فعاد التطبيق إلى الدخول بينهما). **ثم عاين المالك سبع لقطات «بعد» وأقرّ الإصلاح بالعين في الطرفين**، **وأُعيد القياس على شاشة ثانية** (عنابر «الجاح السفلى») فطابق الأولى حرفيًّا: العنوان `[182,177][1082,284]` عند `y=177` · التسميات الخمس `h=51` لكلٍّ منها · والحاوية `[0,2340][1264,2728]` بارتفاع `388`. **فالأرقام ليست قياسًا واحدًا على شاشة واحدة بل متكرّرة على شاشتين، ومقرونة بمعاينة بشرية.**

**172. القياس يقع على الحالة النهائية — قياسان في نقطتين لا يصنعان تفسيرًا**
**قاعدة منهجية، لا ملاحظة. كلّفت ثلاث دورات في جلسة واحدة قبل أن تُكتب.**

> **قياسان صحيحان في نقطتين لا يصنعان تفسيرًا. القياس يقع على الحالة النهائية — القائمة عند نهايتها، لا وسط تمريرها — وإلا قِيس أثر التمرير لا أثر التخطيط.**

**والحادثة مزدوجة، ونفس الخطأ في الاتجاهين المتعاكسين:**
- **قِيس حدّان متطابقان** (`w=244 h=67` في بطاقتين) فاستُنتج «رسمُ حروف وحده» — والقياس وقتها جرى **قبل** إصلاح المناطق الآمنة، حين كان التخطيط كلّه مزاحًا 38.6dp، **فلم يبلغ التذييل حدّ الشريط أصلًا**.
- **ثم قِيس ارتفاعان مختلفان** (`h=41` عند الحافة و`h=67` في الوسط) فاستُنتج «حشو سفلي ناقص» — **وكان البند مُمرَّرًا جزئيًّا خارج المنفذ لا مقصوصًا**.

**والفاصل في الحالتين واحد: القائمة عند نهايتها.** بلغتها بأربع تمريرات ثم خامسة لم تُحرّكها، فكانت البطاقتان `h=67` تنتهيان عند `y=2224` — **فوق حدّ الشريط `2340` بـ116 بكسل**. فلا قصّ ولا حاجة إلى حشو.

**والمصدر كان يقول ذلك قبل أي استنتاج، ولم يُقرأ إلا بعده** (`@react-navigation/bottom-tabs` — `BottomTabBar`):
```
// Absolutely position the tab bar so that the content is below it
// This is needed to avoid gap at bottom when the tab bar is hidden
position: isTabBarHidden ? 'absolute' : undefined,
```
**فالشريط في التدفّق الطبيعي حين يكون ظاهرًا، ومنطقة المحتوى تنتهي فوقه — والمحتوى لا يمرّ تحته إطلاقًا.**

**والقاعدة المشتقّة أوسع من التمرير:** كل قياس يقع على حالة **قابلة للتغيّر بفعل المستخدم** (موضع تمرير · ورقة مفتوحة · لوحة مفاتيح · انتقال بين شاشتين) يجب أن يُؤخذ عند **الحالة المستقرّة**، وأن يُثبَت استقرارها بتكرار الفعل مرة أخرى بلا أثر. **وإلا فما قِيس هو الحالة العابرة لا البنية.**

**وهذا نظير درس القرار #128 بأداة ثالثة:** هناك «مخالفة لا تنطبق تُقرأ نجاحًا»، وفي #130 «فحص ينجح لسبب عرضي»، وهنا **«قياس صحيح في حالة عابرة يُقرأ بنيةً»**. الثلاثة تجعل النتيجة مستقلة عمّا تزعم قياسه.

**173. تسمية الإجراء تُقصّ صامتة — الصندوق يساوي ما قاسه المُقاس، والحبر يتجاوزه**
**عطبٌ رآه المالك بعينه على الجهاز، ومرّ على 11 بوابة و129 اختبارًا و17 تأكيد تخطيط.**

**العرَض:** تذييل بطاقة المزرعة يعرض «عرض» بلا «العنابر».

**والقياس نفى الفرضيات الثلاث الأولى (قصّ تخطيط · رسم حروف · حشو ناقص) قبل أن يصل إلى السبب:**

**١. القيد ليس من الأب — المتاح متطابق في البطاقتين:**

| | صندوق النصّ | أيقونة ⋮ | مدى الصفّ | **المتاح** |
| --- | --- | --- | --- | --- |
| بطاقة موقع | `[895..1149]` **254** | `[116..186]` 70 | `116..1149` = 1033 | **963** |
| بطاقة مزرعة | `[905..1149]` **244** | `[116..186]` 70 | `116..1149` = 1033 | **963** |

**963 بكسل متاحة في الاثنتين، والصندوق أخذ أقلّ من ربعها.** فالأب لم يقيّد شيئًا، **و244 ناتج قياس النصّ نفسه**.

**٢. والحساب المقارن يكشف الشذوذ:**

| | الحبر المطلي | الصندوق | الهامش |
| --- | --- | --- | --- |
| «عرض المزارع» | **248** | **254** | **+6** |
| «عرض العنابر» | أقلّ بـ**3** في نفس النمط | **244** | **سالب** |

قِيست الكلمتان في نفس البطاقة ونفس النمط (تسميتا المؤشّرين، صندوقاهما `w=336 h=47`): **«المزارع» 89 بكسل حبرًا، و«العنابر» 86**. فبنفس هامش الأولى كان الصندوق يجب أن يكون **≈251 لا 244** — **أضيق بسبعة بكسلات ممّا يتوقّعه القياس المقارن**.

**٣. وحين يعجز الصندوق عن استيعاب الحبر ولو ببكسل، ينكسر السطر عند المسافة** — مُثبَت بالبكسل: السطر الأول مقطع حبر واحد `[1047..1146]` = **100 بكسل** («عرض» وحدها)، **ويساره 142 بكسل فارغة**؛ والسطر الثاني عند `y=1437..1441` **خمسة صفوف مرئية قبل القصّ**.

**٤. والصندوق بارتفاع سطر واحد (`h=67`) يجعل الكسر قصًّا صامتًا لا التفافًا ظاهرًا** — وهو أخطر من الالتفاف المرئي لأنه لا يُبلِغ عن نفسه.

**وفرضية الخط سقطت بالقياس لا بالترجيح:** سجلّ إقلاع كامل (**7527 سطرًا**) بلا `Unrecognized font family` واحدة وبلا تحذير RN، والمطابقات الوحيدة سطر إقلاع الحزمة وتحذيرا `[RNScreens]` عن خاصيتَي عنوان. **فالقياس والرسم بخط واحد.** **وحدّها مذكور: غياب الإشارة دليل قوي لا برهان مطلق.**

**ولماذا لم تكشفه بوابة ولا اختبار — وهو الأهمّ:** تأكيدات التخطيط الـ17 تقيس **مخرَج الويب**، وjest يفحص **وجود النصّ في الشجرة لا عرضه المطلي** — **والشجرة كانت تحمل «عرض العنابر» كاملًا طوال الوقت** (`w=244 h=67`). **فالفجوة بين ما قاسه التخطيط وما طلاه الرسم لا تراها أي بوابة قائمة.** نظير القرار #169 (بوابة خضراء فوق عطب لا تراه) و#172 (قياس في حالة عابرة يُقرأ بنيةً).

**والإصلاح جذري لا موضعي — شقّان، وأحدهما وحده لا يكفي:**
- **`flex` على ضاغط الإجراء**: الصندوق يأخذ **المتاح** لا **المقاس**، فيسقط اعتماد التخطيط على دقّة القياس من أصله — 963 متاحة مقابل ~250 مطلوبة، والفائض يبتلع خطأ القياس. **مُثبَت على الجهاز: الصندوق صار `[186,785][1149,852]` — `w=963` بالضبط، بارتفاع سطر واحد.**
- **`numberOfLines={1}`**: وحده لا يكفي لأنه يحوّل عطبًا يقع بفارق بكسل واحد إلى «…» على تسمية إجراء، وهو ما ترفضه روح §8.3. **ومعًا: الفائض يمنع ظهور «…» أصلًا، وإن جاء فائض حقيقي يومًا ظهرت علامة بدل قصّ صامت.** **والصمت هو العدو لا القبح** — نفس درس #161 و#169 و#172.

**وأثر جانبي مُعلَن ومقبول:** `flex` يوسّع منطقة اللمس لتشمل عرض الصفّ — مكسب في تطبيق يُستعمل بإصبع تحت الشمس، وأيقونة ⋮ تبقى مستقلة.

**والعطبان مفصولان بالأرقام، ولا يُضمّان:** هذا **سوء قياس** (صندوق 244 لحبر 245+ ومتاح 963)، **والتفاف صفّ المؤشّرات `2+1` فائض حقيقي**: التسمية `w=336` والفجوة `56` (‏`spacing.lg` 16dp × 3.5)، فثلاثة مؤشّرات تطلب `336×3 + 56×2 = 1120` في متاح ≈`1034` — **فائض 86 بكسل**. **فالأول يُصلحه توسيع الصندوق، والثاني لا يُصلحه إلا إعادة توزيع (شبكة 2×2، §8.4) — ويبقى دفعةً مستقلة.** والرابط بينهما **عرَضي لا سببي**: كلاهما التفاف، وأحدهما ظاهر بـ`flexWrap` والآخر مقصوص بصمت.

**174. جهاز المالك يُقرأ ولا يُلمس — لا حدث إدخال إلى جهاز شخصي**
**قاعدة تسري من الآن، سببها حادثة وقعت.**

> **لا يُرسَل حدث إدخال إلى جهاز المالك الشخصي إلا بثلاثة شروط مجتمعة:**
> **١. التحقّق من الحزمة في المقدمة مباشرة قبله.**
> **٢. أن تكون الإحداثيات مقروءة من شجرة العرض في نفس القراءة، لا من قياس سابق.**
> **٣. إعادة التحقّق بعده، والتوقّف الفوري إن تغيّرت الحزمة.**
>
> **وأي نقرة على إحداثيات محفوظة من قياس أقدم ممنوعة مطلقًا.**

**والعلّة أن الخطر في اجتماع ثلاثة لا في النقر ذاته:** إحداثيات بائتة · بلا تحقّق · جهاز شخصي. **وبهذه الشروط تستحيل الحادثة**: شجرة تطبيق آخر **لا تحمل الهدف أصلًا**، فلا شيء يُنقر — فالحارس بنيوي لا انضباطي.

**والتحقّق أمرٌ مستقلّ تُقرأ نتيجته قبل الفعل — أُضيف بعد مخالفة وقعت:**

> **لا يُدمج التحقّق من الحزمة مع الفعل في أمر مركّب، ولا يُفترض نجاحه.**
> **وكل التقاط لشجرة عرض يسبقه تحقّق مقروء ويتبعه تحقّق ثانٍ، وبينهما لا شيء.**

**والعلّة أن الدمج يجعل التحقّق وصفًا لا حارسًا:** نُفِّذ `uiautomator dump` والتحقّقُ في أمر واحد، **فوقع الالتقاط قبل أن تُقرأ نتيجة التحقّق** — وكان في المقدمة تطبيق مراسلة شخصي لا تطبيقنا، فالتُقطت شجرته. **حُذفت من الجهاز والماك فورًا ولم يُقرأ منها حرف** (فحص الحزمة سبق فتح الملف)، وأُبلغ عنها في حينها.

**وهي نفس علّة الحادثة الأصلية أدناه**: الشرط الأول كان مكتوبًا ومقروءًا، **ونُفِّذ تنفيذًا لا يحكم الفعل**. فالشرط لا يكفي أن يوجد — يجب أن **يسبق** ويُقرأ.

**واستثناءان لا يسقطان بحال:** **إدخال كلمة المرور بيد المالك وحده**، **وأي شاشة تحمل بيانات شخصية تُغلق فورًا بلا قراءة**.

**واستثناء واحد صريح: النيّة (intent) الموجَّهة إلى حزمة مسمّاة بعنوان صريح مسموحة.** والعلّة تفصل الحالتين بوضوح: **الحادثة وقعت من نقرة عمياء على إحداثيات تصيب أيّ تطبيق يكون في المقدمة**؛ **والنيّة الصريحة تحمل الحزمة والعنوان معًا فلا تصيب غيرهما بأي حال**. فالممنوع ما يُصيب بالموضع، والمسموح ما يُصيب بالاسم.

**والقيد مقصور على الجهاز الشخصي** — يسقط تلقائيًّا على محاكٍ أو جهاز اختبار مخصَّص، فالعلّة ليست الأتمتة بل أن الجهاز يحمل رسائل صاحبه وحساباته.

**الحادثة:** أثناء قياس تذييل بطاقة، أُرسلت نقرة بإحداثيات تبويب مأخوذة من قياس أقدم. **وكان في المقدمة تطبيق مراسلة لا تطبيقنا**، فوقعت النقرة فيه، والتقطت اللقطةُ وشجرةُ العرض محتوًى خاصًّا بالمالك. **حُذف الملفان من الماك ومن الجهاز فور اكتشاف الخطأ، ولم يُعالَج شيء من محتواهما ولم يُعَد إنتاجه.** وأُثبت الحذف بالبحث في المستودع والمجلدات المؤقتة معًا.

**والسبب خطأ منهجي لا سهو تنفيذ، وهو نفس صنف القرار #172:** فعلٌ على **حالة عابرة افتُرض ثباتها**. الإحداثيات كانت صحيحة حين قِيست، والشاشة تبدّلت بعدها.

**ولماذا القاعدة منعٌ لا احتياط:** العلاج الإجرائي — «تحقّق من `mCurrentFocus` قبل كل نقرة» — **يُنسى، وفيه سباق زمني**: التطبيق قد يتبدّل بين التحقّق والنقرة، فلا يغلق الثغرة بل يضيّقها. **والمنع يغلقها بنيويًّا**، والقراءة وحدها لا تُحدث أثرًا مهما أخطأت الإحداثيات.

**وكلفتها مقبولة ومعلنة:** كل تنقّل يحتاج المالك، فتصير دورة القياس أبطأ. **وهذا ثمن أرخص من لمسة واحدة في تطبيق شخصي** — وقد وقعت مرة، وكان يمكن أن تكون رسالة تُرسَل أو إعدادًا يتغيّر لا لقطة تُحذف.

**175. أدوار الألوان — الترويسة هوية، وخلفية المشهد لم تكن ما ظنناه**
**كشفها نموذج المصمم. وسقط منها قراران حين فُحص ملف النموذج نفسه بدل لقطته.**

**١. الترويسة `brand.primary` بنصّ وأيقونات `text.onDark` — والنصّ كان صامتًا لا مخالفًا.**
§8.8 تصف **المواضع وحدها** — «أيقونة الحساب يمينًا · العنوان · الجرس يسارًا» — **بلا لون ولا خلفية**. و§7.1 تصف `brand.primary` لـ«الهوية · العناوين · رؤوس الشاشات»، **وهي تحتمل القراءتين**: لون نصّ الرأس أو لون خلفيته.
**والمرجّحان من داخل المشروع لا من النموذج:** القرار #80 ينصّ **«`brandPrimary` يبقى محجوزًا للهوية»** — والترويسة هي الهوية؛ و`text.onDark` **رمز عام في الجدول** مستعمل في **17 موضعًا**، فالسطح الداكن مفهوم قائم لا مستحدث. **ثم أكّده ملف النموذج نصًّا:** `background:#0C3A28 ; color:#FFFFFF`.

**٢. وأيقونات النظام تبعٌ لازم لا إضافة.** الترويسة تحمل المنطقة الآمنة (القرار #171) فتلوّن شريط الحالة معها. وأيقونات النظام يرسمها النظام: **داكنة على `#0C3A28` تعطي 1.28:1، وبيضاء 12.72:1**. **والضبط لكل سياق لا مرة واحدة على الجذر** — شاشات المصادقة الثلاث **بلا ترويسة** (القرار #93) وخلفيتها فاتحة **فتحتاج العكس**. وهذا ما كان يُنسى فيُكتشف على جهاز المالك لاحقًا.

**٣. وخلفية المشهد لم تكن `surface.page` — وهو أثمن ما خرج من الدفعة، ولم نقصده.**
قياس بكسل الفراغ بين البطاقات أعطى **`#F2F2F2`**، والمصدر:
```
@react-navigation/native/src/theming/DefaultTheme.tsx:8:    background: 'rgb(242, 242, 242)',
```
**`contentStyle` مضبوط على المكدّس الجذر ولا يصل مشاهد التبويبات، ولا `sceneStyle` مضبوط في أي مكان.** فأُضيف `sceneStyle` في `useTabBarScreenOptions` — **ملف واحد يعمّ التخطيطات الخمسة** لأنها تستهلك الخطّاف منذ القرار #171.
**والعطب سابق لهذه الدفعة وعمره من عمر التبويبات، وإنما أظهرَته.** ولم يُكتشف إلا **بقياس بكسل على جهاز** — ولا بوابة عندنا تقيس لونًا.

**والأرقام مقيسة من بكسل الجهاز لا محسوبة:**

| القيمة | المقيس | الرمز |
| --- | --- | --- |
| الترويسة | `#1A3929` | `brand.primary` |
| الصفحة | `#ECE7D8` | `surface.page` — **كان `#F2F2F2`** |

| العلاقة | النسبة |
| --- | --- |
| نصّ الترويسة الأبيض مع خلفيتها | **12.64:1** |
| سطر السياق (أبيض 72%) مع الترويسة | **8.91:1** |
| أيقونات النظام الداكنة لو بقيت | **1.35:1** — غير مقروءة |
| البطاقة البيضاء مع الصفحة **بعد** | **1.24:1** |
| البطاقة البيضاء مع `#F2F2F2` **قبل** | 1.12:1 |

**٤. ونتيجة تحسم سؤالًا كان مطروحًا خطأً: ضعف فصل البطاقة كان سببه الخلفية الرمادية لا لون الحدّ.**

إصلاح خلفية المشهد رفع الفصل من **1.12:1 إلى 1.24:1** بلا أن يُمسّ الحدّ إطلاقًا — الصفحة الكريمية أبعد عن الأبيض من رمادي React Navigation.

**ولولا هذا القياس لكان الحدّ المشتقّ قد عالج العرَض وأبقى العلّة**: كنّا سنحمل حدًّا عند شفافية 0.50 — **أثقل مما يلزم** — لنعوّض عن ضعفٍ مصدره خلفية خاطئة أصلًا. **وكان سيبدو ناجحًا**، لأن الرقم يرتفع فعلًا؛ والعلّة تبقى قائمة تحت العلاج.

> **عالج العلّة قبل أن تعوّض عنها.**

**والفصل بين الاثنين لا يظهر إلا بالقياس:** عرَضٌ يُعالَج فيتحسّن الرقم، وعلّةٌ تُترك فتظهر في موضع آخر لاحقًا. وهي القاعدة نفسها التي جعلت #133 يكلّف جولتين — عرَضٌ موجود نُسب إليه الفشل بلا إثبات يربطهما.

**وقراران سقطا — والسبب أهمّ من الحكمين:**

كان النطاق **ستة بنود**، فصار أربعة. سُحب: **تغيير خلفية البطاقة إلى `surface.raised`** · **حدّ مشتقّ `border.card` ورمز `cardBorderAlpha`** · **وتعديل نصّ §8.3**.

**والسبب أن الحكم بُني على لقطة شاشة للنموذج لا على ملفه.** في اللقطة قُرئت البطاقة البيضاء — وعليها ظلّ إضاءة — **كأنها كريمية**، فبُني عليها أن §8.3 تناقض §7.1. **وفحص الملف نفسه نقض ذلك**: البطاقة `background:#FFFFFF ; border:1px solid #E4DECB` في **43 موضعًا**، و`body{background:#EDE7D6}`، والكتل الداخلية `#EFEADA`. **أي أن البطاقة عندنا كانت مطابقة للنموذج أصلًا.**

**و§8.3 كانت صحيحة، والحكم بتناقضها خطأ:** «البطاقة الداخلية» في §7.1 تعني **الكتلة داخل البطاقة**، والنموذج يجعلها `#EFEADA` لا `#FFFFFF` — **وذلك سؤال منفصل لا يخصّ هذه الدفعة.**

**وهو صنف القرار #172 نفسه بأداة رابعة — دليل مأخوذ من تمثيل لا من الأصل**، ووقع **بعد إقرار #172 بساعات**. ومن هنا:

> **عند وجود ملف المصدر، لا يُحكم من صورة له.**

**وقاعدة ثانية من حادثة أخرى في الدفعة نفسها:**

> **لا يوصف رقم بأنه مقيس إلا وقد قِيس.**

قُدِّمت أرقام تباين للحدّ تحت عنوان «المقيس» **وكانت تقديرًا لم يُحسب** (`1.13` و`1.04` مقابل `1.19` و`1.09` الحقيقية)، ثم صُحِّحت طوعًا. **الفارق كان صغيرًا هذه المرّة، ولو كان كبيرًا لبُني قرار على رقم لم يوجد.**

**والثلاث من عائلة واحدة** — #128 و#130 و#172 وهذه: **نتيجة تُقرأ على غير ما تقوم عليه.**

**ودَينٌ معلوم يُسجَّل ولا يُعالَج: دور اللون غير محروس إطلاقًا.** **صفر مطابقة لون في تأكيدات التخطيط الـ17 وفي كل اختبارات jest** — فتغيير أي رمز لون **يمرّ بالبوابة خضراء ولا يكشفه شيء**. ولهذا لم يُكتشف عطب خلفية المشهد إلا بقياس بكسل على جهاز. **ولا بوابة جديدة (موقوفة بقرار المالك).**

**176. شريط توزيع الحالات — «ملخص حالة» لا ثلاثة عدّادات**
**تنفيذ لنصّ §5-د/2 بعد أن كشف نموذج المصمم أن المبنيّ يخالفه، وحسم المالك.**

**§5-د/2 بلفظه: «بطاقات مزارع الموقع بملخص حالة عنابرها».** والمبنيّ كان **ثلاثة `StatTile`** — لا ملخصًا ولا موافقًا لـ§8.4 التي تشترط «شبكة 2×2 أو صف من 3»: التسمية `w=336` والفجوة `56`، فثلاثتها تطلب **`336×3 + 56×2 = 1120`** بكسل في متاح ≈**`1034`** — **فائض 86 بكسل** يلتفّ إلى **`2+1`**، وهو ليس واحدًا من الشكلين.

**فالشريط يحلّ الاثنين معًا:** يعرض التوزيع في سطر واحد بلا عدّادات، **فيزول الفائض من أصله ويطابق النصّ**.

**ومؤشّرا بطاقة الموقع يبقيان كما هما** (المزارع · العنابر) — **عددان لا حالات**، والشريط يمثّل **توزيعًا**. توحيدهما يجعل **الشكل يقود المعنى، والصواب العكس**. وهما مؤشّران لا ثلاثة فلا التفاف أصلًا.

**والحالة الصفرية نصٌّ صريح لا شريط فارغ.** §11 تنصّ: «لا شرطة صامتة لمؤشر غير محسوب — يُعرض سبب غيابه». **وشريط رمادي فارغ شرطةٌ صامتة بشكل آخر**: يوحي بتوزيع معدوم لا بغياب عنابر. فيُعرض «لا عنابر في هذه المزرعة بعد»، **ولا يُخفى الموضع بلا بيان**.

**والاسم `StatusDistributionBar` لا `StatusBar`** — الأخير مستورد من `expo-status-bar` في `AppHeader` و`AuthScreen` (القرار #175)، **والتشابه يُنتج استيرادًا خاطئًا لا يمسكه المترجِم لأن كليهما مكوّن صالح**. رُصد التعارض قبل وقوعه لا بعده.

**ولا لون جديد ولا بيانات جديدة:** المقاطع من `status.info` و`accent.success` و`status.warning` القائمة، و`houseStatusCounts { occupied, ready, other }` مبنيّ ومُختبَر منذ القرار #131 — **فالخادم لم يُمَسّ بسطر**.

**وبرهان التناسب في اختبار لا على الجهاز — والسبب قيدٌ في البيانات لا اختيار:** كل مزارع البذر عنابرها **في حالة واحدة** (`0/2/0` و`0/3/0`)، **فلا مزرعة بحالات مختلطة يُقاس عليها عرض المقاطع**. **وشريط نسب يبدو صحيحًا ونسبه خاطئة عطبٌ صامت** — من صنف #169 و#172 — **فحُرس بقياس حتمي**: `flex` كل مقطع يساوي عدده.
**ومُثبَت بمخالفة متعمَّدة لا بالوصف:** تسوية المقاطع (`flex: 1`) أسقطت اختبارين بالأرقام (`Expected: 5 · Received: 1`)، وبالتراجع عادت **134/134**.

**وتوقّعٌ أُعلن ثم سقط، يُسجَّل كما وقع:** قيل سلفًا إن `farms-houses.rtl.test.tsx` **سيسقط حتمًا** لأنه يستعلم عن التسميات الثلاث. **ولم يسقط** — فحصُ الملف بيّن أنه لا يستعلم عنها أصلًا. **والإعلان المسبق كان صحيح المنهج خاطئ الواقع**، وتصحيحه بالتشغيل لا بالافتراض.

**177. استعادة الجلسة لم تكن تُرسَل أصلًا — والعلّة ترتيب `<Stack.Screen>` لا منطق الجلسة**

**العطب كما رآه المالك:** التطبيق لا يحتفظ بالجلسة. كل إقلاع ينتهي بشاشة الدخول ولو كان الرمز صالحًا.

**والمقيس — لا المُستنتَج — بعد إسقاط تسع فرضيات وتوصيفين:**

- **النداء من مؤثّر `app/index.tsx` لا يصل الخادم إطلاقًا**، والنداء من معالج حدث يصل (`boot-env=0` · `button-press=15`).
- **ثم أدقّ من ذلك**: جسم المؤثّر **لم يُنفَّذ أصلًا**. مصفوفة علامات على نطاق الوحدة بقيت **فارغة عند 3 و10 و60 ثانية**، ولم تُسجَّل حتى `A-before` وهي أول سطر داخل `try`.
- **والطلب لم يُرسَل، لا أنه أُرسل فبقي معلَّقًا**: مستمع Node عارٍ على منفذ مستقل **يسجّل عند الاستلام لا عند الاكتمال** لم يرَ من التطبيق شيئًا في ثلاث جولات، بينما سجّل **كل** طلب ضابط من الجوال نفسه.
- **ثم الحاسم**: مسبار في **طور طلاء** `app/index.tsx` صمت في **ست جولات متتالية**، بينما علامة مرئية من **نفس البناء** ظاهرة على الشاشة. **فالمكوّن لا يُطلى، والكود يعمل.**

**والعلّة المُثبَتة: المُنقِّل يبدأ على `routeNames[0]`، وترتيب `<Stack.Screen>` هو ما يحدّده.**

قياس من داخل `login.tsx` — الشاشة الوحيدة التي يقين أنها تُطلى — عبر `useNavigation().getState()`:

```
routeNames = auth/login, auth/select-account, auth/password, auth/change-password,
             design-system, (farmer), (supervisor), (vet), (owner), platform, index
index = 0 · active = auth/login · type = stack
```

**و`index` آخر القائمة لأنه لم يكن معلَنًا إطلاقًا.** المصدر في الحزمة المثبَّتة، `expo-router/build/useScreens.js:54`:

```js
function getSortedChildren(children, order = [], initialRouteName) {
    if (!order?.length) {
        return children.sort(sortRoutesWithInitial(initialRouteName))...
```

**فمتى كان `order` غير فارغ — أي متى أُعلنت الشاشات صراحةً — يُبنى الترتيب من الإعلانات، ويُلحَق غير المعلَن في الذيل، و`initialRouteName` لا يُستعمل إطلاقًا.** ونحن نعلن عشر شاشات أوّلها `auth/login`، فصار `routeNames[0] = auth/login`، **فبدأ المُنقِّل على شاشة الدخول رغم أن الرابط جذري** (`Linking.getInitialURL()` = `exp://localhost:8081` بلا مسار، مقيس). فلم يُركَّب `app/index.tsx`، فلم يقع توجيهه.

**والعلاج سطر واحد: `<Stack.Screen name="index" />` أوّل معلَن.** وبه وصل مسبار الطلاء أول مرة بعد ست جولات صمت، وظهرت الشاشة الرئيسة.

**و`unstable_settings.initialRouteName` حُذف بدل أن يُترك:** جُرّب فلم يغيّر شيئًا — وهو متجاهَل حتمًا بحكم السطر أعلاه ما دامت الشاشات معلَنة. **وسطرٌ لا أثر له ادّعاءٌ كاذب في الكود**، يُوثَّق سببه هنا ولا يبقى في الملف يوهم قارئه أنه يحكم شيئًا.

**وأربع قواعد تلزم بعد هذا:**

**١. تخطيط الجذر يصيّر مُنقِّلًا من أول طلاء ولا يعيد `null` أبدًا.** بوّابة الانتظار **طبقة فوق `Stack`** لا بديلًا عنه — تراكب معتم بلون `surfacePage` يحجب ما تحته حجبًا تامًّا. والنتيجة المطلوبة قائمة: لا تُعرض أي شاشة — ولا الدخول — قبل استقرار الاستعادة.

**٢. التوجيه عند الإقلاع إعلاني لا أمري.** `router.replace` من داخل تخطيط الجذر يسقط بـ`Attempted to navigate before mounting the Root Layout component` — نصٌّ حرفيّ من `expo-router/build/global-state/routing.js:62`، ظهر شاشةَ خطأ على الجهاز لا في سجل فقط. والبديل الذي تسمّيه الرسالة نفسها هو مُنقِّل مركَّب من أول طلاء وتوجيه في المسار — وهو `<Redirect>` في `app/index.tsx`.

**٣. منطق الاستعادة على نطاق الوحدة لا في مؤثّر شاشة** (`lib/sessionRestore.ts`): يبدأ عند الاستيراد من الجذر فينجو من تفكيك أي مكوّن، بمهلة صريحة 8 ثوانٍ عبر `AbortController` وإعادة واحدة. و`app/index.tsx` **مستهلك رفيع** بلا منطق شبكة.

**٤. فشل الشبكة لا يُنهي الجلسة.** الخادم وحده يقرّر أن الرمز لم يعد صالحًا: **401 أو حساب عُطِّل ← يُمحى الرمز**؛ أما انقطاع الشبكة أو المهلة أو خطأ خادم (5xx) **فلا يمسّ الرمز** — مربٍّ في عنبر ضعيف التغطية يجب ألّا يُطرد إلى شاشة دخول لا يحمل كلمة مرورها. تسع حالات في `lib/sessionRestore.rtl.test.tsx` تحرس التمييز.

**وعطبٌ ثانٍ كشفه الجهاز أثناء العلاج — `304` يُقرأ «تعذّر اتصال»:** `GET /api/auth/me` عاد **`304` بلا جسم** ست مرات، فتعذّر بناء المستخدم **وقُرئت جلسة صالحة تعذُّرَ اتصال**. **والسبب `etag` الافتراضي في Express** — لا إعداد له في المستودع كله، وردٌّ حقيقي يحمل `ETag: W/"84-..."` **وبلا `Cache-Control`**. **وعولج من سببه في الخادم لا بالتعويض عنه على العميل** (القرار #175): `noStore` على **`/api/auth` وحدها** — لا سياسة تخزين عامة للـAPI — تحذف `if-none-match` من الطلب وتضع `Cache-Control: no-store`. **وحذف الترويسة لازم**: `no-store` يمنع التخزين القادم ولا يُبطل نسخة مخزَّنة قبل النشر. ومُتحقَّق من نطاقه في الاتجاهين: مسار المصادقة يحمل `no-store`، و`/health` يبقى بـ`ETag` كما كان.

**وأداةُ قياس سقطت وتُسجَّل كي لا تُعاد:** **`adb reverse` نفق USB، فوضع الطيران لا يقطعه.** فُعِّل الطيران ووصل الخادمَ عشرة طلبات أثناءه. **فالأداة الصحيحة لقياس انقطاع الشبكة في هذه البيئة إطفاء الخادم نفسه** — وبه ثبت السلوك: الشاشة عرضت «تعذّر الاتصال بالخادم — تحقّق من الشبكة ثم أعد المحاولة» لا شاشة الدخول، ثم أعاد الضغطُ وحده الجلسةَ إلى الشاشة الرئيسة **بلا كلمة مرور**. **ونطاق هذا القياس شكل واحد من الفشل** (`connection refused`، أي `status === null` فورًا) **لا المهلة الطويلة** — تلك يغطّيها الاختبار الآلي.

**178. شبكة العنابر — محور الإنتاج، ولون يُقاس لا يُختار بالعين**

**الانحراف:** `docs/app-complete-spec.md` §5-د/2 ينصّ على «**شبكة عنابر ملوّنة بالحالة**»، والمبنيّ قائمة رأسية من بطاقات كاملة العرض.

**والمالك سحب قاعدتين كان قد فرضهما، ويُسجَّل السحب لا القاعدة:**

**١. «الحالة تُلوّن حافةً لا خلفية» — مسحوبة.** §8.3 يقصر نمط الحافة على **القوائم** ويعلّله بقائمة من 20 بطاقة. **والشبكة ليست قائمة**، فالمربّع الممتلئ مسموح بالمواصفة.

**٢. «لا اختصار لنصّ الحالة» — مسحوبة، والمقصود كان الاقتطاع الأعمى.** **تسمية عرض قصيرة مقرَّرة اختيارٌ لا قصّ**: لا حرف يُقصّ ولا «…» تظهر.

**٣. وبقي اعتراض ثالث قائمًا: الدليل لا يُغني عن النص.** دليلٌ بمربّعات ملوّنة **يطلب تمييز الألوان** — وهو ما يعجز عنه المصاب بعمى الألوان. فـ§11 قائم كما هو: كل مربّع يحمل **لونه وأيقونته وتسميته نصًّا** داخله.

**والمحور تغيّر من الإنذار إلى الإنتاج (قرار مالك):**

| الحالة | الفئة | اللون |
| --- | --- | --- |
| مشغول | `producing` | `#1E7A4F` |
| تحت الإخلاء · تحت التنظيف والتطهير | `preparing` | `#8A5A0F` |
| في فترة الراحة · جاهز للإسكان | `idle` | `#2F6B8F` |
| تحت الصيانة · معطّل | `outOfService` | `#5A5A5A` |

**والنغمات أُعيدت تسميتها** من `success/warning/critical/info` إلى `producing/preparing/idle/outOfService`: لم تعد نغمات شارة بل **مواقع من دورة الإنتاج**، والاسم القديم كان يصف خطرًا لا حالة تشغيل.

**والأحمر `#C0392B` خرج من الشبكة عمدًا** — يُحجز لما يستدعي تدخّلًا فعليًّا، **لا لعنبر خارج الإنتاج مؤقتًا**. ويحرسه اختبار: لا فئة في الشبكة تعني خطرًا.

**والسبع تبقى سبعًا** — لا تجميع في أربع مجموعات كما في النموذج: التجميع يُخفي تمييزًا يملكه النظام.

**وتصحيح في المعيار سبق أن أخطأتُ فيه:** عتبة «النص العريض» في WCAG 1.4.3 هي **14 نقطة = 18.66px**، لا 14px. ونصّ المربّع 15px وزن 700 — **نصٌّ عادي بحكم المعيار وحدّه 4.5**، وبلوغ 18.66px في عمود 114px غير معقول.

**والقياسات الخام للنصّ الأبيض على ألوان الحالة السبع:**

| اللون | التباين | الحكم |
| --- | --- | --- |
| `#2F6B8F` | 5.80 | يمرّ |
| `#1E7A4F` | 5.31 | يمرّ |
| `#C0392B` | 5.44 | يمرّ |
| `#B37714` | **3.77** | **يسقط** |

**فاشتُقّ لونان جديدان، والاختيار بينهما وبين إخوتهما كان على وحدة النطاق لا أقصى تباين:**

- **`#8A5A0F` (5.92)** بدل `#B37714` الساقط. والمرشّحون كانوا `#8A5A0F` 5.92 · `#7A4E0C` 7.19 · `#6B4309` 8.63 — **واختير الأول لأن ألوان الشبكة تقع في 5.31–5.92 فيجلس في نطاقها**، بينما 8.63 يبدو أثقل بوضوح من إخوته **فيكسر وحدة الشبكة**.
- **`#5A5A5A` (6.90)** رمادي «خارج الخدمة». والمرشّحون `#5A5A5A` 6.90 · `#4F4F4F` 8.19 · `#454545` 9.59 — **ونفس المعيار**. **ولا يُستعمل `text.body` (`#4A4A4A`) حشوةً**: رمز نصّ يُستعمل خلفيةً **خلط أدوار**، وهو بعينه ما منعه القرار #175.

**و`#8A5A0F` و`#5A5A5A` تعبئةٌ للشبكة لا بديل عن الرموز**: `status.warning` يبقى `#B37714` كما هو لأنه يُستعمل في شارات على **خلفية بيضاء** لا يمسّها هذا القياس.

**وثلاث من السبع تشترك في `preparing`، واثنتان في `idle`** — **فاللون يجمّع والتسمية تميّز**، وهذا سند إضافي لرفض الدليل بديلًا عن النص.

**والتسميات القصيرة السبع** (جدول ثالث بنفس مفاتيح الأولين، والتسمية الكاملة تبقى في القوائم والتفاصيل والشارات): مشغول · إخلاء · تنظيف · راحة · جاهز · صيانة · معطّل، وغير المعروفة «غير معروفة».

**وحارس آلي يمنع النكسة**: كل لون تعبئة يبلغ 4.5 مع الأبيض، **والحارس مُثبَت بمخالفة متعمَّدة** — يفحص أن `#B37714` يسقط فعلًا، وإلا كان حارسًا بلا أسنان (درس القرار #111).

**وتكرار الاسم عولج في بيانات العرض لا في الواجهة:** `fixtures.ts` كان يولّد «الجاح العليا — عنبر 1»، **فينكسر الاسم سطرين ويبتلع المربّع**. **ولا يُعالَج اسم المستخدم في العرض إطلاقًا** — يُعرض كما أدخله، ومعالجته تلاعب ببياناته. **والبذر عطالته على مستوى المستأجر** (#168) فلم تتغيّر الأسماء القائمة؛ أُعيدت تسمية 35 عنبرًا عبر `PATCH /houses/:id` — **لا SQL مباشر** (#27).

**وحدّ التحقق البصري — يُسجَّل صراحةً:** **كل عنابر بيانات العرض «جاهز للإسكان»**، فلم يُرَ على الجهاز إلا `idle` الأزرق. **والأخضر والأصفر والرمادي لم يرَها المالك قط** — مُتحقَّق منها بالقياس والاختبار لا بالعين. **ولم تُغيَّر حالات العنابر لأجل الرؤية**: آلة الحالات من المرحلة 3. §7-ب البند 36.

**والبنية: `LevelList` اكتسب خيار `layout` وحده** — الحالات الأربع تبقى فيه مشتركة بين المستويات الثلاثة كما كانت، **ويتغيّر الترتيب لا غير**، و`HousesLevel` وحده يمرّر `grid`.

**179. لا تبديل فروع والخادم وMetro يعملان على جهاز المالك**

**قاعدة تسري من الآن، سببها حادثة وقعت.**

> **لا يُنشأ فرع جديد ولا يُنتقل بين الفروع بينما الخادم وMetro يعملان على جهاز المالك.**
> **أوقف الخادم وMetro أولًا، أو أنجز العمل الآخر في `git worktree` منفصل.**
> **وفي كل انتقال بين فرعين والمالك ينظر إلى جواله — يُخبَر صراحةً.**

**الحادثة:** بعد دفع فرع شبكة العنابر، أُنشئ فرع وثائق وانتُقل إليه للحفظ عليه. وMetro كان لا يزال يخدم جهاز المالك، **فصار يخدم شجرة العمل الجديدة — أي كودًا بلا الشبكة**. فرأى المالك شاشته **عادت إلى التصميم القديم**.

**والعلّة أن العرَض يُقرأ على غير سببه:** انتكاسةٌ ظاهرة في العمل، بينما الكود المدفوع سليم (`e411bfb` على `origin`). **فالخطأ لم يكلّف كودًا بل ثقةً في نتيجة مرئية** — وهو أثقل من خطأ يظهر في سجلّ.

**ولماذا `worktree` لا `stash`:** القرار #141 يمنع `git stash` مطلقًا، **و`git worktree add` هو البديل المعتمد عندنا أصلًا** لأي مقارنة بشجرة أخرى. وميزته هنا حاسمة: **شجرة العمل التي يخدمها Metro لا تتغيّر إطلاقًا**، فلا يرى المالك شيئًا.

**وهي من عائلة القرار #172 و#174:** فعلٌ على **حالة عابرة افتُرض ثباتها** — الفرع تبدّل تحت خادم يعمل، كما تبدّلت الشاشة تحت إحداثيات محفوظة.
**180. التنقّل بالبطاقة، وحدّ تأكيدات التخطيط**

**١. التنقّل بالبطاقة لا بزرّ داخلها.** كانت بطاقة الموقع تحمل «عرض المزارع» وبطاقة المزرعة «عرض العنابر» — **زرٌّ داخل بطاقة كلّها هدف الضغط**. فصارت البطاقة نفسها هي الإجراء: `onPress` عليها بـ`accessibilityRole="button"`.

**و⋮ لا يُطلق التنقّل.** والإيقاف **صريح** (`event?.stopPropagation()`) لا اتّكالًا على أن RN يمنح الاستجابة للأعمق. **والاختبار مبنيّ ليفشل لو انعكس السلوك**: يؤكّد أن `onPress` **لم يُنادَ ولا مرة** عند ضغط ⋮ — لا يكتفي بأن `onMorePress` نودي؛ فلو حُذف معالج ⋮ لانتشر الضغط إلى البطاقة وسقط الاختبار.

**و⋮ انتقل من التذييل إلى صفّ العنوان بلا خيار `morePlacement`.** خيارٌ يعني تخطيطين يُصانان ويُختبران للأبد لفارق لا يحتاجه أحد. **ومستهلكوه ثلاثة لا رابع** — بطاقتا الموقع والمزرعة، وشاشة عرض النظام بـ`noop` — **ولا واحد منها يعتمد موضع التذييل**، فالنقل بلا خيار.

**٢. تنافي الإجراءين بالنوع لا بالتعليق.** «بطاقة واحدة = إجراء أساسي واحد» كانت **وصيةً في تعليق تُقرأ ولا تُفرض**. فصارت **اتحادًا مميَّزًا** يرفضه المترجم إن مُرِّر `onPress` و`primaryActionLabel` معًا:

```ts
type CardActionProps =
  | { onPress?: undefined; primaryActionLabel?: undefined; onPrimaryAction?: undefined }
  | { onPress: () => void; primaryActionLabel?: undefined; onPrimaryAction?: undefined }
  | { onPress?: undefined; primaryActionLabel: string; onPrimaryAction: () => void };
```

**و`primaryActionLabel` تبقى في المكوّن** — `auth/select-account.tsx` يعتمدها («متابعة بهذا الحساب»)، وحذفها يكسر شاشة الدخول.

**٣. زرّ الإضافة — قيمه من ملف النموذج حرفيًّا** (`docs/design-reference-prototype.md` §4): عرض كامل · ارتفاع 56 · حد **متقطّع** `1.5px` بلون التأكيد · نصف قطر 16 · خلفية بيضاء · نص 15/700 · فجوة 8 · حشو 12/16 · محاذاة مركزية · أيقونة `+` بحجم 24 **لا تنكمش**. وكلها من مقاييس `tokens.json` لا قيمًا حرّة.

**والنصّ يسمّي الوجهة لا الفعل وحده**: «إضافة مزرعة جديدة» · «إضافة عنبر إلى ‹اسم المزرعة›». الزرّ يقع في شاشة ثلاثية المستويات، **و«إضافة» وحدها لا تقول إلى أين**.

**وقياس أطول نصّ ممكن قبل أي كود**، لأن هذا الزرّ بعينه رُصد فيه نقص على الجهاز (§7-ب البند 35): أطول اسم مزرعة في بيانات العرض «مزرعة الخماسية 2»، فالنصّ «إضافة عنبر إلى مزرعة الخماسية 2» — **مطلوب 356 = متاح 356 على عرض 390**، بلا فيض ولا التفاف ولا اقتطاع.

**٤. عطب المحاذاة وسببه.** ⋮ ظهر على الجهاز **أسفل الاسم بنحو 26px** ملتصقًا بالفاصل، وفي بطاقة المزرعة عند مستوى العنوان الفرعي، **وسطرٌ كامل يشغله وحده فتطول البطاقة بلا معلومة**.

**والسبب مقروء في الكود لا مخمَّن:** `headerRow` كان `alignItems: "baseline"` — منقولًا عن النموذج، **والنموذج يضع في ذلك الصفّ نصّين** بينما نحن نضع **نصًّا و`<View>`**. **و`<View>` لا يملك خطّ قاعدة نصّي، فتأخذ Yoga حافته السفلى قاعدةً له** — فيهبط الصندوق كله حتى تلامس حافته السفلى قاعدة العنوان. **والعلاج `center`.**

**٥. وحدّ عامّ يسري على كل تأكيداتنا — وهو أهمّ ما في هذا القرار:**

> **تأكيدات التخطيط تقيس `react-native-web` لا Yoga.** فكل فرق بين النظامين — والمحاذاة أوّلها — **يمرّ خضراء**. وهي حارس للثوابت المشتركة **لا برهان على سلوك الجهاز**. **والبرهان الوحيد على السلوك البصري في أندرويد عينٌ على جهاز فعلي.**

**ومُثبَت بهذه الحادثة نفسها:** أُضيف تأكيد محاذاة يقيس الموضع الرأسي (مركز ⋮ داخل ارتفاع سطر العنوان)، **وشُغِّل قبل الإصلاح فمرّ** — `سطر العنوان 159..183 · مركز ⋮ 164`. **فهو حارس للثابت على الويب، ولا أسنان له ضدّ هذا العطب بعينه**؛ لأن `react-native-web` يحسب خطّ القاعدة بقواعد CSS لا بـYoga فيُحاذي المركز صدفةً. يبقى التأكيد لأنه يحرس ثابتًا حقيقيًّا، **ولا يُقرأ برهانًا**.

**181. تسميات الشريط السفلي — وأول قياس على Yoga**

**العطب:** التسميات الخمس مقتطعة بعلامة قصّ على جهاز المالك — «الرئي…» · «المز…» · «المستخ…» · «التقا…» · «الإعدا…» — **تتكرّر في كل لقطة وتظهر في كل شاشة**، ولم تكشفها بوابة.

**١. العلاج ثلاثة أشقّ:**

- **إزالة الحشو الأفقي** (`tabBarItemStyle: { paddingHorizontal: 0 }`): الافتراض في `@react-navigation/bottom-tabs` هو `padding: 5` جهةً فيأكل 10 من عرض التبويب.
- **ومكوّن تسمية عندنا** (`tabLabel`) يستبدل مكوّن المكتبة. والسبب مقروء في المصدر: `@react-navigation/elements` **يفرض `numberOfLines: 1` داخل `Label`**، **ولا يبلغه `tabBarLabelStyle`** لأنه يمرّر **أنماطًا لا خصائص**.
- **والوزن 700 للنشط** (§8.9) — **لم يكن مطبَّقًا أصلًا**: كل التسميات كانت `familyRegular`.

**٢. `numberOfLines={1}` لا `2` — وقاعدة تُسجَّل بثمنها:**

جُرّب سطران فكانت النتيجة **أسوأ من العطب الأصلي**: زالت «…» وصار النصّ **مبتورًا في منتصف الكلمة** وسطره الثاني مقصوصًا بحافة الشريط — «المزار» · «المستخدم» · «التقارِ» · «الإعدادا» · «الرئيس». **نصٌّ مبتور بلا علامة تقول إنه مبتور.**

> **لا تُمنح سطورًا لا يسعها الارتفاع.** الإذن بسطرين في صندوق لا يسعهما **يستبدل الاقتطاع المهذّب ببترٍ صامت** — والصمت هو العدو لا القبح (نفس درس #161 و#169 و#173).

**والقياس من الجهاز يثبته:** ارتفاع عقدة النصّ 62px (17.7dp) للتسميات التي التفّت، ولا يتّسع لسطرَي 12px بارتفاع سطر 1.7.

**وقياسي على الويب «السطران 28 داخل 71 بفسحة 19» سقط على الجهاز** — سطر الويب 14px وسطر الجهاز ~20.7px. **الدليل السادس على القرار رقم 180.**

**٣. قياسان من الجهاز، وفرضيتان سقطتا:**

```
wm size = 1264 x 2728  ·  density = 560  ·  font_scale = 0.85
عرض الشاشة = 1264 ÷ 3.5 = 361.1dp
```

- **«الشاشة 360dp»** — بل 361.1، والمتاح بعد الحشو 62.2، **و«المستخدمون» تحتاج 57 عند 0.85 فتسع**.
- **«مقياس الخط أكبر من 1»** — بل **0.85**، أي **يصغّر** النص فيزيد الفسحة بدل أن يأكلها.

**فكلتاهما ساقطة، ولا تُعادان.**

**٤. وأول قياس على Yoga في هذا المشروع كلّه** — `uiautomator dump` من الجهاز نفسه، لا `react-native-web`:

```
عنصر التبويب   View       253px = 72.3dp
  └─ النصّ      TextView   103px = 29.4dp   «المزارع»
```

**أبٌ واحد مباشر، بلا عقدة وسيطة تضيق** — فلا شيء يأكل العرض، والنصّ يسع مساحته بأربعين بالمئة فراغًا. **والشجرة تحمل النصّ كاملًا** («المستخدمون» بحروفها لا «المستخ…») — **فالبتر في الطلاء لا في النصّ ولا في التخطيط**، نظير القرار #173: «الصندوق يساوي ما قاسه المُقاس، والحبر يتجاوزه».

**وتُسجَّل أداةً لا حادثة**: هي ما ينقصنا منذ ستة أعطاب لم تكشفها بوابة. §7-ب البند 39.

**٥. وسبب الاقتطاع الأصلي غير مُثبَت — يُكتب كما هو لا كأنه مفهوم:**

> **العلاج نجح ولا يعتمد على تفسيره.** «المزارع» تحتاج 29.4dp من 72.3 متاحة — **عرضٌ كافٍ بضِعفين ونصف** — ومع ذلك كانت تُقتطع تحت `numberOfLines: 1` المفروضة من المكتبة. **ولم يُقَس سبب ذلك.** أُثبت أن إزالة تلك الآلة يُنهي القصّ، **ولم يُثبت لماذا قصّت أصلًا** نصًّا يسع مساحته.

**وخطأ ترتيب في القياس يُسجَّل:** التُقطت سلسلة الآباء **بعد** تطبيق `numberOfLines={1}` لا قبله، فالرقم 103px يخصّ سطرًا واحدًا لا السطرين المسؤولين عن العطب. **نُبِّه إليه قبل البناء عليه**، والسلسلة نفسها لا تتغيّر بعدد الأسطر.

**وحادثة التقاط تُسجَّل بضوابطها:** أول محاولة `uiautomator dump` وقعت وتطبيق مراسلة شخصي في المقدمة، **فالتُقطت شجرته**. حُذفت من الجهاز والماك فورًا **ولم يُقرأ منها حرف** (التحقّق من الحزمة سبق فتح الملف). **والعلّة أن التحقّق والالتقاط نُفِّذا في أمر واحد** — فلم يحكم التحقّقُ الفعلَ بل رافقه. **والتصحيح: قراءة الحزمة أمرًا مستقلًّا، ولا التقاط إلا بعد رؤية نتيجتها** (القرار #174).

**182. تأكيدات التخطيط تعمل على عرض جهاز المالك، لا على عرض افتراضي وحده**

**العطب المسجَّل (§7-ب البند 38):** كل تأكيدات التخطيط كانت تعمل على `viewport: 390×844`، **وجهاز المالك 361.1dp** (1264px ÷ كثافة 560/160). فارق **29dp أي 7.4%** من العرض — **فكل قياس سابق قِيس على شاشة لا وجود لها عنده**.

**ما فُعل:** مشروعان في `playwright.config.ts` **وحده**:

| المشروع | المقاس | المعنى |
| --- | --- | --- |
| `device-361` | 361 × 779 | جهاز المالك (2728 ÷ 3.5 = 779.4) |
| `baseline-390` | 390 × 844 | الأساس القائم، يبقى كما هو |

**والمشتركات تبقى في `use`** فلا تتباعد نسختان منها: `baseURL` · `locale: "ar"` · مسار المتصفح · وإعدادات `devices`. **ولم يتغيّر منطق ملف تأكيد واحد**، ولا `webServer`، ولا `workers: 1`.

**وأُعيدت تسمية عنوانين وحدهما** كانا يثبّتان عرضًا في اسمهما («ثلاثة أعمدة على عرض 390» · «نصّ زرّ الإضافة … على 390») — **فاسم المشروع يظهر في تقرير Playwright بجانب كل تأكيد**، والعرض لا يُكرَّر في العنوان. **العناوين فقط، بلا مساس بـ`expect` ولا بحدٍّ رقمي داخل أي اختبار.**

**ما تُثبته النتيجة — وهذا كل ما تُثبته:** **الـ56 كلها مرّت** (28 × 2)، **فلا أحد من التأكيدات الـ28 القائمة ينكسر على عرض جهاز المالك**. **ولم يُضعَّف تأكيد ولا أُرخي حدّ ولا استُثني مشروع** — لم تدعُ الحاجة.

**وحدٌّ معلَن لا يُطوى:** **`device-361` يحاكي العرض وحده.** مقياس الخط على جهاز المالك **0.85**، **و`react-native-web` لا يطبّقه، ولم تُجرَّب أي مقاربة له** — أي تقريب يصنع رقمًا كاذبًا، **وذلك أسوأ من غيابه**. **فالمشروع يُضيّق فجوة القرار رقم 180 ولا يغلقها: البرهان الوحيد على السلوك البصري في أندرويد يبقى عينًا على جهاز فعلي.**

**والكلفة تُذكر لا تُخفى:** 8 ثوانٍ و28 تأكيدًا ← **15 ثانية و56 تأكيدًا**. والسبب `workers: 1` — **تسلسل مقصود لم يُغيَّر** (التوازي لا يشتري شيئًا في تأكيدات تخطيط صفحة واحدة، ويجعل فشلًا نادرًا أصعب في إعادة إنتاجه).

**وملاحظة تُسجَّل ولم تُعالَج:** وصف «تخطيط شاشات المصادقة على شاشة صغيرة» يضبط مقاسه بنفسه (`test.use({ viewport: 360×640 })`)، **فتأكيداته الثلاثة تعمل بنفس المقاس في المشروعين** — تكرارٌ لا يشتري شيئًا لها وحدها. **لم يُمَسّ**: تغييره تغييرٌ في ما يُقاس لا في الإعداد.

**183. «ما يحتاج تدخلك» رقمٌ بجانب التنبيهات، لا قسمٌ يتصدّر الشاشة**

**قرار مالك.** القسم كان **يتصدّر الشاشة فيدفع كل ما تحته إلى الأسفل، في شاشتين رئيستين** (المشرف §5-ب/1 والمالك §5-د/1). **والرقم يؤدّي غرضه بلا مساحة رأسية.**

**وما يبقى قائمًا ولا يسقط بالحذف — وهو لبّ القرار لا حاشيته:**

**١. التمييز في القرار #33 يبقى كما هو.** التنبيه **حدثٌ مؤرَّخ يختفي بالقراءة**، و«ما يحتاج تدخلًا» **حالةٌ محسوبة تختفي بالمعالجة**.

> **فالرقم ليس عدّادًا للتنبيهات غير المقروءة، بل عدد الحالات التي ما زالت مفتوحة.**
> **ومشكلةٌ قُرئ تنبيهها ولم تُعالَج تختفي من التنبيهات وتبقى في الرقم — وهذا سبب وجوده.**

**٢. وشرط سلامته: ألّا يعرض ضغطُه نفس بنود قائمة التنبيهات.** **تكرار البند في الاثنين يُفقد الثقة فيهما معًا** — نصّ القرار #33 حرفيًّا.

**٣. والقرار #36 «التصعيد يكشف الإهمال» يبقى قائمًا، والرقم هو حامله بعد اليوم.**

**وما يُحتسب فيه لم يُحذف بل أُعيدت صياغته:** القائمتان في §5-ب/1 و§5-د/1 **هما القرار، والقسم كان وعاءها**. فبقيتا تعريفًا لما يدخل الرقم:

| الدور | ما يُحتسب |
| --- | --- |
| المشرف | بلاغ صحي شديد بلا رد · مراجعات معلّقة · شحنات لم تُستلم · عنابر لم تُسجَّل أمس · تغطية علف حرجة |
| المالك | نزاعات · بلاغات شديدة بلا رد · مراجعات متأخرة 48 ساعة · تغطية حرجة · عنابر لم تُسجَّل |

**ونمط «لوحة» صار ثلاثة أقسام لا أربعة** (§9): `صف مؤشرات قابلة للضغط` ← `قائمة مختصرة` ← `إجراءات سريعة`. **والرقم ليس قسمًا في جسم الشاشة فلا موضع له في الترتيب.**

**ولا يُبنى شيء الآن:** الشاشات الرئيسة الأربع كلها `PlaceholderScreen`. **قرارٌ يسبق البناء ولا ينشئه.**

**و`docs/design-reference-prototype.md` لم يُمَسّ** — ترويسته تنصّ على أنه **يُقرأ كما وصل ولا يُعدَّل**، وهو أدنى المراجع رتبة. **فالقرار يعلوه بلا تعديله**، وما فيه من «يحتاج تدخلك» يبقى كما ورد ويُقرأ تحت هذا القرار.

**184. قائمة موجبة للرؤية — من ليس فيها لا يرى شيئًا**

**الشرط الرابع لإغلاق المرحلة 3** (§7-ب البند 32، والقرار #161). **والعطب مقروء لا موصوف:**

```ts
// apps/api/src/lib/entityScope.ts (قبل)
export function visibleFarmCondition(viewer: Viewer): SQL | undefined {
  if (!isAssignmentScoped(viewer.role)) return undefined;
```

**و`undefined` تعني «بلا شرط» أي «كل شيء».** فأي دور خارج `ASSIGNMENT_SCOPED_ROLES` **يرى كل مزارع المستأجر بالسكوت**. **والعطب كامن لا فاعل اليوم**: لا يمسّ إلا `owner` وهو مقصود، و`platform_admin` وهو بلا مسار دخول بعد. **والقلب يقع قبل أن يُضاف أي دور، لا بعده.**

**ما صار — ثلاثة مواضع لا واحد:**

**١. `FULL_VISIBILITY_ROLES` قائمة موجبة** بنفس منطق `ASSIGNMENT_SCOPED_ROLES`: **المالك وحده اليوم**، و`platform_admin` **ليس منها** (القراران #146 و#147). فصار الحكم ثلاثيًّا صريحًا: **رؤية كاملة ← لا شرط · مُسنَد ← شرطه القائم بلا تغيير في منطقه · وأي دور آخر ← يُمنع صراحةً**.

**٢. و`visibleHouseCondition` قُلبت كذلك** — دور مجهول يُمنع فيها بنفسه **ولا يُترك متّكئًا على أن فلتر المزرعة سيمنعه**: **الاتّكال يسقط أول ما يُستدعى الشرط وحده في مسار جديد.**

**٣. والثقب الثالث في `housesService.ts`** كان نفس النمط حرفيًّا:

```ts
const scope = isAssignmentScoped(viewer.role) ? assignedHousesFilter(viewer.id) : undefined;
```

**فنُقل حكمه إلى `visibleHouseScope` في المصدر الواحد** — وإلا انتقل الثقب ولم يُسدّ.

**ونوع الإرجاع صار `SQL` دائمًا لا `SQL | undefined`.** **والنوع القديم هو ما يسمح بالنسيان**: كل مستدعٍ جديد قد يهمل الشرط ولا يكشفه المترجم. **والمستدعون الستة لم يتغيّر منهم سطر واحد** — كلهم يمرّرون النتيجة إلى `and(...)` وهي تقبل `SQL` كما تقبل `undefined`. فالتصحيح **بلا كلفة توسّع**، وهو أوفق للمبدأ الأول: الفرض المركزي لا الاستدعاء اليدوي.

**ولماذا القائمة الموجبة لا الشرط السالب:** الشرط السالب يقول «من ليس مقيَّدًا فلا قيد عليه»، **فيُنتج تجاوزًا صامتًا لكل دور لم يُفكَّر فيه بعد**. والقائمة الموجبة تقول «من ليس معروفًا لا يرى» — **ودورٌ يُنسى فيُحجب أهون من دورٍ يُنسى فيرى كل مزارع الشركة** (نصّ القرار #161).

**واختبار المخالفة المتعمَّدة شرط قبول لا زينة:** يُصطنع دور خارج القائمتين (`"storekeeper" as Role` — النوع لا يسمح به اليوم، **والاختبار يفحص ما يحدث حين يُضاف دور غدًا**) ويُثبت أن الدوال الثلاث تمنعه. **ولولاه لمرّ أي دور جديد بلا حارس**: العطب صامت **ولا يكشفه اختبار لدور لم يوجد بعد**.

**ولم يُضعَّف اختبار قائم ولا عُدِّل توقّع** — ولم يسقط شيء أصلًا: سلوك الأدوار الأربعة المعروفة لم يتغيّر، ويحرسه الاختبار نفسه.

**185. لا يُبنى قرار تدفّق على فحص لحظي قبل أن تستقرّ الشاشة**

**قاعدة تسري من الآن، سببها عطب مقيس لا مقدَّر.**

> **كل فحص لحظي (`isVisible()` وأمثاله) يُبنى عليه قرار تدفّق يجب أن يسبقه انتظارُ استقرار الشاشة.**
> **والانتظار يكون على ما يميّز الحالتين — `A.or(B)` — لا على مهلة زمنية ولا على مهلة أطول.**

**الواقعة:** سقطت تأكيدات التخطيط **مرتين في ساعة على نفس السطر بتأكيدين مختلفين** (تساوي ارتفاع المربّعات، ثم التسمية القصيرة)، **وفي كلتيهما نجح تشغيل آخر على نفس الالتزام بالضبط**. فالكود سليم والدالة المساعدة متذبذبة.

**والسبب مقروء في السطر:**

```ts
// layout-tests/houses-grid.layout.spec.ts · reachHousesGrid (قبل)
const siteCard = page.getByTestId("site-card-3");
if (await siteCard.isVisible().catch(() => false)) await siteCard.click();
await expect(page.getByTestId("house-tile-1")).toBeVisible();
```

`isVisible()` **فحصٌ لحظي لا ينتظر ولا يعيد المحاولة**، بخلاف `expect(...).toBeVisible()`. فبعد ضغط تبويب «المزارع» **قد لا تكون البطاقة صُيّرت بعد فيُرجع `false`**، فيُتخطّى الضغط، **ثم تنتظر الدالة خمس ثوانٍ مربّعاتٍ لن تظهر لأننا ما زلنا على مستوى المواقع**.

**والشرط نفسه ليس خطأ:** التخطّي التلقائي لموقع واحد بمزرعة واحدة (القرار #132) يجعل البطاقة **قد لا توجد أصلًا**، فالقرار لازم. **العطب أن القرار يُتَّخذ قبل أن تستقرّ الشاشة** — لا في وجوده.

**والعلاج ينتظر ثم يقرّر:**

```ts
// بعد
await expect(siteCard.or(houseTile)).toBeVisible();
if (await siteCard.isVisible()) await siteCard.click();
```

فيُنتظر ظهور **أحد الاثنين**: البطاقة إن كان مستوى المواقع قائمًا، **أو الشبكة إن كان قد تُخطّي**. **وعندها يصير الفحص اللحظي سليمًا لأن الصفحة استقرّت** — **ويسقط `catch(() => false)`** فلا حاجة إليه: الانتظار السابق يضمن أن الموضع محسوم.

**ولم تُزَد مهلة ولا أُضيف انتظار زمني ثابت:** **المهلة تخفي العطب ولا تصلحه**، **والانتظار الزمني يجعل الاختبار أبطأ وأهشّ معًا**. والقرار من عائلة #172 و#174 و#179: **فعلٌ على حالة عابرة افتُرض ثباتها**.

**ومُسِحت ملفات `layout-tests` الثلاثة كلها عن نفس النمط، ولم يوجد نظير:** هذا **الموضع الوحيد** في `houses-grid` و`auth` و`tab-labels` الذي يُبنى فيه قرار تدفّق على فحص لحظي. `reachSitesLevel` و`reachSelectAccount` و`reachPassword` و`reachOwnerTabs` **تنتظر بـ`expect(...).toBeVisible()` ولا تفرّع**، و`boxOf` و`tileBox` **تقيسان ولا تقرّران تدفّقًا** (وترميان عند الغياب بدل تمرير `null` صامتًا).

**ولم يُمَسّ منطق أي تأكيد** — الدفعة في الدالة المساعدة وحدها.

**والتحقّق ثلاثيّ لأن العطب تذبذبي:** **مرة واحدة خضراء لا تُثبت شيئًا**. ثلاثة تشغيلات متتالية بعد الإصلاح: **56/56 · 56/56 · 56/56** (14.0 ثانية · 13.9 · 13.8).

**186. الحالة الابتدائية للعنبر تُختار ولا تُفترض**

**قرار مالك، ويغلق §7-ب البند 40.**

> **تُختار الحالة الابتدائية للعنبر صراحةً عند الإنشاء، من قائمة الحالات نفسها.**
> **ولا تُفترض حالةٌ افتراضية صامتة.**

**والحال اليوم مقروء من المخطط لا موصوف:**

```ts
// packages/db/src/schema/farms.ts
status: houseStatusEnum("status").notNull().default("جاهز للإسكان"),
```

**و`createHouse` لا يمرّر حالة إطلاقًا** (`apps/api/src/services/housesService.ts`)، فيأخذ الافتراضي. **فالعنبر الجديد يُنشأ «جاهز للإسكان»** — لا بقرار أحد بل بسكوت الكود.

**والعلّة أن الافتراض الصامت ادّعاء جاهزية لم يؤكّدها أحد:** عنبر أُنشئ للتوّ **قد يكون تحت التنظيف والتطهير، أو تحت الصيانة، أو معطّلًا** — والقائمة تحمل الحالات السبع كلها (`HOUSE_STATUS` في `packages/shared/src/enums.ts`). **ومن له الصلاحية هو من يقرّر أيّها، لا المخطط.**

**وهي من عائلة #161 و#184:** ما لم يُقرَّر صراحةً **لا يُمنح بالسكوت**. هناك رؤية تُمنح لدور لم يُفكَّر فيه، وهنا جاهزيةٌ تُمنح لعنبر لم يره أحد.

**ولا يُبنى شيء الآن:** لم يُمَسّ مخطط ولا ترحيل ولا خدمة. **القرار يسبق التنفيذ ولا ينشئه** — والتنفيذ دفعة لاحقة، **§7-ب البند 40 صار محسومًا ينتظر التنفيذ لا القرار**.

**187. استرداد مدير المنصة بطبقتين — وحقيقة الخادم تُكتب لا تُجمَّل**

**قرار مالك، ويغلق شقّ الاسترداد من §7-ب البند 25** (والقراران #146 و#147 قائمان بلا مساس).

**المشكلة:** كل مسارات الاسترداد في هذا النظام «من فوقك يعيد تعيينك» — **ولا أحد فوق مدير المنصة.**

**وحقيقة تُكتب صراحةً ولا تُجمَّل:**

> **من يملك خادم الإنتاج يملك المنصة، وكل استرداد يمرّ من هناك في النهاية.**
> **فالهدف ليس منع القفل، بل أن يترك كل استرداد أثرًا مكتوبًا لا يُمحى.**

**الطبقة الأولى — ثلاثة مديري منصة لا واحد، يعيد كلٌّ تعيين الآخر.** **ولا آلية جديدة**: هو نمط «من فوقك يعيد تعيينك» نفسه **مطبَّقًا أفقيًّا**. **وشرطه شرطان لا واحد:** أن تُسجَّل كل إعادة تعيين في `admin_audit_log`، **وأن تصل إشعارًا للثلاثة معًا** — **فلا يُعاد تعيين أحدهم بلا علم الباقين**.

**الطبقة الثانية — مفتاح طوارئ يُنفَّذ على خادم الإنتاج نفسه لا من الشبكة.** يولّد **كلمة مرور مؤقتة تُستعمل مرة واحدة وتُبدَّل إجباريًّا عند أول دخول**، **ويكتب في سجل التدقيق من نفّذه ومتى**. **ملاذ أخير حين تسقط الأولى** — لا مسار عادي.

**وما رُفض ولماذا — يُكتب كي لا يُعاد اقتراحه:**

| المرفوض | العلّة |
| --- | --- |
| رابط استرداد إلى بريد مسجَّل | **يجعل حساب البريد هو المنصة** — تنتقل السيطرة إلى نظام خارج حراستنا. ويحتاج بنية إرسال لا نملكها. |
| رموز استرداد مطبوعة | **تنقل العبء إلى حفظ المالك**: تنفع إن حُفظت في مكان آمن، **وتصير كلمة مرور ثانية إن حُفظت في الهاتف**. |

**188. تحقّق بخطوتين بتطبيق مصادقة، لا برسائل نصية**

**قرار مالك، ويغلق شقّ التحقّق من §7-ب البند 25. ويقوم على القرار 187 قيامًا لا استغناء عنه.**

> **TOTP بتطبيق مصادقة. ولا رسائل نصية.**
> **والرمز يُسجَّل على جهازين عند الإنشاء لا على واحد.**

**ولماذا لا SMS:** **الرسائل تعتمد على شبكة قد لا تصل** — وهذا وحده كافٍ في بيئة الميدان عندنا — **وتُختطف بتبديل شريحة**، فتصير الطبقة الثانية أضعف من الأولى لا أقوى منها.

**والجهازان شرطٌ لا احتياط:** التسجيل على جهازين عند الإنشاء **يجعل ضياع أحدهما لا يقفل الحساب**.

**واعتراض المالك يُكتب مع جوابه لا يُطوى:**

> **«ضياع الهاتف قد يكلّفنا كل شيء.»**

**وجوابه أن الضياع قفلٌ لا اختراق** — والفرق بينهما هو كل شيء: **الاختراق يُفقدك النظام لغيرك، والقفل يؤخّرك عنه**. **والقفل تحلّه طبقتا القرار 187.**

**فالتحقّق بخطوتين مقبول لأن الاسترداد وُجد أولًا — ولولاه لسقط الاقتراح.** ترتيبٌ يُسجَّل: **187 قبل 188 لا بعده**.

**والمقابل الذي يشتريه:** **كلمة مرور مسروقة وحدها لا تكفي** لدخول حسابٍ **يرى كل المستأجرين**.

**189. حسابات مدير المنصة تُنشأ من جديد عند الفصل، ولا تُرحَّل**

**قرار مالك، ويغلق السؤال الأخير المفتوح في §7-ب البند 25** — فلم يبقَ في البند قرارٌ معلّق.

> **عند فصل مدير المنصة بنيويًا عن أدوار المستأجر (#146 و#147)، تُنشأ حساباته في موضعها الجديد إنشاءً جديدًا.**
> **ولا يُكتب ترحيلٌ ينقل صفوف `platform_admin` القائمة.**

**والعلّة أن الترحيل هنا يشتري كلفةً بلا مقابل:** الحسابات القائمة **معدودة، ولا مسار دخول لها أصلًا** (`POST /auth/platform-login` غير مبنيّ)، **فلا تاريخ فيها يُحفَظ ولا جلسات تنكسر**. وترحيلها يعني **كتابة ترحيل يقرأ من الجدول الذي نريد تنظيفه** — فيبقي أثر القسمة القديمة حيًّا في الكود الذي وُجد ليمحوها.

**والإنشاء الجديد يشتري ما لا يشتريه الترحيل:** الحسابات الثلاثة التي يفرضها القرار 187 **تُنشأ بشروطها كاملة من أول يوم** — كلمة مرور 12 محرفًا، وتحقّق بخطوتين مسجَّل على جهازين (القرار 188). **وحسابٌ مُرحَّل يصل بلا أيٍّ منهما**، فيلزم بعده مسار «استكمال» لا وجود له.

**فالقيمة `platform_admin` تُزال من `USER_ROLE` بلا خلَف في الصفوف**، ولا يبقى منها شيء يُقرأ.

**ولا يُبنى شيء الآن** — القرار يحكم دفعة الفصل حين تُبنى، ولا ينشئها.

**190. الإسناد بمدة — منفَّذًا: عمودان، وقيدا استبعاد في القاعدة، وشرطٌ واحد في خمسة استعلامات**

**تنفيذ القرار #158 (§7-ب البند 29، وشرط الإغلاق الثالث للمرحلة 3). خلفيّ بحت — ولا مسار إسناد يُبنى هنا:** لا `POST` ولا `DELETE` في `routes/` اليوم، فالدفعة في **المخطط والاستعلامات والقيد** وحدها.

**ما تغيّر — بالأرقام:**

| الموضع | التغيير |
| --- | --- |
| `packages/db/src/schema/farms.ts` | عمودان: `start_date` (مطلوب) · `end_date` (يقبل الفراغ)، والفهرسان الفريدان أُزيلا |
| `packages/db/migrations/0009_assignment_periods.sql` | ترحيل مرقَّم عبر `generate` — **لا `push`** |
| `apps/api/src/lib/entityScope.ts` | `assignmentActiveToday()` — الشرط مرة واحدة، ويُستعمل في أربعة |
| `apps/api/src/middleware/entityAccess.ts` | الخامس، بنفس الشرط مستوردًا لا منسوخًا |
| `apps/api/src/lib/pgErrors.ts` | 23P01 مع 23505، واسما القيدين ورسالتاهما |
| اختبارات | ملف جديد بسبعة، و10 مواضع إدراج في ثلاثة ملفات قائمة والبذر |

**ولا قيمة افتراضية لـ`start_date` عمدًا:** العمود بلا `DEFAULT`، **فكل إدراج يختار بدايته** — نفس منطق القرار 186: ما لا يُقرَّر لا يُمنح بالسكوت. **والصفوف القائمة تُملأ من `created_at`** لا من تاريخ اليوم ولا من قيمة مخترعة: الإسناد بدأ حين أُنشئ، **وهي الحقيقة الوحيدة التي نملكها عنه**.

**و`end_date` آخر يوم مسؤولية شاملًا** لا أول يوم بعدها — **فإسناد انتهى أمس يحمل `end_date = أمس`**، والفراغ سريانٌ بلا أجل. **والتسليم بلا فجوة: ينتهي الأول اليوم ويبدأ الثاني غدًا**، فلكل يوم مسؤول واحد.

**١. لماذا القيد في القاعدة لا في الكود.**

`EXCLUDE USING gist` على `daterange` **لا فهرسًا فريدًا**: فهرس على `(user_id, house_id, start_date)` يبدو كافيًا **ويقبل مدّتين متداخلتين ببدايتين مختلفتين صامتًا** — **التداخل ليس تساويًا، فلا تمنعه أداة التساوي** (نصّ #158، ودرس #128 بأداة مختلفة). **وحارس طبقة الخدمة إجرائي يُعيد الثقبَ أيُّ مسار كتابة جديد لا يمرّ به** — والمبدأ الأول: الفرض في طبقة واحدة.

**و`btree_gist` مُفعَّل في الترحيل نفسه**: لم يكن مثبَّتًا في قاعدتَي التطوير والاختبار (`\dx` = `plpgsql` وحده)، **والامتداد موثوق (trusted) منذ PG13 فيكفيه مالك القاعدة ولا يحتاج superuser** — أُثبت بتفعيله داخل معاملة ثم التراجع عنها قبل كتابة الترحيل.

**٢. ولماذا قيدان جزئيان لا قيد واحد.**

`NULL` **لا تساوي `NULL` في قيد الاستبعاد**، فقيدٌ واحد يجمع `house_id` و`farm_id` **لا يمنع شيئًا**: صفّا عنبر يحمل كلٌّ منهما `farm_id` فارغًا لا يتقابلان أصلًا. **فقيدٌ لصفوف العنبر (`WHERE house_id IS NOT NULL`) وآخر لصفوف المزرعة** — **نفس شكل الفهرسين الجزئيين اللذين حلّا محلّهما، ونفس علّتهما المكتوبة هناك.**

**٣. وأين وُضع الشرط المشترك.**

`assignmentActiveToday(alias)` في `entityScope.ts` — **مصدر واحد يقرؤه الفرض المركزي وطبقة الخدمة معًا**، كما `ASSIGNMENT_SCOPED_ROLES` قبله. **ولم يُنثر الشرط خمس مرات: شرطٌ منسوخ خمس مرات يُنسى في السادس.**

**و«اليوم» `CURRENT_DATE` — تاريخ القاعدة لا تاريخ الخادم:** خادم بمنطقة زمنية مختلفة يُنهي إسنادًا قبل أوانه أو يمدّه، **والقيد الذي يحرس التداخل يقيس بساعة القاعدة** — فيقيس السؤال بها. **والتجهيزات والاختبارات تكتب تواريخها بالمقياس نفسه** (`CURRENT_DATE ± n`)، فلا يقيس الاختبار بغير ما يقيس به المُختبَر.

**٤. وما أُثبت على القاعدة نفسها لا على الكود — ستّ حالات في معاملة تُراجَع:**

| # | الحالة | النتيجة |
| --- | --- | --- |
| ١ | صفّان متتاليان بلا تداخل (ينتهي 03-31 ويبدأ 04-01) | **قُبلا** |
| ٢ | صفّ يتداخل مع مدّة مفتوحة النهاية | **رُفض** (23P01) |
| ٣ | تداخل بيوم واحد على الحدّ (نفس يوم النهاية) | **رُفض** — والنهاية شاملة |
| ٤ | نفس المستخدم على عنبر آخر بنفس المدة | **قُبل** |
| ٥ | صفّ مزرعة مع صفوف عنابر بنفس المدة | **قُبل** — القيدان جزئيان |
| ٦ | صفّا مزرعة متداخلان | **رُفض** |

**وهذه نفس مدرسة #128:** الثقب هناك أُثبت على القاعدة بثلاثة صفوف متطابقة قُبلت **قبل** إصلاحه — **والحارس هنا يُثبت على القاعدة قبل أن يُصدَّق**.

**٥. وخمس مخالفات متعمَّدة لا واحدة** (`assignmentPeriods.integration.test.ts`، سبعة اختبارات): لكل استعلام من الخمسة مسارٌ **يعزله** — الفرض عبر `GET /api/houses/:id`، والفلترة عبر سرد عنابر المزرعة، وفرعا `visibleFarmCondition` عبر سرد مزارع الموقع لمربٍّ ولمشرف، و`visibleHouseCondition` عبر **عدّاد** عنابر بطاقة المزرعة. **واثنان فوقها: إسناد يبدأ غدًا لا يمرّ اليوم · وإسناد بلا نهاية يمرّ.**

**وأُثبت أن الستة حاملة للحمل لا زينة:** بإرجاع الشرط `true` مؤقتًا **سقطت الستة كلها**، وبإعادته مرّت — **واختبار واحد يغطّي الخمسة لا يثبت شيئًا عن أربعة منها** (درس #128 و#107).

**ولم يُضعَّف اختبار قائم ولا عُدِّل توقّع:** ما تغيّر في الاختبارات القائمة **إدراجاتها وحدها** — عشرة مواضع صارت تكتب `start_date` صراحةً، **لأن العمود بلا افتراضي بقرار**، لا لتمرير شيء.

**وثغرة قائمة لُوحظت ولم تُلمس — تُسجَّل ولا تُصلَح هنا:** `GET /api/farms/:farmId` **ليس في أنماط `enforceEntityAccess`**، و`getFarm` لا يأخذ `viewer` — **فأيّ مستخدم في المستأجر يقرأ أي مزرعة فيه بالمعرّف**. **ولا علاقة لها بهذه الدفعة** (قائمة قبلها وبعدها سواء)، **واكتُشفت لأن مخالفتين متعمَّدتين مرّتا على هذا المسار فكشفتاه** — قُرِّرت في §7-ب لا هنا.

**191. قراءة المزرعة كانت بلا فرض — سطر في القائمة لا سطر في الموجّه**

**إغلاق §7-ب البند 43. ثغرة صلاحيات عولجت فورًا باستثناء §7-ب** («ثغرة أمنية أو فساد بيانات يُعالَجان فور اكتشافهما بصرف النظر عن المرحلة») — **بقرار المالك صراحةً.**

**الثقب:** `GET /api/farms/:farmId` **لم يكن في `ENTITY_ID_PATH_PATTERNS`** (كان فيها `/api/farms/:farmId/houses` للسرد وحده)، **و`getFarm` لا يأخذ `viewer` أصلًا** — فلا فرض قبلها ولا فلترة بعدها. **فأيّ مستخدم في المستأجر يقرأ أيّ مزرعة فيه بمعرّفها**: مربٍّ بلا إسناد، ومربٍّ انتهت مدته، ومشرف لم تُسند إليه. **والعزل بين المستأجرين لم يكن متأثرًا** (`tenant_id` مفروض، و404 كانت تعمل).

**وكيف نشأ — يُكتب لأنه نمط لا حادثة:** المسار أُضيف والنمط نُسي، **والقاعدة مكتوبة أصلًا في `CLAUDE.md` وفوق القائمة نفسها**: «كل مسار سرد جديد يُضاف هنا». **فالقاعدة موجودة ومقروءة ولم تُفرض بشيء** — نفس علّة #174: **شرطٌ مكتوب لا يحرس إن لم يكن هناك ما يمنع تجاوزه.** والقائمة تُقرأ بالعين وحدها، **ولا فحص في `check:all` يقابل المسارات بالأنماط** (فحص «المسارات المكررة» يفحص التكرار لا التغطية).

**وكُشف من طريقين مستقلين في نفس اليوم:** **مخالفتان متعمَّدتان** في دفعة القرار 190 وُجِّهتا إلى هذا المسار **فمرّتا بـ200 وكان يُنتظر 403**؛ **ومراجعة المالك** لقائمة الأنماط مقابل المسارات المسجَّلة. **والتقاؤهما دليلٌ على قيمة المخالفة المتعمَّدة أداةً**: هي لم تكن تبحث عن هذا الثقب، **وكشفته لأن التوقّع كان مكتوبًا فسقط عند الاصطدام بالواقع**.

**ولماذا لم يكشفه اختبار قائم:** اختبارات الإسناد الـ15 كلها كانت على **السرد** (`/farms/:id/houses`) وعلى **العنبر** (`/houses/:id`) — **ولا اختبار واحد على قراءة المزرعة نفسها**. **فالتغطية كانت على المسار المجاور**، والمسار المكشوف بلا حارس ولا شاهد.

**الإصلاح — في القائمة لا في الموجّه:** النمط صار `"/api/farms/:farmId"`، **والبادئة تغطّي الاثنين** (`api.use(pattern)` يطابق البادئة لا المسار الكامل) — **نفس ما تفعله القائمة مع `/api/sites/:siteId` منذ القرار #131**. **ولم يُلمس `getFarm` ولا أُضيف فحص داخل الموجّه:** الفرض المركزي لا يُنثَر (المبدأ الأول)، **وإضافة نمط سطر واحد بجوار أخواته لا بحثٌ في كل ملف مسار**.

**و403 لا 404** — الوجود ثم التعيين (المبدأ السادس): موجودة غير مُسندة ← 403، ومزرعة مستأجر آخر ← 404 **لا نقرّ بوجودها**.

**وإثبات السقوط قبل الإصلاح شرط قبول لا زينة:** المخالفتان أُعيدتا **اختبارَين دائمين** بتوقّع 403، **وشُغِّلتا على الكود قبل الإصلاح فسقطتا** (`expected 200 to be 403`) ومعهما ثالثة لمربٍّ بلا إسناد، **ثم خضرّت الثلاث بعده**. **واختبارٌ لم يُرَ ساقطًا لا يُعرف ماذا يحرس** (درس #107).

**وست حالات لا ثلاث:** انتهت مدة المربّي ← 403 · انتهت مدة المشرف ← 403 · مربّي بلا إسناد ← 403 · **المالك يفتح أي مزرعة في مستأجره ← 200** · **المشرف يفتح مزرعته المُسندة ← 200** · **ومزرعة مستأجر آخر ← 404**. (الثلاث الأخيرة كانت تمرّ قبل الإصلاح وبعده — **تُثبت أن الإصلاح لم يغلق ما يجب أن يُفتح**.)

**ومسح كامل لكل مسار يحمل معرّف كيان في الرابط — مسارًا مسارًا لا جملة عامة:**

| المسار | التغطية بعد الإصلاح |
| --- | --- |
| `GET /api/farms/:farmId` | ✅ نمط `/api/farms/:farmId` — **هذا هو المُصلَح** |
| `PATCH /api/farms/:farmId` | ✅ النمط نفسه **و**`requireRole("owner")` |
| `GET /api/farms/:farmId/houses` | ✅ بالبادئة نفسها (كان بنمطه الخاص) |
| `POST /api/farms/:farmId/houses` | ✅ بالبادئة نفسها **و**`requireRole("owner")` |
| `GET /api/houses/:houseId` | ✅ نمط `/api/houses/:houseId` |
| `PATCH /api/houses/:houseId` | ✅ النمط نفسه **و**`requireRole("owner")` |
| `GET /api/sites/:siteId` | ✅ نمط `/api/sites/:siteId` |
| `PATCH /api/sites/:siteId` | ✅ النمط نفسه **و**`requireRole("owner")` |
| `GET /api/sites/:siteId/farms` | ✅ بالبادئة نفسها |
| `POST /api/sites/:siteId/farms` | ✅ بالبادئة نفسها **و**`requireRole("owner")` |

**ولا مسار حادي عشر يحمل معرّف كيان:** الباقي **بلا معرّف في الرابط** — `/api/settings` (`GET` و`PATCH`، وكلاهما `requireRole("owner")`) · `/api/sites` (`GET` مفلتر بالرؤية، و`POST` للمالك) · مسارات `/api/auth/*` · و`/health` و`/ready` **خارج `/api` أصلًا فلا تمرّ بالسلسلة**. **ونمط `/api/batches/:batchId` قائم في القائمة بلا مسار يقابله بعد** — يسبق مسارات الدفعات لا يتأخّر عنها.

**فلم يبقَ مسار مكشوف اليوم.** **والحارس البنيوي — فحصٌ يقابل المسارات بالأنماط آليًّا — لم يُبنَ في هذه الدفعة** ويبقى الاعتماد على القراءة والمراجعة: **يُسجَّل في §7-ب لا يُطوى.**

**192. تسجيل اليوم يعمل بلا اتصال — طابور إرسال موضعي، لسجلّ اليوم وحده**

**قرار مالك صريح، ويغلق §7-ب البند 48.** **وحُسم والشاشة ما زالت هيكلًا:** `app/(farmer)/daily-log.tsx` **لا يزال `PlaceholderScreen`** — **فالقرار يسبق البناء بالكامل ولا شيء يُرقَّع**، والطابور يُبنى مع الشاشة في نقطة حفظ واحدة تُكتب مرة.

> **يُحفظ سجلّ اليوم على الجهاز فور ضغط الحفظ، ويُرسَل حين تعود الشبكة.**
> **ولسجلّ اليوم وحده — لا لغيره.**

**والعلّة من ثلاث، لا واحدة:**

**١. منطق المنتج نفسه يناقض الحال القائم.** «**عنابر لم تُسجَّل أمس**» **بندُ تدخّلٍ يطارده النظام** — في «ما يحتاج تدخلك» للمشرف وللمالك معًا (§5-ب/1 و§5-د/1، والقرار 183). **وترك التسجيل رهين الإشارة يصنع هذه المشكلة بيد النظام لا بتقصير المربّي**: عنبرٌ بلا إشارة يُنتج تنبيهًا يُطارَد، والمربّي فعل ما عليه.

**ومسار الحفظ مسكوت عنه في المواصفة أصلًا:** نصّ الانقطاع الوحيد فيها — «تعذّر تحميل البيانات — السبب: **انقطاع الاتصال بالشبكة في العنبر**» — **في §8.17 «الحالات الأربع لكل قائمة»، أي على القراءة**. **ولا سطر واحد عن الحفظ.** **فالحال القائم ليس قرارًا اتُّخذ بل سكوتًا وُرث** — والقرار يسدّه.

**٢. جودة الأرقام — وهي لبّ المنتج لا حاشيته.** **المؤجَّل يُسجَّل من الذاكرة بعد ساعات فيصير تقديرًا**: النفوق والأكياس والخزانات والحرارة تُكتب كما يتذكّرها لا كما عدّها. **والمنتج كلّه قائم على التقاط الرقم في موضعه ووقته** — وعليه تُبنى المقارنة بين العنابر وبين الأيام، وكشف تغيّر النمط (#162)، **وكلها تنهار على أرقام مقدَّرة قبل أن تنهار على أرقام ناقصة**.

**٣. والكلفة منخفضة بنيويًّا — بأربعة مقروءة لا مقدَّرة:**

- **مفتاح العصمة موجود في القاعدة قبل أن يُطلب:** `daily_logs_batch_date_uq` فهرس فريد **جزئي** على `(batch_id, log_date) WHERE correction_of_id IS NULL` — **فسجلّ واحد لكل دفعة في اليوم، وإعادة الإرسال المكرّرة آمنة بحكم القاعدة لا بحكم انضباط العميل**. **والتصحيح صفٌّ مرتبط يقع خارج الفهرس الجزئي، فلا تعارض مع المبدأ الرابع** (لا `UPDATE` على سجل ميداني).
- **ومبدأ عدم تعديل السجل يلغي التعارضات أصلًا:** لا صفَّ يُعدَّل فلا تعارض يُحلّ — **وهو ما يجعل الطابور طابورًا لا محرّك مزامنة**.
- **ومخزن الجهاز مثبَّت غير مستعمَل:** `@react-native-async-storage/async-storage` في تبعيات الموبايل، ولا يستعمله كود التطبيق بعد (الجلسة في `expo-secure-store`).
- **و`QueryClient` قائم بلا تثبيت ذاكرة** (`app/_layout.tsx`) — **فالجزء القرائي (إبقاء آخر بيانات معروفة عند الانقطاع) إضافةٌ على مستعمَل لا استحداث طبقة**.

**وحدّان شرطان في نصّ القرار لا في حاشيته:**

> **١. النطاق سجلّ اليوم وحده — ولا يتوسّع إلى شاشة أخرى إلا بقرار جديد.**
> **٢. والصدق في الواجهة: سجلٌّ في الطابور يظهر «بانتظار الإرسال» بوضوح، ولا يُعرض كأنه وصل.**

**والثاني ليس تحسين تجربة بل شرط سلامة:** سجلٌّ يبدو محفوظًا على الخادم وهو في جيب الجهاز **يجعل المربّي يظنّ أنه أدّى، والمشرف يظنّ أنه لم يؤدِّ** — **فتُبنى قرارات على حالة لا يعرفها أحد**. **وهو نظير #33 و#153 حرفيًّا: النظام يعرض الواقعة كما هي ولا يجمّلها.**

**وما رُفض يُكتب كي لا يُعاد اقتراحه:** **الأوفلاين الكامل** — قاعدة محلية ومحرّك مزامنة وحلّ تعارضات. **لا يبرّره الحجم**، **وكلفته دائمة على كل شاشة تُبنى بعده** لا مرة واحدة: كل مسار جديد يصير له مسار محلي ومسار مزامنة، **وتعارضٌ يُحلّ في شاشة يُنسى في التي بعدها**.

**193. الفرض المركزي يعرف الموقع — قبل أول مسار مخزون لا بعده**

**تنفيذ القرار #157 البند ١ (§7-ب البند 28، شرط الإغلاق الثاني للمرحلة 3). خلفيّ بحت.**

> **لا يُبنى أي مسار مخزون قبل أن يعرف الفرض المركزي `locationType`/`locationId` ويفرض عليهما ما يفرضه على العنبر والمزرعة.** (نصّ #157)

**ولماذا الآن قبل أول مسار — النصّ نفسه يقولها:** `enforceEntityAccess` كان يمسح `houseId · batchId · farmId · siteId`، **وعقد التحويل لا يحمل أيًّا منها** — فكل مسار مخزون يُبنى قبل الحسم **يمرّ من الحارس دون أن يراه**، **ويصير إصلاحه لاحقًا مراجعةَ كل مسار كُتب بدل تعديل طبقة واحدة** (المبدأ الأول). **والدفعة تعليم الحارس مفردات الموقع لا بناء المخزون:** لا مسار API واحد، ولا نموذج إسناد للمخازن، **ولم يُمَسّ مخطط `inventory` بحرف**.

**ما عُلِّم للحارس — ثلاثة أزواج، وكلا طرفَي التحويل معًا:**

| الزوج | الحكم |
| --- | --- |
| `locationType` / `locationId` | حسب النوع أدناه |
| `fromLocationType` / `fromLocationId` | **يُفحص** |
| `toLocationType` / `toLocationId` | **يُفحص** |

**والطرفان معًا لا أحدهما:** **طلبٌ سليم المصدر معطوب الوجهة يُرفض** — ولو فُحص المصدر وحده **لصار التحويل بابًا خلفيًّا إلى عنبر غير مُسند**: يخرج المربّي علفًا من عنبره إلى عنبر ليس له، فيمسّ رصيدًا لا يملك مسّه.

**وتسمية الحقول على السلك محسومة هنا كي يلتزم بها بناء المسارات لاحقًا:**

> **`camelCase` لا أعمدة القاعدة: `locationType` · `locationId` · `fromLocationType` · `fromLocationId` · `toLocationType` · `toLocationId`.**

**والقراءة قبل الحسم:** المواصفة التقنية §13 تسمّيها `from_location_type/id` — **وهي أسماء أعمدة في المخطط لا حقول على السلك**؛ **وعقد `openapi/spec.json` القائم `camelCase` بلا استثناء واحد** (`farmId` · `siteId` · `tenantId` · `waterTankCapacityL` · `houseStatusCounts`). **فالسلك يخاطب بما يخاطب به أخواته، والقاعدة تبقى بأسمائها** — والحارس يمسح ما على السلك.

**وحكم كل نوع:**

**١. `'house'` — يُحلّ كما يُحلّ `houseId` حرفيًّا:** نفس `assertHouseAssignment` **لا نسخة ثانية منه** (المبدأ الأول: مصدر واحد) — الوجود في المستأجر ← **404**، ثم الإسناد **الساري اليوم** ← **403**. **وشرط «سارٍ اليوم» (القرار 190) يسري على المفردة الجديدة بلا استثناء** لأنها تستدعي نفس الدالة، **لا لأنه أُعيد كتابته فيها**.

**٢. `'warehouse'` — وضعية موجبة مؤقتة:** الوجود في المستأجر ← **404**، **ثم صاحب الرؤية الكاملة وحده يمرّ (`owner`)، وكل دور آخر ← 403**. **والعلّة أن نموذج إسناد المخازن لم يُبنَ بعد**: `warehouses` بلا `site_id`/`farm_id`، و`user_assignments` بلا `warehouse_id`، ولا دور «أمين مخزن» — **ودورٌ بلا قاعدة سماح معلومة يُحجب لا يُفتح** (#161 و184). **وهذا هو الفرع الوحيد الذي يتغيّر يوم يُبنى النموذج** (بقيّة §7-ب البند 32): يصير «المالك أو من يبلغه إسناد المخزن»، **ولا يُمسّ شيء آخر**.

**٣. قيمة غير معلومة ← 403 لا تمرير صامت.** zod سيرفضها في المسارات لاحقًا، **والحارس لا يتّكئ على حارس لم يُبنَ بعد** — **ونوعٌ ثالث يُضاف إلى `LOCATION_TYPE` غدًا يبقى محجوبًا حتى يُدرَج هنا بقرار مكتوب**. **ومعرّفٌ بلا نوع يُرفض كذلك**: ما لا يُعرف نوعه لا يُفحص، وما لا يُفحص لا يمرّ. **وزوجٌ بلا معرّف يمرّ** — لا يشير إلى كيان أصلًا (فلترة سرد بالنوع مثلًا)، **والسرد يحرسه فلتر الرؤية لا هذا الحارس**.

**وموضع الفحص مقصود:** **أزواج الموقع تُفحص قبل قصر الدائرة على الأدوار المقيَّدة بالإسناد.** الحارس كان يخرج مبكرًا لكل دور غير مقيَّد — **ولو فُحص المخزن بعد ذلك الخروج لمرّ كل دور غير مقيَّد بلا فحص إطلاقًا**، وهو نقيض الوضعية الموجبة. **والمالك يمرّ بحكم رؤيته الكاملة لا بحكم تخطّي الحارس** — والفرق يظهر يوم يُضاف دور جديد.

**والإثبات بنمط الـprobe القائم** (`src/middleware/locationScope.integration.test.ts`): **لا مسار مخزون مبنيّ، فالاختبار بمسار مؤقت محلي يركّب السلسلة الحقيقية** `requireAuth → requireTenant → enforceEntityAccess` — نفس الدوال غير معاد كتابتها، **والتعليل في `_probe.integration.test.ts` يشرح لماذا هذا مشروع**. **والمخزن يُدرَج مباشرة في القاعدة** لأنه لا مسار API للمخازن ولا يُبنى هنا.

**وثلاثة عشر اختبارًا، عشرة منها حاملة للحمل — أُثبت سقوطها:** بإبطال فحص المواقع مؤقتًا **سقطت العشرة** (والثلاثة الباقية هي حالات «يجب أن تمرّ» فبقيت خضراء)، وبإعادته مرّت الثلاثة عشر. **والسقوط هنا ليس «حكمًا كان مختلفًا» بل أن الحارس لم يكن يرى الحقول أصلًا** — وهو بعينه الثقب الذي وصفه #157.

**ولم يُضعَّف اختبار قائم ولا عُدِّل توقّع** — ولم يسقط شيء: المفردات الجديدة **تُضاف ولا تغيّر حكم أيٍّ من القديمة**.

**194. الفصل البنيوي لمدير المنصة — جدول مستقل، وenum بلا سادس، و`tenant_id` بلا فراغ**

**تنفيذ القراران #146 و#147 والقرار 189 (§7-ب البند 25، الشقّ البنيوي). خلفيّ بحت.**

**والنطاق محدود بإزالة المخالفات الأربع وحدها:** **لا لوحة منصة، ولا `POST /auth/platform-login`، ولا تحقّق بخطوتين، ولا استرداد** (187 و188) — **دفعة تالية مستقلة**. **والنتيجة المؤقتة مقصودة ومعلنة: لا قدرة دخول لمدير منصة إطلاقًا** — **وهي الحال الفعلية قبل الدفعة أصلًا** (لا مسار دخول له مبنيّ).

**١. الجدول المستقل.** `platform_admins`: هوية · اسم · هاتف (خام وE164) · تجزئة كلمة مرور · `is_active` · `created_at`. **ولا عمود `tenant_id` فيه أصلًا — لا فارغًا ولا مملوءًا.** وفهرس فريد على الهاتف **شاملًا المعطَّلين** (نفس علّة #23). **وبلا حقول TOTP اليوم**: تأتي مع دفعتها، **وإضافة عمود حينها أرخص من حمل أعمدة ميتة**. **ولا مسار API يكتب فيه** — يُنشأ بإجراء مباشر مقيَّد (#147: «الحساب الوحيد الذي لا يُنشئه أحد من داخل التطبيق»، وخارج قاعدة البذر عبر الـAPI لأنه ليس بيانات مستأجر).

**٢. الترحيل `0010_platform_admin_separation` — بترتيب لا يُبدَّل:**

| # | الخطوة | لماذا هنا بالذات |
| --- | --- | --- |
| ١ | `DELETE FROM users WHERE role = 'platform_admin'` | **تُحذف ولا تُرحَّل (القرار 189)**، **وقبل كل ما بعدها**: صفٌّ بهذا الدور يكسر `SET NOT NULL` ويرفضه الـenum الجديد |
| ٢ | حذف `users_platform_phone_unique` | فهرس جزئي يخدم `tenant_id IS NULL` وحدها — ولا صفوف كذلك بعد |
| ٣ | `users.tenant_id SET NOT NULL` | القابلية للفراغ وُجدت لمدير المنصة وحده — **وزوالها يغلق الباب بنيويًّا** |
| ٤ | إنشاء `platform_admins` وفهرسه | يسبق المفتاح الأجنبي الذي يشير إليه |
| ٥ | `admin_audit_log.actor_id → platform_admins(id)` | الفاعل ليس مستخدم مستأجر أصلًا |
| ٦ | تقليص `user_role` إلى أربع | Postgres لا يحذف قيمة من enum: العمود ← `text`، حذف النوع، إنشاؤه بالقيم الأربع، ثم العمود إليه. **والعمود الوحيد المستعمِل للنوع هو `users.role`** |

**وقُرئ قبل كتابة الترحيل ولم يُفترض:** `admin_audit_log` **صفر صفوف** في قاعدتَي التطوير والاختبار، **وصفر صفوف `platform_admin` في `users`**، **وصفر صفوف بـ`tenant_id IS NULL`** — **فلا صفّ يتيم ولا قرار معالجة مطلوب**.

**٣. وزال الاستثناء الوحيد من قاعدة المفتاح المركَّب** (#122، والمذكور في `CLAUDE.md` نصًّا): كان `actor_id` مفردًا هنا لأن الفاعل بلا مستأجر بينما السجل قد يستهدف مستأجرًا، **فالمركَّب يرفض كل صف مشروع**. **والآن الفاعل في جدول بلا `tenant_id` إطلاقًا — فلا مركَّب يُطلب ولا استثناء يُستثنى.** **والفقرة في `CLAUDE.md` لم تُحذف بل صارت تاريخًا**: لماذا وُجد الاستثناء وكيف زال — **فالقاعدة تُقرأ اليوم بلا ثغرة في نصّها، ولا يُعاد اختراع الاستثناء غدًا**.

**٤. الكود.** `requireTenant` **بلا قصر دائرة لأي دور**: كان `if (role === "platform_admin") next()` — **فسلسلة العزل كلها تُتخطّى بقيمة enum واحدة**، وهو حرفيًّا خطر #146. **وشاشات `apps/mobile/app/platform/` الخمس و`_layout` حُذفت** (#147: «لوحة منفصلة عن التطبيق، لا شاشة داخله»)، **ومجموعة `platform` أُزيلت من فاحص تغطية التنقل** وإلا طالب بمجلد يجب ألا يعود.

**و`AuthTokenPayload.tenantId` يبقى `number | null` عمدًا — قرارٌ بعد قراءة مستهلكيه:** `verifyAccessToken` **يُسقِط الحمولة إسقاطًا (`as`) بلا تحقق بنيوي**، فالنوع **ادّعاء عن نصّ وقّعناه سابقًا لا ضمان عن نصّ قادم**؛ **ورمزٌ صالح التوقيع عمره 30 يومًا قد يحمل `null` اليوم**. وتضييقه إلى `number` **يجعل حرّاس `null` القائمين شروطًا ميتة في نظر المدقّق فتُحذف — فيسقط الحارس لا الاحتمال**. **ويُضيَّق يوم يُتحقَّق من الحمولة عند فكّها، لا قبله.**

**٥. وثقبان كشفتهما المخالفة المتعمَّدة — عولجا هنا لأن معيار قبول الدفعة نفسه «لا يرى شيئًا»:**

- **`listSites` كان يعرض أسماء مواقع المستأجر لأي دور غير مقيَّد بالإسناد** — والشرط كان «مقيَّد ← يُشترط أن يرى مزرعة، وغيره يمرّ»، **وغيرُه يشمل كل دور غير معلوم**. فرمزٌ بالدور المحذوف كان يرى **أسماء المواقع** بعدّادات أصفار. **وقُلب إلى قائمة موجبة كنظائره**: المالك بلا شرط، ومن سواه يُشترط أن يرى مزرعة، **والمجهول لا يرى شيئًا**.
- **`enforceEntityAccess` كان يمرّر كل دور غير مقيَّد بالإسناد** — بما فيه المجهول. **والآن: المالك يمرّ بحكم رؤيته الكاملة، ومن ليس في قائمة معلومة ← 403.** **والفرع غير قابل للوصول لمستخدم مشروع** (الأدوار الأربعة كلها إمّا كاملة الرؤية أو مقيَّدة بالإسناد) — **فهو حارس للرموز القديمة والمزوَّرة وحدها**.

**وكلاهما إتمام للقرار 184 لا توسيع نطاق:** نصّه «من ليس في قائمة معلومة لا يرى شيئًا»، **وقد كان مطبَّقًا في شروط الرؤية دون هذين الموضعين**.

**٦. المخالفات المتعمَّدة — ستة اختبارات** (`platformAdminRemoval.integration.test.ts`): رمز قديم بالدور المحذوف ← **403 على سرد المواقع، ولا اسم موقع في الرد** · **403** على سرد مزارع موقع قائم · **403** على الإعدادات · ورمز بلا مستأجر ← **401 من `requireTenant` بلا استثناء** · **والقاعدة نفسها ترفض**: إدراج مباشر بدور `'platform_admin'` يفشل (النوع لا يحمله)، وإدراج مستخدم بلا مستأجر يفشل (`NOT NULL`).

**٧. وأربعة اختبارات قائمة حُوِّلت ولم تُحذف صامتة — تُذكر بأسمائها:**

| الملف | كان يُثبت | صار يُثبت |
| --- | --- | --- |
| `middleware/tenant.test.ts` | مدير المنصة **يمرّ** بـ`tenantId: null` | **401 لرمز قديم بدور غير معلوم** — لا استثناء لأحد |
| `lib/entityScope.test.ts` | `platform_admin` (دور قائم وقتها) خارج القائمتين | **قيمة دور غير معلومة** لا ترث شيئًا بالسكوت |
| `lib/authContext.test.ts` | 401 لـ`tenantId: null` بدور مدير المنصة | نفسه، **والدور رمز قديم بقيمة لم تعد معلومة** |
| `mobile/lib/authFlow.rtl.test.tsx` | `platform_admin` بلا مسار من شاشة الدخول | **أي دور غير معلوم** ← رسالة عربية لا شاشة بيضاء |

**ولم يُضعَّف اختبار ولم يُحذف توقّع** — الأربعة تحوّلت من إثبات سلوكٍ زال إلى **إثبات المنع الذي حلّ محلّه**.

**وما بقي للدفعة التالية:** **لوحة المنصة المنفصلة** · **`POST /auth/platform-login`** بمسار وشاشة منفصلين ورسالة رفض لا تكشف الجدول (#147) · **التحقق بخطوتين TOTP على جهازين** (188) · **والاسترداد بطبقتين** (187) · **وإجراء إنشاء الحساب المقيَّد**.

**195. دخول مدير المنصة والتحقق بخطوتين والطبقة الأولى من الاسترداد**

**تنفيذ القرارات #147 و187 و188 و189 (§7-ب البند 25، شقّ الدخول). خادم فقط — ولا واجهة.**

**ما بُني:**

| الموضع | ما فيه |
| --- | --- |
| `platform_admins` + الترحيل `0011` | `totp_secret` و`must_change_password` |
| `scripts/platform-create-admin.ts` | `platform:create-admin` — يولّد كلمة مؤقتة وسرًّا، ويطبعهما مرة واحدة مع `otpauth://` |
| `lib/platformTotp.ts` | غلاف `otpauth` بالإعدادات القياسية |
| `services/platformAuthService.ts` | الدخول · الملف · تغيير الكلمة · إعادة تعيين مديرٍ آخر |
| `middleware/platformAuth.ts` | `requirePlatformAdmin` — النوع، والحساب الحيّ، وقيد الكلمة المؤقتة |
| `routes/platformAuth.ts` | `/platform/auth/login` · `/me` · `/change-password` · `/platform/admins/reset-password` |
| `shared/password.ts` | `PLATFORM_MIN_PASSWORD_LENGTH = 12` **ثابت منفصل** |

**والمسار خارج سلسلة `/api` كلها** — لا `requireAuth` ولا `requireTenant` ولا `enforceEntityAccess`، **وعنوان مختلف** (`/platform`) عليه `noStore` وحدّ محاولات **5/دقيقة كحدّ المستأجرين**. **ولم يُمَسّ مسار دخول المستأجرين بحرف.**

**١. الدخول خطوة واحدة — الهاتف وكلمة المرور ورمز TOTP في طلب واحد.** **ولا جلسة نصفية بين خطوتين:** جلسةٌ تقول «كلمتك صحيحة، هات الرمز» **تُثبت الكلمة قبل اكتمال التحقق**، فتصير أداة تحقّق من كلمات مسروقة بلا رمز.

**٢. والرفض واحد مهما كان سببه** (#147): هاتف غير موجود · كلمة خاطئة · رمز خاطئ ← **401 بنصّ رسالة دخول المستأجرين حرفيًّا**. **ويحرسه اختبار يقارن النصّين بالمساواة** — فتغيير أحدهما وحده يُسقط البناء، **لأن فرق الرسالة أداة تعداد لحسابات المنصة**.

**٣. مكتبة TOTP: `otpauth@9.5.1` — بنسخة مثبَّتة بلا `^`.** **وسرّ التحقّق ليس موضعًا تُرقّى فيه تبعية تلقائيًا**: ترقية صامتة في مولّد رموز **تكسر كل الأجهزة المسجَّلة دفعةً واحدة**. **وبالإعدادات القياسية بلا اختراع معاملات:** خطوة 30 ثانية · ستة أرقام · SHA-1، **ونافذة قبول خطوة واحدة قبل وبعد** — انحراف ساعة الجهاز لا انحراف السرّ. **وأي مخالفة تجعل الرمز يُرفض على جهاز المستخدم بلا رسالة تشرح.**

**٤. وسم نوع الرمز — ولماذا.** الحمولة تحمل `tokenType: "platform"`، **و`requireAuth` يرفض رمز المنصة على `/api`، و`requirePlatformAdmin` يرفض رمز المستأجرين على `/platform`** — **والرفض في الحالتين بنفس رسالة الرمز غير الصالح، بلا كشف السبب**. **والعلّة أن الفصل في التخزين بلا فصل في الرمز يترك نفس النقطة الواحدة التي يقرّر فيها سطرٌ من أنت** (#146): رمزٌ صالح التوقيع يمرّ على السلسلتين ما دام الحقلان متشابهين. **ورمزٌ بلا وسم يُقرأ رمز مستأجر** — الرموز الصادرة قبل الدفعة تعيش ثلاثين يومًا، **والافتراض لا يمنح شيئًا** إذ رمز المنصة وحده يحمل الوسم. **وبمفتاح توقيع واحد لا مفتاحين:** مفتاحان يعنيان إدارة مفتاحين وتدويرهما، **والوسم يكفي لأن الحارسين يقرآنه قبل أي شيء آخر**.

**٥. والكلمة المؤقتة تقيّد جلسة المنصة كما تقيّد جلسة المستأجر** — مساران مسموحان وحدهما (`/change-password` و`/me`)، بقراءة من `platform_admins` تحت كل طلب على نمط `requireLiveSession`: **تعطيل حساب يقطع جلسته القائمة ولا ينتظر انتهاء رمزه** (#99).

**٦. الطبقة الأولى من الاسترداد (187):** مديرٌ يعيد تعيين كلمة **مديرٍ آخر ولا نفسه** ← كلمة مؤقتة بـ`must_change`، **وسجلّ في `admin_audit_log`: الفاعل والهدف والوقت**. **ومنع النفس ليس تفصيلًا:** من يملك الجلسة يملك الكلمة أصلًا، **والأثر المكتوب يصير سطرًا يشهد لصاحبه**.

**وحدّان معلنان لا يُطويان:**

- **سرّ TOTP واحد يُمسح على جهازين — لا سرّان.** **وهذا هو تصميم القرار 188 وغايته: التوافر لا التعدّد** — ضياع أحد الجهازين لا يقفل الحساب. **وثمنه مذكور: نسخة ثانية من السرّ تعيش على جهاز ثانٍ**، وهو ما ارتضاه القرار صراحةً حين اشترط الجهازين.
- **«إشعار الثلاثة معًا» (شرط 187 الثاني) غير منفَّذ كإشعار** — **لا قناة بريد ولا دفع للمنصة بعد**. **والمنفَّذ اليوم قيدُ تدقيق يظهر لهم جميعًا حين تُبنى اللوحة**، وهو **أقلّ مما اشترطه القرار**: التدقيق يُقرأ عند النظر، والإشعار يصل بلا نظر. **يُسجَّل حدًّا معلنًا يُغلق مع اللوحة، لا يُحسب مستوفًى.**

**٧. والمخالفات المتعمَّدة — عشرة اختبارات**: رمز منصة على `/api/sites` ← يُرفض ولا يرى شيئًا · رمز مالك مستأجر على `/platform` ← 401 بلا كشف · هاتف مستخدم مستأجر من `/platform/auth/login` ← **401 بنصّ رسالة المستأجرين، مقيسًا بالمساواة** · رمز TOTP خاطئ مع كلمة صحيحة ← نفس الرد · كلمة خاطئة مع رمز صحيح ← نفس الرد · مديرٌ بـ`must_change` يطلب غير التغيير ← 403 ويمرّ بعده · وكلمة أقصر من 12 ← 400 · ومديرٌ يعيد تعيين نفسه ← 403.

**وأُثبت حملها:** بإبطال ثلاثة حرّاس مؤقتًا (فحص رمز TOTP · قيد الكلمة المؤقتة · منع إعادة تعيين النفس) **سقطت أربعة اختبارات**، وبإعادتها مرّت العشرة. **وواحد يُذكر بصدق:** اختبار «رمز المنصة على `/api`» **يبقى أخضر حتى بإبطال فحص الوسم في `requireAuth`** — لأن `requireTenant` يلتقطه بعده (رمز المنصة بلا `tenantId`). **فالوسم هناك بابٌ أول لا حارسٌ وحيد**، ولا يُدّعى له أكثر من ذلك.

**وما بقي لمرحلته:** **لوحة التحكم المنفصلة** (المرجع البصري §6.5 من وثيقة الأدوار الستة) · **وواجهات إدارة المستأجرين والاشتراكات** (#148 و#149) · **والطبقة الثانية من الاسترداد** — موقوفة على قرار مالك مكتوب في §7-ب البند 25.

**196. مفتاح الطوارئ — عمود نصّي لاسم المنفّذ بجانب `actor_id`، لا فراغٌ فيه ولا حساب وهمي**

**قرار مالك على سؤال مفتوح في القرار 195، وبه تكتمل الطبقة الثانية من الاسترداد (187). §7-ب البند 25.**

**السؤال:** `admin_audit_log.actor_id` **مفتاح أجنبي `NOT NULL` إلى `platform_admins`** (وهو ما أنشأه القرار 194 حين أزال استثناء المفتاح المركَّب) — **ومنفّذ الطوارئ ليس صفًّا في الجدول**، والسيناريو نفسه يفترض أن الجميع مقفولون.

**والمعتمد — الخيار الأول:**

> **عمود نصّي لاسم المنفّذ بجانب `actor_id`، ووسمٌ صريح يميّز صفّ الطوارئ.**
> **و`actor_id` يبقى `NOT NULL` مفتاحًا، ويحمل في صفّ الطوارئ معرّف المدير الذي أُعيد تعيينه — هدفَ الإجراء.**

**فالصفّ يجيب «ماذا جرى لمن، وبيد من» — بلا كذب على القيد.** `actor_id` يشير إلى **حسابٍ في النظام**، و`emergency_operator` يسمّي **إنسانًا خارجه**، **وهما سؤالان مختلفان لا يُجمعان في عمود واحد**.

**والوسم `is_emergency` صريح لا يُستنتج من امتلاء الاسم:** حقلٌ فارغ **قد يفرغ لخلل أو لتغيير لاحق**، **والوسم يقول ما جرى ولا يُحسب من غياب غيره**. وهو نظير `stocktakes.is_opening`: **الصفّ يُصنَّف بعموده لا بشكل بقية أعمدته.**

**وعلّة المرفوضين تُكتب كي لا يُعادا:**

| المرفوض | العلّة |
| --- | --- |
| `actor_id` قابل للفراغ | **يُضعف قيدًا صارمًا في كل السجلّ من أجل حالة واحدة** — ويجعل «مجهول الفاعل» **ممكنًا في كل صفّ** لا في صفّ الطوارئ وحده. **والقيد الذي يُفتح لحالة يبقى مفتوحًا لكل الحالات.** |
| حساب طوارئ مخصَّص | **يُنشئ هويةً تبدو حسابًا وليست حسابًا** — فيسأل قارئ السجلّ بعد سنة: من هذا؟ ومن يملك كلمته؟ **وحسابٌ لا صاحب له في نظامٍ كل فعل فيه منسوب** يفتح بابًا أوسع مما يسدّ. |

**والاسم مطلوب لا اختياري:** السكربت يرفض التنفيذ بلا اسم **قبل أي كتابة** — **وتنفيذٌ بلا اسم يجعل الأثر بلا صاحب، وهو نقيض غاية القرار 187 كلها** («الهدف ليس منع القفل، بل أن يترك كل استرداد أثرًا مكتوبًا لا يُمحى»).

**والكتابتان في معاملة واحدة:** تحديث الكلمة وكتابة الأثر معًا (المبدأ الثاني) — **وإخفاق الثاني بعد نجاح الأول يعيد تعيين كلمة بلا أثر**، وهي بعينها الحالة التي بُني المسار ليمنعها.

**وأُثبتت الذرّية بإخفاق حقيقي لا بحقنة اختبار:** اسم منفّذ أطول من `varchar(128)` **ترفضه القاعدة عند الإدراج** — في نفس الموضع الذي يهمّ — **والمقيس أن الكلمة لم تتغيّر وأن لا صفّ تدقيق كُتب**. **وأُثبت أن الاختبار حامل للحمل:** بإخراج الكتابتين من المعاملة **سقط وحده**، وبإعادتها مرّ.

**والسكربت `platform:emergency-reset` يُنفَّذ على الخادم مباشرة لا من الشبكة — ولا مسار API له إطلاقًا:** ملاذ أخير حين تسقط الطبقة الأولى، لا مسار عادي. **ومن يصل الخادم يملك المنصة أصلًا** (نصّ 187).

**وخمسة اختبارات:** الرفض بلا اسم بلا مساس بالكلمة · هاتف لا يقابله مدير ← 404 · التنفيذ السليم (الكلمة تتغيّر، و`must_change_password` يصير صادقًا، والأثر يحمل **الوسم والاسم والهدف**) · **والمدير بعده يدخل بالمؤقتة ويُحجب عن كل مسار عدا التغيير** · والذرّية.

**وبهذا اكتمل شقّ الاسترداد بطبقتيه** (187): الأفقية بين ثلاثة مديرين (القرار 195)، والطوارئ على الخادم (هنا). **ويبقى ما كُتب كما هو: اللوحة وواجهات الإدارة تُبنى في مرحلتها، و«إشعار الثلاثة معًا» يُغلق معها** — فهو اليوم قيدُ تدقيق لا إشعار.

**197. دورة التجهيز — اتساق المستأجر أولًا، ثم الفجوات الثماني بنيةً لا وصفًا**

**تنفيذ القرار #153 في المخطط (§7-ب البند 26). مخطط وترحيل فقط — لا مسار API ولا شاشة** (المرحلة 3).

**٠. وأولًا خرقٌ لم يكن في البند: الجداول الثلاثة كانت بلا `tenant_id` إطلاقًا.**

`house_prep_cycles` و`house_prep_steps` و`house_status_history` — **ومفاتيحها كلها مفردة** (`house_id → houses.id` · `cycle_id` · `completed_by` · `product_id` · `changed_by`). **وقاعدة `CLAUDE.md` ملزمة بلا استثناء:** كل مفتاح أجنبي بين جدولين يحملان `tenant_id` يجب أن يكون مركَّبًا — **والمفرد يتحقق من وجود الصف لا من مالكه**.

**وكيف نشأ — التاريخ يقرؤه لا التخمين:** الجداول أُنشئت في الترحيل **`0000` (2026-08-24)**، **وقاعدة المفتاح المركَّب رُسِّخت في `0007` (2026-08-25)** الذي حوّل **39 مفتاحًا**. **فالثلاثة كُتبت قبل القاعدة بيوم، ولم يشملها التحويل لأنها بلا `tenant_id` أصلًا** — **فلم تظهر في جرد «جدولين يحملان `tenant_id`»**. **والقاعدة لم تُخرق، بل مرّت من تحتها**: شرطُها لا ينطبق على جدول بلا العمود، **فغيابُ العمود أخفى الخرق بدل أن يمنعه**.

**والإصلاح على نمط `0007` لا نمط يُخترع:** `tenant_id NOT NULL` في الثلاثة، و**قيد تفرّد `(id, tenant_id)`** على `house_prep_cycles` (يشير إليه `house_prep_steps`)، **و12 مفتاحًا مركَّبًا** بدل المفردة. **والترتيب مقصود:** التفرّد قبل المفتاح الذي يشير إليه — و`drizzle-kit` ولّده بعده، فأُعيد ترتيبه يدويًا **كما فُعل في `0007` حرفيًّا**.

**والجداول الثلاثة فارغة في قاعدتَي التطوير والاختبار — قُرئ لا افتُرض** (`count(*) = 0` في الستة)، **فلا ترحيل بيانات ولا قيمة افتراضية مصطنعة**.

**١. الراحة: تُغيَّر البنية لا التفسير.** `rest_started_at` **يبقى** — الشرط الأول (مضيّ المدة) **يُقاس منه** — **ويُضاف الاكتمال**: `rest_confirmed_at` و`rest_confirmed_by`. **ولماذا اخترت هذا لا استبدال العمود:** حذف البداية يجعل «هل مضت المدة؟» غير قابل للحساب، **فالبنية تحتاج الطرفين لا أحدهما**.

**وثلاثة قيود تجعل الشرطين بنيةً لا تفسيرًا:**

| القيد | ما يمنعه |
| --- | --- |
| `rest_confirmed_at >= rest_started_at + rest_target_days` | **التأكيد قبل انقضاء المدة** — «لا التأكيد قبل انقضائها» (#153) مفروضًا في القاعدة |
| `(rest_confirmed_at IS NULL) = (rest_confirmed_by IS NULL)` | **تأكيد بلا صاحب** — كل أثر منسوب لمن صنعه |
| `rest_target_days >= 3` | النزول تحت الحدّ المطلق |

**والقيد الأول هو لبّ الإصلاح:** بلا مدة مستهدفة على الدورة **لا يمكن التعبير عن «مضيّ المدة» في القاعدة أصلًا** — فالفجوة (١) والفجوة (أ) **تُغلقان معًا أو لا تُغلقان**.

**٢. المستويان الثاني والثالث:** `farms.rest_days` (فارغة = **اتبع سياسة المستأجر**، لا صفر ولا «بلا راحة») و`house_prep_cycles.rest_target_days` (**بلا قيمة افتراضية**: المنشئ يحسب المستهدف ويثبّته، فلا تُفتح دورة بمدة لم يقرّرها أحد — نفس منطق 186). **وترتيب المستويات مكتوب في تعليق المخطط:** سياسة المستأجر أرضية، **والمزرعة ترفعها صعودًا فقط**، **والدورة تمدّد لهذه المرة وحدها**، **والنزول عن الأرضية للطبيب أو المالك وبسبب مكتوب**.

**٣. الحدّ الأدنى المطلق:** `ABSOLUTE_MIN_REST_DAYS = 3` في `@dawajin/shared` **ثابتًا في الكود لا صفًّا في جدول** — **صفٌّ يُحرَّر، والحدّ الأدنى المطلق لا يُحرَّر**. **ومكرَّر عمدًا في ثلاثة قيود `CHECK`** (`tenants.min_rest_days` · `farms.rest_days` · `rest_target_days`): **القاعدة لا تستورد TypeScript، والحارس الإجرائي وحده يُعيد الثقبَ أي مسار كتابة جديد لا يمرّ به**. **والتكرار مقصود ومكتوب: تغييره يلزمه تغيير موضعين — وهو المطلوب.**

**٤. الاعتماد — حقلان لا حقل:** `approved_at` و`approved_by` بجانب `completed_*`، **بقيدين**: لا اعتماد لخطوة لم تُعلَّم، ولا اعتماد بلا صاحب. **وقيد ثالث أُضيف قياسًا لا نصًّا — ويُذكر كذلك:** `approved_by <> completed_by`. **علّته #155 («من يُدخل رصيدًا لا يصادق عليه») و#153 («تعليم غيره عنه يجعل الأثر كذبًا»)**، **وهو استنتاج لا نصّ صريح في #153** — **يُسقطه المالك بكلمة إن كان يريد للمشرف أن يعتمد ما نفّذه بنفسه.**

**٥. الإسناد والمدة المستهدفة على الخطوة:** `assigned_to` (مفتاح مركَّب) و`target_hours`. **وبالساعات لا بالأيام**: خطوات التنظيف والتطهير **تُنجَز داخل اليوم**، ومدة بالأيام **تجعل «ساعتان» غير قابلة للتعبير**. **والراحة وحدها بالأيام لأنها انتظار لا فعل.**

**٦. أثر تعديلات المدة — لا جدول جديد، والقائم يكفي.** الثلاثة تُسجَّل في السجلات القائمة بأعمدتها الموحَّدة (`before`/`after`/`reason`، القرار #49):

| المستوى | السجل | `entity_type` |
| --- | --- | --- |
| سياسة المستأجر | `settings_audit_log` (المسار قائم فعلًا في `settingsService`) | `setting` |
| مدة المزرعة | `entity_audit_log` | `farm` |
| مدة الدورة | `entity_audit_log` | `house_prep_cycle` |

**ولماذا لا جدول رابع:** «من · من أي رقم إلى أي رقم · ولماذا» **هو بعينه ما تحمله الأعمدة الموحَّدة** — `actor_id` و`before`/`after` و`reason`. **وجدولٌ رابع يعني رابع موضع يُقرأ منه تاريخ التغيير، ونسيانَه في أول تقرير يجمع الأثر.** **وما يبقى للمرحلة 3: إلزام `reason` عند التقصير** — العمود موجود ونُلّه مسموح في القاعدة، **والإلزام حكم مسار لا حكم عمود**، ويُبنى مع المسار.

**٧. §12.2 — صفّان لا صفّ:** «اعتماد خطوة تجهيز» (منفصل عن «خطوة تجهيز» لأن المنفّذ غير المعتمد)، و«تعديل مدة الراحة» بصلاحياته الأربع. **و§3.3 صُحِّحت:** «10 أيام إلزامية» صارت **سياسة بثلاثة مستويات بحدٍّ أدنى مطلق**، والافتراضي 10 مذكور بوصفه افتراضًا لا إلزامًا. **وهما مواصفتان ملزمتان، وتعديلهما هنا مقصود ومعلن: القرار #153 يعلوهما، والنصّ القديم كان يناقضه.**

**٨. والإثبات على القاعدة نفسها — أحد عشر اختبارًا** (`src/schema/housePrep.integration.test.ts`)، **ولا اختبار على منطق لم يُبنَ**: دورة تشير إلى عنبر مستأجر آخر ← تُرفض · صفّ بلا `tenant_id` ← يُرفض · خطوة يعتمدها مستخدم من مستأجر آخر ← تُرفض · اعتماد خطوة لم تُعلَّم ← يُرفض · المنفّذ يعتمد نفسه ← يُرفض · تأكيد الراحة قبل انقضاء المدة ← يُرفض · وبعدها ← يُقبل والاكتمال مسجَّل بصاحبه · تأكيد بلا صاحب ← يُرفض · ومدة يومين أو سياسة بيوم أو مزرعة بيومين ← كلها تُرفض.

**وما يبقى للمرحلة 3 صراحةً:** **المسارات والشاشة** · **إلزام السبب المكتوب عند التقصير** · **حساب المستهدف من المستويات الثلاثة عند فتح الدورة** · **وحصر التقصير بالطبيب والمالك** (§12.2 يصفه، والفرض يُبنى مع المسار). **والسؤال المفتوح في #153 يبقى مفتوحًا: هل يُجهَّز عنبرٌ واحد في كل مرة أم أكثر؟** — لا يمسّه هذا المخطط.

**198. نموذج المخازن — المخزن بمستوى، وأمين المخزن دورًا، والافتتاحي بشاهدين**

**تنفيذ القرارين #161 و#157 في المخطط (§7-ب: بقيّة البند 32، وشقّا البند 28 القائمان). مخطط وترحيل فقط — لا مسار API ولا شاشة** (المرحلة 3).

**١. المخزن كيان واحد بمستوى.** `warehouses` صار يحمل `level` (**مركزي · موقع · عنبر**) و`site_id` و`house_id`، **بقيد يفرض أن كل مستوى يحمل مرجعه وحده**: المركزي بلا موضع، والموقع بـ`site_id`، والعنبر بـ`house_id` — **على نمط `user_assignments_one_level_ck`**. **والمستوى حقل فالتوسع إعداد لا برمجة** (#161 «أولًا»). **ولا مستوى «مزرعة»** (#161 «ثالث عشر» البند ٣).

**وقيود #161 الثلاثة — اثنان في القاعدة وواحد حكم مسار:**

| القيد | أين |
| --- | --- |
| **مخزن العنبر واحد لكل عنبر** | فهرس فريد **جزئي** على `house_id` (جزئي لأن `NULL` في الفهرس الفريد «مميّزة دائمًا» فلا تمنع تكرار صفوف المستويات الأخرى — علّة #128) |
| **لا يُلغى مخزن فيه رصيد** | **حكم مسار لا قيد عمود**: الرصيد **محسوب من الدفتر لا مخزَّن**، فالحارس **قراءة مجموع تحت قفل** (#161 «التغطية الجزئية ٣») — **سُجِّل للمرحلة 3 ولم يُبنَ** |
| **لكل مخزن صاحب مسؤول** | **يُشتق من المستوى ولا يُخزَّن عمودًا** (#161 «ثانيًا»): المركزي بصفّ إسناد لأمين المخزن، ومخزن الموقع للمشرف المسؤول عنه، ومخزن العنبر لمربّيه |

**ولماذا لا عمود «صاحب»:** الاشتقاق منصوص في #161، **وعمودٌ ثالث يتيح تعيينًا يناقضه** — فيصير للمخزن صاحبان: واحد بالجدول وواحد بالقاعدة. **وضمانُ أن لكل مخزن صاحبًا فعليًّا حكمُ مسار** (لا يُنشأ مخزن مركزي بلا إسناد) — **سُجِّل للمرحلة 3**.

**و«مخزن العنبر يُنشأ مع العنبر تلقائيًا» لم يُبنَ — وقُرئ لا افتُرض:** `housesService.createHouse` **لا ينشئ مخزنًا اليوم**، **وموضع الكتابة واحد في المشروع كله** (#152) فالخطّاف تعديل في دالة واحدة. **حكم مسار للمرحلة 3.**

**٢. أمين المخزن (`storekeeper`) — أول دور يُضاف بعد قلب الافتراض.** والشرط الذي وضعه #161 («لا يُضاف أي دور جديد قبل قلب افتراض الحارس») **مستوفًى بالقرار 184**: **الدور يصل محجوبًا لا مفتوحًا**.

**وأين يُدرَج — قراءةً من #161 «سابعًا» لا اجتهادًا:** «يرى مخزنه وأوامر الصرف الصادرة منه والواردة إليه فقط. **لا يرى أرصدة العنابر ولا بيانات الدفعات**». **فهو ليس في `FULL_VISIBILITY_ROLES` ولا في `ASSIGNMENT_SCOPED_ROLES`** — **والقائمتان تحكمان رؤية المزارع والعنابر، ونطاقه محورٌ آخر: المخازن**. وبحكم القرار 184 **يُرجع له `visibleFarmCondition` شرطًا كاذبًا و`enforceEntityAccess` يرفضه على كل كيان** — **وهو المطلوب حرفيًّا: لا يرى عنبرًا ولا مزرعة ولا دفعة**.

**وكيف يُسند:** `user_assignments.warehouse_id` **مستوًى ثالثًا**، والقيد صار «**واحدٌ بالضبط من ثلاثة**» بدل «أحدهما»، **ومعه قيد استبعاد تداخل ثالث** على نمط أخويه (القرار 190) — **والترتيب الذي فرضه #161 «ثالث عشر» البند ١٠ محفوظ**: الإسناد بمدة أولًا (منفَّذ)، **ثم مستوى المخزن فوقه**. **وسؤال «مخزن بعينه أم عدة أم الشركة كلها» يبقى للمالك** (#161 «حادي عشر» السؤال ١) — **والبنية تحتمل الثلاثة بلا ترجيح**.

**٣. والوضعية المؤقتة في `enforceEntityAccess` تغيّرت كما وُعد.** كان الفرع «المالك وحده يمرّ على المخزن **حتى يُبنى النموذج**» (القرار 193، والتعليق يسمّي نفسه) — **وصار: المالك بحكم رؤيته الكاملة، أو من يبلغه إسناد هذا المخزن ساريًا اليوم** (نفس `assignmentActiveToday`، القرار 190). **ولم يُمَسّ شيء آخر في الملف.**

**وحدٌّ معلن:** **وصول المشرف إلى مخزن موقعه بحكم إسناد مزارعه لم يُبنَ.** #161 يجعله صاحبه، **ولا يُشتق ذلك من صفوف الإسناد القائمة بلا حكم مسار مكتوب** (الموقع قد يضمّ مزارع عدة، ومن منها يمنحه المخزن؟) — **فيبقى للمرحلة 3، أو يُسنَد المخزن صراحةً كأي مخزن آخر.**

**٤. الرصيد الافتتاحي (#157 البند ٢):** `stocktakes` صار يحمل `approved_by`/`approved_at` و`closed_by`، **بثلاثة قيود**: **المصادِق غير المُدخِل** (`approved_by <> opened_by` — المبدأ #155، نفس قيد اعتماد خطوة التجهيز في القرار 197) · ولا مصادقة بلا صاحب · ولا إغلاق بلا من أغلقه.

**وأين يُفرض منع الافتتاحي الثاني — قرارٌ بعلّته:** #157 يقول «قيد يمنع افتتاحيًا ثانيًا **بعد أول حركة**»، **وشرط «بعد أول حركة» يقرأ جدولًا آخر فلا يُعبَّر عنه في `CHECK`**. **فالمفروض في القاعدة أقوى: فهرس فريد جزئي يمنع افتتاحيًّا ثانيًا لكل موضع أبدًا** — **والأقوى لا يسمح بما منعه القرار**، وعلّة القرار تسنده: «الافتتاحي الثاني **يمحو كل ما قبله بلا أثر** — وهو المحو الوحيد الممكن في نظام دفتره غير قابل للحذف». **وما فقدناه بالتشديد: تصحيح افتتاحي خاطئ قبل أي حركة** — **حكم مسار يُبنى إن أُريد التساهل فيه، ويُسجَّل الآن لا يُكتشف لاحقًا.**

**٥. حقول الاستلام الثلاثة — على الحركة لا على الصنف.** `inventory_movements` صار يحمل `received_expiry_date` و`received_withdrawal_days` و`received_storage_conditions`. **والعلّة منصوصة في #161 «ثالث عشر» البند ٤:** «تاريخ الصلاحية **خاصية عبوة لا صنف** — يُسجَّل على دفعة التوريد لا على المنتج، **وحركة الاستلام هي حاملة التاريخ**». **و`products` يبقى حاملًا لقيم الصنف الافتراضية، وهذه ما وصل فعلًا في هذه العبوة** — نفس نمط «السائد وقت الإدخال» في `daily_log_feed_rows.bag_weight_kg`.

**وحدٌّ معلن لا يُطوى:** **لا مفهوم «عبوة» (lot) في المخطط بعد**، **فالرصيد يبقى بركةً واحدة**: الحركة تعرف تاريخ صلاحية ما وصل بها، **ولا تعرف أيّ الكميات المصروفة جاءت منها**. **فمنع صرف المنتهي (#161 «سادسًا») قابل للتنفيذ تقريبًا لا يقينًا** بهذه البنية — **يُسجَّل صراحةً، ويُحسم مع بناء مسارات المخزون: إما كيان عبوة، وإما صرف بالأقدم صلاحيةً (FEFO) محسوبًا من الحركات.**

**٦. وفئات الأصناف صارت ستًّا** بفصل «معقمات ومطهرات» عن «مستلزمات» (#161 «ثالث عشر» البند ٧) — **وحدّ ما يحمله مخزن العنبر بالفئة لا بكتلة**، وكتلةٌ تضمّ المطهر والمعدّة الإنشائية معًا لا تُفرَض عليها قاعدة.

**٧. وخرقٌ مخفيّ من صنف القرار 197 في جدول تلمسه الدفعة:** `stocktake_items` **بلا `tenant_id` ومفاتيحه مفردة** — **فلم يظهر في جرد `0007`**، بنفس علّة جداول التجهيز أمس. أُصلح هنا لأنه من عائلة ما تلمسه الدفعة. **وثلاثة أخرى من نفس الصنف لم تُلمس ولم تُطوَ — تُسجَّل بأسمائها:** `daily_log_feed_rows` · `log_notes` · `health_task_executions` — **كلها بلا `tenant_id` ومفاتيحها مفردة**، **ودفعة مستقلة لأنها ليست من عائلة المخازن.**

**٨. الإثبات على القاعدة — اثنا عشر اختبارًا** (`src/schema/warehouseModel.integration.test.ts`): مستوى «عنبر» بلا مرجع ← يُرفض · بمرجعين ← يُرفض · مركزي بموضع ← يُرفض · مخزنان لعنبر واحد ← الثاني يُرفض · مخزن يشير إلى عنبر مستأجر آخر ← يُرفض · إسناد بمستويين ← يُرفض · إسناد بلا مستوى ← يُرفض · وتداخل مدّتين على نفس المخزن ← يُرفض · وافتتاحي يصادق عليه مُدخِله ← يُرفض · وبمستخدم آخر ← يُقبل · وافتتاحيّ ثانٍ لنفس الموضع ← يُرفض · وإغلاق بلا من أغلقه ← يُرفض.

**٩. وما لم يُبنَ في هذه الدفعة — صراحةً:** لا مسار API ولا شاشة · **ولا لقطات الرصيد الدورية** (§7-ب البند 45 — تُصمَّم مع المسارات) · **ولا الناقل ولا المورّد كيانين** (#157 البند ٣ و#161 «ثالث عشر» البند ٩ — يمسّان الشحنات وسجل الزيارات، دفعة مستقلة) · **ولا الأعمال الستة بلا صاحب** · **ولا الأكياس الفارغة ومعادلتها** (#161 «عاشرًا») · **ولا وزن الكيس بمصدر واحد** (#161 «ثالث عشر» البند ٥ — تعارضٌ ثلاثي المصادر لم يُحلّ).

**١٠. وفارقٌ وجدته بين ما طُلب وما تنصّ عليه القرارات — أقف عنده ولا أجتهد:** **`LOCATION_TYPE` والقيد المبنيّ عليه في `inventory_movements`** (`location_type='house' AND location_id = house_id`) **يناقضان «مخزن العنبر كيان له معرّفه الخاص»** (#161 «ثاني عشر» التعارض الأول، وهو موسوم 🔴 هناك: «**القيد يرفض النموذج الجديد لا يستوعبه**»). **والقسمة ترث إلى أربعة جداول** — `inventory_movements` · `stocktakes` · `wastage` · `inventory_transfers` — **أي أنها تمسّ الدفتر نفسه وثابته في §13.3**. **ولم أغيّرها في هذه الدفعة**: تحويل عنونة الدفتر إلى `warehouse_id` قرارُ نموذجٍ لا تفصيلُ مخطط، **والجداول فارغة اليوم فكلفته أدنى ما تكون** — **يُعرض على المالك دفعةً مستقلة**.

**199. عنونة دفتر المخزون بمخزن واحد — لا زوج نوع ومعرّف**

**تنفيذ ما سجّله القرار #161 «ثاني عشر» البند ١ تعارضًا أحمر، وأتمّه القرار 198. مخطط وترحيل وطبقة حساب وحارس — ولا مسار API ولا شاشة.**

**العلّة مكتوبة سلفًا:** القيد القائم

```sql
CHECK ( (location_type='house' AND location_id = house_id AND house_id IS NOT NULL)
        OR (location_type='warehouse' AND house_id IS NULL AND farm_id IS NULL) )
```

**يجعل معرّف الموقع هو معرّف العنبر نفسه**، **ومخزن العنبر صار كيانًا في `warehouses` له معرّفه** (القرار 198) — **فالقيد يرفض النموذج الجديد لا يستوعبه**. **والقسمة ترث إلى أربعة جداول**: `inventory_movements` · `stocktakes` · `wastage` · `inventory_transfers`.

**والبديل المرفوض — إبقاء الزوج وإضافة `warehouse_id` بجانبه:** يجعل **للموضع الواحد عنوانين**، **وكل قارئ يختار أيّهما يصدّق**؛ ويحتاج قيدًا يربطهما يعيد إنتاج القيد الذي جئنا نحذفه. **وعنوانان لموضع واحد في دفترٍ لا يُعدَّل** يعني أن تصحيح أحدهما لاحقًا **يترك الآخر شاهدًا كاذبًا**.

**ولماذا الآن:** الجداول الأربعة **فارغة في قاعدة التطوير**، ولا مسار مخزون مبنيّ يكتب فيها — **فالكلفة أدنى ما تكون**، وبعد أول بيانات حقيقية تصير **ترحيل دفترٍ لا يُعدَّل**.

**١. `farm_id` و`house_id` حُذفا من الدفتر — بحكم مكتوب لا بالسكوت.**

**مزرعة الحركة وعنبرها يُشتقّان من مخزنها** (`warehouses.house_id → houses.farm_id`)، **وإبقاؤهما عمودين يعني مصدرين للحقيقة الواحدة** — **والقيد الذي كان يمنع تناقضهما هو نفسه ما حُذف**، فتبقى الأعمدة بلا حارس. **وهو نظير قاعدة الدفتر نفسها** (#14): لا عمود يحمل ما يمكن اشتقاقه.

**وما يخسره الاستعلام يُذكر لا يُخفى:** «كل حركات مزرعة» صارت **وصلتين** (`movements → warehouses → houses`) بدل عمود مباشر. **والثمن مقبول لأن البديل عمودٌ يكذب بلا أن يشتكي أحد**، ولأن الفهرس الجديد `(warehouse_id, product_id)` يخدم السؤال الشائع — **رصيد صنف في مخزن** — بلا وصلة.

**و`batch_id` يبقى كما هو:** بُعدٌ أعمالي (أي دفعة استهلكت) **لا عنونة موضع**، ولا يُشتقّ من المخزن.

**٢. `LOCATION_TYPE` حُذف من القاعدة ومن `packages/shared`** — **ولم يبقَ له مستهلك واحد**: الجداول الأربعة تحوّلت، و`computeBalance` والحارس معها. **ويُذكر مكانه في `enums.ts` تعليقًا** كي لا يُعاد استحداثه ظنًّا أنه سقط سهوًا.

**٣. الترحيل ينقل البيانات صراحةً لا اعتمادًا على الفراغ — بثلاث حالات مرتَّبة:**

| الحالة | العنونة |
| --- | --- |
| `location_type='house'` | مخزن ذلك العنبر — **و`location_id` هو معرّف العنبر بحكم القيد القديم نفسه**، فالربط يقين. ويُنشأ المخزن بمستوى «عنبر» إن لم يوجد |
| `location_type='warehouse'` ومعرّفه يقابل مخزنًا في نفس المستأجر | **هو هو** — العنونة نقلُ اسمٍ لا تغييرُ موضع |
| `location_type='warehouse'` ومعرّفه لا يقابل مخزنًا | المخزن المركزي للمستأجر، ويُنشأ إن لم يكن |

**والحالة الثانية تخالف حرفية التكليف** («كل صفّ `warehouse` يُعنون إلى المركزي») — **والمخالفة مقصودة ومكتوبة**: `location_id` في تلك الصفوف **كان معرّف مخزن فعليًّا** (القيد القديم لم يقيّد هذه الحالة)، **فالعنونة إلى المركزي كانت ستدمج مخازن متعددة في واحد فتُتلف أرصدةً كانت منفصلة**. **وأُثبت الفرق على قاعدة غير فارغة**: مستأجر بمخزنين مركزيين بقي رصيداهما منفصلين (100 و40) ولم يُجمعا في 140.

**وثلاثة حرّاس في الترحيل نفسه:** الأعمدة **تُضاف قابلة للعدم** · ثم النقل · **ثم فحصٌ يوقف الترحيل بخطأ صريح إن بقي صفّ واحد بلا مخزن** · ثم `SET NOT NULL`. **وبلا الحارس يمرّ الترحيل على قاعدة فارغة ويسقط على قاعدة فيها بيانات — فيصير صحيحًا في الاختبار خاطئًا في الإنتاج.**

**والإثبات بأرقام على قاعدة فحص مستقلة** (`dawajin_migration_check`، بُنيت حتى الترحيل 0014 ثم غُذّيت بخمس حركات وجرد وهالك وتحويل): **الترحيل مرّ بلا خطأ · صفر صفّ بلا `warehouse_id` · وثابت §13.3 صامد**: مجاميع كل (مستأجر · صنف) **متطابقة حرفيًّا قبل وبعد**، **وعدد مجموعات المواضع خمسٌ قبل وخمسٌ بعد** — فلا دمج ولا تفكيك.

**٤. طبقة الحساب والحارس.** `computeBalance` تأخذ `warehouseId` بدل الزوج — **والثابت §13.3 نفسه لم يتغيّر** (مجموع الحركات = مجموع أرصدة كل المخازن)، **ويحرسه اختباران** بالدالتين الحيّتين لا بعمود مخزَّن.

**وحقول الحارس صارت ثلاثة بلا نوع:** `warehouseId` · `fromWarehouseId` · `toWarehouseId`، **و`assertWarehouseAccess` نقطة الفرض الوحيدة**. **والطرفان معًا في التحويل لا أحدهما** (القرار 193). **وقيمة غير معلومة تُرفض بـ403 ولا تُمرَّر صامتة** — نصٌّ ليس رقمًا أو صفرٌ أو سالب **لا يشير إلى مخزن**، وهو نظير `locationType='silo'` الذي رفضه 193.

**٥. ومخزن العنبر يُحلّ بإسناد عنبره** (#161 «ثانيًا»: صاحبه مربّيه): `assertWarehouseAccess` **تستدعي `assertHouseAssignment` نفسها** لا نسخة ثانية منها — **فلا حكم إسناد مكتوب مرتين ولا شرط «سارٍ اليوم» (القرار 190) منسوخ**. **وبه يُغلق أحد الحدَّين المعلنين في القرار 198.**

**والحدّ الثاني يبقى معلنًا كما هو:** **وصول المشرف إلى مخزن موقعه** بحكم إسناد مزارعه — **لا يُشتقّ بلا حكم مسار مكتوب** (الموقع قد يضمّ أكثر من مزرعة، #113)، فيبقى للمرحلة 3 أو يُسنَد المخزن صراحةً. **ومنع صرف المنتهي صلاحيته يبقى معلنًا كذلك** — يحتاج نموذج عبوة (lot) لم يُقرَّر بعد.

**٦. وأُثبت أن الحارس حامل للحمل:** بإبطال `assertWarehouseAccess` مؤقتًا **سقطت ثمانية اختبارات من ثلاثة عشر**، وبإعادته مرّت الثلاثة عشر.

**200. وسمُ إغلاق عُمِّم فأخفى دَينًا — وما بقي يُسمَّى بالاسم أو لا يُوسَم البند**

**واقعة تُسجَّل بلفظها، والقاعدة مستخلَصة منها لا مُملاة عليها.**

**ما جرى:** §7-ب البند 32 وُسم بعد القرار 198 بـ«**مُغلَق … وما بقي مساراتٌ لا مخطط**»، وذُيّل بأربعة أشياء باقية كلها مسارات أو سؤال مالك. **وثلاثةٌ من صنف آخر كانت خارج القائمة**، وكلّها مخطط لا مسار:

| الباقي | واقعه في الكود |
| --- | --- |
| **جدول طلبات المربّي** (#161 «خامسًا»، والتغطية ٤) | **لا جدول إطلاقًا** — والقرار يجعل الطلب كيانًا يُربط بأمر الصرف تلقائيًّا |
| **المورّد والناقل كيانين** (ثالث عشر ٩) | `products.supplier varchar(160)` · `shipments.carrier_name varchar(128)` — **نصّان حرّان** |
| **الأكياس الفارغة** (عاشرًا وثالث عشر ٨) | **لا أثر لها**، ومعادلة التحقق («الممتلئ المستلم = الفارغ + المتبقي») تقوم عليها |

**ومعها قرارات مالك لم تكن في الوسم:** وزن الكيس بمصادره الثلاثة — **ونصّ #161 «ثالث عشر» ٥ يقول «يُحسم قبل البناء»** — والأسئلة الخمسة في «حادي عشر»، و«انقطاع تبريد» بلا مكان يُنسب إليه (#157 البند ٥).

**والعلّة في شكل الجملة لا في نسيان كاتبها:**

> **«وما بقي مساراتٌ لا مخطط» دعوى عن كل ما لم يُذكر.**
> **والقائمة تُكذَّب ببندٍ واحد خارجها، والتعميم لا يُكذَّب حتى يُقرأ القرار كله من جديد.**

فالقائمة **تدعو قارئها إلى المقابلة**، والتعميم **يعفيه منها** — ومن يقرأ «ما بقي مسارات» يخطّط دفعةَ مساراتٍ ويكتشف الجدول الناقص **بعد أن يبني عليه**.

**وثانية تُذكر:** الوسم كُتب **في نفس نفَس الإنجاز** — بيد من أغلق الدفعة، ساعةَ أغلقها. **فورث تفاؤلها**: ما نُفِّذ حاضرٌ في الذهن، وما لم يُقرأ في تلك الجلسة غائب. **وليست هذه غفلة فرد** — من يقيس عمله ساعة فراغه منه يقيسه بأحسن حالاته.

**والقاعدة المستخلَصة:**

> **١. لا يُوسَم بندٌ بإغلاق ما دام فيه باقٍ لم يُسمَّ باسمه.** والوسم يُسمّي الباقي أو لا يُكتب.
> **٢. والباقي يُصنَّف قبل أن يُوصف — مخطط · مسار · قرار مالك.** فالثلاثة تُجدوَل بثلاث طرق مختلفة، **وخلطها في «ما بقي» واحدة يُخفي أثقلها**: قرارُ المالك يسبق البناء، والمخطط يسبق المسار، **والمسار وحده هو ما يُجدوَل مع الشاشة**.
> **٣. ولا يُوسَم بندٌ من ذاكرة الدفعة، بل بمقابلة القرار المصدر بندًا بندًا.**

**ولماذا الصنف الثالث تحديدًا:** بندٌ في «قرار مالك» **ينتظر إنسانًا لا وقتًا**. وإن دُفن في «ما بقي مسارات» **لم يصل إلى من يحسمه**، فيُكتشف يوم البناء **فيوقفه** — وهي أغلى لحظة يُكتشف فيها.

**وما فُعل الآن:** البند 32 أُعيد وسمه «**مُغلَق جزئيًّا**» بثلاث خانات مسمّاة، **وخُتم بأن لا وسم إغلاق ما دامت (أ) و(ج) غير فارغتين**؛ والبند 28 صُحّح بما تغيّر تحته بالقرارين 198 و199؛ **وثلاثة بنود انتقلت إلى «ينتظر قرار المالك» في `docs/STATE.md`** — وهي الخانة التي كان التعميم يحجبها.

**وذيلٌ للحادثة نفسها — التصحيح ترك بقيّة:** بعد إعادة وسم البند 32 بقيت في **نفس الخلية** جملةٌ من الوسم السابق — «**وبقية البند (نموذج المخازن بمستوًى واحد بثلاث قيم) قائمة كما هي**» — **وقد بُني النموذج بالقرار 198، والسطر الأول من الرأس نفسه يقرّ به**. **فصار البند يناقض نفسه داخل خليةٍ واحدة**، وهو عين ما أُصلح في البند 28 قبله بساعة.

**والعلّة أن التصحيح لمس رأس الخلية وذيلها وترك وسطها:** الخلية طويلة، **والنصّ المحفوظ للسياق فيها لا يُميَّز عن الدعوى القائمة إلا بقراءته كاملًا** — فيمرّ سطرٌ قديم بوصفه حاضرًا. **فتُضاف إلى القاعدة رابعة:**

> **٤. مراجعة الوسم لا تكتمل بلا مسح الخلية كلها — والنصّ المحفوظ يُعلَّم بحدّيه** («النصّ الأصلي للسياق» … «انتهى النصّ المحفوظ»)، **فما بينهما يُقرأ تاريخًا، وما خارجهما وحده يُقرأ حالة**.

**وثالثةٌ تُسجَّل لأن تكرارها هو الملاحظة:** التصحيح **لم ينتشر إلى `docs/STATE.md`** — فبقي فيه «**وبه أُغلقت بقيّة البند 32 وشقّا البند 28**» و«**وما بقي مساراتٌ لا مخطط**» **بلفظها الذي كُتب القرار ضدّه**، **بينما قسم «ينتظر قرار المالك» في نفس الملف يسمّي ثلاثة بنود ليست مسارات** — فالملف يناقض نفسه بين قسمين متجاورين.

**وهذا أخطر مواضع البقيّة الثلاثة**: `STATE.md` **يُقرأ أول كل جلسة بحكم تعريفه**، و`work-plan.md` و`decisions.md` **لا يُقرآن إلا عند الحاجة إليهما**. **فالدعوى المُصحَّحة كانت تُقرأ يوميًّا، والقرار الذي يكذّبها ينتظر من يفتحه** — والمدة بينهما هي المدة التي يُخطَّط فيها على وسمٍ باطل.

**والقاعدة الخامسة — أين يُبحث عن أثر الوسم بعد تصحيحه:**

> **٥. الوسم لا يُصحَّح في موضعه بل في مسار انتشاره: من البند إلى كل ما نقل خلاصته.**
> **ويُبدأ بالأقرب إلى القارئ لا بالأقرب إلى الكاتب** — `STATE.md` أولًا لأنه أول ما يُقرأ، ثم مداخل المراحل في `work-plan.md`، ثم القرارات التي أحالت إليه.
> **والبحث بلفظ الوسم الملغى نصًّا** (`grep`)، لا بتذكّر مواضعه: **الجملة المنسوخة تُوجد بلفظها، والذاكرة تُوجد ما كُتب لا ما نُقل.**

**والثلاث معًا تقول شيئًا واحدًا:** وسمٌ خاطئ **يترك أثرًا أوسع من موضعه** — في رأس الخلية، وفي وسطها، وفي كل ملف نقل خلاصتها. **فكلفة التعميم لا تُدفع مرة**، وهي وحدها كافية لتفضيل قائمةٍ تُكذَّب على جملةٍ تُريح.

**ولا كود في هذه الدفعة** — مراجعة سجل، على نفس فرع القرار 199.

**201. وزن الكيس ثابتٌ بمصدر واحد — والإعداد يُحذف، واللقطة المجمَّدة ليست استثناءً**

**قرار مالك يحسم تعارضًا مسجَّلًا منذ #161** («ثالث عشر» ٥ و«ثاني عشر» التعارض السادس): **وزن كيس العلف ثابت ٥٠ كجم ولا يتغيّر.**

**العلّة — ثلاثة مصادر لرقم واحد:**

| المصدر | ما كان | ما صار |
| --- | --- | --- |
| `products.package_size` | حجم عبوة على الصنف، **قابل للعدم** | **المصدر الوحيد** — والعلف بلا قيمة يأخذ ٥٠ |
| `tenants.feed_bag_weight_kg` | `numeric(6,2) NOT NULL DEFAULT 50` — إعداد يملك المالك تغييره | **محذوف** عمودًا وترحيلًا وكلَّ مستهلك |
| `daily_log_feed_rows.bag_weight_kg` | «السائد وقت الإدخال» | **باقٍ كما هو** — لقطة مجمَّدة لا مصدر |

**ورقمٌ واحد بثلاثة مواضع ليس تكرارًا يُحتمَل بل ثلاثة أجوبة مختلفة لسؤال واحد**، والذي يُقرأ منها يقرّره من كتب المسار — **فيختلف حساب مسارين عن نفس الكيس، ولا يكشفه اختبار لأن كليهما «صحيح» في موضعه**.

**١. لماذا يُحذف الإعداد ولا يُترك ساكنًا.** إعدادٌ لا يُقرأ منه أحد اليوم يبدو غير ضارّ — **والضرر ليس في استعماله بل في وجوده**: إعدادٌ يملك المالك تغييره **يناقض «ثابت» من حيث المبدأ**، وأول تغيير يُعيد التعارض كاملًا في نظام صار يفترض أنه محسوم. **والحذف الآن كلفته عمود ومسار إعدادات؛ وبعد أول مسار مخزون يقرؤه تصير كلفته مراجعة كل ما بُني فوقه.** والمحذوف بالكامل: العمود · حقله في `PATCH /settings` وطبقة الخدمة · مخططا OpenAPI · بذر العرض · سبعة تجهيزات اختبار · ومفتاحه في قاعدة ESLint `no-magic-config-number` (لم يعد إعدادًا فلا يُحرَس كإعداد).

**٢. والافتراضي في القاعدة لا في منطق الحساب** — **ولا رقم ٥٠ مكتوبًا في أي مسار**. **و«٥٠» تجيب عن كيس العلف وحده:** الدواء واللقاح والمطهّر تختلف عبواتها، **فالحقل يبقى على الصنف ولا يُحوَّل إلى ثابت عام**.

**وهذا وحده يمنع `DEFAULT 50` على العمود** — لأنه **يُعطي اللقاحَ خمسين بالسكوت**، وهو عين ما نُهي عنه: موضعٌ يفترض ٥٠ لكل الأصناف. **والافتراضي المشروط بعمود آخر لا يُعبَّر عنه بـ`DEFAULT` في Postgres**، فالمعالجة **مُشغِّل يملأ وقيدٌ يضمن**:

- `products_feed_package_size_default_trg` — `BEFORE INSERT OR UPDATE`: صنف علف بلا حجم عبوة يأخذ ٥٠. **وعلى التعديل كما على الإدراج** كي لا يُمحى الافتراضي لاحقًا بتعديلٍ يمرّر عدمًا فيعود الصنف إلى «بلا وزن» من باب آخر.
- `products_feed_package_size_ck` — `category <> 'علف' OR package_size IS NOT NULL`: **ضامنٌ لأثر المُشغِّل لا بديلٌ عنه**، فلو عُطِّل المُشغِّل يومًا سقط الإدراج بدل أن يمرّ صامتًا.

**وهذا أول مُشغِّل في المستودع، ويُسجَّل لفظًا** — والبديل الوحيد المتاح هو ملء الرقم في طبقة الخدمة، **وهو الرقم في منطق الحساب الذي مُنع بنصّ الحكم**.

**٣. ولماذا `bag_weight_kg` ليس استثناءً من «مصدر واحد».** لأنه **ليس مصدرًا يُقرأ منه اليوم** بل **لقطةٌ مجمَّدة وقت الكتابة**: السجل الميداني لا يُعدَّل (المبدأ الرابع)، **فسجلٌّ قديم يبقى محسوبًا بما كان لا بما صار**، وتغيير عبوة الصنف بعد سنة لا يُعيد حساب ماضٍ. **وقاعدة المصدر الواحد تحكم ما يُقرأ لا ما حُفظ.**

**والتمييز موثَّق في المخطط بلفظه** (`daily-logs.ts`) **قصدًا**: من يقرؤه لاحقًا مصدرًا ثالثًا فيحذفه توحيدًا للمصادر **يمحو الماضي وهو يظنّ أنه ينظّف**. وهو نفس نمط `tank_capacity_l` في الماء و`received_*` في حركة الاستلام (198).

**٤. ومُسح الكود نصًّا قبل كتابة سطر** (`grep` على الألفاظ الثلاثة لا تذكّرًا للمواضع، القاعدة الخامسة في القرار 200): **ولا مستهلك واحد يقرأ الوزن للحساب اليوم** — لا مسار سجل يوميّ ولا بذر أصناف نظامية موجود بعد. **فالحكم الرابع («حيثما يُقرأ الوزن يُقرأ من الصنف») نُفِّذ بلا موضع كتابة واحد**، وقيمته أنه ثبّت المصدر **قبل** أن يوجد قارئ — وهي أرخص لحظة يُثبَّت فيها.

**٥. والإثبات على القاعدة — ستة اختبارات:** علف بلا حجم عبوة ← ٥٠ · علف بعبوة ٢٥ ← يبقى ٢٥ · **لقاح بلا عبوة ← يبقى بلا عبوة** (فالخمسون لم تُعمَّم) · محو حجم عبوة علف بتعديل ← يُملأ ٥٠ · العمود المحذوف ← غير موجود في `information_schema` · **وسجلٌّ يوميّ بوزن ٤٥ تتغيّر عبوة صنفه إلى ٦٠ ← يبقى ٤٥ و`kg` كما كُتب**.

**٦. وإتمامٌ للحكم نفسه — المصدر الواحد يحمل وحدته، وإلا فهو نصف مصدر.** العمود المحذوف كان `tenants.feed_bag_weight_kg`، **والوحدة مكتوبة في اسمه**. والمصدر الجديد `products.package_size` **رقم بلا وحدة**: أول صياغة لهذا القرار جعلت المُشغِّل يملأ ٥٠ **ولا يملأ `package_unit`** — فالعمود يبقى فارغًا، **ولا يقرؤه سطر واحد في المستودع**.

**فما مُنع عاد من باب آخر:** القرار يمنع «٥٠» في منطق الحساب، **وحذف الوحدة من القاعدة ينقلها إلى ذاكرة القارئ** — فيصير «كجم» ثابتًا ضمنيًّا في منطق أول قارئ يُبنى، **وهو نفس صنف الخطأ لا خطأ آخر**. **والفارق أن الرقم يُرى فارغًا فيُسأل عنه، والوحدة الضمنية لا تُرى.**

**فالمُشغِّل يملأ الاثنين في شرط واحد** (`package_size := 50` و`package_unit := 'كجم'` لصنف العلف بلا قيمة)، **والقيد يضمن الاثنين**: `products_feed_package_size_ck` صار يطلب `package_size IS NOT NULL AND package_unit IS NOT NULL`.

**ولفظ الوحدة مأخوذ من المشروع لا مختارًا:** «كجم» هو لفظ الكيلوغرام في `STOCK_UNIT` و`DOSE_BASIS` والوثيقتين وواجهة الموبايل — **بلا لفظ منافس واحد في المستودع**، فلا اختلاف يُسمّى هنا.

**واختلافٌ آخر يُسمّى ولا يُوحَّد:** `package_unit` **نصّ `varchar(16)` لا `stock_unit`**، **والمجالان مختلفان فعلًا**: وحدة عبوة اللقاح قد تكون «مل» أو «جرعة» **وليستا في `STOCK_UNIT`** (وثيقة الشاشات تفصل «وحدة المخزون» عن «حجم العبوة ووحدته» عن «وحدة الجرعة»). **فربط العمود بالقائمة يرفض عبوات مشروعة** — وتوحيدهما **قرارُ نموذج لا تفصيل مخطط، يُسمّى ويُوقف عنده**.

**٧. وفُحصت باقي الفئات بنفس العين — والثقب كان فيها كلها.** `package_size` بلا `package_unit` **كان يُقبل صامتًا من أي فئة**: دواءٌ بحجم عبوة ١٠٠ بلا وحدتها صفٌّ سليم في القاعدة اليوم. **وهو نفس الثقب لا ثقب مشابه.**

**ووُسِّعت الدفعة إليه لأن القيد نفسه يغطيه بلا كلفة ولا قرار:** `products_package_unit_ck` — `package_size IS NULL OR package_unit IS NOT NULL`. **ولا يفرض حجمًا على أحد** (صنفٌ بلا حجم يبقى بلا حجم)، **ولا يقرّر أي وحدة لأي فئة**، **ولا يحتاج افتراضًا لغير العلف** — **يمنع الحالة المعطوبة وحدها**. وما احتاج قرارًا هو ما وُقف عنده: **أي وحدة تخصّ أي فئة**، وذلك للصنف لا للمخطط.

**والوحدة لا تُخترع لصنف قائم:** الترحيل يملأ **العلف وحده** (وحدته معلومة بحكم القرار)، **ثم حارسٌ يوقف الترحيل** إن بقي صفٌّ من أي فئة يحمل حجمًا بلا وحدة — **بالرفع لا بالتخمين**، فوحدةٌ مخترعة للقاح مصدرٌ كاذب وهو أسوأ من التوقف. وهو نفس نمط حارس الترحيل في القرار 199.

**والتعبئة الرجعية تشمل الاثنين معًا** (`COALESCE` على كليهما في جملة واحدة) — **فلا صنف علف قائم يمرّ بنصف تعبئة**. **وعُدِّل ترحيل 0016 في موضعه ولم يُضف 0017**: لم يُدمج ولم يُطبَّق على قاعدة إنتاج، **وترحيلٌ يصحّح ترحيلًا لم يُنشر يترك في السجل خطوةً لم توجد قطّ عند أحد**.

**٨. والاختبارات صارت تسعة** — والأربعة الجديدة/المشدودة: علف بلا حجم ولا وحدة ← **يأخذ الاثنين** · علف بعبوة ٢٥ ← يبقى ٢٥ **ووحدته تُملأ** · **محو الوحدة بتعديل ← تُملأ من جديد** · **دواء بحجم بلا وحدته ← يرفضه القيد**، ودواء بحجم ووحدته يُقبل، ودواء بلا حجم يُقبل (فالقيد يمنع المعطوب ولا يفرض حجمًا).

**وحدّ معلن واحد — ✅ رُفع بالقرار 213** (`ensureSystemProducts` تُنشئ الخمسة في معاملة إنشاء المستأجر، **والعلف يأخذ وزنه من مُشغِّل هذا القرار بلا أن يكتبه البذر**). **والنصّ الأصلي:** **بذر الأصناف النظامية غير مبنيّ** — `products.is_system` موجود وله فهرس فريد للعلف بالمرحلة، **ولا مسار ولا بذرة تُنشئ تلك الأصناف**. فالحكم اختار الموضع الأول من الموضعين اللذين أباحهما المالك (المخطط أو البذر)، **وبناء البذر قرارُ نموذج لا تفصيل مخطط** (من يُنشئ الأصناف النظامية، ومتى — عند إنشاء المستأجر أم بمسار مستقل) — **يُسمّى ويُوقف عنده هنا**.

**202. المورّد والناقل كيانين — نصّان حرّان يصيران هويتين يُجمَّع عليهما**

**تنفيذ حكم مكتوب سلفًا لا اجتهاد جديد** (#161 «ثالث عشر» ٩): **«المورّد والناقل كيانان يُنشآن مرة واحدة لا نصوصًا حرة.»** **ونطاق الدفعة مخطط وترحيل فقط — لا مسار API ولا شاشة.**

**العلّة: ثلاثة قرارات تتقاطع على كيانين غير موجودين.** الناقل (#157 البند ٣) · سجل المورّد (#160 السؤال الرابع — خصم النافق عند الوصول من سجل المورّد **يفترض وجود السجل أصلًا**) · متابعة أداء المورّد عبر الشحنات (#161 «تاسعًا»). **فيُنشآن مرة واحدة لا مرة لكل قرار** — والحال قبل هذه الدفعة `products.supplier varchar(160)` و`shipments.carrier_name varchar(128)`، **نصّان لا يُجمَّع عليهما شيء**.

**والحجّة القاطعة ليست ذوقًا في النمذجة بل تقريرٌ معطوب:** تقرير الفاقد يطلب «الفروقات حسب المربي والمشرف **والناقل**» — **والتجميع على نصّ يدوي مستحيل** («أبو محمد» و«ابو محمد» ناقلان)، **وعلى كيان ممكن**. **فالتقرير الثالث من الخمسة كان غير قابل للتنفيذ كما هو موصوف** (#156 البند ٥)، **وبهذه الدفعة صار قابلًا**.

**١. جدولان بحقول ما تسمّيه القرارات وحده.** `suppliers` و`carriers`: `tenant_id` · `name` · `is_active` · `created_at`. **ولا هاتف ولا عنوان ولا تصنيف** — **لا قرار يسمّيها**، **وعمودٌ يُخترع اليوم يُبنى عليه غدًا**. و`is_active` و`created_at` نمط كل كيان كتالوجي في هذا المخطط (`products` · `warehouses`) لا اختراعًا.

**وكل مفتاح إليهما مركَّب بلا استثناء** — `(supplier_id, tenant_id) → suppliers(id, tenant_id)` و`(carrier_id, tenant_id) → carriers(id, tenant_id)`، **ولكل جدول `UNIQUE (id, tenant_id)`** كما تفرض القاعدة الملزمة (#120 و#122). **والمفرد كان سيتحقق من وجود الصفّ لا من مالكه**، فيقبل صنفًا يشير إلى مورّد مستأجر آخر.

**و`UNIQUE (tenant_id, name)` فوق ذلك** — **وهو أصل الحكم نفسه**: اسمٌ واحد لكيان واحد. **ولا يمسّ الاختلاف الإملائي**: «أبو محمد» و«ابو محمد» نصّان مختلفان فيبقيان صفَّين، **ودمجهما قرارُ بيانات لا قيدُ مخطط**.

**٢. «المورّد أو الفقاسة» و«المورّد أو المطحنة» — اسمان لدورٍ واحد لا كيانان.** #160 «أولًا» يسمّي حقل شحنة الكتاكيت «المورّد أو الفقاسة»، و#161 «تاسعًا» يسمّي حقل توريد العلف «المورّد أو المطحنة».

**والعبارتان تسميتان لمن اشتُري منه** — تاجرًا كان أو منتِجًا — **لا تصنيفٌ يطلب النظامُ حفظه**. **والقرارات الثلاثة تطلب هويةً يُجمَّع عليها الأداء، ولا واحد منها يطلب التفريق بين فقاسة وتاجر.** **وجدولان بحقول متطابقة يفرّقان مورّدًا يبيع الاثنين** — والشركة الواحدة قد تملك مطحنة وتورّد الدواء، فتصير صفَّين لا يُجمَّع عليهما أداء، **وهو عين العطب المُصلَح هنا**.

**والقرار قابل للتراجع في اتجاه واحد فقط، وهذا ما يجعل حسمه آمنًا:** إن أراد المالك التفريق يومًا **فهو عمودُ نوعٍ على هذا الكيان** — إضافةٌ رخيصة؛ **وفكُّ كيانين إلى واحد بعد أن تُبنى عليهما المسارات ليس كذلك**.

**٣. رقم المركبة يبقى على الشحنة ولا ينتقل إلى الناقل.** **لأنه صفة واقعةٍ لا صفة كيان:** الناقل الواحد يملك أكثر من شاحنة ويبدّلها بين شحنة وأخرى — **فوضعه على الكيان يجعل الناقل ذا مركبة واحدة**، **ويُعيد كتابة الماضي عند أول تبديل** فتظهر شحنة العام الماضي بالشاحنة التي يقودها اليوم.

**وهو نفس ما يحرسه نمطان قائمان:** «السائد وقت الإدخال» (`bag_weight_kg` و`tank_capacity_l`) و«ما وصل فعلًا في هذه العبوة» (`received_*`، القرار 198). **ما يصف الحدث يُحفظ مع الحدث.** **وأُثبت على البيانات المنقولة لا وصفًا:** ناقلٌ واحد بعد النقل يحمل **مركبتين مختلفتين** في شحنتيه.

**٤. المسح النصّي قبل الكتابة — وما وُجد وما تُرك.** مُسح المستودع عن كل حقل نصّي يسمّي طرفًا خارجيًّا (`grep` على `supplier`/`carrier`/`vendor`/`driver`/«مطحنة»/«فقاسة» وعلى كل عمود `varchar` في المخطط):

| ما وُجد | الحكم |
| --- | --- |
| `products.supplier varchar(160)` | **حُوِّل** → `supplier_id` |
| `shipments.carrier_name varchar(128)` | **حُوِّل** → `carrier_id` |
| `batches` **ومصدر الكتاكيت** | **لا حقل أصلًا** — لا مورّد ولا فقاسة ولا ناقل. **وسلسلة استقبال الكتاكيت غير ممثَّلة بأي جدول** (#160 التعارض ٣: «لا مسار استقبال كتاكيت إطلاقًا، ولا الكتاكيت منتج») — **فلا نصّ يُحوَّل، وإضافةُ عمودٍ هنا بناءُ نموذجِ استقبالٍ لم يُقرَّر**. تُرك. |
| `admin_audit_log.emergency_operator varchar(128)` | **تُرك عمدًا** — اسم منفّذ الطوارئ (القرار #67)، **وهو ليس طرفًا يُتعامَل معه بل أثرُ واقعة**، وقد يكون شخصًا بلا حساب في النظام. **ولا تقرير يُجمِّع عليه.** |
| `stocktake_items.reason` · `house_prep_steps.label` | نصوص لا تسمّي أطرافًا. تُركت. |

**٥. النقل صريح لا اعتمادًا على الفراغ، ولا يُتلف.** كل قيمة نصّية قائمة تصير صفًّا (`INSERT ... SELECT DISTINCT`) ثم يُشار إليها بمساواة تامة، **ثم حارسٌ داخل الترحيل يوقفه إن بقي صفٌّ يحمل اسمًا بلا مرجع** — **قبل حذف النصّ، فبعده لا يبقى ما يُقارَن به** (نمط حارس القرار 199).

**والمساواة تامة لا تطبيع:** المتشابه إملائيًّا **لا يُدمج باجتهاد الترحيل**. **ونصٌّ فارغ أو فراغات وحدها لا يسمّي أحدًا** فلا يصير كيانًا ولا يوقف الحارس.

**وأُثبت على قاعدة فيها صفوف فعلًا لا فارغة** (`dawajin_dev`، مستأجران):

| قبل | بعد |
| --- | --- |
| ٧ أصناف بنصّ مورّد (منها فارغ بفراغات) · **٥ نصوص متمايزة** | **٥ صفوف `suppliers`** · **٦ أصناف بمرجع** · ٢ بلا مرجع (عدم وفراغات) |
| ٣ شحنات بنصّ ناقل · **نصّان متمايزان** («أبو محمد» و«ابو محمد») | **٢ صفّا `carriers`** — **لم يُدمجا** · ٣ شحنات بمرجع |

**٦. والاستلام الأعمى لا يتأثر — فُحص لا افتُرض.** #157 البند ٣ يقطع اللبس صراحةً: **الناقل معلوم لحظة الاستلام، والكمية وحدها هي المخفية.** ولا شيء في هذه الدفعة يمسّ ما يُخفيه §3.6: `sent_quantity` و`counted_quantity` و`variance` **لم تُلمس**، **وما تغيّر هو شكل حقلٍ كان مرئيًّا للمربّي أصلًا** — رأى الاسم نصًّا فصار يراه من قائمة. **ولا حقل مخفيّ صار مكشوفًا ولا العكس.**

**٧. وثلاثة حدود معلنة — تُسمّى ولا تُبنى هنا:**

- **ربط الناقل بسجل الزيارات** (#154 و#157 و#160): **لا جدول زيارات في المخطط إطلاقًا**. الربط مذكور في ثلاثة قرارات **ولا موضع له اليوم**، ولا يُنشأ جدول زيارات في دفعة كيانين.
- **موضع المورّد على الصنف — مشكوك فيه ولم يُحسم.** #161 «تاسعًا» يطلب متابعة الأداء **عبر الشحنات**، وصنفٌ واحد قد يُشترى من مورّدين في شهرين — **فالمورّد أقرب إلى خاصية دفعة التوريد منه إلى خاصية الصنف**، على نفس نمط الصلاحية في القرار 198 («خاصية عبوة لا صنف، وحركة الاستلام هي حاملة التاريخ»). **ونقلُه إلى حركة الاستلام قرارُ نموذج لا تفصيل مخطط** — والدفعة حوّلت الحقل **في موضعه** ولم تنقله.
- **السؤال الرابع في #160 يبقى مفتوحًا** (هل يُخصم النافق عند الوصول من الكمية المشتراة في سجل المورّد؟) — **قرار مالك**. **وهذه الدفعة تزيل ما كان يمنع طرحه**: كان يفترض سجل مورّد غير موجود، وصار موجودًا.

**٨. والإثبات على القاعدة — تسعة اختبارات:** شحنة إلى ناقل مستأجر آخر ← يرفضها المفتاح المركَّب · صنف إلى مورّد مستأجر آخر ← كذلك · ومن نفس المستأجر ← يُقبلان · ومرجع غير موجود ← يُرفض · ومورّدان بنفس الاسم في نفس المستأجر ← يرفضهما الفهرس · ونفس الاسم في مستأجرين ← يُقبل · واختلاف إملائي ← صفّان لا صفّ · **وناقلٌ واحد بمركبتين ← كلتاهما محفوظتان** · والنصّان المحذوفان ← لا وجود لهما.

**ولا مسار ولا شاشة في هذه الدفعة** — والبند 32 خانة (أ) نقصت بندًا، **وفيها جدول الطلبات والأكياس الفارغة**، **فلا يُختم البند بوسم إغلاق** (القرار 200).

**203. الصرف الخارجي من المخزن — نوعٌ ثالث عشر، ومصادقةٌ متبادلة تُقيّد المالك عمدًا**

**قرار مالك، ونطاق الدفعة مخطط وترحيل فقط — لا مسار API ولا شاشة.**

**الثغرة كما هي، لا موصوفةً بأخفّ مما هي:** `INVENTORY_MOVEMENT_TYPE` **اثنا عشر نوعًا ولا واحد منها خروجٌ إلى خارج النظام**. كلها تنقل كميةً بين موضعين في النظام (استلام · شحن · مرتجع · تحويل) أو تستهلكها فيه (يومي · علاج · تجهيز) أو تُسوّيها (جرد · هالك).

**فبيعٌ خارج المنظومة يجد بابين، وكلاهما خطأ:**

| الباب | العطب |
| --- | --- |
| يُسجَّل «هالك/تلف» | **كذبٌ في سجل لا يُعدَّل** (المبدأ الرابع) — والكيس المبيع يظهر تالفًا إلى الأبد، **وتقرير الفاقد يعدّه فاقدًا** |
| لا يُسجَّل | **ينقص الرصيد بلا سبب**، **وتكشف معادلة §13.3 فارقًا بلا تفسير** — يُقرأ سرقةً أو إهمالًا |

**والنظام كان يجبر على أحد الخطأين** — فالنوع ليس إضافةً بل سدُّ بابٍ كان يُدخل الكذب إلى دفترٍ بُني على ألّا يُعدَّل. **وله سابقة مكتوبة تنتظره:** #161 «عاشرًا» يقول إن الأكياس الفارغة **«أصل قابل للبيع وإعادة الاستخدام: تُسجَّل حركة خروجها عددًا فقط، بلا سعر ولا قيمة»** — **حكمٌ لا موضع له في النموذج حتى اليوم.**

**والحكم: لا تخرج كمية إلا بمصادقة متبادلة، في اتجاهين متناظرين.**

> **أمين المخزن المركزي يرسل أمر صرف والمالك يصادق** — عندها تُسجَّل الحركة وتُعتمد.
> **والمالك يرسل أمر صرف بالكمية والجهة المستفيدة ولا تخرج إلا بموافقة أمين المخزن.**
> **فالطرفان لا بد منهما في الاتجاهين، ومن بدأ الأمر لا يصادق عليه.**

**والأثر معلن ومقصود: أمر المالك نفسه لا يُخرج شيئًا بلا توقيع أمين المخزن.** **وهو تقييدٌ للمالك يقبله الحكم صراحةً** — **ولا يناقض «الآمر بالصرف من المركزي هو المالك»** (#161 «ثالث عشر» ٢): **المالك يبقى الآمر، والأمر وحده لا يُخرج**. الأمر والإخراج فعلان لا فعل واحد، **وما أضافه هذا القرار توقيعٌ ثانٍ على الثاني لا نزعُ الأول**.

**١. الأمر كيانٌ مستقل عن الحركة — والفصل هو الفرض لا الترتيب.** `external_issue_orders` جدولٌ قائم بذاته، **والحركة تُولد بالمصادقة لا قبلها**: أمرٌ معلَّق **لا يمسّ الرصيد لأنه ليس حركة**، ومرفوضٌ **لا يُنتج حركة أبدًا**.

**ولو كانت الحركة هي الأمر لكان «لا تخرج إلا بموافقة» وعدًا في مسار:** كتابة الحركة تكون قد وقعت والرصيد نقص، **ثم تأتي الموافقة تصفُ ما جرى بدل أن تأذن به**. **والفرض في القاعدة بثلاثة معًا:**

- **حارس `external_issue_movement_guard`** على `inventory_movements` — **يرفض كل حركة من هذا النوع لا يقابلها أمرٌ مصادَق عليه** في نفس المستأجر وبنفس المخزن والصنف، **وبكمية سالبة تساوي كمية الأمر بالضبط**.
- **فهرس فريد جزئي** على `source_uuid` حيث `source_type = 'external_issue_order'` — **أمرٌ واحد حركةٌ واحدة**، وبلا هذا يُصرف الأمر المصادَق عليه مرتين.
- **وقيود الجدول** على الحالة والقرار والكمية.

**وأُثبت أن الحارس حامل للحمل لا زينة:** بتعطيله **سقطت خمسة اختبارات من سبعة عشر**، وبإعادته مرّت السبعة عشر.

**٢. القيد على الشخص لا على الدور — وهذا أقوى، والفرق ليس تفصيلًا.** `CHECK (decided_by IS NULL OR decided_by <> initiated_by)`، على نمط `stocktakes` (#155) و`house_prep_steps` (197).

**والصياغة البديلة «دوران مختلفان» تبدو مكافئة وليست كذلك: الدورُ يتغيّر والشخصُ هو من وقّع.** من بدأ الأمر أمينًا للمخزن ثم رُقّي مالكًا **يصادق على أمر نفسه بلا مخالفة واحدة** لقاعدة الدورين — الأمر بدأه دورٌ وصادق عليه دورٌ آخر، **والشخص واحد**. **وشرطُ المصادقة شاهدٌ ثانٍ، والشاهد إنسان لا صفة وظيفية.** **والقيد على الشخص لا يُخترق بترقية.**

**٣. الاتجاهان صفٌّ واحد لا عمودان.** بادئٌ ومصادِق، **لا «أمر مالك» و«أمر أمين»** — فعمودان لكل دور **يجعلان كل قاعدة تُكتب مرتين**، ودورًا ثالثًا يُضاف يفتح ثالثًا، **ويجعلان التناظر وعدًا في نصّ بدل أن يكون شكل الجدول**.

**وحدٌّ معلن: أيُّ الدورين يملأ أيّ خانة فرضُ صلاحية يُبنى مع المسارات** — **والمخطط لا يفرّق بينهما ولا يجب**. و§12.2 صار يحمل صفَّين متناظرين (إرسال · مصادقة) بخانتَي المالك وأمين المخزن ✅ في كليهما.

**٤. الجهة المستفيدة نصّ حرّ لا كيان — بمعيار القرار 202 نفسه لا خلافًا له.** المعياران هناك: **هل يُجمَّع على هذا الطرف شيء؟** و**هل القرار قابل للتراجع في اتجاه واحد؟**

- **الأول ينفي الكيان هنا.** الناقل صار كيانًا لأن **تقريرًا مكتوبًا يطلب التجميع عليه** («الفروقات حسب المربي والمشرف والناقل»، #157 البند ٣) **فكان معطوبًا بلا كيان**. **ولا تقرير يجمّع على الجهة المستفيدة** — والحكم يطلب **«لتكتمل الحركة الكلية للمخزن المركزي أمام المالك»**: **الجمع على المخزن لا على المشتري**.
- **والثاني يرجّح النصّ.** التحويل من نصّ إلى كيان **هو ترحيل 0017 نفسه وقد أُثبت** (قيم متمايزة تصير صفوفًا، وحارسٌ يمنع الإتلاف). **والاتجاه العكسي — كيانٌ لا يقرؤه تقرير — كلفتُه كتالوجٌ يُنشأ ويُسمّى ويُعطَّل لجهةٍ تُباع لها مرة واحدة.**

**فالقاعدة المستخلَصة، وهي ما يفصل 202 عن هذا القرار:** **النصّ يصير كيانًا حين يُجمَّع عليه، لا حين يُذكر.**

**ولم يُنشأ كيان ثالث لأن القائمين لا يفيان ولا يُقحمان:** `suppliers` من نشتري منه و`carriers` من ينقل — **والمستفيد من يشتري منا**، **واستعمال `suppliers` له يجعل «متابعة أداء المورّد عبر الشحنات» (#161 «تاسعًا») تجمّع مبيعاتٍ في سجل مشتريات**.

**٥. قائمة السبب مغلقة ومعها نصّ — والفرق عن `wastage_reason` جوهري.** #161 «ثالث عشر» ١١ سجّل أن **«النظام يعرض أسبابًا لا يستطيع كشفها ولا منعها»** («انقطاع تبريد» · «انتهاء صلاحية») **فيقبل تسجيل نتيجةٍ لا يملك سببها**.

**وسبب الهالك واقعةٌ في العالم يعجز النظام عن كشفها؛ وسبب الصرف الخارجي تصريحٌ من البادئ عن فعلٍ هو صاحبه.** **لا يُكشف بل يُنسب إلى من وقّعه** — والقائمة هنا **لا تَعِد بما لا تملك**، بل تسمّي ما أعلنه إنسان. **فالقائمة المغلقة مشروعة هنا حيث لم تكن هناك.**

**وقصيرة عمدًا: «بيع» · «أخرى».** **«بيع» وحده ما تسمّيه أحكام المالك** (#161 «عاشرًا» وهذا القرار)، **ولا يُخترع لها ثالث**. **و«أخرى» تُلزم بنصّ بقيد** — «أخرى» بلا نصّ **سببٌ لا يسمّي شيئًا**، **والنصّ هو باب نموّ القائمة**: ما يتكرر فيه يصير قيمةً بقرار، **لا بالحدس اليوم**.

**٦. ولا سعر ولا قيمة ولا أي عمود مالي — القرار #136، ومكتوبٌ في المخطط بلفظه.** #161 «عاشرًا»: «تُسجَّل حركة خروجها **عددًا فقط، بلا سعر ولا قيمة**». **وكُتب في `inventory.ts` وفي §7.3 نصًّا كي لا يُضاف عمود سعر لاحقًا «إتمامًا»** — فحقلٌ ناقص يُغري من يقرأ الجدول بإكماله، **والنصّ يقول له إن النقص قرار**. **ورقمٌ مالي في يد أمين مخزن يغيّر معنى التطبيق من توثيق ميداني إلى أداة محاسبة** (#136)، **والحسابات في منصة منفصلة** (#137). **ويحرسه اختبار يقرأ `information_schema`.**

**٧. والنوع لا يُقصر على المركزي في المخطط — حدٌّ معلن لا قيد.** **ما يقيّده من يأمر لا الجدول**: أمرٌ على مخزن عنبر يُقبل في القاعدة، **ومنعه صلاحيةٌ تُبنى مع المسارات**. **وقيدٌ في الجدول كان سيسبق حكمًا لم يصدر** — الأكياس الفارغة في مخزن العنبر «أصل قابل للبيع» (#161 «عاشرًا») **وأين تُجمَّع سؤالٌ مفتوح للمالك** («حادي عشر»)، **فقفلُ الجدول اليوم يقفل بابًا قد يفتحه جوابه**.

**٨. والأمر المصادَق عليه لا يُعدَّل** (المبدأ الرابع): `correction_of_uuid` — **نفس نمط `shipments` القائم لا نمطٌ ثانٍ**، والتصحيح **بأمر مضاد مرتبط بالأصل والأصل يبقى ظاهرًا**.

**٩. وثابت §13.3 يبقى مفروضًا:** الخروج الخارجي **حركةٌ سالبة كغيرها** لا استثناء منه — **وأُثبت باختبار يقيس طرفيه قبل الأمر وبعد المصادقة**، لا بجملة.

**والإثبات على القاعدة — سبعة عشر اختبارًا:** أمر معلَّق ← الرصيد كما هو والثابت صامد · حركة لأمر معلَّق ← يرفضها الحارس · أمر مرفوض ← لا حركة له · حركة بلا أمر ← تُرفض · **أمرٌ يصادقه بادئه ← يرفضه القيد** · **ويرفضه بادئه ← كذلك** · والاتجاهان متناظران عملًا · وقرارٌ بلا صاحب أو حالةٌ تخالف قرارها ← يُرفضان · **والمصادقة تُنقص بمقدار الكمية بالضبط والثابت صامد قبل وبعد** · وكمية تخالف أمرها ← تُرفض · **وأمرٌ واحد حركتان ← تُرفض الثانية** · ومخزن غير مخزن الأمر ← يُرفض · **وأمرٌ إلى مخزن مستأجر آخر ← يرفضه المفتاح المركَّب** · و«أخرى» بلا نصّ ← تُرفض وبنصّ ← تُقبل · وكمية غير موجبة ← تُرفض · وأمرٌ على مخزن عنبر ← يُقبل (فالنوع غير مقصور) · **ولا عمود مالي في الجدول**.

**١٠. وإتمامٌ للحكم نفسه — قاعدةٌ بقيت في التعليق وحده.** «الأمر المصادَق عليه لا يُعدَّل» كُتب في `external-issue.ts` **ولم يفرضه قيدٌ ولا حارس**: حارس الحركة يعمل على `inventory_movements` **عند الإدراج**، **ولا شيء كان يحرس `external_issue_orders` عند التعديل**.

**فكان يمرّ صامتًا:**

| ما كان يمرّ | ما ينتج عنه |
| --- | --- |
| قلبُ أمرٍ مصادَق إلى «مرفوض» **بعد ولادة حركته** | الحركة تبقى في الدفتر **ويقول أمرها إنه لم يُوافَق عليه** — **كميةٌ خرجت بلا موقّع** |
| تعديلُ الكمية بعد المصادقة | الأمر يقول ٩٩٩ والدفتر ٥٠ |
| حذفُ الأمر | **حركةٌ يتيمة** — والدفتر يشير إليه بـ`source_uuid` **لا بمفتاح أجنبي** |

**وهذا أخطر ما في الباب لأن ثابت §13.3 لا يكشف واحدًا منها:** الثابت يقيس **الحركات مقابل الأرصدة**، **والحركة سليمة في الحالات الثلاث — الكاذب هو الأمر**. **فالتزوير يقع خارج ما يقيسه المقياس**، ويبقى الدفتر متسقًا وهو يشهد لكمية بلا شاهد.

**والقاعدة المستخلَصة:**

> **قاعدةٌ تُكتب في تعليق ولا تُفرض في القاعدة ليست قاعدة، بل نيّةُ من كتبها.**
> **وأخطرها ما يقع خارج ما يقيسه ثابتُ النظام** — فالثابت يطمئن قارئه، **والاطمئنان في غير موضعه أسوأ من غيابه**.
> **ومقياس الاكتمال أن يُسأل عن كل جملة معياريّة في التعليقات: أيّ قيدٍ أو حارسٍ يفرضها؟** فما لا جواب له **إمّا يُفرض وإمّا يُنقل إلى الحدود المعلنة**.

**والمعالجة `external_issue_order_freeze_guard`** — **بنفس آلية الدفعة لا بآلية ثانية**: **ما خرج من «معلّق» مجمَّد**، لا حالته ولا كميته ولا مخزنه ولا صنفه ولا مستفيده ولا سببه ولا من قرّر ولا متى. **والمعلَّق مسوّدة تُعدَّل ويُقرَّر عليها** — **فالتجميد على الخروج من «معلّق» لا على الوجود**.

**ولماذا حارسٌ لا `CHECK`:** القيد **يرى الصفّ الجديد وحده فلا يعرف أنه كان معلّقًا** — و«لا يُعدَّل بعد القرار» **حكمٌ على الانتقال لا على القيمة**. **والمقارنة على الصفّ كله** (`NEW IS DISTINCT FROM OLD`) **لا على قائمة أعمدة**، فعمودٌ يُضاف غدًا يدخل التجميد بلا تعديل في الحارس.

**والحذف يُمنع كذلك، بعلّتين لا بواحدة:** `inventory_movements.source_uuid` **مرجعٌ متعدد المصادر** (شحنة · جرد · سجل يومي · أمر صرف) **فلا يحتمل مفتاحًا أجنبيًّا إلى جدول واحد** — **فحذف أمرٍ مصادَق عليه يترك حركةً يتيمة في دفترٍ لا يُحذف منه**؛ **وحذف المرفوض يمحو رفضًا وقّعه إنسان**. **وبهذا وحده صار «والأصل يبقى ظاهرًا» صحيحًا لا موعودًا.** **والمعلَّق يُحذف** — لا حركة له فلا يُتْم، **وقفلُه كان سيسبق حكمًا لم يصدر** (سحب الأمر، الحدّ الثالث أدناه).

**١١. ومُسحت بقيّة الدفعة بنفس العين، فوُجد اثنان:**

- **🔴 الحركة كانت تخالف أمرها في الوحدة وتمرّ:** الحارس يقابل المخزن والصنف والكمية **ولا يقابل الوحدة** — وكميةٌ متطابقة بوحدة مختلفة **تخالف أمرها وهي تبدو مطابقة له**. **وهو حرفيًّا ما نصّ عليه القرار 201** («الرقم بلا وحدته نصف مصدر»). **أُصلح في نفس الحارس.**
- **🔴 والاقتران كان في اتجاه واحد:** حركةُ صرفٍ خارجيّ بلا أمر ممنوعة، **وأمرٌ تستهلكه حركة من نوع آخر كان يمرّ** — فيشغل خانته في الفهرس الفريد **بلا أن يمرّ من الحارس أصلًا**. **أُصلح: الاقتران في الاتجاهين.**

**وواحدٌ وُجد وتُرك بعلّة:** `correction_of_uuid` **بلا مفتاح ولا فحص وجود** — **وهو نمط `shipments.correction_of_uuid` القائم بحرفه**، والدفعة أُمرت باتباعه لا باختراع ثانٍ. **وشقّه الأخطر أُغلق ضمنًا**: «الأصل يبقى ظاهرًا» صار مفروضًا بمنع الحذف. **وتوحيد النمطين بفحصٍ في الجدولين معًا دفعةٌ مستقلة.**

**والاختبارات صارت سبعة وعشرين** (كانت ١٧)، والعشرة الجديدة: أمر مصادَق تُقلب حالته ← يُرفض · تُعدَّل كميته ← يُرفض · يُعدَّل مستفيده ← يُرفض · وأمر مرفوض يُعدَّل ← يُرفض · **وأمر معلّق يُعدَّل ← يُقبل** · **ويُصادَق ← يُقبل** · وأمر مقرَّر يُحذف ← يُرفض · **ومعلّق يُحذف ← يُقبل** · وحركة بوحدة تخالف أمرها ← تُرفض · وأمرٌ تستهلكه حركة من نوع آخر ← تُرفض. **وأُثبت أن حارس التجميد حامل للحمل: بتعطيله سقطت خمسة، وبإعادته مرّت السبعة والعشرون.**

**وعُدِّل ترحيل 0018 في موضعه ولم يُضف 0019** — لم يُدمج ولم يُطبَّق على قاعدة إنتاج، **وأُعيد بناء قاعدة الاختبار من الصفر** فمرّ الترحيل المعدَّل من أوله لا مُرقَّعًا.

**وحدٌّ ثالث معلن — سحب الأمر من بادئه غير مبنيّ.** الحكم يسمّي **مصادقةً ورفضًا** ولا يسمّي إلغاءً، **وقيدُ «من بدأ لا يقرّر» يمنع البادئ من إغلاق أمره بنفسه** — فحالةٌ رابعة («مسحوب») **تحتاج استثناءً من القيد أو عمودًا آخر، وكلاهما قرارُ نموذج**. **يُسمّى ويُوقف عنده.**

**ولا مسار ولا شاشة في هذه الدفعة.**

**204. ما يحتاجه المحاسب من الميدان — تسمية ثغرات تسجيل لا تصميم منصة**

**قرار سياق ونطاق. يُفصِّل القرار #137 ولا ينقضه، ولا سطر كود فيه.**

**ما طلبه المالك لمنصة المحاسب:** أجور العمالة · إيجار المزارع · تكاليف الصيانة · أجور المشرفين والأطباء · أجور النقل · أسعار العلف والأدوية واللقاحات والمعقمات وأدوات المربين قبل بدء الدورة · **ومطابقة أعداد الطيور الخارجة للتسويق**.

**وهذه دفعة تسمية لا تصميم:** لا شاشة ولا جدول لمنصة المحاسب، **ولا عمود مالي واحد** — والمطلوب سؤالٌ واحد يُطرح على كل بند: **ما الواقعة الميدانية التي يُضرب فيها الرقم، وهل هي مسجَّلة اليوم بما يكفي لأن تُطابَق؟**

**أولًا — تصنيف البنود السبعة، بالدليل من المخطط.**

| البند | له أثر ميداني يُطابَق به؟ | الدليل |
| --- | --- | --- |
| **إيجار المزارع** | **لا — وهو الأنقى** | `farms`: `id · tenant_id · site_id · name · power_sources · rest_days · created_at`. **عقدٌ دوري لا يمرّ منه شيء بالميدان**، ولا ينتظر من النظام شيئًا. |
| **أجور المشرفين والأطباء** | **لا** | لا حقل. **ونظيره الميداني قائم وليس هو ما يُدفَع عليه**: `user_assignments` بمدة (#158، القرار 190) يقول **من كان مسؤولًا عن ماذا ومتى** — **وهو مدّة إسناد لا ساعات عمل**. |
| **أجور العمالة** | **لا — وأسوأ من «لا»: محجوبة بنيويًّا** | لا حقل، **و«العامل» ليس دورًا في `USER_ROLE` ولا يملك حسابًا** (#157 البند ٣/٦)، و`house_prep_steps.assigned_to` **مفتاحٌ إلى `users`**. **فالنظير الوحيد الممكن — من عمل ماذا ومتى — مغلقٌ لا ناقص:** من يُدفع له أجره **لا يمكن إسناد خطوة إليه أصلًا**. |
| **تكاليف الصيانة** | **جزئي وضعيف** | `house_status_history` يسجّل الانتقال إلى «تحت الصيانة» بـ`changed_by` و`changed_at` و`reason` نصًّا — **فمدّة التعطّل قابلة للقياس**. **ولا سجل لما أُصلح ولا بمَ ولا من أصلحه** (#156 البند ٦: «مسؤول الصيانة» عمل بلا صاحب). **فالمطابقة على مدّةٍ لا على عمل.** |
| **أسعار العلف والأدوية واللقاحات والمعقمات والأدوات** | **نعم — ونظيره ناقصٌ بحقلٍ واحد حاسم** | حركة «استلام» في `inventory_movements` تحمل الصنف والكمية والوحدة والمخزن و`received_expiry_date`/`withdrawal_days`/`storage_conditions` ومن أدخل ومتى (القرار 198). **ولا تحمل من ورّد**: `supplier_id` **على `products` لا على الحركة** (القرار 202). **والفاتورة تسأل: من مَن · ماذا · كم · متى — والحركة تجيب عن ثلاثة من أربعة.** |
| **أجور النقل** | **نعم للداخلي · ولا للخارجي** | `carriers` كيان (القرار 202)، **و`carrier_id` على `shipments` وحدها** — و`shipments` **حركة داخلية** (مخزن ← عنبر، §13.1). **والساق التي يُدفع عليها لطرفٍ ثالث — المورّد ← المخزن — بلا ناقلٍ إطلاقًا**: حركة الاستلام لا تحمل `carrier_id`. |
| **أعداد الطيور الخارجة للتسويق** | **مستحيلة بنيويًّا لا صعبة** | «ثانيًا» أدناه. |

**وقراءة المالك صحّت في جوهرها وخالفت في موضعين، فيُسمَّيان:** **الأربعة «تكاليف بحتة» ثلاثةٌ ونصف** — الصيانة لها نظيرٌ ضعيف (مدّة تعطّل) لا عدم. **والاثنان اللذان «لهما نظير مسجَّل» لكلٍّ منهما حقلٌ واحد مفقود بالضبط** — المورّد على الحركة، والناقل على ساق التوريد. **وهذا أدقّ من «مسجَّل» ومن «غير مسجَّل» معًا: النظير قائم وينقصه عمود، وهو أرخص ما في هذا القرار وأثمنه.**

**ثانيًا — التسويق: رقمان إجماليان على صفّ الدفعة، ولا جدول تسويق في المخطط.** تُحقِّق: `batches` يحمل `sold_bird_count integer` و`market_avg_weight_g integer` **قابلين للعدم على صفّ الدفعة**، **ولا جدول حمولات ولا تصريح تسويق في الجداول الأربعة والثلاثين كلها**.

**فالمطابقة مستحيلة بنيويًّا:** الدفعة تُسوَّق **على عدة حمولات في عدة أيام** بأوزان وأعمار مختلفة، **ورقمٌ واحد لا يُطابق ثلاث شاحنات** — ولا يقول أيّها خرجت متى ولا بكم ولا من عدّها. **وكتابته `UPDATE` على صفّ الدفعة عند الإغلاق** — **يتقاطع مع المبدأ الرابع** (لا تعديل على سجل ميداني).

**وليست هذه ثغرة تُكتشف اليوم بل تُرفع أولويتها اليوم:** القرار #160 سجّلها تعارضًا سابعًا بلفظها، **وحكم فيها سلفًا: «التسويق حركة لا `UPDATE`، و`sold_bird_count` يُستبدل بحمولات»**، **وأنّ كل حساب بعد تسويق جزئي على الرصيد الحالي لا العدد الابتدائي**. **فما أضافه المحاسب مستهلكٌ ثانٍ لحكمٍ صادر** — وهو ما ينقله من «يُنفَّذ في المرحلة 3 أو 4» إلى بندٍ له موعد. **ولا يُبنى له جدول في هذه الدفعة.**

**ثالثًا — القرار 203 معيارًا، والمخطط كله ممسوحًا عليه.** أمر الصرف الخارجي **واقعة مكتملة جاهزة للمطابقة**: هوية (`uuid`) · كمية بوحدتها · موضع (مخزن) · صنف · جهة · سببٌ مصنَّف · **توقيعان** (بادئ ومصادِق) · وختمان زمنيان — **وبلا عمود مالي واحد**. **فالمعيار ستّة: هويةٌ · كميةٌ بوحدتها · موضعٌ · موقّعٌ · ختمٌ زمني · وسببٌ مصنَّف.**

**والمسح على المخطط كله لا على بنود المالك وحدها:**

| الواقعة | حالها على المعيار |
| --- | --- |
| `external_issue_orders` (203) | **مكتملة — وهي المعيار.** توقيعان |
| `inventory_transfers` | **مكتملة** — `created_by`/`created_at` و`confirmed_by`/`confirmed_at`، طرفان وصنف وكمية ووحدة |
| `shipments` | **أكملها** — مُرسِل ومستلم وعدّ أعمى وفرق ونزاع ومن أغلقه |
| `stocktakes` (الرأس) | **مكتمل** — `opened_by` · `approved_by` · `closed_by` بختومها |
| `wastage` | **ناقصة بموقّع** — هوية وكمية ووحدة ومخزن وسببٌ مصنَّف و`created_by`، **وبلا مصادِق**: الهالك **يُخرِج كميةً بتوقيعٍ واحد**، والصرف الخارجي أقلّ خطرًا منه ويطلب توقيعين |
| `stocktake_items` | **ناقصة** — **الرأس موقَّع والسطر لا**، وهو حامل الكمية والفرق؛ و`reason` **نصّ حرّ يُغلق به فرقٌ بأي حجم** (#156 البند ٤) |
| `house_prep_steps` | **موقَّعة مرتين ✅ وبلا موضع** — `product_id` و`quantity_used` و`completed_by`/`approved_by`، **ولا عمود مخزن**: من أي مخزن خرج المطهّر؟ |
| `health_task_executions` | **ناقصة مرتين** — `executed_by` و`executed_at` و`quantity_used`، **بلا مخزن**، **وبلا `tenant_id`** (أدناه) |
| `daily_log_feed_rows` | **بلا موضع وبلا `tenant_id`** — والموقّع على السجل الأب (`daily_logs.created_by`) |
| `daily_logs.mortality_count` | **رقمٌ في صفّ لا واقعة** — عددٌ وسببٌ مصنَّف، بلا هويةٍ للواقعة ولا كميةٍ موقَّعة مستقلة. **وهو الخروج الوحيد للطيور اليوم** (#160) |
| `batches.sold_bird_count` · `market_avg_weight_g` | **أضعف حالة في المخطط — رقمان إجماليان** |

**ونمطان خرجا من المسح، وهما أثمن ما فيه لأنهما يتكرران ولا يظهران في بندٍ واحد:**

**🔴 النمط الأول — ثلاث وقائع تستهلك مخزونًا ولا واحدة منها تسمّي مخزنها.** `health_task_executions` و`house_prep_steps` و`daily_log_feed_rows` **تحمل صنفًا وكمية ولا تحمل `warehouse_id`** — **بينما الدفتر يعنون `warehouse_id` إلزاميًّا منذ القرار 199**. **فحركة الاستهلاك تحتاج مخزنًا لا تسمّيه واقعتُها**، فيستنتجه من يكتبها — **وأول مسار يُبنى يخترع قاعدة الاستنتاج**، ثم لا يُطابَق الاستهلاك بمخزنه لأن المصدر لا يعرفه. **يُسمّى ولا يُبنى هنا.**

**🔴 والنمط الثاني ليس ثغرة تسجيل بل خرق عزل — أربعة جداول بلا `tenant_id`.** `daily_log_feed_rows` · `log_notes` · `health_task_executions` · `batch_diagnoses`. **وهي تحمل مفاتيح مفردة إلى `users` و`products` و`batches`** (`author_id → users.id` و`executed_by → users.id` مقروءان في المخطط) — **والمفرد يتحقق من وجود الصفّ لا من مالكه**. **وهو صنف القرار 197 نفسه، الذي أُصلح في `stocktake_items` بالقرار 198** — **وأربعة إخوةٍ له بقيت**. **ويخالف قاعدة `CLAUDE.md` الملزمة بلا استثناء.** **وُجد في مسحٍ عن المحاسبة، وهو أخطر ما فيه ولا علاقة له بها** — ويُجدوَل باستثناء §7-ب (ثغرة أمنية).

**رابعًا — القاعدة العامة.**

> **الرقم المالي يُضاف متى شئت؛ والواقعة التي يُضرب فيها لا تُضاف بأثر رجعي.**
>
> السعر **صفةٌ تُلحق بحدثٍ وقع**: كم كيسًا · من أي مورّد · إلى أي مخزن · بتوقيع من · متى. **وكلها لا تُعرف إلا لحظة الحدث** — فمن لم يسجّلها ساعتها **لا يستنتجها لاحقًا**، لأن الاستنتاج يحتاج نفس الواقعة التي لم تُسجَّل.
>
> **وكلفة التأجيل ليست كلفة كتابة عمود، بل كلفة ماضٍ لا يُسترجع:** عمودٌ يُضاف بعد الإطلاق **يبدأ فارغًا لكل ما مضى**. فأول تقرير محاسبي عن سنةٍ مضت **يُبنى على أشهرٍ بلا بيانات** ولا سبيل لملئها: **المورّد الذي سلّم في مارس لا يُعرف اليوم، والحمولات الثلاث لا تُعاد قسمتها من رقمٍ واحد**. **فالثغرة تُدفَع مرة عند الكتابة، وتُدفَع كل سنةٍ عند القراءة.**
>
> **والمقياس عمليّ لا بلاغيّ:** لكل واقعة تُخرج شيئًا أو تستهلكه، تُسأل الستة — **هوية · كمية بوحدتها · موضع · موقّع · ختم زمني · سبب مصنَّف**. **وما نقصه واحدٌ منها لا يُطابَق ولو بدا مسجَّلًا** — وهذا بالضبط حال `sold_bird_count`: عمودٌ موجود ومكتوب فيه رقم، **ولا يُطابَق شيئًا**.

**و#136 لا يُنقض بهذا، ولا يُمسّ:** لا عمود مالي في أي جدول، **ولا رقم مالي يصل شاشة مربٍّ أو مشرف**. **وعلّة #136 «حدود مسؤولية»** — رقمٌ مالي في يد ميدانيّ **يحوّل التطبيق من توثيق ميداني إلى أداة محاسبة** ويجرّ تدقيقًا وصلاحياتٍ وحفظًا لا يحتملها التصميم. **والمنع على التطبيق الميداني لا على النظام**، **والجواب مقرَّرٌ سلفًا: منصة منفصلة تشارك القاعدة** (#137). **وهذا القرار يسمّي ما تحتاجه تلك المنصة من الميدان — وكله غير ماليّ.** **والمحاسب مذكور جمهورًا في قرارٍ مدموج بالفعل:** #139 يجعل مخرَج Excel «بيانات خام قابلة للتعديل **للمحاسب**».

**خامسًا — منصة المحاسب أول مستهلك ثانٍ للخادم، فتُحسم معها افتراضات البند 23.** تُحقِّق: §7-ب البند 23 يقول **«لحظة بدء منصة الويب — لا قبلها»** ويسمّي الافتراضات: **عمر الرمز وغياب مسار التجديد** · **CORS في الإنتاج (`[]` — صفر أصول)** · **وخانةٌ ثالثة تجمع اثنين**: تسمية `expo_push_token` **وحدّ المعدّل لكل IP** (`100/دقيقة` — **ومنصةٌ خلف وكيل تُجمّع مستخدميها في عنوان واحد**).

**والإحالة كانت ناقصة في الاتجاهين:** البند يحيل إلى #138 **ولا يسمّي المحاسب ولا #137**، ولا شيء في #137 يشير إلى البند. **فأُضيفت الإحالتان** — **ومنصة المحاسب هي «لحظة بدء منصة الويب» بعينها**، فالبند لم يعد ينتظر حدثًا مجهولًا.

**سادسًا — المحاسب دورٌ داخل المستأجر لا مدير منصة، ويُسجَّل صراحةً كي لا يُقرأ خطأً.**

- **يرث العزل**: `tenant_id` من الرمز حصريًا (المبدأ السابع)، **ويرى دفاتر مستأجره وحده**.
- **ويدخل عبر نفس الخادم لا بالقاعدة مباشرة** (#138) — **واتصالٌ مباشر يتخطّى `visibleFarmCondition` و`assignedHousesFilter` و`enforceEntityAccess` كلها**.
- **ولا علاقة له بـ`platform_admins`** (القرارات 194–196): **ذاك جدولٌ بلا `tenant_id` إطلاقًا** لأن مدير المنصة **عابرٌ للمستأجرين**، **والمحاسب داخل واحد**. **وخلطهما يمنح محاسبَ عميلٍ رؤيةَ دفاتر عملاء آخرين** — وهو عين ما بُني الفصل لمنعه (#146 و#147).
- **وإضافته دورًا آمنةٌ بالبناء لا بالانتباه** (القرار 184): الحارس **قائمة موجبة**، **فدورٌ جديد لا يرى شيئًا حتى يُدرَج**.

**وقرار نموذج يُسمّى ويُوقف عنده:** **ما الذي يراه المحاسب داخل مستأجره** — كل المزارع أم المُسندة؟ وهل يُدرَج في `ASSIGNMENT_SCOPED_ROLES`؟ **قاعدة الرؤية تختلف بالدور ولا تُوحَّد** (#131)، **وهي قرار مالك لا اختصار هندسي**. لا يُحسم هنا.

**وما سُجِّل بندًا في §7-ب:** 49 (المورّد على حركة الاستلام) · 50 (الناقل على ساق التوريد) · 51 (التسويق حمولات) · 52 (المخزن غائب عن ثلاث وقائع استهلاك) · 53 (أربعة جداول بلا `tenant_id` — أمني) · 54 (الهالك بتوقيع واحد وسطر الجرد بلا موقّع).

**ولا كود ولا مخطط ولا ترحيل في هذه الدفعة.**

**205. `tenant_id` على أربعة جداول — وقاعدةٌ ملزمة بأربع مخالفات قائمة تُقرأ توصية**

**إصلاح أمني عولج فورًا باستثناء §7-ب بقرار المالك (البند 53). مخطط وترحيل فقط — لا مسار ولا شاشة.**

**الثغرة، مُثبتة لا مفترضة.** `daily_log_feed_rows` · `log_notes` · `health_task_executions` · `batch_diagnoses` — **بلا عمود مستأجر إطلاقًا، فمفاتيحها مفردة حتمًا**. **والمفرد يتحقق من وجود الصفّ لا من مالكه** (القاعدة الملزمة في `CLAUDE.md`، القراران #120 و#122).

**وأُثبتت على قاعدة فحص مستقلة عند حالة `main` قبل هذا الترحيل** — أُنشئت وهُجِّرت إلى 0018 وزُرع فيها مستأجران، **ثم أُدرجت أربع مخالفات بـ`SQL` مباشرًا**:

| المخالفة | ما جرى |
| --- | --- |
| صفّ علف على سجلّ مستأجر أ يستشهد بصنف مستأجر ب | **`INSERT 0 1`** |
| ملاحظة على سجلّ مستأجر أ يكتبها مستخدم مستأجر ب | **`INSERT 0 1`** |
| تنفيذ مهمة مستأجر أ ينفّذه مستخدم مستأجر ب | **`INSERT 0 1`** |
| تشخيص على دفعة مستأجر أ يستشهد ببلاغ مستأجر ب ويكتبه طبيبه | **`INSERT 0 1`** |

**أربعٌ من أربع، قُبلت صامتة.** **ولا واحدة رُفضت** — والعزل بين المستأجرين، وهو المبدأ السابع، **مخروقٌ في أربعة مواضع من الجداول الأربعة والثلاثين**.

**١. المسح على المخطط كله لا على الأربعة — لأن القائمة أثبتت أنها ليست شاملة.** عُدَّت الجداول الأربعة والثلاثون واحدًا واحدًا وصُنّفت **بأسماء جداولها لا بعددها**:

- **تحمل `tenant_id` غير قابل للعدم (٢٦):** `entity_audit_log` · `settings_audit_log` · `daily_logs` · `external_issue_orders` · `sites` · `farms` · `houses` · `batches` · `user_assignments` · `health_tasks` · `health_observations` · `house_prep_cycles` · `house_prep_steps` · `house_status_history` · `warehouses` · `suppliers` · `carriers` · `products` · `inventory_movements` · `shipments` · `wastage` · `inventory_transfers` · `notifications` · `stocktakes` · `stocktake_items` · `users`.
- **تحمله قابلًا للعدم بحقّ (٢):** `breed_standards` — **`NULL` تعني منحنًى عامًّا مشتركًا**، بفهرس جزئي يحرس وحدته؛ و`admin_audit_log` — **`NULL` تعني فعلًا غير مخصوص بمستأجر**. **ولا مفتاح من أيّهما إلى جدول مستأجر**، فلا يُصيبهما ما يُصيب المفتاح المركَّب حين يكون أحد عموديه `NULL` (`MATCH SIMPLE` يُرضيه العدم) — **فُحص ولم يُفترض**.
- **لا تحمله بحقّ (٢):** `tenants` — **هو المستأجر، و`id` هو المفتاح**؛ و`platform_admins` — **عابرٌ للمستأجرين بحكم القرار 194**، **و`CLAUDE.md` تنصّ على أنه «جدول بلا `tenant_id` إطلاقًا»**، وبه زال آخر استثناء من قاعدة المفتاح المركَّب.
- **لا تحمله بلا حقّ (٤):** الأربعة أعلاه. **وليس فيها خامس.**

**٢. وعقبةٌ اكتُشفت في الطريق ولم تكن في وصف الثغرة: مرجعان فريدان مفقودان.** `health_tasks` و`health_observations` **بلا `UNIQUE (id, tenant_id)`** — **و Postgres يرفض المفتاح المركَّب بلا مرجعٍ فريد مطابق ولو كان `id` مفتاحًا أساسيًّا** (القاعدة الملزمة تنصّ على هذا الشرط بلفظه). **فغيابهما كان يمنع الإصلاح لا يؤجّله**، وأُضيفا قبل المفاتيح التي تشترطهما.

**وهذا ما تعنيه «تحقّق من وجوده على كل هدف قبل أن تكتب المفتاح ولا تفترضه»:** أربعة من الأهداف الستة كانت تحمله وائتنان لا — **والافتراض كان سيُنتج ترحيلًا يسقط عند التطبيق لا مراجعةً تُصحَّح**.

**٣. ولم يُوجد أبٌ بلا مستأجر.** فُحص كل أبٍ قبل كتابة الترحيل: `daily_logs` و`health_tasks` و`batches` **كلها تحمل `tenant_id` غير قابل للعدم** — **فلا ثغرة تسبق هذه في الترتيب**، والنقل من الأب مأمون.

**٤. والنقل صريح، وحارسُه بشقّين — والثاني هو ثمرة الثغرة لا سببها.** `tenant_id` يُشتقّ من الأب، **ثم يُقيَّد بعد النقل لا قبله**، وحارسٌ يوقف الترحيل إن بقي صفٌّ بلا مستأجر (**يتيمٌ لا أب له**) **أو إن وُجد صفٌّ عابرٌ للمستأجرين وقع فعلًا**.

**والثاني لا يُصحَّح باجتهاد الترحيل ولا يُحذف:** **إخراج سطرٍ من سجلٍّ ميدانيّ قرارُ إنسان لا خطوةُ نشر** (المبدأ الرابع). **فيُرفع بعدده ويُوقف النشر.** **وأُثبت عمله على البيانات المخالفة نفسها:** الترحيل **سقط برسالته** — «٥ صفًّا عابرًا للمستأجرين موجودٌ فعلًا» (**خمسٌ لا أربع: `batch_diagnoses` يخالف مرتين — في البلاغ وفي كاتبه**) — **ثم مرّ بعد تنظيفها**، **و`tenant_id` اشتُقّ صحيحًا من كل أب**.

**٥. والإثبات بعد الإصلاح على نفس القاعدة ونفس الجمل:** الخمس **رُفضت كلها بأسماء قيودها** — `daily_log_feed_rows_product_id_tenant_fk` · `log_notes_author_id_tenant_fk` · `health_task_executions_executed_by_tenant_fk` · `batch_diagnoses_observation_id_tenant_fk` · `batch_diagnoses_created_by_tenant_fk`. **ولا صفّ استقرّ.**

**وخمسة عشر اختبار تكامل** تحرسها دائمًا — **ولكل جدول من الأربعة: أبٌ من مستأجر آخر ← يُرفض · مستخدمٌ من مستأجر آخر ← يُرفض · ومن نفس المستأجر ← يُقبل** — **ومعها اثنان بنيويّان**: الأربعة تحمل `tenant_id` غير قابل للعدم، **ولا مفتاح مفرد بقي فيها** (يُقرأ من `pg_constraint`، فلا يُضاف مفتاح مفرد جديد بعد اليوم بلا أن يسقط اختبار).

**والإدراج بـ`SQL` مباشرًا لا عبر مسار عمدًا:** المقصود إثبات أن **القاعدة** ترفض، **لا أن طبقة الخدمة تفلتر** — **وحارس الخدمة إجرائي يُعيد الثقبَ أي مسار كتابة جديد لا يمرّ به** (`CLAUDE.md`).

**٦. والواقعة التي تستحق التسجيل ليست الثغرة بل تاريخها.**

**سُمّيت مرتين قبل أن تُصلَح:**

| متى | كيف سُمّيت |
| --- | --- |
| **تقرير PR #69** (القرار 198) | أُصلح `stocktake_items` — **الخامس** — **وسُمّي معه ثلاثة إخوة له بأسمائهم**، ولم يُصلَحوا |
| **القرار 204** | وُجد الرابع (`batch_diagnoses`) في مسحٍ عن المحاسبة — **فالقائمة الأولى لم تكن شاملة**، وسُجّل البند 53 |
| **الآن** | أُصلحت الأربعة، **وأُثبتت الثغرة قبل سدّها كما أُثبتت في #120** |

**والقاعدة المستخلَصة:**

> **قاعدةٌ ملزمة لها مخالفاتٌ قائمة معلومة لا تُقرأ قاعدة، بل توصيةً حسنة.**
> **وليس العطب في نصّها بل في أن مخالفتها لم تكلّف شيئًا:** الثلاثة سُمّيت في تقرير دفعةٍ سابقة **فمرّت دفعتان فوقهما بلا أن يعترض شيء** — لا اختبار ولا فحص آليّ ولا مراجعة. **فالتسمية بلا كلفةٍ تُنتج قائمةً تطول، لا ثغرةً تُسدّ.**
> **ولذلك لا يكفي أن تُصلَح الأربعة:** يُضاف **اختبارٌ بنيويّ يقرأ `pg_constraint`** فيسقط عند أول مفتاح مفرد جديد — **فتصير القاعدة مفروضةً لا مكتوبة**، وهو نفس ما فعله القرار 203 حين نقل حكمًا من تعليقٍ إلى حارس.
> **والمقياس: القاعدة التي لا يفرضها شيء تُقاس بمخالفاتها القائمة لا بوضوح نصّها.**

**وحدٌّ معلن — الاختبار البنيويّ يحرس الأربعة لا المخطط كله.** **وتعميمه على الجداول الأربعة والثلاثين** (فحصٌ آليّ في `check:all` يمرّ على كل جدول يحمل `tenant_id` ويرفض أي مفتاح مفرد إلى جدولٍ يحمله) **أقوى وأوسع من هذه الدفعة** — **ويُسمَّى ولا يُبنى هنا**: هو فحصٌ في بوابة البناء لا تعديلُ مخطط، ونطاق هذه الدفعة «مخطط وترحيل فقط».

**ولم تُضَف `UNIQUE (id, tenant_id)` إلى الأربعة نفسها** — **لا شيء يشير إليها اليوم**، وهو نفس ما فُعل بـ`stocktake_items` في القرار 198. **تُضاف يوم يُشار إليها.**

**206. فحصٌ يفرض قاعدة المفتاح المركَّب — والتسمية بلا كلفة تُنتج قائمةً تطول لا ثغرةً تُسدّ**

**فحصٌ ووثائق — لا مخطط ولا ترحيل.** والقرار 205 سمّى تعميم الاختبار البنيويّ عملًا لم يبنه، **وهذه الدفعة تبنيه.**

**ولا يُعاد هنا سرد الثغرة — هي في 205 كاملة.** المسجَّل ما تعلّمناه منها.

**١. القاعدة المستخلَصة، مصوغةً على واقعتها لا على مبدأ عام.**

> **التسمية بلا كلفةٍ تُنتج قائمةً تطول، لا ثغرةً تُسدّ.**
>
> القاعدة مكتوبة منذ #120 و«**ملزمة بلا استثناء**»، **ومرّت تحتها أربع مخالفات**. **وثلاثة منها سُمّيت بأسمائها في تقرير PR #69** — لا في زاوية منسيّة بل في تقرير دفعةٍ أصلحت أخاها الخامس. **ثم مرّت دفعتان فوقها** (199 و200) **بلا أن يعترض شيء**: لا اختبار ولا فحص آليّ ولا مراجعة. **حتى وجدها مسحٌ عن المحاسبة** — بابٌ لا علاقة له بها.
>
> **والدرس ليس «كان يجب أن ننتبه أكثر»** — الانتباه وقع مرتين، **مرة عند التسمية ومرة عند نسيانها**. **الدرس أن التسمية سجّلت الدَّين ولم تُنشئ له مطالِبًا**: نصٌّ في تقريرٍ **لا يقرؤه أحد يوم يكتب جدولًا جديدًا**، **وقيدٌ في بوابة البناء يعترض في تلك اللحظة بالضبط**.
>
> **فالمقياس: القاعدة التي لا يفرضها شيء تُقاس بمخالفاتها القائمة لا بوضوح نصّها** (القرار 205) — **وهذا القرار يجعل عدّادها صفرًا مفروضًا لا صفرًا مرصودًا.**

**٢. المصدر `pg_constraint` لا تعريفات drizzle — وبحجّة، وبما يفوته مسمًّى.**

**يقيس ما هو مطبَّق لا ما هو مكتوب.** **والترحيلات في هذا المستودع مكتوبة ومعدَّلة يدويًّا** — 0016 و0017 و0018 و0019 كلها حُرّرت بعد توليدها (ترتيب `NOT NULL` بعد التعبئة، وحارس داخل الترحيل، ومُشغِّل لا يعرفه drizzle) — **فالمخطط والقاعدة قد يفترقان بحقّ**. **ومفتاحٌ مركَّب يعيش في `schema/*.ts` ولم يصل القاعدة يحمي صفرًا من الصفوف.** والقاعدة تحمي الصفوف لا الملفات.

**وقراءة الكتالوج تعطي عدد أعمدة المفتاح يقينًا** (`conkey`) **بلا تحليل شجرة TypeScript** ولا تمييزٍ هشّ بين `foreignKey({...})` و`.references()` — **وهو نفس الشرط الذي أسقط الفاحص الأول لصحّة enum** (القرار #127: معيارٌ «غير قابل للتحقق بنيويًا فأعلن نجاحًا وهو يحرس صفرًا»). **ونفس الأداة التي أُثبتت بها الثغرة في 205 وفي #120.**

**و CI يهجّر قاعدة الاختبار قبل `check:all` مباشرة** (`ci.yml` خطوتان منفصلتان)، فما يُقرأ رأس الترحيلات بالضبط.

**وما يفوته يُسمَّى ولا يُسكَت:**

- **جدولٌ يُضاف إلى `packages/db/src/schema` بلا ترحيل مولَّد** — لا وجود له في القاعدة **فلا يراه الفحص**. حراسته **فحصُ انحرافٍ بين المخطط والترحيلات** (`drizzle-kit generate` لا يُنتج شيئًا) — **عملٌ آخر لم يُبنَ**، ويُسمَّى هنا.
- **وقاعدةٌ متأخرة تُنتج خضرةً كاذبة** — **فلا تُفترض حداثتها بل تُفحص**: الفاحص يقارن الترحيلات المطبَّقة بعدد مدخلات `_journal.json` **ويُسقط البناء إن كانت القاعدة خلفه**. **ومتغيّرٌ ناقص يُسقط البناء كذلك، لا يمرّ**: بوابةٌ تُتخطّى بمتغيّر بيئة ليست بوابة.

**٣. والاستثناءات قائمة موجبة بعلّتها، لا شرط سالب** (القرار 184). أربعة، مقروءةٌ من 205 بعللها لا بأسمائها:

| المستثنى | العلّة |
| --- | --- |
| `tenants` | **هو المستأجر نفسه** — و`id` هو المفتاح، فلا عمود مستأجر فيه أصلًا |
| `platform_admins` | **عابرٌ للمستأجرين** (القرار 194)، و`CLAUDE.md` تنصّ على أنه «جدول بلا `tenant_id` إطلاقًا» |
| `breed_standards` | `tenant_id` **قابل للعدم بحقّ**: `NULL` تعني منحنًى عامًّا مشتركًا، ويحرس وحدته فهرس جزئي |
| `admin_audit_log` | `tenant_id` **قابل للعدم بحقّ**: `NULL` تعني فعلًا غير مخصوص بمستأجر |

**وأُثبت أن القائمة موجبة لا مُرضية:** أُنشئ جدولٌ مخالف في القاعدة بـ`SQL` (`tenant_id` ومفتاح مفرد إلى `houses`) **فسقط الفحص فورًا بلا سطر كود واحد يُعدَّل** — **فجدولٌ جديد لا يُستثنى بالسكوت** — ثم أُزيل.

**٤. وصنفٌ ثانٍ من المخالفة أضافه الفحص — وهو أخطر من الأول لأنه يبدو صحيحًا.**

**مفتاحٌ مركَّب من جدولٍ `tenant_id` فيه قابل للعدم لا يحمي شيئًا حين يكون العمود عدمًا:** `MATCH SIMPLE` **يُرضيه أن يكون أحد عمودَي المفتاح `NULL` فيتخطّى الفحص كله**. **وأُثبت لا وُصف:** جدول فحصٍ بمفتاح مركَّب سليم الشكل **قَبِل صفًّا بـ`tenant_id = NULL` ومعرّفِ عنبرٍ لا وجود له إطلاقًا**.

**والمفرد يُرى بالعين، وهذا لا يُرى** — فيُحرَس آليًّا أو لا يُحرَس. **ولا حالة منه اليوم**: الجدولان اللذان عمودهما قابل للعدم بحقّ **لا مفتاح من أيّهما إلى جدول مستأجر** — **فالحراسة هنا استباقية لا علاجية**، وهي أرخص ما تكون قبل أن تقع.

**٥. وشُغّل على `main` فكان أخضر: ٧٥ مفتاحًا بين جداول تحمل `tenant_id`، كلها مركَّبة وتشمل `tenant_id`.** **فلا مخالفة خامسة** — ومسحُ 205 كان شاملًا كما ادّعى، **وهذه أول مرة يُقاس فيها ادّعاء الشمول بأداة لا بقراءة**.

**٦. والمرجع الفريد يُعرَض ولا يُسقِط البناء — بحجّة، لأنه عطبٌ من صنف آخر.**

**قراءةُ القيود من القاعدة تجعل فحصه لغوًا للمفاتيح القائمة:** Postgres **لا يقبل إنشاء مفتاح مركَّب أصلًا بلا مرجعٍ فريد مطابق** — **فوجود المفتاح إثباتٌ لوجود المرجع**. **ولا يبقى ما يُفحص إلا الغياب حيث لا مفتاح بعد.**

**والغياب هناك لا يُنتج ثغرة بل يمنع الإصلاح** — وهو ما اصطدم به القرار 205 في `health_tasks` و`health_observations`: **عقبةٌ تُكتشف يوم الإصلاح فتُطيله، لا ثقبٌ يُنفَذ منه**. **وإسقاط البناء عليه يُنشئ قاعدةً جديدة لم يقرّرها أحد** («كل جدول مستأجر يحتاج المرجع») **فيَشي بستة عشر جدولًا لا يشير إليها أحد**.

**فيُعرَض في رسالة الفحص مسمًّى بأسمائه**: هذه هي الجداول التي **يُرفض المفتاح المركَّب إليها حتى يُضاف المرجع** — **فمن يبني مفتاحًا غدًا يقرأ العقبة قبل أن يصطدم بها**، ولا يُعاقَب اليوم على ما ليس مخالفة. **وهو التمييز بين ما يجب أن يسقط البناء وما يجب أن يُقرأ فقط** — وخلطهما يُنتج بوابةً تصرخ فيُسكتها أول من يمرّ.

**٧. والرسالة تسمّي ما قرأته لا ما استنتجته** (القرار #143): الجدول والقيد وأعمدته والهدف — `probe_violation.probe_violation_house_id_fkey` على `(house_id)` ← `houses` — **لا «مخالفة في المخطط»**. **وعرضٌ خام أصدق من تسمية مخترَعة.**

**وهذا الفحص أول فاحصٍ في `check:all` يقرأ من القاعدة** — والعشرة قبله تقرأ المصدر. **والفرق مقصود: هذه القاعدة الوحيدة فيها التي تحرس صفوفًا لا ملفات.**

**207. حوكمة المخازن — أحد عشر حكمًا، وأربعةٌ من أسئلة #161 «حادي عشر» تُغلق**

**قرارات مالك. تسجيل فقط — لا كود ولا مخطط ولا ترحيل.**

**وتنبيهٌ على اللفظ يسري على القرار كله — وهو تكرارٌ لا زلّة:** المالك قال «**مخزن المزرعة**» **والمقصود مخزن الموقع** — **ولا مستوى «مزرعة» في المخازن** (#161 «أولًا»: مركزي · موقع · عنبر). **و#161 «ثالث عشر» ٣ صحّح هذا اللفظ بعينه سلفًا** («**مخزن مزرعته» خطأ لفظ: المقصود مخزن الموقع**). **والآن تكرّر ثالثةً — وهو مكتوبٌ في سؤال #161 «حادي عشر» ٥ نفسه** («هل تُجمَّع في **مخزن المزرعة** أو المركزي»). **فاللفظ يجري على اللسان لأن المزرعة هي الوحدة المألوفة ميدانيًّا، والموقع وحدة إدارية** — **والموقع قد يضمّ أكثر من مزرعة** (#113)، **فالخلط بينهما يُنشئ مخزنًا لكل مزرعة حيث أراد المالك واحدًا للموقع**. **يُكتب هنا بلفظه الصحيح في كل موضع، ويُسجَّل أن التصحيح صار ثلاث مرات.**

**١. أمين المخازن — واحدٌ للمستأجر كله، للمخزن المركزي الرئيسي للشركة.** **لا يُسند بمخزن بعينه**، فليس مقيَّدًا بالإسناد ولا في قائمة الرؤية الكاملة: **نطاقه محورٌ آخر**.

**وهذا يُغلق السؤال الأول** (#161 «حادي عشر» ١: «مخزن واحد بعينه · عدة مخازن مركزية · للشركة كلها») **بالخيار الثالث**.

**وواقعةٌ تُسجَّل لأنها تُقوّي الحكم لا تُضعفه:** القرار 198 **اتّخذ هذا الحكم قبل أن يصل جواب المالك** — `storekeeper` أُضيف دورًا **خارج `ASSIGNMENT_SCOPED_ROLES` وخارج `FULL_VISIBILITY_ROLES` معًا**، **بحكم قلب الحارس (184) لا باختيار**: دورٌ غير مُدرَج **لا يرى شيئًا**. **فما بُني على الافتراض الآمن وافق الحكم حين وصل.** **وهذه ثمرة قلب الحارس بعينها**: الافتراض الآمن **لا يحتاج أن يكون صحيحًا ليكون سليمًا**.

**و`user_assignments.warehouse_id` — بُني في 198 ولا يحتاجه الأمين الأساسي.** تُحقِّق من وجوده: العمود قائم بمفتاح مركَّب `(warehouse_id, tenant_id)` وقيدِ «واحدٌ بالضبط من ثلاثة» وقيدِ استبعاد تداخل ثالث. **ويُسجَّل أنه غير مستعمل في الحالة الافتراضية — لا أنه فائض:** الحالة الافتراضية **أمينٌ واحد لمركزيٍّ واحد فلا إسناد يُكتب**، **والعمود يبقى للحالات القادمة** (أمينٌ ثانٍ لمخزن موقع، أو مستأجرٌ بعدة مراكز). **وحذفه اليوم يُنقض غدًا**، **وتركه بلا تسمية يُقرأ سهوًا** — فيُسمّى.

**٢. و#161 «سابعًا» يبقى كما هو بلا تعديل حرف:** **أمين المخزن لا يرى أرصدة العنابر ولا بيانات الدفعات.**

**ويُسجَّل أنه نُظر فيه ولم يُنقض — والتسجيل هو المقصود لا الحكم:** طُرح على المالك **واجبُ مراجعةٍ يحتاج رؤية العنابر**، **فحُلّ بنقل الواجب إلى من يراها أصلًا لا بتوسيع رؤية الأمين**. **والقاعدة التي يحرسها هذا:**

> **حين يحتاج دورٌ رؤيةً لأداء واجب، يُنقل الواجب قبل أن تُوسَّع الرؤية.**
> **وتوسيع الرؤية يُشترى مرة ويُدفع دائمًا** — لأنه لا يُقيَّد بالواجب الذي بُرِّر به.

**ولولا تسجيل هذا لعاد الحكم يُفتح بنفس الحجّة بعد شهر** — **فالحجّة سليمة الظاهر، والردّ عليها ليس بديهيًّا بل قرار**.

**٣ و٤. الجرد الدوري بمستوييه.**

| المخزن | من يجرد | من يصادق | التوقيت |
| --- | --- | --- | --- |
| **المركزي** | أمين المخازن | **المالك وحده** | — |
| **مخزن الموقع** | المشرف | **المالك** | **عند نهاية الدفعة، لا بتقويم دوري** |
| **مخزن العنبر** | **لا جرد دوري** (حكم ٦) | — | — |

**وكلاهما على المبدأ #155** («من يُدخل رصيدًا لا يصادق عليه»): **المُدخِل غير المصادِق في المستويين**. **وهذا يُغلق السؤال الثالث.**

**ونقصٌ في المخطط وُجد ولم يُفترض، فتُحقِّق:** `stocktakes` يحمل `warehouse_id` و`is_opening` و`approved_by` و`closed_by` بختومها — **ولا رابط بدفعة ولا بدورة إطلاقًا**، **وصفر ذكر لـ`batch` أو `cycle` في `stock-counts.ts` كله**. **فربط الجرد بحدث نهاية الدفعة عمودٌ ناقص** — §7-ب البند 55، **يُسمّى ولا يُبنى**.

**٥. ومخزن الموقع عهدة لا جرد — والفرق أنها معادلة لا عدّ.**

> **الوارد إلى مخزن الموقع = المُرسَل إلى العنابر + المتبقّي فيه**
> **والمُرسَل إلى العنابر = المُستهلَك فيها + المتبقّي فيها**

**وقيمتها في أنها تنحلّ إلى سلسلة موصولة: أي فرق يظهر عند طرفٍ بعينه لا رقمًا عامًّا لا يُعرف صاحبه.** **والعدّ يقول «ينقص عشرون كيسًا»، والمعادلة تقول أين انقطعت السلسلة** — **وهو الفرق بين رقمٍ يُتَّهم به الناس ورقمٍ يُسأل عنه موضع**.

**وهو ثالث ثابت في النظام، ومحوره غير محوريهما — والمقابلة تُكتب لأن خلط المحاور يُنتج فحصًا يقيس غير ما يظنّ:**

| الثابت | محوره | متى يُفحص |
| --- | --- | --- |
| §13.3 | لكل (**مستأجر · منتج**) | **بعد كل عملية** |
| الأكياس الفارغة (#161 «عاشرًا») | **لكل عنبر** | فورًا عند الاستهلاك — «بلا انتظار جرد» |
| **العهدة (هذا)** | **لكل مخزن موقع وسلسلته النازلة إلى عنابره** | **عند نهاية الدفعة** مع الجرد |

**فالأول يحرس الدفتر من التناقض، والثاني يكشف خروجًا غير مسجَّل فورًا، وهذا يحرس عهدةً بين طرفين.** §7-ب البند 57.

**٦. ولا جرد دوري مستقل لمخزن العنبر — ومعادلة العهدة تغني عنه.** **لأن المتبقّي في العنبر إمّا يُستهلك أو يُحوَّل إلى عنبر آخر بحركة مسجَّلة بأمر المشرف** (#159) — **فلا رصيد يتيه بلا أثر**. **والافتتاحي يبقى كما هو**: المربّي يُدخله والمشرف يصادق (#161 «ثالث عشر» ٦ و«ثامنًا»).

**والحكم لا يُلغي عدًّا بل يستبدل به إثباتًا أقوى:** الجرد يقيس **لحظةً**، والمعادلة تحرس **الفترة كلها**.

**٧. الأدوية واللقاحات — صرفها بيد أمين المخزن بموافقة الطبيب، أو بأمر صرف يرسله الطبيب لجرعة علاج لعنبر معيّن.** **وهذا يُغلق السؤال الثاني بـ«نعم، يحتاج موافقة الطبيب».**

**وهو شكل القرار 203 بعينه لا آلية جديدة:** أمرٌ **ببادئ ومصادِق**، **في اتجاهين متناظرين**، **ومن بدأ لا يصادق** — **بطرفين مختلفين: أمين المخزن والطبيب بدل أمين المخزن والمالك.**

**وقُوبلت الحالتان، والفرق واحد يُسمّى:**

| | الصرف الخارجي (203) | صرف الأدوية (هذا) |
| --- | --- | --- |
| الطرفان | أمين المخزن · **المالك** | أمين المخزن · **الطبيب** |
| الوجهة | **خارج المنظومة** — جهة مستفيدة نصًّا | **عنبر داخل المنظومة** |
| الأثر في الدفتر | حركة **«صرف خارجي»** | حركة **«تنفيذ علاج»** أو صرف داخلي — **نوعٌ قائم لا جديد** |
| **الفرق الجوهري** | **بلا عنبر ولا جرعة** | **له عنبر وجرعة** — فالموافقة **طبّية لا إدارية** |

**فلا يُخترع نوع حركة ثالث عشر ثانٍ، ولا آلية أمرٍ ثانية.** **والأمر الطبّي يحمل ما يحمله الطبّي: العنبر والجرعة** — وهما ما يجعل الطبيب هو المصادِق دون غيره.

**٨. وبيع الدواء خارجيًّا لا يحتاج الطبيب.** **والعلّة تُكتب لأنها تمنع قراءة الحكم السابع على إطلاقه:** **موافقة الطبيب تخصّ الصرف للاستعمال — له عنبر وجرعة** — **والبيع الخارجي فعلٌ تجاري بلا عنبر ولا جرعة**، **فيكفيه توقيعا المالك وأمين المخزن** (203). **والطبيب يوقّع على ما يدخل الطير، لا على ما يخرج من الشركة.**

**وتُحقِّق أن 203 لا يقيّد الفئة فعلًا:** `external_issue_orders` يحمل `product_id` **بلا أي قيدٍ على `category`**، **ولا شرط في حارس الحركة يذكر فئة** — **فالدواء والعلف والنشارة سواء**، والنشارة في فئة «مستلزمات». **وهو ما نصّ عليه 203 صراحةً** («النوع لا يُقصر على المركزي… ما يقيّده من يأمر لا الجدول») **ويسري على الفئة كما يسري على المخزن**.

**وفائدةٌ عرضية تُسجَّل لأنها تحوّل خسارةً إلى مخرج:** **بيع دواء قارب انتهاء صلاحيته أفضل من تركه يتلف** — **والصلاحية مسجَّلة على حركة الاستلام منذ القرار 198** (`received_expiry_date`)، **فالنظام يعرف ما قارب**. **فالصرف الخارجي مخرجٌ مشروع لما كان سيصير هالكًا** — **ويلتقي بحكم 203 «لتكتمل الحركة الكلية أمام المالك»**: ما خرج بيعًا يُقرأ بيعًا لا تلفًا.

**٩. والأكياس الفارغة تعود إلى المخزن الذي صُرفت منه — أي مخزن الموقع.** **وهذا يُغلق السؤال الخامس.**

**والقاعدة تتبّعية لا ثابتة: الفارغ يعود من حيث جاء الممتلئ.** **فلا يُكتب موضعُ التجميع رقمًا في الإعدادات** — **يُشتقّ من مسار الممتلئ**، فيصحّ في مستأجرٍ بمخزن مركزي وحده وفي آخر بمخازن مواقع، **بلا سطر كود لكل شكل** (نفس منطق «المستوى حقل فالتوسع إعداد لا برمجة»، #161 «أولًا»). **وهي متسقة مع معادلة العهدة**: ما نزل من مخزن الموقع يعود إليه فارغًا، فالسلسلة تُغلق عند نفس الطرف الذي فتحها.

**١٠. والمحاسب يرى جميع مزارع المالك الذي يعمل معه — فموضعه `FULL_VISIBILITY_ROLES` مع المالك لا القائمة المقيَّدة بالإسناد.** **وهذا يحسم السؤال المفتوح في القرار 204.**

**والعلّة: الإسناد تصديقٌ ميدانيّ لا صلاحيةُ قراءة دفاتر** — المشرف والطبيب يُسندان بالمزرعة **لأن مسؤوليتهما ميدانية على بعضها** (#128)، **والمحاسب مسؤوليته على دفاتر المستأجر كلها** فتقييده بمزرعة **يُنتج دفترًا ناقصًا لا حمايةً**. **وحسابٌ ناقص أسوأ من حسابٍ لا يُفتح.**

**والعزل بين المستأجرين غير متأثر إطلاقًا**: `tenant_id` من الرمز حصريًا (المبدأ السابع)، **فـ«جميع المزارع» تعني مزارع مستأجره وحده** — **وهو التمييز الذي سجّله القرار 204 صراحةً** بين المحاسب و`platform_admins`. §7-ب البند 58.

**١١. و«انقطاع تبريد» يُستبدل بسببين خيارين معًا: «تالف بسبب النقل» و«سوء تخزين».**

**والعلّة تُكتب لأنها تغلق ما فتحه #161 «ثالث عشر» ١١** («قائمة الأسباب أغنى من النموذج **مرتين**»): **«انقطاع تبريد» واقعةٌ في العالم يعجز النظام عن كشفها، ولا يملك ما يُنسب إليه** — `warehouses` **بلا ظروف تخزين**، و`storage_conditions` **صفةٌ على المنتج لا على المكان** (#157 البند ٥). **فالنظام كان يعرض سببًا لا يستطيع كشفه ولا منعه ولا نسبته.**

**والبديلان تصريحٌ من المسجِّل عن حالةٍ رآها، لا كشفٌ يُطلب من النظام** — **وهو نفس تمييز القرار 203 في حقل السبب** («سبب الهالك واقعةٌ في العالم؛ وسبب الصرف الخارجي تصريحٌ من البادئ عن فعلٍ هو صاحبه»). **والفرق أن «تالف بسبب النقل» و«سوء تخزين» يصفان ما رآه المسجِّل في الكيس أمامه**، **و«انقطاع تبريد» يدّعي معرفةَ سببٍ في مكانٍ وزمانٍ لم يشهدهما**.

**وبه تُغلق الشكوى الثانية من الاثنتين** — **والأولى («انتهاء صلاحية») أغلقها القرار 198** حين صارت الصلاحية مسجَّلة على حركة الاستلام. **فالشكوى المسجَّلة مرتين أُغلقت مرتين.**

**ومُسحت `WASTAGE_REASON` كلها لا الاثنتين وحدهما**، والباقي بعد الاستبدال: **«تلف بالرطوبة» · «كسر» · «تلوث» · «أخرى»** — **وكلها حالاتٌ يراها المسجِّل في الصنف أمامه ويصرّح بها**، **لا وقائع يُطلب من النظام كشفها**. **فلا ثالثةَ للشكوى.**

**وثانيةٌ وُجدت في نفس المسح وتُسجَّل بندًا:** `wastage.notes` **نصّ اختياري بلا قيد**، **بينما `external_issue_orders` يُلزم النصّ مع «أخرى» بقيدٍ في القاعدة** (203) — **فسببٌ اسمه «أخرى» بلا نصّ لا يسمّي شيئًا**، **والجدولان يفترقان في نفس الحكم**. §7-ب البند 56.

**وما لم يُحسم — يُسمَّى ولا يُوفَّق فيه باجتهاد:**

**السؤال الرابع في #161 «حادي عشر» يبقى مفتوحًا**: «الصنف المنتهي صلاحيته… **من يأمر بإتلافه؟**» **راجَعتُ الأحكام الأحد عشر بندًا بندًا فلا واحد منها يمسّه**: الحكم ٨ يخصّ **ما قارب** الانتهاء (بيعٌ خارجيّ مشروع) **لا ما انتهى**، **ومخرج المنتهي حركةُ هالك بسبب «انتهاء صلاحية» — والآمر بها غير مسمًّى**.

**وتبيّن أنه سؤال §7-ب البند 54 نفسه بلفظ آخر** («أيحتاج الهالك مصادِقًا وبأي حدّ؟») — **دَينٌ واحد سُجّل مرتين باسمين مختلفين في موضعين**، فبدا اثنين. **وهو نفس ما استخلصه القرار 206**: التسمية تُنشئ قائمةً تطول لا مطالِبًا واحدًا. **يُحسمان معًا أو لا يُحسم أيّهما.**

**وأربعةٌ من الخمسة أُغلقت** (١ و٢ و٣ و٥)، **والإحالة في الاتجاهين** (القاعدة الرابعة في القرار 200): **مُعلَّمة في #161 «حادي عشر» بأسمائها، وفي §7-ب البند 32 خانة (ج)**. **و(ج) لم تفرغ — فيها واحد**، **و(أ) فيها جدول الطلبات والأكياس الفارغة** (والحكم ٩ حسم **أين** تُجمَّع، **والمخطط لها لم يُبنَ**) — **فلا وسم إغلاق للبند 32.**

**ولا كود ولا مخطط ولا ترحيل في هذه الدفعة.**

**208. التسويق وخروج الطيور — خمسة أحكام، وفيها نقضٌ صريح لحكم قائم**

**قرارات مالك. تسجيل فقط — لا كود ولا مخطط ولا ترحيل.**
**وبها تُغلق «ثامنًا» في #160 بالكامل: لا سؤال مفتوحًا فيها بعد اليوم.**

**١. من يسجّل عدّ التحميل: المربّي.** بلفظ المالك: **«هو المسؤول الأول عن خروج أي كميات خارج عنبره»**. **وهذا يُغلق السؤال الأول** (المربّي · المشرف · طرف ثالث) **بالخيار الأول** — **والعلّة عهدةٌ لا قربٌ من المكان**: الطيور في عهدته حتى تغادر بابه، **فمن يشهد خروجها هو من كانت في يده**.

**٢. ونقضٌ صريح: لا عدّ أعمى عند التحميل.**

**«سادسًا» تنصّ اليوم:** «**والعدّ عند التحميل استلام أعمى: من يحمّل يعدّ ويسجّل ولا يرى الرقم المتوقع**». **وهذا منقوض** — **ومكتوبٌ في موضعه هناك لا هنا وحده**، كي لا يقرأه قارئ «سادسًا» قائمًا (القاعدة الرابعة في القرار 200).

**وفعل المربّي مطابقة لا عدّ أعمى، شقّان:** **مطابقة الكميات في التصريح بما تم إخراجه من عنبره** · **وتأكيد هوية السائق** — أهو نفس السائق المذكور بالتصريح. **ولا دور للسائق في التسجيل.**

**والعلّة، وهي مبرّر النقض لا زينته:**

> **الاستلام الأعمى يحمي حين يكون العادّ مستلِمًا، وينقلب حين يكون مُسلِّمًا.**

**في استلام العلف لا مصلحة للعادّ في تضخيم رقمه** — البضاعة **تدخل عهدته** فيزيد ما يُسأل عنه، **فالعمى يمنعه من نسخ رقمٍ يراه ويُلزمه بالعدّ**. **وعند التحميل تنعكس المصلحة**: البضاعة **تخرج**، **والزائد يغادر المزرعة**. **فنفس الآلية تُنتج الأثر المعاكس** — تحمي في اتجاه وتُغري في الآخر.

**وشيئان اختلطا تحت اسم «العدّ الأعمى»، والتمييز بينهما هو ما يجعل الحكم مفهومًا لمن يبنيه:**

| | **حدّ التصريح** | **العمى** |
| --- | --- | --- |
| المسألة | **سلطة** — كم أذن المالك وكم بقي | **دقّة عدّ** |
| يُرى؟ | **يُرى ويُفرض** | يُحجب |
| شرطه | — | **وجود عادَّين يُقارن رقماهما** |
| وإخفاؤه/غيابه | **يحوّل مخالفةً كان يمكن منعها إلى اكتشافٍ بعد فوات الأوان** | **والمربّي وحده يعدّ هنا، فلا رقم ثانٍ** |

**فالعمى بلا رقم ثانٍ يحجب معلومة ولا يقارن شيئًا** — **كلفةٌ بلا عائد**. **وحدّ التصريح يبقى ظاهرًا مفروضًا**: هو الذي يمنع تحميل ما لم يُؤذن به **قبل أن تخرج الشاحنة**، لا بعدها.

**وتُحقِّق نصًّا لا تذكّرًا:** مُسحت الوثيقتان عن «الاستلام الأعمى» و«عدّ أعمى» — **ولا موضع يصف عدًّا أعمى عند التسويق خارج «سادسًا» نفسها**. §3.6 في الوثيقة الشاملة و§14.3 في التقنية و«دورة الشحنة» **كلها تصف استلام الشحنة من المخزن** (المربّي مستلِمًا)، **وهي قائمة لا تُمسّ**. **و#160 «عاشرًا» ٧ يعمّم النمط على ثلاثة مسارات — الشحنة · التحويل · الكتاكيت — والتسويق ليس فيها.** **فالنقض يصيب جملةً واحدة، ولا وصفٌ منقوض بقي في مواصفة ملزمة.**

**٣. تأكيد هوية السائق — ضابط جديد لا يوجد في النظام.** التصريح يسمّي السائق، **والمربّي يؤكّد أن من حضر هو هو**.

**وموضعه الحمولة لا كيان الناقل — على نفس حكم القرار 202 في `vehicle_number`:** **المركبة صفة واقعة لا صفة كيان**، **والسائق كذلك**. **ووضعه على الكيان يجعل للناقل سائقًا واحدًا ويُعيد كتابة الماضي** — فتظهر حمولةُ العام الماضي بالسائق الذي يقود اليوم.

**ونقطة معلَّقة بصياغة المالك المبدئية — لا تُحسم هنا:** **إن حضر سائق غير المذكور، يُسجَّل الاختلاف ولا يُحمَّل إلا بتأكيد المالك** — **لا منعًا مطلقًا** (فقد يُرسَل بديل) **ولا مرورًا بتنبيه يُتجاهَل**.

**وعلاقتها بالمبدأ الخامس** («لا يُمنع الميدان بسبب الإدارة») **تُكتب لأنها تبدو مخالفةً وليست كذلك:** **المبدأ يمنع تعطيل الميدان بنقص إعدادٍ أو رصيد** — **إعدادٌ ناقص لا يغيّر الواقعة، وسائقٌ آخر يغيّر من يأخذ البضاعة**. **فهذه ليست إدارةً ناقصة بل شخصًا آخر يأخذ البضاعة**، **والوقف هنا وقفٌ على الواقعة نفسها لا على ورقةٍ عنها**. **وصيغة المالك تقف في منتصف الطريق عمدًا**: لا منع (فالبديل واقع تشغيليّ) ولا مرور (فالتنبيه يُتجاهَل) — **بل تصعيدٌ إلى صاحب البضاعة**. **ولم تُحسم صياغتها النهائية، فتبقى معلَّقة.**

**٤. حمولة خرجت ولم تصل: لا حالة «في الطريق» للطيور.** بلفظ المالك: **بعد خروج الكميات من العنبر بتأكيد المربّي، لم تعد مسؤولية أحد، والأمر متروك للمالك بتأكيد الحالة مستلمةً أو مبيعة.**

**فالخروج يكتمل عند باب العنبر**، **وتأكيد المالك بعده واقعةُ حالةٍ لا عهدةٌ تنتقل**. **وهذا يُغلق السؤال الثاني.**

**ويخالف نمط العلف عمدًا — والفرق يُسجَّل بعلّته كي لا يُقرأ سهوًا:** #159 يجعل للتحويل حالةً ثلاثية (صادر · في الطريق · وارد) **لأن العلف يعود إن لم يصل** — فالكمية بين طرفين وأحدهما سيستلمها، **والحالة الوسطى تحفظ عهدةً معلَّقة**. **والطير المُحمَّل لا يعود**: خرج من المنظومة، **فحالةٌ وسطى تحفظ عهدةً لا حائز لها** — **ومسؤوليةٌ بلا حائز تُقرأ مسؤولية المربّي بحكم آخر من مسّها**.

**وأثره على «سابعًا» — وهو أثمن ما في الحكم:** شرط الإغلاق («**المستلم يجب أن يساوي مجموع الخروجات بالضبط**، وأي فرق «فاقد غير مفسَّر» لا يُبتلع) **يصير قابلًا للتحقق فعلًا** — **لأن كل خروج موثّق عند باب العنبر بتأكيد المربّي**. **وبحالةٍ وسطى كان الشرط يقبل جوابًا مفتوحًا** («خرجت ولم تصل بعد») **فيُبتلع الفرق تحت اسمٍ مشروع** — **وهو بالضبط التحايل الذي بُني الشرط لمنعه.**

**٥. النافق عند الوصول لا يُخصم من سجل المورّد.** بلفظ المالك: **يُحسب من لحظة تأكيد المربّي استلام الكمية وعددها وكمية النافق عند الوصول، وبها تبدأ الدفعة.**

**فهو شرطُ بداية للدفعة لا حكمٌ على أحد:** **لا على عنبر المربّي** (فلا يدخل نسبة نفوقه — وهو ما نصّ عليه «عاشرًا» ١ سلفًا: «خارج البسط والمقام معًا») **ولا على سجل المورّد**. **وهذا يُغلق السؤال الرابع.**

**والتضييق يُسمّى صراحةً:** #160 «ثانيًا» يقول **«يُنسب للمورّد أو الناقل لا للعنبر»** — **والشقّ الأول ضُيِّق**: يبقى **خارج عهدة المربّي** كما هو، **ولا يُنسب خصمًا في سجل المورّد**. **والحاكم عند التعارض 208** — **مكتوبٌ في موضعه هناك كذلك**، فقارئ «ثانيًا» لا يقرأ منقوضًا.

**والثمن معلن: متابعة أداء المورّد لا تمرّ من النافق بعد اليوم.** **وما يصلح لها مقروءٌ مما هو مسجَّل فعلًا لا مخترَعًا:** **الكمية** المستلمة مقابل المشتراة (#160 «ثانيًا»: الفرق **عجزٌ ظاهر ومحل نزاع**) · **وحقول الاستلام الثلاثة على الحركة** (القرار 198): `received_expiry_date` و`received_withdrawal_days` و`received_storage_conditions` — **فمورّدٌ يسلّم قريبَ الانتهاء متكررًا يظهر في تواريخه**. **ولا مقياس ثالث يُخترع هنا**: أيُّ مقياسٍ آخر لأداء المورّد **قرار مالك لم يصدر**.

**وثلاثة تُسمّى ولا يُجتهد فيها:**

- **السائق والناقل قد يكونان شخصًا واحدًا — والنموذج يحمله في موضعين.** `carriers` كيانٌ باسمٍ (القرار 202)، **والاسم في الواقع كثيرًا ما يكون اسم السائق نفسه** («أبو محمد») — **فحين يكون الناقل فردًا، تسمية السائق على الحمولة تُكرّر نفس الإنسان في مكانين**. **والتفريق بينهما** (شركةُ نقلٍ لها سائقون · أم فردٌ يقود بنفسه) **قرارُ نموذج لم يُطلب**. **يُسمّى ويُوقف عنده.**
- **ولا رمز تسليم مقرَّر للتسويق.** في الشحنة **يملي الناقل رمزًا من أربعة أرقام على المربّي** «يثبت أن الطرفين كانا معًا لحظة التسليم» (§3.6) — **وهنا لا دور للسائق في التسجيل**، **وتأكيد الهوية هو الضابط الوحيد المسمّى في هذا الباب**. **أيُطلب رمزٌ أيضًا؟ لم يُقرَّر** — يُسمّى ولا يُفترض.
- **وصيغة «سائق مختلف» المعلَّقة** (الحكم ٣) — **لم تُحسم**.

**وما بقي مفتوحًا من «ثامنًا»: لا شيء.** الأربعة أُغلقت — الثالث في «عاشرًا» سلفًا، **والثلاثة الباقية هنا**.

**ودفعة التسويق صارت غير محجوبة بأسئلة مفتوحة — ولم تُبنَ:** تصريحٌ وحمولات (#160 «سادسًا» و«عاشرًا» ٦) · **هوية سائق** (هذا القرار) · **وتحويل `sold_bird_count` و`market_avg_weight_g` إلى محسوبين من الحمولات** لا عمودين يُكتبان عند الإغلاق (**«عاشرًا» ٦ و١٢**، والقرار 204 والبند 51). §7-ب البندان 31 و51، **ولا وسم إغلاق لأيّهما.**

**209. الثلاثة الباقية — الهالك بمصادِق، وفحص التبعيات، والنسخ الاحتياطي**

**قرارات مالك. تسجيل فقط — لا كود ولا مخطط ولا ترحيل.**
**وبها يفرغ قسم «ينتظر قرار المالك» في `docs/STATE.md`** — **ولا واحد منها مبنيّ**: **القرار غير التنفيذ، وهما شيئان** (القرار 200).

**١. الهالك بمصادِق — وبنفس قاعدة الجرد في القرار 207 لا بقاعدة شبيهة.**

**تُحقِّق من المخطط أولًا:** `wastage` يحمل `warehouse_id` و`product_id` و`quantity` و`unit` و`reason` و`notes` و`photo_url` و`created_by` و`created_at` — **وصفر عمودٍ للمصادقة**: لا `approved_by` ولا `confirmed_by` ولا نظيرهما. **فمن يسجّل التلف يُخرج الكمية من الرصيد وحده بلا شاهد.**

**وهو يخالف المبدأ #155 في كل موضع آخر:** الجرد بمصادِق (207) · والرصيد الافتتاحي بمصادِق (#155) · وخطوة التجهيز بمعتمِد غير منفّذها (197) · **والصرف الخارجي بتوقيعين** (203). **فالهالك — وهو يُخرج كميةً من الرصيد كما يُخرجها الصرف الخارجي — كان الوحيد الذي يمرّ بتوقيع واحد.**

**والقاعدة: المُخرِج يسجّل وصاحب المستوى الأعلى يصادق.**

| المخزن | يسجّل | يصادق |
| --- | --- | --- |
| المركزي | أمين المخازن | **المالك** |
| الموقع | المشرف | **المالك** |
| العنبر | **المربّي** | **المشرف** |

**وهي بعينها قاعدة الجرد في القرار 207 لا شبيهةٌ بها** — **فلا تُكتب مرتين بصيغتين**. **والمعنى الواحد بصيغتين يتفارق عند أول تعديل**: تُعدَّل إحداهما ويُنسى نظيرها، **فيصير للنظام حكمان في مسألة واحدة** — وهو ما كلّف #99 و#128 سلفًا.

**والحكم يغيّر §12.2 في موضعين، ويُسمَّى التغييران لأنهما ليسا شكليَّين:**

- **المربّي يكسب صلاحيةً لم تكن له:** الصفّ اليوم `❌` له في الهالك، **والحكم يجعله المسجِّل في مخزن عنبره**. **وتوسيع صلاحية دورٍ ميداني يُكتب واعيًا لا يمرّ ضمنًا** — **والمقابل أن المشرف يصادق**، فالتوسيع مقترنٌ بشاهده لا مطلقًا.
- **والمصادقة صفٌّ مستقل لا خانة في صفّ التسجيل** — على نمط ما فُعل بالجرد في 207: **من يسجّل ومن يصادق فعلان لا فعل واحد**، وجمعهما في صفّ يُخفي أن المبدأ #155 يفصلهما.

**وبه يُحسم البند 54 والسؤال الرابع في #161 «حادي عشر» معًا — وهما سؤال واحد سُجّل مرتين باسمين**، كما كُشف في القرار 207. **فالصنف المنتهي صلاحيته يخرج بحركة هالك سببها «انتهاء صلاحية»، والآمر بها هو المصادِق أعلاه بحسب مستوى المخزن.** **ولم يكن ينقص السؤالين حكمان بل حكمٌ واحد** — **وهو ما تُنتجه التسمية المكرَّرة**: دَينٌ يبدو اثنين فيُقدَّر بضعف كلفته ويُؤجَّل بضعفها (القرار 206).

**وحدٌّ للكميات الصغيرة — مسحوب عمدًا، والعلّة تُكتب لأن سحبه يبدو تراخيًا وهو نقيضه:**

**«كمية صغيرة» بلا وحدة تصلح لثلاثة أشياء لا يجمعها رقم واحد** — **خمسة كيلو علف · وخمس جرعات لقاح · وخمس قطع مستلزمات**. **ولا أسعار في النظام نُوحّد بها** (#136: لا حسابات مالية إطلاقًا) — **فالسعر هو المقياس الوحيد الذي يجعل الثلاثة قابلةً للمقارنة، وهو ممنوع بقرار**.

**فأي رقم يُكتب اليوم لا يُبرَّر — وهو ما يمنعه #143**: **لا يُسمّى سبب لم يُقرأ**، **وحدٌّ مخترَع أسوأ من غياب حدّ** لأنه يُقرأ سياسةً مدروسة وهو تخمين. **فكل تلف يحتاج مصادقة في النسخة الأولى**، **ويُجدوَل بندٌ لمراجعته ببيانات حقيقية بعد الميدان**: أي صنف يتلف · وكم مرة · وبأي كمية. **فالحدّ يُشتقّ من واقعٍ يُقاس لا من رقمٍ يُقترح** — §7-ب البند 59.

**٢. فحص التبعيات — أداةٌ وبوابةٌ وقارئ.**

- **`Dependabot` ملفًا في المستودع** — **يفتح PR عند كل ثغرة ويحمل إصلاحها**. **فهو لا يُبلّغ بل يقترح التصحيح**، والفرق أن البلاغ يُضاف إلى قائمةٍ والتصحيحَ يُدمَج.
- **وخطوة في بوابة البناء تُسقط البناء على الثغرة الحرجة أو العالية وحدها لا على كل ثغرة.**
- **والمالك هو من يقرأ النتيجة — تصله PR في نفس القائمة التي يقرأها لا في بريد ثانٍ.**

**وعلّة التمييز بين المستويات تُكتب:** بوابةٌ تسقط على كل ثغرة **تسقط يوميًّا على ثغرةٍ في أداة تطوير لا تصل الإنتاج** — **فيُعطَّل أول من يمرّ بها**، **وبوابةٌ تُعطَّل مرة تُعطَّل دائمًا**. **والتمييز يُبقي للسقوط معناه.** وهو نفس منطق القرار 206 في المرجع الفريد: **ما يجب أن يُسقط البناء يُفصل عمّا يجب أن يُقرأ فقط**.

**وعلّة «نفس القائمة» أدقّ مما تبدو:** بريدٌ ثانٍ يُنشئ مكانًا ثانيًا يجب تفقّده، **وما يحتاج تفقّدًا ثانيًا لا يُتفقَّد** — والـPR تصل حيث يقرأ المالك أصلًا.

**والتنفيذ دفعةٌ مستقلة لا تُبنى هنا، ومعها حدٌّ معلن يُكتب كي لا يُكتشف مرة ثالثة:** **خطوة `ci.yml` تحتاج صلاحية `workflow` لا يملكها الوكيل** — **اصطدم بها القرار 206 فعلًا** حين رُفض دفعُ تصحيحِ تعليقٍ في نفس الملف. **فتُضاف بيد صاحب الصلاحية**، **وملف `dependabot.yml` وحده لا يحتاجها** (ليس workflow). §7-ب البند 41.

**٣. النسخ الاحتياطي — أربعة، وثلاثة منها لم تكن في §3.3.**

- **يوميًّا آليًّا** · **يُحفظ ثلاثين يومًا** — **وكانا مكتوبين في §3.3 بلا قرار خلفهما** (البند 42: «كُتبا في المواصفة بلا قرار، ويُعرضان على المالك لا يُبنى عليهما»). **فما تغيّر أنهما صارا قرارًا لا رقمَين موروثَين.**
- **خارج الخادم — في مزوّد أو منطقة أخرى.** **ونسخة على نفس الخادم تموت معه** — **وهذا الشرط غير مذكور في §3.3 أصلًا**.
- **ويُختبر الاسترجاع قبل أول عميل حقيقي، ثم كل ثلاثة أشهر.**

**والاختبار ليس شكليًّا:** **تُؤخذ نسخة، وتُبنى منها قاعدة، ويُتحقَّق أن الأرقام تطابق.** **فـ«اختبار» بلا هذه الخطوات يقرأ حجم الملف ويُسمّيه اختبارًا.**

**والجملة التي تجعل هذا شرطًا لا تحسينًا مكتوبةٌ في §3.3 نفسها:** «**نسخة لم تُختبر ليست نسخة**». **وبحكمها لا نسخة عندنا اليوم** — **ولم يُختبر استرجاع مرة واحدة قطّ** (البند 42). **فالمواصفة تحكم على حالنا بلسانها، وما كان ينقص قرارَ المالك بل التنفيذ.**

**وتضييقٌ يُسمَّى ولا يُوفَّق فيه:** §3.3 تقول «**استعادة مختبَرة شهريًا**»، **والحكم يجعلها كل ثلاثة أشهر** — **فالدورية خُفّفت لا شُدّدت**. **وهي قرار مالك يعلو نصًّا كُتب بلا قرار** (البند 42)، **والحاكم 209**. **وما اشتدّ في مقابله شرطان لم يكونا**: **خارج الخادم**، **وأول اختبار قبل أول عميل حقيقي لا بعد شهرٍ من التشغيل**. **فالمحصّلة أشدّ لا أخفّ** — **لأن نسخةً تُختبر شهريًّا وهي على نفس الخادم لا تنفع في العطل الوحيد الذي بُنيت له**.

**والتنفيذ ينتظر اختيار خادم الإنتاج ولا يُبنى في فراغ: الدورية والمدّة قرارٌ صار مكتوبًا، والأداة تتبع المنصة** — نسخُ مزوَّدٍ مُدارة أو `pg_dump` مجدوَلًا إلى تخزين خارجي، **ولا تُسمّى أداة قبل أن تُعرف المنصة** (#143). §7-ب البند 42.

**وثلاثة وُجدت في المسح النصّي وتُصحَّح هنا، اثنان منها من صنع دفعاتٍ سابقة:**

- **🔴 §12.2 كانت تناقض نفسها في «الجرد الدوري» — والتناقض من صنع القرار 207.** أضاف 207 **ثلاثة صفوف للجرد الدوري بمستوييه ومصادقته**، **وترك الصفّ القديم المجمَّع «جرد دوري · تحويل · هالك» قائمًا بفاعليه القدماء** (المشرف والطبيب «بفئته» والمالك). **فصار للجرد الدوري حكمان في مصفوفة واحدة.** **وهو عين ما حذّر منه القرار 200** («فصار البند يناقض نفسه داخل خليةٍ واحدة») — **ووقع بعده بدفعتين**. **والعلّة أن الإضافة أسهل من المراجعة**: من يضيف صفًّا يقرأ ما يكتب، **ولا يقرأ ما يُبطله ما كتب**. **فيُفصل الصفّ المجمَّع.**
- **وقائمة الإطلاق تقول «الفحوص الستة تعمل وتفشل البناء» — وهي اثنا عشر** منذ القرار 206. **صُحّحت.**
- **وصفّ الهالك في §12.2 يُفصل بمستوياته ومصادقته** على هذا القرار.

**210. الشخص الواحد بأكثر من صفة — الصفة على الإسناد، والاختيار يُضيّق ولا يوسّع**

**قرار مالك محسوم. تسجيل فقط — لا كود ولا مخطط ولا ترحيل.**
**والتنفيذ بعد المرحلة 3: المسارات التي ستقرأ منه لم تُكتب بعد** (§7-ب البند 60).

**الحال اليوم — مقروءٌ من المخطط لا مفترضًا:**

- **`users.role` عمود `user_role NOT NULL` بقيمة واحدة** — **فالحساب يحمل دورًا واحدًا حتمًا**.
- **والمخرج الطبيعي — حسابان لنفس الشخص — مسدود:** `users_tenant_phone_uq` فهرسٌ فريد على `(tenant_id, phone_e164)` **بلا شرط جزئي**، **و`is_active` عمودٌ خارجه** — **فيشمل المعطَّلين** كما قرّر #23.
- **فالطريق الوحيد اليوم رقمان مختلفان لإنسان واحد.** **وهو يكسر السجل** (فعلان لشخص واحد يظهران لشخصين، ولا تقرير يجمعهما) — **ويُخترق به قيد «المصادِق غير المُنفِّذ»**: **يُدخل بحساب ويصادق بالآخر**، والقيد يقارن `user_id` فيرى رقمين مختلفين **فيمرّ**. **فالثغرة ليست في القيد بل فيما يجعل الالتفاف حوله ممكنًا.**

**١. الصفة على صفّ الإسناد لا على الحساب.** `user_assignments` يحمل **الصفة التي يعمل بها المستخدم في ذلك الموضع**: زيدٌ مشرفٌ على مزرعة أ، وطبيبٌ على ب، **وطبيبٌ أيضًا على أ إن أُسند إليها بالصفتين — صفَّان**.

**و`users.role` يبقى الدور الأساسي ولا يُحذف:** **شاشة الدخول وقائمة التنقّل تحتاج «من أنت» قبل أن تعرف «أي مزرعة تفتح»** — `homeRouteForRole` تختار مكدّس التبويب قبل وجود أي مزرعة مفتوحة. **فالعمودان يجيبان عن سؤالين مختلفين، لا أحدهما نسخةٌ من الآخر.**

**والقرار: عمودٌ على صفّ الإسناد لا جدولٌ ثالث — وبحجّة:**

- **الصفة صفةُ الإسناد نفسه** (شخصٌ × موضعٌ × مدة)، **ولا وجود لها مستقلًّا** — **ولا يُجمَّع عليها شيء** (معيار القرار 202: النصّ يصير كيانًا حين يُجمَّع عليه لا حين يُذكر).
- **وجدولٌ ثالث يسمح لصفّ إسنادٍ واحد أن يحمل صفتين** — **وهو ما لا يريده الحكم**: الحكم يقول **صفَّان** لا صفٌّ بصفتين.
- **والحجّة الحاسمة قيدية:** **قيود الاستبعاد تحرس تفرّد الإسناد في القاعدة لا في الكود** (`EXCLUDE USING gist`، #158 حكم ٢ والقرار 190). **وبعمودٍ على نفس الصفّ تدخل الصفة مفتاحَ الاستبعاد طبيعيًّا. وبجدولٍ ثالث يعبر الحارسُ جدولين فلا يُعبَّر عنه بقيد استبعاد إطلاقًا** — فينتقل إلى فحصٍ في الكود، **وهو ما ترفضه قاعدة المستودع** («قيد في القاعدة لا فحص في الكود»).
- **وطبقة القراءة أسخن ما في النظام:** `assignedHousesFilter` و`visibleFarmCondition` تُنفَّذان مع كل سرد — **وجدولٌ ثالث يضيف وصلةً إليهما بلا مقابل**.

**🔴 وتعارضٌ يُسمّى ويُوقف عنده — وهو أثقل ما في هذه الدفعة:** **الحكم يصطدم بقيد استبعاد قائم ومبنيّ**، **لا بقيد «مستوى واحد بالضبط» كما قد يُظنّ**.

- **`user_assignments_one_level_ck` لا يتأثر**: الصفة ليست مستوًى، والصفّ يبقى حاملًا **واحدًا بالضبط** من `house_id`/`farm_id`/`warehouse_id`.
- **و`user_assignments_farm_period_ex` يمنع الحكم حرفيًّا:** مفتاحه `(user_id WITH =, farm_id WITH =, daterange WITH &&)` — **فصفَّان متزامنان لنفس (زيد، مزرعة أ) مرفوضان من القاعدة اليوم**، وهو بعينه ما يطلبه الحكم.

**والمعالجة إدخال الصفة في مفتاح الاستبعاد** — **وهي لا تُضعف القيد بل تُبقيه على معناه**: كان يمنع «إسنادًا مكرَّرًا لنفس الشخص لنفس الموضع في نفس المدة»، **وهويّة الإسناد تحت النموذج الجديد صارت (شخص · موضع · صفة)**. **ونظيراه على `house_id` و`warehouse_id` يتبعانه.** **والقيود الثلاثة مكتوبة `SQL` خامًا في الترحيل `0009` لا في المخطط** (drizzle لا يعبّر عن `EXCLUDE USING gist`) — **فمن يقرأ المخطط وحده لن يرى ما يجب تعديله**. **يُسمّى هنا ولا يُبنى.**

**وسؤالٌ فرعيّ يُسمّى ولا يُحسم هنا:** **أتُعاد قيمُ `USER_ROLE` نفسها للصفة أم تُعرَّف قائمةٌ أضيق؟** **`USER_ROLE` تحمل `owner` و`storekeeper` و`platform_admin` — ولا معنى لأيٍّ منها صفةَ إسنادٍ على مزرعة**، **فإعادة استعمالها تسمح بصفّ `capacity = 'owner'` لا معنى له**. والخياران: قائمة أضيق، أو قيدُ `CHECK` يحصر القيم المقبولة. **يُقرَّر عند البناء بلا اجتهادٍ اليوم.**

**٢. قواعد الرؤية تُقرأ لكل مزرعة على حدة لا للشخص كله.** **فمزارعُه المرئية اتحادُ ما تفتحه صفوفه، وكلُّ مزرعة بصفتها.**

**وهذا يحترم تحذير #131 ولا ينقضه — والفرق يُكتب لأن من يقرأ #131 وحده قد يظنّه منقوضًا:**

| | **ما حذّر منه #131** | **ما يفعله هذا القرار** |
| --- | --- | --- |
| موضع الشرط | **شرطٌ واحد لكل شخص** يجمع قواعد الأدوار بـ`OR` | **شرطٌ لكل صفّ إسناد**، بصفته |
| الأثر | **إسنادُ عنبرٍ واحد يكفي ليرى المشرف مزرعةً لم تُسند إليه** | **لا يفتح صفٌّ إلا موضعه بصفته** |
| النتيجة | **توسيع صامت** | **اتحادُ نوافذَ ضيّقة، لا نافذةٌ واحدة واسعة** |

**فالتوحيد الذي رفضه #131 يخلط القواعد؛ وهذا يُبقيها منفصلة ويجمع نتائجها.** **والفرق أن الأول يغيّر شرط الرؤية، والثاني يجمع مخرجات شروطٍ لم تتغيّر.**

**٣. المبدّل — الاختيار حيث تقع الحاجة لا عند الباب.**

- **لا شاشة اختيار بعد كلمة المرور.** **الحالة نادرة، فلا يُسأل أربعةٌ من كل خمسة سؤالًا لا يعنيهم أول كل صباح تحت الشمس.** **والصفة تخصّ المزرعة لا الجلسة**، **فاختيارٌ عند الدخول يجبره على قرار قبل أن يعرف أين سيعمل**.
- **ومن يحمل صفتين في نفس المزرعة يرى شريطًا أعلى شاشة تلك المزرعة**: «**تعمل الآن بصفة: مشرف ▾**». **وموضعه أعلى شاشة المزرعة لا في بطاقتها قبل فتحها** — **الصفة تخصّ ما بداخلها، ورؤيتها وهو يعمل أنفع من رؤيتها وهو يختار**.
- **ومن يحمل صفتين في مزرعتين مختلفتين لا يُسأل أصلًا:** **فتح المزرعة يحدّد الصفة وحده.**
- **ومن يحمل صفة واحدة لا يرى شيئًا إطلاقًا** — **لا شريط ولا مساحة محجوزة ولا فراغ يدلّ عليه.**

**وثلاثة قيود بصرية:** **لا نمط بصري جديد يُستحدث** (المبدّل مكوّن قائم في نظام التصميم) · **ولا إيموجي** — أيقونة SVG من `lucide-react-native` · **ولا ظلال ولا تدرّجات** (التطبيق يُستعمل تحت شمس مباشرة).

**٤. والقاعدة الحاكمة التي لا تُكسر:**

> **الاختيار يُضيّق ما يراه ولا يوسّعه أبدًا. والفرض على صفوف الإسناد لا على ما اختاره المستخدم.**

**والعلّة أمنية حرفية:** **لو حُمل الدور المختار في الرمز وفُرض عليه، لصار قيمةً يقولها المستخدم عن نفسه ويصدّقها الخادم** — **وهو بعينه ما يمنعه المبدأ السابع**: `tenant_id` **من الرمز الموقَّع حصرًا لا من الطلب**. **فمن اختار «طبيب» وطلب فعلًا طبّيًّا في مزرعة لا يحمل فيها الصفة يُرفض بحكم إسناده لا بحكم اختياره.**

**والاختبار الصحيح لأي شاشة اختيار — قاعدةً عامة تسري على ما بعدها:**

> **هل يتغيّر ما يُسمح به لو ألغيناها؟ إن كان الجواب نعم فهي ثغرة لا ميزة.**

**والمبدّل يجتاز الاختبار:** إلغاؤه **لا يغيّر ما يُسمح به بحرف** — يغيّر ما يُعرَض وما يُكتب في السجل. **فهو عدسةٌ لا مفتاح.**

**وثلاثة تُسجَّل صراحةً لأنها ستُقرأ خطأً:**

**أ · هذا ليس نظام رؤية بلا صلاحيات.** **من بدّل إلى «طبيب» في مزرعة أُسند إليها طبيبًا فهو الطبيب هناك بكل صلاحياته في §12.2** — **يرى شاشات الطبيب ويتصرّف بها لا يطالعها**. **و«لا شاشة جديدة» تعني لا نمط بصري يُستحدث، لا نفيَ قدرة.** **والصياغة هي التي أوقعت اللبس أول مرة، فيُكتب التمييز بلفظه** كي لا يتكرّر.

**ب · والحدّ المقصود: لا يصادق على فعل نفسه.** **من أصدر أمرًا بصفة لا يوافق عليه بصفة أخرى** — **والقيد يقارن الشخص لا الصفة** (#155، والقرار 203: «**الدورُ يتغيّر والشخصُ هو من وقّع**»).

**فهذا هو الفارق الوحيد بين رجلٍ بصفتين ورجلين: الشاهدان يجب أن يكونا إنسانين.** **ولو فُتح لصارت «المصادقة المتبادلة» توقيعًا واحدًا مرتين** — **وسقط معها كل ما بُني عليها**: الجرد (207) · والرصيد الافتتاحي (#155) · والهالك (209) · **والصرف الخارجي بتوقيعيه** (203).

**والثمن معلن:** **في مزرعة فيها رجل واحد يجمع الصفتين، بعض الأفعال ستحتاج توقيع المالك** — **وهو ثمن الرقابة لا عطبٌ فيها**. **ويتسق مع ما سبق:** 203 قَبِل أن **أمر المالك نفسه لا يُخرج شيئًا بلا توقيع أمين المخزن** — **فالتقييد المتبادل مقبولٌ سلفًا في هذا النظام.**

**ج · والصفة تُكتب في السجل مع كل فعل، سواء اختارها أو اشتُقّت من صفّ واحد.** **فيُقرأ بعد شهر «زيد أقرّ هذا بصفته مشرفًا» لا «زيد أقرّ هذا».**

**وهذا هو المكسب الحقيقي من المبدّل، وغير المرئي منه أهمّ من المرئي:** الشريط يُرى دقيقة، **والصفة في السجل تُقرأ بعد سنة حين يُسأل عن فعلٍ لا يُذكر سياقه**. **والفعل بلا صفةٍ يُنسب إلى الدور الأساسي بالحدس** — **وحدسٌ في سجلٍّ لا يُعدَّل أسوأ من فراغ**.

**والمواضع التي تقرأ الدور اليوم وتحتاج أن تقرأه من الإسناد — معدودةٌ بأسمائها لا موصوفة، فحجم الدفعة القادمة يُقاس بها:**

| الصنف | العدد | المواضع |
| --- | --- | --- |
| **مواضع قرارٍ تتأثر** | **١١** | `entityScope.ts` ×٧ — `visibleFarmCondition` (`hasFullVisibility` · `isAssignmentScoped` · فرع `farmer`) · `visibleHouseCondition` (فرع `farmer` · الشرط المزدوج) · `visibleHouseScope` (شرطان) · `entityAccess.ts` ×٣ (٢٧٥ · ٣٧٠ · ٣٧٤) · `sitesService.ts` ×١ |
| **مواضع نقلٍ لا قرار** | **٧** | `auth.ts` (يقرأه من الرمز) · `authService.ts` ×٣ (يكتبه في الرمز ويُرجعه) · `sites.ts` · `farms.ts` · `houses.ts` (تمرّره `viewer`) |
| **غير متأثرة** | **١٢+** | **ثمانية `requireRole("owner")`** — **والمالك ليس دورًا يُسند فلا تمسّه الصفة** · و`requireRole.ts` نفسه · و`seed/accounts.ts` · **والموبايل كله**: `homeRouteForRole` و`roleLabel` و`AccountSheet` **دورٌ أساسي يبقى**، و`infrastructureCapabilitiesFor` (`role === "owner"`) |

**والقراءة المهمة من الجدول: الأثر مركَّز لا منتشر** — **ثلاثة ملفات تحمل كل مواضع القرار**، **ولا مسار من الثمانية المحمية بالدور يتأثر**. **وهو ثمرة الفرض المركزي** (المبدأ الأول): **لو كان الفرض منثورًا في الموجّهات لكان العدد بعدد المسارات.**

**211. جدول طلبات المربّي — والطلب ليس ملاحظة**

**تنفيذ حكم مكتوب سلفًا** (#160 و#161 «خامسًا»)، **ونطاق الدفعة مخطط وترحيل فقط — لا مسار API ولا شاشة ولا وظيفة تصعيد مبنيّة.**

**الحال قبل هذه الدفعة — تُحقِّق لا تُفترض:** **لا جدول طلبات في المخطط إطلاقًا** (صفر ذكر لـ`request` في جداول المخطط عدا `request_id` في سجل التدقيق، وهو معرّف طلب HTTP لا طلب مربٍّ)، **و`log_notes` للسجل اليومي وحده** — مفتاحه `daily_log_id`. **فالطلب اليوم إمّا ملاحظة تُقرأ ولا تُتابَع، أو مكالمة لا أثر لها.**

**والعلّة مكتوبة في الحكم نفسه ولا تُعاد صياغتها:** «تأخر العلف يعني توقف نمو، **وهي خسارة لا تظهر في أي تقرير آخر**. ومربٍّ يطلب ومشرف لا يستجيب **أشيع سبب لانخفاض الأداء ولا يعرفه أحد**». **فالجدول ليس تحسين تتبّع بل الشرط الذي يجعل هذه الخسارة قابلةً للرؤية أصلًا.**

**١. حقول الجدول — وما أُضيف على ما سمّاه الحكم يُسمَّى بعلّته.**

**ما سمّاه الحكم:** من طلب (`requested_by`) · لأي عنبر (`house_id`) · أي صنف (`product_id`) · كم (`quantity`) · متى (`created_at`) · وحالته (`status`).

**وثلاثة أُضيفت:**

| المُضاف | العلّة |
| --- | --- |
| `tenant_id` | **القاعدة الملزمة** — ومعه كل مفتاح مركَّبًا `(fk, tenant_id)` |
| `unit` | **الرقم بلا وحدته نصف مصدر** (القرار 201). **و«كم» بلا وحدة لا يُلبّى**: كيسٌ أم كيلو أم عبوة — **والمشرف الذي يقرأ «١٠» لا يعرف ماذا يُخرج** |
| `fulfilled_at` | **يقترن بالحالة بقيد** — نمط `external_issue_orders` (203): لا تلبية بلا وقتها ولا وقتٌ بلا تلبية |

**ولم يُضف `uuid`**: لا شيء يشير إلى الطلب من خارج القاعدة (بخلاف أمر الصرف الذي يشير إليه الدفتر بـ`source_uuid`) — **والمرجع الوحيد إليه مفتاحٌ مركَّب**. **ولا `notes`**: لم يسمّه الحكم، **ولا يُخترع حقلٌ لأن الجدول يبدو ناقصًا بدونه**.

**٢. والمرجع على التحويل لا على الطلب — قرارُ نموذج بحجّة.**

**تُحقِّق أولًا:** أمر الصرف من مخزن الموقع إلى مخزن العنبر **هو صفٌّ في `inventory_transfers`** — طرفاه `from_warehouse_id`/`to_warehouse_id`، ومخزن العنبر مستوًى فيه (القرار 198). **وآلية #159 الثلاثية تُعمَّم عليه** وهي بندٌ باقٍ في §7-ب (البند 32 خانة (ب)).

**والفرضية المطروحة — «طلبٌ واحد قد يُلبّى بأكثر من تحويل، وتحويلٌ واحد قد يلبّي أكثر من طلب» — فُحصت في النصّين ولم تُؤكَّد:** **#160 «خامسًا» يقول «يُربط الطلب بالأمر» بالإفراد**، **و#159 لا يذكر الطلب إطلاقًا** — **فلا نصّ يُجيز التلبية الجزئية ولا نصّ يمنعها**.

**فالقرار: المرجع على `inventory_transfers.request_id`** — **وهو الشكل الذي لا يقرّر المسألة ولا يُغلقها:**

- **يوافق اتجاه الحكم السببيّ:** «للمشرف إصدار أمر صرف **مباشرة منه**» — **الأمر يصدر من الطلب فيعرف مصدره**، لا العكس.
- **ويحتمل أكثر من تحويل للطلب الواحد بلا جدول وسيط** — **بمجرد تكرار المرجع**. **ولو كان المرجع على الطلب** (`fulfilled_by_transfer_id`) **لأغلق التلبية الجزئية بعمود واحد** — **وهو إغلاقٌ لم يقرّره أحد**.
- **ولا يُنشئ جدولًا وسيطًا لم يُطلب** — **وبناء بنيةٍ لاحتمالٍ لم يُقرَّر أسوأ من تركه مفتوحًا** بالشكل الأبسط.

**والنصف الثاني من الفرضية — «تحويلٌ واحد يلبّي أكثر من طلب» — هذا الشكل يقول «لا»**، **ويُسمَّى صراحةً**: **إن قرّر المالك خلافه فهو جدولٌ وسيط يُضاف لاحقًا** بنفس نمط ترحيل 0017 (قيم متمايزة تصير صفوفًا). **والاتجاه الآخر — جدولٌ وسيط اليوم ثم تراجع — أغلى.**

**والتلبية الجزئية نفسها قرار مالك يُوقَف عنده** (§7-ب البند 61): **لأنها تجرّ حالةً ثالثة** («ملبّى جزئيًّا») **لم يسمّها أحد** — **والشكل المختار يحتملها بلا أن يفترضها**.

**٣. والحالات قيمتان لا ثلاث: مرفوع · ملبّى.**

**الحكم يسمّي «لم يُلبَّ» ولا يسمّي رفضًا.** **والفرق ليس لفظيًّا: الرفض واقعةٌ بسبب مكتوب، والترك صمت** — **والتصعيد في الحكم يقوم على الصمت** («طلب لم يُلبَّ **خلال مدة** يظهر للمالك»).

**وقيمةُ رفضٍ تغيّر معنى التصعيد لا تضيف إليه:** **أيتصاعد المرفوض؟** **إن لم يتصاعد صار الرفض بابًا لإسكات الطلب** — يضغط المشرف «مرفوض» فيسقط الطلب من قائمة المالك، **وهو نقيض ما كُتب الحكم لأجله**. **وإن تصاعد فما فائدة الرفض؟** **فالمسألة قرار مالك لا تفصيل مخطط**، §7-ب البند 61. **ولا تُخترع القيمة.**

**٤. والتصعيد — لا يلزمه عمود واحد جديد، وهذا هو الاكتشاف.**

**تُقرأ §16 فتُسمّى بنودها الخمسة** (cron ساعي، **كلها للمالك**): **بلاغ شديد >ساعتين · ملاحظة >٤٨ ساعة · شحنة >٤٨ ساعة · نزاع >٧٢ ساعة · عنبر بلا تسجيل يومين**.

**فالطلب غير الملبَّى بندٌ سادس على نفس الآلة لا آلية جديدة** — **واستعلامه `status = 'مرفوع' AND created_at < now() - العتبة`**. **والعمودان قائمان لسببٍ آخر** (الحالة لأنها حالة، و`created_at` لأنه «متى» في الحكم) — **فلا عمود يُضاف للتصعيد ولا حقل حالةٍ ثانٍ**. **ولا فهرس يُبنى قبل أن يوجد استعلامه.**

**والعتبة — اقتراحٌ معلَّق لا قرار، وحجّته من جنس عتبات §16:** **أربعٌ وعشرون ساعة**. **أقصر من الشحنة (٤٨) لأن الطير يأكل يوميًّا فالضرر يتراكم بدورة يوم لا يومين**، **وأطول من البلاغ الشديد (ساعتان) لأن العلاج لا ينتظر دورة صرف والعلف ينتظرها**. **والعتبة قرار مالك** — §7-ب البند 61.

**٥. والتجميد — ما يُجمَّد هنا غير ما جُمِّد في 203، والفرق في شكل الكيان لا في المبدأ.**

**أمر الصرف الخارجي له طور مسوّدة** («معلّق») **فجُمِّد الصفّ كله بعد القرار**. **والطلب لا مسوّدة له — مرفوعٌ ساعةَ يوجد** — **فيُجمَّد جوهره من ميلاده وتبقى حالته وحدها حيّة**: `tenant_id` · `house_id` · `product_id` · `quantity` · `unit` · `requested_by` · `created_at`.

**و`created_at` أخصّها بالحراسة: هو ما يُقاس منه التصعيد** — **فتعديله يُخفي طلبًا متأخرًا بجعله جديدًا**، وهو التفافٌ على الحكم من داخله.

**ولماذا حارسٌ لا `CHECK`:** القيد يرى الصفّ الجديد وحده **فلا يعرف ما كان** — **و«لا يُعدَّل» حكمٌ على الانتقال لا على القيمة** (نفس علّة 203 و209).

**ولا يُحذف إطلاقًا** — **بخلاف أمر الصرف الذي يُحذف معلَّقًا**: **حذفُ طلبٍ لم يُلبَّ يمحو الدليل الذي كُتب الحكم لحفظه بعينه**، **وحذفُ الملبَّى يترك تحويلًا يشير إلى عدم**.

**والإثبات على القاعدة — ستة عشر اختبارًا:** طلبٌ لعنبر مستأجر آخر · ولصنفه · ويرفعه مستخدمه · وتحويلٌ يشير إلى طلب مستأجر آخر ← **الأربعة يرفضها المفتاح المركَّب** · ومن نفس المستأجر ← يُقبل · وكمية غير موجبة ← تُرفض · و«ملبّى» بلا وقت ووقتٌ بلا «ملبّى» ← يُرفضان · ومعًا ← يُقبلان · **وتعديل الكمية ووقت الرفع والطالب والعنبر ← يُرفض** · **وحذف الطلب ← يُرفض** · **والحالة ووقتها يتغيّران ← يُقبلان** · **وطلبٌ واحد يحمله تحويلان ← يُقبلان** · وتحويلٌ بلا طلب ← يُقبل.

**وأُثبت أن حارس التجميد حامل للحمل — بدقّة:** بتعطيله **سقطت ثلاثة**: تعديل الكمية · وتعديل `created_at` · والحذف. **ولم يسقط اختبار تعديل العنبر والطالب** — **لأن المفتاح المركَّب يرفضه استقلالًا عن الحارس**: تغييرُ `house_id` إلى عنبر مستأجرٍ آخر يخالف المفتاح قبل أن يبلغ الحارس. **فالحارس يحمل ثلاثة، والمفتاح يحمل الباقي — ولا يُنسب إلى أحدهما عمل الآخر.**

**212. الأكياس الفارغة — صنفٌ مستقل يُبقي ثابت الدفتر سليمًا**

**تنفيذ حكم مكتوب سلفًا** (#161 «عاشرًا» و«ثالث عشر» ٨)، **ونطاق الدفعة مخطط وترحيل فقط — لا مسار API ولا شاشة.**

**١. الرصيدان — وهو أهمّ قرار في الدفعة، ومعيار الاختيار ثابت §13.3 لا الأناقة.**

**الدفتر يعنون `(warehouse_id, product_id)`** (القرار 199)، **والرصيد مجموع الحركات**. **وثلاثة أشكال تُقابَل بأثرها على الثابت لا بشكلها:**

| الشكل | أثره على §13.3 | أثره على المعادلة | الحكم |
| --- | --- | --- | --- |
| **صنفٌ مستقل** | **يبقى صحيحًا بحرفه** — صنفٌ كسائر الأصناف، ومجموع حركاته يساوي مجموع أرصدته | **استعلامٌ واحد** على نفس الجدول | **المختار** |
| بُعدٌ على الحركة | يبقى صحيحًا **إن جُمِع على البُعد** — **لكنه يوسّع مفتاح الرصيد لكل الأصناف** ليخدم فئةً واحدة، **وكلُّ قراءة رصيدٍ بعده تخاطر بخلط الممتلئ بالفارغ إن نسيت التجميع** | استعلامٌ واحد | **مرفوض** |
| مخزنٌ ثانٍ للعنبر | يبقى صحيحًا | استعلامٌ واحد | **🔴 مرفوض بقيدٍ قائم**: `warehouses_house_uq` — **«مخزن العنبر واحد لكل عنبر»** (القرار 198) |

**والمعيار الذي حكم: أيّها لا يحتاج استثناءً في ثابت الدفتر.** **والثلاثة تجتازه** — **فحُكم بينها بالثاني: أيّها لا يوسّع ما يحرسه الثابت**. **وبُعدٌ على الحركة يوسّع مفتاح الرصيد في جدولٍ يقرؤه كل مسار مخزون قادم** — **وهو نفس شكل ما نُهي عنه في القرار 201**: عمودٌ يعني شيئًا لفئة واحدة يُفرض على الكل.

**وما يخسره المختار — يُسمَّى:** **«الكيس الفارغ» صنفٌ في كتالوج المنتجات** وله فئة و`stock_unit`، **وليس شيئًا يُشترى**. **و«مستلزمات» أقرب فئة قائمة ولا تصفه بدقّة** — **ولا تُضاف فئة سابعة لأجله**، فالفئات ستّ بقرار (#161 «ثالث عشر» ٧). **فالخسارة تسميةٌ غير دقيقة في كتالوج، مقابل ثابتٍ لا يُمسّ وعنونةٍ لا تتغيّر** — **وهي مقايضة تُكتب لا تُخفى**.

**وصنفان لكل مستأجر بالضبط** (صالح · تالف) **بفهرس جزئي**، **وقيدٌ يفرض شكلهما**: نظاميّان (`is_system`)، فئتهما «مستلزمات»، ووحدتهما «كيس». **ورصيد الفارغ مجموعهما.**

**والفارغ صنفٌ واحد مشترك بين مراحل العلف لا صنفٌ لكل مرحلة** — **لأن المعادلة تُفحص لكل عنبر لا لكل صنف** (القرار 207)، **والعنبر يمرّ ببادئ ونامٍ وناهٍ في دفعة واحدة**، **وأكياسها الفارغة لا تُفرَّق ماديًّا**.

**٢. وحالتا الكيس — والتالف مفروضٌ في القاعدة لا موصوفٌ في تعليق.**

**«الكيس التالف ليس هالكًا»** («ثالث عشر» ٨): **يبقى في رصيد الفارغ ولا يخرج منه**، **والفرق فرقُ أثرٍ على الرصيد لا فرقُ تسمية** — **تسجيله هالكًا يُنقص رصيد الفارغ فتختلّ المعادلة فتفقد قدرتها على كشف ما بُنيت له**.

**ويفرضه `empty_bag_movement_guard`: حركة `هالك/تلف` على صنف كيسٍ فارغ مرفوضة.** **وهذا هو درس القرار 203 مطبَّقًا** — **قاعدةٌ تُكتب في تعليق ولا تُفرض في القاعدة ليست قاعدة**. **والحارس لا `CHECK`** لأن الشرط يقارن الحركة **بصنفها في جدول آخر**.

**والانتقال من صالح إلى تالف حركتان في نفس المخزن** (`−` من الصالح و`+` على التالف) — **ورصيد الفارغ الكلي لا يتغيّر**، وهو ما تُثبته الاختبارات.

**٣. والمعادلة ثابتٌ ثالث — والقرار 207 قابل الثلاثة سلفًا، فلا تُعاد المقابلة بل تُتمَّم.**

**207 سمّى محورها ووقتها** («لكل عنبر · فورًا عند الاستهلاك — بلا انتظار جرد») **ولم يكن لها حينها شكلٌ يُحسب به**. **وهذا ما يُضاف — صورتها المحسوبة من الدفتر بلا عمود مخزَّن:**

> **لكل مخزن عنبر: Σ (الوارد من أصناف العلف) == رصيد الفارغ (صالح + تالف) + رصيد أصناف العلف**

**وقيمتها أنها تكشف ما لا يكشفه ثابت §13.3 إطلاقًا** — **وهذا أدقّ ما في الباب**: **كيسٌ يخرج بلا تسجيل يُبقي الدفتر متسقًا تمامًا** (الحركة السالبة مسجَّلة ومجموعها يطابق الرصيد) **ويُظهر فرقًا في المعادلة فورًا**. **فالثابت يحرس الدفتر من التناقض مع نفسه، والمعادلة تحرس المخزن من الواقع** — **وأُثبت الفرق باختبارٍ يخرج ثلاثة أكياس بلا تفريغ: المعادلة تُظهر ٣ وثابت §13.3 صامد**.

**٤. والاستهلاك حركتان لا واحدة — وثابت §13.3 لا يُختلّ بينهما إطلاقًا.**

`استهلاك يومي` **(−) على صنف العلف** · و`تفريغ كيس` **(+) على صنف الكيس الفارغ** — **ونوعٌ رابع عشر أُضيف لأن الطرف الموجب لا يقابله نوعٌ قائم**: أنواع التحويل تنقل **صنفًا واحدًا بين مخزنين**، **وهذا ينقل بين صنفين في مخزنٍ واحد**.

**واللحظة بين الطرفين لا تُختلّ فيها §13.3 — بخلاف التحويل بين مخزنين:** **الثابت لكل (مستأجر · منتج)**، **والحركتان على صنفين مختلفين** — **فكلٌّ منهما يُبقي ثابت صنفه صحيحًا وحده**. **والذي يختلّ في اللحظة البينية هو معادلة الأكياس لا الدفتر** — **ولذلك تُكتب الحركتان في معاملة واحدة** (المبدأ الثاني)، **لا لأن الرصيد يضيع بل لأن المعادلة تُنبّه على فرقٍ لم يقع**.

**٥. وخروج الأكياس إلى مخزن الموقع تحويلٌ بآلية #159 بلا استثناء.** **و#161 «رابعًا» يقطع اللبس بلفظه:** «**كل حركة من مخزن إلى مخزن تخضع لنفس آلية #159**… **ومخزن العنبر ليس نوعًا خاصًا — هو مخزن مستواه عنبر**». **فلا آلية جديدة، ولا صنف الكيس يستثنى منها.**

**والمشرف يعتمد الخروج** (#161 «عاشرًا») — **ويوافق مصادِقي 207 و209 ولا يخالفهما**: **صاحب المستوى الأعلى يصادق**، **ومخزن العنبر مصادِقه المشرف** في الجرد (207) والهالك (209) **وهنا كذلك**. **فقاعدةٌ واحدة تحكم الثلاثة، ولا تُكتب رابعةً بصيغة أخرى.**

**٦. وبيع الأكياس حالةٌ من القرار 203 لا شيء غيره — وأُثبت أنه يعمل عليها كما هو.** أمر صرف خارجي على صنف الكيس الفارغ من مخزن الموقع، **سببه «بيع»**، **بمصادقة متبادلة ومن بدأ لا يصادق**.

**ولا ينقصه شيء بُني لأجله:** `external_issue_orders` **لا يقيّد الفئة** (تُحقِّق منه في القرار 209: `product_id` بلا قيد `category`، ولا شرط فئة في حارس الحركة)، **ولا يقصر النوع على المخزن المركزي** (القرار 203 نصًّا). **فالكيس الفارغ يمرّ عليه كما يمرّ العلف والدواء والنشارة.**

**وما ينقص ليس خاصًّا بالأكياس بل حدٌّ معلن سلفًا في 203:** **أيّ الدورين يوقّع على صرفٍ خارجيّ من مخزن موقع** — **فرضُ صلاحية يُبنى مع المسارات**. **يُسمّى ولا يُحسم هنا.**

**٧. ولا عمود مالي — والحكم ينصّ عليه بلفظه** («تُسجَّل حركة خروجها **عددًا فقط، بلا سعر ولا قيمة**»، #161 «عاشرًا») **وهو #136 بعينه**. **ولم يُضف عمود واحد من هذا الجنس، ويحرسه اختبار يقرأ `information_schema`.**

**والإثبات على القاعدة — أربعة عشر اختبارًا:** استهلاك كيس ← ينقص الممتلئ ويزيد الفارغ **بمقدار واحد بالضبط** · وثابت §13.3 صامد للصنفين قبل وبعد · والمعادلة متوازنة · **وكيسٌ يخرج بلا تسجيل ← المعادلة تُظهر ٣ والدفتر متسق** ثم يُغلق الفرق بالتسجيل · ونقلٌ من صالح إلى تالف ← **رصيد الفارغ الكلي لا يتغيّر** · **وتسجيل كيس فارغ هالكًا ← يُرفض** · وهالكٌ على علف ← يُقبل (فالمنع على الكيس وحده) · و«تفريغ كيس» على علف أو بكمية غير موجبة ← يُرفض · وصنفٌ ثالث ← يرفضه الفهرس · وكيسٌ غير نظاميّ أو بغير فئته ← يرفضه القيد · **ولا عمود مالي** · وحركة إلى مخزن مستأجر آخر أو على صنفه ← يرفضها المفتاح المركَّب.

**وأُثبت أن الحارس حامل للحمل: بتعطيله سقطت أربعة من أربعة عشر، وبإعادته مرّت كلها.**

**وبقيّةٌ صُحّحت هنا وهي من صنع دفعةٍ سابقة:** **خانة (ج) في البند 32 كانت لا تزال تحمل «الصنف المنتهي — من يأمر بإتلافه؟»** — **وقد حُسم بالقرار 209** مع البند 54 لأنهما سؤال واحد. **فالتصحيح يومها لمس البند 54 و`docs/STATE.md` وترك خانةً ثالثة تنقل السؤال نفسه** — **وهو حرفيًّا ما وصفه القرار 200** («التصحيح لمس رأس الخلية وذيلها وترك وسطها»). **فتُصحَّح الخانة، وتُسجَّل البقيّة لأن تكرارها هو الملاحظة.**

**وخانتا (أ) و(ج) فرغتا معًا — و(ب) لم تفرغ، فلا وسم إغلاق للبند 32** (القرار 200). **والباقي كله مساراتٌ سبعة**، **فالبند صار «مخططًا مكتملًا بمسارات لم تُبنَ»** — **وصفٌ أدقّ مما كان، لا إغلاق.**

**وحدٌّ معلن كان شرط تشغيل هذا كله — ✅ رُفع بالقرار 213:** **بذر الأصناف النظامية بُني**، **وصنفا الكيس الفارغ يُنشآن مع المستأجر في معاملته**. **وأُثبت أن هذا القرار صار يعمل لا أنه بُني وحده**: مستأجرٌ جديد، واستهلاك كيس وتفريغه، **ومعادلة الأكياس تُوازن** — على أصنافٍ لم يُنشئها الاختبار. **والنصّ الأصلي:** بذر الأصناف النظامية غير مبنيّ (القرار 201) — وصنفا الكيس الفارغ نظاميّان لا يُنشئهما مستخدم، فلا شيء يُنشئهما اليوم.

**213. بذر الأصناف النظامية — وبه صار ما بُني في 212 يعمل**

**دالةٌ وبذر ووثائق — لا مسار API جديد ولا شاشة.**

**العلّة: فهرسان يحرسان أصنافًا لا يُنشئها أحد.** `products_system_feed_uq` (198) و`products_empty_bag_uq` (212) — **فما بُني في 212 لا يعمل**: معادلة التحقق تقرأ رصيد صنفَي الكيس الفارغ **وهما غير موجودين**، **فتقرأ صفرًا وتُظهر فرقًا كاذبًا بحجم كل ما استُهلك**.

**وأُعيد التحقق من الحال لا افتُرض:** `INSERT INTO tenants` **موضعٌ واحد خارج الاختبارات** — `apps/api/src/scripts/seed/accounts.ts` (بذر العرض) — **ومعه تجهيزة الاختبارات `test-support/hierarchy.ts`**. **ولا مسار API لإنشاء مستأجر إطلاقًا.** فالقراءة صحيحة، **ولا موضع ثالث**.

**١. دالةٌ واحدة `ensureSystemProducts` — والاسم يقول إنها لا تُنشئ مرتين.**

**تُستدعى في نفس معاملة إنشاء المستأجر** — **فلا يوجد مستأجر بلا أصنافه لحظةً واحدة**، على نمط المخزن المركزي في 198. **ومن موضعين اليوم** (بذر العرض · وتجهيزة الاختبارات) **ومن مسار إنشاء المستأجر غدًا** — **بلا تعديل فيها**، وهو المقصود بموضع الكتابة الواحد (#152).

**وعدم التكرار مفروضٌ بالفهرسين في القاعدة لا بفحصٍ في الكود:** `ON CONFLICT` **يستدلّ على كل فهرس جزئي بشرطه** — `(tenant_id, feed_stage) WHERE is_system AND category = 'علف'` و`(tenant_id, empty_bag_condition) WHERE empty_bag_condition IS NOT NULL`. **وفحصُ «هل توجد؟» قبل الإدراج كان يترك سباقًا بين معاملتين** يمرّان الفحص معًا ثم يصطدمان — **والفهرس لا يترك ذلك السباق**.

**وترجع بعدد ما أُدرج فعلًا** — **فتُكمل الناقص ولا تُعيد البناء**: مستأجرٌ له صنفٌ واحد يُدرَج له أربعة، **ولا يُمسّ الموجود**.

**٢. وخمسة أصناف — مستخرَجة من الفهرسين لا مخترَعة.** `FEED_STAGE` ثلاث قيم (بادئ · نامي · ناهي) → **ثلاثة أصناف علف نظامية**؛ و`EMPTY_BAG_CONDITION` قيمتان → **صنفا كيسٍ فارغ**. **فخمسة لا سادس.**

**ولا صنف نظاميّ ثالث يلزم — مُسح لا افتُرض:** `is_system` **لا مستهلك له في المستودع كله** إلا الفهرس والقيد في `inventory.ts` — **فلا عائلة ثالثة تنتظر البذر**.

**ولا تكتب الدالة وزن الكيس ولا وحدته:** مُشغِّل `products_feed_package_size_default` (القرار 201) **يملأ ٥٠ و«كجم»** — **وكتابتهما هنا تُكرّر الرقم في موضع ثانٍ**، وهو بعينه ما حسمه 201 («لا رقم ٥٠ مكتوبًا في منطق الحساب في أي موضع»). **وأُثبت باختبار يقرأ العمودين بعد البذر ولم يكتبهما.**

**٣. والحكم على الصنف النظاميّ — مستخرَجٌ من الثوابت، ويفارق قراءة المالك في موضع يُسمّى.**

**ما يُمنع، وعلّته أن الآلة تقرؤه:**

| المُجمَّد | ما ينكسر بتغييره |
| --- | --- |
| `is_active` (**تعطيلًا**) | **معادلة 212 تُبطَل** — رصيدُ صنفٍ معطَّل يُستبعَد فيظهر فرقٌ كاذب |
| `feed_stage` · `empty_bag_condition` | **يُحرّك الصنف من خانةٍ إلى خانة** فتصير للمستأجر خانتان فارغتان وأخرى مزدوجة — **بلا أن يعترض الفهرس الجزئي** |
| `is_system` · `category` · `stock_unit` | يُخرج الصنف من نطاق الفهرس أو يُغيّر وحدة ما بُني عليه |
| **الحذف** | يُبطل ما يُبنى عليه، وهو أشدّ من التعطيل |

**وما يبقى مفتوحًا عمدًا — وهنا أفارق قراءتك، فأسمّي:** **الاسم يُعدَّل ولا يُجمَّد.** القراءة المطروحة أن **«إعادة تسمية صنف علف تُغيّر معنى سجلات ماضية»** — **وهي صحيحة في أثرها ولا تُنتج المنع، لسببين:**

- **الاسم هو اسم المنتج التجاري** (#161 «تاسعًا»: «تُدخَل… **اسم المنتج التجاري** · المورّد أو المطحنة») — **ومستأجرٌ يبدّل مطحنته يجب أن يبدّل الاسم**. **ومنعُه يترك في النظام اسمًا لا يطابق ما في المخزن**، وهو ضررٌ حاضر مقابل ضررٍ في تقرير.
- **و«إعادة التسمية تُعيد وسم الماضي» ليست خاصّةً بالصنف النظاميّ** — تسري على كل صنف. **وجوابها في هذا النظام مستقرّ: تُجمَّد القيمة على الصفّ لا يُجمَّد الكيان** — `bag_weight_kg` (201) و`tank_capacity_l` و`received_*` (198). **فمعالجتها حيث تُبنى التقارير، لا بتجميد أسماء خمسة أصناف.**

**فالقاعدة المستخلَصة: يُجمَّد ما تقرؤه الآلة، ويُترك ما يقرؤه الإنسان.**

**والفرض حارسٌ في القاعدة** (`system_product_guard`) **لا طبقة خدمة ولا تعليق** — **درس القرار 203 بلفظه**. **وحارسٌ لا `CHECK`** لأن الحكم على **الانتقال** (ما كان → ما صار) **والقيد يرى الصفّ الجديد وحده**.

**٤. وأين يقع البذر من قاعدة `seed:demo` عبر الـAPI حصرًا — تُكتب الحجّة صراحةً لا يُسكت عنها.**

**القاعدة لا تنطبق، والعلّة في غرضها لا في لفظها:** هي تحرس **بيانات أعمال تُنشأ بصلاحية دور** من أن تلتفّ على الصلاحية — **بذرٌ يُدرج سجلًّا يوميًّا بـSQL يتخطّى كل ما يفرضه المسار**. **والأصناف النظامية لا صلاحية لها تُلتفّ عليها**: **لا دور يملك إنشاءها**، **ولا مسار API لها يُمرّ منه** — **والحارس يمنع تعديل بنيتها بعد إنشائها**.

**وهي بنية لا بيانات عرض، كالمخزن المركزي في 198**: **تُنشأ مع المستأجر لأن المستأجر لا يصحّ بدونها**. **وبذر العرض نفسه يُنشئ المستأجر بإدراج مباشر أصلًا** (لا مسار API لإنشاء مستأجر)، **فالأصناف في نفس معاملته لا تُحدث استثناءً جديدًا بل تكمل ما هو قائم**.

**٥. ولا تعبئة رجعية في ترحيل — والجواب من القاعدة لا من الظنّ.**

**قُرئت القاعدتان:** قاعدة التطوير **ثلاثة مستأجرين وصفر صنف نظاميّ** (بقايا فحوص نقل بيانات سابقة)، وقاعدة الاختبار **١٣٢٤ مستأجرًا** من بقايا جولات الاختبار. **ولا قاعدة إنتاج أصلًا** — §7-ب البند 42: النشر «قبل أول بيانات حقيقية لعميل»، **ولم يصل**.

**فالتعبئة الرجعية تمرّ على بقايا اختبارٍ ومستأجري عرضٍ يُعاد بناؤهم بأمر واحد** — **وتخترع لهم أسماءً تجارية لمستأجرين لم يُنشئهم أحد عمدًا**. **والدالة نفسها هي العلاج متى لزم**: **استدعاؤها على مستأجر قائم يُكمل ناقصه ولا يُكرّر** — **فآلة الإصلاح موجودة وتعمل عند الطلب، ولا يلزم ترحيلٌ يشغّلها على بيانات لا قيمة لها**.

**والإثبات على القاعدة — أحد عشر اختبارًا، وآخرها هو المقصد:**

خمسة أصناف بمراحلها وحالاتها · **ووزن الكيس ووحدته موجودان بلا أن يكتبهما البذر** · **واستدعاءٌ ثانٍ وثالث يُدرج صفرًا** · **ومستأجرٌ بأصناف جزئية يُكمَّل بأربعة ولا يُمسّ الموجود** · وتعطيل صنف كيسٍ فارغ ← يُرفض · وتعطيل صنف علف ← يُرفض · وتغيير المرحلة أو الحالة أو الفئة أو الوحدة أو `is_system` ← يُرفض · وحذف صنف نظاميّ ← يُرفض · **والاسم وحجم العبوة يُعدَّلان** ← يُقبلان · **وصنفٌ عاديّ يُعطَّل ويُحذف** ← فالحارس على النظاميّ وحده.

**وأُثبت أن الحارس حامل للحمل: بتعطيله سقطت أربعة من أحد عشر، وبإعادته مرّت كلها.**

**والأخيرة هي غرض الدفعة كلها:** **مستأجرٌ جديد، واستهلاك كيس وتفريغه، ومعادلة الأكياس تُوازن** (المستلم ١٠ = الفارغ ٤ + المتبقي ٦) — **على أصنافٍ لم يُنشئها الاختبار بل وُجدت مع المستأجر**. **فما بُني في 212 صار يعمل، والحدّان المعلنان في 201 و212 رُفعا وعُلّما في موضعيهما.**

**وواقعةٌ صغيرة تُسجَّل لأنها تُثبت الحارس بلا قصد:** أول صياغة لاختبار «تُكمل الناقص» **حذفت صنفًا نظاميًّا لتصطنع الحالة** — **فرفضه الحارس المكتوب في نفس الدفعة**. **فأُعيدت كتابة الاختبار ببناء حالةٍ جزئية بالإدراج لا بالحذف** — **وهي الحالة الواقعية أصلًا** (مستأجرٌ أُنشئ قبل هذه الدفعة، أو معاملةٌ انقطعت). **والحارس أثبت نفسه على كاتبه قبل أن يُثبته اختبار.**

**وحدٌّ معلن يُسمَّى ولا يُبنى:** **أيظهر «كيس فارغ» في قائمة اختيار الأصناف أمام المربّي؟** **هو أثرٌ محاسبيّ لا شيء يُطلب** — **ويُحسم مع بناء الشاشة**، §7-ب البند 62.

**214. ملف `dependabot.yml` — نصفُ الحكم الثاني في 209، والنصف الآخر بيد صاحب الصلاحية**

**دفعة صغيرة مستقلة، وهي التنفيذ الذي أحال إليه القرار 209** («التنفيذ دفعةٌ مستقلة لا تُبنى هنا»). **ولم يُمسّ `ci.yml` بحرف.**

**١. خمسة بيانات لا واحد — والأنماط تُنسخ من `pnpm-workspace.yaml` لا تُعدّ يدويًّا.**

**تُحقِّق من البنية:** `pnpm-workspace.yaml` يحمل `apps/*` و`packages/*`، **وخمسة `package.json` في الشجرة**: الجذر · `apps/api` · `apps/mobile` · `packages/db` · `packages/shared`.

**🔴 ومنظومة `npm` واحدة على الجذر لا تغطّي الحزم — نُقض بالقرار 217 بقياسٍ على الأداة:** **الجذر يبلغ بيانات الأعضاء ويُحدّثها** (فرعُه عدّل `apps/mobile/package.json` لترقية `expo-secure-store` **وهي غير معلَنة في بيان الجذر إطلاقًا**) — **فالأنماط كانت تُكرّر لا تُكمل، وأنتجت PRين لكل ترقية**. **والنصّ الأصلي يبقى للسياق:** Dependabot **يُعدّل بيانَ الحزمة** (manifest)، **وتبعيات `apps/api` معلَنة في بيانها هي** — **فبيانٌ لا يُذكر لا يُفحص**. **وقفل `pnpm-lock.yaml` واحدٌ في الجذر ويُحدَّث مع أيّها تغيّر**، فالقفل الواحد لا يعني بيانًا واحدًا.

**فـ`directories` بأنماط تطابق ملف مساحة العمل** (`/` · `/apps/*` · `/packages/*`) **لا بقائمة صريحة بخمسة مسارات**. **والعلّة أن القائمة الصريحة تُنشئ مصدرًا ثانيًا للحقيقة**: حزمةٌ تُضاف إلى `packages/` تدخل مساحة العمل تلقائيًّا **وتبقى خارج الفحص حتى ينتبه أحد** — **وهو بعينه صنف العطب الذي سجّله القرار 206** (قاعدةٌ لا يفرضها شيء تُقاس بمخالفاتها). **وبالأنماط تدخل الحزمة الجديدة بلا تعديل هنا.**

**٢. و`github-actions` منظومةً ثانية** — **وإجراءات CI تتقادم كما تتقادم الحزم**، **وثلاثة مثبَّتة بوسم رئيسيّ** في `ci.yml`: `actions/checkout@v4` · `pnpm/action-setup@v4` · `actions/setup-node@v4`. **وموضعها الجذر دائمًا** (`.github/workflows`)، **فـ`directory: "/"` لا أنماط**.

**٣. ونقصٌ في القرار 209 يُسمَّى ولا يُملأ من عندي: الوتيرة ليست فيه.**

**والحقل `schedule.interval` إلزاميّ** — **فلا يُكتب الملف بدونه**، **والنقص لا يمنع الكتابة بل يمنع أن تُقرأ القيمة قرارًا**. **فـ«أسبوعيًّا» اقتراحٌ معلَّق** — **مشتقٌّ من علّة 209 نفسها لا من الذوق**: «**المالك هو من يقرأ النتيجة — تصله PR في نفس القائمة التي يقرأها**» — **وقائمةٌ تُغرَق لا تُقرأ**. **ويوميًّا على خمسة بيانات يُنتج ما يُهجَر، وشهريًّا يترك ثغرةً معروفة أسابيع.** **والعتبة قرار مالك كعتبة التصعيد في 211.**

**ولنفس العلّة جُمّعت التحديثات الصغرى والترقيعية** (`groups`) **وحُدَّ عدد الـPR المفتوحة**: **خمسة بيانات × تبعياتها تُنتج قائمةً تُهجَر**، **والهجر يُبطل الحكم كله** — فالحكم يقوم على أن المالك يقرأ.

**٤. وحدٌّ ثانٍ يُسمَّى كي لا يُكتشف مرة ثالثة — وهو أهمّ ما في هذه الدفعة:**

> **هذا الملف يُعرّف «تحديث الإصدارات» (version updates) وحده.**
> **و«يفتح PR عند كل ثغرة ويحمل إصلاحها» في القرار 209 هو «تحديثات الأمان» (security updates)** — **وهي إعداد مستودع في `Settings ← Code security`، لا سطرٌ في `dependabot.yml`.**

**فالحكم الثاني في 209 نُفِّذ نصفه بهذه الدفعة، ونصفه بيد صاحب الصلاحية** — **ثلاثة أشياء لا يملكها الوكيل، تُجمع هنا في موضع واحد كي تُقرأ مرة**:

| ما ينقص | من يفعله |
| --- | --- |
| **تفعيل تحديثات الأمان** (وتنبيهات Dependabot) | **إعداد مستودع** — صاحب الصلاحية |
| **خطوة البوابة** التي تُسقط البناء على الحرج والعالي وحدهما | **`ci.yml`** — **وتحتاج صلاحية `workflow`** (اصطدم بها القرار 206) |
| **إقرار الوتيرة** | **قرار مالك** — والمكتوب اقتراح |

**والقاعدة المستخلَصة من تكرار هذا الصنف:** **حكمٌ يقع نصفه خارج المستودع يُنفَّذ نصفًا ويُسمّى نصفًا** — **ووسمُه «منفَّذًا» لأن ملفه كُتب هو بعينه وسمُ الإغلاق الذي حذّر منه القرار 200**.

**ولم يُمسّ `ci.yml`:** **الرمز بلا صلاحية `workflow`، والمحاولة تفشل بعد عمل ضائع** — **وقد وقعت فعلًا في القرار 206** حين رُفض دفعُ تصحيحِ تعليقٍ في نفس الملف. **فالحدّ المكتوب هناك عمل هذه المرة قبل المحاولة لا بعدها** — **وهي أول مرة يوفّر فيها حدٌّ معلن عملًا بدل أن يشرح خسارته.**

**ولا فحص آليّ يحرس هذا الملف:** `check:all` **لا يقرأ `.github/`**، **وصحّته يقرّرها GitHub عند الدفع**. **وفُحص محليًّا بنيويًّا لا دلاليًّا** — يُسمَّى ولا يُدَّعى غيره.

**وثانيةٌ من الصنف نفسه ظهرت في هذه الدفعة، وتُسجَّل لأنها تُعمَّم:** صفّ البند 41 في §7-ب **خرج بشرطتين رأسيتين في آخره** (`| |`) — **عمودٌ سادس فارغ في جدولٍ كل صفوفه بخمسة**، **يظهر عند العرض ولا يُرى في المصدر**. **و`docs/` مُستبعَد في `.prettierignore`، فلا مُنسِّق يمسك هذا الصنف** — **ولا فاحص في `check:all` يقرأ جداول Markdown**.

**فالمستودع يحرس كودَه ولا يحرس وثائقه** — **ووثائقه هي المرجع الملزم فيه** (`CLAUDE.md`: الوثيقتان تحكمان كل قرار). **والعطب هنا لا يكسر بناءً بل يكسر قراءة**: جدولٌ بعمودٍ زائد يُقرأ مزاحًا صفًّا كاملًا.

**وأُصلح بعدّ الشرطات لا بالنظر** — **والعدّ هو الدرس**: خللٌ بحرفٍ واحد في سطرٍ طوله ألفُ حرف **لا تمسكه العين**، **ويمسكه `gsub(/\|/,"|")` في سطر واحد**.

**ولا يُبنى له فاحص في هذه الدفعة** — نطاقها ملفٌ واحد، **ولا يُجدوَل هنا كي لا يتّسع تصحيحُ حرفٍ إلى دفعة**. **لكنه يُسمَّى ويُترك مسمًّى:** **فاحصُ اتساق جداول Markdown في `check:all`**، **أو رفعُ `docs/` من `.prettierignore`** — **أيّهما، عملٌ لم يُقرَّر ولم يُجدوَل**. **ومن يقرأ هذا السطر ولم يجد له بندًا فالبند لم يُكتب، لا أنه أُغفل.**

**215. سلسلة لقطات الترحيلات انكسرت — وترحيلٌ يدويٌّ نُسخت لقطته كما هي**

**إصلاح عاجل يسبق كل ما عداه: `drizzle-kit generate` لا يعمل، والمرحلة 3 تبدأ بتعديل مخطط.**

**العطب مقروء لا موصوف:** لقطتا `0021` و`0022` **تحملان المعرّف نفسه والأب نفسه**:

```
0020: id=8f0c86e1…  prevId=4c4e1079…
0021: id=3392ff30…  prevId=8f0c86e1…
0022: id=3392ff30…  prevId=8f0c86e1…   ← نفس المعرّف ونفس الأب
```

**فلابنٍ واحد أبوان، وترفض الأداة السلسلة:** «`[0021_snapshot.json, 0022_snapshot.json] are pointing to a parent snapshot … which is a collision`».

**١. والعلّة في كيفية كتابة الترحيل 0022 لا في الأداة.** كان ترحيل القرار 213 **حارسًا لا تغيير مخطط فيه** (`system_product_guard`) — **و`drizzle-kit` لا يعبّر عن المُشغِّلات فلا يولّد لها شيئًا**. **فكُتب الترحيل بيد، ونُسخت لقطته من سابقتها كما هي** لتبقى السلسلة متصلة شكلًا — **فورثت معرّف أختها ومعرّف أبيها معًا**.

**والنسخ كان الخطوة الصحيحة في نصفها:** محتوى اللقطة **يجب أن يطابق سابقتها** لأن المخطط لم يتغيّر — **وقد تحقّقتُ أن الملفين متطابقان حرفًا بحرف عدا المعرّفين**. **والخطأ في المعرّفين وحدهما**: **اللقطة تصف المخطط وتحمل موضعها في السلسلة معًا** — **والنسخ ينقل الوصف الصحيح ومعه الموضع الخطأ**.

**٢. والإصلاح أضيق ما يمكن: سطران في ملف واحد.** معرّفٌ جديد لـ`0022`، وأبوها `0021` لا جدّها. **ولم يُمسّ `0021` ولا ما قبله، ولا ملف `.sql` واحد** — **والترحيلات مطبَّقة على قواعد قائمة، وتعديل ملف SQL يفتح فجوة بين المكتوب والمطبَّق** لا يكشفها شيء.

**٣. وأُثبت الإصلاح لا ادُّعي:** `drizzle-kit generate` **يعمل بعده ويقول «No schema changes, nothing to migrate»** — **فلم يُنتج ترحيلًا جديدًا**، **واللقطة تصف الواقع**. **ولو أنتج شيئًا لكان العطب أكبر**: لقطةٌ لا تطابق المخطط. **وعدد ملفات `.sql` قبل وبعد: ٢٣ و٢٣.**

**٤. ولماذا لم تمسكها بوابة — والجواب هو الفاحص الجديد.**

**`check:all` لم يكن يشغّل `generate` ولا `check`**، **والاختبارات تقرأ القاعدة المهجَّرة لا ملفات اللقطات** — **فالعطب لا يظهر إلا لمن يحاول توليد ترحيل جديد**. **ومرّت فوقه دفعتان**: **212** (الأكياس الفارغة — وفيها شُغّل `generate` فعلًا **قبل** إنشاء 0022 فمرّ) **و213** (بذر الأصناف — **وفيها وُلد العطب**، وشُغّل `generate` فيها قبل كتابة اللقطة لا بعدها). **ثم ظهر في أول محاولة توليد بعدهما.**

**والفاحص المضاف `drizzle-kit check`** — **وأُثبت في الاتجاهين لا في اتجاه**: **بكسر السلسلة عمدًا سقط برسالة التصادم وخرج برمز ١**، **وبعد الإصلاح خرج بصفر**. **ولا يحتاج اتصال قاعدة** — يقرأ ملفات `migrations/` وحدها.

**وهو مكمِّل لفاحص المفتاح المركَّب (206) لا مكرِّر له:** **ذاك يقرأ القاعدة فيمسك انحراف المطبَّق عن المكتوب؛ وهذا يقرأ الملفات فيمسك انحراف الملفات عن بعضها.** **عطبان مختلفان لكلٍّ فاحصه** — **وقد وقع كلاهما فعلًا في هذا المستودع**.

**والقاعدة المستخلَصة — لأي ترحيل يدويّ قادم:**

> **الترحيل الذي تكتبه بيدك تكتب لقطته بيدك كذلك — أو تنسخها وتصحّح نسبها.**
> **واللقطة تحمل شيئين لا شيئًا واحدًا: وصفَ المخطط، وموضعَها في السلسلة.** **والنسخ ينقل الأول صحيحًا والثاني خطأً** — **فيبدو الملف سليمًا لأن ما يُقرأ منه صحيح، وما يكسره لا يُقرأ إلا من الأداة.**
> **ولا يُختم ترحيلٌ يدويّ قبل تشغيل `drizzle-kit check`** — **وهو اليوم في البوابة فلا يُنسى.**

**وثالثة تُسجَّل لأنها نمط:** **هذا ثالث عطبٍ في هذا المستودع لا تمسكه بوابةٌ لأنها لا تنظر إليه** — بعد أربع مخالفات لقاعدة المفتاح المركَّب (205 و206) **وشرطة رأسية زائدة في جدول Markdown** (214). **والثلاثة من صنف واحد: المستودع يحرس ما يُشغَّل، ولا يحرس ما يُقرأ.** **وكلٌّ منها أُغلق بفاحصٍ بعد وقوعه لا قبله** — **وهو أرخص ما يمكن بعد الوقوع، وأغلى مما لو سُئل السؤال قبله: ما الذي لا ينظر إليه أحد؟**

**ولم تُمسّ ترقيات التبعيات ولا PR #87 في هذه الدفعة.**

**216. ترقية الأمان — وإصلاحُ كودٍ لا تكتبه أداةُ الترقية**

**تنفيذ ما شخّصته دفعة التشخيص، على فرعنا لا على فرع Dependabot** — **لأن الترقية وحدها تُسقط البناء**، **والإصلاح الذي يُنجحها كودٌ لا يكتبه Dependabot**.

**١. `drizzle-orm` 0.36.4 → 0.45.2 — والعطب الذي جاء معها.**

**منذ 0.45 يُغلَّف خطأ المشغّل في `DrizzleQueryError` ويُنقل خطأ `pg` إلى `cause`** — **فالرمز واسم القيد يصيران `undefined` على الخطأ المرميّ**:

```
constructor      : DrizzleQueryError
code             : undefined     ← كان "23505"
cause.code       : "23505"
cause.constraint : "tenants_pkey"
```

**و`pgErrors.ts` كان يقرأهما من الخطأ مباشرة** — فلا يتعرّف على 23505، **فيسقط 409 إلى 500 في خمسة اختبارات**.

**والإصلاح يمشي في سلسلة `cause` لا في مستوى واحد — والعلّة في الاختيار تُكتب:**

- **لا من الأعلى وحده** (ينكسر بعد الترقية) · **ولا من `cause` وحده** (ينكسر قبلها، **وينكسر بعودةٍ إلى الخلف**) · **ولا بفرعين يُجرَّبان بالترتيب** (يعمل، **ويحمل افتراض عمقٍ واحد فينكسر بغلافٍ ثانٍ**).
- **بل بحثٌ عن أول خطأ يحمل رمز `SQLSTATE` في السلسلة** — **فالدالة لا تعرف من غلّف ولا كم غلافًا**، **وتعمل قبل الترقية وبعدها وبعد التالية**.
- **وبحدٍّ للعمق** (`MAX_CAUSE_DEPTH`) — **لأن سلسلة `cause` قد تدور**، وحلقةٌ بلا حدّ تُعلّق العملية بدل أن تُرجع خطأً.
- **وبشكل `SQLSTATE` لا بأي `code` نصيّ** — **خمسة محارف من أرقام وحروف كبيرة** (`23505` · `23P01` · `42P01`)، **ورمز `pg` دائمًا كذلك فالتضييق لا يفقد شيئًا**. **وأول صياغةٍ لهذه الدالة قبلت أيّ `code` نصيّ فتوقّفت عند أول كائن يحمله** — **وأخطاء Node تحمل `code` نصيًّا** (`ECONNRESET`) **ومكتبات تضع `ERR_…`** — **فغلافٌ حاملٌ لرمزه هو يحجب خطأ `pg` تحته فيعود 500 بدل 409**: **العطب نفسه من باب آخر، وداخل الإصلاح المكتوب ضدّه**. **والغلافُ غير المطابق يُتجاوَز ولا يُنهي البحث** — **فالنزول يستمرّ إلى ما تحته لا يقف عنده**.
- **والدرس أن الحجّة المكتوبة فوق الدالة كانت أصحّ من الشرط المكتوب فيها**: النصّ يقول «لا تعرف من غلّف ولا كم غلافًا»، **والشرط كان يفترض أن الغلاف لا يحمل رمزًا** — **فالتناقض بين التعليق والكود لا يُمسك إلا بقراءتهما معًا**، وهو ما مسكه المراجع لا الاختبار.

**والاختبار هو ما كان سيمسكه ولم يكن موجودًا:** `pgErrors.test.ts` كان يختبر **الشكل الخام وحده**، **فمرّ العطب من تحته إلى التكامل**. **وأُضيفت سبعة تختبر الشكلين معًا**: خام · مغلَّف بغلاف · **بغلافين** · و`23P01` مغلَّفًا · وغلافٌ بلا خطأ `pg` (فلا 409 يُخترع) · **وغلافٌ يحمل `ECONNRESET` فوق خطأ `pg`** (فلا يحجبه) · **وسلسلة دائرية** (فلا دوران لا نهائي). **فما لا يُختبر شكلاه لا يُقال إنه يعمل في الحالتين.** **والسابع كُتب قبل إصلاحه وأُثبت سقوطه على الكود القائم** — **فاختبارٌ يُكتب بعد الإصلاح ويمرّ من أول مرة لا يُعرف أيقيس شيئًا أم لا**.

**٢. `@vitest/coverage-v8` — والقِران الذي كسره التثبيت الدقيق.**

كان مثبَّتًا على `2.1.9` **بلا `^`** — **فلم تره مجموعة Dependabot حين رفعت `vitest`**، فظهر `unmet peer vitest@2.1.9: found 3.2.7`.

**ورُفع إلى `^3.2.6` لا إلى تثبيتٍ دقيق جديد** — **والعلّة أن التثبيت الدقيق هو ما أنتج الكسر لا ما منعه**: قُصد به ضمانُ تطابق النسختين، **فأنتج تخلّف إحداهما عن الأخرى**. **ونطاقٌ واحد للاثنين يجعلهما يتحرّكان معًا** — **وهو ما أُريد أصلًا**.

**٣. `drizzle-kit` 0.28.1 → 0.31.10 — لأن الأداتين متقارنتان.**

**و`drizzle-kit` 0.28 هو خطّ `drizzle-orm` 0.36**، **فتركُه يجعل الأداة تقرأ مخططًا كتبته مكتبةٌ أحدث منها بتسع نسخ صغرى**.

**وأُثبت أثر الترقية على الترحيلات قبل أي شيء آخر — وهو أول ما يُتحقَّق منه بعد القرار 215 مباشرة:**

- **`drizzle-kit check` → رمز ٠**، السلسلة سليمة.
- **`drizzle-kit generate` → «No schema changes, nothing to migrate»**، **وملفات `.sql` ٢٣ قبل و٢٣ بعد**.

**فصيغة اللقطة لم تتغيّر ولم يُولَّد ترحيلٌ مختلف** — **وهو العطب الذي كان يُخشى، ولم يقع**.

**٤. والإثبات على السبب لا على النتيجة.** **عُطِّل المشي في السلسلة وحده** — لا الترقية ولا شيء غيره — **فسقطت الخمسة بأسمائها**: `farms` (اسم مكرَّر) · `houses` (اسم مكرَّر) · و`_probe` بصيغ الجوال الثلاث. **وبإعادته مرّت ٤٠٧ اختبارًا.** **فما شُخِّص هو ما أُصلح، لا شيء آخر صادف أن أصلحه.**

**والحصيلة: ٣٤٥ اختبار تكامل و٤٠٧ اختبار وحدة وتغطية، و13/13 في البوابة.**

**٥. وما أُغلق وما بقي — بالقياس لا بالانطباع.**

**أُغلق:** **`drizzle-orm`** — **وهي الوحيدة التي تصل الإنتاج** (تبعية مباشرة في `apps/api` و`packages/db`) · **وثلاث ثغرات `vitest` موسومة Critical** — **وقد كان تعرّضنا لها صفرًا أصلًا** (شرطها «خادم Vitest UI مُنصت»، و`@vitest/ui` غير مثبَّت ولا `--ui` في أي سكربت)، **فأُغلقت بالترقية لا بالحاجة**.

**وبقي أحد عشر:** `vite` ×٣ · `postcss` ×٤ · `image-size` ×٢ · `uuid` · `esbuild`. **ولم تُوسَّع الدفعة إليها**: ترقية سلسلة Expo تمسّ التطبيق كله ولها دفعتها.

> **تصحيحٌ في موضعه (بالقرار 219) — وما كان هنا كان خطأً:**
>
> **كان مكتوبًا أن الأحد عشر «كلها في `@dawajin/mobile` وحدها»، والحجّة أن `pnpm audit` على `@dawajin/api` و`@dawajin/db` و`@dawajin/shared` «يُرجع صفرًا للثلاثة».** **وكلا شقّيه باطل.**
>
> **أمّا الحجّة فباطلة لأن الأمر لا يوجد:** `pnpm audit` **لا يُنطَّق على حزمة أصلًا** — `pnpm --filter @dawajin/api audit` يفشل (`ERROR Unknown option: 'recursive'`)، و`pnpm audit --dir packages/shared` **يُرجع شجرة الجذر نفسها بأحد عشر تنبيهًا** لا صفرًا. **فالصفر رقمٌ نُسب إلى أمرٍ لا يُنتجه** — لم يُقَس ولم يكن قابلًا للقياس بتلك الصورة.
>
> **وأمّا الدعوى فباطلة بالقياس الصحيح** (من `findings[].paths`): **خمسة أزواج (تبعية، خطورة) تبلغ حزم الخادم** — `vite` عالية و`vite` متوسطة ×٢ تبلغ `apps/api` و`packages/shared` عبر `vitest 3.2.7 › vite 5.4.21` · و`esbuild` متوسطة تبلغ **الثلاث جميعًا** عبر `vitest›vite` وعبر `drizzle-kit 0.31.10 › @esbuild-kit` · و`uuid` متوسطة تبلغ `apps/api` عبر `node-cron 3.0.3 › uuid 8.3.2`، **و`node-cron` تبعية إنتاج في `apps/api` لا أداة تطوير**. **والستّة الباقية وحدها مقصورة على `apps/mobile`** (`postcss` ×٤ · `image-size` ×٢).
>
> **ولا يُقلب هذا حكمًا:** الأحد عشر ما تزال **خارج نطاق دفعة 216** ومؤجَّلة، **وتعرّضنا لثغرة `uuid` صفرٌ مقيسًا** (القرار 219، سؤال (أ)). **الباطل هو الحجّة والحصر، لا القرار المبنيّ عليهما.**
>
> **والدرس امتدادٌ لدرس 217:** **رقمٌ يُنسَب إلى أمرٍ لا يُنتجه يمرّ لأنه يوافق ما نتوقّعه.** **فلا يُكتب رقمٌ في قرار إلا وقد رآه كاتبه مخرَجًا لأمرٍ نفّذه — لا استنتاجًا من صيغة الأمر.**

**و`esbuild` المباشر في `apps/api` رُفع إلى 0.28.2** — **والباقي منه نسخٌ متعدية** (0.18 · 0.19 · 0.21 · 0.24) **تحت سلسلة الموبايل**.

**والقاعدة المستخلَصة:**

> **ترقيةُ الأمان التي يفتحها روبوت تُغلق على فرعنا لا على فرعه متى احتاجت كودًا** — **فالروبوت يرفع النسخة ولا يقرأ ما كسرته**.
> **وكسرٌ يظهر في خمسة اختبارات تكامل وسببه سطران في دالةٍ واحدة** — **علامةٌ على أن اختبار الوحدة كان يختبر شكلًا واحدًا لما له شكلان**.

**ولم يُلمس فرع Dependabot ولم تُغلق #87** — **تُغلق بنفسها حين تصل الترقية `main`**.

**217. مدخلُ الجذر يكفي — ونقضُ حجّةٍ في 214 بقياسٍ لا برأي**

**تضييق `.github/dependabot.yml` إلى مدخلٍ واحد على الجذر.** **ونقضٌ صريح لحجّةٍ كتبتُها في القرار 214**، **مشارٌ إليه من هناك** كي لا يقرأها أحدٌ قائمة.

**١. ما نُقض — بلفظه.** كتب القرار 214:

> «**ومنظومة `npm` واحدة على الجذر لا تغطّي الحزم:** Dependabot **يُعدّل بيانَ الحزمة** (manifest)، **وتبعيات `apps/api` معلَنة في بيانها هي** — **فبيانٌ لا يُذكر لا يُفحص**».

**وهذا خطأ.** **ولم يكن مقيسًا يوم كُتب بل مستنتَجًا من وصف الأداة** — **والوصف لا يحكم على السلوك، والسلوك هو الحاكم**.

**٢. والدليل الذي نقضه — من الفروع لا من الوثائق.**

| الفرع | المدخل الذي أنتجه | البيانات التي عدّلها فعلًا |
| --- | --- | --- |
| `expo-secure-store-57.0.2` | **الجذر** (بلا مقطع مجلَّد) | **`apps/mobile/package.json`** |
| `apps/mobile/expo-secure-store-57.0.2` | `/apps/*` | `apps/mobile/package.json` — **نفس السطر بعينه** |
| `vitest-4.1.11` | **الجذر** | **`apps/api/package.json` و`packages/shared/package.json` معًا** |
| `packages/shared/vitest-4.1.11` | `/packages/*` | `packages/shared/package.json` وحده |

**والحاسم في الجدول سطره الأول:** **`expo-secure-store` غير معلَنة في بيان الجذر إطلاقًا** — صفر ورود — **ومع ذلك عدّلها مدخل الجذر في بيان الموبايل**. **فالجذر يبلغ بيانات الأعضاء ويُحدّثها**، **ولا يقف عند القفل**.

**والعلّة أن مساحة عمل pnpm قفلها واحد في الجذر** — **فالأداة تحلّ منه المساحة كلها وتُعدّل البيان المالك للتبعية**، **أيًّا كان موضعه**.

**٣. فالأنماط كانت تُكرّر لا تُكمل — وأُنتج PRان لكل ترقية:** #93 و#94 و#95 تكرّر #96 و#97 و#98 حرفيًّا، **ثلاث ترقيات كلٌّ منها مرّتين**.

**٤. وميزةٌ للجذر لم تكن في الحسبان، وهي أثمن من إسقاط التكرار:** **تبعيةٌ معلَنة في بيانين يُحدّثها الجذر في PR واحدة** (`vitest` في `apps/api` و`packages/shared` معًا) — **والأنماط تُحدّثها في اثنتين مستقلّتين**، **فتُدمج إحداهما وتتأخّر الأخرى فتتفارق النسختان**.

**وهذا بعينه ما كسر قِران `vitest` و`@vitest/coverage-v8` في القرار 216** — **فالشكل المُصلَح هنا يمنع تكرار ذلك الكسر لا يُسقط ضجيجًا فحسب**.

**٥. وما يُخسر بالتضييق — يُسمَّى ولا يُقايَض صامتًا.**

**لا شيء مقيسٌ يُخسر** — **ويُكتب حدّ القياس بدقّة**: **قِيس أن الجذر يبلغ ثلاثة بيانات من الخمسة** (`apps/mobile` · `apps/api` · `packages/shared`)، **والجذر نفسه رابعها بحكم موضعه**. **و`packages/db` لم يُقَس** — **لا لأنه يُستثنى بل لأن لا ترقية مفتوحة تمسّه اليوم** (تبعيتاه رُقّيتا في 216). **والآلية واحدة: قفلٌ واحد ومساحة عمل واحدة** — **فالاستنتاج قويّ، وليس قياسًا، ويُقال كذلك**.

**والخسارة النظرية الوحيدة:** **مدخلٌ لكل مجلَّد يتيح جدولًا أو تجميعًا مختلفًا لكل حزمة** — **ولا نستعمل ذلك ولا حاجة إليه**. **وإن لزم يومًا يُضاف مدخلٌ للحزمة المعنيّة وحدها، لا تُعاد الأنماط.**

**وشرطا الحكم مُستوفيان بالقياس:** **لا حزمة تخرج من الفحص** (الجذر يبلغها كلها) · **ولا PRان لترقية واحدة** (مدخلٌ واحد لا يزاحمه ثانٍ).

**والقاعدة المستخلَصة:**

> **حجّةٌ عن سلوك أداة تُقاس على الأداة لا تُستنتج من وصفها.**
> **و214 كتب حجّته بثقةٍ وهو لم يشغّل الأداة مرة** — **فبدت الأنماط احتياطًا وكانت تكرارًا**. **والاحتياط الذي لم يُقَس لا يُعرف أهو احتياط أم كلفة.**
> **ولم يكشفه فحصٌ ولا مراجعة، بل ستّ PR مفتوحة تكرّر بعضها** — **فالواقع كشفه بعد أسبوع، ولو قِيس يومها لما كُتب**.

**ولم تُغلق أي PR من Dependabot ولم يُدفَع على فروعها** — **الإغلاق يليها بيد صاحب الصلاحية**، **والمكرَّرات تسقط بنفسها حين يصل هذا التضييق `main`**.

**218. فاحصُ تغطية أنماط الكيانات — والحارس يصف نفسه بدل قائمةٍ تُنسى**

**تنفيذ §7-ب البند 44.** **والعلّة واقعة مسجَّلة لا فرضية:** القاعدة موصوفة في `CLAUDE.md` **وفوق القائمة في `app.ts` نفسها** — **ومع ذلك أُضيف `GET /api/farms/:farmId` ونُسي نمطه، فمرّت مزرعةٌ كاملة بلا فحص إسناد** (§7-ب البند 43، والقرار 191). **ولم تكشفه مراجعة ولا اختبار**: كشفته **مخالفتان متعمَّدتان في دفعةٍ أخرى مرّتا بـ200 وكان يُنتظر 403**.

**فالقاعدة كانت موصوفة لا مفروضة — وهو رابع عطبٍ من الصنف الذي سجّله القرار 215**: **المستودع يحرس ما يُشغَّل ولا يحرس ما يُقرأ** (بعد المفتاح المركَّب 205/206 · وجدول Markdown 214 · وسلسلة اللقطات 215).

**١. معرّف الكيان: شكلٌ لا قائمة — وما يُخسر مسمًّى.**

**أي مقطع `:param` في المسار.** **ولا تُعدّ الأسماء** (`houseId` · `farmId` · `siteId` · `batchId`) — **فالقائمة تحمل ثقب ما تحرسه**: **معرّفٌ خامس يُضاف لا يُعدّ فيها فلا يُفحص**، **وهو بعينه شكل العطب الذي وُجد الفاحص ليمنعه**. **وفاحصٌ يُعيد مرض ما يحرسه لا يُبنى.**

**والخسارة تُكتب لا تُقايَض صامتًا:** مقطعٌ ليس معرّف كيان — `/:reportType` مثلًا — **سيُطالَب بنمطٍ أو حارس**. **فالشكل يُنتج إنذارًا كاذبًا حيث تُنتج القائمة ثقبًا صامتًا** — **والمقايضة مقصودة**: **الإنذار الكاذب يوقف البناء فيُكتب له استثناء بعلّة، والثقب الصامت يُشحن**. **ولا مقطع من هذا الصنف اليوم**: المسارات العشرة كلها معرّفات كيانات (`siteId` · `farmId` · `houseId`) — **مقيسًا من الشجرة**.

**٢. الحارس: قِيس أولًا، فتعذّر، فجُعل قابلًا للقياس — لا قائمةً موجبة.**

**حاولتُ القياس قبل البديل كما يقتضي:** `introspectRoutes` يعطي الطريقة والمسار، **والوسطاء ظاهرون في الشجرة عددًا** (مسارٌ محروس يحمل معالِجَين وغيره واحدًا) — **وأسماؤهم كلها `<anonymous>`**، **لأن `requireRole` يُرجع دالةً بلا اسم وأدوارها محبوسة في إغلاقها لا تُقرأ من خارجه**. **فالعدّ يفرّق ولا يميّز: أي وسيطٍ آخر يزيده.**

**والبديل المتّبع في هذا المستودع قائمةٌ موجبة بعلّة** (`ASSIGNMENT_SCOPED_ROLES` · استثناءات `composite-fk`) — **ورُفض هنا بعلّة**: **قائمةٌ ثانية تُكتب بيد تُنسى كما نُسي النمط**، **فيصير للفاحص ثقبٌ من جنس ما يحرسه**. **والقائمة الموجبة تصلح حيث لا يُقاس الشيء؛ وهذا يُقاس بسطرين.**

**فأُعلنت الأدوار على الدالة نفسها** (`RoleGuard.roles`) **فصار الحارس يصف نفسه**، **ويقرأه الفاحص من نفس شجرة التوجيه** — **لا من قائمة ولا من مسحٍ نصّي**.

**والشرط أدقّ من «`requireRole("owner")`» حرفيًّا:** **ألّا يكون في أدوار الحارس دورٌ مُسنَد** — **فالإسناد إنما يُفرض على الأدوار المُسنَدة**، ومسارٌ مقصورٌ على غيرها لا يحتاجه. **فيصحّ اليوم لـ`owner`، ويصحّ غدًا لـ`requireRole("owner", "storekeeper")` بلا تعديل في الفاحص.**

**٣. وتصدير `ENTITY_ID_PATH_PATTERNS`** — كان ثابتًا محليًّا داخل `createApp`. **نُقل إلى نطاق الوحدة بتعليقه كما هو ومُصدِّرًا، ولم يُعَد ترتيب الملف ولا مُسّ سطرٌ آخر.**

**٤. والإثبات أنه يحمل وزنًا — بقياس رمز الخروج مباشرة لا عبر أنبوب** (الفخّ الذي وقعتُ فيه في القرار 215):

- **بمسارٍ حقيقيّ يحمل معرّفًا بلا نمط ولا حارس** (`GET /api/probe/:widgetId`) — **البوابة تسقط برمز ١** ورسالتها تسمّي المسار وسببه.
- **وبإزالته — رمز ٠ و14/14 خضراء.**

**والمقاس مسارٌ مركَّب فعلًا في الشجرة لا كائنٌ مصطنع** — **فما اختُبر هو ما سيقع**.

**والقاعدة المستخلَصة:**

> **حين يتعذّر قياس شرطٍ من الشجرة، فالخيار الأول أن يُجعل قابلًا للقياس لا أن يُستبدل بقائمة.**
> **والقائمة الموجبة أداةٌ لما لا يُقاس** — **وهي هنا كانت ستُنشئ نسخةً ثانية من العطب**: نمطٌ يُنسى، ثم قائمةٌ تُنسى لأن أحدًا نسي أن الطريق الثاني موجود.
> **وسطران في `requireRole` أرخص من قائمةٍ تُصان إلى الأبد.**

**ولم تُوسَّع الدفعة:** فاحصُ جداول Markdown (214) وفاحصُ انحراف `generate` (215) **مسمّيان ولهما دفعتهما**.

**219. بوابةُ الثغرات — بقائمة استثناءات موجبة، وموضعها `ci.yml` لا `check:all`**

**تنفيذ الحكم الثاني في القرار 209** (§7-ب البند 41): **البوابة تُسقط البناء على الحرج والعالي وحدهما**.

**١. وقبل البوابة: تصحيحُ رقمٍ كتبتُه أنا في القرار 216 ومشى.** الادّعاء أن الأحد عشر تنبيهًا «كلها في `@dawajin/mobile` وحدها» **بحجّة أن `pnpm audit` على حزم الخادم الثلاث يُرجع صفرًا**. **والأمر لا يوجد:** لا `--filter` ولا `--dir` ينطّقان `audit` على حزمة — **`--dir packages/shared` يُرجع شجرة الجذر نفسها**، و`--filter` يفشل بـ`Unknown option: 'recursive'`. **والقياس الصحيح ينقض الحصر أيضًا: خمسة أزواج تبلغ حزم الخادم**، وفيها `uuid` عبر `node-cron` **وهي تبعية إنتاج**. **التصحيح مكتوب في موضعه من 216 وفي `STATE.md`** (قاعدة 200: يُلاحَق الادّعاء على مساره لا في موضع واحد).

**واختلافُ عدٍّ يُسجَّل ولا يُطوى:** المالك عدّ ١٣ (٥ عالية · ٨ متوسطة)، **وأنا أقيس ١١ (٥ عالية · ٦ متوسطة)** على `d9be3cf` بـ`pnpm install --frozen-lockfile`. **الخمس العالية متطابقة**، والفرق في المتوسطة — **وأرجّح فارق توقيت في قاعدة التنبيهات لا في الشجرة**، ولم أُثبته فلا أجزم به.

**٢. سؤال (أ): أتبلغنا ثغرة `uuid` على مسار إنتاج؟ لا — والشرط غير متحقّق، فلا وقوف.**
**المسار حقيقي:** `node-cron@3.0.3` **تبعية إنتاج** في `apps/api` وتسحب `uuid@8.3.2`. **لكن التنبيه مشروط بـ«when buf is provided» وبالدوال `v3`/`v5`/`v6`.** **وقُرئت مواضع النداء كلها لا واحدًا:** ثلاثة مواضع — `src/scheduled-task.js:19` و`src/storage.js:11` و`src/background-scheduled-task/index.js:20` — **وكلها `uuid.v4()` بلا وسيط**، **وصفر ورود لـ`v3(`/`v5(`/`v6(`** في الحزمة كلها. **وزيادةً: `node-cron` غير مستورَدة في `apps/api/src` إطلاقًا.** **فالتعرّض صفرٌ مقيسًا لا مقدَّرًا** — **والاستثناء هنا اسمُه صحيح: تنبيهٌ لا يبلغنا، لا خطرٌ نُطيقه.**

**٣. سؤال (ب): أيُغلق شيء بترقية بدل استثناء؟ نعم — أربعةٌ من خمسة، وبثمن.**
`vitest 4.1.11` يسحب `vite ^6||^7||^8` (المتاح 8.2.2، والمُصلَح ≥6.4.3) **فتسقط ثغرات `vite` الثلاث ومعها `esbuild 0.21.5` من جهة vitest**. **ولا تسقط `esbuild` من الجهة الأخرى:** `packages/db › drizzle-kit@0.31.10 › @esbuild-kit/esm-loader › core-utils › esbuild@0.18.20` — **قيدٌ داخل `drizzle-kit` لا نملكه**. **و`uuid` لا مخرج لها إلا بحركة `node-cron` نفسها.** **والثمن: قفزة رئيسية ثالثة في نافذةٍ واحدة** بعد ترقيتي 216، **ومعها `@vitest/coverage-v8` بالقفل**. **ولم يُرقَّ شيء في هذه الدفعة** — الجواب وحده كما طُلب، **وشرط الرفع مكتوب في القائمة نفسها ليجد الترقيةَ من يأتي بعد**.

**٤. سؤال (ج): الموضع — خطوة في `ci.yml`، لا فاحصٌ خامس عشر في `check:all`.**
**والعلّة قيدُ الأداة لا الذوق: `audit` يحتاج شبكة**، و`check:all` **مربوط بـ`&&` قبل كل التزام** (القاعدة 145) **ويُشغَّل بلا شبكة أحيانًا**. **وبوابةٌ تسقط لأن المطوّر بلا شبكة تُدرَّب على التخطّي** — **وهو عين ما حذّر منه 209: «بوابةٌ تُعطَّل مرة تُعطَّل دائمًا»**.
**وما يُخسَر بهذا الاختيار — ويُسمّى:** **الإنذار يصل عند الدفع لا قبل الالتزام**. **وهي خسارة مقبولة لأن الثغرة لا تُحدثها التزامتنا** بل تُكتشف في تبعيةٍ قائمة منذ أيام، **فتأخُّر الكشف دقائق لا يغيّر شيئًا**. **وما كان يُخسَر بالاختيار الآخر أثقل: بوابةٌ حمراء بلا ذنب، ثم تخطٍّ معتاد.**
**والسكربت في المستودع لا في `ci.yml`** — **لتكون قائمة الاستثناءات مراجَعةً في مراجعة كود لا مدفونة في إعدادات**، **و`ci.yml` ينادي سطرًا واحدًا**.

**ونصّ الخطوة مكتوبٌ هنا لا في الملف** (رمزي لا يملك صلاحية `workflow`) — **تُضاف في وظيفة `ci` بعد `pnpm install`:**

```yaml
      - name: بوابة الثغرات (حرج/عالٍ فقط)
        run: pnpm audit:gate
```

**٥. وشكلُ البوابة: قائمة استثناءات موجبة على نمط `composite-fk.ts` — لكل مدخل معرّفه وعلّته وشرط رفعه.**
**والعلّة مقيسة: `pnpm audit --audit-level=high` يخرج اليوم برمز ١** — **فبوابةٌ بلا قائمة حمراء من أول يوم**، **والحمرة الدائمة تُقرأ عطلًا لا إنذارًا**. **والقائمة موجبة: تنبيهٌ جديد لا يُستثنى بالصمت** — **يُسقط البناء حتى يُكتب له سببٌ وشرطُ رفع**.
**والخمسة كلها مقيسةٌ لا موصوفة:** `vite` (`GHSA-fx2h-pf6j-xcff`) **تبلغ الخادم فعلًا بـ١٢ مسارًا وتُسمّى بذلك صراحةً في علّتها** — **وعذرها أنها أداة اختبار لا تُشحن** (`--packages=external`، ولا يعمل خادم vite عندنا)، **لا أنها بعيدة**. **والأربعة الباقية `postcss`×٢ و`image-size`×٢ — صفرُ مسارات في حزم الخادم و٣٨ و٩٠٠ في `apps/mobile`.**

**٦. وثلاثة قرارات صغيرة داخل السكربت تُسمّى:**
- **الاستثناء الذي زال تنبيهه يُعرَض ولا يُسقط البناء** — **تمييز القرار 206 بين ما يجب أن يسقط وما يُقرأ فقط**: زوالُ خطرٍ خبرٌ سارّ، **وإسقاط البناء عليه يُدرِّب على تعديل القائمة انعكاسًا**.
- **وعجزُ القياس سقوطٌ لا نجاح:** مخرَجٌ فارغ أو غير قابل للتحليل (شبكة، صيغة) **يخرج بـ١** — **بوابةٌ تخضرّ حين تعجز عن القياس ليست بوابة**.
- **ورسالة الفشل تُميّز حزم الخادم عن الموبايل في العدّ** — **لأن السؤال الأول عند كل تنبيه جديد هو هذا بعينه**.

**٧. والإثبات — بقياس رمز الخروج مباشرة لا عبر أنبوب** (فخّ 215):
- **الحالة الراهنة: `EXIT=0`** — «١١ تنبيهًا، منها ٥ حرجة/عالية — ٥ مستثناة بعلّة و٠ خارج القائمة».
- **وبحذف استثناء `vite` وحده — الوحيد الذي يبلغ الخادم: `EXIT=1`**، والرسالة تُسمّي المعرّف والحزمة و**«مسارات: ١٢ — منها ١٢ في حزم الخادم»** مع مثال المسار. **ثم أُعيد المدخل فعادت خضراء.**
- **والمتوسطة لا تُسقط شيئًا وهي ست: مقيسٌ في العدّ نفسه** (١١ إجمالًا، ٥ حاجبة).

**والقاعدة المستخلَصة:**

> **رقمٌ يُنسَب إلى أمرٍ لا يُنتجه يمرّ لأنه يوافق ما نتوقّعه** — **فلا يُكتب رقمٌ في قرار إلا مخرَجَ أمرٍ نفّذه كاتبه.**
> **وبوابةٌ بلا قائمة استثناءات إمّا كاذبة الخضرة أو دائمة الحمرة** — **والقائمة الموجبة هي ما يجعل «لا نُطيق هذا» قرارًا مكتوبًا بشرط رفع، لا سكوتًا.**

**ولم يُرقَّ شيء، ولم تُدمَج ولا أُغلقت PR من Dependabot، ولم يُلمَس `ci.yml`.**

**220. آلة حالة العنبر — وقوفٌ على نقصٍ، ثم بناءٌ على حكم**

> **قرارٌ على شطرين، والفاصل بينهما حكمُ مالك.** **الشطر الأول وقوفٌ عند نقص
> في الوثيقة** (كما كُتب يوم وقف، ونصّه محفوظ أدناه بلا تعديل)، **والشطر
> الثاني — «٨ · البناء» — كُتب بعد أن حسم المالك الثلاثة**. **ولم يُنشأ قرار
> ثانٍ**: الحكم أتمّ هذا القرار ولم ينقضه.

**الشطر الأول — الوقوف (كما كُتب).**

**دفعة استخراج ووقوف، لا دفعة بناء.** **ولم يُكتب سطر كود واحد** — والعلّة أن الأربعة المطلوب استخراجها **ثلاثةٌ منها في الوثيقة كاملة، والأول ناقصٌ نقصًا يحبس العنبر**. **ونقصُ الوثيقة قرار مالك لا اجتهاد.**

**١. خريطة الانتقالات — جدول واحد بمصدر كل سطر.**

**والمصدر الأول §3.3 من الوثيقة الشاملة** (`app-complete-spec.md:97`):

```
مشغول → تحت الإخلاء → تحت التنظيف والتطهير → في فترة الراحة → جاهز للإسكان → (مشغول)
              بالإضافة إلى: تحت الصيانة · معطّل  (يُدخل إليهما من أي حالة)
```

| من | إلى | مصدره | صاحبه | موضعه |
|---|---|---|---|---|
| مشغول | تحت الإخلاء | §14.6: «تصفية (owner) → [معاملة] → إغلاق + `status='تحت الإخلاء'` + إنشاء دورة تجهيز» | **المالك وحده** (§12.2 «تصفية دفعة») | **`POST /batches/:id/close` — المرحلة 4، خارج النطاق** |
| تحت الإخلاء | تحت التنظيف والتطهير | §3.3، سهمٌ **لا يدّعيه مسار آخر** | مشرف · مالك | **`PATCH /houses/:id/status`** |
| تحت التنظيف والتطهير | في فترة الراحة | §14.6: «عند اكتمال الإلزامية: **انتقال تلقائي** لـ'في فترة الراحة'» | تلقائي بلا فاعل | **`PATCH /prep-steps/:id/complete` — خارج النطاق** |
| في فترة الراحة | جاهز للإسكان | §14.6: «→ `houses`+`history`+`min_rest_days` → إغلاق الدورة»، و§3.3 بشرطيه | مشرف · مالك | **`PATCH /houses/:id/status`** |
| جاهز للإسكان | مشغول | §3.3 «(مشغول)»، و§14.6 «بدء دفعة في عنبر غير جاهز: مسموح بسبب + `housed_before_ready=true`» | عبر إسكان الدفعة | **مسار الدفعات — خارج النطاق** |
| **أي حالة** | تحت الصيانة | §3.3: «يُدخل إليهما من أي حالة» | مشرف · مالك | **`PATCH /houses/:id/status`** |
| **أي حالة** | معطّل | §3.3، نفس الجملة | مشرف · مالك | **`PATCH /houses/:id/status`** |
| تحت الصيانة | **؟** | **🔴 لا مصدر في أي وثيقة** | ؟ | ؟ |
| معطّل | **؟** | **🔴 لا مصدر في أي وثيقة** | ؟ | ؟ |

**وأول ما يقوله الجدول ليس النقص بل القسمة:** **خمسة من الانتقالات السبعة لا يملكها هذا المسار** — **ثلاثةٌ منها أثرٌ جانبيّ لعمليات أخرى** (تصفية الدفعة · اكتمال خطوات التجهيز · إسكان الدفعة)، **وكلها خارج النطاق المرسوم**. **فـ«آلة الحالات السبع» موزَّعة على أربعة مسارات لا مسار واحد** — **والذي يخصّ `PATCH /status` أربعة انتقالات لا سبعة**.

**وهذه القسمة ليست تفصيلًا تنظيميًّا — هي حارسُ صلاحية:** **«مشغول → تحت الإخلاء» أثرُ تصفية الدفعة، والتصفية للمالك وحده** (§12.2: مشرف ❌). **فلو قَبِلها `PATCH /status` لصفّى المشرف دفعةً من الباب الخلفي** — **ومَلَكَ ما لا يملكه**. **والقاعدة العامة المستخلَصة:** **انتقالٌ هو أثرُ عمليةٍ أخرى لا يُتاح مستقلًّا في مسار الحالة، وإلا صار مسارُ الحالة تجاوزًا لكل صلاحية معلّقة على تلك العملية.**

**٢. 🔴 والنقص — وهو ما يُوقف الدفعة.**

**§3.3 تكتب باب الدخول إلى «تحت الصيانة» و«معطّل» ولا تكتب لهما بابَ خروج.** **ولا سطر في أي وثيقة يقول إلى أين يعود عنبرٌ انتهت صيانته** — لا في الوثيقة الشاملة، ولا في §14.6، ولا في §12.2، ولا في قرار سابق. **مقيسٌ لا مقدَّر: مسحُ `docs/` كلها عن الخروج من الحالتين يُرجع صفرًا.**

**والنقص يحبس العنبر فعلًا، ولا يُترك للتنفيذ يسدّه:**

- **الدخول «من أي حالة» يشمل «مشغول»** — بنصّ §3.3. **فعنبرٌ فيه دفعة حيّة يدخل الصيانة، ولا مخرج مكتوب** — **فتبقى الدفعة نشطةً في عنبر خارج الخدمة إلى الأبد**، **والطيور في عنبر لا تعرف الآلة كيف تُعيده**.
- **وثلاثة أجوبة ممكنة، وكلها اجتهاد لا استخراج:** يعود إلى **حالته السابقة** (فيلزم عمودٌ يحفظها أو قراءةُ `house_status_history`) · أو إلى **«جاهز للإسكان»** دائمًا (**وهو ادّعاء جاهزية لم يؤكّدها أحد — عين ما رفضه القرار 186**) · أو إلى **حالة يختارها صاحب الصلاحية** كما يُختار عند الإنشاء (**وهو أقربها إلى 186، ولا يزال اختيارًا لا نصًّا**).
- **والفرق بين الثلاثة أثرٌ دائم في القاعدة لا تفصيلَ عرض:** الأول يفرض بنيةً جديدة، والثاني يكذب، والثالث يفتح بابًا إلى «مشغول» بلا دفعة.

**ولذلك وقفتُ ولم أبنِ:** **بناءُ بابٍ بلا مخرج شحنُ مصيدةٍ معروفة**، **واختيارُ مخرجٍ من عندي قرارُ مالك يُتّخذ بالسكوت** — وهو ما يمنعه 186 و184 و#161 بلفظها.

**وسؤالان أصغر يُرفعان مع الأول لأنهما من بابه:**
- **هل `reason` إلزامي في انتقالٍ ما؟** **الوثيقة لا توجبه في أيٍّ من الأربعة**، و`house_status_history.reason` **يقبل العدم** في المخطط. **ولا سند لإيجابه** — **وأنبّه أنّي كدت أستند إلى «معطّل … سبب التعطيل مكتوب» (§8.2) وهي عن الزرّ المعطّل لا العنبر المعطّل**، فتُسجَّل هنا كي لا تُقرأ سندًا بعد اليوم. **والمقترح المعلَّق: إيجابه على «تحت الصيانة» و«معطّل» وحدهما** — **لأن القرار 204 قرأ `house_status_history.reason` أنه الأثر الوحيد لسبب خروج العنبر من الخدمة** («ولا سجل لما أُصلح ولا بمَ ولا من أصلحه»)، **فسبب فارغ يُفرغ الأثر الوحيد**. **قرار مالك.**
- **ومسؤول الصيانة عملٌ بلا صاحب** (#157 البند ٦/٦، و§7-ب البند 28): **المشرف يغيّر الحالة ولا أحد يملك الصيانة نفسها**. **ولا يمنع هذا بناء الانتقال** — يُسجَّل أنه يبقى مفتوحًا تحته.

**٣. من يملك الانتقال — مستخرَجٌ كاملًا، ولا نقص فيه.**

**§12.2 صفٌّ واحد صريح:** «تغيير حالة عنبر | مربٍّ ❌ | **مشرف ✅** | طبيب ❌ | **مالك ✅** | أمين مخزن ❌ | مدير منصة ❌». **فالحارس قائمة موجبة من اثنين** — **ولا يُمنح دور بالسكوت** (القرار 184). **والطبيب ❌ هنا وإن ملك «تعديل مدة الراحة» تقصيرًا** — **عمليتان مختلفتان: تغيير المدة غير تأكيد انقضائها**، والثانية وحدها انتقال حالة.

**والقسمة أعلاه هي ما يجعل الصفّ الواحد كافيًا:** الانتقالات المحجوزة للمالك **محجوزةٌ بمسارها لا بعمود في هذا الصفّ**.

**٤. `min_rest_days` — أيّهما يحكم؟ ولا واحد منهما وقت الانتقال.**

**ثلاثة مستويات لا مصدران** (القرار #153، و`packages/shared/src/restPolicy.ts`):

1. **`tenants.min_rest_days`** — `NOT NULL DEFAULT 10`، `CHECK ≥ 3`. **سياسة المستأجر، وهي الأرضية والافتراضي.**
2. **`farms.rest_days`** — **يقبل العدم**، `CHECK (NULL OR ≥ 3)`. **والعدم يعني «اتبع سياسة المستأجر» لا صفرًا ولا «بلا راحة»** (نصّ `farms.ts`). **يرفعها المشرف صعودًا فقط.**
3. **`house_prep_cycles.rest_target_days`** — `NOT NULL` **بلا افتراضي عمدًا**، `CHECK ≥ 3`. **يحسبه منشئ الدورة من المستويين ويثبّته عليها.**

> **والحاكم وقت الانتقال هو الثالث وحده: `rest_target_days` المثبَّت على الدورة.** **ولا تُقرأ السياسة عند التأكيد** — **وإلا غيّر تعديلُ السياسة في منتصف الراحة مدةَ راحةٍ جارية بأثر رجعي**، وهو ما يُبطل معنى تثبيتها على الدورة أصلًا.

**والشرط مفروضٌ في القاعدة لا في الكود:** `house_prep_cycles_rest_confirmed_after_target_ck` **يمنع التأكيد قبل انقضاء المدة**، و`..._rest_confirmation_pair_ck` **يمنع تأكيدًا بلا صاحب**. **فالشرطان اللذان تشترطهما §3.3 — مضيّ المدة وتأكيد بشري — بنيةٌ قائمة لا تفسير يُكتب.** **وحدٌّ أدنى مطلق ٣ أيام مكرَّرٌ عمدًا في الثلاثة وفي `ABSOLUTE_MIN_REST_DAYS`.**

**٥. ما يُكتب عند كل انتقال — مستخرَجٌ كاملًا.**

**في معاملة واحدة:** `houses.status` · `houses.status_changed_at` · **وصفٌّ في `house_status_history`** (`from_status` · `to_status` · `changed_by` · `changed_at` · `reason`). **والجدول قائم في المخطط ولا يكتب فيه أحد اليوم** — **مقيس: صفر مرجع له في `apps/api/src` كلها.** **ويُضاف على انتقال الراحة وحده:** `rest_confirmed_at` و`rest_confirmed_by` على الدورة، **ثم إغلاقها** (§14.6 «إغلاق الدورة»).

**٦. وعلاقة البند 40 بصحّة الآلة — سُئلتُ فأقيس: لا تعتمد عليه، بشرطٍ واحد يُسمّى.**

**الآلة تُعرَّف بالحالة الحاليّة لا بتاريخ الوصول إليها**، **فأيّ حالة ابتدائية عقدةٌ صالحة في الجدول** — وتنفيذ 186 لا يغيّر سطرًا فيه. **والشرط:** **عنبرٌ يُنشأ مباشرةً «في فترة الراحة» لا دورة تجهيز له** (الدورة تُنشأ عند التصفية، §14.6) — **فانتقال «راحة → جاهز» يجد نفسه بلا دورة يقرأ منها `rest_target_days` ولا يكتب فيها التأكيد**. **وهي حالةٌ قائمة اليوم أيضًا** (كل عنبر يُنشأ بلا دورة)، **فالبند 40 يوسّعها ولا يُحدثها**. **تُسجَّل ولا تُحسم في هذه الدفعة**: جوابها تابعٌ لجواب المخرج أعلاه.

**٧. وأين أقترح القطع.**

> **دفعتان: حكمُ مالكٍ ثم بناء.**
> **(أ) قرار المالك على الثلاثة أعلاه** — **مخرج «تحت الصيانة» و«معطّل»** (وهو الحابس) · **وإلزام `reason` من عدمه** · **والعنبر المنشأ في حالة بلا دورة**. **ولا كود فيها.**
> **(ب) البناء كاملًا بعده** — الجدول أعلاه بمخرجه، والحارس بالقائمة الموجبة، والقفل وإعادة القراءة تحته، و422 بسببه، والإثبات بمخالفة لكل انتقال ممنوع وباختبار تزامن.

**والعلّة أن القطع هنا لا في منتصف البناء:** **البناء على خريطةٍ ناقصةٍ بابًا يُعاد كلُّه حين يُحسم الباب** — **فالمخرج يمسّ الجدول والحارس والاختبارات معًا**، وربما عمودًا في المخطط إن كان الجواب «يعود إلى حالته السابقة». **وإعادةُ بناءٍ كاملة أغلى من انتظار حكم.**

**والقاعدة المستخلَصة:**

> **بابٌ بلا مخرج ليس آلة حالات — هو مصيدة.** **وحين تكتب الوثيقة الدخول وتسكت عن الخروج فالسكوت نقصٌ يُرفع، لا فراغٌ يملؤه المنفّذ.**
> **وانتقالٌ هو أثرُ عمليةٍ أخرى لا يُفتح مستقلًّا في مسار الحالة** — **وإلا صار مسار الحالة بابًا خلفيًّا لكل صلاحية معلّقة على تلك العملية.**

---

**٨ · البناء — بعد أن حُسمت الثلاثة بحكم المالك.**

**والحكم جاء ردًّا على الوقوف أعلاه**، ونصّه يُنقل كما هو لا يُعاد صوغه:

> **(١) المخرج من «تحت الصيانة» و«معطّل»: اختيارٌ صريح كما عند الإنشاء** — منطق القرار 186 نفسه: **لا تُدَّعى جاهزية لم يؤكّدها أحد**. **وقيدٌ واحد يضيّقه: عنبرٌ فيه دفعة نشطة لا يعود إلا إلى «مشغول»، فالدفعة هي التي تقرّر لا الشخص.** **والدخول من «مشغول» يبقى مسموحًا كما تقول §3.3** — والعلّة واقعية: **سقفٌ ينهار أو مروحة تتلف والطيور بالداخل**، فالصيانة تقع على عنبر مشغول فعلًا. **ولا عمود جديد يحفظ «الحالة السابقة»** — القيد أعلاه يُغني عنه، **وقد قِيس بديله ورُفض لأنه بنية دائمة لحالة يحسمها سؤال عن الدفعة**.
>
> **(٢) السبب: إلزاميّ عند الخروج من الخدمة** (إلى «تحت الصيانة» أو «معطّل») — **فهو الأثر الوحيد الباقي يشرح لماذا توقّف العنبر** (وهو ما قرأه القرار 204 في هذا الحقل). **واختياريّ عند العودة.**
>
> **(٣) عنبر يُنشأ في «فترة الراحة»: ممنوع** — **الراحة ذيلُ دورة تجهيز لا نقطةَ بداية**. **والمسموح عند الإنشاء: جاهز للإسكان · تحت الصيانة · معطّل.** **وهذا حكمٌ على الإنشاء لا على الانتقال، فتنفيذه في دفعة البند 40.**

**والثالث سُجِّل ولم يُنفَّذ هنا بنصّ الحكم** — **§7-ب البند 40 يحمله كودًا**، ولم تُوسَّع هذه الدفعة به. **وأثره على الآلة كُتب في الحارس لا في الإنشاء:** الآلة تفترض الحالة قائمة، **وعنبرٌ في الراحة بلا دورة تجهيز حالةٌ قائمة اليوم** — **فيُرفض بـ`no_open_prep_cycle` ولا يُمرَّر**: **لا مدة انقضت لأنه لا مدة قرّرها أحد**، وتمريره ادّعاء جاهزية — عين ما رفضه 186.

**والجدول في موضع واحد لا شروطًا منثورة:** `packages/shared/src/houseStatusMachine.ts` — **أربع قواعد بمصدر كلٍّ منها ووسم «أسببٌ إلزامي؟»**، و`classifyHouseTransition` تقرأ الجدول ولا تعرف حالةً بعينها. **وأربعة انتقالات لا سبعة:**

| الصنف | من ← إلى | حارسه |
|---|---|---|
| `prep-advance` | تحت الإخلاء ← تحت التنظيف والتطهير | — |
| `rest-confirm` | في فترة الراحة ← جاهز للإسكان | الدورة مفتوحة · الراحة بدأت · المدة انقضت |
| `out-of-service` | **أي حالة** ← تحت الصيانة · معطّل | **سببٌ إلزامي** |
| `return-to-service` | تحت الصيانة · معطّل ← **أي حالة خدمة** | **بدفعة نشطة: «مشغول» وحدها** |

**والثلاثة الباقية من §3.3 يملكها غيرُ هذا المسار، وتُسمَّى في `TRANSITIONS_OWNED_ELSEWHERE` لا تُترك فراغًا** — **فرسالة الرفض تقول «لمن هو» لا «ممنوع» وحدها**، ومن يقرأها يعرف أين يذهب.

**والقسمة حارسُ صلاحية لا تنظيم — وتُكتب بهذه الصفة:** «مشغول ← تحت الإخلاء» **أثرُ تصفية الدفعة، والتصفية للمالك وحده** (§12.2: مشرف ❌). **فلو قَبِلها مسارُ الحالة لصفّى المشرف دفعةً من الباب الخلفي وملَكَ ما لا يملكه.** **والقاعدة عامّة: انتقالٌ هو أثرُ عمليةٍ أخرى لا يُفتح مستقلًّا في مسار الحالة، وإلا صار بابًا خلفيًّا لكل صلاحية معلّقة على تلك العملية.**

**والتنفيذ يلتزم ولا يشرح:** معاملة واحدة · **قفل الصفّ أولًا ثم قراءة الحالة والحرّاس تحته** · `houses` و`status_changed_at` و`house_status_history` معًا — **فإمّا الثلاثة وإمّا لا شيء** · **و422 يسمّي الحالة الحالية والانتقال المرفوض** بستة رموز متمايزة (`invalid_house_transition` · `reason_required` · `house_has_active_batch` · `no_open_prep_cycle` · `rest_not_started` · `rest_not_elapsed`). **والصلاحية `requireRole("supervisor", "owner")` قائمةً موجبة** (184). **ولا فحص إسناد في الموجّه:** `/api/houses/:houseId` **نمطٌ بادئة في `ENTITY_ID_PATH_PATTERNS` يغطّي المسار أصلًا** — **مقيس لا مفترَض: المشرف غير المُسند يُرفض بـ403 قبل أن تصل الآلة**، وهو ما أسقط ستة اختبارات حتى أُسند.

**و`rest_target_days` من الدورة لا من السياسة** — **وله اختبارٌ مخصَّص:** سياسة المستأجر ٣ ودورة ١٠ ← **يُرفض**. **والعلّة أن السياسة تتغيّر، فقراءتها وقت التأكيد تجعل تعديلًا في منتصف الراحة يغيّر مدة راحةٍ جارية بأثر رجعي.**

**٩ · والإثبات — بالمخالفات بأسمائها، وبإسقاط كل حارس على حدة.**

**٦٥ اختبارًا** (٣١ للجدول وحده، و٣٤ تكاملًا). **والمخالفات مسمّاة لا معدودة:** الثلاثة التي يملكها غيرُ هذا المسار **باسم صاحب كلٍّ منها** · **وعشر قفزاتٍ ورجعاتٍ داخل الدورة** · **والسبع إلى نفسها** · **ومسحٌ للأزواج التسعة والأربعين كلها** يقابل الجدول بقائمةٍ مكتوبة بأسمائها لا مشتقّة منه. **ومخالفتا الحكمين الجديدين:** عنبرٌ بدفعة نشطة يُطلب خروجه من الصيانة إلى «جاهز للإسكان» ← **422 `house_has_active_batch` والحالة لم تتغيّر**، ثم إلى «مشغول» ← **200** · وخروجٌ من الخدمة بلا سبب ← **422 `reason_required`**.

**وكلُّ حارس أُسقط وحده فسقط اختباره — مقيسًا لا موصوفًا:**

| ما أُسقط | ما سقط |
|---|---|
| `.for("update")` | **1** — «خروجان متزامنان … يتسلسلان ولا يتشابكان» |
| إلزام السبب | **2** — «خروجٌ إلى «تحت الصيانة» بلا سبب» و«… إلى «معطّل»» |
| حارس الدفعة النشطة | **1** — «بدفعة نشطة لا يعود إلا إلى «مشغول»» |
| حارس الراحة | **4** — انتقال الراحة، والمدة غير المنقضية، والمدة من الدورة لا السياسة، والدورة الغائبة |

**ولا انتقال بلا صفّ:** كل اختبار رفضٍ يؤكّد **`historyCount == 0`** ومعه **أن الحالة لم تتغيّر**، وكل نجاحٍ يقرأ صفّه بـ`from` و`to` وسببه.

**وحدٌّ معلن على أحد براهين التزامن — يُكتب لأنه كاد يمرّ ادّعاءً:** برهانُ القفل الحتميّ (إمساك الصفّ بيدٍ خارجية ثم قياس أن الطلب لا يُجيب) **يثبت التسلسل على الصفّ ولا يثبت أن القراءة تحت القفل** — **مقيس: بإسقاط `.for("update")` يبقى أخضر** لأن جملة `UPDATE` تحجب وحدها. **والذي يثبت القراءة تحت القفل هو اختبار التشابك، وهو الذي سقط فعلًا.** **فكُتب الحدّ في الاختبار نفسه بدل أن يُنسب إليه ما لا يفعله.**

**والقاعدة المستخلَصة من الشطر الثاني:**

> **برهانُ تزامنٍ يخضرّ بعد إسقاط القفل ليس برهانَ قفل** — **والفرق بين «يتسلسل» و«يقرأ تحت القفل» يُقاس بالإسقاط لا يُفترض من شكل الاختبار.**

**221. دورة التجهيز — الفتحُ دالّة مشتركة لا مسارًا، والانتقال التلقائي من الآلة لا خارجها**

**المرحلة 3، الدفعة الثانية:** `GET /houses/:houseId/prep-cycle` · `PATCH /prep-steps/:stepId/complete` · والانتقال التلقائي إلى «في فترة الراحة» (§14.6).

**١. العقبة المطروحة — تحقّقتُ فوجدتها كما وُصفت، وحُسمت بالشكل المرجَّح.**
**مقيس: صفر إنشاء لـ`house_prep_cycles` أو `house_prep_steps` خارج المخطط وتجهيزات الاختبار.** و§14.6 تجعل الإنشاء أثرَ تصفية الدفعة (المالك، المرحلة 4). **والبدائل الثلاثة قيست:** فتح `POST /prep-cycle` مستقلًّا **هو الباب الخلفي الذي منعه 220 بلفظه** — مرفوض؛ وتأجيل كل شيء للمرحلة 4 **يؤجّل بندَي بوابة الخروج معه**؛ **والدالة المشتركة تفكّ الارتباط**: `openPrepCycle(exec, {tenantId, houseId})` تحسب `rest_target_days` وتُنشئ الخطوات، **بلا مسار API، تُستدعى اليوم من التجهيزة وغدًا من معاملة التصفية بلا تعديل** — نمط `ensureSystemProducts` (213). **وأُثبت في الاختبار أن `POST /houses/:id/prep-cycle` يُرجع 404.**

**ومعها قيدٌ في القاعدة لا فحصٌ في الكود: دورة مفتوحة واحدة لكل عنبر** — فهرس جزئي `house_prep_cycles_open_per_house_uq` (ترحيل 0023، بنمط «افتتاحيٌّ واحد» في 198)، **فدورتان مفتوحتان تجعلان «أيّها تبدأ راحتها؟» بلا جواب**. **والتعبئة قبل القيد صريحة في نفس الترحيل** (نمط 205): تبقى الأحدث وتُغلق الأقدم — **وقد عضّت فعلًا: الفهرس رفض التطبيق على قاعدة الاختبار حتى كُتبت**.

**٢. المستخرَجات الأربعة:**

**(أ) الخطوات:** التسع من §3.3 **بأسمائها وترتيبها حرفيًّا** (`DEFAULT_PREP_PROTOCOL` في `packages/shared/src/prepProtocol.ts`) — والقرار #153: «تبقى كما في §3.3 ولا تُختزل». **والمستأجر يملك تعديل القائمة بنصّ #153** («لا تُثبَّت قائمة واحدة») — `tenants.prep_protocol` يعلو الافتراضي، **ويُقرأ يوم فتح الدورة فلا تتغيّر خطوات دورةٍ جارية بتغيير الإعداد**، **وإعدادٌ فاسد يُسقط بـ500 ولا يُتجاوز إلى الافتراضي صامتًا**. **وأيّها إلزامي؟ الوثيقة تقول «8-9» و«عند اكتمال الإلزامية» ولا تسمّي المتروك** — **فالافتراضي كلّها إلزامية** (قراءةً لافتراضي المخطط `is_required = true` وللأحوط: خطوة تُترك سهوًا تفتح عنبرًا لم يكتمل تعقيمه، **وعكسُه يُصحَّح بإعدادٍ لا بترحيل**). **وتسمية المتروك — إن أُريدت — للمالك عبر بروتوكوله.**

**(ب) `rest_target_days`:** يحسبه فاتح الدورة `max(tenants.min_rest_days, farms.rest_days ?? 0)` ويثبّته — **والمزرعة ترفع صعودًا فقط، و`max` يفرض الاتجاه هنا أيضًا: صفُّ مزرعةٍ أقصرُ من السياسة كيفما وُجد لا يُقصّر** (مُثبَت باختبار: مزرعة 4 وسياسة 10 ← 10). والتمديد للدورة الواحدة تعديلٌ لاحق له صفّه في §12.2 لا معامِلًا في الفتح.

**(ج) من يُكمل:** §12.2 «خطوة تجهيز: مربٍّ ✅ **المُسنَدة** · مشرف ✅ · طبيب ❌ · مالك ✅». **والكلمة «المُسنَدة» على الخطوة لا العنبر** — بخلاف «عنابره» في بقية صفوف المصفوفة، **والفرق اللفظي مقصود فيُقرأ**: المربّي يُكمل خطوةً `assigned_to` له وحدها، **وخطوةٌ بلا مُسنَد لا يُكملها مربٍّ** (لا منح بالسكوت — 184)؛ **وإسنادُ الخطوات مسارُ المشرف ولم يُبنَ بعد، فإكمالها اليوم للمشرف والمالك** — مسمًّى لا مسكوتًا عنه. **وهل الانتقال يشترط الاعتماد أم الإكمال؟ قِست فوجدت الإكمال:** §14.6 «عند **اكتمال** الإلزامية» وخطة العمل «عند اكتمال كل الخطوات الإلزامية» — **والاعتماد (`approved_*`) مسارٌ آخر خارج النطاق ولا يحجب الانتقال**.

**(د) الانتقال التلقائي:** **نُقل «تنظيف ← راحة» من `TRANSITIONS_OWNED_ELSEWHERE` إلى جدول الآلة بصنفه `prep-complete`** — ولا يُكتب انتقال خارج الآلة. **ومعه حقل `performedBy` على كل قاعدة**: أربعةٌ `status-route` وواحد `prep-completion`، **ومسار الحالة يرفض الثاني يدويًّا بـ422 `transition_not_manual` يسمّي صاحبه** — **فالنقل إلى الجدول لم يفتحه يدويًّا**، وإلا صار بابًا خلفيًّا يتخطّى «الخطوات تفتح العنبر» (#153). **والقسمة حارسُ صلاحية لا تنظيم — تُكتب هنا بهذه الصفة كما في 220.** ويكتب الانتقال في معاملة الإكمال نفسها: `houses.status` · `status_changed_at` · صفّ `house_status_history` (بمُكمل الخطوة الأخيرة) · **و`rest_started_at` على الدورة** — يُقاس منها الشرط الأول.

**٣. وثلاثة أحكام صغيرة داخل التنفيذ تُسمّى:**
- **اكتملت الإلزامية والعنبر ليس في «تحت التنظيف والتطهير»** (لم يُنقل من الإخلاء بعد، أو دخل الصيانة أثناء التجهيز): **تُرفض الخطوة الأخيرة بـ422 `house_not_in_cleaning` يسمّي الحالة والعلاج** — **لأن السكوت يعلّق العنبر**: تكتمل الخطوات ولا مُطلِق يبقى (عين حادثة #21)، والانتقال لا مصدر له من غير التنظيف. والمعاملة واحدة فالإكمال نفسه لا يقع.
- **إكمال اختيارية بعد بدء الراحة تسجيلُ عمل لا مُطلِقٌ ثانٍ** — `rest_started_at` المضبوط يمنع إطلاقًا ثانيًا (مُثبَت: صفّ سجل واحد).
- **حركة الاستهلاك (§14.6 «الخطوة ← حركة استهلاك») مؤجَّلة عمدًا إلى دفعة المخزون**: أول كتابة في الدفتر تدخل مع حرّاسه لا قبلهم — **فلا يقبل المسار منتجًا ولا كمية اليوم**، والعمودان (`product_id` · `quantity_used`) ينتظران دفعتهما.

**٤. والفرض المركزي — `stepId` معرّفٌ مشتق يُحلّ مركزيًّا لا بدالة جلب في كل خدمة.**
أُضيف النمط `/api/prep-steps/:stepId` إلى `ENTITY_ID_PATH_PATTERNS`، **وامتدّ `resolveHouseId` — الموضع الوحيد المخوَّل بالاشتقاق — ليحلّ الخطوة إلى دورتها إلى عنبرها**، فيفرض عليها ما يفرضه على العنبر (المبدأ الأول، ونمط `batchId` نفسه). **وفاحص أنماط الكيانات (218) يعدّها: 12 مسارًا كلها مغطّاة.**

**٥. الإثبات — والتزامن بندُ بوابة الخروج نصًّا (حادثة #21):**
- **28 اختبارًا في `prepCycle.integration.test.ts` و`prepCycleTransition.integration.test.ts`** (قُسم الملف على حدّ الأسطر، والتجهيزة مشتركة في `test-support/`) — والمخالفات بأسمائها: دورة مكتملة · خطوة مرتين · إلزامية باقية فلا انتقال ولا صفّ · الأخيرة والعنبر في الإخلاء · «تنظيف ← راحة» يدويًّا · طبيب 403 · مربٍّ غير مُسنَد 403 · خطوة مستأجر آخر 404 · دورة ثانية يرفضها الفهرس (**والقيد يُقرأ من سلسلة `cause` — درس 216**).
- **وبرهانا قفلٍ حتميّان لا سباقا توقيت** (بنمط 220): الإكمال **ينتظر قفل صفّ الدورة** فعلًا · **والعدّ بعد القفل لا قبله** — إكمالٌ ملتزمٌ أثناء الانتظار يُرى فيقع الانتقال، **ولو عُدّ قبل القفل لعلق العنبر مكتملَ الخطوات بلا مُطلِق**. **وفخّ يُسجَّل للاحق: طلب supertest كسولٌ لا ينطلق حتى يُنتظر** — بُني البرهان الثاني بلا `then` فورّي فاخضرّ كاذبًا مهما كان القفل، وصُحّح.
- **وخطوتان أخيرتان متزامنتان: انتقالٌ واحد وصفٌّ واحد في السجل** — بندُ البوابة بشكله.
- **وأُسقط كل حارس وحده — ستة إسقاطات، وما سقط بأسمائه:** قفل الدورة ← البرهانان الحتميّان · عدّ الإلزامية ← أربعة اختبارات الانتقال المبكر · حارس «مرة واحدة» ← اختبار الاختيارية بعد الراحة · حارس `performedBy` ← اختبار اليدويّ · حارس الإسناد ← اختبار المربّي · حارس الاكتمال ← اختباراه. **ثم أُعيد كلٌّ فاخضرّت الـ26.**

**وتغيّر عقدٌ من أمس يُسمّى:** «تنظيف ← راحة» يدويًّا كان يُرفض `invalid_house_transition` من قائمة «يملكه غيره»، **وصار `transition_not_manual` من الجدول نفسه** — واختبار أمس حُدِّث إليه، والقائمة نقصت إلى اثنين (أثرَي عمليات الدفعة).

**والقاعدة المستخلَصة:**

> **ما مُلك بالانتقال التلقائي لا يُفتح بالطلب اليدوي — والجدول يحمل مُجريه كما يحمل مصدره.**
> **وطلبُ اختبارٍ لا ينطلق إلا حين يُنتظر لا يختبر تزامنًا — يختبر تسلسلًا ويسمّيه تزامنًا.**

**222. الحالة الابتدائية تُختار ولا تُفترض — وإسقاطُ الافتراضي هو ما يجعلها إلزامًا**

**تنفيذ القرار 186 و§7-ب البند 40، وشقّه الثالث من القرار 220.** **قرارٌ صدر قبل دفعتين، وهذه دفعتُه.**

**١. الحال قبل الدفعة — مقيسٌ لا موصوف:** العمود `status` بـ`DEFAULT 'جاهز للإسكان'` في القاعدة، **و`createHouse` لا يمرّر حالة إطلاقًا** — فكل عنبر يولد «جاهزًا للإسكان» **بسكوت الكود لا بقرار أحد**. وهو عين ما رفضه 186: **ادّعاءُ جاهزيةٍ لم يؤكّدها أحد**.

**والإسقاط هو الحكم لا زينةً حوله:** **مع بقاء الافتراضي يبقى الإغفال ممكنًا صامتًا** — يُنسى الحقل في مسارٍ جديد فيعود العطب بلا صوت. **فأُسقط بترحيل مرقَّم** (`0024_house_status_no_default.sql`، بـ`generate` لا بيد)، **والعمود بلا افتراضي مقيسٌ في اختبار يقرأ `information_schema`** لا في ادّعاء.

**وأثرٌ جانبيّ نافع يُسجَّل:** الإسقاط جعل **`typecheck` نفسه يمسك مواضع الإدراج المكتوبة** (`.insert(houses)`) — أربعة مواضع سقطت فورًا. **وما لم يمسكه هو `SQL` الخام** (أحد عشر موضعًا في اختبارات المخطط)، **فمُسحت يدويًّا**: **قاعدةُ الأنواع تحرس ما تراه، ولا ترى نصًّا في قالب**.

**٢. الأربع الممنوعة ميلادًا — وعلّةُ كلٍّ مقيسةٌ على الجدول لا منقولة.**

مسحتُ مخارج كل حالة من `HOUSE_STATUS_TRANSITIONS` برنامجيًّا، **والنتيجة أن الأربع صنفان لا صنف واحد**:

| الحالة | علّة المنع | مقيسٌ على الجدول |
|---|---|---|
| **مشغول** | تفترض دفعةً نشطة | مخرجه الأمامي «تحت الإخلاء» **أثرُ تصفية الدفعة** — والتصفية تُغلق دفعةً غير موجودة، **فلا تقع أبدًا** |
| **تحت الإخلاء** | تفترض دفعةً أُغلقت للتوّ **ودورةً أُنشئت معها** (§14.6) | مخرجه الأمامي `prep-advance` يقع في «تحت التنظيف» **فيرث عطبها** |
| **تحت التنظيف والتطهير** | **لا دورة فلا خطوات فلا انتقال** | مخرجه الأمامي الوحيد `prep-complete` **تلقائيّ `performedBy: prep-completion`**، ومُطلِقه إكمالُ خطوةٍ من دورةٍ لا وجود لها |
| **في فترة الراحة** | **الراحة ذيلُ دورة لا نقطةَ بداية** (حكم المالك، 220) | مخرجه `rest-confirm` يحرسه `no_open_prep_cycle` — **لا مدةَ انقضت لأنه لا مدةَ قرّرها أحد** |

**والعلّة الرابعة صحيحةٌ كما قيلت لي، وأزيد عليها دقّةً بعد القياس:** **ليست الحالات الأربع محبوسةً حبسًا مطلقًا** — `out-of-service` تُبلَغ من كل حالة، ومنها `return-to-service` تبلغ كل شيء. **لكن ذلك المخرج ليس نجاةً بل إفسادًا للسجل**: **يقتضي إعلانَ صيانةٍ أو تعطيلٍ بسببٍ مكتوب إلزاميّ** — **فيُكذَب في الحقل الذي قرأه القرار 204 أنه الأثر الوحيد لسبب توقّف العنبر**. **فالحبس عن المسار الأمامي تامّ، والمخرج الوحيد يمرّ بكذبة.**

**والقاعدة الجامعة المستخرَجة:** **الأربع الممنوعة هي بالضبط ما يفترض تاريخًا لا يملكه مولود** — **دفعةً أو دورةَ تجهيز**. **والثلاث المسموحة لا تفترض شيئًا.**

**٣. وثلاثة قرّرتُها بحجّة:**

**(أ) السبب إلزاميّ عند الميلاد خارج الخدمة — نعم، وحكمُ 220 يسري بلفظه.** السؤال الذي يجيب عنه الحقل واحد: **«لماذا هذا العنبر متوقّف؟»** — **ولا يفرّق السائلُ بين عنبرٍ وُلد معطّلًا وعنبرٍ عُطّل**. **والمولود أحوجُ إليه لا أقلّ:** من انتقل **له حالةٌ سابقة ووقتُ انتقال يشهدان**، **والمولود لا شيء له**. **فالإلزام على الخروج من الخدمة، ميلادًا كان أو انتقالًا.**

**(ب) صفُّ ميلادٍ في `house_status_history` بـ`from_status = NULL` — نعم، والثابت يمتدّ.** **الميلاد ليس انتقالًا فلا يُلزمه ثابتُ 220 حرفيًّا** — **لكن غرضَ الثابت أن يُجيب السجلُّ سؤالَ «كيف صار العنبر إلى ما هو فيه؟»**، **وسجلٌّ فارغ لا يُجيبه بل يُبهمه**: لا يفرّق قارئُه بين «وُلد هنا ولم ينتقل» و«حُذف تاريخه» و«لا عنبر أصلًا». **وأثرُ ذلك على من يقرأ تاريخ عنبرٍ لم ينتقل قطّ هو الفرق بين سلسلةٍ تبدأ من أصلها وبين فراغ.**

**والحاسم أن (أ) يوجب سببًا ولا موضع له غير هذا الحقل** — **لا عمود له في `houses`، و`entity_audit_log` يحفظ لقطة الصفّ لا سببًا**. **فبلا صفّ الميلاد يضيع السببُ الذي أوجبناه للتوّ.** **وشاهدٌ من المخطط: `from_status` يقبل العدم ولا كاتب له اليوم** (`houseStatusService` يكتب الحالةَ المقفولة دائمًا) — **عمودٌ فارغ كان ينتظر هذا بعينه**. **فصار الثابت: لا حالةَ بلا صفٍّ يشرحها.**

**(ج) الرفض من التحقّق لا من القاعدة — وقُسم على طبقتيه كما في 220.** **`zod` يفحص الشكل** (الحقل موجود، وقيمته إحدى السبع) ← **400 برسالة عربية في `details`**؛ **والخدمة تفرض الحكم** (أيّ الثلاث تُولد، والسبب) ← **422 بعلّةٍ مكتوبة من `HOUSE_BIRTH_EXCLUSIONS`**. **وهو نفس فصل 220 بين شكل المسار وحكم آلته.** **ورسالة القاعدة الخام (`null value in column "status"`) لا تصلح لمستخدم ولا تصدر أصلًا: التحقّق يسبقها** — مُثبَت.

**٤. والمسح — لأن الافتراضي لن يوجد:** **ستةَ عشر موضعًا كانت تُدرج عنابر بلا حالة**، كلها أُصلحت: أحد عشر `SQL` خامًّا في اختبارات المخطط · وأربعة إدراجات مكتوبة · **وتجهيزةُ `houseVia` — موضعٌ واحد يخدم عشرات الملفات** (وأخذت معاملًا للحالة، **فمن احتاج غيرها يمرّرها ولا تُفترض هنا كما لم تُفترض هناك**) · **وبذرُ العرض** (`HouseFixture` صار يحمل `status`). **ومستهلكٌ حقيقيّ خارج الخادم: `apps/mobile/lib/infrastructureApi.ts` كان يرسل الاسم وحده** — **فصار يرسل «جاهز للإسكان» صراحةً، وحدٌّ معلن يُكتب: شاشةُ البنية التحتية لا تسأل عن الحالة بعد**، **فالعنبر المنشأ منها اليوم يُدَّعى له جاهزية** — **وهو شقّ الشاشة من البند 40، باقٍ**.

**٥. الإثبات — والمخالفات بأسمائها:**
- **عشرون اختبارًا** في `houseInitialStatus.integration.test.ts`. **والأربع الممنوعة كلٌّ باسمها ← 422 `invalid_initial_status`**، **ورسالتُها تحمل علّتها** (مُثبَت أن رسالتَي «مشغول» و«تحت الإخلاء» تذكران **الدفعة**، ورسالتَي «تنظيف» و«راحة» تذكران **الدورة**) — **ولا صفّ عنبرٍ يُكتب**.
- **العمود بلا `DEFAULT`** مقروءًا من `information_schema` · **وإنشاءٌ بلا حالة ← 400 من التحقّق برسالة عربية** ولا صفّ · **وقيمةٌ خارج السبع ← 400**.
- **وكلُّ حالةٍ مسموحة ← 201 والحالة هي المرسَلة**، **وصراحةً `not.toBe("جاهز للإسكان")`** للخارجتين من الخدمة — **فلا يمرّ الاختبارُ لو عاد الافتراضي**.
- **وصفُّ الميلاد:** صفٌّ واحد بـ`from_status = NULL` وصاحبِه · **والسببُ محفوظ فيه** · **وميلادٌ مرفوض ← لا صفّ** (المعاملة واحدة) · **وميلادٌ ثم انتقال ← صفّان متسلسلان يُقرأ منهما الأصل**.
- **وخمسة حرّاس أُسقط كلٌّ وحده، وما سقط بأسمائه:** القائمة الموجبة ← **سبعة** · إلزام السبب ← **اثنان** · صفّ الميلاد ← **ثلاثة** · إلزام الحقل في `zod` ← **واحد** · تمرير الحالة إلى الإدراج (إعادةُ الافتراضي ضمنًا) ← **اثنان**. **ثم أُعيد كلٌّ فاخضرّت العشرون.**
- **و٤٢٧ اختبار تكامل في ٣٢ ملفًا خضراء** بعد المسح.

**والقاعدة المستخلَصة:**

> **الافتراضي في القاعدة ليس راحةً بل ادّعاءً يوقّعه المخطط نيابةً عمّن لم يُسأل** — **وإسقاطُه، لا التحقّقُ فوقه، هو ما يجعل الاختيار إلزامًا**.
> **وحالةٌ تفترض تاريخًا لا يملكه المولود ليست بدايةً بل نهايةً وُضعت في أوّل السطر.**

**223. لقطة الرصيد — عند الجرد لا بدورةٍ رقمية، والقطعُ بترتيب الكتابة لا بتاريخ الحدث**

**تنفيذ §7-ب البند 45.**

**١. مخالفة التوقيت — قِستُها فوافقت، والحجّة تصحّ.**
البند يقول «داخل دفعة بناء مسارات المخزون لا تُرقَّع بعدها»، **وعلّتُه أن اللقطة تغيّر شكل قراءة الرصيد في كل مسار يُكتب**. **والقياس: `computeBalance` بلا مستهلك إنتاجيّ واحد** — مسحتُ المستودع فكل مستدعييها اختبارات (وفي `inventory_movements` نفسها **لا كاتب في الإنتاج**). **فما يخشاه البند — مراجعةُ كل مسار كُتب — لا محلّ له: لم يُكتب أيّ مسار بعد.** **والبناء الآن يقلب البند لصالحه**: المسارات تُكتب على الشكل النهائي من أول سطر، **وهو عين ما أراده البند لا خلافه**.

**٢. متى تُكتب اللقطة؟ عند اعتماد الجرد — والحدود الطبيعية أغنت عن الرقم.**

**ورقمُ الدورية مرفوض بسنده:** لا «كل ٣٠ يومًا» ولا «كل ألف حركة» — **لا قاعدة إنتاج ولا حجم مقيس** (§7-ب البند 42)، **والقاعدة #143 تمنع رقمًا لا يُبرَّر**. **ولم أجد لهما سندًا في وثيقة، فلم أخترعه.**

**والفرضية المطروحة صحّت بالقياس، وأزيد عليها اختزالًا:** **الجرد المعتمَد لحظةُ رصيدٍ شهد عليه إنسان** — `stocktakes` تحمل `closed_by`/`closed_at` و`approved_by`/`approved_at`، **والمصادِق غير المُدخِل بقيد** (#155). **فاللقطة عنده تُثبّت رقمًا مصادَقًا عليه لا رقمًا محسوبًا وحده** — **وهي أقوى من لقطةٍ يكتبها مؤقّت: هذه لها شاهد، وتلك لا شاهد لها.**

**وإغلاق الدفعة ليس حدًّا ثانيًا بل هو الأول نفسه:** §14.6 تجعل التصفية **«قفل → جردٌ إلزامي → …»** — **فالإغلاق يحتوي جردًا ولا يقع بدونه**. **فحدٌّ واحد لا اثنان**، **وإضافةُ الإغلاق حدًّا مستقلًّا كانت ستكتب لقطتين للحظة واحدة**.

**٣. القطع بمعرّف الحركة لا بوقتها — والخطر المطروح لا محلّ له في دفترنا.**

**السؤال:** حركةُ تصحيحٍ تُكتب اليوم وتخصّ أمس، بعد لقطةٍ بينهما — أتُفوَّت أم تُحسب مرتين؟

**والجواب مقيسٌ على المخطط: لا هذا ولا ذاك، لأن الدفتر بلا عمود تاريخ حدث أصلًا.** أعمدة `inventory_movements` كاملةً لا تحمل `occurred_at` ولا `movement_date` — **`created_at` وحده، أي وقت الكتابة**. **والتصحيح في نظامنا حركةٌ جديدة بالفرق** (§14: «تأكيد: سجل تصحيح + **حركة معاكسة بالفرق**»)، **لا تعديلٌ لحركةٍ قديمة** (المبدأ الرابع). **فحركةُ التصحيح تأخذ معرّفًا أكبر من حدّ اللقطة، فتُحسب مرةً واحدة بعدها** — مُثبَت باختبار يقارن معرّفها بحدّ القطع ثم يقارن الطريقتين.

**فالخطر المطروح يفترض محورَ تاريخِ حدثٍ لا يملكه الدفتر.** **والاتجاهات الثلاثة المعروضة تُحسم بهذا:** «إبطالُ اللقطات التي تلت» **لا داعي له** · «القراءة بترتيب الكتابة» **هي الواقع لا اختيارًا** · **و«منع التأريخ الرجعي» ليس قرار مالكٍ اليوم لأنه ممنوعٌ بالبنية لا بالحكم** — **ولو أُضيف `occurred_at` يومًا لصار قرار مالك حينها**، **ويبقى القطعُ على ترتيب الكتابة**، **و«الرصيد بتاريخ ماضٍ» سؤالٌ آخر بآلية أخرى** — **وهو سؤالٌ لا يجيبه النظام اليوم أصلًا، فاللقطة لا تُفقده شيئًا**.

**٤. والخطر الحقيقي ليس التأريخ الرجعي بل ترتيبُ الالتزام.**
`id` تسلسليّ، **ومعاملةٌ أخذت معرّفًا أدنى قد تلتزم بعد أخرى أعلى** — فلقطةٌ تُكتب بينهما تفقد الأدنى إلى الأبد. **والعلاج قفلُ صفّ المخزن**: `writeBalanceSnapshot` **لا تُستدعى إلا من معاملةٍ تحمله**، **وبه يصير `MAX(id)` لحظتَها قطعًا تامًّا**. **وهو قفلٌ لا تفرضه اللقطة بل يحتاجه الجرد أصلًا: عدٌّ يسابقه إدخالُ حركةٍ عدٌّ خاطئ قبل أن يكون لقطةً خاطئة.** **وحدٌّ معلن: لا مسار حركةٍ اليوم ليلتزم به، فهو شرطٌ مكتوب يرثه أول مسار يُبنى لا حارسٌ آليّ.**

**٥. الثابت الذي لا يُكسر — مُثبَت بالحذف.**
**اللقطة مشتقّة لا مصدر:** `computeBalance` تقرأ «آخر لقطة + ما بعدها»، **و`computeBalanceFromLedger` تتجاهلها وتجمع الدفتر كلَّه** — **ووجودُ الثانية هو ما يجعل الدعوى قابلة للقياس**. **واختبارٌ صريح يحذف كل اللقطات ويعيد الحساب فيطابق.**

**وخاصيةٌ ظهرت في الإثبات وتُسجَّل لأنها تفسّر حدَّه:** **أيُّ لقطةٍ صالحةٌ قطعًا — الأحدث اختيارُ كلفةٍ لا صحّة.** **مقيس:** أسقطتُ `DESC` إلى `ASC` **فلم يسقط اختبار واحد**، لأن «أقدم لقطة + ما بعدها» صحيحٌ أيضًا وإنما يمسح أكثر. **ولم أصطنع اختبارًا يُسقطه** — **بل كتبتُ الخاصية اختبارًا يفحص كل لقطةٍ على حدة**: رصيدها + ما بعد حدّها == المجموع الكامل. **وهي بعينها ما يجعل الحذف آمنًا: الجواب لا يعتمد على أيّ لقطةٍ اختيرت.**

**٦. وثابت §13.3 بعد اللقطة كما قبلها** — مُثبَت بحركاتٍ قبل اللقطة وبعدها على مخزنين: `Σ` الحركات == رصيد المركزي + رصيد مخزن العنبر.

**٦-ب. وحارسُ تجميدٍ في القاعدة — تنفيذًا لنصّ البند «تُكتب ولا تُعدَّل».**

**والفهرس الفريد لا يُغني عنه:** `..._cut_uq` يمنع **لقطةً ثانية بنفس الحدّ**، **ولا يمنع `UPDATE` يغيّر `balance` أو `through_movement_id` على صفٍّ قائم** — **وصفٌّ معدَّل يُفسد كل قراءة بعده بلا أن يصرخ شيء**، لأن القراءة تثق باللقطة ولا تعيد حسابها. **فأُضيف `balance_snapshot_freeze_guard`** بنمط `system_product_guard` (213): **في القاعدة لا في الخدمة** (درس 203)، **وحارسٌ لا `CHECK`** لأن الحكم على الانتقال والقيد يرى الصفّ الجديد وحده.

**وتمييزٌ مقصود مكتوبٌ في الحارس نفسه لا في القرار وحده — وهو عكس معظم حرّاسنا:**

> **التعديل يُمنع · والحذف يبقى مسموحًا.**

**وحرّاسنا تمنع الحذف عادةً** (`system_product_guard` و`external_issue_order_freeze_guard`) **لأن المحذوف هناك مصدرٌ يُبنى عليه**: صنفٌ نظاميّ تقوم عليه معادلة، وأمرُ صرفٍ هو سندُ حركة. **واللقطة مشتقّة لا مصدر** — **و«تُحذف كلها فيُعاد الحساب بلا فقد» هي الدعوى التي بُني عليها البند كله وأُثبتت باختبارها**. **فمنعُ الحذف ينقض الدعوى** — يجعل اللقطة صفًّا لا يُستغنى عنه، **أي مصدرًا** — **ومنعُ التعديل يحرسها**: يبقى الصفّ إمّا صادقًا كما كُتب وإمّا غير موجود، **ولا حالة ثالثة يكذب فيها**. **و`DELETE` غائبٌ عن أحداث المُشغِّل حكمًا لا سهوًا، مكتوبًا في الترحيل.**

**والقاعدة العامة:** **يُمنع من المشتقّ ما يجعله يكذب، لا ما يجعله يختفي** — **فاختفاؤه يُعيد الحساب، وكذبُه يُفسده صامتًا**.

**وأُثبت في الاتجاهين:** `UPDATE` على `balance` وعلى `through_movement_id` **يُرفضان برسالة الحارس** (مقروءةً من سلسلة `cause` — **درس 216 مرةً أخرى**: drizzle يغلّف رسالة السائق) **والرصيد لا يتغيّر** · **والحذف يمرّ ثم يعود الحساب إلى الدفتر مطابقًا** · **وبإسقاط المُشغِّل وحده يسقط الاختباران المسمَّيان ثم يعودان بإعادته**.

**٧. وما لم يُلمس عمدًا:** **جداول التدقيق الثلاثة بلا فهرس كما هي** — البند يقول صراحةً إن فهارسها تُضاف **يوم يُبنى عرض السجل** (المرحلة 6) لا قبله، **فالكتابة تصير أبطأ بلا قارئ**. **ولم يُبنَ مسار API ولا مسار مخزون** — النطاق البنية والقراءة والكتابة المشتركة.

**وفهرسٌ مكرّر أُسقط قبل أن يُولد:** ولّدتُ `lookup_idx` بنفس أعمدة `cut_uq` **فحُذف وأُعيد التوليد نظيفًا** — **سؤال القراءة بادئةُ الفهرس الفريد بعينها**، **وفهرسٌ مكرّر يُبطئ الكتابة بلا قارئ يزيد** (وهو تحديدًا ما يحذّر منه البند نفسه في التدقيق).

**والقاعدة المستخلَصة:**

> **حدُّ اللقطة يُؤخذ من حدود المجال لا من رقمٍ يُخترع** — **والجرد المعتمَد حدٌّ له شاهد، والمؤقّت حدٌّ بلا شاهد.**
> **وخطرُ «ما بعد اللقطة» يفترض محور تاريخٍ للحدث؛ فحيث لا محور إلا ترتيب الكتابة، القطعُ عليه دقيقٌ بالبناء** — **والخطر الباقي هو ترتيب الالتزام، وعلاجه قفلٌ تحتاجه العملية أصلًا.**

**224. مخزن العنبر يُنشأ معه، ولا يُلغى مخزن فيه رصيد — وشقٌّ ثالث يقف على حكم مالك**

**تنفيذ شقّين من السبعة الباقية في §7-ب البند 32، والثالث موقوف.**

**١. مخزن العنبر يُنشأ مع العنبر — والحكم منصوصٌ لا مستنبَط.**

**#161 «أولًا» بلفظه:** «**مخزن العنبر واحد لكل عنبر ويُنشأ معه تلقائيًا — لا يُنشئه المالك ولا يحذفه**».

**والحال قبل الدفعة حالةُ القرار 213 بحرفها:** المخطط يحرس «واحدٌ لكل عنبر» بفهرس جزئي (`warehouses_house_uq`)، **و`createHouse` لا يُنشئ مخزنًا إطلاقًا** — **قيدٌ يحرس شيئًا لا يُنشئه أحد**. **وبلا مخزن العنبر لا طرف ثانيَ للتحويل أصلًا** (#159).

**والشكل نمط `ensureSystemProducts`:** دالّة مشتركة `ensureHouseWarehouse` **تُستدعى داخل معاملة إنشاء العنبر لا بعدها** — **فلا يوجد عنبر بلا مخزنه لحظةً واحدة** — **ولا مسار API لها**. **وعدمُ التكرار بالفهرس الجزئي لا بفحصٍ قبليّ** (`ON CONFLICT`): **الفحص قبل الإدراج يترك سباقًا بين معاملتين** (نصّ 213).

**والاسم: مشتقٌّ من اسم عنبره — ولا تسمّيه وثيقة.** مسحتُ الوثائق فوجدت #161 يقرّر **وجوده وتلقائيّته ولا يسمّيه**. **فاشتُقّ `مخزن {اسم العنبر}`، والاختيار يُعلَّل:** ثابتٌ واحد لكل العنابر **يجعل قائمة اختيار طرفَي التحويل بلا معنى**؛ **واسمٌ يُدخله المنشئ يفتح ما أغلقه الحكم** («لا يُنشئه المالك»). **وهو اسمٌ ابتدائيّ لا مجمَّد** — تمييز 213 بعينه: **المجمَّد ما تقرؤه الآلة، والمتروك ما يقرؤه الإنسان**.

**والعنابر القائمة تُعبَّأ في الترحيل نفسه** (`0027`، نمط 205): **مقيسٌ لا مفترَض — قاعدتا التطوير والاختبار بلا مخزن عنبرٍ واحد** (٣٦ و٨٧٢٣ عنبرًا بلا مخزن)، **فبلا التعبئة يسري الحكم على الجديد وحده**. **وبعدها صفرٌ في القاعدتين.** **وسكربتٌ جانبيّ مرفوض: يُنسى على قاعدةٍ ويُشغَّل على أخرى.**

**والحذف لا يُسأل عنه** — **§7-ب البند 13: لا `DELETE` للعنبر**، فلا مصير للمخزن عند حذفٍ لا يقع.

**٢. لا يُلغى مخزن فيه رصيد — في القاعدة، ويقرأ الرصيد بتعريف 223 نفسه.**

**العلّة:** `is_active` يُقلب بلا حارس، **ومخزنٌ معطَّل وفيه رصيد يُخفي بضاعة** — الرصيد باقٍ في الدفتر ولا واجهة تعرضه، **فتختفي البضاعة من الأعين ولا تختفي من الحساب**.

**وموضعه القاعدة لا الخدمة، والعلّة تُسمّى:** **درس 203** — قاعدةٌ تُكتب في تعليق ولا تُفرض ليست قاعدة — **ونظيرُ 213**: **لا مسار يقلب `is_active` اليوم**، **فالحارس يسبق المسار كما سبقه حارس 213**، والمسار حين يُبنى يجده مفروضًا. **وحارسٌ لا `CHECK`** لأن الحكم على **الانتقال** (مفعَّل → معطَّل) **وعلى جدولٍ آخر** (الدفتر واللقطات)، **و`CHECK` لا يقرأ غير جدوله**.

**والاعتراض الوجيه على اختيار القاعدة — ويُواجَه لا يُتجاوز:** الحارس فيها **يعيد كتابة تعريف الرصيد بلغةٍ ثانية**، فيتباعد عن `computeBalance`. **ولم أُجب عنه بالعدول إلى الخدمة** بل بثلاثة:
- **الحارس يقرأ بتعريف 223 حرفيًّا** — «آخر لقطة + مجموع ما بعدها» بـ`LATERAL` — **لا `SUM` على كل الحركات**، وإن كان الاثنان متساويين اليوم بالبرهان: **المساواة خاصيةٌ مُثبَتة لا تعريف**، **واستعلامٌ يجمع الدفتر كله يتباعد أوّلَ يوم يتغيّر معنى اللقطة**.
- **والتكرار بين الطبقتين سابقةٌ قائمة معلَّلة**: `ABSOLUTE_MIN_REST_DAYS` مكرَّر عمدًا في TypeScript وفي قيود القاعدة (القرار 197) — **«الرقم واحد في موضعين لأن الطبقتين لا تتحدثان لغة واحدة»**.
- **واختبارٌ يقارن حكم الحارس بحكم `computeBalance` في أربع حالات** (لقطة في مواضع مختلفة، ورصيد صفر وغير صفر) **فالتباعد يسقط البوابة لا يمرّ صامتًا**.

**٣. 🔴 وصول المشرف إلى مخزن موقعه — وقفتُ عنده: الوثائق تصفه ولا تحسمه.**

**ما تقوله الوثائق، مقروءًا لا مستنبَطًا:** #161 «ثانيًا» يجعل صاحب مخزن الموقع «**المشرف المسؤول عن مزارع ذلك الموقع**» · و§12.2 يكرّرها «**مخزن موقعه**» في أربعة صفوف · و207 يجعله يجرده ويُدخل افتتاحيّه.

**وكلها تصف الصاحب في الحالة البسيطة ولا تشرّع للمنقسمة.** **و`user_assignments` بلا `site_id` إطلاقًا** (مقيس: `house_id` · `farm_id` · `warehouse_id` وحدها) — **والمشرف يُسنَد بالمزرعة** (#128). **فعبارة «مخزن موقعه» تفترض ربطًا لا يعبّر عنه النموذج حين تنقسم المسؤولية.**

**والانقسام هو الغالب لا النادر — مقيسٌ على بيانات المالك نفسها:** **خمسةٌ من سبعة مواقع فيها أكثر من مزرعة** (الجبل ٢ · الكرنة ٢ · الصعيد ٣ · الجاح ٢ · الخماسية ٢، وواحدةٌ لكلٍّ من الطويلة والحمراء).

**وأثر الاتجاهين بالقياس لا بالوصف:**

| | **(أ) يبلغه بإسناد أي مزرعة فيه** | **(ب) يُسنَد المخزن صراحةً** |
|---|---|---|
| **ما يفتحه لمشرفٍ مُسنَد لمزرعة واحدة** | **مخزنَ موقعٍ يخدم ٣ مزارع في «الصعيد»** — **اثنتان ليستا له**؛ وفي أربعة مواقع أخرى مزرعةً واحدة زائدة | **لا شيء زائد** — ما أُسنِد له فقط |
| **كلفته الإدارية** | **صفر صفوف** — يُشتق من القائم | **صفٌّ لكل (مشرف · مخزن موقع)**: ٧ مخازن مواقع في بيانات العرض، وصفٌّ يُصان عند كل تبديل مشرف |
| **خطره** | **توحيدٌ بـ`OR` يوسّع صامتًا** — تحذير #131 بلفظه: يُضاف مشرفٌ لمزرعة فيرث مخزن الموقع بلا قرار | **يُنسى الصفّ فيُحجب مشرفٌ عن مخزنه** — عطبٌ صائح لا صامت |

**ولا أختار بينهما:** **(أ) أرخص وأخطر، و(ب) أغلى وأصرح** — **والمقايضة قرار مالك لا اجتهاد منفّذ**، **وهي بعينها المقايضة التي سجّلها تعليق `assertWarehouseAccess` ولم يحسمها** (القرار 199). **فالشقّ يبقى مفتوحًا، والتعليق في مكانه لم يُمَسّ.**

**٤. الإثبات:**
- **ستة اختبارات لمخزن العنبر:** المخزن موجودٌ فورًا بعد ردّ الإنشاء بمستواه ومفعَّلًا · والاسم مشتقّ · وواحدٌ لا اثنان · **ومخالفة متعمَّدة: مخزنٌ ثانٍ يرفضه `warehouses_house_uq`** (مقروءًا من سلسلة `cause`) · **وفشلُ إنشاء العنبر لا يترك مخزنًا يتيمًا** — المعاملة واحدة.
- **اثنا عشر لحارس التعطيل:** رصيدٌ موجب وسالبٌ يُرفضان (**الحكم «غير صفر» لا «موجب»**) · وحركاتٌ مجموعها صفر تمرّ (**العبرة بالرصيد لا بوجود حركات**) · وصنفٌ واحد من صنفين يكفي · ورصيدُ مخزنٍ آخر لا يمنع · **والتفعيل يمرّ ولو كان فيه رصيد** (الحكم على التعطيل وحده) · وتعديلُ الاسم يمرّ · **ورصيدُ مستأجرٍ آخر لا يُحسب**.
- **وإسقاطُ كل حارس وحده:** إسقاط إنشاء المخزن من المعاملة ← **الستة** · وإسقاط المُشغِّل ← **خمسة بأسمائها**. ثم أُعيد كلٌّ فاخضرّت الثمانية عشر.
- **و٤٥٩ اختبار تكامل في ٣٥ ملفًا خضراء**، بعد إصلاح تجهيزةٍ كانت تُنشئ مخزن العنبر بيدها: **صارت تقرأ ما بناه المسار الحقيقي** — **وهو أثرٌ صحيح للحكم لا عطبٌ فيه**.
- **وحدٌّ مكتوب في الاختبار نفسه:** تأكيد «لا عنبر بلا مخزن» **على المستأجر لا على القاعدة كلها**، **لأن تجهيزات أخرى تُدرج عنابر بـ`SQL` خام فتتخطّى `createHouse`** — **حالةُ اختبارٍ لا إنتاج: لإنشاء العنبر موضعُ كتابةٍ واحد في المشروع كله**.

**والقاعدة المستخلَصة:**

> **قيدٌ يحرس شيئًا لا يُنشئه أحد ليس حراسةً بل انتظارًا** — **ومن يبني المُنشِئ يجد الحارس مكتوبًا من قبله.**
> **وحارسٌ في القاعدة يقرأ ما تقرؤه الخدمة يجب أن يقرأه بتعريفها لا بمكافئٍ له** — **فالمكافئ يصحّ اليوم ويتباعد غدًا، والاختبار الذي يقارن الحكمين هو ما يمنع التباعد.**

**225. مخزن الموقع يُسنَد صراحةً ولا يُشتق — قرارُ مالكٍ يُثبّت القائم ويُغلق بابًا لم يُفتح**

**قرار مالك، ويغلق الخانة الثالثة من ذيل §7-ب البند 32** — الشقّ الذي وقف عنده القرار 224.

> **وصولُ المشرف إلى مخزن الموقع يكون بإسنادٍ صريح لذلك المخزن، ولا يُشتق من إسناد مزارعه.**

**١. والعلّة مسؤولية لا أمن — وهذا ما يميّزها.**
**جردُ مخزن الموقع مسؤولية المشرف بمصادقة المالك** (القرار 207)، **وموقعٌ فيه ثلاث مزارع قد يكون له ثلاثة مشرفين** — **فالاشتقاق يجعل ثلاثةً مسؤولين عن مخزنٍ واحد، ومسؤوليةٌ يشترك فيها ثلاثة لا يحملها أحد**. **والإسناد الصريح يسمّي واحدًا يُسأل.**

**والفرق عن التعليل الأمنيّ ليس لفظيًّا:** الحجّة الأمنية كانت ستقبل الاشتقاق لو كان المشرفون كلهم «موثوقين»؛ **وحجّة المسؤولية ترفضه ولو كانوا كذلك** — لأن العطب في **غياب صاحبٍ يُسأل** لا في وصول من لا يُوثق به.

**وسندُه مقيس: خمسةٌ من سبعة مواقع في بيانات المالك فيها أكثر من مزرعة** (الجبل ٢ · الكرنة ٢ · الصعيد ٣ · الجاح ٢ · الخماسية ٢) — **فالانقسام هو الغالب لا الحالة النادرة**، والحكم يخاطب القاعدة لا الشاذّ.

**٢. ولا تغيير في الفرض إطلاقًا — مقيسٌ قبل أن يُكتب سطر.**
`user_assignments.warehouse_id` **قائم منذ القرار 198** بمفتاحه المركَّب وقيدِ «واحدٌ بالضبط من ثلاثة»، **و`assertWarehouseAccess` يطلب اليوم صفَّ إسنادٍ لهذا المخزن بعينه ساريًا** لكل مخزنٍ ليس مخزن عنبر — **ولا يقرأ `farm_id` في ذلك الفرع إطلاقًا**.

**فالحكم يُثبّت ما هو قائم ويُغلق بابًا لم يُفتح** — **ولم يُمَسّ منطق حارس بسطر**. **والذي تغيّر تعليقُه:** كان يعلن أن هذا «**لم يُبنَ ويبقى معلنًا**» ويطرح اتجاهين، **فصار يكتب الحكم بعلّته**. **والفرق بين الصيغتين هو الفرق بين حدٍّ وقرار:** **الحدُّ يدعو من يأتي بعدُ إلى سدّه، والقرار ينهاه عن نقضه** — **وتعليقٌ يقول «لم يُبنَ» عن أمرٍ حُسم رفضُه يُغري ببنائه**.

**٣. والاختبار هو جوهر الدفعة — لأن ما لا يتغيّر في الكود لا يحرسه إلا اختبار.**
موقعٌ بمزرعتين ومشرفَين، **وأربع حالات**:
- **مشرفٌ مُسنَدٌ لمزرعةٍ في الموقع بلا إسناد المخزن ← 403**.
- **وإسنادٌ منتهي المدة لا يفتحه ← 403** — «سارٍ اليوم» يسري هنا كغيره (القرار 190).
- **ثم يُسنَد المخزن صراحةً ← يمرّ**.
- **ومشرفُ المزرعة الأخرى في نفس الموقع يبقى 403** — **وهو البرهان**: لو اشتُقّ الوصول من إسناد المزارع لمرّ.

**وأُثبت حمْلُها بمخالفةٍ متعمَّدة في الحارس نفسه:** أُدخل الاشتقاق (`OR` على مزارع الموقع) **فسقطت ثلاثة من الأربعة بأسمائها**، ثم أُعيد الحارس فاخضرّت السبعة عشر. **وهذه هي الحراسة التي أضافتها الدفعة**: **الحكم كان مطبَّقًا بلا شاهد، فصار له شاهد يسقط إن نُقض.**

**٤. وتمييزٌ يُكتب كي لا يختلط سؤالان:**
**هذا الحكم يخصّ المشرف ومخزن الموقع وحدهما.** **وأمّا «كيف يُسنَد أمين المخازن» — مخزنٌ بعينه أم عدة مخازن أم الشركة كلها** (#161 «حادي عشر» السؤال ١) — **فسؤالٌ آخر لم يُحسم بهذا ولا يُقاس عليه**: **أمين المخازن صاحب المخزن المركزي** (#161 «ثانيًا») **والمركزيّ واحد**، فسؤالُه في العدد لا في الاشتقاق. **ويبقى مفتوحًا.**

**والقاعدة المستخلَصة:**

> **حكمٌ يوافق ما هو مبنيّ ليس بلا أثر** — **أثرُه أن يصير للمبنيّ شاهدٌ يسقط إن نُقض، وأن يُبدَّل في التعليق «لم يُبنَ» بـ«قُرِّر رفضه».**
> **والمسؤولية المشتركة بين ثلاثة ليست ثلاثة حرّاس بل صفرًا** — **فالإسناد الصريح يسمّي، والاشتقاق يوزّع حتى يضيع.**

**226. خانة الحالة الابتدائية — ولا خيار مُسبَق الاختيار**

**يغلق الشقّ الأخير من §7-ب البند 40** — شقّ الشاشة الذي سجّله القرار 222 حدًّا معلنًا. **وبه يكتمل البند.**

**١. العلّة كما سُجِّلت:** الخادم صار يرفض الإنشاء بلا حالة (222)، **والشاشة لا تسألها فترسل «جاهز للإسكان» صراحةً** — **فادّعاء الجاهزية زال من القاعدة وبقي في الواجهة**، وهو ما رفضه 186 نصًّا. **وحُذف الإرسال الصريح ومعه تعليقُ الحدّ**: **الحدُّ زال فلا يبقى نصّه**.

**والحكم:** المستخدم يختار، **ولا خيار مُسبَق الاختيار** — **فحقلٌ بقيمة افتراضية يُعيد الادّعاء بلبوس آخر**، **والفرق بين «اختار جاهزًا» و«لم يختر فوُضع جاهزًا» هو كل ما في 186**.

**٢. أين يعيش الحقل؟ في الورقة المشتركة عبر منفذها القائم — لا ورقةً مفردة.**
**مسحتُ مستدعيي `NameSheet` فوجدتها ثلاثة** (المواقع · المزارع · العنابر)، **والمنفذ موجودٌ ومستعمَل**: `FarmsLevel` تمرّر `children` (رقائق مصادر الطاقة) و`extraError` منذ بنائها، **وترفع حالتها إلى `useFarmForm`**. **فالتوسعة تسير في طريقٍ مطروق لا تشقّ ثانيًا** — و`useHouseForm` نظير `useFarmForm` حرفيًّا.

**وما أخسره يُسمّى:** **الورقة المشتركة تحمل الآن شرطًا لا تحمله أختاها** — **الحالة في الإنشاء وحده** (`form.isEditing ? null : …`)، **ومصادر الطاقة في الحالتين**. **فالمشترك يزداد شرطًا يخصّ مستوًى واحدًا**، وهو ثمنُ ألّا تُفرَد ورقةٌ رابعة تُصان بجوار ثلاث. **والبديل أسوأ:** ورقةٌ خاصة تنسخ الاسم والحفظ والخطأ والإغلاق، **فتتباعد عن أخواتها عند أول إصلاح** — وهي العلّة المكتوبة في `useEntitySheet` نفسها.

**٣. أي مكوّن؟ `Chip` — و`SegmentedControl` مردودٌ بقياسه لا بذوق.**
**قرأت المكوّنين:** `SegmentedControl` **فلترُ قوائم** (§8.13) **يلزمه `count` لكل خيار ويُخفي ما عدّاده صفر**، **و`selectedKey` عنده نصٌّ لازم لا يقبل العدم** — **فاستعمالُه هنا يقتضي تزوير عدّادات وتمثيلَ «لم يُختر» بقيمة**. **و`Chip` يحمل `selected` لكل خيار على حدة، فحالةُ «لم يُختر بعد» تُمثَّل بلا تحايل** — **وهي الحالة التي يقوم عليها الحكم كله**. **ولم يُنشأ مكوّن جديد.**

**والتسميات الثلاث عربية طويلة**، فصفُّها `flexWrap: "wrap"` — **يلتفّ ولا يُقصّ** على 361dp.

**٤. والسبب — يظهر ويُلزَم بحسب المختار.**
`statusNeedsReason` تقرأ الحالتين الخارجتين من الخدمة، **فالحقل لا يظهر لغيرهما ولا يُرسَل فارغًا** (القرار 222). **وحجبُ الحفظ بسببٍ مكتوب قبل الضغط** — §8.2 («السبب يظهر **قبل** الضغط لا بعده») و§11 («لا زر يفشل عند الضغط»): **لذلك أُضيف `blockSubmit` إلى الورقة المشتركة**، **لأن `extraError` كان يعرض ولا يمنع** — **فورقة المزرعة تُرسل بلا مصدر طاقة وتتّكل على رفض الخادم**. **ولم أغيّر سلوك المزرعة**: المنفذ أُضيف واستُعمل للعنبر وحده، **وتوصيلُه هناك دفعةٌ أخرى**.

**٥. ورسالة 422 كانت لا تصل — وهذا عطبٌ وجدته لا بنيتُه.**
`infrastructureErrorMessage` **بلا فرع 422 إطلاقًا**، فيسقط إلى «تعذّر تحميل البيانات» — **وهي تقول «تحميل» عن حفظٍ رُفض لسببٍ معلوم**. **فأُضيف الفرع، والنصّ يُكتب بالرمز لا يُنقل من الخادم خامًا**: `toApiFailure` **يحمل الحالة والرمز وحدهما ولا يحمل رسالة الخادم أصلًا** (مقيس)، **والرمز عقدٌ ثابت وصياغةُ ما يقرؤه المستخدم تخصّ الشاشة**. **ولم تُمَسّ رسالة 500 ولا الافتراضية** — **العطب نفسه فيهما، وإصلاحه توسيعٌ لا تقتضيه الدفعة**.

**٦. والتعديل لا يمسّ الحالة** (القرار 220): **الحقل في الإنشاء وحده**، **وورقة التعديل تبقى اسمًا** — **مُثبَتًا بجسم `renameHouse`**.

**٧. الإثبات — ٢٥ اختبارًا:**
- **جوهر 186:** بلا اختيار **لا خيار واحد محدَّد** (`[false, false, false]`) · وواحدٌ فقط عند الاختيار · **والخيارات مقروءة من `HOUSE_CREATABLE_STATUSES` لا مكتوبة نصًّا**.
- **الثلاث تُرسَل كما اختيرت** — واختبارٌ يؤكّد أن ما وصل الخادم **ثلاث قيم مختلفة لا قيمة واحدة**.
- **والسبب** يظهر عند الحالتين وحدهما · **ولا يُرسَل فارغًا ولا فراغاتٍ** · ويُرسَل مُشذَّبًا.
- **ورسالة 422 مفهومة** لكل رمز، **والمجهول يُقال رفضًا لا عطبَ تحميل**.
- **وقواعد §10:** الالتفاف لا القصّ · والتسمية `rtl` محاذاةً ويمينًا.
- **وأُسقط كل حارس وحده، وما سقط بأسمائه:** خيارٌ مُسبَق ← **اختبار 186 نفسه** · السبب يظهر دائمًا ← **اثنان** · حجبُ الحفظ ← **واحد** · إرسال السبب دائمًا ← **اثنان**. ثم أُعيد كلٌّ فاخضرّت الخمسة والعشرون. **و٢١٢ اختبار موبايل خضراء.**

**والقاعدة المستخلَصة:**

> **القيمة الافتراضية في حقلٍ اختياريّ هي الافتراضُ في القاعدة بلبوسٍ آخر** — **وإسقاطُها من الطبقتين معًا هو ما يجعل الاختيار اختيارًا.**
> **ومكوّنٌ لا يمثّل «لم يُختر بعد» لا يصلح لسؤالٍ جوابُه قد يكون «لم يُختر بعد»** — **وتمثيلُها بقيمةٍ هو العطب لا حلّه.**

**227. الاستلام من مورّد — أولُ كتابة في الدفتر، وقفلٌ يُنفَّذ ولا يُثبَت بعد**

**المرحلة 3، أول مسار مخزون:** `POST /api/inventory/warehouse-receipt` وحده. **ولم يُبنَ تحويلٌ ولا جردٌ ولا هالك ولا مُهِّد لها.**

**والثلاثة الباقية في §7-ب البند 32 لا يخصّها هذا المسار — وتحقّقتُ فوافقتُ:** حدّ فئات مخزن العنبر · «أمين حفظ لا آمر صرف» · منع صرف المنتهي — **ثلاثتها في الصرف لا الاستلام**، ومحلّها دفعة التحويل.

**١. الفرض المركزي — تحقّقتُ ولم أُضف فحصًا.**
`enforceEntityAccess` **مركَّب على `/api` كلّه لا على أنماط الروابط وحدها** (مقيس في `app.ts`): التركيب الأول يمسح **`query` و`body`**، والتركيب بالأنماط للروابط. **و`warehouseId` يصل في الجسم فيُمسَح** — **فلا فحص في الموجّه** (المبدأ الأول). **وفاحص 218 لم يعترض: المسار بلا `:param`.**

**٢. من يستلم — §12.2 وحدها، وقائمة موجبة.**
`RECEIPT_CATEGORIES_BY_ROLE` في `@dawajin/shared` تنقل الصفّ حرفيًّا: **مشرف علف+مستلزمات · طبيب دواء+لقاح · مالك الكل · مربٍّ لا شيء**. **ودورٌ لا يُذكر لا يستلم** (القرار 184).

**وقراءتان تُسجَّلان لأن سكوت المصفوفة قد يُقرأ سهوًا:**
- **«فيتامين» و«معقمات ومطهرات» لا يبلغهما إلا المالك** — **ليستا في صفّ المشرف ولا الطبيب**، وصفّ المالك «الكل» يشملهما. **قراءةٌ للمصفوفة لا حكمٌ عليها** — **فإن أُريد غيرُها فهو قرار مالك**. (والفئات ستٌّ فعلًا: «معقمات ومطهرات» بُنيت.)
- **🔴 و`storekeeper` مذكورٌ في المصفوفة «✅ المركزي» ولا يبلغ هذا المسار اليوم** — **ليس في `ASSIGNMENT_SCOPED_ROLES` ولا في `FULL_VISIBILITY_ROLES`، فالفرض المركزي يرفضه بـ403 قبل أي مسار** (القرار 194: «من ليس في قائمة معلومة لا يمرّ»). **حدٌّ معلن ومُثبَت باختبار** — **ولم يُبنَ الدور هنا**: إدخالُه قائمةَ رؤيةٍ قرارُ نموذج أدوار لا شقُّ استلام.

**٣. ماذا يُكتب في الصفّ.**
`movement_type = "استلام"` **وكميةٌ موجبة** (§13.2: «استلام (+) مخزن»)، **والوحدة تُطابَق بوحدة الصنف** — **وحدتان لصنفٍ واحد تجعلان الرصيد جمعَ أكياسٍ ولترات، وهو رقمٌ بلا معنى يمرّ صامتًا**.

**والثلاثة الملتقَطة اختيارية، ولم أخترع لها إلزامًا:** #157 البند ٤ يقول إنها **تُلتقط** عند استلام الأدوية — **ووصفُ الالتقاط ليس إيجابًا**. **وإلزامها يمنع استلام علفٍ لا صلاحية له، وإلزامها على الأدوية وحدها حكمٌ لم يصدر** — فيبقى قرار مالك.

**و`source_type = "warehouse_receipt"` و`source_uuid` مولَّدٌ لهذه الواقعة:** **سائرُ الحركات تشير إلى مستندٍ له `uuid`** (شحنة · جرد · أمر صرف)، **والاستلام وحده بلا مستند** — **ولا كيان «إيصال استلام» في المخطط**، فالحركة نفسها هي الواقعة.

**🔴 وحدٌّ معلن يُسمّى ولا يُتجاوز: الحركة لا تحمل مورّدها.** **لا عمود `supplier_id` في `inventory_movements` ولا `uuid` في `suppliers`** (مقيسٌ في المخطط). **فالاستلام يُسجَّل ولا يُنسب**، **ومتابعةُ أداء المورّد التي بُني لها الكيان** (القرار 202، و#161 «تاسعًا») **لا تقوم على هذا الدفتر اليوم**. **عمودٌ في دفعةٍ تالية لا اختراعٌ هنا** — وهو أوّل ما يُنظر فيه مع دفعة التحويل.

**٤. القفل — نُفِّذ كما اشترط 223، وأقول بصدق إنه لا يُثبَت في هذه الدفعة.**
**نُفِّذ الشرط: قفلُ صفّ المخزن أولًا، ثم تُقرأ الحرّاس تحته، ثم يُكتب** — **وهذا أول مسار يرثه**.

**وإسقاطُه لا يُسقط اختبارًا واحدًا — مقيسٌ لا مُدَّعى.** والعلّة مفهومة لا مجهولة:
- **الاستلام إلحاقٌ محض لا يقرأ ثم يكتب** — **ومجموعُ إلحاقين صحيحٌ بأي ترتيب**، فلا يحتاج تسلسلًا لصحّة نفسه.
- **وبرهانا التسلسل الحتميّان يبقيان خضراوين بلا القفل** — **لأن المفتاح الأجنبي `inventory_movements_warehouse_id_tenant_fk` يأخذ `FOR KEY SHARE` على صفّ المخزن عند الإدراج، وهو يتعارض مع `FOR UPDATE` الحائز**. **فالذي يُثبتانه تسلسلٌ توفّره القاعدة لا قفلُنا** — **وصُحّحت دعواهما في الاختبار نفسه بدل أن تُترك موهِمة**.
- **وما يحرسه القفل فعلًا هو دقّةُ قطع اللقطة** (223): **كاتبٌ بمعرّفٍ أدنى لم يلتزم بينما أعلى منه التزم، فلقطةٌ تُكتب عندها تقطع على الأعلى وتفقد الأدنى أبدًا**. **ولا كاتبَ لقطةٍ في أي مسار بعد** — `writeBalanceSnapshot` بلا مستدعٍ إنتاجيّ. **فالشرط يُنفَّذ ولا يُثبت إلا يوم يُبنى مستهلكه.**

**وهذا من صنف ما سجّلته في 223 عن `DESC`:** **حارسٌ صحيحٌ لا يُسقط اختبارًا اليوم يُكتب حدُّه ولا يُصطنع له إسقاط**.

**٥. الثوابت المحفوظة:** **لا عمود رصيد** (مُثبَتٌ بمسح المخطط) · **والقراءة بـ`computeBalance` وحدها** — **وتُستدعى تحت نفس المعاملة والقفل**، فالرقم المُعاد هو ما يراه القارئ بعدها لا تقديرٌ له · **وثابت §13.3 مُثبَتٌ بعد استلامين على مخزنين**.

**وحارسٌ أُضيف يقابل حارس 224 من جهته:** **مخزنٌ معطَّل لا يُستلم فيه** — **ذاك يمنع تعطيل مخزنٍ فيه رصيد، وهذا يمنع إدخال رصيدٍ في مخزنٍ معطَّل** — **ولولاهما معًا لدخلت بضاعةٌ مخزنًا لا واجهة تعرضه**.

**٦. الإثبات — ٢٣ اختبارًا:**
الرصيد يزيد بالمقدار مقروءًا بـ`computeBalance` · والحركة موجبة بنوعها ومصدرها · والملتقَط يُحفظ وبلا التقاطٍ يبقى عدمًا · **والمخالفات بأسمائها:** كميةٌ غير موجبة · وحدةٌ لا تطابق · مخزنٌ معطَّل · صنفٌ معطَّل · مخزنُ مستأجرٍ آخر ← **404 لا 403** · مربٍّ ← 403 · **ومشرفٌ يستلم علفًا ولا يستلم دواءً** · **وطبيبٌ عكسه** · **وفيتامينٌ للمالك وحده** · **وأمينُ مخزنٍ يرفضه الفرض المركزي**.
**وأُسقط كل حارس وحده وما سقط بأسمائه:** الفئة ← **ثلاثة** · الوحدة ← **واحد** · المخزن المعطَّل ← **واحد** · **والقفل ← لا شيء، وعلّتُه أعلاه**.

**والقاعدة المستخلَصة:**

> **اختبارٌ يخضرّ بعد إسقاط الحارس لا يُثبت الحارس بل يُثبت غيره** — **فيُقاس ما الذي أخضره، ويُصحَّح ادّعاؤه، ولا يُصطنع له إسقاط.**
> **وأولُ كاتبٍ في دفترٍ يرث شروطه كلّها، وإن لم يحتج بعضَها لنفسه** — **فالشرط يُنفَّذ لمن يأتي، ويُكتب أنه لم يُثبَت بعد.**

**228. التحويل — الأمر والخروج، و«في الطريق» حالةٌ لا مكان**

**المرحلة 3، ثاني مسار مخزون:** إصدار أمر التحويل وتنفيذ الخروج. **ولا تأكيد ولا استلام** — دفعةٌ ثانية.

**والحقائق الثلاث المطروحة قِستُها فوافقت:** الجدول بلا حالة ولا كمية مستلمة **فالدفعة تحتاج ترحيلًا** · وتعارضُ #159 «سابعًا/١» أصلحه 199 **فلم أطارده** · وعدُّ #159 «خامسًا» ناقصٌ ثلاثة: **الكمية المستلمة** (دفعة التأكيد) **و«من صرف» و«وقت الخروج»** (أُضيفا الآن). **والمزرعتان مشتقّتان من المخزنين فلا عمود لهما.**

**١. أين تكون الكمية بين الخروج والتأكيد — الشكل (أ): حركةُ خروجٍ وحالةٌ صريحة.**

**الخروج يكتب «تحويل صادر» سالبةً من المرسِل، والأمر يصير «في الطريق»** — **وهي حالةٌ مقروءة لا مستنتَجة**: عمود `status` **و`GET /api/inventory/in-transit` يسألها بالاسم**. **فليست «لا مكان»** — شرط #159 «ثالثًا».

**وما رُفض — المخزن الوسيط (ب) — بثلاث علل:**
- **يلزمه مستوًى رابع في `warehouse_level`، والمستويات الثلاثة أماكن** (مركزي · موقع · عنبر، **ولا مستوى «مزرعة» بحكم #161**) — **فيُدخِل في جدول الأماكن صفًّا ليس مكانًا**.
- **و«صاحب المخزن يُشتق من مستواه»** (#161 «ثانيًا») — **فلا صاحب لمخزنٍ لا موضع له**، و`assertWarehouseAccess` يقف أمام مخزنٍ لا يبلغه إسنادُ أحد.
- **وأثقلُها: العجز عند التأكيد.** **#159 «رابعًا» يجعل الفرق «عجزًا ظاهرًا»** — **وفي المخزن الوسيط يبقى رصيدًا عالقًا فيه أبدًا** يحتاج حركة تسوية ثالثة، **وفي المختار فرقُ رقمين على صفٍّ واحد**.

**وأثرُ المختار على الثوابت مقيس:** **§13.3 يبقى صحيحًا بالبناء** (طرفه الأيسر مجموع الأيمن) — **والذي يتغيّر معناه**: الكمية في الطريق **ليست في مخزن ولا في الدفتر**، **فالمملوك ماديًّا = Σ الحركات + Σ ما في الطريق** — **ثابتٌ ثانٍ بمحور آخر**، كما في جدول §13.3 نفسه (الأكياس الفارغة والعهدة). **وأُثبت الاثنان.** **و`computeBalance` لم تتغيّر بحرف، ولقطة 223 كذلك** — لا مخزن جديد يُلقَط.

**٢. الحالة عمودٌ بقيدٍ يطابقها بما وقع** — `صادر` تلزمها ألّا يكون خروجٌ ولا تأكيد، و`في الطريق` تلزمها خروجٌ بلا تأكيد، و`مستلم` تلزمهما. **فلا حالةٌ تدّعي فعلًا لم يقع.** **وبلا افتراضيّ عمدًا** (درس 222)، **وتعبئةُ الصفوف القائمة صريحةٌ في الترحيل** (نمط 205): **٣٤٣ صفًّا بلا خروجٍ مسجَّل فكلّها «صادر»**.

**٣. الحرّاس الثلاثة من §7-ب البند 32 — اثنان بُنيا وواحد وقفتُ عنده.**

- **✅ «أمين حفظ لا آمر صرف»** (#161 «ثالث عشر» ٢): **`storekeeper` لا يُصدر أمرًا** — والمشرف وحده يبدأ (#159 «ثانيًا»). **وتعارضٌ يُسمّى ولا يُطوى:** §12.2 صفّ «تحويل» يخوّل **المالك**، **و#159 يجعل المشرف وحده** — **وهو التعارض الثاني في #159 «سابعًا» المسجَّل منذ يومه**. **والمتّبع حكمُ #159**: أخصُّ الحكمين وأحدثهما، **والصفّ العامّ كُتب قبل أن يُفصَّل التحويل**. **وتوحيدُهما قرار مالك.**
- **✅ منع صرف المنتهي** — **تقريبٌ لا يقين، والحدّ مكتوب لا مُدَّعى**: الصلاحية على حركة الاستلام (198) **والرصيد بركةٌ واحدة لا عبوات**، **فلا يُعرف أيُّ جزءٍ جاء بأي عبوة**. **فالمنع على الحالة القاطعة وحدها: كلُّ استلامٍ حاملٍ صلاحيةً منتهٍ** — **وصالحٌ واحد بينها يمرّ**، **ولا يُدَّعى كشفُ الخلط بلا عبوة أو FEFO**.
- **🔴 حدّ فئات مخزن العنبر — وقفتُ: المحتوى غير مقرَّر.** **#161 «ثالث عشر» ٧ يقرّر الآليّة** («بالفئة لا بكتلة «مستلزمات»» — **وجعل الفئات ستًّا لتصير قابلة للفرض**) **ولا يسمّي الفئات المسموحة**. **والإشارة الوحيدة في نصّ التعارض الثامن: «لا معدات ولا مستلزمات إنشائية»** — **ووصفٌ بالنفي لا يُنتج قائمة موجبة**، **والقرار 184 يمنع منحها بالسكوت**. **فاللائحة قرار مالك، ولم أخترعها.**

**٤. القفل — وهنا يحمل وزنًا خلافًا للقرار 227، وأُثبت بإسقاطه.**
**الخروج يقرأ ثم يكتب**: يقرأ الرصيد للكفاية ثم يخصم. **وبإسقاط `.for("update")` وحده مرّ خروجان متزامنان على رصيدٍ يكفي واحدًا (200 و200) وصار الرصيد سالبًا** — **وهو البرهان الذي عجز عنه الاستلام لأنه إلحاقٌ محض**. **فما كُتب شرطًا في 223 ونُفِّذ بلا شاهد في 227 صار له شاهد اليوم.**

**٥. وإسقاطان لم يُسقطا شيئًا — يُقالان لا يُخفيان.**
**حارس «المشرف وحده يُصدر» في الخدمة** و**حارس إسناد المزرعتين** — **إسقاط كلٍّ منهما لم يُسقط اختبارًا**، **لأن `requireRole("supervisor")` على الموجّه و`enforceEntityAccess` على الجسم يمنعان قبلهما**. **وهما شبكةٌ ثانية لمن يستدعي الخدمة مباشرةً لا حارسان ميّتان** — **والترتيب صحيح**: الفرض المركزي يمنع، وحكمُ #159 مكتوبٌ في موضعه. **ولم أصطنع لهما إسقاطًا** (نمط 223 و227).

**٦. الإثبات — ٢١ اختبارًا:** الأمر لا يمسّ الدفتر · والخروج يخصم من المرسِل ولا يدخل المستلم · **وما في الطريق مقروء** · والحركة «تحويل صادر» سالبة تشير إلى مستندها بـ`uuid` **بخلاف الاستلام** (227) · **والمخالفات بأسمائها:** مزرعةٌ غير مُسندة · مشرفٌ بمزرعةٍ واحدة · طرفان متطابقان · أمين مخزنٍ ومربٍّ ومالكٌ يُصدرون · خروجٌ مرتين · رصيدٌ غير كافٍ · مخزنٌ معطَّل · وكلُّ المخزون منتهٍ · **والثابتان معًا**.

**وأثرٌ جانبيّ نافع:** تجهيزةُ الاختبار حاولت تعطيل مخزنٍ فيه رصيد **فمنعها حارس القرار 224** — **فاحترمَته بدل أن تلتفّ عليه**.

**والقاعدة المستخلَصة:**

> **«في الطريق» حالةُ أمرٍ لا موضعُ بضاعة** — **وجعلُها مخزنًا يُدخِل في جدول الأماكن ما ليس مكانًا، ويحوّل العجز من فرقِ رقمين إلى رصيدٍ عالقٍ أبدًا.**
> **والقفلُ يُثبَت حيث تُقرأ الحالة ثم تُكتب، لا حيث يُلحَق** — **فالشرط الواحد يحمل وزنًا في مسار ولا يحمله في آخر، ويُقال ذلك في الموضعين.**
