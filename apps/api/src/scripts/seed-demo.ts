/**
 * بيانات تجريبية — عبر الـ API حصريًا، بحارس بيئة، ممنوع الإدراج المباشر في
 * قاعدة البيانات (decisions.md #27). كل بيانات تُنشأ هنا يجب أن تمر بنفس
 * مسارات الـ API وصلاحيات الدور التي يستخدمها مستخدم حقيقي — هذا ما كشف
 * تسعة أخطاء في تطوير سابق كان الإدراج المباشر يخفيها.
 *
 * لا شيء بعد: لا مسارات /auth أو /users مبنية بعد (المرحلة 1). يُستكمل هذا
 * السكربت فور توفر POST /auth/login ومسار إنشاء أول مستأجر ومالك.
 */

const env = process.env.NODE_ENV ?? "development";
if (env === "production") {
  console.error("[seed:demo] رفض التشغيل: ممنوع توليد بيانات تجريبية في production");
  process.exit(1);
}

console.log(
  "[seed:demo] لا مسارات API جاهزة بعد للبذر عبرها (المرحلة 1). " +
    "سيُستكمل هذا السكربت عند بناء /auth/login وإنشاء أول مستأجر."
);
